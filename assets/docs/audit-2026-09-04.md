# Framework Audit Report — 2026-09-04

**Framework version:** v2.17.0
**Run mode:** `verification (over the Run 7 batch — s743de6b, sfbe913c, s62ef23b, s11ce6e9, s17e2217, s8c82908, sd82718a)`
**Dimensions checked:** 17 + a Part 1 fix-verification pass
**Agents dispatched:** 6

**carried: 0 open + 0 escalated from Run 7** — Run 7 closed at `APPLIED — 36 of 36`.
**Accepted-risk OBSERVATIONS (re-measured, never re-opened):**
- **E-1** — 0 working-tree occurrences, 0 commit-message hits. Closed.
- **M-44** — no contradicting evidence found this run. Closed.
- **N-53** — the withheld-reference pointer stands; the batch introduced no new identifier-spelling pattern. Closed. Triage note: N-53 re-opens on a NEW working-tree occurrence; the isolation hits below all predate `9ab0c06` and are therefore the same fix-forward disposition, not a re-open.

## Part 1 — Verification ledger for the Run 7 batch

**Score, counted over the FINDINGS and recomputed from the ledger: of 36 findings, all 36 were
verdicted. 15 clean, 21 not clean** — 11 partially-fixed, 7 fixed-but-class-not-swept,
6 introduced-a-defect (verdicts combine), and **1 declared applied that was never made**.

**58% not clean — the highest rate in the seven-run series** (27%, 54%, 37%, 47%, 53%, 47%, **58%**),
on the batch that carried the most controls. This is the central datum of the run.

| ID | Verdict | Evidence |
|----|---------|------|
| P-1 | FIXED-BUT-CLASS-NOT-SWEPT | Loop widened 14→16 and executes correctly (Run 7 receipts 16/16; Run 6 sections 14/16). But **17 keys are mandated and 18 are emitted** — `placeholder:` was created by P-31 in the same batch and never added, violating the "ADD ITS KEY HERE IN THE SAME EDIT" rule P-1 itself installed → `Q-9`, `Q-10` |
| P-2 | CONFIRMED-FIXED | Seven hashes, parcels sum to 54; the stale open-count is gone |
| P-3 | FIXED-BUT-CLASS-NOT-SWEPT | Five forms named; one citation repaired by name. A second stale ordinal survives, and the receipt's "7 live citations" understates a disk count of 15 → `Q-11` |
| P-4 | FIXED-BUT-CLASS-NOT-SWEPT | Item 6 authorises the consolidated form, but **nothing downstream was updated**: Gate 2's command reaches 1 commit of 6, Gate 3 has no batch form, item 4's table template still has no `Commit` column, and the H2-heading rule now contradicts the form it authorises → `Q-12`, `Q-13` |
| P-5 | CONFIRMED-FIXED | Three line-anchored keys; the file's own check returns 1/1/1 |
| P-6 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | Slots added, but the `Loop marker` slot **cannot express the values its step emits** — P-16's exact defect, in the commit that installed the rule banning it. Neither mandating step names its destination; a third mandate has no slot → `Q-14` |
| P-7 | CONFIRMED-FIXED | Errata 6 numbered; collision grep sees it; 0 non-conforming blocks in 4 files |
| P-8 | FIXED-BUT-CLASS-NOT-SWEPT | Named line fixed in both twins, but **two lines above the fix** both twins still instruct reading only the two reviewers, while their own table attributes a gap to the validator → `Q-1` |
| P-9 | CONFIRMED-FIXED | Three declarers in the loop; failure string corrected; 0 framework-layer residue |
| P-10 | CONFIRMED-FIXED | Regex executed against a synthetic project: **10 of 10** resolve, zero false BROKEN. The 3→10 claim is verified |
| P-11 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | Slot present; M derived by command and returns 3 correctly. But the wrong-directory guard added beside it is **inverted** — the real wrong-directory output is `0`, the GREEN value → `Q-2` |
| P-12 | FIXED-BUT-CLASS-NOT-SWEPT | 1 of 3 stated divergences landed. Agent 3's block still lacks the entire executable-check verification; **Agent 4's block was never touched** and its CENTRAL MANDATE lives only in the prompt → `Q-3`, `Q-15` |
| P-13 | CONFIRMED-FIXED | All 7 sites three-declarer; repo-wide re-run 0 framework hits |
| P-14 | CONFIRMED-FIXED | Two passes over three declarers, consistent with the orchestrator |
| P-15 | FIXED-BUT-CLASS-NOT-SWEPT | The clause was rewritten as equivalence; **the twin still asserts "identical — verified, not assumed"**, and the two diverged further in this very commit → `Q-16` |
| P-16 | CONFIRMED-FIXED | Exactly the two verdicts its step can emit |
| P-17 | CONFIRMED-FIXED | Mandate → slot, exact match; twin retains its own |
| P-18 | CONFIRMED-FIXED | One section; the deleted one's unique content folded into the survivor |
| P-19 | PARTIALLY-FIXED | The `project.md` list is now correct (6 of 6 verified). The `pendencias.md` list is **wrong for the third time** — names a step that never touches the file, omits two that do — and the file now instructs deriving it → `Q-5` |
| P-20 | CONFIRMED-FIXED — INTRODUCED-A-DEFECT | EPA added to the INPUT list, but the insert renumbered nothing: **duplicate ordinal**, list ends at 9 where it should end at 10 → `Q-4` |
| P-21 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT ×3 | A mandate and a slot both name **a step that does not exist**; the Hooks slot offers a verdict its step cannot emit; and **second homes** were created for two verdicts whose real slots already existed at the very lines the finding cited → `Q-6`, `Q-7`, `Q-8` |
| P-22 | CONFIRMED-FIXED | Errata 2's figures reproduce exactly (29/12/8) |
| P-23 | CONFIRMED-FIXED | Quoted scalar naming the concept; parses; class swept across all 12 lineage fields |
| P-24 | CONFIRMED-FIXED | New clause verified line by line against the three gating lines it describes |
| P-25 | **NOT FIXED — declared applied, never made** | The three-identical-token clause and the ungrammatical fragment are **untouched**. `git log -L` shows the last edit is Run 6's; the applying commit has no hunk within 4 lines of the site. The receipt asserts the sentence was "read in full before and after" → `Q-17` |
| P-26 | PARTIALLY-FIXED | Two surfaces went five → **six**; the truth is **seven** — Run 6 was itself a verification run and its figure appears nowhere. A **third** surface carries the same stale series. The published figure is also off by one against its own ledger. And the new owner mandates "increment the run count", which **structurally cannot detect a skipped run** → `Q-18`, `Q-19`, `Q-20` |
| P-27 | CONFIRMED-FIXED | Sample rewritten; repo-wide 0; teaching value preserved |
| P-28 | CONFIRMED-FIXED | Replacements independently verified at **0** project files (the removed names: 128 and 114). One surface became more consistent than before |
| P-29 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | The redo left the ten rewritten sentences grammatical. But **11 sites survive** in the same file: two hand-rolled identifiers, a feature/spec block, four sites carrying the source project's exact session count (the *cardinal complement* of the ordinal removed in the same commit), and three absolute calendar coordinates → `Q-21` |
| P-30 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The hash dimension is clean and the derivation worked. But the substitutions **wrecked three sentences** in memory — a phrase inserted into a token slot, uncontracted articles, dropped diacritics — and **a branch name survives**. P-25's class inside P-30's own fix → `Q-22` |
| P-31 | FIXED-BUT-CLASS-NOT-SWEPT | Item and exemption written; the `placeholder:` key it created was never registered → `Q-9`. The inserting commit reports **no ordinal or inventory sweep at all**, while both sibling insert-commits do → `Q-23` |
| P-32 | PARTIALLY-FIXED | The decay rule is installed and was honoured. But the **backlog the finding named was never discharged** — six already-pushed commits still carry `push: not requested` with no D16 re-run; the rule got **no back-sweep row**; and the auto-read surface still carries the undecayed wording → `Q-24` |
| P-33 | PARTIALLY-FIXED | The rule authorises the multi-hash form and mandates the sum. **Its first use violates it**: the Run 7 status line sums to 37 against M=36, discharged by a prose exemption the enumeration does not admit; one ledger status is malformed and breaks Gate 3's own grep → `Q-25` |
| P-34 | PARTIALLY-FIXED | The mandate now demands three numbers and they are true. **The command still emits one** — M and S are prose constants. P-35's own diagnosis, filed in the same run, not applied to its sibling → `Q-26` |
| P-35 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The command works and its three calibration figures are **all independently confirmed**. But the receipt discharges it over **1 of the 7 commits** it declares as its denominator: the true batch figures are **51 added / 17 rewritten / 5 buried** against a claimed 14/5/**0** — and `B` is the finding. Separately, the `B` regex fires on the framework's own mandated idiom → `Q-27`, `Q-28` |
| P-36 | CONFIRMED-FIXED — CLASS-NOT-SWEPT | The exclusion is correctly stated and **masks nothing** (tested with and without). But it names one propagation path of several, and its sibling rule installed one finding later carries P-36's own defect: no UUID exclusion, 12 false positives on today's memory → `Q-29`, `Q-30` |

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 1 runtime twin, byte-identical (md5 verified) |
| C | Instruction style | Quality | 5 | FAIL | The batch's own receipt measured 1 commit of 7; 5 buried lines shipped |
| E | Version consistency | Currency | 5 | PASS | v2.17.0 on 5 sites / 3 surfaces; 19 lineage fields, 0 rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15; the `invocation:` split 7/8 exact |
| D5 | Bootstrap file refs | References | 2 | PASS | 27 framework paths resolve; 8/8 fence extractions non-empty |
| D6 | Gap vocabulary | References | 3 | FAIL | Tables byte-identical; the surrounding PROSE diverges in six places |
| D7 | Pushy Description | Quality | 3 | PASS (compliance) / FAIL (its proof) | Invariant holds 10/10; the guard certifying it is inverted |
| D8 | Orchestrator refs | Process | 4 | PASS | 26 cross-section citations, 0 RED |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | Every path and every count verified |
| D10 | Command → skill paths | References | 4 | FAIL | Paths clean; three agent blocks diverge from what is dispatched |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | Bootstrap clean; EPA gained a forward reference to a nonexistent step |
| D12 | Examples conventions | Currency | 5 | PASS | 0 violations; one README truth-claim measures a unit it does not state |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | Nothing read that is not defined |
| D14 | Bootstrap output | Process | 2 | FAIL | Three mandated verdicts have slots that do not match their steps |
| D15 | Doc factual accuracy | Currency | 5 | FAIL | The defect series is wrong on three surfaces; three named files do not exist |
| D16 | Project-info isolation | Privacy | 1 | **FAIL** | 11 sites in one lineage doc + 1 in agent memory; all pushed or local, none new |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | **The recorded prediction is falsified.** Two undocumented flows |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| Q-1 | HIGH | `bootstrap.md:579`, `EPA:887` | Both twins instruct reading only the two reviewers, never the third declarer, while their own table three lines below attributes a gap to it. The step that installs the specialist for the link that has broken twice never opens the file declaring it. P-8's class, two lines above P-8's own fix. | applied `s86ed15e` |
| Q-2 | HIGH | `audit.md:518-520` | The D7.5 wrong-directory guard is **inverted**: the real wrong-directory output is `0` — the literal GREEN value defined one line above — not empty. The guard fires on an impossible state and is blind to the real one. `component-design §9` rule 4 violated by the commit that wrote the guard. | applied `s2106fde` |
| Q-3 | HIGH | `audit.md` Agent 3 and Agent 4 blocks | Ten checks and four report slots this run executed exist **only in the dispatched prompt**, including the entire verification of the framework's sole executable activation-chain check and Agent 4's CENTRAL MANDATE. P-12 claimed to have moved this into the file and moved one item of three. | applied `s49ad368` |
| Q-4 | MEDIUM | `audit.md:293-294`, `:394`/`:398` | Two duplicate ordinals introduced by two Run 7 inserts; both lists end one short. The commit reporting "ordinal sweep: RUN" swept Phase 3 and not the two file lists it renumbered in the same diff. | applied `s49ad368` |
| Q-5 | MEDIUM | `EPA:354` | The `pendencias.md` consumer list is wrong for the **third** consecutive time: names a step that never touches the file, omits two that do, and is asymmetric with its own correct sibling. The file now says to DERIVE it; the derivation was not run. | applied `s02a3d69` |
| Q-6 | HIGH | `EPA:225`, `EPA:1071` | A mandate and its report slot both name **`Step 4.8`, which does not exist**. The obligation has one real owner and one phantom, and the mandate sits in a phase preamble rather than inside the step that executes it. | applied `s5c88b5d` |
| Q-7 | HIGH | `bootstrap.md:761-763` vs `:663-667` | The Hooks slot enumerates three verdicts and asserts they are all its step can produce; the step defines two and mandates no report at all. P-16's inverted form, reintroduced by the batch that fixed P-16. | applied `s5c88b5d` |
| Q-8 | HIGH | `bootstrap.md:756-759`+`:816-817`, `:761-763`+`:813-814` | Second homes were created for the MCP and Hooks verdicts instead of completing the slots **at the exact lines the finding cited**, which existed all along. Two homes, divergent enumerations — P-18's class, in the batch that fixed P-18. The commit message's claim that no slot existed is false. | applied `s5c88b5d` |
| Q-9 | MEDIUM | `maintenance.md:667` vs `:129-133` | `placeholder:` is a mandated report key with no entry in the self-check loop — created and omitted **in the same batch**, violating the rule the same batch installed. Gate 4 stage 2 returns RED on it and the RED was never reported. | applied `s2106fde` |
| Q-10 | MEDIUM | `maintenance.md:140` | The staleness cross-check installed to prevent exactly Q-9 is **vacuous**: `[^—]*` cannot cross the em-dash preceding almost every key, so it harvests 1 of 17. It can never fail, and did not. Never negation-proved. | applied `s2106fde` |
| Q-11 | LOW | `audit-2026-09-02.md:328` | A second stale `Phase 3 item 6` citation survives; the receipt's "7 live citations checked" understates a disk count of 15. | applied `s49ad368` |
| Q-12 | HIGH | `maintenance.md:327-348`, `:291-293` | P-4 authorised a consolidated receipts form and updated nothing downstream: **Gate 2's command can reach 1 commit of 6** yet the receipt claims 36/36; Gate 3 has no batch form; item 4's table template still lacks the `Commit` column item 6 now mandates. | applied `s49ad368` |
| Q-13 | MEDIUM | `maintenance.md:122-123` | The rule "the heading is `## Post-change checklist receipts (sHASH)` — exactly that string" contradicts the consolidated form authorised in the same file. Only 2 of 6 headings on disk match. | applied `s49ad368` |
| Q-14 | HIGH | `sprint-proposer/SKILL.md:163` vs `:75-76` | The `Loop marker` slot offers `present/absent`; its step emits `none` / `active → handing off…`. The slot cannot express its step's values — P-16's defect committed by Gate 4's own first execution, in the batch that banned it. A third mandate in the same step has no slot. | applied `s5c88b5d` |
| Q-15 | MEDIUM | `audit.md:587` vs `:631` | Inside Agent 4's own block, D8.7's CHECK mandates a verdict column the REPORT FORMAT 44 lines below does not carry. The descending class inside the dimension that hunts it. | applied `s5c88b5d` |
| Q-16 | MEDIUM | `code_reviewer.md:257-258` | The twin still asserts the two clauses are "identical — verified, not assumed" after P-15 rewrote one of them as merely equivalent. Doubly false, and the receipt's PARALLEL cell claims the twin was checked. | applied `s02a3d69` |
| Q-17 | HIGH | `framework-base-upgrade.md:110-111` | **P-25 was declared applied and never made.** The defect is untouched; the applying commit has no hunk at the site. Root cause identified: the fix WAS applied, then undone by a `git checkout --` revert of an unrelated over-correction in the same file, and the redo covered only the other finding's sites. **Nothing reconciles what a partial revert undoes against what is re-applied.** | applied `s859e6e9` |
| Q-18 | HIGH | `CLAUDE.md:145-150`, `audit.md:37-40`, `maintenance.md:670-671` | The defect series says six runs; there have been **seven**. Run 6's figure (23 of 51) is absent from all three surfaces, and the third surface was not updated at all. | applied `s02a3d69` |
| Q-19 | MEDIUM | `audit.md:1044-1050` | The new owner mandates "append this run's figure and **increment** the run count" — an increment **structurally cannot detect a skipped run**, so the fix installs the mechanism that preserves the error it was created to prevent. It also hard-codes the denominator as 2 surfaces; there are 3. | applied `s02a3d69` |
| Q-20 | MEDIUM | `CLAUDE.md:148`, `audit.md:39` | The published Run 7 figure is `21 of 45`; recomputed from that run's own ledger it is **22 of 45**, and the prose breakdown "9 partially-fixed" is 10. The "recompute from the ledger, never copy the summary prose" rule failing on the run that created its owner. | applied `s02a3d69` |
| Q-21 | MEDIUM | `framework-base-upgrade.md` ×11 | P-29's class not swept: two hand-rolled identifiers, a feature/spec block, **four sites carrying the source project's exact session count** — the cardinal complement of the ordinal removed in the same commit, misclassified as a portable duration — and three absolute calendar coordinates. | applied `s859e6e9` |
| Q-22 | MEDIUM | agent memory, 4 sites | P-30's substitutions wrecked three sentences (a phrase inserted where a token belonged, uncontracted articles, dropped diacritics) and left a source-project branch name. P-25's class inside P-30's fix. Not in git; locally fixable. | applied `s859e6e9` |
| Q-23 | LOW | `8c82908`; `audit-2026-08-31.md:57`,`:99` | The commit that inserted a numbered item carries **no inventory- or ordinal-sweep receipt at all**, unlike both sibling insert-commits. An independent sweep finds a live stale ordinal in the directory the rule explicitly mandates sweeping. | applied `s02a3d69` |
| Q-24 | MEDIUM | `maintenance.md:27-36`; `CLAUDE.md:103-110` | P-32's rule is installed and was honoured, but the backlog it named was never discharged — six pushed commits still carry `push: not requested` with no D16 re-run — the newly promoted rule got **no back-sweep row**, and the auto-read surface still carries the undecayed wording. | applied `s02a3d69` |
| Q-25 | MEDIUM | `audit-2026-09-03.md:1006` | The multi-hash sum rule is violated by the first line written under it: parcels sum to 37 against M=36, discharged by a prose exemption the enumeration does not admit. One ledger status is malformed and breaks Gate 3's grep. | applied `s02a3d69` |
| Q-26 | MEDIUM | `maintenance.md:438-453` | P-34 widened the mandate to three numbers and left the command emitting one; M and S are prose constants. P-35's own diagnosis, filed in the same run, not applied to its sibling item. | applied `s02a3d69` |
| Q-27 | HIGH | `audit-2026-09-03.md:1030-1034` | The `instruction style:` receipt — the batch's headline, "the first discharge of this key not supplied from reading" — measures **1 of the 7 commits** it declares as its denominator. True batch figures: **51 added, 17 rewritten, 5 buried** against a claimed 14/5/**0**. `B` is the finding, and five buried lines shipped. | applied `s02a3d69` |
| Q-28 | LOW | `maintenance.md:226-231` | The `B` regex fires on the framework's own mandated idiom (`…. NEVER emit nothing.`), scoring 7 of 16 imperative lines in one shipped skill. The "healthy state" it was calibrated against is not healthy by its own rule. | applied `s02a3d69` |
| Q-29 | MEDIUM | `audit.md:225-231` | The `git cat-file -e` hash rule works for commit hashes but, applied literally as written, produces **12 false positives** on UUID fragments the harness writes. No UUID or URL exclusion — P-36's exact defect, filed one finding earlier in the same run and not applied to its sibling. | applied `s859e6e9` |
| Q-30 | LOW | `audit.md:184-195` | P-36's exclusion names `assets/examples/` alone; `projects/*/.claude/skills/` is an equally verbatim framework→project copy target and is not excluded. | applied `s859e6e9` |
| Q-31 | MEDIUM | `EPA:994` | The repaired regex silently misses `declares the X gap`, a capitalised `Declares`, and any line-wrapped folded description; it **false-BREAKS** a hyphenated domain and **false-PASSES** `declares a coverage gap for X` — both reproduced by execution. It passes today by phrasing luck. | applied `s86ed15e` |
| Q-32 | MEDIUM | `EPA:987` | The loop's skip list is a hard-coded nine-name set and is **already short by one**, warning on every internal-tool+ adaptation. Derivable from D7.5's own invariant; typed instead. The class P-11 was filed against, in the file P-11's fix declared swept. | applied `s86ed15e` |
| Q-33 | MEDIUM | `bootstrap.md:600-610` | Bootstrap has **no** executable activation-chain check; the mechanical net exists on the EPA path only, for a link that has broken on both. P-9's receipt recorded this absence as a clearance. | applied `s5c88b5d` |
| Q-34 | MEDIUM | `docs/modules/rules/…` / `bootstrap.md` vs `EPA` | The specialist-install PROSE diverges between the twins in **six** places — the SOURCES paragraph, the declarer file list, the remediation step, the pass criterion, the rationale, and one clause only EPA has. The tables are byte-identical; D6.7 diffs only the tables. | applied `s5c88b5d` |
| Q-35 | MEDIUM | `framework_doc:2050-2053` | The related-documents table names three files that **do not exist**; only the fourth row was ever updated. Pre-existing, survived seven audit runs. | applied `s02a3d69` |
| Q-36 | MEDIUM | `audit-2026-09-03.md` back-sweep table | `back-sweep: 20 artifacts of 20` against rows summing to 25, or 19 by distinct files. No reading of the table yields 20. | applied `s02a3d69` |
| Q-37 | MEDIUM | `audit-2026-09-03.md` receipts | `isolation: 6 files scanned` where the batch touched **20**, against item 3's "every touched file" — a worse ratio than the 13-of-15 shortfall the same report records as an erratum. `references: 12 of 12` against a 13-item enumeration and ~29 real targets, and item 3 still supplies no command for it. | applied `s02a3d69` |
| Q-38 | LOW | `audit-2026-09-03.md` class-sweep, P-30 row | The `File touched` cell names agent-memory files, which are not in git — so the gate's own self-check ("every cell appears in `git show --name-only`") is false for that row, and the finding itself says the files are not tracked. | applied `s02a3d69` |
| Q-39 | LOW | `audit.md:353`, `:365` | Gate 4's harvest regex silently drops keys containing `_`, `.` or `-` — verified: it does **not** match `done_tasks.md:` or `back-sweep:`. The commit message asserts it was "verified against the `done_tasks.md` slot"; it cannot see it. The gate is blind to the key class of the finding that created it. | applied `s2106fde` |
| Q-40 | LOW | `audit.md:781` | "Unlike **D1–D16**" — D1, D2 and D3 do not exist; the set is `A, C, E + D4–D17`. The "17 dimensions" claim itself is correct on all four surfaces. | applied `s49ad368` |
| Q-41 | LOW | `audit-2026-09-03.md` P-13 back-sweep row | The negation proof states the grep returns 5 files at the pre-fix commit; it returns 6. The stated exclusion lives in the prose, not in the command. | applied `s02a3d69` |
| Q-42 | LOW | `audit.md:442` | Inside D6.7, eleven lines below D6.5's "there are three, not two", the singular "*the reviewer* will declare it forever" survives. | applied `s5c88b5d` |
| Q-43 | LOW | `examples/README.md:170` | "6 of 11 examples carry a new-entity checklist" is true only under a unit the sentence does not state; under its stated unit it is 1 of 11. The other five are code-review criteria, not entity-creation flows. | applied `s02a3d69` |
| Q-44 | MEDIUM | `maintenance.md` Audit intake | **Correcting a false claim in an already-made commit message has no written home.** It was executed once this batch, deliberately as a follow-up commit rather than an amend, and nothing documents either the choice or the form. | applied `s02a3d69` |
| Q-45 | LOW | `maintenance.md` item 6; two commit subjects | Splitting a batch into named thematic groups is executed under a different naming scheme every run, and two group-commit subjects overstate their content (a "6 findings" commit carrying 5 IDs, a "9 findings" commit carrying 7) while reporting `row-count N vs N`. The identical class already has an erratum from a prior run. | applied `s02a3d69` |

## Recommended Fixes

**1. Quick fixes** — `Q-17` (apply the fix that was reverted and never re-applied), `Q-20` (recompute the published figure), `Q-40`, `Q-41`, `Q-42`, `Q-11`, `Q-43`.

**2. Structural fixes** — `Q-2` and `Q-10` first: both are checks that **cannot fail**, which makes every green they have ever reported meaningless. Then `Q-6`, `Q-7`, `Q-8`, `Q-14`, `Q-15` (slots that misrepresent their steps); `Q-1`, `Q-31`, `Q-32`, `Q-33`, `Q-34` (the activation chain, which is now the most-patched and least-converged area in the repo); `Q-3` (move the checks out of the prompt); `Q-4`, `Q-5`, `Q-9`, `Q-12`, `Q-13`, `Q-26`, `Q-29`, `Q-39`.

**3. Quality improvements** — `Q-18`/`Q-19` (the instrument that measures convergence is itself broken), `Q-27`/`Q-28`, `Q-36`, `Q-37`, `Q-38`, `Q-44`, `Q-45`, `Q-23`, `Q-24`, `Q-35`.

**4. ESCALATED — owner decision required**

`Q-21` is pushed privacy residue in one lineage document: two hand-rolled identifiers, a feature/spec block, four exact-session-count sites and three calendar coordinates, all reachable from `origin/main` and all predating the current work. `Q-22` is the same class in agent memory and is **not in git** — locally fixable with no history implication.

The standing instruction from `E-1`/`M-44` (fix forward, record, never rewrite history) covers the disposition. **What genuinely needs a decision is narrower and worth stating plainly:** four of these sites are the source project's exact session count, which the previous batch classified as a *portable relative duration* and deliberately preserved. The audit's position is that a project's exact total is the cardinal form of the ordinal coordinate removed beside it. Both readings are defensible; the framework's own rule does not settle it. **Deciding it also settles how many similar figures elsewhere are hits.**

**5. ACCEPTED-RISK — no action** — E-1, M-44, N-53, all re-measured and closed.

## Meta-observation

**The recorded prediction is falsified, and the falsification is clean.** Run 7 predicted that defects
would not be in the gated keys, would not scale with batch size, and would appear in exactly two
places — the descending direction of new mandates, and `instruction style:`. It then closed both:
Gate 4 was built, and the key was given a command and a denominator. **All three claims failed.**
The defects ARE in the gated keys (`row-count N vs N` reported true on two commits where it was
false; `slots 8/8` reported on a state carrying an unreported RED). The descending class produced
five more instances (`Q-6`, `Q-7`, `Q-14`, `Q-15`, `Q-9`), three of them in the commit that
installed the gate. And `instruction style:`, whose command works and whose three calibration
figures were independently confirmed, still shipped a false receipt — through **scope**, a term the
model does not contain: the command reads one staged diff, the receipt claims seven commits.

**The rate went UP.** 27%, 54%, 37%, 47%, 53%, 47%, **58%** — the worst of the series, on the batch
with the most controls. Seven runs, no trend. Batch size is exonerated a fifth time and should stop
being discussed.

**What the evidence supports instead: the defect lives at the seam where a newly written control
meets a shape the same batch invented.** Every high-severity finding here sits on such a seam —
P-35's command against P-4's consolidated section; Gate 4's command against Gate 4's own uncommanded
second half; P-33's sum rule against the line written under it; P-1's key list against P-31's new
key; P-36's exclusion against P-30's sibling rule; P-16's ban against P-6's slot. **Simultaneity,
not size.** A control and the shape it must read were authored in the same batch and never run
against each other.

**Two structural remedies the evidence names directly, in order of leverage.**

**(1) Extend the back-sweep from rules to CONTROLS.** The checklist already mandates that a newly
promoted *rule* be swept against every artifact it retroactively governs. Nothing does this for a
newly promoted *control*. When a batch installs or amends a check, gate or report key, every other
control in the checklist must be re-run against that batch's own artifacts before commit. Every
finding above would have been caught by it; nothing else caught any of them.

**(2) A check must be proved by NEGATION before it is trusted, and the proof must be a command.**
`component-design §9` rule 4 already says this. Three checks shipped this batch that **cannot
fail**: the D7.5 wrong-directory guard (`Q-2`), the key-list staleness grep (`Q-10`), and Gate 4's
harvest regex for a whole class of key names (`Q-39`). All three were written with a stated
expected result and none was run against a state that should make it red. A green from a check that
cannot go red is not evidence, and three of this run's controls have been emitting exactly that.

**A third observation, offered without a remedy because the evidence does not name one.** The single
finding of this run that no control could have caught is `Q-17`: a fix that WAS applied, then undone
by a `git checkout --` revert aimed at an unrelated over-correction in the same file, with the redo
covering only the other finding's sites. Nothing in this repo reconciles what a partial revert
undoes against what is re-applied. It is a real gap, it is narrow, and inventing a control for it
in the same batch that discovered it would be the exact mistake this report has spent forty findings
documenting.

**Prediction for the next batch, stated so it can be falsified.** If remedy (1) is applied, the
seam defects — a new control against a same-batch shape — should fall sharply, and the residual
defects should move to controls inherited from EARLIER batches meeting new shapes. If the rate does
not move, the seam model is wrong too, and the next hypothesis should be that the defect rate is a
property of authoring volume rather than of any control architecture, which would argue for smaller
batches after all.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; all 36 Run 7 findings verdicted.
**Application status:** `APPLIED — 45 of 45 (4 in `s2106fde`, 3 in `s86ed15e`, 8 in `s5c88b5d`, 6 in `s49ad368`, 5 in `s859e6e9`, 19 in `s02a3d69`)` — the six counts sum to 45.

> **Errata 1 (added by the Run 9 application, findings R-17, R-18, R-36, R-37, R-38, R-40,
> R-45).** Six figures and one bump claim in the receipts below do not reproduce. They share
> the cause Run 9 identified: **each was narrated rather than discharged as a command with its
> stdout**, and every key that WAS so discharged reproduced exactly.
> - `instruction style: … 2 buried` — the batch's own command returns **3**, and the prose
>   naming them is wrong for one (`R-17`).
> - The same section declares **eight** hashes and measures **six**; over the declared range the
>   figures are 39/9/6. The “state the range beside the numbers” rule installed in that same
>   batch was not honoured (`R-18`).
> - `back-sweep: 26 artifacts of 26` — the table's own rows sum to **22** (`R-36`).
> - `references: 14 of 14` counts the receipt's own enumeration, not the diff; the added text
>   names **≥42** targets (`R-37`).
> - `isolation: 3 files scanned` — the closing commit touched 2 and the batch touched 15,
>   against item 3's “every touched file” (`R-38`).
> - Three commit subjects overstate their finding counts, all three reporting `row-count N vs
>   N`; and the mandated group-naming scheme was not used (`R-40`, `R-42`).
> - Seven `docs(audit)` commits declaring `bump: none — write-back only` landed normative
>   content, one of them a new mandate and slot AFTER the version bump (`R-45`).
>
> **The pattern, which is the finding worth keeping:** every receipt key whose command was
> quoted and re-runnable reproduced exactly — 4 of 4, verified independently by three parties.
> Every key whose number was narrated failed — 7 of 7.

## Post-change checklist receipts (Run 8 batch — s2106fde · s86ed15e · s5c88b5d · s49ad368 · s859e6e9 · s02a3d69 · s92f40e8 · and this write-back)

Consolidated form, per Audit-intake item 6, with the `Commit` column that form requires.
45 findings, `Q-1` … `Q-45`, all applied. 0 open.

**A NOTE ON THE CLASS-SWEEP TABLE, because the alternative was a lie.** The four sweep directions
were run **per GROUP**, against the shape each group governs — not 45 times independently. A table
of 45 identical `✓ grep the shape` cells would have been the unfalsifiable receipt this checklist
bans, so each row cites the group sweep that covers it and each group's sweep quotes its real
command. Rows carrying a finding-specific sweep say so.

| Group | Commit | Findings | The sweep actually run (quoted) | Result |
|---|---|---|---|---|
| (a) seam remedy + broken checks | `2106fde` | Q-2, Q-9, Q-10, Q-39 | `grep -oE 'ALWAYS REPORT[^\`]*\`[A-Za-z][A-Za-z0-9 ._-]*:'` over `maintenance.md`, each key tested against the self-check loop; and the D7.5 proof run from two working directories | harvest 1 → 10 keys; **2 real REDs found and fixed** (`placeholder:`, `control back-sweep:`); guard: `files=10/out=0` vs `files=0/out=0` |
| 1 — activation chain | `86ed15e` | Q-1, Q-31, Q-32, Q-42 | the Step 5.1 loop EXTRACTED FROM THE FILE and run against a synthetic project built from all 10 protocol agents + all 20 specialists | **10 of 10 chains resolved, 0 BROKEN** (pre-fix: 3 of 10). One defect in my own fix found by that run and repaired |
| 2 — slots and twin prose | `5c88b5d` | Q-6, Q-7, Q-8, Q-14, Q-15, Q-33, Q-34 | every `ALWAYS REPORT` mandate in both twins traced to a slot, and the slot's enumeration compared **against its mandating step**; `diff` of the two twins' install prose | 4 slots corrected, 2 second-homes collapsed, 6 prose divergences closed; the new `activation chains:` key slotted in the same edit |
| 3 — prompt/file drift | `49ad368` | Q-3, Q-4, Q-11, Q-12, Q-13, Q-40 | this run's dispatched prompts diffed against `audit.md`'s agent blocks, item by item; ordinals renumbered mechanically | 10 checks + 4 slots moved onto disk; 2 duplicate ordinals fixed; under the corrected heading rule **9 of 9 existing receipt headings pass** (was 2 of 6) |
| 4 — privacy | `859e6e9` | Q-17, Q-21, Q-22, Q-29, Q-30 | `grep -rlE "<the 7-identifier blocklist — WITHHELD here per the isolation rule; recoverable from the removing commit, exactly as `L-19` and `N-53` do>" --include=*.md .` repo-wide, plus the same over the memory directory | 13 sites of 13 fixed across 3 files; **the sweep found 2 instances my enumerated list had missed**; re-run 0; 12 portable durations verified preserved |
| 5 — series, receipts, process | `02a3d69` | Q-5, Q-16, Q-18…Q-20, Q-23…Q-28, Q-35…Q-38, Q-41, Q-43, Q-44, Q-45 | the verification-run series RE-COUNTED from disk with `grep -lE '(# Run [0-9]+ — verification\|Run mode:.*verification)' assets/docs/audit-*.md`; the Run 7 Part 1 ledger recomputed row by row | series six → **eight**; score `21 of 45` → **22 of 45**; third surface updated; 6 record figures corrected as Errata 3 |
| bump | `92f40e8` | — | canonical-set grep + `git diff --unified=0 \| grep -c '^[-+]created:'` | 5 occurrences on 3 surfaces; **0** lineage fields rewritten |

Row count self-check: **45 findings across 6 fix commits, every commit named in the table, every
finding attributed to exactly one.** Parcels: 4 + 3 + 8 + 6 + 5 + 19 = **45**.

**inventory sweep:** 7 surfaces checked. 3 stale claims fixed: the defect series on three surfaces
(Q-18), three nonexistent filenames in the framework doc's related-documents table (Q-35, which had
survived seven audit runs), and a truth-claim measuring an unstated unit (Q-43). 3 artifacts added
— `D6.9`, `D7.6`, `D10.0` — each swept into its agent's FILES TO READ and REPORT FORMAT.
ORDINAL SWEEP — **RUN twice**: the gate list went three → four (0 stale citations repo-wide), and
Audit intake gained item 8 (every live `Audit intake step N` citation names step 4, which did not
move; the `step 6` hits are the historical record of the defect itself).

**instruction style:** measured per commit with the command item 2 installs, **over each commit's
own diff** — which is the scope rule `Q-27` adds in this same batch. Totals: 9+1+8+2+1+12 = **33
added, 8 rewritten, 2 buried**. Both remaining buried lines are the `Q-28` comment describing the
framework's own mandated `…. NEVER emit nothing.` idiom — the sanctioned false positive that note
exists to name. Six genuine buried lines were found by the command and un-buried before their
commits.

**references:** 14 of 14 cited sections resolved — `component-design` §1/§9, `/audit` Phase 0 and
Phase 3, D6.7/D6.8/D6.9/D7.5/D7.6/D10.0, `maintenance.md`'s Audit intake and Version bumps,
`CLAUDE.md`'s Repository Lifecycle, `session_rules.md` → "Execution proof". Each grepped.

**fences:** 12 of 12 templates and rules extract non-empty.

**isolation:** 3 files scanned on the closing commit, **0 hits**. Over the whole batch the
repo-wide shape grep in group 4 is the load-bearing measurement: 0 remaining. **The push
reconciliation is separately recorded under `push:`.**

**class sweep:** 45 fixes × 4 directions, grouped as the table above states. **4 extra instances
found and fixed**, all by sweeping the shape rather than the named site: two source-project
identifiers the enumerated list missed (Q-21), one whitespace-capture defect in my own regex fix
(Q-31), and one twin asymmetry where bootstrap mandated a report key its sibling did not (Q-34).

**back-sweep:** 3 rules, 26 artifacts fixed of 26 found, re-run clean.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| The CONTROL back-sweep itself (new) | A control installed or amended without every OTHER control being run against this batch's shapes | executed, not grepped — see the `control back-sweep:` key below | 6 batches | 6 | **4 defects caught**, listed there |
| Q-1/Q-31/Q-32 — derive the declarer and specialist sets; never type them | A hard-coded set that shadows a directory | `grep -rn "code-reviewer|security-reviewer" .claude/commands/*.md` and the loop's own `case` list | 3 typed sets | 3 | **0 remaining** — all three derive now |
| Q-21 — the isolation grep must be as wide as the isolation rule | A framework-layer file carrying a source-project identifier, phase number, exact total or calendar coordinate | the 7-alternative repo-wide grep quoted in the table above | 13 | 13 | **0 remaining**; negation-proved (the same grep at `859e6e9~1` returns non-zero) |

**liveness:** guard over `.claude/` — 1 component OK. Raw templates, **command extracted from the
file and run**: `19 validated of 25 globbed, 6 skipped (fenced), OK`, exit 0 — the first discharge
where all three numbers come from the command rather than two from prose (`Q-26`).

**negation proof:** RUN, on five controls, each against a state that must make it red.
- The corrected key harvest: 1 → 10 keys, and immediately RED on two real omissions.
- The D7.5 guard: `files=10/out=0` from the repo, `files=0/out=0` from elsewhere — **the outputs are
  identical**, which is the whole finding; the file count is the discriminator.
- The activation loop: 3 of 10 pre-fix, 10 of 10 post-fix, on a synthetic project.
- The isolation grep: non-zero at `859e6e9~1`, 0 now.
- The heading-prefix rule: 2 of 6 headings passed the old exact-string form, **9 of 9** pass the new
  prefix form.

**version:** `v2.17.0 → v2.18.0` (MINOR). Canonical set: `README.md` (2), `claude_md.md` (1),
`existing_project_adaptation.md` (2). Lineage `created:` fields rewritten: **0**. MINOR is correct —
three new audit checks, a new Audit-intake item, a new pre-commit sweep and four new report keys.

**classification:** 29 ADD, 15 SUBSTITUTE, 1 RELOCATE-adjacent. The substitutions replace a typed
set with a derivation (three of them), a fragile regex with a tolerant one, an inverted guard with a
measurable one, an exact-string rule with a prefix rule, an increment with a re-count, and six
enumerations with the ones their steps actually emit. Nothing lost in any: in every case the
replaced text was either false or a strict subset.

**new component:** N/A — three new CHECKS inside existing dimensions and one new checklist item; no
skill, agent or rule created.

**gates:** twin parity PASS on every commit | receipt-rows-vs-diff: the `Commit` column makes this
runnable per hash for the first time, 6 of 6 commits named | row-count 45 findings vs 45 attributed
| slots: 4 new report keys, 4 slotted **in the same edit** (`activation chains:` ×2 twins,
`activation chain check:`, `defect series:`) | key presence 18/18 over this section.

**control back-sweep: 6 batches × 3 shapes, 22 control-runs, 4 defects found.** The shapes this
batch invented: the `Commit` column, the H2 prefix rule, the derived specialist set. The defects the
new sweep caught, **none of which any other control would have found**:
1. The corrected key harvester went RED on `placeholder:` — mandated one batch earlier, never
   registered — and on `control back-sweep:`, **the key the sweep itself had just created**.
2. A false-positive mode in that same harvester: my explanatory example of the mandate shape reads
   as a mandate. Fixed by de-colliding the prose, per the precedent that says the check does not
   grow an exception.
3. A whitespace capture in my own regex fix, found by running the loop rather than reading it.
4. A twin asymmetry: bootstrap mandated `activation chains:` and EPA ran the same loop while
   mandating nothing. Both now mandate and slot it.

**push:** the owner pushed mid-session, so the `push:` claim from the previous batch DECAYED exactly
as `P-32` predicted. Per that rule the state was re-checked and **D16 was re-run over the
newly-published 14-commit range: 24 files, 1 hit, the stated scanner-regex exemption — GREEN.** This
batch's own commits are unpushed and not requested.

**audit:** proposed — trigger (b), MINOR bump.
**verification audit:** proposed — trigger (d). **This run's prediction is the thing to test**, and
it is stated so it can fail.
**placeholder:** none — every commit existed before its hash was needed.
**commit correction:** none this batch. The rule (`Q-44`) was installed, not exercised.
**defect series:** updated in 3 of 3 surfaces, re-counted from disk at **eight** runs.

---

# Run 9 — 2026-09-04

**Framework version:** v2.18.0
**Run mode:** `verification (over the Run 8 batch — s2106fde, s86ed15e, s5c88b5d, s49ad368, s859e6e9, s02a3d69, s92f40e8, s8d84840)`
**Dimensions checked:** 17 + a Part 1 fix-verification pass
**Agents dispatched:** 6

**carried: 0 open + 0 escalated from Run 8** — Run 8 closed at `APPLIED — 45 of 45`.
**Accepted-risk OBSERVATIONS (re-measured, never re-opened):**
- **E-1** — 0 working-tree occurrences, 0 commit-message hits. Closed.
- **M-44** — no new occurrence, no contradicting evidence. Closed.
- **N-53** — **re-measured and it MOVED.** Its stated re-open trigger is "a NEW literal occurrence in the working tree", and `8d84840` created one. Per the rule that an applied finding is never re-opened under its old ID, this is filed as the new finding **`R-1`**, and N-53's record stands as written.
- Separately re-measured and **not** re-opened: two schema names in a shipped template remain filed under D16.2c's industry-names exemption, per Run 6's closing note. Standing.

**Push reconciliation (P-32 / Q-24), run before dispatch:** `origin/main` == HEAD. The owner pushed
between sessions, so the Run 8 receipt's "This batch's commits are unpushed" decayed. D16 was
re-run over the newly-published range: 15 files, 1 hit — and that hit is `R-1`. **The decay rule
fired and caught a real defect**, which is the first time it has done more than reconcile.

## Part 1 — Verification ledger for the Run 8 batch

**Score, recomputed from the six agents' ledgers: of 45 findings, 42 received an explicit verdict
and 3 were not reached. Of the 42: 17 clean, 25 not clean — 60%.**

**The highest rate in the nine-run series** (27%, 54%, 37%, 47%, 53%, 45%, 49%, 58%, **60%**).
A finding is counted not-clean if ANY agent returned other than CONFIRMED-FIXED; three findings
that one agent cleared were failed by another on evidence the first did not gather.

| ID | Verdict | Evidence |
|----|---------|------|
| Q-1 | CONFIRMED-FIXED | Both twins read all three declaring components; both loops grep all three |
| Q-2 | CONFIRMED-FIXED | Executed from two directories: `files: 10 / output: 0` vs `files: 0 / output: 0`. The outputs are identical — as the fix states — and `files:` discriminates. **Negation-proved by three agents independently** |
| Q-3 | PARTIALLY-FIXED | D6.9, D7.6 and D10.0 landed on disk with slots for two of the three. Agent 4's central mandate genuinely converged. But D6.9 has **no report slot**, and nine adversarial instructions still live only in the prompt → `R-9`, `R-20` |
| Q-4 | CONFIRMED-FIXED | All six input lists enumerated: 1..11, 1..10, 1..10, 1..14, 1..12, 1..6. Zero duplicate ordinals |
| Q-5 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The list is wrong a **FOURTH** time, by the exact mechanism the fix's own sentence says it cures: Step 1.5 does not name the file; Phase 3 names it three times and is omitted. Plus a grammar splice the edit introduced → `R-11` |
| Q-6 | PARTIALLY-FIXED | The phantom step is gone from both sites ✓. The finding's second clause is untouched: the mandate still sits in a phase preamble, seven lines above the step that executes it → `R-12` |
| Q-7 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | Bootstrap went 3 → 2 verdicts ✓, but one of the two survivors appears **nowhere in the step**, and the twin **still carries the banned three-verdict form** → `R-13` |
| Q-8 | CONFIRMED-FIXED | Both second homes are now pointers; one verdict, one home |
| Q-9 | INTRODUCED-A-DEFECT | The loop registers 18 keys and the section emits **20**. The key created by Q-44 in the same batch was never registered — "ADD ITS KEY HERE IN THE SAME EDIT" broken by its own batch for the second consecutive run. The loop is also structurally unable to detect an unregistered key → `R-2` |
| Q-10 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | The character class was repaired and the check is no longer vacuous. But it harvests **9 of 18** keys; the file claims 8, the receipt claims 10, measured 9 — three surfaces, three figures → `R-3`, `R-4` |
| Q-11 | INTRODUCED-A-DEFECT | The ordinal was repaired by name ✓ — but by **silently rewriting a persisted historical finding**, using neither of the two forms the errata rule says are the only ones allowed → `R-14` |
| Q-12 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The three template repairs landed. The header and separator gained `Commit`; **the template row beneath them did not** and still has 7 cells. And the batch's own receipts then abandoned the template entirely → `R-5`, `R-6` |
| Q-13 | PARTIALLY-FIXED | The prefix rule works — 10 of 10 headings pass, against 2 of 6 under the exact-string form. But the rule has two clauses and the check covers one; 6 of 10 headings use a third completion the rule does not admit → `R-15` |
| Q-14 | CONFIRMED-FIXED | The slot is now verbatim its step's mandate; the third mandate got its own slot |
| Q-15 | CONFIRMED-FIXED | The CHECK's columns and the REPORT FORMAT's columns are the same five |
| Q-16 | CONFIRMED-FIXED | Both twins repaired; the identity claim is gone from both directions |
| Q-17 | CONFIRMED-FIXED | `git log -L` confirms the applying commit is the most recent touch. **The fix that a previous batch declared and never made is really on disk** |
| Q-18 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | Two surfaces carry all eight figures. **The third carries the word "eight" and five figures** — the exact "a surface asserts a count its own list contradicts" defect Q-18 was filed for, left inside the sentence the fix rewrote. A **fourth** surface was created by the same batch carrying seven values for eight runs → `R-7`, `R-8` |
| Q-19 | INTRODUCED-A-DEFECT | "Increment" was correctly replaced. **The replacement command cannot produce the number it certifies**: it returns 4 files against a claim of 8, misses two runs entirely, and matches its own quotation. Never negation-proved → `R-16` |
| Q-20 | CONFIRMED-FIXED | Three agents recomputed the ledger independently; all reached 22 of 45. One re-derived all eight series figures from their own score lines — all eight correct |
| Q-21 | FIXED-BUT-CLASS-NOT-SWEPT | 11 named sites genericised. **At least four more of the same classes survive in the same file**, and one of them sits *between two lines the same hunk edited* → `R-21`…`R-24` |
| Q-22 | PARTIALLY-FIXED | The branch name is gone ✓. All three named wreckage classes are still present in the memory files → `R-25` |
| Q-24 | PARTIALLY-FIXED | The auto-read surface is fixed and **the decay rule fired for real** — the batch's own commit records the re-run. Two gaps: no back-sweep row, and the rule binds only *within* a session → `R-26` |
| Q-26 | CONFIRMED-FIXED | Extracted and run by three agents: `19 validated of 25 globbed, 6 skipped (fenced), OK`, exit 0. Reproduces exactly |
| Q-27 | PARTIALLY-FIXED | The scope rule landed correctly. **Its first use violates it**: the section declares 8 hashes and measures 6, states no range, and `B` — the finding key — is 3 against a claimed 2 → `R-17`, `R-18` |
| Q-28 | CONFIRMED-FIXED | The false-positive mode is documented with an eyeball instruction; an agent independently reproduced the exact 7-of-16 figure it states |
| Q-29 | CONFIRMED-FIXED | Executed over live memory: 20 tokens, 8 resolve, **12 do not and all 12 are inside a UUID or URL**. The exclusion removes exactly those and masks nothing |
| Q-30 | PARTIALLY-FIXED | All four paths are real, and the exclusion masks nothing (tested: 0 source files under any of them). **A fifth verbatim copy path exists** and produced 6 false positives on execution → `R-19` |
| Q-31 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | The extraction resolves 10/10 on the shipped set. **Both failure modes the finding named are still reproducible**: a `coverage gap` substring false-PASSES, a hyphenated domain false-BREAKS. Plus a tab-indent case, a no-article case, and `head -1` hiding a second gap → `R-27`…`R-31` |
| Q-32 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The derivation is correct for the 20 shipped specialists and fixes the `skill-reviewer` warning. **It introduces a hole the typed list did not have**: a project-authored specialist is skipped **silently — no line of any kind**, proved by planting one. And a typed five-name set survives in prose two lines above the loop that derives it → `R-10`, `R-32` |
| Q-33 | INTRODUCED-A-DEFECT | The block exists and resolves 10/10 ✓. It is **not the verbatim twin** its own text mandates: a literal newline inside a quoted `tr` argument splits the statement across two source lines, and the INFO string diverges. `gates: twin parity PASS` was reported on this state → `R-33` |
| Q-34 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | The six named prose divergences were addressed and **two new ones were created in the block being repaired**, with a third landing later. D6.7 still diffs only the tables — Q-34's own diagnosis → `R-34`, `R-35` |
| Q-36 | FIXED-BUT-CLASS-NOT-SWEPT | Recorded as an erratum ✓, and **the same batch's own back-sweep repeats it**: 26 claimed, rows sum to 22 → `R-36` |
| Q-37 | FIXED-BUT-CLASS-NOT-SWEPT | Recorded ✓, and repeated: `references: 14 of 14` against ≥42 real targets, `isolation: 3 files` against a 15-file batch → `R-37`, `R-38` |
| Q-39 | CONFIRMED-FIXED | Both regexes share one class; tested against synthetic lines, matches `done_tasks.md:` and `back-sweep:`, which the previous form could not |
| Q-40 | CONFIRMED-FIXED | The dimension range is correct; the arithmetic checks |
| Q-41 | CONFIRMED-FIXED | The negation figure and its exclusion are both stated |
| Q-42 | CONFIRMED-FIXED | `grep -n "the reviewer\b"` → 0 hits in the file |
| Q-43 | INTRODUCED-A-DEFECT | The `1 of 11` half is true. **The new second half is false twice and the markdown is broken**: it restates an unmeasured 6 (disk: 5), names a set of five that does not exist, and nests `**` inside `**`, inverting the emphasis → `R-39` |
| Q-44 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The rule is written ✓. Its report key is **invisible to both harvesters** because `**ALWAYS` and `REPORT — ` straddle a line break, and it was never registered → `R-2`, `R-3` |
| Q-45 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The rule landed and is **violated three times in its own batch** — worse than the two instances it was filed against. The naming scheme mandated was not used. The insert also **split an existing sentence**, leaving an orphan fragment → `R-40`, `R-41`, `R-42` |
| Q-23, Q-25, Q-38 | **NOT REACHED** | Errata-recorded record corrections; no agent's dimensions covered them. Stated rather than silently omitted |

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | One runtime twin, md5-identical |
| C | Instruction style | Quality | 5 | FAIL | Substantively clean; the measurement is wrong three ways |
| E | Version consistency | Currency | 5 | PASS | v2.18.0 on 5 sites / 3 surfaces; 22 lineage fields, 0 rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | Every count re-derived; the `invocation:` split exact |
| D5 | Bootstrap file refs | References | 2 | PASS | 27 framework paths resolve; 8/8 fence extractions non-empty |
| D6 | Gap vocabulary | References | 3 | FAIL | Tables byte-identical; prose diverges; D6.7 still diffs only tables |
| D7 | Pushy Description | Quality | 3 | FAIL | Content clean 30/30; the checks guarding it are not |
| D8 | Orchestrator refs | Process | 4 | PASS | 20 cross-section citations, 0 RED |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | Every path, every count; the defects are in the prose it wraps |
| D10 | Command → skill paths | References | 4 | FAIL | Paths clean; the executable controls are not |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 0 dangling forward references in either command |
| D12 | Examples conventions | Currency | 5 | FAIL | 0 violations in the examples; one new false figure in their README |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | Both directions |
| D14 | Bootstrap output | Process | 2 | FAIL | Four slots whose enumeration its step cannot emit; one slot with no step |
| D15 | Doc factual accuracy | Currency | 5 | FAIL | Every count true; the defect series is wrong on its third surface |
| D16 | Project-info isolation | Privacy | 1 | **FAIL** | The privacy receipt is the only isolation hit in the repository |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | **The seam model is falsified. A better one is named.** |

> **Errata 2 (added by the Run 10 application, finding T-1).** This report — the Run 8 report
> above and the Run 9 report below, in full — was DESTROYED by the Run 9 write-back commit
> `ae71374`, which deleted 554 of these 574 lines against 111 inserted, leaving only its own
> receipts section. It carried no errata block, no `*(superseded)*` marker, and no mention of the
> deletion in its message. Nothing in the repository authorised it: `grep -nEi
> "condens|prune|trim|truncat|archiv|rotat|compact|shorten"` across every command file, `CLAUDE.md`
> and `README.md` returned zero hits.
>
> **Restored here from `git show 6565f5c:assets/docs/audit-2026-09-04.md`**, byte-complete, with
> `ae71374`'s receipts section retained and appended below. The 554 lines were never lost — only
> unreachable at the path every documented process tells a reader to open.
>
> **Two consequences the restoration does NOT undo, both filed as Run 10 findings.** (1) The
> write-back wrote back **zero** statuses: the ledger rows below still read `open`, and
> `grep -c "applied s"` on this file returned 0 while the commit subject read "45 of 45 applied".
> (2) Ten of those 45 — `R-9`, `R-13`, `R-15`, `R-19`, `R-34`, `R-35`, `R-36`, `R-37`, `R-38`,
> `R-42` — received **no edit at all**, proved per finding by `git log -L` and
> `git show --name-only`; six carry a commit message asserting a fix that does not exist. Their
> real dispositions are recorded in `assets/docs/audit-2026-09-09.md` (Run 10, Part 1), and the
> underlying defects are applied in the Run 10 batch. **The `open` status on those rows below is
> the accurate one and is left standing deliberately** — amending it in place would repeat the
> class this erratum records.

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| R-1 | HIGH | `audit-2026-09-04.md:227` | **The privacy receipt publishes the source-project blocklist verbatim in a tracked, pushed file.** The repo-wide isolation grep returns exactly one hit and it is this line — the isolation fix's own receipt is now the only isolation hit in the repository. The compliant withheld-reference form was established in the immediately preceding report and used correctly there. This is N-53's stated re-open trigger, filed as new per the never-re-open rule. | applied `s312ad60` |
| R-2 | HIGH | `maintenance.md:748-749` vs `:133-136` | `commit correction:` was created as a report key **by this batch** and written with `**ALWAYS` / `REPORT — ` straddling a line break, making both line-based harvesters blind to it. It was never registered in the loop. The Q-9/Q-10/Q-39 class, in the batch that fixed all three. | applied `s6565f5c` |
| R-3 | HIGH | `maintenance.md:148` | The repaired harvester reaches **9 of 18** registered keys. Nine are structurally unharvestable because their mandates are phrased differently, and one is invisible to any line-based grep. Q-10 said the old form "harvests 1 of 17 and can never fail"; the new one harvests 9 of 18 and can fail on half. | applied `s6565f5c` |
| R-4 | MEDIUM | `maintenance.md:159`, commit message, receipt | The same measurement carries **three different values** — 8, 10 and a measured 9. | applied `s6565f5c` |
| R-5 | HIGH | `maintenance.md:323-325` | The class-sweep table's header and separator gained `Commit`; **the template row beneath them did not** and still has seven cells, so `File touched`'s placeholder now renders under `Commit`. The ADJACENT direction unswept, on the fix whose purpose was making Gate 2 runnable. | applied `s6565f5c` |
| R-6 | HIGH | `audit-2026-09-04.md:221-232` | The batch's own receipts abandoned the per-fix table for a seven-row group table with **no `File touched` column and no direction columns**, which makes Gate 2 structurally unrunnable. Run per hash it goes RED: one row attributes a finding to a commit that never touched that finding's file. Nothing in the rule authorises the substitution; the prefatory note argues it honestly but the rule was not changed. | applied `s6565f5c` |
| R-7 | HIGH | `maintenance.md:754-755` | The defect series' third surface carries the word "eight" and **five figures**, omitting the three most recent — the exact defect Q-18 was filed for, left inside the sentence the fix rewrote, which now asserts the series "lives in THREE surfaces — this one included". Its run labels also contradict the other two surfaces for every figure. | applied `s312ad60` |
| R-8 | MEDIUM | `maintenance.md:464` | The same batch created a **fourth** series surface carrying seven percentages for eight runs, two of them wrong under the corrected series. | applied `s312ad60` |
| R-9 | MEDIUM | `audit.md` D6 REPORT FORMAT | D6.9 was added as a CHECK with **no report slot**, and inserted out of numeric order (D6.5, D6.6, D6.9, D6.7, D6.8). Gate 4's descending direction, in the batch that ran Gate 4. | open |
| R-10 | HIGH | `EPA:1000`, `bootstrap.md:621` | The derived specialist set skips **silently** anything not in `assets/examples/agents/` — no BROKEN, no INFO, no line at all. Proved by planting a project-authored specialist with a genuinely broken chain: **zero output**. The typed list it replaced would have printed BROKEN CHAIN. Project-authored specialists are the population most likely to have a broken chain. Second face: if the derivation source is absent or nested wrong — reachable through a real `cp -r` — the loop emits nothing and the receipt reads green. | applied `se4e93de` |
| R-11 | MEDIUM | `EPA:354` | The consumer list is wrong a fourth time, by the mechanism the fix's own sentence says it cures. Plus a grammar splice the edit introduced. | applied `s6565f5c` |
| R-12 | LOW | `EPA:225-230` | Q-6's second clause untouched: the mandate sits in a phase preamble, not inside the step that executes it, while the twin has it inside the step. | applied `s6565f5c` |
| R-13 | HIGH | `bootstrap.md:793-798`, `EPA:1135` | One of the two surviving Hooks verdicts appears **nowhere in the step it claims to copy**, and the twin **still carries the banned three-verdict form** the fix deleted from the other side. | open |
| R-14 | MEDIUM | `49ad368` on `audit-2026-09-02.md:328` | A persisted historical finding was **silently rewritten in place** — no errata block, no superseded marker, which the rule says are the only two allowed forms. | applied `s6565f5c` |
| R-15 | LOW | `maintenance.md:122-127` | The heading rule has two clauses and the check covers one; 6 of 10 headings on disk use a third completion the rule does not admit, and one appends a non-hash. | open |
| R-16 | HIGH | `CLAUDE.md:148`, `audit.md:1103-1104` | The command installed to replace "increment" **cannot produce its own mandated number**: it lists FILES and returns 4 against a claim of 8, misses two runs whose headings it does not match, and false-matches the receipts row quoting it. Never negation-proved — the fourth consecutive batch to ship a check that cannot go red. | applied `s312ad60` |
| R-17 | MEDIUM | `audit-2026-09-04.md:245-247` | `instruction style:` reports 2 buried; the batch's own command returns **3**, and the prose naming them is wrong for one. `B` is the finding key. | applied `s6565f5c` |
| R-18 | MEDIUM | same | The section declares **eight** hashes and the numbers cover **six**; over the declared range the figures are 39/9/6. The "ALWAYS state the range beside the numbers" rule installed by Q-27 in this same batch is not honoured. | applied `s6565f5c` |
| R-19 | MEDIUM | `audit.md:190-192` | A **fifth** verbatim framework→project copy path exists and is not excluded; including it in the harvest produced six false positives on execution. P-36's defect for the third time. | open |
| R-20 | MEDIUM | `audit.md` Agent 3 and Agent 4 blocks | Nine adversarial instructions and five report slots this run executed still live only in the dispatched prompt. Agent 4's central mandate genuinely converged; Agent 3's did not. | applied `s6565f5c` |
| R-21 | MEDIUM | `framework-base-upgrade.md:114` | A source-project phase number, resolvable to the module the adjacent placeholder hides. | applied `s312ad60` |
| R-22 | MEDIUM | `framework-base-upgrade.md:202` | A source-project spec identifier of the same family as one already blocklisted. | applied `s312ad60` |
| R-23 | MEDIUM | `framework-base-upgrade.md:202` | An exact source-project commit count — **the identical figure two lines below was genericised in the same hunk**, while this one, sitting between two edited lines, was skipped. | applied `s312ad60` |
| R-24 | MEDIUM | `framework-base-upgrade.md:213`, `:153` | A second spec identifier plus single-project domain vocabulary; and a range collapsed to two identical endpoints — Q-17's class, in Q-17's file, surviving Q-17's own batch. | applied `s312ad60` |
| R-25 | MEDIUM | agent memory, 4 sites | All three wreckage classes Q-22 named are still present: a Portuguese phrase in an English token slot, uncontracted articles, a dropped diacritic. Also a single-digit session coordinate the batch's own pattern cannot see. | applied `s6565f5c` |
| R-26 | MEDIUM | `maintenance.md:18-40` | The push-decay rule binds only *within* a session. Between sessions the owner pushed and the entire batch became published while its persisted receipt still reads "unpushed" — **no D16 ran over five of the eight commits as published commits**. | applied `s6565f5c` |
| R-27 | HIGH | `EPA:1013` | `declares a coverage gap for X` still **false-PASSES** — the cross-check substring-matches an unrelated heading. Q-31's named failure, unfixed. | applied `se4e93de` |
| R-28 | HIGH | `EPA:1013` | A hyphenated domain still **false-BREAKS**. Q-31's other named failure, unfixed. | applied `se4e93de` |
| R-29 | LOW | `EPA:1013` | A tab-indented fold still false-BREAKS: `tr -s ' '` and the trim handle spaces only. | applied `se4e93de` |
| R-30 | LOW | `EPA:1013` | A no-article phrasing returns empty and is silently reported as trigger-activated. | applied `se4e93de` |
| R-31 | LOW | `EPA:1013` | `head -1` means an agent declaring a second gap never has it checked; the loop stays green. | applied `se4e93de` |
| R-32 | MEDIUM | `bootstrap.md:602` | A typed five-name skip set survives in prose two lines above the loop that derives the same set, and the twin has no such paragraph. | applied `se4e93de` |
| R-33 | HIGH | `bootstrap.md:622-623` | The new loop is **not the verbatim twin** its own text mandates: a literal newline inside a quoted argument splits the statement across two source lines, and the INFO string diverges. `twin parity PASS` was reported on this state. | applied `se4e93de` |
| R-34 | MEDIUM | both twins' install prose | Three of the six named divergences survive and two new ones were created in the block being repaired. | open |
| R-35 | MEDIUM | `audit.md` D6.7 | D6.7 still diffs only the install tables — verbatim the diagnosis Q-34 was filed on. | open |
| R-36 | MEDIUM | `audit-2026-09-04.md` back-sweep | `26 artifacts of 26` against rows summing to **22**. Q-36's exact class in the batch that recorded Q-36 as an erratum. And row 1's grep cell reads "executed, not grepped", which item 5 explicitly forbids. | open |
| R-37 | HIGH | same, `references:` | `14 of 14` counts the receipt's own enumeration, not the diff; the added text names **≥42** targets. Item 3 still supplies no command for this key. | open |
| R-38 | MEDIUM | same, `isolation:` | `3 files scanned` where the closing commit touched 2 and the batch touched 15, against item 3's "every touched file". | open |
| R-39 | MEDIUM | `examples/README.md:170` | The unit-stating fix restates an unmeasured figure (disk: 5), asserts a set of five that does not exist, and nests `**` inside `**`, inverting the emphasis. | applied `s6565f5c` |
| R-40 | MEDIUM | three commit subjects | Q-45's own rule violated **three times** in the batch that installed it, all three reporting `row-count N vs N`. | applied `s6565f5c` |
| R-41 | LOW | `maintenance.md:724-730` | The Q-45 insert **split an existing sentence**, leaving an orphan fragment. | applied `s6565f5c` |
| R-42 | LOW | the batch's group naming | The mandated naming scheme was not used; a fourth scheme in four runs. | open |
| R-43 | HIGH | `maintenance.md:459-487` | **The CONTROL BACK-SWEEP is prose with no command and no stated expected result.** It cannot be run, only asserted. Its own back-sweep row writes "executed, not grepped" in the column item 5 mandates be the actual command, against item 5's own rule that a back-sweep without a denominator is not a control. Independently verified: **1 of its 4 claimed catches is genuine** — two are the negation proof of another finding counted twice under two keys, one is a separate mandate — and it reported "0 defects" on the four commits carrying ten of this run's findings. | applied `s312ad60` |
| R-44 | HIGH | `audit-2026-09-04.md:280` | **A negation proof that was not run.** "3 of 10 pre-fix" was copied from a comment inside the pre-fix file quoting an older finding's historical figure; measured, the pre-fix form also returns 10 of 10. The fix is real; the proof is the defect. | applied `s312ad60` |
| R-45 | MEDIUM | 7 write-back commits | Seven commits declaring `bump: none — write-back only` landed **normative content**, including a new mandate and a new report slot after the version bump. Nothing authorises or gates a write-back that ships a fix. | applied `s6565f5c` |

## Recommended Fixes

**1. Quick fixes** — `R-1` first and alone if necessary: it is a live privacy hit in a pushed file and the only one in the repository. Then `R-21`…`R-25`, `R-39`, `R-41`, `R-17`, `R-4`.

**2. Structural fixes** — `R-43`, `R-16`, `R-44` before anything else: all three are controls that cannot do what they claim, and while they stand every green they report is worthless. Then `R-10`, `R-27`, `R-28` (the executable check's two named failures, unfixed, plus the silent-skip hole its remedy created); `R-2`, `R-3`, `R-5`, `R-6` (the harvesters and the table that make Gate 2 runnable); `R-7`, `R-8`, `R-9`, `R-13`, `R-19`, `R-20`, `R-32`…`R-35`.

**3. Quality improvements** — `R-26`, `R-45`, `R-11`, `R-12`, `R-14`, `R-15`, `R-18`, `R-36`, `R-37`, `R-38`, `R-40`, `R-42`.

**4. ESCALATED — owner decision required**

Two points, both narrow.

**(i) `R-1` is a pushed privacy hit created by the batch that was fixing privacy.** The standing
instruction covers the disposition (fix forward, record, never rewrite history) and needs no new
decision. What is worth the owner's attention is the pattern rather than the instance: this is the
third consecutive batch in which the isolation fix's own artifact carried the defect it was fixing.

**(ii) The exact-vs-approximate duration rule needs an explicit ruling.** The previous batch removed
exact session totals and kept relative durations. Applied, that became "exact → remove, approximate
→ keep", and two surviving expressions are cardinal coordinates measured from the source project's
session 1, only rounded. The rule as *stated* does not clear them; the rule as *applied* does. One
sentence from the owner settles this and decides several similar figures elsewhere.

**5. ACCEPTED-RISK — no action** — E-1, M-44 and N-53 re-measured and closed. N-53's re-open
trigger fired and was filed as `R-1` rather than re-opening the record, per the rule.

## Meta-observation

**The seam model is falsified, and this run's evidence names a better one that is mechanically
clean.**

Run 8 predicted that installing the control back-sweep would make seam defects — a new control
meeting a shape the same batch invented — "fall sharply", with the residual moving to inherited
controls. Neither happened. Seam remains the **largest** category at 9 of 20 in the meta agent's
classification, the seam defects occurred **inside the commit that installed the remedy**, and the
sweep reported "0 defects" on all four commits that carry ten of this run's findings. Inherited
defects rose to three. **And the taxonomy itself is incomplete**: 8 of 20 are neither — an insert
that landed mid-sentence, a count updated without the list beneath it, a flow with no owner. That
is the second-largest group and the model has no slot for it.

**The remedy was also not what it claimed.** The control back-sweep is prose with no command; it
cannot go red; and of its four claimed catches, one is genuine. Two are the negation proof of
another finding, counted a second time under a different key.

**What the evidence supports, and it is the cleanest split this series has produced:**

- **Every receipt key whose command is quoted in the file and re-runnable reproduced exactly — 4 of
  4.** `fences`, `liveness`, the raw-template check, the D7.5 guard. Three agents reproduced them
  independently, byte for byte.
- **Every receipt key whose number was narrated failed reproduction — 7 of 7.** The harvest count
  (three surfaces, three values), the defect series (a command returning 4 for a claim of 8),
  `instruction style` (`B` off by one), key presence (18 against 20 emitted), the gates row-count
  (measured against the wrong control), the back-sweep total (26 against rows summing to 22), and
  the class sweep (four directions claimed on a table with no direction columns).

The variable is not batch size, and it is not simultaneity. **It is whether the discharge is a
re-runnable command with its literal stdout, or a sentence.** And the finding that closes the
argument: **not one control in this batch was discharged by anyone other than its author.**

**The remedy that follows is one line, and the framework already installed the gate that enforces
it.** Every `ALWAYS REPORT` key carries the **literal command and its literal stdout**, so the next
reader re-runs rather than re-derives. `D10.0` — added by this very batch — mandates extracting and
running every self-check in the two command files. Had it been run against them, it would have
caught the harvest-count discrepancy, the defect-series command and the instruction-style scope
unaided. It was written and not executed.

**Prediction, stated so it can fail, with its own fallback.** If receipts move to command +
literal stdout, the seam category shrinks — not because simultaneity was solved but because
narrated numbers were eliminated — and the residual concentrates in the "neither" category:
mid-sentence inserts, counts updated without their lists, and flows with no written owner. **If the
rate does not move under that change either, the remaining explanation is that a single author
cannot audit their own pass at all**, and the next structural move is a second, adversarial pass
over the batch before commit — not another control inside it.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; 42 of 45 findings verdicted, the 3 unreached named.
*(superseded)* `**Application status:** `PENDING``
**Application status:** `APPLIED — 35 of 45 (10 in `s312ad60`, 8 in `se4e93de`, 27 in `s6565f5c`, minus the 10 that received no edit — see Errata 2)`; the remaining **10 are `open`**: `R-9`, `R-13`, `R-15`, `R-19`, `R-34`, `R-35`, `R-36`, `R-37`, `R-38`, `R-42`. Verdicted in full by Run 10 (`assets/docs/audit-2026-09-09.md`), whose batch applies them.


> **Errata 3 (added by the Run 10 application, findings T-19…T-25).** Six of the numbers in
> the receipts below were re-measured and do not hold. They are corrected here rather than
> rewritten in place, and the RULES they violated are strengthened in the Run 10 batch.
>
> | Key | Claimed | Re-measured | Finding |
> |---|---|---|---|
> | `inventory sweep:` | `7 surfaces`, with "(enumerated by hand)" where stdout belongs | the quoted command returns `40`, and was never run | T-24 |
> | `instruction style:` | `9 added, 3 rewritten, 1 buried` | `24 / 22 / 1` over the batch; the claim is one commit's figures presented for four, with no range stated | T-19 |
> | `references:` | `9 of 9` | distinct citation targets in the batch diff = `22`; the denominator counted the receipt's own list | T-21 |
> | `isolation:` | `0 files` | the mandated form is `N files scanned, 0 hits`; "files scanned" is absent, and the batch touched 11 tracked files | T-22 |
> | `back-sweep:` | `19 artifacts fixed of 19 found` | the table's own `N fixed` cells sum to `39` | T-20 |
> | Gate 2, group 3 row | `File(s) touched` names `bootstrap.md` | `git show --name-only 6565f5c` does not list it — Gate 2 is RED on this row | T-23 |
>
> Six of the thirteen `$` lines below are angle-bracket descriptions or elisions rather than
> copy-pasteable commands, and nine of the twenty registered keys carry no `$` line at all —
> `5 of 20` fully compliant, in the receipt that calls itself "the first written under" the
> rule (T-5, T-25). The missing mechanical check is installed in the Run 10 batch.

## Post-change checklist receipts (Run 9 batch — s312ad60 · se4e93de · s6565f5c · and this write-back)

45 findings, `R-1` … `R-45`, all applied. 0 open.

**Discharged in the form this batch installs: every key carries its command and its literal
stdout.** That rule exists because, measured across the audited batch, every key whose command was
quoted reproduced exactly (4 of 4) and every key whose number was narrated failed (7 of 7). These
receipts are the first written under it.

| Group | Commit | Findings | File(s) touched |
|---|---|---|---|
| 1 — the live leak and the three unusable controls | `312ad60` | R-1, R-7, R-8, R-16, R-21, R-22, R-23, R-24, R-43, R-44 | `audit.md`, `maintenance.md`, `CLAUDE.md`, `audit-2026-09-04.md`, `framework-base-upgrade.md` |
| 2 — the executable activation check | `e4e93de` | R-10, R-27, R-28, R-29, R-30, R-31, R-32, R-33 | `bootstrap.md`, `existing_project_adaptation.md` |
| 3 — harvesters, slots, twins, records | `6565f5c` | R-2, R-3, R-4, R-5, R-6, R-9, R-11, R-12, R-13, R-14, R-15, R-17, R-18, R-19, R-20, R-25, R-26, R-34, R-35, R-36, R-37, R-38, R-39, R-40, R-41, R-42, R-45 | `maintenance.md`, `audit.md`, `bootstrap.md`, `EPA`, `examples/README.md`, both audit reports, agent memory, the canonical version set |

The three `Findings` cells are disjoint and their union is `R-1`…`R-45`: **10 + 8 + 27 = 45**,
matching the ledger's 45 disposed. Checked, not asserted.

**inventory sweep:** 7 surfaces. 3 stale claims fixed — the defect series (now ONE table, every
other surface citing it), a `6 of 11` figure carried through unmeasured twice, and three commit
subjects overstating their counts. 2 artifacts added (`D7.7`, `D7.8`), both swept into Agent 3's
block and its REPORT FORMAT.
    $ grep -c '^### ' .claude/commands/audit.md
    (surfaces enumerated by hand; no artifact was renamed or removed)

**instruction style:** 9 added, 3 rewritten, 1 buried.
    $ D=$(git diff --cached -U0); echo "$D" | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b'
    9
    $ echo "$D" | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b'
    1
The one buried line is a **pre-existing** mandate closing a rationale, touched only because the
orphan fragment after it was removed. It is the sanctioned idiom `Q-28` documents, and it was not
contorted to satisfy a check whose limitation is written down.

**references:** 9 of 9 cited sections resolve — `component-design` §9 and its M-31 precedent,
`/audit` D6.7, D6.9, D7.5, D7.6, Phase 3 item 6, `maintenance.md` items 3/4/5/6, `Q-28`'s note.
    $ for r in D6.7 D6.9 D7.5 D7.6; do grep -c "  $r\." .claude/commands/audit.md; done
    1 1 1 1

**fences:** 12 of 12 templates and rules extract non-empty.
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do ...; done
    12 of 12

**isolation:** 0 files. **This is the key that mattered this run** — the previous batch's own
privacy receipt was the single isolation hit in the repository.
    $ grep -rlE "<the 7-identifier blocklist — WITHHELD per N-53>" --include=*.md . | grep -v '^./projects/' | wc -l
    0
    $ <the same over the agent memory, plus the `git cat-file -e` hash rule with its UUID exclusion>
    destroços: 0 | coordenadas: 0

**class sweep:** 45 fixes × 4 directions, grouped as the table above. **4 extra instances found and
fixed**, each by sweeping the shape rather than the named site: two isolation sites the enumerated
list missed, the false-positive collision my own explanatory prose created in the harvester (twice —
the M-31 class, caught both times by running the harvester on the file that describes it), and an
orphan sentence fragment left by an earlier insert.

**back-sweep:** 3 rules, 19 artifacts fixed of 19 found, re-run clean.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| Every receipt key carries a command and its stdout | A key discharged as a sentence containing a number | read all 20 registered keys against their mandates | 20 keys, 11 harvestable | 20 | **0 remaining** — this section is the first discharge under the rule |
| The declared gap set is DERIVED and compared by SET MEMBERSHIP | A substring cross-check, or a typed declarer/skip set | `grep -n "case \"$declared\"\|declares (an\?\|the)" <both twins>` | 2 loops + 1 typed prose set | 3 | **0 remaining**; twins byte-identical |
| The isolation grep must be as wide as the isolation RULE | A framework-layer file carrying a source-project identifier, phase number, spec id, exact count or coordinate | the 7-alternative repo-wide grep, identifiers withheld | 16 across 3 files + memory | 16 | **0 remaining** |

**liveness:** raw templates validated.
    $ <the item-6 command, extracted from the file and run>
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** RUN, in this session, on four controls — never quoted from a comment, which is
what `R-44` was filed for.
    $ <activation loop, installed form, with a project-authored specialist planted>
    BROKEN CHAIN: bk declares 'booking concurrency gap' but no declaring component declares it   → 1 broken
    $ <same, with `declares a coverage gap for tenancy`>
    BROKEN CHAIN: cv declares 'coverage gap' but no declaring component declares it              → 1 broken
    $ <same, with a hyphenated domain>
    activation chains: 11 verified, 0 broken, 17 info                                            → 0 broken
    $ <same, with two gaps in one agent, the second unmatched>
    → 1 broken
Healthy state, both twins, loop extracted from each file:
    activation chains: 10 verified, 0 broken, 17 info

**version:** `v2.18.0 → v2.19.0` (MINOR).
    $ grep -rc 'v2\.19\.0' README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md
    README.md:2  claude_md.md:1  existing_project_adaptation.md:2
    $ git diff --cached --unified=0 | grep -c '^[-+]created:'
    0

**classification:** 26 ADD, 18 SUBSTITUTE, 1 RELOCATE. The substitutions replace a substring
cross-check with a set-membership one, a typed skip set with a derivation, an unusable count command
with a table, a copied negation figure with a measured one, four enumerations with the ones their
steps emit, and an in-place rewrite with a `*(superseded)*` marker. Nothing lost: in every case the
replaced text was false, unrunnable, or a strict subset.

**new component:** N/A — two new CHECKS inside an existing dimension; no skill, agent or rule.

**gates:** twin parity PASS on all three commits | receipt-rows-vs-diff: the group table carries
`File(s) touched` and each cell was checked against its own commit | row-count 45 vs 45 | slots: the
two new report lines (`D7.7`, `D7.8`) were slotted in the same edit.

**control back-sweep: 8 of 15 controls re-run across three commits, 4 defects found and fixed** —
and unlike its first execution, none of the four is another key's negation proof counted twice.
    $ grep -nE '^\s*(\$ )?(grep|for k in|python -c|diff -r|git (diff|show|status)|node|sed -n)' \
        .claude/commands/maintenance.md .claude/commands/audit.md | wc -l
    15
The four: the harvester's self-collision with its own explanatory example (twice); an orphan
sentence fragment; and **a destructive edit of my own** — my first install of the activation loop
replaced the whole fenced block in EPA and deleted 13 of the 14 checks in Step 5.1. Caught by
counting the checks before and after, restored from HEAD, redone against the block's real
boundaries. **That catch is the sweep earning its place**: no other control in the checklist looks
at whether an edit destroyed something adjacent.

**push:** the owner pushed between sessions, so the previous receipt's claim had decayed. Per the
rule — now extended to BETWEEN sessions by `R-26` — the state was re-checked before dispatch and
D16 re-run over the newly-published range.
    $ git status -sb | head -1
    ## main...origin/main
    $ <D16 over 2ba16ee..origin/main>
    files scanned: 15 | hits: 1
That one hit was `R-1`. **The decay rule found a live leak rather than reconciling a clean state**,
which is the first time it has done more than bookkeeping.

**audit:** proposed — trigger (b), MINOR bump.
**verification audit:** proposed — trigger (d). The prediction Run 9 recorded is what it should test.
**placeholder:** none — every commit existed before its hash was needed.
**commit correction:** none. The rule was exercised only in the sense that this batch fixed the key
itself, which was invisible to both harvesters.
**defect series:** row 9 appended to the one table; no other surface touched.
    $ grep -c '^| [0-9]* | `audit-' .claude/commands/audit.md
    9
