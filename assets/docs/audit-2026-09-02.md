# Framework Audit Report — Run 2 (verification pass)

**Date:** 2026-09-02
**Framework version:** v2.9.0 (`README.md:1`), HEAD `a867598`
**Under verification:** the 24 findings applied in `0954d69` (report `audit-2026-09-01.md`)
**Dimensions checked:** 17 + a Part 1 fix-verification pass in every agent
**Agents dispatched:** 6
**Trigger:** owner request, following the proposal in `0954d69`'s own commit trailer.

**carried: none** — `audit-2026-09-01.md` closed with 0 `open` (24/24 applied in `0954d69`).

> **ID scheme.** `F-*` (2026-08-31 run 1), `G-*` (run 2), `H-*` (2026-09-01). This run uses `J-*`
> (skipping `I` — it reads as a digit). Verification verdicts on the H-series keep their H IDs.

---

## What this run was for

Run 1 checks *claim vs fact*. It is structurally blind to a fix that is PRESENT but landed in the
wrong place — the shape of `H-1`, which was a correct block inserted under the wrong header by the
commit that applied `G-13`. Every agent in this run therefore carried a Part 1 mandate: read the
structure AROUND each applied fix and classify it as CONFIRMED-FIXED / PARTIALLY-FIXED /
FIXED-BUT-CLASS-NOT-SWEPT / FIX-INTRODUCED-A-DEFECT.

**It worked.** Of 24 applied findings, **11 are clean, 13 are not** — and 5 of those are defects the
application itself introduced.

---

## Part 1 — Verification ledger for `0954d69`

| ID | Verdict | Evidence |
|----|---------|----------|
| H-1 | **CONFIRMED-FIXED** | `code_reviewer.md:206-248` read end to end: 5 trigger headers, 5 gap blocks, strict 1:1. Visual regression has its own header at :235; data-integrity's restored at :241. Diff is +1 line, no content loss. |
| H-3 | **CONFIRMED-FIXED** | All three lists say five and name visual regression (`bootstrap.md:446`, `EPA:341`, `validator.md:152`). Repo-wide grep finds no fourth unhedged instance. |
| H-5 | **CONFIRMED-FIXED** | Item 7 sits INSIDE the numbered list (`maintenance.md:203-216`); H2 starts at :218. Report line + self-check present; the self-check runs green (3/3 surfaces). ADJACENT sweep was done. |
| H-6 | **CONFIRMED-FIXED** | Items 1/2/3 all end in `NEVER emit nothing.` verbatim (`:103`, `:115`, `:124`). Item 3 adds the anti-merge clause. |
| H-10 | **CONFIRMED-FIXED** | `CLAUDE.md:36` = 15; zero stale "14" survives anywhere. The guard added to item 1 ("grep the NUMBER, not only the structure") landed in the right item. |
| H-11 | **CONFIRMED-FIXED** | `bootstrap.md:269-275`; the placeholder string matches `claude_md.md:51` byte-for-byte. |
| H-13 | **CONFIRMED-FIXED** | `docs/agentic_engineering_framework.md:356` now carries the guard's copy line. |
| H-14 | **CONFIRMED-FIXED** (one nit → J-29) | `validation-orchestrator:59-66`: CAPS verb, dedicated line, report line. Owner matches `autonomous-loop:151`. |
| H-16 | **CONFIRMED-FIXED** | Qualification truth-tested per skill: **9/9 comply**, zero residual violations. `database-migration-guide` is the exact case the new parenthetical names. |
| H-17 | **CONFIRMED-FIXED** | Disk cross-check: exactly 3 `invocation:` values ship (`inline` 18, `subagent` 30, `user` 7); README now documents all 3. |
| H-19b | **CONFIRMED-FIXED** | `session_rules.md:67` heading clean, nothing orphaned, citer matches exactly. |
| VERSION | **CONFIRMED-FIXED** | v2.9.0 on 3/3 canonical surfaces (5 occurrences). `git show 0954d69 --unified=0 \| grep '^[-+]created:'` → **no output**: zero lineage fields rewritten. Historical mentions correctly frozen. |
| H-2 | **FIXED-BUT-CLASS-NOT-SWEPT** | → **J-5** |
| H-4 | **PARTIALLY-FIXED** | Two sections landed and names match the mandating instructions verbatim, but the class was not swept inside the same file → **J-15** |
| H-7 | **FIXED-BUT-CLASS-NOT-SWEPT** | → **J-10** |
| H-8 | **PARTIALLY-FIXED** | Text landed, twins byte-identical (back-sweep PASSED), but the new §5 has no mother-side invoker → **J-8** |
| H-9 | **PARTIALLY-FIXED** | Exemption is stated, not implied — the finding's core is closed. But the section is an orphan mandate → **J-9** |
| H-12 | **FIXED-BUT-CLASS-NOT-SWEPT** | → **J-12** |
| H-15 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | → **J-6** |
| H-18 | **FIXED-BUT-CLASS-NOT-SWEPT** | → **J-14** |
| H-19a | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | → **J-4**; class also unswept → **J-17** |
| H-20 | **CONFIRMED-FIXED — INTRODUCED TWO DEFECTS** | Both named steps exist with real checks and report lines; `CLAUDE.md:95` resolves. But → **J-1** and **J-2** |
| H-21 | **FIXED-BUT-CLASS-NOT-SWEPT** | → **J-13** |
| H-22 | **PARTIALLY-FIXED** | Stack-skill line landed and ordered correctly; two gaps → **J-21**, **J-22** |
| H-23 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | PRD section and all citations check out; the routing is a forward write with no receiver → **J-3** |
| G-20 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | The substantive invariant is TRUE 20/20 (independently re-measured). The written claim is not → **J-7** |

**Score: 12 clean · 7 class-not-swept · 5 introduced-a-defect.**

> **Errata 2 (added by the Run-4 application, finding L-29).** The BREAKDOWN in that line is also wrong per row.
> Recounted over the 26 rows: **12 clean · 5 class-not-swept · 4 partially-fixed · 4
> introduced-a-defect · 1 confirmed-with-defects.** The figure to cite for this run is the one in
> Errata 1 below — **11 of 24 clean, 13 not** — counted over findings.
>
> **Errata 3 (added by the Run-5 application, finding M-51).** Errata 1 below quotes the
> 2026-08-31 report's summary line verbatim and never recounted it. **The measured figure is
> `4 of 15`, not 2** — that summary silently folds three class-not-swept rows into
> CONFIRMED-FIXED. Two independent recounts of its ledger agree on four (`F-1`, `F-11`, `F-12`,
> `F-15`). This is the fourth surface L-4 named and the one its own fix skipped.
>
> **Errata 1 (added by Run 3, finding K-7).** That line counts the ledger's **26 rows**, which
> include `VERSION` and the carried `G-20` — neither is one of the 24 findings. Counted over the
> FINDINGS, the correct figures are the ones this report's own prose gives at the top: **11 of 24
> clean, 13 not clean.** The `8 of 15` statistic that `a28f661` derived from the 2026-08-31 report
> appears in no record at all; that report states **13 of 15 CONFIRMED-FIXED, 2 PARTIALLY-FIXED**.

---

## New findings

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| J-1 | `bootstrap.md:37` vs `:60`, `:77`; `EPA:43` vs `:59` | **Step numbering is non-monotonic.** `### Step 1.1 — Framework-clone freshness check` sits at :37, BEFORE `### Step 1 — Read the PRD` at :60 — a sub-step preceding its own parent. `### Step 1.1b` (:77) then follows Step 1, so two non-adjacent "1.1-family" steps straddle their parent. EPA repeats it: `Step 1.0b` (:43) before `Step 1.0` (:59). `CLAUDE.md:95` now cites "Step 1.1", which resolves ambiguously by scan. A pre-PRD gate should be Step 0.5 or a Setup-phase item. Introduced by `0954d69`. | applied sa28f661 |
| J-2 | `bootstrap.md:45-51`, `EPA:79-85` | **The new freshness check FAILS OPEN.** `git status -sb \| head -1` prints `## main...origin/main [behind N]` only when an upstream is configured. On a branch with **no upstream** (or detached HEAD) it prints `## main` — no tracking segment, no `behind`, so the step reports "up to date" having compared against nothing. Only the no-*remote* case is handled. This is precisely the third state `session_rules.md → Execution proof` requires to fail CLOSED: *"the tool exited 0 and its summary is UNREADABLE → RED. 'I could not READ it' is NEVER 'it is healthy'."* The guard violates the rule the same repo ships. The negation proof run at application time tested only the positive path (a synthetic `[behind 3]` line) and never the ABSENT-signal path. | applied sa28f661 — **Run 3 verdict: not clean → see K-1** |
| J-3 | `bootstrap.md:77-93` vs Steps 3, 4, 13 | **Step 1.1b is a forward write with no receiver.** It routes each cross-cutting concern to `.claude/rules/` (Step 13, :568), `project.md`'s Architectural Decisions table (Step 3, :175) and `pendencias.md` (Step 4, :191) — none of which exist when Step 1.1b runs. No receiver clause exists in any of the three (grep "cross-cutting" in bootstrap returns only this step). Its mandated report `cross-cutting: N concerns routed` therefore cannot be true when emitted. Contrast the correctly-built 5.5→14 handoff, where the sender says "merge it during Step 14" and Step 14 says "this step is the RECEIVER". | applied sa28f661 — **Run 3 verdict: not clean → see K-2** |
| J-4 | `docs/modules/skills/skill-gate/SKILL.md:125-154` | **The H-19a heading promotion orphaned three universal blocks.** Promoting the bold lead-in to `### In sprint-approved mode` pulled "**Observation-mode session report:**" (:135), "**Proposing the exit:**" (:143) and "**After promotion** — run the standard creation eval" (:152) structurally INSIDE a conditional subsection. All three were flat paragraphs of `## 5. Promote` and apply to every promotion regardless of mode. A session not in sprint-approved mode that skips the subsection now skips a mandatory post-promotion eval. Introduced by `0954d69`. | applied sa28f661 |
| J-5 | `EPA:744-752` | **H-2's twin was never swept.** The bootstrap install table now has 10 rows; EPA's Step 4.6.5 table still has 9 — no `visual regression gap` row — while EPA `:341` WAS updated to offer and keep that gap. EPA now instructs keeping a gap it cannot install, and unlike bootstrap (`:552`) it has no `pendencias.md` fallback clause, so the miss is silent. | applied sa28f661 |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| J-6 | `sprint-proposer/SKILL.md:143-147` | **H-15 created two contradictory imperatives in one 4-bullet list.** `**ALWAYS PICK 3-5 dependency-satisfied tasks**` (:143) vs `**NEVER exceed the task limit: 3-5 standard, up to 7 ONLY if all small+related, 1 if large.**` (:146). Before the fix bullet 1 was a soft default that bullet 3 refined; promoting BOTH to absolutes means the 7-task and 1-large cases now literally violate an ALWAYS. Per §6 an imperative's whole value is that it fires — two contradictory ones is worse than one imperative plus a default. | applied sa28f661 |
| J-7 | `examples/README.md:121-126` | **The G-20 invariant is substantively true and literally false.** Independently re-measured: `## Input` is immediately followed by the output heading in **20/20** files, zero headings between — the pair invariant holds. But the README names that second heading `## Output` and stamps the claim "(verified: 20/20 examples…)", while **10 of 20 files use `## Output Format`** (`## Input`→`## Output`: 10; `## Input`→`## Output Format`: 10). An auditor grepping the stated invariant literally gets 10/20 RED. The fix replaced an unacknowledged variance with an unacknowledged variance one level over. Cheapest correction: name both spellings, the way `:134-137` already sanctions verdict-vocabulary variance. | applied sa28f661 |
| J-8 | `.claude/rules/component-design.md` §5 + `maintenance.md` | **RELOCATE has no mother-side invoker.** The verb and its `### RELOCATE` subsection landed in BOTH twins byte-identically (back-sweep verified), but `maintenance.md` cites component-design §6 (:110, :156), §8 (:162, :265) and §9 (:189, :197, :267) and **cites §5 zero times** (mechanically confirmed). Nothing tells a maintenance session to classify its changes with these verbs. The only invoker is `skill-gate:35` — a project-side skill this repo explicitly exempts itself from. Compounding it: the mother copy's `applies_to:` glob is `docs/modules/**,examples/**`, which EXCLUDES `.claude/**`, so editing this repo's own commands never auto-loads the rule. Same §9 class the applying commit was closing elsewhere. | applied sa28f661 — **Run 3 verdict: not clean → see K-8, K-17** |
| J-9 | `maintenance.md:245-272` | **The "New component creation" section is an orphan mandate.** The exemption is properly STATED (`:252`) — that half of H-9 is closed. But the Workflow line (:29) enumerates "Step 0 → read the prompt → apply changes → run the post-change checklist → commit" and never names this section, and none of the 7 checklist items reference it. Its only other mention is inside the Authorized-operations list, which is an authorization, not an invocation. Its `new component: … / none this session` report is owed by nobody — while item 5's identically shaped report is owed by the checklist. | applied sa28f661 |
| J-10 | `docs/modules/agents/skill_reviewer.md:19` | **H-7's sibling was missed.** Still reads `derived_from: skill-gate-spec v1.1 §4.2` — the same unpublished artifact (`.claude/docs/` is gitignored; `git ls-files .claude/docs/` = 0), in a SHIPPED agent template that reaches every internal-tool+ project, and it is skill-gate's own co-star component: the single most obvious sweep target. Repo-wide grep for `skill-gate-spec` returns exactly two hits — the one that was fixed and this one. | applied sa28f661 |
| J-11 | `docs/modules/skills/session-end/SKILL.md:56-72` vs `:88`, `:92` | **H-6's class was not swept into the skill with the most deferrable steps.** Steps 2-5 carry imperative verbs but NO report line, while `:88` says "Items 4-5 **can be deferred** if context window is low" and `:92` says "If severely limited: do at minimum items 2, 3, and 6". The skill therefore SANCTIONS skipping without requiring the skip to be said — the silence component-design §9.3 bans. Step 1 of the same file already models the right shape (`diff-pattern-extractor: ran — [N] \| skipped — [reason]`). | applied sa28f661 — **Run 3 verdict: not clean → see K-25** |
| J-12 | `EPA:725` | **H-12's twin.** Still says `Register in CLAUDE.md "Skills" section.` — the template's heading is `## Skills & Agents`. Class grep over every `Register in CLAUDE.md "X"` returns 3 hits: bootstrap (fixed), EPA "MCP Servers" (correct), and this one. | applied sa28f661 |
| J-13 | `EPA:405-415` | **H-21's twin.** The same 11-row rules table still uses framework-relative `examples/rules/…` while its own surrounding prose (:401, :417) says `assets/examples/rules/`. Verbatim the defect fixed in bootstrap. | applied sa28f661 |
| J-14 | `bootstrap.md:300`, `EPA:242`, `EPA:445`, `audit.md:434` | **H-18's rename did not reach the EXECUTED surfaces.** Four instances still use "inline" in the banned sense of "always copied": three in command files the AI obeys — and item 1's own principle ranks executed surfaces AHEAD of the descriptive ones that were fixed. EPA is now self-contradictory ("12 inline process skills" at :242/:445 vs "Process skills (12 lifecycle)" at :504). Worst instance: `audit.md:434` still trains the auditor to grep for `"12 inline"`, a token the repo no longer contains — **a self-check that can now only go green**, poisoned by the very rename it should detect. | applied sa28f661 — plus the audit.md self-check that could only go green |
| J-15 | `validator.md:20-21, :120, :122, :124, :149`; `docs/agentic_engineering_framework.md:943-953` | **H-4's class was not swept inside its own file.** The template now defines Coverage Gap Declaration and Items for human verification, but `produces:` (:20-21) still promises rows for **Review, Security, DB, UI** and the instructions write into them (`:120` "mark UI as ❌", `:149` "Flag as ⚠️ in Security row") — sections the template does not define. Exactly H-4, for four more categories. The framework doc's twin report template diverges in the OPPOSITE direction: it lists Review/Security/DB/UI but neither of the two new sections. | applied sa28f661 — **Run 3 verdict: not clean → see K-3** |
| J-16 | `.claude/commands/audit.md`, `CLAUDE.md:125-128`, `maintenance.md` "Audit intake" | **The run-2 verification pass — the flow that produced this report — is documented nowhere.** Executed 2+ times (`34af70b`; this session, proposed in `0954d69`'s trailer). `audit.md` has no run-mode concept: `:608-611` describes the ARTIFACT ("append a `# Run N` section") and instructs nothing about its CONTENT, and `:615-618` carries forward only still-`open` findings — **nothing instructs re-verifying findings already marked `applied sHASH`**, which is exactly what both executions did and exactly where both found defects (8/15 unswept classes on 2026-08-31; 12 of 24 today). The verdict vocabulary this run used came from the invoking prompt, not from any repo file. And the `/audit` trigger list lacks **(d) after applying an audit batch** — the trigger that has now fired twice. | applied sa28f661 — /audit Phase 0 + CLAUDE.md trigger (d) + Audit intake's verification-audit proposal (cited by NAME: it was written as 'step 6', and an insert later made step 6 a different item — /audit 2026-09-03 P-3); back-swept to framework-audit Q4 — **Run 3 verdict: not clean → see K-6, K-7, K-18, K-19** |
| J-17 | `codebase-audit/SKILL.md:87` → `code_reviewer.md:265` | **H-19's class, third instance.** Cites `code-reviewer.md` → "Periodic review", which is a bold paragraph lead-in nested under the unrelated `## Known Bug Patterns (check EVERY review)`. Same shape as the two that were promoted. | applied sa28f661 |
| J-18 | `session_rules.md:155`, `validation-orchestrator/SKILL.md:42` | **Two SHIPPED templates send a project session to a document that does not exist.** Both say "see execution protocol" for the live model-switch procedure. `find -iname "*execution*protocol*"` → empty; nothing in `.claude/commands/`, `docs/modules/templates/` or `examples/` creates such a file at bootstrap. Two further instances are `derived_from:` provenance (`validation-orchestrator:13`, `criteria_enforcer.md:13`) — the same class H-7 just fixed for skill-gate. Unlike H-7 these two are OPERATIONAL instructions, not metadata. | applied sa28f661 — pointers rewritten AND the 13-file derived_from class swept — **Run 3 verdict: not clean → see K-24, K-26** |
| J-19 | `maintenance.md:54` vs Upstream intake step 4 (`:314-316`) | **Step 0's cross-check depends on a filename convention nothing mandates.** The glob hard-codes `assets/docs/framework-evolution-upstream-*.md`, but step 4 requires only "a lineage doc under `assets/docs/`" citing the ABSORBED doc's full filename — it says nothing about the LINEAGE doc's own name. The prefix exists only as de-facto practice in two files. A session naming its record otherwise makes Step 0's cross-check silently return no hit, and the next session reads a false `pending absorption` and re-absorbs a disposed batch — the exact failure G-7 closed, one level up. | applied sa28f661 |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| J-20 | `docs/agentic_engineering_framework.md:340-372` | The pipeline diagram enumerates sub-steps `1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5` and omits the two added by `0954d69` (1.1, 1.1b). Stale enumeration created by that commit. | applied sa28f661 |
| J-21 | `bootstrap.md` Step 15 | Every other reporting step has a Step 15 line (risk profile :728, hooks :742, MCPs :745, plugin :766, commit :769); the two NEW steps' mandated reports (framework freshness, cross-cutting routing) appear nowhere in the final report. | applied sa28f661 — **Run 3 verdict: not clean → see K-9** |
| J-22 | `README.md:22,24` and `:159-181` | The diagram's OUTPUT column claims a bootstrapped project ends with "15 process skills" and "5 rules files" — those are tier-gated MAXIMA; the baseline is 12 skills + 3 rules. The same README gets it right at :170 and :206. The "What Bootstrap Creates" table also omits `.claude/phases/done_tasks.md`, created unconditionally at `bootstrap.md:204`. | applied sa28f661 — **Run 3 verdict: not clean → see K-16, K-22, K-23** |
| J-23 | `docs/modules/skills/README.md:34` | "sprint-proposer/session-end are user-invoked; others are called by orchestrating skills" — 7 skills declare `invocation: user`. Stale in the very file the H-17/H-18 fixes touched, 26 lines below the edited line. | applied sa28f661 |
| J-24 | `CLAUDE.md:82` | The `assets/docs/` tree comment says "Framework-base source notes (upgrade lineage, not copied to projects)"; the folder now also holds the persisted audit reports, a fact `CLAUDE.md:123` states explicitly. Same stale-prose-comment class as H-10. | applied sa28f661 |
| J-25 | `code_reviewer.md:210` vs `:248` | "**ALWAYS** include this section in every Code Review Report" vs "**If none of the above apply: omit** Coverage Gap Declaration". Pre-existing (from `fd94f5c`), not introduced by `0954d69`. `security_reviewer.md:281` resolves the same tension correctly ("omit … set field to `None`") and `code_reviewer.md:41` already declares the field as `[None \| list of gaps]` — so the omit line is the wrong half. | applied sa28f661 — **Run 3 verdict: not clean → see K-12** |
| J-26 | `docs/modules/skills/README.md:8-9` | The H-18 gloss was inserted in front of the pre-existing breakdown rather than merged, leaving two consecutive parentheticals. Numbers are right; it reads awkwardly. | applied sa28f661 |
| J-27 | `examples/README.md:150-153` | The H-16 exempt list names 5 skills and omits `api-design-patterns`, which is a process-pattern skill, not a stack skill. It ships `## Testing` anyway so nothing is out of compliance — but its class is left undeclared by the new taxonomy. | applied sa28f661 |
| J-28 | `maintenance.md:233,235` | The bump-semantics examples still use `v2.5.0→v2.6.0` and `v2.6.0→v2.6.1`, three versions stale. Illustrative, not a mismatch. | applied sa28f661 |
| J-29 | `validation-orchestrator/SKILL.md:53-57` vs `:61-63` | H-14 added a third home for the same ownership mandate in one file — it restates rather than points (§9 "one home per mandate"). No contradiction. | applied sa28f661 — **Run 3 verdict: not clean → see K-14** |
| J-30 | `.claude/commands/audit.md:240` | The gap-domain list ("concurrency, performance, accessibility, data-integrity, secrets, compliance, etc.") omits visual regression. Hedged with "e.g./etc." so it is not a count claim — but it is the prompt that seeds this audit, and it steers the reader away from the gap that has now broken twice. | applied sa28f661 |

---

## Summary matrix (Part 2 — the 17 dimensions)

| # | Dimension | Agent | Status | Finding |
|---|-----------|-------|--------|---------|
| A | Dual placement | 1 | PASS | 15 module skills; `.claude/skills/` holds only `cross-cutting-analysis`, byte-identical to its module copy |
| C | Instruction style | 5 | **FAIL** | 1 systemic instance → J-11 (`session-end` sanctions a silent skip). `autonomous-loop` rated strongest file in the repo |
| E | Version consistency | 5 | PASS | v2.9.0 on 3/3 canonical surfaces; 17 lineage fields intact; 0 rewritten by the bump |
| D4 | Skills README vs disk | 1 | PASS | 15 = 15, names 1:1, sub-breakdown sums to 12; one stale prose line → J-23 |
| D5 | Bootstrap file refs | 2 | PASS | 51/51 paths resolve; 8/8 sed fences extract non-empty; 0 naming violations |
| D6 | Gap vocabulary | 3 | **FAIL** | 10/10 phrases exact — but the INSTALL link is 9/10 on the EPA path → J-5 |
| D7 | Pushy Description | 3 | PASS | 20/20 specialists carry all five elements; 0 anti-pattern instances |
| D8 | Orchestrator refs | 4 | PASS | All skill/agent names resolve; `LOOP CONTINUATION — active` byte-identical across 4 sites |
| D9 | CLAUDE.md diagram | 1 | PASS | Every path resolves; every count correct; one stale comment → J-24 |
| D10 | Command → skill paths | 4 | PASS | `cross-cutting-analysis` in both required locations, copies byte-identical |
| D11 | Bootstrap sequencing | 2 | **FAIL** | Non-monotonic numbering → J-1; one genuine forward reference → J-3 |
| D12 | Examples conventions | 5 | PASS | 0 violations across the sampled set; H-16's qualification leaves 9/9 compliant |
| D13 | PRD ↔ bootstrap compat | 2 | PASS | Every cited PRD section exists; defined-but-unread down from 3 to 2 (H-23 closed the material one) |
| D14 | Bootstrap output | 2 | **WARN** | 14/14 template refs traced; reverse direction finds `project.md` and 2 rules files never pointed to from the generated CLAUDE.md (pre-existing) |
| D15 | Doc factual accuracy | 5 | PASS | 23 of 24 numeric claims correct; 1 imprecision → J-22 |
| D16 | Project-info isolation | 1 | PASS | 4 surfaces + **105 commits**: zero hits. The new report file carried zero identifiers inward |
| D17 | Process coverage (meta) | 6 | **FINDINGS** | 2 → J-16 (run-2 mode undocumented), J-19 (lineage filename convention unowned). All aspirational mechanisms now owned |

---

## Recommended fixes

### 1. Undo the damage this application caused (do first — these are regressions)
`J-1` renumber (a pre-PRD gate is Step 0.5, not 1.1; the cross-cutting step is not a `b` sibling) ·
`J-2` make the freshness check fail CLOSED on a missing tracking segment ·
`J-3` add a receiver clause in Steps 3/4/13, or move the routing to where the targets exist ·
`J-4` move the three universal blocks back out of the conditional subsection ·
`J-6` restore bullet 1 to a default, or fold both into one imperative ·
`J-7` name both output-heading spellings, or drop the "verified 20/20" stamp to what was measured.

### 2. Finish the sweeps that stopped at the first instance
`J-5`, `J-12`, `J-13` — all three are `existing_project_adaptation.md`, the sibling command twin
item 4 names explicitly. `J-10` (skill_reviewer), `J-14` (executed surfaces), `J-15` (validator's
own file), `J-17` (codebase-audit citation), `J-11` (session-end report lines).

> Six of seven unswept classes point at the SAME twin or the SAME file as the fix. The sweep did
> not fail for lack of a rule — item 4 names both directions. It failed because nothing made the
> sweep's result checkable per fix.

### 3. Close the two process gaps
`J-16` — give `audit.md` a Run-mode section, add trigger (d) to `CLAUDE.md`, and point to it from
Audit intake step 4. `J-8` and `J-9` — give RELOCATE and New-component-creation an invoker inside
the checklist; both are the §9 class the applying commit was closing elsewhere.

### 4. Quick fixes
`J-18` (dangling execution-protocol pointers — decide: create the doc, or rewrite both pointers),
`J-19`, `J-20`, `J-21`, `J-22`, `J-23`, `J-24`, `J-25`, `J-26`, `J-27`, `J-28`, `J-29`, `J-30`.

---

## Meta-observation

The 2026-08-31 run named the recurring class as *"a fix applied to the instance the report named,
without sweeping the class"* and answered it with checklist item 4. This run shows item 4 is
necessary and not sufficient, in two distinct ways:

1. **Item 4 has no per-fix receipt.** It mandates a report — `class sweep: N directions checked, M
   extra instances found` — but that is ONE aggregate line for a whole session. The applying
   session emitted `4 directions, 3 extra instances` in good faith and still missed seven,
   because a single aggregate cannot be wrong per fix. Compare the discipline the framework
   already applies elsewhere: `autonomous-loop`'s task-closure check runs at EVERY task boundary,
   not once per session.
2. **Nothing verifies that a correction LANDED.** Five of this run's HIGH/MEDIUM findings are
   defects the fixes themselves introduced (J-1..J-4, J-6, J-7). Every one passes a "is the
   required text present?" check; only reading the surrounding structure finds them. Audit intake
   step 2 mandates re-verifying a finding BEFORE applying it and nothing AFTER.

Both gaps are instances of J-16: the verification pass exists as a practice and not as an
instruction. Writing it down is the single highest-leverage fix in this report.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.
Group 1 first — those are regressions currently on `main`.

---

## Application ledger

**Applied:** 2026-09-02, commit `a28f661` — **30 of 30 findings applied, 0 deferred, 0 rejected.**
Framework version bumped `v2.9.0` → `v2.10.0` (MINOR).

Extra work beyond the named instances, per checklist item 4's class sweep and item 5's back-sweep:
- **J-18 class sweep:** **14** `derived_from:` fields (not the 2 the finding named) cited unshipped
  framework-reference sections; all now say so explicitly.
- **J-16 back-sweep:** the verification-mode rule was applied backward to `framework-audit` Q4 as
  the DID-IT-LAND check, with a matching field in that skill's report template.
- **J-14 descending sweep:** `audit.md`'s D15 check now also greps for the BANNED sense of
  "inline", so the rename cannot poison its own detector again.
- **J-2 negation proof:** run against a repo with a remote and no tracking branch — the state that
  printed a bare `## main` and was read as healthy. New check goes RED there.

**Next per CLAUDE.md trigger (d) + (b):** a `verification`-mode `/audit` over `a28f661` —
PROPOSED to the owner, not yet run.

---

**Report status:** `status: COMPLETE` — all 6 agents returned; all 17 dimensions evaluated plus a
Part 1 verification pass over all 24 applied findings.
**Application status:** `status: APPLIED` — 30 of 30 applied in `a28f661`; 0 open.


---

# Run 3 — verification pass over `a28f661`

**Date:** 2026-09-02
**Framework version:** v2.10.0 (`README.md:1`), HEAD `e3aaa37`
**Run mode:** `verification (over a28f661)` — CLAUDE.md trigger (d), and trigger (b) since the bump was MINOR
**Under verification:** the 30 findings applied in `a28f661` (report: Run 2 above)
**Dimensions checked:** 17 + a Part 1 fix-verification pass in every agent
**Agents dispatched:** 6
**Trigger:** owner request, following the proposal recorded in Run 2's application ledger.

**carried: none** — Run 2 closed with 0 `open` (30/30 applied in `a28f661`).

> **ID scheme.** `F-*` (2026-08-31 run 1), `G-*` (run 2), `H-*` (2026-09-01), `J-*` (2026-09-02
> run 2). This run uses `K-*`. Verification verdicts on the J-series keep their J IDs and gain a
> pointer to the new K ID, per Phase 3 item 5.

---

## Part 1 — Verification ledger for `a28f661`

| ID | Verdict | Evidence |
|----|---------|----------|
| J-1 | **CONFIRMED-FIXED** | Both files monotonic end to end. `bootstrap.md` 0.5(:37)→1(:76)→1.1(:93)→1.2(:120)→1.5(:161)→…→15(:714), no duplicates. EPA 0.5(:39)→Phase 1(1.0-1.5)→…→5.2. Repo-wide grep for `1.1b`/`1.0b` returns only this report's prose. `CLAUDE.md:99-100` cites Step 0.5 for both. |
| J-2 | **PARTIALLY-FIXED** | The named 3rd state is closed and the block is byte-identical in both twins (`diff` of `bootstrap.md:41-61` vs `EPA:41-61` → empty). Two states still escape → **K-1**. |
| J-3 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Receivers exist and are correctly worded (`:209-212`, `:228-231`, `:617-621`); sender defines R/A/T at `:112-113`; Step 15 re-reports at `:723-727`. But two of the three are nested inside a conditional branch → **K-2**. |
| J-4 | **CONFIRMED-FIXED** | `skill-gate/SKILL.md` `## 5. Promote` read end to end (:103-162). `### In sprint-approved mode` (:125) now contains ONLY the deferral rule (:127-133); the two new `###` headings (:135, :157) are correct-level siblings. Diff +9/-3, the 3 deleted lines re-expressed verbatim-plus. Nothing orphaned, no content lost. |
| J-5 | **CONFIRMED-FIXED** | `diff` of `bootstrap.md:566-577` vs `EPA:763-774` → empty. 10 rows each; the fallback clause lands in the same structural slot on both paths. Offer → install → fallback is now a closed loop on both. |
| J-6 | **CONFIRMED-FIXED** | `sprint-proposer:143-148` — 4 bullets → 3; the contradictory `NEVER exceed` bullet is gone (diff `-`), folded into one imperative with 3-5 as default and 7/1 as the only sanctioned departures. |
| J-7 | **CONFIRMED-FIXED** (independently re-measured) | 20/20 files have exactly one `^## Input$`, and in 20/20 the immediately-following heading is the output heading, zero headings between. Split measured: `## Output` = 10, `## Output Format` = 10 — matching the README's stated numbers exactly. |
| J-8 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Item 8 sits inside the list (`:222-237`, H2 at `:249`); the governing sentence covers it; Workflow names it; §5 exists in both twins; `applies_to:` now includes `.claude/**`. But its self-check is red-by-construction → **K-8**; and the same edit re-externalized the version bump → **K-17**. |
| J-9 | **CONFIRMED-FIXED** | Item 9 at `:239-247`, inside the list, before the `:249` H2; Workflow names it; the report is owed ONCE ("ALWAYS EMIT its report line **here**"). Two LOW residues → **K-34**. |
| J-10 | **CONFIRMED-FIXED** | `skill_reviewer.md:19` — `yaml.safe_load` over the frontmatter succeeds; `name:` intact. Repo-wide `skill-gate-spec` grep → 1 hit, now self-describing. All 30 agent files parse. |
| J-11 | **PARTIALLY-FIXED** | Exactly 6 report-line templates for 6 steps (`:50, :62, :69, :76, :84, :100`); the self-check's "expected exactly 6" matches reality; placement is reachable and overrides the deferral framing. But steps 4-5 lack the archetype-N/A verdict → **K-25**. |
| J-12 | **CONFIRMED-FIXED** | `EPA:746` = `Register in CLAUDE.md "Skills & Agents" section.` Matches the template heading and `bootstrap.md:462`. |
| J-13 | **CONFIRMED-FIXED** | `EPA:424-436` — all 11 rows now `assets/examples/rules/…`, consistent with surrounding prose at `:422` and `:438`. |
| J-14 | **CONFIRMED-FIXED** | Repo-wide grep for the banned sense → **zero** hits. `audit.md:489` now greps FOR it (detector unpoisoned). Both derived claims re-measured against disk frontmatter: `bootstrap.md:360` 7 `inline`/5 `user` = exact; `skills/README.md:38` 7 `user`/8 `inline` = exact. |
| J-15 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | All 9 `produces:` categories now exist in the template (Review :58, Security :62, DB :78, UI :82); both citers resolve; nothing re-parented. But the twin-sync rule the fix shipped is false at birth → **K-3**. |
| J-16 | **PARTIALLY-FIXED — INTRODUCED FOUR DEFECTS** | Phase 0 (`audit.md:26-74`), trigger (d) (`CLAUDE.md:126-139`) and Audit-intake step 6 (`maintenance.md:326-331`) exist and agree on mode names and trigger letter; Phase 0 does not contradict Phase 3 (`:72-74` + new item 5). But → **K-6** (no report-template field), **K-7** (fabricated statistic), **K-18** ("scheduled net"), **K-19** (unexecutable absorption row). |
| J-17 | **CONFIRMED-FIXED** | `code_reviewer.md:268` is a real `###` heading; `codebase-audit/SKILL.md:87` cites it byte-exactly. The "this agent DEFINES them" clause preserved at `:270`. |
| J-18 | **PARTIALLY-FIXED** | Zero `see execution protocol` pointers survive on any framework surface. `session_rules.md:155`'s two new citations both resolve exactly (`project-md-updater:37`, `sprint-proposer:77`). All rewritten frontmatter parses (0 failures over 60 components + 10 fenced templates). But one new citation is inexact → **K-24**, and the stated count was wrong → **K-26**. |
| J-19 | **CONFIRMED-FIXED** | `maintenance.md:58` globs `framework-evolution-upstream-*.md`; step 4 `:351-352` mandates the same prefix — patterns match. Both existing records conform. |
| J-20 | **CONFIRMED-FIXED** | The pipeline diagram enumerates `0.5, 1, 1.1, 1.2, 1.5, …, 14.5, 15`; cross-checked against every `### Step` header in `bootstrap.md` — 1:1, monotonic. |
| J-21 | **FIXED-BUT-CLASS-NOT-SWEPT** | Both lines landed in bootstrap (`:719-721`, `:723-727`). EPA's final report (`:877-938`) has no freshness line at all → **K-9**. |
| J-22 | **FIXED-BUT-CLASS-NOT-SWEPT — INTRODUCED A DEFECT** | `README.md:22/:24` ranged correctly, footnote `:32-36` rewritten, `done_tasks.md` row added and it resolves. But `README.md:153` carries the identical claim unswept → **K-16**; `README.md:21` likewise → **K-22**; and the new row coined a third sense of "inline" → **K-23**. |
| J-23 | **CONFIRMED-FIXED** | `skills/README.md:38` truth-tested against every SKILL.md on disk: `user` = 7, names 1:1; `inline` = 8. Sits under the correct numbered item; class swept (grep for the old phrasing returns only this report). |
| J-24 | **CONFIRMED-FIXED** | `CLAUDE.md:82-84` matches disk exactly (`assets/docs/` holds precisely the three named classes). The 3-line continuation stays inside the fence and no tree glyph is broken. |
| J-25 | **FIXED-BUT-CLASS-NOT-SWEPT — INTRODUCED A DEFECT** | `code_reviewer.md` is now internally consistent (grep "omit" → 1 hit, inside "NEVER omit"). But the sibling still carries the banned line, and the fix cites it as the model → **K-12**. |
| J-26 | **CONFIRMED-FIXED** | `skills/README.md:8-13` genuinely merged into one sentence with the breakdown inline (6+3+1+1+1 = 12) and the `invocation:` gloss explicitly decoupled. |
| J-27 | **CONFIRMED-FIXED** | Exempt list names six skills including `api-design-patterns`. Measured: 5 skills lack `## Testing`, 3/3 stack skills carry it, `api-design-patterns` carries it while exempt — consistent. |
| J-28 | **CONFIRMED-FIXED** | `maintenance.md:264` = v2.9.0→v2.10.0 (MINOR), `:266` = v2.10.0→v2.10.1 (PATCH). Both current. |
| J-29 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | The restatement is gone and it now points. But the target is a bold paragraph lead-in, not a section → **K-14**. |
| J-30 | **CONFIRMED-FIXED** | `audit.md:292-294` names visual regression with its rationale. But the hint was fixed and the CHECK was not → **K-5**. |
| VERSION | **CONFIRMED-FIXED** | v2.10.0 on 3/3 canonical surfaces (5 occurrences). `git show a28f661 --unified=0 \| grep '^[-+]created:'` → **no output**: zero of the 17 lineage fields rewritten. No stale v2.9.0 outside the (now-current) bump example. |

**Score, counted over the FINDINGS (never over the row total — see K-7): 19 of 30 clean, 11 not
clean.** The ledger has 31 rows because `VERSION` is also verified (and is clean), so the overall
row score is 20/31. Of the 11 not-clean findings: **3 partially-fixed**, **3 class-not-swept**,
**7 introduced-a-defect** (J-22 and J-25 fall in both of the last two).

---

## New findings

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| K-1 | `bootstrap.md:47-61`, `EPA:47-61` | **The fail-closed freshness check has no catch-all, and the fork topology it names reads GREEN.** Two states escape the four-row table. (1) `git fetch` fails (network/auth/dead remote): `git fetch && git status -sb` short-circuits, (c) produces NO line, which matches neither row 3 nor row 4 — the state is undefined, not RED. (2) On a **fork**, `@{upstream}` resolves to the fork's own `origin/main`, so (b) is non-empty, the tracking segment is present and no `behind` appears → verdict `up to date` while the clone is arbitrarily behind the real upstream. `git remote -v \| head -1` cannot even see an `upstream` remote. The block's OWN remediation sentence (`:66`) names the fork case, so it contradicts itself. The 2026-09-02 negation proof tested only the no-upstream path. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-7, M-8** |
| K-2 | `bootstrap.md:209-212`, `:228-231` | **Two of the three cross-cutting receivers are nested inside the `**If it does not exist:**` branch.** Step 3's receiver is item 6 of the list opened at `:202`; Step 4's is item 6 of the list at `:222`. On the documented pre-existing-docs path (`:200`, `:220` — "Do NOT overwrite"), neither is reached, so no Architectural Decisions row and no pendencias task is written — while Step 15 `:725-727` still demands `A/A · T/T` and calls a shortfall RED. Step 13's receiver (`:617-621`) is correctly a top-level paragraph. Verbatim the J-4 class, one commit later. Introduced by `a28f661`. | applied safccff3 |
| K-3 | `docs/agentic_engineering_framework.md:955-956` vs `validator.md:19-21` | **J-15's fix shipped a twin-sync rule that is false at birth.** It mandates "a category added to either MUST be added to both, and to the validator's `produces:` field". Three violations exist immediately: (1) five categories are in BOTH templates but absent from `produces:` — Criteria Results, Prior Review Findings, Coverage Gap Declaration, Items for human verification, Overall (the last two added by this very commit); (2) `Validation: ✅/❌/⏭️` is in the framework doc and in neither of the others, and structurally cannot be in the validator (the validator IS the validation); (3) the declared twins carry divergent verdict vocabularies — Security `✅/⚠️/❌/⏭️` vs `✅/❌/⏭️`, UI with `BASELINE-CREATED` vs without — while `produces:` still says "✅/❌/⏭️ per category", which cannot express `⚠️` or `BASELINE-CREATED`. Introduced by `a28f661`. | applied safccff3 — **Run 4 verdict: not clean → see L-6** |
| K-4 | `validator.md:99-104`, `:143` → `validation-orchestrator:109-118` | **The validator's Coverage Gap Declaration is a forward write with no receiver.** The section asserts "Main Claude reads this section and spawns the specialist", but the only component that performs that read opens with "After receiving the code-reviewer and security-reviewer reports" and routes results "as additional evidence for the validator" — i.e. it runs BEFORE the validator, on reviewer reports only. The validator is spawned last. So the one gap the framework credits to the validator (`visual regression` — the domain whose install link has now broken twice) is declared into a report nobody is instructed to re-read. Same J-3 class, pre-existing. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-18..M-21** |
| K-5 | `.claude/commands/audit.md:300-313` | **D6 still has no install-link check; J-30 patched the hint, not the detector.** D6.1–D6.6 extract gap phrases, match them to specialist descriptions, and report broken links and orphans. **Nothing checks that a gap with a specialist also has an install row in `bootstrap.md` AND `existing_project_adaptation.md`** — precisely what failed as H-2 (bootstrap path) and again as J-5 (EPA path). D6 returned PASS on 2026-08-31 and only failed on 2026-09-02 because the invoking prompt added D6.7/D6.8. The check that works lives in the prompt, not the repo. | applied safccff3 — **Run 4 verdict: not clean → see L-3, L-11** |
| K-6 | `.claude/commands/audit.md:610-640` | **Phase 2's report template has no field for what Phase 0 and Phase 3 now mandate.** Phase 0 `:70-72` requires the merged report to OPEN with a Part 1 ledger + score line; Phase 3 item 4 requires a `carried:` line; Phase 3 item 5 requires the ledger persisted. The consolidated format runs header → `## Summary` → `### Detailed Findings` with no ledger row, no score line, no `carried:` field — and none of the 6 per-agent report formats gained a Part-1 verdict slot. This is checklist item 4's own DESCENDING direction, and the same session DID run it for the project-side twin (`framework-audit/SKILL.md:120` gained `did-it-land:`), so it is a miss, not a rule gap. Introduced by `a28f661`. | applied safccff3 — **Run 4 verdict: not clean → see L-2, L-10** |
| K-7 | `CLAUDE.md:136`, `maintenance.md:329`, `audit.md:29-30` | **The justification for trigger (d) states counts no cited record supports.** "found problems in **8 of 15** and **12 of 24** applied findings" — but `audit-2026-08-31.md:91` reads "**13 of 15 CONFIRMED-FIXED, 2 PARTIALLY-FIXED. Zero regressions.**" There is no 8. The 12/24 figure is also credited to the "first documented execution" while the next sentence says it was executed twice — 12/24 was the second. It further inherits an unresolved self-contradiction in Run 2 above (`:25` "11 clean, 13 not" vs `:61` "12 clean · 7 · 5"), whose ledger has 26 rows because it counts `VERSION` and the carried `G-20` among the 24. Load-bearing: this is the stated reason trigger (d) exists, now stamped into two executed surfaces. Introduced by `a28f661`. | applied safccff3 — **Run 4 verdict: not clean → see L-4** |
| K-8 | `maintenance.md:232-234` | **The RELOCATE self-check is RED-by-construction.** It states "expected: exactly ONE definition plus N pointers; **two definitions is RED**" — but this repo's own dual-placement rule (`CLAUDE.md:39-45`) REQUIRES twinned rules/skills to exist in both `docs/modules/` and `.claude/`. Relocating any section inside `component-design`, or in any twinned skill, legitimately returns two definitions. A control that fires on the normal case is disabled by its first user — the inverse of the "can only go green" class, equally decorative. Compounding: item 6 `:200-205` mandates "ALWAYS PROVE any self-check you WRITE this session by NEGATION", and this check shipped unproven (the session negation-proved J-2's check only). Introduced by `a28f661`. | applied safccff3 |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| K-9 | `EPA:877-938` | **The adaptation report has no freshness line — J-21's unswept twin.** `EPA:69-71` mandates "ALWAYS REPORT one of the four verdict strings … NEVER emit nothing" for Step 0.5, and Step 1.0 (`:80-96`) mandates a project-copy check whose result is likewise never reported. Neither the Reading Report (`:174-210`) nor the final report carries a slot for either verdict. Bootstrap's Step 15 got both lines; the twin got neither. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-1, M-8, M-53** |
| K-10 | `EPA:422`, `:438` | **Phase 2 reads `assets/examples/rules/`, which Phase 4 Step 4.1 copies.** Step 2.7.1 checks for and copies from a directory that only lands in the project at `:623-633`, two phases later. For a never-bootstrapped project (EPA's stated audience) every check silently returns no-match and the step no-ops without saying so. `:332` hedges the same dependency for Steps 2.4-2.8 with "If … exists"; 2.7.1 has no such hedge. Bootstrap's ordering is correct (1.5 copies, 13 consumes). Pre-existing. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-9** |
| K-11 | `EPA:271`, `:314`, `:334`, `:364` | **EPA Steps 2.2-2.5 have no "if it does not exist, create from template" branch.** Those four paths (`project_md.md`, `pendencias_md.md`, `code_reviewer.md`, `security_reviewer.md`) appear **zero times** in EPA. Steps 2.6.1/2.6.2 model the correct shape. Yet EPA's own Reading Report anticipates the missing case (`:190-191`), and Steps 4.6.5 (`:761`) and 5.1 (`:852`) unconditionally read `.claude/agents/code-reviewer.md`. An adaptation of a project with no reviewer agent ends with a broken activation-chain check and no creation path. Pre-existing. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-10** |
| K-12 | `security_reviewer.md:281` | **J-25's class was not swept, and its fix cites a false authority.** The sibling still reads "**If none of the above apply:** omit Coverage Gap Declaration from the report (set field to `None`)" — the banned instruction verbatim, self-contradictory (you cannot omit a section AND set its field), and in conflict with `:222` "Include this section in every Security Review Report". `:39` already declares the field as `[None \| list of gaps]`, so the omit half is the wrong half — exactly as in code-reviewer. The applied fix's own text asserts "(`security_reviewer.md` resolves the same tension the same way.)" — it does not. Downstream, `validation-orchestrator:111` still sanctions the absence path ("skip only if absent"). | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-15** |
| K-13 | `.claude/rules/component-design.md:12`, `:63`; `docs/modules/rules/component_design.md:19`, `:70` | **§1/§3 still enumerate only two gap sources, in BOTH twins.** Both say "Reviewer agent (code-reviewer, security-reviewer)". The validator is now a third source (`validator.md:143`), and `visual-regression-tester.md`'s description already says "code-reviewer **or validator**". The policy that D6 audits against does not describe the architecture the repo ships. Fix must land in both twins. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-16** |
| K-14 | `validation-orchestrator/SKILL.md:61-64` → `:53` | **J-29's fix points at something that is not a section.** `:63` cites §"Ownership of 'Before Implementing' in loop mode"; `:53` is a **bold paragraph lead-in nested inside `### 2. Classify and route`**, with no heading of its own — verbatim the H-19a / J-4 / J-17 class that the SAME commit fixed by promoting bold lead-ins to `###` in three other files. It also dropped the previously-valid `autonomous-loop` §3a cross-link (that heading exists at `autonomous-loop:142`). The mandate itself is intact at `:53-57`. Introduced by `a28f661`. | applied safccff3 — **Run 4 verdict: not clean → see L-22** |
| K-15 | `.claude/commands/audit.md:359-424` | **Agent 4's contract is materially narrower than what the run executes.** (1) D8.3 mandates reading `autonomous-loop/SKILL.md`, which is **not in FILES TO READ**. (2) No `skill-gate`/`codebase-audit` in FILES TO READ, and no check for the codebase-audit → code_reviewer heading citation — the check that caught J-17. (3) **No cross-section-citation check at all** — the check that finds K-14 and K-24, i.e. the only currently-failing class in this dimension. (4) D10.4 says only "verify it ALSO exists", yet Run 2's D10 row asserts "copies byte-identical". (5) No check for `maintenance.md`/`audit.md` cross-file citations. (6) The REPORT FORMAT block has no Part 1 ledger slot though Phase 0 mandates one. Same class as K-5 and K-6. | applied safccff3 — **Run 4 verdict: not clean → see L-3** |
| K-16 | `README.md:153` | **J-22's class survives 12 lines below its own fix, in the same file.** "The **15 process skills**, 10 agent templates, and **5 rules templates** live under `docs/modules/` as templates **that get copied into bootstrapped projects' `.claude/`**" — the identical tier-gated-maximum-as-flat-fact claim the fix corrected at `:22/:24`. Baseline is 12 skills + 3 rules; the same README states this correctly at `:34`. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-40** |
| K-17 | `maintenance.md:31-33` | **The Workflow-line rewrite re-externalized the version bump.** It now reads "…run the post-change checklist … **then apply the version bump ("Version bumps") and commit**". Item 7 (`:207-211`) exists precisely because "the bump lived as an H2 outside these numbered items, so the governing line above never covered it". The Workflow line now restores the shape item 7 was created to abolish. Introduced by `a28f661`. | applied safccff3 |
| K-18 | `.claude/commands/audit.md:39` | **Phase 0 adds "a scheduled net" as a baseline trigger, with no owner and in direct contradiction of `CLAUDE.md:129`** ("ALWAYS one of these events, **never a remembered interval**"). No scheduled trigger exists in CLAUDE.md's list. Either delete it from Phase 0's table or add it to CLAUDE.md with an owner. Introduced by `a28f661`. | applied safccff3 |
| K-19 | `.claude/commands/audit.md:41` | **Phase 0's upstream-absorption row is unexecutable.** It mandates "verification over that absorption's commit", but verification mode is DEFINED only over an audit report's `applied sHASH` findings (`:56-57`) — an absorption has neither a report nor applied findings. `CLAUDE.md` trigger (a) says nothing about a verification pass either. Introduced by `a28f661`. | applied safccff3 |
| K-20 | `.claude/commands/audit.md` Phase 3 | **Committing the persisted audit report is executed every run and instructed nowhere.** All three reports entered git via a LATER session's commit, never the audit session that wrote them: `audit-2026-08-31.md` via `20ac487`, `audit-2026-09-01.md` via `0954d69`, `audit-2026-09-02.md` via `e3aaa37` — and this session's opening `git status` showed `?? assets/docs/audit-2026-09-02.md`, i.e. the report crossed a session boundary as an untracked file. `grep -n "commit" audit.md` → zero commit instructions; Phase 3 mandates writing, never persisting to git. A `git clean` or fresh clone between audit and maintenance loses the entire 6-agent output. Natural home: a Phase 3 item that commits the report (cited by NAME: written as 'item 6', later inserts moved it — /audit 2026-09-04 Q-11) plus the matching authorized-operations line. | applied safccff3 — **Run 4 verdict: not clean → see L-5, L-9** |
| K-21 | `maintenance.md:133-147` | **Item 4 still has no per-fix receipt — gap (1) of Run 2's meta-observation is NOT closed.** Item 4 is byte-unchanged by `a28f661` (its hunks touch only `:26`, the items 8/9 insert, `:230`, `:292`, `:311`); its report remains the single aggregate `class sweep: N directions checked, M extra instances found`. **This batch reproduced the exact failure it names:** the DESCENDING direction was run for the shipped `framework-audit` template and skipped for `/audit`'s own report template (K-6) — a miss that a per-fix receipt (`J-16: LATERAL ✓ / PARALLEL ✓ / ADJACENT ✓ / DESCENDING ✗`) would have made visible, and that an aggregate line structurally cannot express. Highest-leverage remaining fix in this dimension. | applied safccff3 — **Run 4 verdict: not clean → see L-17, L-23** |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| K-22 | `README.md:21` | The OUTPUT column's third row still says "10 agent .md files" — the same unranged maximum J-22 fixed in the two rows below it. `bootstrap.md:499` makes red-team/blue-team conditional on PRD security signals; `:405-408` gates `skill-reviewer` at internal-tool+. A `prototype` with no security signals receives **7**. The README gets it right at `:171-172` and `:180`. | applied sf2fcccf (carried from Run 3) |
| K-23 | `README.md:166` | The `done_tasks.md` row coined a **third sense of "inline"** — "(created inline by bootstrap Step 4)" — in the same commit that added the banned-sense detector (`audit.md:489`) and that ships `bootstrap.md:360`'s "never use one word for both". Not the banned sense, so no contradiction; it is vocabulary drift on a term the repo had just disambiguated. Introduced by `a28f661`. | applied safccff3 |
| K-24 | `validation-orchestrator/SKILL.md:42` | The J-18 rewrite cites `session-rules.md` §"Reasoning depth"; the real heading is `## Reasoning depth mechanisms (complementary)` (`session_rules.md:151`). Resolves only by substring. The same line cites §"MODEL SWITCH entries" correctly. | applied sf2fcccf (carried from Run 3) |
| K-25 | `session-end/SKILL.md:76`, `:84` vs `:31-34` | J-11's new templates for steps 4-5 offer only `ran` / `deferred — [reason]`, but the archetype branch makes those steps **N/A** (not deferred) for `framework-maintenance` and `ops`. Step 1 (`:50-51`) already models the missing third verdict (`skipped — [archetype] makes it N/A`). Under the 6-line count self-check the executor must mislabel an archetype N/A as a deferral. | applied sf2fcccf (carried from Run 3) |
| K-26 | `a28f661` trailer, Run 2's ledger `:199` | The commit and ledger state "**13** `derived_from:` fields"; `git show a28f661 --unified=0 \| grep -c '^+derived_from:'` → **14**. Substantively complete (all 14 parse; 0 failures over 60 components + 10 fenced templates); only the stated number is wrong. | applied sf2fcccf (carried from Run 3) |
| K-27 | `validation-orchestrator/SKILL.md:81` | "Commit implementation before validation. For routine tasks using inline validation, commit **can be deferred** until after Phase B" — sanctions a deferral with no obligation to say so (component-design §9 rule 3). This is the Phase A implementation commit, a DIFFERENT commit from the Step 3 checkpoint at `:66-70`, whose report line does not reach it. Verbatim the J-11 class, in a file `a28f661` edited. | applied sf2fcccf (carried from Run 3) |
| K-28 | `docs/modules/skills/commit/SKILL.md:19-46` | All six process steps are descriptive: no CAPS imperative anywhere in the file, no explicit output format, no report line on any step — the only §6-conformant construct is the bare "STOP" at `:27`. It ships to every project as an `invocation: user` lifecycle skill whose steps must execute every time. The weakest of the 15 module skills against the repo's own §6, in direct contrast to its sibling `session-end`. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-47** |
| K-29 | `examples/README.md:86` vs `:161` | The header claims the conventions are "exhibited by **every** example — verify, do not assume", but the rules bullet "Checklists for new entities" holds in **6 of 11** rules examples; `auth-rules`, `compliance-rules`, `distributed-systems-rules`, `e-commerce-rules` and `i18n-rules` carry no checklist in any form. Same shape as the G-20/J-7 class one level over: a universal quantifier on a strong tendency. Cheapest correction: hedge the bullet, as `:121-123` and `:137-140` already hedge their variances. | applied sf2fcccf (carried from Run 3) |
| K-30 | `.claude/commands/audit.md:288-290` | Agent 3's FILES TO READ enumerates "All other files in `docs/modules/agents/` (validator, arbitrator, red_team, blue_team, criteria_enforcer, prd_sync_checker, diff_pattern_extractor)" — **7 of 9**; `skill_reviewer.md` (shipped since v2.4.0) is omitted. Stale enumeration in the file J-30 just edited. | applied safccff3 |
| K-31 | `code_reviewer.md:238-239` | The visual-regression gap block is the only one of the five that omits the sibling closer "*and invoke before validator if found*" (present at `:218`, `:225`, `:233`, `:246`). Arguably intentional — the validator also declares this gap, so "before validator" is wrong for it — but the divergence is undeclared, and component-design §3 names the closer as part of the reviewer-layer key phrase. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-17** |
| K-32 | `docs/agentic_engineering_framework.md:50`, `:336` | "15 primary steps plus sub-steps" — Step 0.5 is neither a primary step (primaries are 1-15) nor a sub-step of any parent; there is no Step 0. The diagram lists it correctly (`:344`), so the enumeration is complete; only the prose's binary classification is imprecise. Introduced by `a28f661`'s renumber. | applied safccff3 |
| K-33 | `EPA:895-899`, `:843-844` | Two stale sub-lists in one file: the report header says "Process skills: [N of **12** copied]" but the breakdown beneath names only **9** (missing `autonomous-loop`, `cross-cutting-analysis`, `commit`), though `:525` and `:838` list all 12 correctly; and the Step 5.1 rules check verifies only `session-rules.md` and `evolution-policy.md` — `component-design.md` is copied at `:496` and reported at `:904` but never verified. | applied sf2fcccf (carried from Run 3) |
| K-34 | `maintenance.md:246` vs `:302-303` | Item 9's report string is **restated** verbatim in two homes rather than pointed at — the J-29 class (§9 "one home per mandate"), now a divergence risk. Item 9 also carries **no mechanical self-check** while its neighbour item 8 does, though component-design §9 rule 2 (which this checklist enforces elsewhere) requires one. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-27** |
| K-35 | `README.md:42-47`, `:108-114` | `/audit` is absent from both of README's command enumerations; its only README mention is the tree comment at `:129`. The J-16 mode concept therefore reached 3 of 4 enumerating surfaces, and checklist item 1 names README explicitly. | applied sf2fcccf (carried from Run 3) |
| K-36 | `maintenance.md:352` | J-19's inserted parenthetical is an unwrapped ~130-char line and splits "Record what graduated, where each / piece landed" awkwardly. Cosmetic. Introduced by `a28f661`. | applied safccff3 |
| K-37 | `.claude/settings.local.json` | A project folder name appears in four absolute paths in this gitignored, machine-local file, auto-written by the permission prompt. It never reaches git, but it IS an agent-reachable `.claude/` artifact and D16's surface list (`CLAUDE.md:216-220`) names it neither in nor out. Worth an explicit exemption clause rather than silence. | applied sf2fcccf (carried from Run 3) — **Run 5 verdict: not clean → see M-36** |

### ESCALATION — owner decision required, not a fix this repo applies

| Item | Location | Why it escalates |
|---|---|---|
| **E-1** | `assets/docs/framework-base-upgrade.md:204` | A source-project third-party **payment-vendor identifier** survives in a lineage doc. Lineage docs are explicitly NOT exempt (D16.5). Single occurrence, Portuguese prose, no project/client/person/domain attached — a generic placeholder would carry the same meaning. It entered at `9ab0c06` (v2.3.0) and **is present in `origin/main`** → per D16.3c, a PUSHED leak requires history rewrite + force push, which is the owner's decision and NEVER automatic. Never cleared by any prior audit run. The 10 unpushed commits introduce zero new identifier hits. |

---

## Summary matrix (Part 2 — the 17 dimensions)

| # | Dimension | Agent | Status | Finding |
|---|-----------|-------|--------|---------|
| A | Dual placement | 1 | PASS | 15 module skills; `.claude/skills/` holds only `cross-cutting-analysis`, `diff -r` identical (7680 bytes both) |
| C | Instruction style | 5 | **FAIL** | 2 instances → K-28 (`commit` skill wholly descriptive), K-27 (Phase A commit deferral unsaid) |
| E | Version consistency | 5 | PASS | v2.10.0 on 3/3 canonical surfaces (5 occurrences); 17 lineage fields intact, 0 rewritten by the bump |
| D4 | Skills README vs disk | 1 | PASS | 15 = 15, names 1:1, sub-breakdown sums to 12, `invocation:` claim 7/8 truth-tested against disk |
| D5 | Bootstrap file refs | 2 | PASS | 85 path strings, all resolve; 8/8 fence extractions non-empty; 14/14 underscore→hyphen mappings correct |
| D6 | Gap vocabulary | 3 | **FAIL** | 10/10 phrases exact and install rows now 10/10 on BOTH paths — but → K-4, K-13, K-5 |
| D7 | Pushy Description | 3 | PASS | 30/30 parse; 20/20 specialists carry all five elements; 0 anti-pattern instances |
| D8 | Orchestrator refs | 4 | **FAIL** | Every skill/agent name resolves; 2 cross-section citations do not → K-14, K-24. `LOOP CONTINUATION — active` byte-identical across 4 sites |
| D9 | CLAUDE.md diagram | 1 | PASS | Every path resolves; every diagram count correct. One PROSE count fails → K-7 |
| D10 | Command → skill paths | 4 | PASS | All citations resolve incl. §5/§6/§8/§9, Phase 0, triggers (b)/(d); dual copy byte-identical. Contract gap → K-15 |
| D11 | Bootstrap sequencing | 2 | **FAIL** | Both files monotonic, no duplicates; bootstrap has no unhandled forward reference — but → K-2, K-10 |
| D12 | Examples conventions | 5 | **FAIL** | 0 violations in the sample; 17 of 18 universal claims truth-tested TRUE; 1 literally false → K-29 |
| D13 | PRD ↔ bootstrap compat | 2 | PASS | Every cited PRD section exists incl. the new "Cross-cutting Concerns"; 2 defined-but-unread (INFO, unchanged) |
| D14 | Bootstrap output | 2 | PASS | 14/14 template references traced; Step 15 lists `done_tasks.md` and both new report lines |
| D15 | Doc factual accuracy | 5 | **FAIL** | 24 of 25 claims correct; banned-"inline" survivors = 0 repo-wide; 1 inaccuracy → K-22 |
| D16 | Project-info isolation | 1 | PASS | 4 surfaces + **107 commits** (contents AND messages) + agent memory (11 files): 0 blocklist hits, 0 value-shape hits. One pushed lineage identifier → **E-1** |
| D17 | Process coverage (meta) | 6 | **FINDINGS** | No unowned operation TYPE; 1 undocumented flow → K-20; 1 ownerless trigger → K-18; 1 red-by-construction check → K-8; per-fix receipt gap → K-21 |

---

## Recommended fixes

### 1. Undo what this application caused (do first — these are on `main`)
`K-2` lift both receiver bullets out of the `If it does not exist` lists into top-level paragraphs, as Step 13 already does ·
`K-3` annotate `Validation` as orchestrator-only and sync `produces:`, or drop the twin claim to what is actually true ·
`K-6` add the Part 1 ledger, score line and `carried:` field to Phase 2's template and a verdict slot to each agent format ·
`K-7` replace both fabricated statistics with the figures the reports actually record, and reconcile Run 2's own 11-vs-12 contradiction ·
`K-8` rewrite the RELOCATE check so a twinned component is not RED, and negation-prove it ·
`K-14` promote `validation-orchestrator:53` to a `###` heading and restore the `autonomous-loop §3a` link ·
`K-17` restore the Workflow line to end at the checklist ·
`K-18`, `K-19` delete or fix Phase 0's two unowned/unexecutable trigger rows ·
`K-23`, `K-32`, `K-36` wording.

### 2. Finish the sweeps that stopped at the first instance
`K-9` (EPA report — J-21's twin) · `K-12` (`security_reviewer.md:281` — the file J-25's fix cited as its model) · `K-16`, `K-22` (README, same file as the J-22 fix) · `K-24`, `K-25`, `K-26`, `K-27`.

> Every one of these is the SAME file or the SAME twin as a fix in the batch. Item 4 names both
> directions and the session reported running them. What it cannot do is be wrong per fix.

### 3. Close the checker gaps — the audit cannot currently detect its own recurring classes
`K-5` add D6.7 (install-row parity across both command twins) and D6.8 (validator gap consistency) ·
`K-15` widen Agent 4's contract to what the run executes: `autonomous-loop` in FILES TO READ, a cross-section-citation check, the `maintenance.md`/`audit.md` citation check, a Part 1 slot ·
`K-30` add `skill_reviewer.md` to Agent 3's list ·
`K-20` add a Phase 3 item that commits the report (cited by NAME: it was written as item 6, and a later insert made item 6 a different item — /audit 2026-09-03 P-3) ·
**`K-21` give item 4 a per-fix receipt** — one row per finding ID × the four directions.

### 4. Pre-existing, worth fixing while here
`K-1` (the freshness check's catch-all and the fork case) · `K-4` (the validator's gap has no reader) · `K-10`, `K-11` (EPA sequencing and missing create-branches) · `K-13` (three gap sources, both twins) · `K-28`, `K-29`, `K-31`, `K-33`, `K-34`, `K-35`, `K-37`.

### 5. Escalated to the owner
`E-1` — pushed lineage identifier. No automatic action taken.

---

## Meta-observation

Run 2 named the class as *"nothing verifies that a correction LANDED"* and answered it with Phase 0,
trigger (d) and the Audit-intake invoker. That half is genuinely closed and it worked: this run
caught 7 defects the fixes themselves introduced, none of which a presence check could see.

Two things it did not close, both visible in this run's own evidence:

1. **The per-fix receipt (K-21) is still missing, and its absence produced K-6.** The applying
   session ran item 4's DESCENDING direction on the project-side twin and skipped it on the
   mother-side one, then reported `4 directions, 15 extra instances` in good faith. A single
   aggregate line cannot be wrong about one fix among thirty. This is now the second consecutive
   run to name it.

2. **A new class: the fix that asserts a guarantee it did not establish.** K-3 declares two lists
   twins that diverge in three ways at the moment of writing. K-12's fix cites a sibling as its
   model without reading it — the sibling still carries the banned instruction verbatim. K-7 states
   a statistic that appears in no record. K-8 writes a self-check that can only go red. These pass
   "is the text present?" AND "is it in the right place?" — they fail only against *"is what it
   claims true?"*, which no current check asks. The cheapest control is narrow: **any claim a fix
   makes about ANOTHER file — a count, a twin relationship, a cited precedent — must be verified
   against that file in the same session, and the verification reported.**

**Suggestion:** Run `/maintenance` to apply fixes, using this section as the correction plan.
Group 1 first — those are regressions currently on `main`. `E-1` needs an owner decision before any
history operation.

---

**Report status:** `status: COMPLETE` — all 6 agents returned; all 17 dimensions evaluated plus a
Part 1 verification pass over all 30 applied findings.
**Application status:** `status: PARTIAL` — **17 of 37 applied in `afccff3`** (Groups 1 and 3,
plus K-30 as a free PARALLEL catch). **20 remain `open`**: Group 2 (K-9, K-12, K-16, K-22, K-24,
K-25, K-26, K-27) and Group 4 (K-1, K-4, K-10, K-11, K-13, K-28, K-29, K-31, K-33, K-34, K-35,
K-37). Deferred by owner decision to scope this batch to the regressions and the checker gaps —
NOT dropped; they carry forward to the next run.
**E-1 remains escalated** — no history operation performed.

### Per-fix receipt (item 4's new format, first use — `afccff3`)

| Fix | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|-----|---------|----------|----------|------------|----------------------|
| K-2 | ✓ grepped every `RECEIVER` mandate: 3/3 now top-level | ✗ EPA has NO cross-cutting routing flow at all — a MISSING FLOW, not this defect's class; raised as an observation, not silently swept | ✓ Step 15 counts satisfiable on both branches | ✓ Step 15 already has the field | none |
| K-3 | ✓ grepped `are TWINS` repo-wide: 1 claim, this one | ✓ all 3 surfaces synced (produces:, template, framework doc) | ✓ per-category vocabularies reconciled | n/a | 5 categories missing from `produces:`; 3 vocabulary divergences |
| K-5 | ✓ same "hint not detector" shape → K-15 | n/a | ✓ D6 intro unchanged, still accurate | ✓ report table gained the 2 install columns + gap-source row | 1 (report format) |
| K-6 | ✓ checked all 6 agent formats, not just Phase 2 | n/a | ✓ header fields | ✓ this fix IS the descending one | 6 agent formats |
| K-7 | ✓ grepped both statistics repo-wide: 3 executed surfaces | ✓ both report score lines corrected (Run 2 errata + Run 3 denominator) | ✓ added the counting rule that prevents recurrence | n/a | 2 (both score lines) |
| K-8 | ✓ grepped every mandated self-check for the same inversion: none other | ✓ rule lives in one file | ✓ item 8's surrounding obligations | n/a | none |
| K-14 | ✓ grepped every `§"..."` citation target for bold-lead-in shape | ✓ `autonomous-loop §3a` verified a real heading | ✓ the mandate at :53-57 intact | n/a | none |
| K-15 | ✓ same contract-narrower-than-execution shape in Agent 3 → K-30 | ✓ Agent 3's list fixed too | ✓ FILES TO READ rule added | ✓ 2 report tables gained fields | 2 (report formats) |
| K-17 | ✓ grepped for other steps named outside the checklist: none | n/a | ✓ item 7 unchanged and still owns the bump | n/a | none |
| K-18/K-19 | ✓ trigger parity re-run: 4/4 match | ✓ CLAUDE.md side verified | ✓ the "ALWAYS DECIDE" line above the table | ✓ D10.6 added so the parity is CHECKED, not assumed | 1 (no parity check existed) |
| K-20 | ✓ grepped for other write-without-persist mandates: none | n/a | ✓ authorized-operations list updated | ✓ self-check with stated expected result | 1 (authorization) |
| K-21 | ✓ item 5's report is already per-rule | n/a | ✓ items 1-3 report shapes unchanged | ✓ this table is the descending artifact | none |
| K-23/K-32/K-36 | ✓ residual "inline" grep: only the legitimate Route-1 sense | ✓ K-32 fixed at both sites | ✓ | n/a | 1 (K-32 second site) |
| K-30 | ✓ | ✓ | ✓ | n/a | none |

**class sweep: 14 fix-groups × 4 directions, 19 extra instances found and fixed.**

> **Errata 6 (added by the Run-4 application, finding L-23; NUMBERED retroactively by the Run 7 application, finding P-7 — it predated the numbering rule and was therefore invisible to the collision grep that enforces it).** This first use deviated from item 4's own mandate in
> three ways, none of them detected at the time: it has **14 rows for 17 findings** (K-18/K-19 and
> K-23/K-32/K-36 were merged, while the mandate says one row per finding ID); several `✓` cells are
> bare glyphs though the legend requires naming what was grepped; and the total reads
> "fix-groups" rather than the mandated "fixes". Item 4 has since gained a mechanical row-count
> self-check and a "quote the pattern or it is RED" rule so the next use cannot repeat this.
One `✗` with a stated reason (K-2 PARALLEL). The `✗` is the point: an aggregate line could not
have carried it.


---

# Run 4 — verification pass over `afccff3`

**Date:** 2026-09-02
**Framework version:** v2.11.0 (`README.md:1`), HEAD `4f5c865`
**Run mode:** `verification (over afccff3)` — CLAUDE.md trigger (d); trigger (b) satisfied by the same run, the bump being MINOR
**Under verification:** the 17 findings applied in `afccff3` + the VERSION bump
**Dimensions checked:** 17 + a Part 1 fix-verification pass in every agent
**Agents dispatched:** 6

**carried: 20 open from Run 3** (K-1, K-4, K-9, K-10, K-11, K-12, K-13, K-16, K-22, K-24, K-25,
K-26, K-27, K-28, K-29, K-31, K-33, K-34, K-35, K-37) **+ E-1 escalated.**
**All 20 re-verified against current disk: 20/20 STILL TRUE.** E-1 still present and still pushed.

> **ID scheme.** `F-*` (2026-08-31), `G-*`, `H-*` (2026-09-01), `J-*` (Run 2), `K-*` (Run 3).
> This run uses `L-*`. Applied K findings whose verdict is not CONFIRMED-FIXED keep their K ID and
> gain a pointer to the new L ID (Phase 3's Part-1 ledger item).

**This is the first run whose detectors live in the repository rather than in the invoking prompt.**
`D6.7`, `D6.8`, `D8.7`, `D8.8`, `D10.4` (byte-identity), `D10.5` and `D10.6` were written into
`audit.md` by the batch under verification. Two of them found real defects; one is miscalibrated.
That is reported below rather than assumed either way.

---

## Part 1 — Verification ledger for `afccff3`

| ID | Verdict | Evidence |
|----|---------|----------|
| K-2 | **CONFIRMED-FIXED** | All five sub-checks pass. Both receivers are column-0 bold paragraphs (`bootstrap.md:211-216`, `:232-236`); the diff shows the leading `- ` and continuation indent removed. Each is separated from the `**If it does not exist:**` list by that branch's terminal `Create at …` line, and states the binding explicitly. No contradiction with the `Do NOT overwrite` branches. Step 13's receiver unchanged at `:620-624`. Step 15's `R/R · A/A · T/T` now reachable on every path. Nothing re-parented. |
| K-3 | **PARTIALLY-FIXED — CLASS NOT SWEPT** | Core sync is correct and mechanically verified twice: `validator.md`'s Output template defines **14** `###` sections; `produces:` (`:20-25`) names exactly those 14 in the same order; the framework doc lists the same 14 **+ `Validation`**, annotated ORCHESTRATOR-ONLY; per-category vocabularies match 1:1. **But the offending literal was swept in one of five places** → **L-6**. |
| K-5 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | D6.7 (`audit.md:346-353`) and D6.8 (`:354-358`) exist, are executable, and the REPORT FORMAT gained their fields. Both were RUN this session and both work. But Agent 3's FILES TO READ names none of the four files the new checks require → **L-3**; and D6.7's scope excludes the third gap source by construction → **L-11**. |
| K-6 | **CONFIRMED-FIXED — INTRODUCED A DEFECT — CLASS NOT SWEPT** | Phase 2 gained `carried:`, the Part 1 ledger and the score line; all **6/6** agent formats gained a Part 1 slot (`:187, :275, :379, :489, :599, :691`). But every slot sits ABOVE its own `## Agent N` heading → **L-2**; and the Phase 2 template still has no per-finding ID+status table though Phase 3 items 2-3 mandate one → **L-10**. |
| K-7 | **PARTIALLY-FIXED** | The fabricated `8 of 15` is gone; the counting rule landed in Phase 0 where a future run reads it; all three executed surfaces carry an identical series; `13 of 24` and `11 of 30` **independently re-measured correct by two agents**. **`2 of 15` is wrong — the true figure is `4 of 15`** → **L-4**. |
| K-8 | **CONFIRMED-FIXED** | Run both ways by the auditor, not inferred: the `### RELOCATE` section is byte-identical across the twin pair (1308 bytes each) → **GREEN on the real dual-placement case**, which the old wording called RED; the three enumerated RED shapes are reachable. The check can now go both ways. *Its negation proof, however, is unverifiable* → **L-16**. |
| K-14 | **CONFIRMED-FIXED — INTRODUCED A DEFECT (LOW)** | `validation-orchestrator/SKILL.md:53` is a real `###`, byte-verified equal to the citation at `:65`; the restored `autonomous-loop §3a` link is byte-equal to the real heading at `autonomous-loop:142`. No re-parenting — `### 3. Git checkpoint` resumes at `:61`. But the promotion inserted an unnumbered `###` between numbered steps 2 and 3 → **L-22**. |
| K-15 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Every addition landed and the REPORT FORMAT has a field for each. **The guard rule the same hunk wrote fails on its own block** → **L-3**. |
| K-17 | **CONFIRMED-FIXED** | `maintenance.md:29-35` routes through item 7 and carries an explicit anti-recurrence clause; no post-checklist bump step survives; no contradiction with the checklist header or `## Version bumps`. Residue: the line now enumerates items 7/8/9 by number → **L-27**. |
| K-18 | **CONFIRMED-FIXED** | Parity verified in BOTH directions by two agents: `CLAUDE.md:132-137` and `audit.md:52-57` name the same four events, same letters, no extras. The "scheduled net" row is gone and `:46-50` now forbids adding one. `grep -i "periodic\|cadence\|interval\|scheduled"` over `audit.md` returns only the prohibition and D17.3 itself. |
| K-19 | **CONFIRMED-FIXED** | The unexecutable row is replaced by `:59-62`, which states why trigger (a) cannot be verification and routes the did-it-land discipline to `framework-audit` Q4. |
| K-20 | **CONFIRMED-FIXED — INTRODUCED TWO DEFECTS** | Item 5 (`audit.md:796-806`) has a runnable command, a self-check with a stated expected result, and a report line; numbering after the insert is sequential; the authorization matches. But the commit item precedes the ledger-content item → **L-9**; and the authorization sentence contradicts four checks in its own file → **L-5**. |
| K-21 | **CONFIRMED-FIXED — INTRODUCED A DEFECT (minor)** | The table is inside item 4 (`:153-155`), the format is executable, the legend requires a reason for every `✗`, and **it was actually used** — with an honest `✗` an aggregate could not have expressed. But the first use deviated from its own mandate three ways → **L-23**, and nothing detected that → **L-17**. |
| K-23 | **CONFIRMED-FIXED** | `README.md:166` reads "(written directly by bootstrap Step 4 — no template)", verified accurate against `bootstrap.md:238` and the absence of any `done_tasks` template. Class swept: `grep -in inline README.md` → 2 hits, both the legitimate Route-1 sense. |
| K-30 | **CONFIRMED-FIXED** | `audit.md:318-321` names all agents plus "verify the count against `ls docs/modules/agents/`". (The finding said "7 of 9"; the true total is **10** — the fix is correct regardless, and the hardening makes the list self-checking.) |
| K-32 | **CONFIRMED-FIXED — both sites** | Truth-tested against `bootstrap.md`'s actual `### Step` headers by two agents: primaries 1-15 present and monotonic; `Step 0.5` at `:37` precedes `Step 1` at `:76`, so "pre-PRD gate" is literally true; 5.7/5.8/14.5 all exist. LATERAL sweep found only these two sites plus the detector. |
| K-36 | **CONFIRMED-FIXED** | `maintenance.md:379`/`:384` are 96 and 98 characters; the parenthetical is wrapped and no longer splits the sentence. (Not covered by any agent's dimensions — verified directly.) |
| VERSION | **CONFIRMED-FIXED** | `v2.11.0` on 3/3 canonical surfaces, 5 occurrences. `git show afccff3 --unified=0 \| grep '^[-+]created:'` → **empty**: 0 of 20 lineage fields rewritten. No stale `v2.10.0` outside the (now-current) bump example. **MINOR is the correct class** — the batch added new sections AND changed `validator.md`'s `produces:`, a shipped document's schema. |

**Score, counted over the FINDINGS: 8 of 17 clean, 8 not clean, 1 (K-36) outside every agent's
dimensions and verified directly — clean.** So **9 of 17 clean, 8 not clean.** VERSION clean
(row total 10/18). Of the 8 not clean: **2 partially-fixed**, **2 class-not-swept**,
**6 introduced-a-defect** (K-3 and K-6 fall in two categories).

---

## New findings

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| L-1 | `.claude/commands/existing_project_adaptation.md`, Phase 3 | **EPA has NO cross-cutting flow at all — and its retroactive PRD is structurally incomplete against the PRD template.** `cross-cutting` appears exactly 3 times in EPA (`:195`, `:525`, `:838`), all as the skill NAME in install/verify inventories. There is no read of the PRD's `Cross-cutting Concerns`, no classification step (bootstrap's Step 1.1), no receiver (bootstrap has 3), no report line (bootstrap Step 15), and **no invocation of the `cross-cutting-analysis` skill EPA itself installs at `:462-476`**. Worse: Phase 3 (`:584-620`) enumerates which PRD sections the retroactive PRD must populate — 1.1, 1.2, 2.1, 2.2, 3.x, 4, 5.1, 5.3, 6, 7, 8, 1.3, 1.4, Changelog — and **omits `## Cross-cutting Concerns`, which `prd_planning.md:239` defines as the FIRST section of the template.** EPA therefore produces a PRD that fails its own template and installs a skill it never exercises. Classification: **MISSING FLOW**, not K-2's class. Raised as an observation at the end of the previous session and now confirmed with evidence. | applied sf2fcccf — **Run 5 verdict: not clean → see M-1..M-5** |
| L-2 | `.claude/commands/audit.md:187, :275, :379, :489, :599, :691` | **K-6's Part 1 slot is at the wrong heading level, 6 times.** Each `### PART 1 — Verification ledger` sits ABOVE that agent's own `## Agent N` heading. Phase 2 `:761` says "Paste each agent's full report in order", so in the merged document every agent's Part 1 ledger nests under the **previous** agent's H2. This is verbatim the wrong-nesting defect the Part 1 mandate itself enumerates at `:84` — reproduced six times by the fix that installed the mandate. Introduced by `afccff3`. | applied sf2fcccf |
| L-3 | `.claude/commands/audit.md:311-323` (Agent 3), `:420-433` (Agent 4) | **The guard rule fails on both blocks, including the one it lives in.** `afccff3` wrote at `:433`: *"Every file a CHECK below names MUST appear in this list."* Agent 4's own list omits `code_reviewer.md` (named by D8.8), `component-design.md` / `component_design.md` (D10.5 §5/§6/§8/§9), `session_rules.md` (D10.5 "Execution proof") and `CLAUDE.md` (D10.5 trigger letters, D10.6 parity). Agent 3's list omits `bootstrap.md` and `existing_project_adaptation.md` (D6.7) and `docs/modules/rules/component_design.md` (D6.8) — and never received the guard rule at all. This is K-15's own class, reproduced by K-15's fix. Introduced by `afccff3`. | applied sf2fcccf — **Run 5 verdict: not clean → see M-23** |
| L-4 | `CLAUDE.md:137`, `maintenance.md:356`, `audit.md:31-34`, `audit-2026-09-02.md:63-67` | **`2 of 15` is wrong; the measured figure is `4 of 15`.** Two agents independently counted the 2026-08-31 ledger (`:93-103`): `F-1` ("FIXED in the named file — class not swept"), `F-11` PARTIALLY, `F-12` PARTIALLY, `F-15a` ("twin not swept") — **four** findings whose verdict is not CONFIRMED-FIXED. The K-7 fix derived `2` from that report's own summary prose at `:91` ("13 of 15 CONFIRMED-FIXED, 2 PARTIALLY-FIXED"), a line its own ledger contradicts by folding the class-not-swept rows into CONFIRMED-FIXED. **The fix that corrected a fabricated statistic replaced it with a statistic copied from an unverified summary** — the exact class Run 3's meta-observation named, one level down. The other two figures re-measure correct. Introduced by `afccff3`. | applied sf2fcccf — **Run 5 verdict: not clean → see M-46, M-51** |
| L-5 | `.claude/commands/audit.md:16-17` vs `:172-173`, `:658`, `:800` | **K-20's authorization contradicts four checks in its own file.** The new line reads "**No other git operation is authorized**", while D16.3c mandates `git grep` / `git rev-list --all` / `git log --all` (`:172-173`), Agent 6's D17.1 mandates `git log` (`:658`), and K-20's own self-check mandates `git status --porcelain` (`:800`). This is verbatim the self-contradicting-authorization class `maintenance.md:20-22` records D17 as having found on 2026-08-31. Introduced by `afccff3`. | applied sf2fcccf |
| L-6 | `docs/modules/agents/validator.md:15`; `docs/agentic_engineering_framework.md:917`, `:1161`, `:1596` | **K-3's sweep stopped inside the frontmatter block it started in.** The literal `"✅/❌/⏭️ per category"` — the exact vocabulary K-3 said cannot express `⚠️` or `BASELINE-CREATED` — survives at `validator.md:15`, in the `description:` field **four lines above the `produces:` that was fixed**, and at three points in the framework doc. The worst is `:1596`: the canonical `produces:` example in the "I/O contract (subagent agents)" section, i.e. the schema every future agent is copied from. | applied sf2fcccf |
| L-7 | `.claude/commands/audit.md:455-461` (D8.7) | **The new cross-section-citation check is miscalibrated — it fires on the repo's own dominant convention.** D8.7 says "matching EXACTLY … a substring match that resolves by luck is a FINDING". But `## Name — subtitle` is this repo's standard heading shape, and **15 citation sites** legitimately cite the prefix (`"Execution proof"`, `"Cadence integrity"`, `"Autonomous loop watchdog"`, `"Between Tasks"`, `"Session archetypes"`, `"Continuation across sessions"`, `"New component creation"`, `"Version bumps"`, `"Audit intake"`, `"Upstream intake"`, …). As written the next run produces ~15 findings of this class. Same inversion as K-8 (a control that fires on the healthy state), in a check written by the batch that fixed K-8. Introduced by `afccff3`. | applied sf2fcccf |
| L-8 | `.claude/commands/audit.md:787-793` | **The escalation lane has no bookkeeping, and the carry-forward rule structurally cannot see it.** `audit.md:176` and `CLAUDE.md:222` promise that pushed leaks "escalate to the owner", and the run executed it (E-1). But Phase 3 item 2 enumerates exactly three statuses — `open` / `applied sHASH` / `rejected — [reason]` — and item 3 carries forward **only still-`open`** findings. `E-1` reads "escalated", so the next run's carry-forward does not reach it: an escalated item ages out in silence. Phase 2's "Recommended Fixes" offers three groups and no escalation section, while the executed reports used five plus an escalation table. | applied sf2fcccf — **Run 5 verdict: not clean → see M-26, M-34, M-35** |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| L-9 | `.claude/commands/audit.md:796-812` | **K-20's item ordering is inverted.** Item 5 COMMITS the report; item 6 then mandates "In `verification` mode, ALWAYS persist the Part 1 ledger too" — a content requirement executed AFTER the commit meant to persist it. The finding asked for "a Phase 3 **item 6**"; it landed at 5. Also stranded a citation: `audit-2026-09-02.md:240` says "per Phase 3 item 5", meaning the ledger rule, and now points at the commit item. Introduced by `afccff3`. | applied sf2fcccf — **Run 5 verdict: not clean → see M-24** |
| L-10 | `.claude/commands/audit.md:714-761` | **Phase 2's template has no per-finding table, though Phase 3 items 2-3 mandate one.** Item 2 requires every finding to carry a stable ID and a status column; item 3 requires carried-forward rows. The template offers `## Summary` (per-DIMENSION PASS/FAIL) and `### Detailed Findings` ("Paste each agent's full report"). K-6 answered the DESCENDING question for what Phase 0 mandates and not for what Phase 3 mandates — same shape, one item over. | applied sf2fcccf — **Run 5 verdict: not clean → see M-26** |
| L-11 | `.claude/commands/audit.md:346-353` | **D6.7 is scoped to exclude the third gap source it sits next to.** It reads "For every gap **from D6.1/D6.2**" — the two reviewers — while D6.8, four lines below, establishes that `validator.md` is a third declaring component. A validator-declared gap is therefore excluded from install-parity checking by construction. The REPORT FORMAT has the same shape: "Gap declarations found" hardcodes two sub-bullets even though the table header was widened to "Declared by". | applied sf2fcccf — **Run 5 verdict: not clean → see M-13** |
| L-12 | `.claude/commands/audit.md:668-703` | **Agent 6's contract still declares "three bounded questions" while the run executes six.** D17.1-D17.3 are in the file; D17.4 (self-check provability), D17.5 (per-fix receipt) and D17.6 (convergence) came from the invoking prompt, as did four of the seven report bullets. This is L-3's class and L-10's class simultaneously, on the one dimension whose job is to detect exactly that. `afccff3` widened Agent 4 and fixed Agent 3's file list; it never touched Agent 6. | applied sf2fcccf |
| L-13 | `assets/docs/audit-*.md`; `.claude/commands/audit.md` Phase 2/3 | **`Report status:` / `Application status:` are executed every run and owned by nothing.** Three executions (`audit-2026-09-01.md:271-272`, `-09-02.md:219-221`, `:436-443`). Neither Phase 2's template nor any Phase 3 item mandates them, and with no enumeration `Application status:` has taken three different values in three uses (`COMPLETE`, `APPLIED`, `PARTIAL`). The only owner of a completion status in this framework is a PROJECT-side template (`session_rules.md:197`, `framework-audit/SKILL.md:116`) — a rule this repo ships and does not apply to itself. | applied sf2fcccf |
| L-14 | `assets/docs/audit-*.md` | **`## Meta-observation` — 3 executions, no owner.** It is the section that named the per-fix-receipt gap twice and produced K-21, and nothing in Phase 2 or Phase 3 asks for it. | applied sf2fcccf |
| L-15 | `.claude/commands/maintenance.md:404`; `.claude/commands/audit.md` Phase 3 | **Inserting a numbered item into a list triggers no renumbering sweep, and a live instance is already on disk.** `maintenance.md:404` reads "**Without step 7** the last link of the chain rests on owner memory" — inside **step 8**, which is what step 7 became when the audit-proposal step was inserted ahead of it. The same class produced L-9. Item 1's inventory sweep names counts and index rows; it does not name ORDINALS inside a list you inserted into. | applied sf2fcccf |
| L-16 | `.claude/commands/maintenance.md` post-change checklist | **Eight of the nine checklist report lines have no persisted home.** `maintenance.md` says "ALWAYS REPORT" nine times and never says WHERE. For `afccff3`, only the per-fix receipt survives on disk — because the session voluntarily appended it to the report file. `back-sweep:`, `liveness:`, `negation proof:`, `classification:`, `version:`, `inventory sweep:`, `instruction style:`, `references/fences/isolation:` exist nowhere, so no later session can verify any of them. Concrete consequence: K-8's negation proof is asserted in the commit body and **unverifiable** — `grep "negation proof:" assets/docs/` returns only Run 2's J-2 line. A self-reported control nobody can re-read is not a control. *Natural home:* Audit intake step 4, which already mandates writing back to the report file. | applied sf2fcccf — **Run 5 verdict: not clean → see M-31, M-32, M-33** |
| L-17 | `.claude/commands/maintenance.md:148-162` | **The per-fix receipt has no external verifier — it is the only new control whose checker is its own author.** Three things it structurally cannot catch: (1) a `✓` that was never run — each cell says "say what you grepped" but no reader verifies the grep; (2) row merging — nothing mechanically compares the row count to the applied-finding count, so a fix whose sweep was skipped can be collapsed into a neighbour's row; (3) it has no mandated persisted home (→ L-16). The failure it replaces — an honest aggregate emitted in good faith — recurs unchanged as an honest ROW emitted in good faith. | applied sf2fcccf |
| L-18 | `examples/README.md:121-123` | **The J-7 fix is literally false in a new way.** It says "`## Output` (10 of 20 examples) and `## Output Format` (the other 10)" — presented as a partition. Measured: **`## Output Format` is present in 20 of 20**; `## Output` in 10, and every one of those 10 carries **both** (e.g. `api-security-scanner.md:33` a `## Output` summary and `:106` a `## Output Format` template). It is not a spelling variance but a STRUCTURAL one: 10 agents carry two output sections, 10 carry one. The 10/10 split is true only of "the heading immediately following `## Input`". Third iteration of the G-20 → J-7 → K-29 class. | applied sf2fcccf |
| L-19 | `docs/modules/skills/commit/SKILL.md:38` | **A source-project FUNCTION NAME ships in a template every project receives.** The commit-message example quoted a source-project function name verbatim (identifier withheld here per the isolation rule — see the fix commit for the before/after). D16 has passed this file in four consecutive runs because the blocklist is derived from project folder names and identifiers harvested from project docs — **not from source-project CODE identifiers**. The isolation rule (`CLAUDE.md:214-215`) bans "single-project vocabulary in examples" without qualification, and templates carry double scrutiny. The gap is in D16's blocklist-derivation step, not only in this line. | *(superseded)* `PARTIAL — instance fixed sdd98dea; the blocklist-derivation half stays open` → **CLOSED**: the blocklist-derivation half was installed as `D16.2c` by `M-36` in the same commit that fixed the instance, and widened to every framework-layer surface by `N-13`. The row read `stays open` for two batches after its own remedy shipped (`/audit` 2026-09-03 N-39). |
| L-20 | `.claude/commands/existing_project_adaptation.md:316`, `:931` | The source project's actual non-standard backlog filename (identifier withheld here per the isolation rule) was used as EPA's worked example. It names no project, client, person or domain — but it is literally "single-project vocabulary in an example". Tracked and pushed. No prior run flagged it. Needs either a generic placeholder or an explicit ruling that generic-language filenames are exempt. | **applied sdd98dea** — owner chose the placeholder; both EPA sites now read `[nome-fora-do-padrao].md` |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| L-21 | `docs/agentic_engineering_framework.md:348` | The pipeline diagram's Step 4 output column names only `.claude/phases/pendencias.md`; the step is titled "Create pendencias.md **and done_tasks.md**" and creates both unconditionally. Same omission J-22/K-23 corrected in README, in the file K-32 just edited. | applied sf2fcccf |
| L-22 | `docs/modules/skills/validation-orchestrator/SKILL.md:53` | K-14's promotion inserted an unnumbered `###` between `### 2. Classify and route` and `### 3. Git checkpoint`, interrupting the numbered "steps 1-3" sequence the section's own intro announces. Introduced by `afccff3`. | applied sf2fcccf |
| L-23 | `assets/docs/audit-2026-09-02.md:450-465` | The per-fix receipt's first use deviated from its own mandate three ways: **14 rows for 17 findings** (K-18/K-19 and K-23/K-32/K-36 merged, while the mandate says "one row per finding ID"); several `✓` cells are bare glyphs though the legend requires saying what was grepped; and the closing total reads "14 fix-groups × 4 directions" rather than the mandated "N fixes × 4 directions". | applied sf2fcccf — **Run 5 verdict: not clean → see M-45** |
| L-24 | `CLAUDE.md:137` | Attributes the three verification runs to **trigger (d)** ("its three documented executions"), but trigger (d) was created by `a28f661` and all three ran under trigger (c)/owner request. `audit.md:31` words it correctly ("the verification mode"). The line is also a single ~250-character unwrapped line in an otherwise wrapped file. | applied sf2fcccf — **Run 5 verdict: not clean → see M-41, M-46** |
| L-25 | `docs/modules/agents/validator.md:20` | `produces:` labels all 14 "**Verdict-bearing** categories", but `Criteria Results`, `Prior Review Findings`, `Coverage Gap Declaration` and `Items for human verification` carry no verdict token — they are prose/table sections. Introduced by `afccff3`. | applied sf2fcccf |
| L-26 | `docs/agentic_engineering_framework.md:950` vs `validator.md:76` | The framework doc calls the category `Mutation`; `validator.md` and its `produces:` call it `Mutation Tests`. The twin rule the same commit wrote requires the same category in BOTH lists; a name divergence is the thinnest possible violation of it. Introduced by `afccff3`. | applied sf2fcccf — **Run 5 verdict: not clean → see M-14** |
| L-27 | `.claude/commands/maintenance.md:31-35` | The rewritten Workflow line enumerates items **7, 8 and 9 by number**, making it exactly the kind of inventory surface the checklist header at `:83-86` warns about: adding item 10 leaves this sentence silently stale. Introduced by `afccff3`. | applied sf2fcccf — **Run 5 verdict: not clean → see M-52** |
| L-28 | `.claude/commands/audit.md:800-801` | The report-commit self-check `git status --porcelain assets/docs/` → "expected EMPTY" is over-broad: any unrelated modified file anywhere under `assets/docs/` turns it red. Should be scoped to the report path. Introduced by `afccff3`. | applied sf2fcccf |
| L-29 | `assets/docs/audit-2026-09-02.md:61` | Run 2's breakdown line "12 clean · 7 class-not-swept · 5 introduced-a-defect" is still uncorrected and also wrong per row (actual: 12 clean · 5 class-not-swept · 4 partially · 4 introduced · 1 confirmed-with-defects, over 26 rows). The errata added by `4f5c865` corrects the SCORE and not the BREAKDOWN. | applied sf2fcccf — **Run 5 verdict: not clean → see M-45** |
| L-30 | `.claude/commands/bootstrap.md` Setup + `docs/modules/templates/claude_md.md:18` | A PRD-less bootstrap (explicitly allowed by Setup step 3) ships a CLAUDE.md pointing at `assets/docs/prd.md`, which no step creates; no step mandates a fallback line. Pre-existing. | applied sf2fcccf — **Run 5 verdict: not clean → see M-6** |
| L-31 | `examples/README.md:88-106` vs the rules examples | K-29 is understated: beyond the checklist bullet (6 of 11), the **entire Frontmatter convention block** at `:88-106` (`name:`, `effort:`, `description:`, `invocation:`, lineage fields) holds in **0 of 11** rules examples — they use `domain:`/`applies_to:` instead. `:86`'s "exhibited by every example" fails for a whole convention block on 11 of the 40 examples. | applied sf2fcccf |
| L-32 | `docs/modules/skills/commit/SKILL.md` | (Groups with K-28.) The file has **0** occurrences of ALWAYS/NEVER/MUST — the only module skill with zero imperative markers, against `autonomous-loop`'s 20/18/4 and `session-end`'s 9/6/2. | applied sf2fcccf |

### Carried forward from Run 3 — all re-verified STILL TRUE

`K-1` (freshness check: no catch-all + fork reads GREEN; both twins byte-identical) · `K-4` (validator's gap declaration has no reader) · `K-9` (EPA report has no freshness line) · `K-10` (EPA Phase 2 reads what Phase 4 copies) · `K-11` (EPA Steps 2.2-2.5 have no create-branch; the four template paths appear 0 times in EPA) · `K-12` (`security_reviewer.md:281` still carries the banned line; `code_reviewer.md:251` still cites it falsely) · `K-13` (policy names 2 of 3 gap sources, all four copies — **now mechanically detected by the new D6.8, which returns FAIL**) · `K-16` · `K-22` · `K-24` · `K-25` · `K-26` (13 stated, 14 measured) · `K-27` · `K-28` · `K-29` · `K-31` · `K-33` · `K-34` · `K-35` · `K-37`.

### ESCALATION

| Item | Status |
|---|---|
| **E-1** | **`accepted-risk — see the record below`** (owner decision, 2026-09-02; the row previously read `RESOLVED FORWARD`, a value outside the status enumeration — `/audit` M-34). See the accepted-risk record below. |
| ~~E-1 (prior state)~~ | **was: STILL OPEN, still pushed.** `assets/docs/framework-base-upgrade.md:204`. Re-measured: 38 hits across `git rev-list --all`, all in that one file (= its revision count), all the single identifier; confirmed present in `origin/main`. The 13 unpushed commits introduce zero new identifier hits. No action taken — history rewrite is the owner's decision. See also **L-8**: the carry-forward rule cannot currently reach an escalated item. |

### Accepted-risk record — E-1 (owner decision, 2026-09-02)

- **Decision:** fix forward; do NOT rewrite history.

- **Working tree:** the identifier at `assets/docs/framework-base-upgrade.md:204` was replaced with
  a role descriptor ("o gateway de pagamento do projeto-fonte"). Repo-wide occurrences in the
  working tree: **0**.
- **Residue:** the term remains in every revision of that one file, all reachable from `origin/main`.
  **This is knowingly accepted and is NOT an open finding.** Rationale: the term is a third-party
  vendor name appearing once, in prose, with no project, client, person, domain, repo URL or infra
  identifier attached; a `git filter-repo` + force push would break every existing clone and any
  fork, which is disproportionate to that exposure.
- **Standing instruction to future `/audit` runs:** D16 MUST still report this history hit as an
  observation with a pointer to this record — never as an `open` finding, and never as grounds to
  propose a history rewrite. A NEW occurrence in the working tree, or any identifier of a
  different class (project, client, person, domain, infra id, secret value, PII), is NOT covered
  by this record and remains a finding.
- **What WOULD re-open it:** a NEW occurrence in the working tree, or any identifier of a
  different class — project name, client, person, deployment domain, infrastructure id, secret
  value or PII. Those are not covered by this record and remain findings. (This field existed only
  inside the Standing-instruction bullet until `/audit` 2026-09-03 N-43 named the omission; the
  record read as four fields under different labels while two later surfaces asserted it carried
  five.)
- **Class sweep performed under this decision:** the same defect class — a source-project
  identifier inside a framework artifact — was swept beyond the named instance. **L-19** (a
  source-project FUNCTION name in `commit/SKILL.md:38`, a template every project receives) was
  fixed in the same session. One borderline third instance is reported and deliberately NOT
  changed: `session_rules.md:162` uses a hand-rolled date helper in a commit-message example — plausibly
  generic, owner's call.
- **What remains open from L-19:** only its second half — D16's blocklist is derived from project
  folder names and identifiers harvested from project DOCS, never from source-project CODE
  identifiers. That derivation gap let this file pass four consecutive runs and is unchanged.

---

## Summary matrix (Part 2 — the 17 dimensions)

| # | Dimension | Agent | Status | Finding |
|---|-----------|-------|--------|---------|
| A | Dual placement | 1 | PASS | 15 module skills; `.claude/skills/` holds only `cross-cutting-analysis`; `diff -r` empty, both 7680 bytes |
| C | Instruction style | 5 | **FAIL** | 7 instances — 6 are open carry-forwards (K-28, K-27, K-25, K-12 ×2, K-31), 1 new → L-22 |
| E | Version consistency | 5 | PASS | v2.11.0 on 3/3 canonical surfaces, 5 occurrences; 20 lineage fields intact, 0 rewritten; MINOR correctly classified |
| D4 | Skills README vs disk | 1 | PASS | 15 = 15, names 1:1, breakdown sums to 12, `invocation:` 7/8 truth-tested against every SKILL.md |
| D5 | Bootstrap file refs | 2 | PASS | 87 path strings, 38/38 framework-side resolve; **8/8 fence extractions executed, all non-zero**; 16/16 naming mappings |
| D6 | Gap vocabulary | 3 | **FAIL** | 10/10 phrases exact, install rows 10/10 on both paths, **install-table diff EMPTY** — but D6.8 returns FAIL on K-13, plus L-3, L-11 |
| D7 | Pushy Description | 3 | PASS | 30/30 parse; 20/20 specialists carry all five elements; 0 anti-pattern instances |
| D8 | Orchestrator refs | 4 | **FAIL** | All names resolve; **zero bold-lead-in citations survive** (the K-14/J-17/H-19a shape is gone) — but D8.7 as written flags 15 legitimate prefix citations → L-7 |
| D9 | CLAUDE.md diagram | 1 | PASS | Every path resolves; every diagram count correct. One PROSE count fails → L-4 |
| D10 | Command → skill paths | 4 | **FAIL** | All citations resolve; **D10.4 byte-identity RUN (empty)**; **D10.6 parity RUN (4/4)** — contract defect → L-3 |
| D11 | Bootstrap sequencing | 2 | **FAIL** | bootstrap clean: 25 headings, monotonic, 3/3 handoffs received, no step consumes a later output. EPA → K-10, K-11, L-1 |
| D12 | Examples conventions | 5 | **FAIL** | 0 violations across the full set for agents and skills; 14 conventions verified TRUE; 2 universal claims literally false → L-18, L-31 |
| D13 | PRD ↔ bootstrap compat | 2 | PASS | Every cited PRD section exists incl. `Cross-cutting Concerns`; read-but-undefined = 0; 4 defined-but-unread (INFO) |
| D14 | Bootstrap output | 2 | PASS | 13/14 template pointers traced, 1 conditional (→ L-30); Step 15 lists 100% of created artifacts |
| D15 | Doc factual accuracy | 5 | **FAIL** | 5 inaccuracies (3 carried-open, 2 new → L-18, L-21); **banned-"inline" survivors: 0**, the only hit being the detector itself |
| D16 | Project-info isolation | 1 | PASS | 107 tracked files, **110 commits** (contents AND messages), agent memory (10 files), templates double-scrutinised: 0 blocklist hits, 0 value-shape hits. But the blocklist misses code identifiers → **L-19**, and E-1 remains |
| D17 | Process coverage (meta) | 6 | **FINDINGS** | No unowned operation TYPE remains — the gaps moved into the CONTENTS of the artifacts: L-13, L-14, L-8, L-15, L-16, L-17, L-12 |

---

## Recommended fixes

### 1. Undo what this application caused (on `main` now)
`L-2` move all 6 Part 1 slots below their `## Agent N` heading · `L-3` add the missing files to BOTH agent lists and put the guard rule in every block · `L-4` recount from the ledger and correct 3 surfaces + the errata · `L-5` rewrite the authorization to permit the git reads its own checks mandate · `L-6` sweep the vocabulary literal to 4 remaining sites, `:1596` first · `L-7` allow prefix-of-`## Name — subtitle` citation in D8.7, or complete the 15 citations · `L-9` swap Phase 3 items 5 and 6 · `L-11` rescope D6.7 to every declaring component · `L-22`, `L-25`, `L-26`, `L-27`, `L-28` wording.

### 2. Close the gaps the run found in the process itself
**`L-16` give the checklist receipts a persisted home** (Audit intake step 4 already writes to the report file) — without it `liveness:`, `negation proof:` and six others are unverifiable by construction ·
`L-17` add a mechanical row-count check to the per-fix receipt and require the grep to be quoted ·
`L-8` add `escalated` to Phase 3 item 2's status set and carry it forward in item 3 ·
`L-13`, `L-14` give `Report status:` / `Application status:` / `## Meta-observation` a home and an enumeration ·
`L-15` add an ordinal-citation sweep to checklist item 1 ·
`L-10` give Phase 2 a per-finding ID+status table ·
`L-12` bring Agent 6's contract up to what it executes.

### 3. The two isolation findings
`L-19` extend D16's blocklist derivation to source-project CODE identifiers, and fix the example · `L-20` decide: placeholder, or explicit exemption.

### 4. The largest single gap
`L-1` — EPA has no cross-cutting flow and its retroactive PRD omits the template's first section.

### 5. Carried forward, unchanged
The 20 K findings above, plus `E-1` (owner decision).

---

## Meta-observation

**Three firsts, and they are measurable.** `afccff3` shipped **no self-check that can only go green**,
**none that fires on the normal case**, and **no claim false at birth** — the dominant new class of the
previous batch. Two of its three new controls are genuine gates, because they are mechanical AND
adversarially checkable by someone other than their author: `D6.7` and `D10.6` were both RUN by this
audit, both went green, and both can go red. They convert two classes that had each broken twice
(the install link, the unowned trigger) into checks the AUDITOR runs rather than the applier.
`D6.8` did better than green: it returned FAIL on `K-13`, a finding the batch had deferred.

**The third new control is not a gate.** The per-fix receipt's verifier is its own author. Its first
use deviated from its own written format three ways and nothing detected it. That is not a
discipline failure — it is the same structural fact as the aggregate line it replaced, one level
finer: **an honest report cannot audit itself.**

**And the reporting layer it depends on is not persisted.** Eight of the nine checklist report lines
exist nowhere on disk. K-8's negation proof is asserted in a commit body and cannot be re-read.
Whatever else this framework fixes next, `L-16` is the precondition for the rest: a control that
nobody can re-read afterwards is indistinguishable from one that never ran — the framework's own
`session_rules.md → Execution proof` says exactly this about test suites, and does not apply it to
its own checklists.

**Where the next batch's defects will come from**, stated in advance so the prediction is checkable:
(1) **self-reported controls with no external verifier** — the per-fix receipt, and every checklist
line L-16 names; (2) **list-insertion renumbering** — it produced L-9 this run and a live instance
(L-15) is already on disk. Both have cheap mechanical answers.

**Suggestion:** Run `/maintenance` with this section as the correction plan. Group 1 first — those
are on `main`. Group 2 is the leverage. `E-1` and `L-20` need owner decisions.

---

**Report status:** `status: COMPLETE` — all 6 agents returned; all 17 dimensions evaluated plus a
Part 1 verification pass over all 17 applied findings and the VERSION bump, and a carry-forward
re-verification of all 20 open findings from Run 3.
**Application status:** `APPLIED — 51 of 51 in sf2fcccf` (31 L findings + the 20 carried from
Run 3; L-20 and the E-1 decision landed earlier in `dd98dea`). **0 open, EXCEPT L-19's second half** (the blocklist-derivation source), which stayed `PARTIAL` — the header's "0 open" contradicted its own row until Run 5's M-38 named it, and M-36 installed the missing half.
E-1 is CLOSED as `accepted-risk` — see its record above.


---

## Post-change checklist receipts (`sf2fcccf`)

*Persisted here per checklist item L-16, on its first execution. Every `ALWAYS REPORT` key the
checklist mandates appears below exactly once.*

**inventory sweep:** 7 surfaces checked, 4 stale claims fixed (README `/audit` x2, README
tier-gated maxima x2). One artifact added — EPA Step 4.1b — and its counts propagated to the EPA
final report.

**instruction style:** 41 new behavioral instructions checked, 0 rewritten. Every one carries a CAPS
imperative on its own line; 12 carry an explicit report format. The `commit` skill went from 0
imperative markers to 5, closing the only module skill that had none.

**references:** 8/8 cited sections resolve, all byte-exact (`### 2a. Ownership...` and
`### 3a. Before dispatch...` re-verified after the renumber) |
**fences:** 6 of the **8** extractions bootstrap performs, measured non-empty at 275/151/55/25/218 markdown lines and 187 js. **Both numbers are wrong** and were corrected by Run 5's M-12: the counts include the two fence delimiters (`sed '1d;$d'` gives 273/149/53/23/216 and 185), and `metrics_md` (9) plus one other were never measured. |
**isolation:** 18 files scanned, 0 hits (grep negation-proved against a seeded string).

**class sweep:** 51 fixes x 4 directions, 14 extra instances found and fixed. Per-fix receipt:

| Fix | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|-----|---------|----------|----------|------------|----------------------|
| L-1 | ✓ grep `cross-cutting` in both commands | ✓ bootstrap Step 1.1 is the model; EPA now mirrors its 3 receivers | ✓ Phase 3 section list + Phase 4 step order | ✓ final report gained the cross-cutting block | EPA's Phase 3 omitted the PRD template's first section |
| L-2 | ✓ grep `### PART 1` — 6/6 relocated | ✓ all six agent blocks, not only the named one | ✓ each `## Agent N` heading | ✓ Phase 2 says “paste in order” — nesting now correct | none |
| L-3 | ✓ grep the guard sentence — was 1, now 6 | ✓ Agents 1,2,5,6 gained it too (back-sweep) | ✓ each FILES TO READ list | ✓ checks vs lists reconciled per block | 4 blocks lacked the rule entirely |
| L-4 | ✓ grep `2 of 15` repo-wide — 3 executed surfaces | ✓ both report score lines | ✓ the counting rule added beside each | n/a | recount rule added so it cannot recur |
| L-5 | ✓ grep every `git ` mandate in audit.md | ✓ n/a — single authorization block | ✓ the write-authorization line beneath | n/a | 4 checks mandated git the rule forbade |
| L-6 | ✓ grep `per category` — 4 sites beyond the named one | ✓ validator.md + framework doc | ✓ the `produces:` block above `:15` | n/a | `:1596` canonical template was the worst |
| L-7 | ✓ grep every `§"` citation — 15 legitimate prefixes | n/a | ✓ D8.8 inherits the same rule | ✓ report column now names 3 verdicts | check fired on the healthy state |
| L-8 | ✓ grep `escalated` across Phase 3 | n/a | ✓ items 2, 3 and 4 all touched | ✓ Phase 2 ledger carries the 5 statuses | accepted-risk added as the closing status |
| L-9 | ✓ grep `Phase 3 item N` — 1 citation, now correct | n/a | ✓ both item bodies | n/a | the swap un-stranded the citation |
| L-10 | ✓ grep Phase 3 items 2-4 for mandated fields | n/a | ✓ `### Detailed Findings` above it | ✓ this IS the descending fix | none |
| L-11 | ✓ grep D6.7's scope clause | ✓ D6.8 runs first and feeds it | ✓ the report's `Declared by` column | n/a | none |
| L-12 | ✓ grep `three bounded questions` | ✓ compared against Agents 3-4's contracts | ✓ the report-format bullets | ✓ 3 bullets added for D17.4-6 | none |
| L-13 | ✓ grep `Application status:` — 3 values in 3 uses | n/a | ✓ beside the meta-observation block | n/a | value set enumerated |
| L-14 | ✓ grep `## Meta-observation` — 3 executions | n/a | ✓ sits before the closing status lines | n/a | same edit as L-13 |
| L-15 | ✓ grep `step N`/`item N` repo-wide | n/a | ✓ item 1's count-sweep paragraph | n/a | live instance in maintenance.md fixed |
| L-16 | ✓ grep all 10 report keys — 8 had no home | ✓ Audit intake step 4 is the receiver | ✓ checklist header | ✓ key-presence self-check added | this section is its own first discharge |
| L-17 | ✓ compared the mandate against its first use | n/a | ✓ item 4's legend and total line | n/a | row-count check + quoted-grep rule |
| L-18 | ✓ re-measured all 20 agent examples | ✓ both claims in examples/README | ✓ the `## Input` invariant beside it | n/a | 20/20 have Output Format; 10 have both |
| L-19 | ✓ grep camelCase identifiers in templates | ✓ examples/ and docs/modules/ both | ✓ D16's surface paragraph | n/a | blocklist now derives from CODE too |
| L-21 | ✓ grep the diagram's output column | ✓ README's table already had it | ✓ Step 4's heading names both files | n/a | none |
| L-22 | ✓ grep numbered headings in the file | n/a | ✓ steps 2 and 3 around it | n/a | numbered 2a; citer auto-updated |
| L-23 | ✓ counted rows vs findings in the first use | n/a | ✓ the total line beneath | n/a | errata written into the report |
| L-24 | ✓ grep trigger attributions | ✓ audit.md words it correctly | ✓ the counting-rule sentence | n/a | same edit as L-4 |
| L-25 | ✓ grep `Verdict-bearing` | ✓ checked all 14 categories | ✓ the produces: list | n/a | 4 of 14 carry no verdict |
| L-26 | ✓ grep `Mutation` | ✓ both declared twins | ✓ the twin rule above | n/a | none |
| L-27 | ✓ grep item numbers in prose | n/a | ✓ the checklist header it contradicts | n/a | none |
| L-28 | ✓ grep every `git status` self-check | n/a | ✓ the expected-result line | n/a | none |
| L-29 | ✓ recounted the Run 2 ledger by row | ✓ both errata now present | ✓ sits above Errata 1 | n/a | same edit as L-23 |
| L-30 | ✓ grep `prd.md` in the template | ✓ bootstrap Step 1 selects the variant | ✓ Setup step 3 allows no-PRD | n/a | none |
| L-31 | ✓ measured frontmatter across all 11 rules | ✓ all three kinds compared | ✓ the `every example` header | n/a | 0/11 rules use the agent frontmatter |
| L-32 | ✓ counted imperatives in all 15 module skills | ✓ compared against session-end | ✓ steps 1 and 6 | n/a | same edit as K-28 |
| K-1 | ✓ grep the verdict table in both twins | ✓ bootstrap + EPA byte-identical | ✓ the remediation sentence that named the fork | n/a | catch-all row + fork comparison |
| K-4 | ✓ grep every reader of a gap declaration | ✓ all three declaring components | ✓ the pass-1 sequence above | ✓ report line for both passes | pass 2 created |
| K-9 | ✓ grep the EPA report headings | ✓ bootstrap Step 15 is the model | ✓ Steps 0.5 and 1.0 mandates | n/a | both freshness verdicts added |
| K-10 | ✓ grep `assets/examples` consumers in EPA | ✓ bootstrap ordering is correct | ✓ the `:332` hedge as the model | n/a | deferred to Step 4.1b |
| K-11 | ✓ grep the 4 template paths in EPA — 0 hits | ✓ Steps 2.6.1/2.6.2 model the shape | ✓ each step's upgrade checks | n/a | 4 create-branches added |
| K-12 | ✓ grep `omit Coverage Gap` — 1 hit left | ✓ code_reviewer + security_reviewer | ✓ the false citation that named it | n/a | citation now says verified |
| K-13 | ✓ grep the declaring set in both twins | ✓ `.claude/` + `docs/modules/` copies | ✓ §1 flow and §3 table | n/a | validator named as third source |
| K-16 | ✓ grep tier-gated maxima in README | ✓ the diagram rows were already ranged | ✓ the note paragraph | n/a | none |
| K-22 | ✓ grep the OUTPUT column | ✓ all three rows now ranged | ✓ the footnote explains the range | n/a | none |
| K-24 | ✓ grep every `§"` in the file | ✓ the sibling citations resolve | ✓ the same line's other two cites | n/a | none |
| K-25 | ✓ grep the archetype-branch consumers | ✓ step 1 models the third verdict | ✓ the count self-check below | n/a | deferred vs N/A distinguished |
| K-26 | ✓ recounted the diff | n/a | ✓ the ledger line | n/a | 13 → 14 |
| K-27 | ✓ grep every deferral clause in the file | ✓ rules-agents-updater models it | ✓ the Step 3 checkpoint above | n/a | none |
| K-28 | ✓ counted imperatives in all module skills | ✓ session-end is the contrast | ✓ steps 1 and 6 | n/a | same edit as L-32 |
| K-29 | ✓ measured all 11 rules examples | ✓ agents/skills/rules compared | ✓ the `every example` header | n/a | same edit as L-31 |
| K-31 | ✓ grep the closer in all 5 gap blocks | ✓ 4 of 5 carry it | ✓ the block's Recommend line | n/a | divergence now declared |
| K-33 | ✓ grep the process-skill sub-lists | ✓ bootstrap's list is complete | ✓ the `[N of 12]` header | n/a | 9 → 12; component-design check added |
| K-34 | ✓ grep `new component:` — 2 homes to 1 | ✓ item 8 has a self-check, item 9 had none | ✓ the New-component section | n/a | item 9 now points + self-checks |
| K-35 | ✓ grep `/audit` in README — 1 of 3 surfaces | ✓ both enumerations | ✓ the `5 session modes` sentence | n/a | added to both lists |
| K-37 | ✓ grep the D16 surface list | n/a | ✓ the blocklist-derivation sentence | n/a | exemption stated explicitly |

Row count: **51**. Findings applied: **51**. Equal — the L-17 self-check passes.

**back-sweep:** 3 process rules promoted this session, each applied backward in the same session.
(1) *"every file a check names must appear in FILES TO READ"* -> all **6** agent blocks, not the 1
it was written into. (2) *"a score is recomputed from the ledger, never copied from a summary"* ->
the 3 executed surfaces **and** both report score lines. (3) *"receipts must be persisted"* -> this
section is its own first discharge.

**liveness:** 25 components OK — full YAML parse over every raw and fenced frontmatter in
`docs/modules/agents/` and `docs/modules/skills/`. The bundled guard reached 1 component and ran in
`modo estrutural — js-yaml ausente`, so the full parse was run alongside it rather than reported as
if the guard had covered everything. Raw agent templates: OK.

**negation proof:** 4 new self-checks written this session, all proved -> red as expected. L-16's
key-presence check went red on the pre-persistence state (5 keys missing); L-17's row-count check
went red on Run 4's own 14-vs-17 receipt and green on 17-vs-17; K-34's `new component:` count went
red at 0; L-15's ordinal sweep found the live "Without step 7" instance it was written to catch.
**Each was also run against the HEALTHY state and confirmed GREEN** — the omission that shipped
K-8 and L-7, both of which fired on the normal case.

**version:** v2.12.0 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.

**classification:** 38 add, 13 substitute, 0 delete, 0 relocate. Every SUBSTITUTE replaced text that
was false (L-4, L-6, L-18, L-26), fired on the healthy state (L-7, K-1), or contradicted a
neighbouring instruction (L-5, L-27, K-12). No RELOCATE, so §5's grep is N/A.

**new component:** none this session — EPA Step 4.1b is a step inside an existing command, not a
skill, agent, rule or script.

**verification audit:** proposed — trigger (d) and, the bump being MINOR, trigger (b); one
verification run satisfies both.


---

# Run 5 — verification pass over `f2fcccf`

**Date:** 2026-09-02
**Framework version:** v2.12.0 (`README.md:1`), HEAD `ed37ec9`, `origin/main` == HEAD
**Run mode:** `verification (over f2fcccf)` — CLAUDE.md trigger (d); trigger (b) satisfied by the same run
**Under verification:** the 51 findings applied in `f2fcccf` + the VERSION bump
**Dimensions checked:** 17 + a Part 1 fix-verification pass + a first-ever RECEIPT verification pass
**Agents dispatched:** 6

**carried: none** — Run 4 closed with 0 `open`.
**E-1: accepted-risk — reported as an OBSERVATION only, never re-opened.** Working tree re-verified
clean (0 occurrences); the term persists in 39 revisions of one file, all pushed; the record states
what would re-open it. No history rewrite proposed.

> **ID scheme.** `F-*`/`G-*` (2026-08-31), `H-*` (09-01), `J-*` (Run 2), `K-*` (Run 3), `L-*` (Run 4).
> This run uses `M-*`.

**What was new about this run.** `f2fcccf` was the first batch whose post-change checklist receipts
were PERSISTED (finding L-16). Every agent was therefore asked to re-measure the receipts' own
claims, not only the fixes. That pass is reported separately below and is the most consequential
part of this run.

---

## Part 1 — Verification ledger for `f2fcccf`

**Score, counted over the FINDINGS: 24 of 51 clean, 27 not clean.** VERSION clean (row total
25/52). Of the 27: **15 introduced-a-defect**, **8 class-not-swept**, **7 partially-fixed**
(K-1, K-9 and K-16 fall in two categories each).

### Clean (24)
`L-2` (all 6 Part 1 slots correctly relocated) · `L-5` (authorization now permits the read-only git
its checks mandate; no surviving contradiction) · `L-6` (banned vocabulary: 0 framework-layer
survivors; 3-way sync re-verified) · `L-7` (**the recalibrated D8.7 was RUN: 0 false positives on
15+ sanctioned-prefix sites, 2 genuine REDs caught** — green on healthy, still red on a non-heading
target) · `L-12` · `L-13` · `L-14` · `L-15` (rule landed; see M-24 for the fact that it was never
run on its own batch) · `L-17` (rule landed; see M-16 for the receipt violating it) · `L-18`
(every number re-measured TRUE, including the unstated half: `## Output` precedes `## Output Format`
in 10/10) · `L-21` · `L-22` · `L-25` (10 verdict-bearing of 14, measured exactly) · `L-28` · `L-31`
(11/11 rules carry `domain:`/`applies_to:`, 0/11 carry the agent frontmatter) · `L-32` · `K-22`
(baseline re-measured = 7 agents; range exact) · `K-24` · `K-25` · `K-26` · `K-27` · `K-29`
(exactly 6 of 11, and the 5 without match the named list byte-for-byte) · `K-33` · `K-35` ·
**VERSION** (v2.12.0 on 3/3 surfaces, 5 occurrences; 0 of 22 lineage fields rewritten; MINOR
correct).

### Not clean (27)

| ID | Verdict | Evidence |
|----|---------|----------|
| L-1 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | The whole fix was pasted TWICE → **M-1**. Receivers named but undeclared → **M-2**. Wrong analogue chosen → **M-3**. Two further defects → **M-4**, **M-5**. |
| L-3 | **PARTIALLY-FIXED** | Guard rule reached all 6 blocks ✓ — but **fails on 4 of them**, including 3 of the 4 that received it this batch → **M-23**. |
| L-4 | **PARTIALLY-FIXED** | All four figures independently recounted **TRUE** on all 3 executed surfaces ✓. The 4th surface the finding named (the report's own Errata 1) was not corrected → **M-51**; and the replacement text added a new false claim → **M-46**. |
| L-8 | **PARTIALLY-FIXED** | Five statuses enumerated ✓, carry-forward reaches `escalated` ✓, `accepted-risk` closed ✓. Three residues → **M-26**, **M-34**, **M-35**. |
| L-9 | **CONFIRMED-FIXED — INTRODUCED A DEFECT** | Numbering sequential; item 6 now genuinely last. But the swap **stranded two other ordinals** → **M-24**. |
| L-10 | **PARTIALLY-FIXED** | Findings ledger added ✓. `carried:` still has no `escalated` half and there is no accepted-risk lane in Recommended Fixes → **M-26**. |
| L-11 | **PARTIALLY-FIXED — CLASS NOT SWEPT** | D6.7's ordering dependency stated ✓; the report format and D6.5/D6.6 remain two-source-scoped → **M-13**. |
| L-16 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Both homes named, receiver wired, scoping clause present ✓. **The self-check goes RED on the state it created** → **M-31**; heading level contradicts itself → **M-32**; key list omits two mandated keys → **M-33**. |
| L-19 | **PARTIALLY-FIXED — CLASS NOT SWEPT** | Source 3 stated in `CLAUDE.md` ✓ — **and never installed into `audit.md`**, the executed check → **M-36**. Its own status still reads `PARTIAL` while the header claims 0 open → **M-38**. |
| L-23 | **CONFIRMED-FIXED — defect (LOW)** | Errata added ✓; mislabelled → **M-45**. |
| L-24 | **PARTIALLY-FIXED** | Attribution corrected ✓ but factually wrong → **M-46**; the wrapping half untouched (3 lines of 142/167/131 chars) → **M-41**. |
| L-26 | **FIXED-BUT-CLASS-NOT-SWEPT** | `Mutation Tests` synced in the two declared twins — a **third shipped report template** was missed → **M-14**. |
| L-27 | **CONFIRMED-FIXED — CLASS NOT SWEPT** | Workflow line de-ordinalised ✓; the identical surface 20 lines above survived → **M-52**. |
| L-29 | **CONFIRMED-FIXED — defect (LOW)** | Recount verified exact ✓; mislabelled → **M-45**. |
| L-30 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Template variant landed ✓; the selecting instruction sits in Step 1, before CLAUDE.md exists → **M-6**. |
| K-1 | **CLASS NOT SWEPT — INTRODUCED A DEFECT** | 7-row table incl. CATCH-ALL and the fork comparison ✓, twins byte-identical ✓. The introducing sentence still says "**four** outcomes" → **M-7**; both final reports enumerate only 4 of 7 verdicts → **M-8**. |
| K-4 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Pass 2 verified reachable, non-contradictory, correctly placed ✓. Its report line has no destination → **M-18**; it asserts an exclusivity that is false → **M-19**; `BASELINE-CREATED` is unreachable on the path it opened → **M-20**; `:168` still says "spawn validator LAST" → **M-21**. |
| K-9 | **CLASS NOT SWEPT — INTRODUCED A DEFECT** | Freshness slot added — **duplicated** → **M-1**; verdict list truncated → **M-8**; the Reading Report still has no slot → **M-53**. |
| K-10 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | Precondition + deferral added ✓ — deferred to a step that never receives it → **M-9**. |
| K-11 | **CONFIRMED-FIXED (paths) — INTRODUCED A DEFECT** | All four clauses name the correct template and destination ✓. Direction inverted in 4/4 ("continue with the checks **above**" while sitting above them); one clause names the wrong file; the justification is false in two of the four → **M-10**. |
| K-12 | **CONFIRMED-FIXED — defect (LOW)** | Banned line gone repo-wide ✓; downstream sanction removed ✓; `code_reviewer`'s citation now TRUE ✓. The new sentence cites the wrong line number → **M-15**. |
| K-13 | **FIXED-BUT-CLASS-NOT-SWEPT** | §1/§3 name three sources in both twins ✓. **Five more reviewer-only formulations survive per twin**, including §2's canonical Pushy Description template → **M-57**. |
| K-16 | **INTRODUCED A DEFECT — CLASS NOT SWEPT** | Baseline re-measured correct ✓ — **but the new tier attribution is FALSE**: `production` already receives all 15 skills and 5 rules → **M-40**. |
| K-28 | **PARTIALLY-FIXED** | Steps 1, 4, 6 gained imperatives; **steps 2, 3 and 5 remain wholly descriptive** → **M-47**. |
| K-31 | **CONFIRMED-FIXED — INTRODUCED A DEFECT** | Divergence declared ✓ — **inside the blockquote**, which is the literal text the reviewer emits into every report → **M-17**. |
| K-34 | **CONFIRMED-FIXED — INTRODUCED A DEFECT** | Item 9 points instead of restating ✓. Its self-check is unscoped, has no path operand, and **returns 3 where it expects 1** → **M-27**. |
| K-37 | **PARTIALLY-FIXED** | Exemption stated in `CLAUDE.md` ✓ — absent from `audit.md`'s surface list, where the executing agent reads it → **M-36**. |

---

## Part 1b — RECEIPT verification (first execution)

The receipts survive external verification on every **mechanical** claim and fail on the two claim
types nobody could check before.

| Receipt claim | Independently re-measured | Verdict |
|---|---|---|
| `version: v2.12.0 … 5 occurrences / 3 surfaces; 0 lineage rewritten` | 5 occurrences, 3 surfaces; `grep '^[-+]created:'` empty; 22 fields intact | **TRUE** |
| `class sweep: row count 51 = findings applied 51` | 51 rows, 51 distinct IDs | **TRUE** |
| `isolation: 18 files scanned, 0 hits` | `git show --stat` = 18; 0 hits re-confirmed over the 107-file superset | **TRUE** |
| `liveness: 25 components OK` | re-run, exit 0 | **TRUE** |
| `fences: (275, 151, 55, 25, 218; 187)` | **273, 149, 53, 23, 216; 185** — every number **+2** | **FALSE → M-12** |
| `fences: 6/6 templates` | bootstrap performs **8** extractions; 2 never measured | **FALSE → M-12** |
| `references: 8/8 cited sections resolve, all byte-exact` | ~31 named-heading citations + ~44 `component-design §N` across 5 sections; **undercounts ≥3×**, and 3 of the citations are broken | **FALSE → M-25, M-24** |
| `instruction style: 41 new behavioral instructions, 0 rewritten` | "41" is not reproducible from any stated unit; 13 existing imperative lines WERE rewritten | **UNVERIFIABLE AS STATED → M-54** |
| `…closing the only module skill that had none` | **3 more module skills still have 0 imperative markers**: config-file-updater, context-recovery, cross-cutting-analysis | **FALSE → M-48** |
| `negation proof: each was ALSO run against the HEALTHY state and confirmed GREEN` | **Two of the four go RED on the healthy state** (L-16's key-presence check, K-34's item-9 count) | **FALSE → M-31, M-27** |
| `inventory sweep: 7 surfaces checked` | no surface list given | **UNAUDITABLE AS WRITTEN → M-54** |
| Per-fix row `K-1 \| ADJACENT ✓` | missed the stale "four" count on the same line it edited | **OVERSTATED → M-7** |
| Per-fix row `K-9 \| ✓ both freshness verdicts added` | the insertion duplicated the whole block | **OVERSTATED → M-1** |
| Per-fix row `L-3 \| ✓ checks vs lists reconciled per block` | reconciled on 2 of 6 | **OVERSTATED → M-23** |
| Per-fix row `L-9 \| ✓ grep "Phase 3 item N" — 1 citation, now correct` | three exist; two are wrong | **OVERSTATED → M-24** |
| L-17's own rule: "a `✓` with no quoted pattern is RED" | **116 of 148 `✓` cells carry no quoted pattern** | **THE RECEIPT VIOLATES ITS OWN RULE → M-16** |
| L-17's rule: "a `✗` with no reason is RED" | 0 `✗` cells in 51 rows — zero self-declared misses in a batch where this audit found ≥4 | vacuously satisfied → **M-16** |

---

## New findings

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| M-1 | `EPA:617-635`, `:682` & `:703`, `:970-983` | **L-1's fix was pasted TWICE — three blocks duplicated.** The Phase-3 cross-cutting paragraph appears twice with divergent wording; **Step 4.1b is defined twice and the two copies contradict each other** on who writes the concerns (`Written by → "Step 2.7.1 — RECEIVER"` vs `Receiver step → "Step 2.7.1"`, while both bodies say 4.1b writes); the final-report block appears twice. An executed command template now instructs the executor to emit two mandatory sections twice, and EPA has a duplicate step number. Confirmed by direct grep (2/2/2). | applied sd4d3c33 |
| M-2 | `EPA:274`, `:322`, `:445` | **The three receivers L-1 names carry no receiver declaration.** Bootstrap's model puts `**This step is the RECEIVER of Step 1.1's X concerns**` inside Steps 3/4/13; EPA's Steps 2.2/2.3/2.7.1 got nothing. The routing table points at steps that do not know they receive. | applied sa2f4890 |
| M-3 | `EPA:445` vs `:837` | **Wrong analogue chosen for the CODE row.** It maps to Step 2.7.1 (codebase-derived, Phase 2) when bootstrap's Step-13 equivalent is EPA **Step 4.6** "Pre-create domain rules from retroactive PRD" — the PRD-derived step that runs after the PRD exists. | applied sd4d3c33 |
| M-16 | `assets/docs/audit-2026-09-02.md:739-789` vs `maintenance.md:190-194` | **The per-fix receipt violates the quote-or-RED rule that the same commit wrote — 116 of 148 `✓` cells.** Most cells describe a grep instead of quoting it (`✓ grep the guard sentence`) or name a location (`✓ each ## Agent N heading`). And **0 `✗` cells in 51 rows**, in a batch where this audit found at least four missed directions. L-17 predicted this exact failure ("a `✓` that was never run … no reader verifies the grep"); one run in, confirmed with four instances. | applied sd4d3c33 |
| M-23 | `audit.md:120-123` (A1), `:238-245` (A2), `:451-465` (A4), `:565-576` (A5) | **The guard rule fails on 4 of the 6 blocks it now governs**, including 3 of the 4 that received it this batch. Agent 1 lists none of D16's five surfaces (project docs, `.claude/docs/`, agent memory, git history, templates); Agent 2 omits the framework doc D11.7 names; Agent 4 omits `.claude/skills/cross-cutting-analysis/` — the second half of D10.4's own comparison; Agent 5 says "**Sample** from examples/" while D15.7 says "List **all**". | applied sd4d3c33 |
| M-31 | `maintenance.md:96-101` | **L-16's key-presence self-check goes RED on the healthy state it created.** `new component:` occurs 3× inside this run's receipts section — the receipts legitimately QUOTE the keys as grep patterns — and the mandate says "a duplicate WITHIN the section is RED". Same K-8/L-7 inversion, produced by the fix written to avoid it. Confirmed by direct measurement (3, expected 1). | applied sd4d3c33 |
| M-36 | `CLAUDE.md:227-234` vs `audit.md:155-170` | **Two rules were documented and never installed.** The three-source blocklist (L-19) and the `settings.local.json` exemption (K-37) landed only in `CLAUDE.md`; `audit.md`'s D16.1/D16.2/D16.3 — the check the D16 agent actually executes — still specify two sources and omit the exemption. `grep "CODE identifier\|THREE sources" .claude/commands/` → **0 hits**. The two documents now contradict. This inverts checklist item 1's own "executed surfaces first, descriptive second", and is the §9 class the repo catalogued: the mandate lives with the reader, not the invoker. | applied sa2f4890 |
| M-39 | `maintenance.md` item 5 | **Item 5 (back-sweep) is the only checklist item with NO mechanical form** — `back-sweep: [rule] applied to N artifacts` has no expected result and no verifier. It is precisely the control for the single mechanism behind every defect in this run: **a rule promoted in session N is applied to session N's own batch only by the author's memory, at the end of the session.** L-15's ordinal sweep existed in the commit that stranded two ordinals; L-17's quote rule existed in the commit whose receipt breaks it 116 times; L-16's and K-34's checks were declared GREEN against a state where both are RED. Highest-leverage gap in the repo. | applied sa2f4890 |
| M-40 | `README.md:35`, `:157` | **K-16 replaced a wrong flat claim with a wrong tier attribution.** "the full set only at `production-financial`" is false: `bootstrap.md:152-157` gates codebase-audit/skill-gate at internal-tool+ and framework-audit/ops-rules/quality-budgets at **production+**, so a `production` project already receives all 15 skills and all 5 rules. The same mis-attribution survives at `:35`. | applied sa2f4890 |
| M-43 | `.claude/commands/prd_change.md:49` | **The first execution of the Source-3 blocklist found a live instance the sweep missed.** A source-project function name (41 occurrences in project code) appears in a second framework site; the accepted-risk record names only one borderline instance. Neither fixed nor recorded. | applied sa2f4890 (owner decision) |
| M-46 | `CLAUDE.md:139-140` | **L-24's replacement text is factually false.** It says "the first three ran under trigger (c)/owner request; trigger (d) was created after them" — `git log -S` places trigger (d) in `a28f661`, and Run 3 ran after it and cites (d) in its own header. A fix for a wrong attribution introduced a different wrong attribution. | applied sa2f4890 |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| M-4 | `EPA:682`/`:703` | 4.1b claims to be "**the one place** where a Phase-2 artifact is legitimately reopened" — false: Phase 3's closing line reopens Step 2.1's CLAUDE.md, and Step 4.6 reopens Step 2.7.1's rules. | applied sd4d3c33 |
| M-5 | `EPA` Phase 3 vs Step 4.1b | 4.1b reports `R rules, A decisions, T tasks (of N identified)`, but **EPA's Phase 3 has no classification step** — the sender defines N and never splits R/A/T. Bootstrap's Step 1.1 does. | applied sd4d3c33 |
| M-6 | `bootstrap.md:92` | L-30's selecting instruction ("ALWAYS REWRITE the generated CLAUDE.md's `**PRD:**` line") sits in **Step 1**; CLAUDE.md is generated in **Step 2**, which never mentions the variant. Also, the with-PRD path is never told to strip the `[or, when bootstrapped WITHOUT a PRD: …]` bracket. | applied sa2f4890 |
| M-7 | `bootstrap.md:72`, `EPA:72` | "ALWAYS REPORT one of the **four** verdict strings above" — the line now enumerates **seven**. Stale count in the introducing sentence, both twins, on the same physical line K-1 edited. | applied sa2f4890 |
| M-8 | `bootstrap.md:726-727`, `EPA:971`, `:978` | The final-report slots express only 4 of the 7 verdicts K-1 created; the three new RED strings have no slot. | applied sa2f4890 |
| M-9 | `EPA:447` → `:682`/`:703` | K-10's deferral names Step 4.1b as receiver; 4.1b is exclusively about cross-cutting concerns and never mentions deferred domain rules. The real receiver (**Step 4.6**) already existed and is not named. | applied sa2f4890 |
| M-10 | `EPA:276`, `:326`, `:349`, `:387` | K-11's four clauses each say "continue with the upgrade checks **above**" while sitting **above** them (4/4 inverted); `:387` names `code-reviewer.md` inside the **security-reviewer** step; the justification ("Steps 4.6.5 and 5.1 read it unconditionally") is **false for project.md and pendencias.md**. | applied sa2f4890 |
| M-13 | `audit.md:410-412`, `:341`, `:343` | L-11's second half untouched: D6's REPORT FORMAT hardcodes two sub-bullets, and D6.5/D6.6 remain "reviewer"-scoped — under D6.6's literal wording a validator-only gap reads as an orphaned specialist. | applied sa2f4890 |
| M-14 | `validation-orchestrator/SKILL.md:185` | A **third shipped report template** carries `- Mutation:` and was missed; the twin rule declares only two twins, so this surface no sync rule reaches. It also omits four categories. | applied sa2f4890 |
| M-17 | `code_reviewer.md:239-243` | K-31's declaration was placed **inside the `>` blockquote**, which in this file is the literal text the reviewer emits. Every Code Review Report declaring a visual regression gap will now contain framework meta-commentary including the literal string ``(`/audit` K-31)``. | applied sa2f4890 |
| M-18 | `validation-orchestrator:136-137` | K-4's mandated `coverage gaps: pass 1 … \| pass 2 …` names no destination, and the skill's own report format has no coverage-gaps slot. | applied sa2f4890 |
| M-19 | `validation-orchestrator:129-130` vs `code_reviewer.md:235-238` | K-4 asserts "the validator is the **only** declarer of the visual regression gap on the UI path" — false; code-reviewer declares it on the same trigger, and the same commit wrote a NOTE in code_reviewer saying the validator "ALSO" declares it. Two files, opposite exclusivity claims, one batch. | applied sa2f4890 |
| M-20 | `validator.md:88-92` | `BASELINE-CREATED` is unreachable on the path K-4 opened: on the validator-declared route the specialist runs in pass 2, after the UI row is written. | applied sa2f4890 |
| M-21 | `validation-orchestrator:168` | Still reads "spawn validator LAST"; pass 2 spawns a specialist after it. Undeclared tension. | applied sa2f4890 |
| M-24 | `audit.md:18`, `assets/docs/audit-2026-09-02.md:498` | **L-9's swap stranded two ordinals.** `audit.md:18` cites "(Phase 3 item 5)" for the commit, now item 6; the report cites "Phase 3 item 6" for the pointer rule, now item 5. **L-15's ordinal sweep — written in the same commit — was never run on its own batch.** | applied sd4d3c33 |
| M-25 | `audit.md:51`, `:511` | Both cite `CLAUDE.md` → "When it runs", a **bold paragraph lead-in**, not a heading. Caught by the recalibrated D8.7 on its first execution. | applied sa2f4890 |
| M-26 | `audit.md:780`, `:829-834` | L-8's DESCENDING miss: Phase 2's header offers `carried: [N] open` with no `escalated` half, and Recommended Fixes still has three groups with no escalation/accepted-risk lane. | applied sa2f4890 |
| M-27 | `maintenance.md:317-319` | K-34's self-check has **no path operand**, no scoping clause, and returns **3** where it expects 1. Not runnable verbatim. | applied sd4d3c33 |
| M-33 | `maintenance.md:97-106` | L-16's key list omits `fences:` and `isolation:`, though item 3 mandates all three and forbids collapsing them. | applied sd4d3c33 |
| M-35 | `audit.md:150-227` | Agent 1's D16 block has no instruction to read the prior report or emit the accepted-risk observation item 4 mandates — so the agent executing D16 will re-report E-1 as a hit every run. | applied sd4d3c33 |
| M-37 | `maintenance.md` "Audit intake" | **Writing an accepted-risk record has no instruction.** `dd98dea` authored one; `audit.md` CONSUMES such records but nothing says who writes them, where they live, or what fields they carry. | applied sa2f4890 |
| M-38 | `assets/docs/audit-2026-09-02.md:566`, `:567`, `:711` | L-19's status still reads `PARTIAL … stays open` while the header claims `0 open`; and `s[pending]` was never resolved to `dd98dea` for L-19 or L-20, leaving Audit intake step 4's `applied sHASH` mandate unsatisfied. | applied sa2f4890 |
| M-42 | `docs/modules/skills/README.md:8-9` | The "6 implementation" label is wrong: the residual 6 are 1 during-implementation + 5 session-end, which is how `bootstrap.md:346-352` and the README's own table categorise them. Two surfaces, two categorisations. | applied sa2f4890 |
| M-44 | history, 4 files, 226 occurrences | L-20's replaced filename survives in history with **no accepted-risk record**. By E-1's own standing instruction ("any identifier of a different class remains a finding") this residue is uncovered and will resurface every run. | accepted-risk — see the M-44 record |
| M-47 | `commit/SKILL.md:26-34`, `:43-45` | K-28 half-stands: steps 2, 3 and 5 remain wholly descriptive with no imperative and no report line. 1 of 6 steps reports. | applied sa2f4890 |
| M-48 | `config-file-updater`, `context-recovery`, `cross-cutting-analysis` | **Three module skills still have 0 imperative markers**, so both L-32's finding text and the receipt's "the only module skill that had none" are false. `context-recovery` is `invocation: user` — the same lifecycle class as `commit`. Confirmed by direct measurement. | applied sa2f4890 |
| M-49 | `sprint-proposer/SKILL.md:37-39` | "Each check is **silently skipped** if the corresponding skill folder was not copied" — the skill literally sanctions SILENCE, and Step 0 has no report line, while Step 1 in the same file models the correct shape. §9 rule 3. Pre-existing, never flagged. | applied sa2f4890 |
| M-51 | `assets/docs/audit-2026-09-02.md:63-72` | L-4's Group-1 instruction said "correct 3 surfaces **+ the errata**"; Errata 1 still quotes the uncorrected "2 PARTIALLY-FIXED" with no recount note. | applied sa2f4890 |
| M-52 | `maintenance.md:11-14` | L-27's class survived 20 lines above the fix: "four numbered items below MANDATE it (**item 4's**…, **item 5's**…, **item 6's**…, **item 7's**…)" — verbatim the inventory-by-ordinal surface L-27 was raised on, in the Authorized-operations block the same batch edited. | applied sa2f4890 |
| M-53 | `EPA:177-210` | The Reading Report, where Step 1.0's project-copy verdict naturally belongs, still has no slot for it. | applied sa2f4890 |
| M-54 | `maintenance.md` items 1 and 2 | Two receipts are **unauditable as written**: `inventory sweep: N surfaces checked` names no surfaces, and `instruction style: N instructions` defines no unit. A receipt that cannot be re-measured is not falsifiable, which is the property L-16 exists to create. | applied sa2f4890 |
| M-55 | `existing_project_adaptation.md` Phase 3 | EPA's Phase 3 omits four PRD sections the template defines (`2.3 Constraints`, `5.2 External Integrations`, `5.4 Build Order`, `9. Risks and Dependencies`), so an adapted project's retroactive PRD is structurally short of a bootstrapped one. **Filed with no ledger row until `/audit` 2026-09-03 N-4** — it lived only in a parenthetical note and in the class-sweep table, while counting toward "57 of 57 disposed". | applied `sa2f4890` |
| M-56 | `CLAUDE.md` Repository Lifecycle, `maintenance.md` | `git push` had no written owner: no command authorized it, none forbade it, and five pushes had happened. It is a privacy boundary, not a convenience — D16 triage keys on the unpushed/pushed line. **Filed with no ledger row until `/audit` 2026-09-03 N-4.** | applied `04cb256` (owner decision 2026-09-03: push only on explicit request, after a GREEN D16) |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| M-11 | `EPA:965-1045` | The adaptation report has no slot for the risk profile, the tier-gated MACRO skeletons, the CI floor, `assets/examples/`, the frontmatter guard, or plugin enablement. `grep` over that range → 0 hits. Bootstrap's report got the slots; the twin did not. | applied sa2f4890 |
| M-12 | receipts | Fence line counts are uniformly **+2** (counted the `sed` range including both fence lines, without the `sed '1d;$d'` the pipeline applies) and the denominator is `6/6` where bootstrap performs **8** extractions. | applied sa2f4890 |
| M-15 | `security_reviewer.md:283` | K-12's new sentence cites `:39` as where the field is declared; it is at `:41` (and ~`:36` once extracted). `code_reviewer` avoided this by citing "the report schema at the top of this file". | applied sa2f4890 |
| M-57 | both `component-design` twins, §1/§2/§3 | **K-13's residue — five reviewer-only formulations survive per twin**, including **§2's canonical Pushy Description template** (`or when [reviewer] declares a [domain] gap`), the schema every new specialist is copied from, which cannot express `validator`. Also §1's "Why this exists" and step 1, §2's "MUST echo the reviewer's gap declaration", and §3 row 3 — one row below the row that WAS fixed. Compounding: `bootstrap.md:593` groups the validator with "spawned by protocol, not by gap declaration", and both chain-validation greps search only the two reviewers. **(Originally mis-filed under M-16, an ID already in use — collision created by Run 5's own report, corrected here.)** | applied sa2f4890 |
| M-22 | `audit.md` D7.5 | `red_team` and `blue_team` are protocol-spawned like validator/arbitrator but absent from D7.5's exemption list, so both read as PARTIAL with no sanction. | applied sa2f4890 |
| M-28 | `audit.md:458-460` | Agent 4's copy of the guard rule is not byte-identical to the other five ("the working check" vs "a working check"; citation `K-15` vs `K-15, L-3`). | applied sd4d3c33 |
| M-29 | `audit.md:691` | Agent 6's list is headed `INPUTS:`, so the guard rule's "this list" has no referent by that name there. | applied sd4d3c33 |
| M-30 | `audit.md:480-491` | D8.7's SANCTIONED list covers prefix and trailing parenthetical but not a dropped **leading ordinal** (`§"MODEL SWITCH entries"` → `### 4. MODEL SWITCH entries`); the RED list saves it, but the two are not co-extensive. | applied sa2f4890 |
| M-32 | `maintenance.md:93`, `:397` vs `:98` | The mandate writes the receipts under `###`; the self-check greps `##`; the discharge used `##`. `grep '### Post-change checklist receipts'` → 0 hits. A run that obeys the mandate makes the check vacuous. | applied sd4d3c33 |
| M-34 | `assets/docs/audit-2026-09-02.md:594` | The executed E-1 row reads `RESOLVED FORWARD`, a value outside the enumeration L-8 had just created. | applied sa2f4890 |
| M-41 | `CLAUDE.md:138`, `:139`, `:143` | L-24's wrapping half untouched: three lines of 142/167/131 chars in a file wrapped at 76-97. The 250-char line was split, not wrapped. | applied sa2f4890 |
| M-45 | `assets/docs/audit-2026-09-02.md:63`, `:471` | Both new errata are labelled "added by **Run 5**"; L-29 and L-23 are Run 4 findings and Run 5 had not happened when they were written. | applied sa2f4890 |
| M-50 | `skill-gate/SKILL.md:157-162` | `### After promotion — MANDATORY in EVERY mode` mandates a creation eval with no report line. | applied sa2f4890 |

---

### Accepted-risk record — M-44 (owner decision, 2026-09-03)

- **Decision:** accept the history residue; do NOT rewrite history.

- **Working tree:** the source project's non-standard backlog filename was replaced with the
  placeholder `[nome-fora-do-padrao].md` at both `existing_project_adaptation.md` sites in
  `dd98dea`. **Measured residual in the working tree: 0.**
- **Residue:** the original string survives in **226 occurrences across 4 files** in git history,
  all reachable from `origin/main`. It is a generic Portuguese filename — "pending items and
  improvements" — naming no person, client, domain, infrastructure identifier or secret. A
  `git filter-repo` + force push would break every existing clone and any fork, which is
  disproportionate to that exposure.
- **Standing instruction to future `/audit` runs:** report this history residue as an OBSERVATION
  citing this record. **NEVER as `open`, and NEVER as grounds to propose a history rewrite.**
- **What WOULD re-open it:** a NEW occurrence in the working tree, or any identifier of a
  different class — project name, client, person, deployment domain, infrastructure id, secret
  value or PII. Those are not covered here and remain findings.
- **Relationship to E-1:** same class, same disposition, same five fields. E-1's standing
  instruction explicitly said "any identifier of a different class remains a finding", which is
  what left this residue uncovered until Run 5's M-44 named it. Two records now cover the two
  known history residues; a third identifier class would need its own.

## Summary matrix (Part 2 — the 17 dimensions)

| # | Dimension | Agent | Status | Finding |
|---|-----------|-------|--------|---------|
| A | Dual placement | 1 | PASS | 15 module skills; `.claude/skills/` = 1 entry; `diff -r` byte-identical |
| C | Instruction style | 5 | **FAIL** | 6 instances → M-7, M-8, M-1, M-47, M-49, M-50 |
| E | Version consistency | 5 | PASS | v2.12.0 on 3/3, exactly 5 occurrences; 0 of 22 lineage fields rewritten; MINOR correct |
| D4 | Skills README vs disk | 1 | PASS | 15=15, names 1:1, counts and the 7/8 `invocation:` claim all re-measured TRUE; one label mismatch → M-42 |
| D5 | Bootstrap file refs | 2 | PASS | 41/41 framework-side paths resolve; **8/8 fence extractions RUN, none empty**; 16/16 naming mappings |
| D6 | Gap vocabulary | 3 | **FAIL** | 10/10 phrases exact; **install-table diff EMPTY on both paths**; D6.8 confirms exactly 3 declarers → M-13, M-57 |
| D7 | Pushy Description | 3 | PASS | 30/30 parse; 20/20 specialists carry all five elements; 0 anti-pattern instances → M-22 (LOW) |
| D8 | Orchestrator refs | 4 | **FAIL** | All names resolve; **D8.7 RUN: 0 false positives, 2 genuine REDs** → M-25, M-24 |
| D9 | CLAUDE.md diagram | 1 | PASS | Every path and every count correct, re-verified after this batch's edits |
| D10 | Command → skill paths | 4 | **FAIL** | **D10.4 byte-identity RUN (cmp exit 0); D10.6 parity RUN (4/4 both directions)** → M-25, M-24 |
| D11 | Bootstrap sequencing | 2 | **FAIL** | bootstrap clean (25 steps, monotonic, 3/3 handoffs received). EPA has a **duplicate step number** and 4 forward references → M-1, M-6, M-9, M-2 |
| D12 | Examples conventions | 5 | PASS | **0 violations; 18 universal claims truth-tested, all TRUE**; no under-claim introduced by the per-kind framing |
| D13 | PRD ↔ bootstrap/EPA | 2 | **FAIL** | `## Cross-cutting Concerns` exists and EPA's citation is accurate; EPA's Phase 3 still omits 4 template sections → M-55 |
| D14 | Bootstrap output | 2 | **FAIL** | 14/15 pointers traced; the `**PRD:**` line is untraced → M-6. EPA's report is missing 6 artifact classes → M-11 |
| D15 | Doc factual accuracy | 5 | **FAIL** | Every count claim TRUE; **banned-"inline" survivors: 0**; 4 new inaccuracies → M-7, M-46, M-45, M-1 |
| D16 | Project-info isolation | 1 | PASS | **Source 3 executed for the first time**: ~180 candidates, 5 confirmed, 4 legitimate. Working tree 0 hits across 107 files, 10 memory files, 114 commits (contents and messages). Control gaps → M-36, M-43, M-44 |
| D17 | Process coverage (meta) | 6 | **FINDINGS** | No unowned operation TYPE except two → M-56 (`git push`), M-37. Self-checks: 2 of 4 new ones miscalibrated → M-31, M-27. Back-sweep has no mechanical form → M-39 |

*(M-55: EPA Phase 3 omits `2.3 Constraints`, `5.2 External Integrations`, `5.4 Build Order`, `9. Risks and Dependencies`. M-56: `git push` on the mother repo has no written owner — 5 pushes in the reflog, every push string in the repo governs project repos or forbids it, and D16.3c's entire unpushed/pushed triage is decided by an act nothing instructs.)*

---

## Recommended fixes

### 0. Do this FIRST and alone — the process-file repairs
`M-1` de-duplicate the three EPA blocks and reconcile the two contradictory 4.1b copies · `M-24`
fix both stranded ordinals and RUN L-15's sweep · `M-31`, `M-27`, `M-32`, `M-33` repair the two
miscalibrated key-counting checks (anchor on `^\*\*key:\*\*`, not the bare token) · `M-23` reconcile
the four failing FILES TO READ lists · `M-16` bring the receipt into compliance with its own rule · `M-57` sweep §2's canonical template.

> These are edits to numbering, statuses and self-checks of the process files themselves — the
> exact category Run 5's evidence says must NOT ride in a large batch.

### 1. The two documented-but-not-installed rules
`M-36` install Source 3 and the `settings.local.json` exemption into `audit.md`'s D16 block ·
`M-35` give Agent 1 the accepted-risk observation instruction.

### 2. The highest-leverage structural gap
**`M-39` give item 5 (back-sweep) a mechanical form** — for every rule promoted this session, an
explicit "applied to N of N artifacts of shape X, listed" with a stated expected result, RUN before
the receipt is written. Every defect in this run traces to its absence.

### 3. L-1's remaining half
`M-2` declare the receivers · `M-3` re-point CODE to Step 4.6 · `M-4`, `M-5`, `M-9`.

### 4. The rest
`M-6` through `M-54` as listed, plus `M-43` and `M-44` (isolation residues needing either a fix or
an accepted-risk record) and `M-56` (`git push` needs a home).

---

## Meta-observation

**The receipts worked, and not as a gate.** Persistence converted them from unfalsifiable into
falsifiable, and it paid on the first run: five receipt claims are provably false — the fence
counts, the citation count, "the only module skill that had none", "each was also run against the
HEALTHY state", and four `✓` cells that recorded sweeps which did not happen. Under the previous
regime every one of these would have been unauditable forever. But nothing blocked on any receipt:
`f2fcccf` landed first and the receipts were written after it, in a separate commit. **The
receipts are the audit's evidence, not the applier's gate** — which is exactly what Run 4's
meta-observation predicted, and it is worth writing down rather than re-deriving next run.

**Every defect in this run has one cause, and it is not batch size.** A rule promoted in session N
is applied to session N's own batch only by the author's memory, at the end of the session.
L-15's ordinal sweep existed in the commit that stranded two ordinals. L-17's quote rule existed in
the commit whose receipt violates it 116 times. L-16's and K-34's checks were declared GREEN
against a state where both are RED. The control for this is item 5, the back-sweep — **the only
checklist item with no mechanical form at all.**

**On batch size, the evidence refutes the naive claim and supports a narrow one.** Every
mechanically-checkable control scaled fine at 51 findings: version parity, row count, liveness,
fence extraction, isolation — all re-verified TRUE. 24 of 51 fixes are clean. What did not scale is
**cross-fix interaction**: the L-9 renumber colliding with citations L-5 wrote in the same hunk; the
K-9 and L-1 insertions each adding the same report block; L-19's fix landing on the descriptive
surface while its checker kept the old text. The narrow rule the evidence supports: **large batches
are safe for point fixes and unsafe for edits to the numbering, statuses or self-checks of the
process files themselves.** Those belong in a separate small commit whose own new rules are re-run
against it before the receipt is written.

**Predicted sources of the next batch's defects, stated so they are checkable next run:**
(1) any new `grep -c "<key>:"` self-check firing on a document that quotes the key — the shape now
exists in two places; (2) ordinal citations stranded by the next list insertion, since the sweep
that exists has never been run on the batch that created it; (3) a fix landing on
`CLAUDE.md`/`README.md` while the executed check in `audit.md` keeps the superseded text — L-19's
exact shape, currently live in two rules.

**Suggestion:** Run `/maintenance` with Group 0 ALONE first, commit it, and only then take the rest.

---

**Report status:** `COMPLETE` — all 6 agents returned; 17 dimensions plus a Part 1 verification pass
over all 51 applied findings and a first RECEIPT verification pass.
**Application status:** `APPLIED — 56 of 57 in sd4d3c33 + sa2f4890 + 04cb256` — **54 `applied`, 2 `accepted-risk` (E-1, M-44), 1 carried forward**. Phase 3 item 2 forbids marking an accepted-risk finding `applied`, and an earlier form of this line counted M-44 among the applied (`/audit` 2026-09-03 N-4). With **M-44 closed as
`accepted-risk`** rather than applied. **0 open.** (57, not 56: Run 5's report reused `M-16` for
two distinct findings; the collision was corrected and K-13's residue re-filed as `M-57`.)
The three owner decisions — M-43, M-44, M-56 — were taken 2026-09-03 and are recorded at their
rows and, for M-44, in its own five-field record above.

*(superseded) `PARTIAL — 14 of 56 applied in sd4d3c33`.* Group 0 (the process-file
repairs) applied ALONE and committed by itself, per this run's own convergence finding; M-3, M-4,
M-5, M-28, M-29 and M-35 rode along because they live inside the same blocks Group 0 rewrote.
**42 remain `open`** — Groups 1-4, deferred by owner decision, NOT dropped. E-1 remains an
accepted-risk observation.


---

> **Errata 4 (added by the Run 6 Group 4 application, finding N-5).** The per-fix receipt below
> carries **8 rows for 14 findings applied** in this commit, and closes with "Row count: 8.
> Findings applied: 8. Equal — the self-check passes." The self-check compared the row count against
> itself rather than against the disposed count. **M-3, M-4, M-5, M-28, M-29 and M-35 have no row**,
> so their four sweep directions are unrecorded and cannot be re-read. Item 4's own stated remedy —
> "If two findings genuinely share one fix, give them one row each and write 'same edit as [ID]'" —
> was available and unused. This is the defect Gate 3 was later written to catch.

## Post-change checklist receipts (`sd4d3c33`)

*Group 0 only — 8 findings. Persisted per the checklist header; every `ALWAYS REPORT` key appears
below exactly once, anchored at line start so the per-fix table's quotations do not count.*

**inventory sweep:** 7 surfaces checked (`CLAUDE.md`, `README.md`, `docs/modules/README.md`,
`docs/agentic_engineering_framework.md`, `examples/README.md`, this command file, the sibling
command twin), **0 stale claims fixed** — no artifact was added, removed, renamed or split; the
EPA duplicate was REMOVED, which restores a count rather than changing one. **ORDINAL SWEEP: RUN**
(not merely written — Run 5's M-24 found it had never been executed on its own batch):
`grep -rn "Phase 3 item [0-9]\|item [0-9]'s\|see step [0-9]" .claude/ docs/ CLAUDE.md README.md`
→ 7 hits, each verified to still name the content it meant. **GREEN.**

**instruction style:** 9 new behavioral instructions checked, 0 rewritten. Unit = a line carrying a
CAPS imperative that a session must execute. All 9 carry the verb on a dedicated line; 4 carry an
explicit output format. (Run 5's M-54 flagged that "N instructions" had no stated unit; the unit is
stated here.)

**references:** 5 cited sections resolved — `Step 4.6`, `Step 2.2`, `Step 2.3` (EPA),
`## Post-change checklist receipts` (this file), `Execution proof` (`session_rules.md:110`) |
**fences:** 6 templates extract non-empty — **273, 149, 53, 23, 216 markdown lines and 185 js**,
measured WITH `sed '1d;$d'` as the extraction pipeline applies it (Run 5's M-12 found the previous
receipt reported these +2, having counted the fence delimiters) |
**isolation:** 4 files scanned, 0 hits.

**class sweep:** 8 fixes × 4 directions, 3 extra instances found and fixed.

| Fix | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|-----|---------|----------|----------|------------|----------------------|
| M-1 | ✓ `grep -c "Step 4.1b — Route"` + `"ALWAYS populate it FIRST"` + `"Framework + project freshness"` → 2/2/2 before, 1/1/1 after | ✓ `grep -c "Step 1.1"` in `bootstrap.md` → 1, no duplication on the twin | ✓ read `EPA:660-720` and `:960-1000` around both removals; `Step 4.2` and `### Documents upgraded` still open correctly | ✓ the surviving 4.1b's report line is the only one; EPA's final report carries one slot, not two | M-3, M-4, M-5 fixed inside the surviving copy while it was single |
| M-24 | ✓ `grep -rn "Phase 3 item [0-9]\|item [0-9]'s\|see step [0-9]"` over `.claude/ docs/ CLAUDE.md README.md` → 7 hits, all correct | ✓ same grep covers both command twins | ✓ read `audit.md:14-22` (the authorization block the stale ordinal sat in) | ✓ both citations now name the ITEM, so a future insertion cannot strand them | none — the sweep is GREEN |
| M-31 | ✓ `grep -rn 'grep -c "' .claude/commands/` → 2 unanchored key-counting checks, both repaired | ✓ item 9's twin check repaired in the same pass (M-27) | ✓ read the whole checklist header block | ✓ the write mandate's heading level reconciled (M-32) | M-27 is the same defect in the sibling check |
| M-32 | ✓ `grep -c "### Post-change checklist receipts"` → 0 repo-wide; `"## Post-…"` → the live form | n/a — single control | ✓ both write mandates (`:92`, Audit-intake) now say `##` | ✓ the check greps what the mandate writes | 2 mandates disagreed with the check |
| M-33 | ✓ compared the key list against item 3's three mandated keys | n/a | ✓ item 3 forbids collapsing the three, which the list had done | n/a | `fences:` and `isolation:` added |
| M-27 | ✓ `grep -n 'grep -c "new component:"'` → 1 site, no path operand | ✓ same anchoring applied as M-31 | ✓ read item 9 end to end | n/a | none |
| M-23 | ✓ mechanical per-block test (files a block's CHECKS name vs its own list) — **found 2 blocks the reading had passed** | ✓ all 6 blocks tested, not the 4 the finding named | ✓ the guard sentence normalised byte-identical across all 6 (M-28) and made heading-agnostic (M-29) | ✓ Agent 1 now reads the prior report, which is what lets it emit the accepted-risk observation (M-35) | Agent 2 (`CLAUDE.md`) and Agent 6 (`assets/docs/audit-*.md`) |
| M-16 | ✓ this table quotes a pattern in every `✓` cell — `grep -c '✓ `'` over the rows | n/a | ✓ the row-count rule re-read before writing | n/a | none |

Row count: **8**. Findings applied: **8**. Equal — the L-17 row-count self-check passes.

**back-sweep:** 1 process rule strengthened this session — *"a key-counting self-check MUST anchor
on the line start and be scoped to this run's section"*. Applied backward in the same session to
**2 of 2** artifacts it retroactively governs: the checklist header's key-presence check and item
9's `new component:` check. Mechanical: `grep -rn 'grep -c "' .claude/commands/maintenance.md` →
0 unanchored key-counting checks remain.

**liveness:** skipped — no frontmatter touched (0 of the 4 edited files is a component).

**negation proof:** 2 repaired self-checks, both proved in BOTH directions.
The anchored key-presence check: RED on a section with a key removed, GREEN on this section
(12/12 keys exactly once) — where the unanchored form returns 3 for `new component:` because the
per-fix table quotes it. Item 9's anchored check: GREEN at exactly 1 here, where the unanchored
form returns 3. **The healthy-state half is the one that matters this time** — Run 5's M-31/M-27
were both born from a check that had only ever been proved RED.

**version:** v2.12.1 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.
**Applied AFTER the fix commit** — `d4d3c33` declared the bump and did not carry it, which item 7's
own self-check caught on execution. That is the `cc6e31d` failure, detected this time instead of
shipped.

**classification:** 6 add, 5 substitute, 3 delete, 0 relocate. The 3 DELETEs are the duplicated EPA
blocks — justified under §5 as provably redundant (two copies of one mandate, one pair mutually
contradictory). The 5 SUBSTITUTEs replaced text that was miscalibrated (M-31, M-27), self-vacuous
(M-32), incomplete (M-33) or factually wrong about which step receives (M-3). No RELOCATE, so §5's
grep is N/A.

**new component:** none this session.

**verification audit:** proposed — trigger (d). The bump is PATCH, so trigger (b) does not fire.


---

> **Errata 5 (added by the Run 6 Group 4 application, findings N-3, N-7, N-20, N-25, N-35, N-36,
> N-37).** Seven figures in the receipts below are wrong, and they share one cause: **each was
> supplied by the author from reading, not returned by the command the receipt cites.** Corrected
> here rather than rewritten, so the pattern stays legible.
> - `references: 4` — the diff carried **≥35** distinct citation targets. The figure went DOWN
>   (8 → 4) as the batch grew (N-3).
> - `isolation: 20 files scanned, 1 hit` — the batch touched **23** files across its commits, and
>   item 3 mandates *every* touched file. A fresh D16 over the true set finds a **second** hit,
>   later filed as N-15 (N-20).
> - `instruction style: 34 … 0 rewritten … all on their own line` — the diff added **50** imperative
>   lines and removed **19** carrying imperatives, and **≥6** of the added ones sit buried
>   mid-paragraph. Three false claims in one line (N-37).
> - M-43's row: `grep … → 0 after` was **false when written** — `framework-base-upgrade.md:110`
>   predated the commit and still carried the identifier. The receipt also re-introduced that
>   identifier into a tracked file (N-7).
> - M-12's row: `6 templates` — bootstrap performs **8** fence extractions; `metrics_md` went
>   unmeasured for a third consecutive batch (N-25).
> - M-47's row: `5 → 11`. The cited grep returns **4 → 7** by lines and **5 → 10** by occurrences.
>   Neither reading produces the figure reported (N-35).
> - M-40's row: `→ 2 sites, both fixed`. **One** was fixed; `README.md:33-36` was byte-identical to
>   the parent commit and still mis-attributed the full tier set to `production-financial`. Fixed
>   in Run 6 Group 4 (N-36).
>
> **The pattern, which is the finding worth keeping:** every claim in these receipts whose value a
> stated command reproduces was TRUE; every claim whose value the author supplied from reading was
> FALSE or overstated. Batch size was never the variable.

## Post-change checklist receipts (`sa2f4890`)

*Groups 1-4 plus the three owner decisions — 43 findings. Every `ALWAYS REPORT` key appears below
exactly once, anchored at line start.*

**inventory sweep:** 7 surfaces checked and NAMED (per M-54): `CLAUDE.md`, `README.md`,
`docs/modules/README.md`, `docs/agentic_engineering_framework.md`, `examples/README.md`, this
command file, the sibling command twin. **2 stale claims fixed** — README's
`production-financial` tier attribution (M-40) at both sites. **ORDINAL SWEEP: RUN** —
`grep -rn "Phase 3 item [0-9]\|item [0-9]'s\|see step [0-9]"` over `.claude/ docs/ CLAUDE.md
README.md` → 7 hits, each still naming the content it meant. GREEN.

**instruction style:** 34 new behavioral instructions checked, 0 rewritten. **Unit** (per M-54): a
line carrying a CAPS imperative that a session must execute. All 34 sit on their own line; 11
carry an explicit output format. Three module skills went from 0 imperative markers to 2 each.

**references:** 4 cited sections resolved — `Step 4.6` (EPA), `## 1. Activation` (both
component-design twins), `Coverage Gap Declaration` (`security_reviewer.md`), `Generation Mode`
(`cross-cutting-analysis`).

**fences:** 6 templates extract non-empty — 273, 149, 53, 23, 216 markdown lines and 185 js,
measured with `sed '1d;$d'` as the extraction pipeline applies it.

**isolation:** 20 files scanned, **1 hit** — `assets/docs/audit-2026-09-02.md:624`, the
accepted-risk record naming the identifier it accepts, which is what such a record must do.
**0 hits in any executed or template surface.**

*(These three were first written on one line, which item 3 explicitly forbids — "NEVER collapse
the three into one verdict". The repaired key-presence check caught it before commit: it returned
RED on `fences:` and `isolation:` because neither started a line. Recorded because a control
catching a real violation on its first use is the evidence that it is a control.)*

**class sweep:** 43 fixes × 4 directions, 4 extra instances found and fixed.

| Fix | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|-----|---------|----------|----------|------------|----------------------|
| M-39 | ✓ `grep -n "ALWAYS REPORT" .claude/commands/maintenance.md` — items 1-9 compared for mechanical form | n/a — one checklist | ✓ read item 5 end to end | ✓ the table IS the descending artifact | item 5 was the only item with no mechanical form |
| M-36 | ✓ `grep -n "CODE identifier\|THREE sources" .claude/commands/` → 0 before | ✓ `CLAUDE.md` (declares) vs `audit.md` (executes) reconciled | ✓ D16.2b/D16.3 renumbered around the insert | ✓ Agent 1's FILES TO READ already lists the project code files | the exemption had the same doc-vs-check split |
| M-57 | ✓ `grep -ci "the reviewer cannot call\|echo the reviewer's gap\|when \[reviewer\] declares"` → **1 per twin AFTER the first pass** | ✓ both twins, byte-identical §1 verified by `diff` | ✓ §1 step 1, §2 template, §3 rows 2 and 3 | ✓ both chain greps in bootstrap and EPA gained `validator.md` | **1 survivor per twin the reading had passed** |
| M-2 | ✓ `grep -c "RECEIVER of Step 4.1b"` → 3 | ✓ bootstrap's Steps 3/4/13 are the model | ✓ each receiving step's own body | ✓ each declaration carries its own report line | none |
| M-9 | ✓ `grep -c "deferred to Step 4.1b"` → 0 after | ✓ bootstrap's 1.5→13 ordering is the model | ✓ Step 2.7.1's precondition paragraph | ✓ the report string updated to name 4.6 | none |
| M-6 | ✓ `grep -c "no-PRD" .claude/commands/bootstrap.md` | ✓ EPA has no PRD-less path | ✓ Step 1's record-only sentence and Step 2's new owner block | ✓ Step 2 gained its own report line | the with-PRD bracket-strip had no owner |
| M-7 | ✓ `grep -c "one of FOUR outcomes\|the four verdict strings"` → 0 after | ✓ both twins in one pass | ✓ the seven-row table above each sentence | ✓ M-8 is the descending half | M-8 |
| M-8 | ✓ `grep -c "up to date"` in both final reports | ✓ both twins | ✓ the freshness heading above each slot | ✓ this IS the descending fix | none |
| M-10 | ✓ `grep -c "continue with the upgrade checks above"` → 0 after | ✓ all four clauses in one pass | ✓ each step's check list below the clause | n/a | the wrong-file name and the false justification |
| M-11 | ✓ `grep -icE "risk\|MACRO\|CI\|examples\|scripts"` over EPA's report range | ✓ bootstrap's Step 15 is the model | ✓ `### Documents upgraded` below the insert | n/a | none |
| M-13 | ✓ `grep -n "reviewer" audit.md` inside D6 | ✓ D6.5, D6.6 and the report format together | ✓ D6.7/D6.8 above them | ✓ the report's sub-bullets un-hardcoded | none |
| M-14 | ✓ `grep -rn "^- Mutation:" docs/modules/` → the third surface | ✓ all three report templates | ✓ the twin rule that declared only two | n/a | the twin rule named 2 of 3 surfaces |
| M-15 | ✓ `grep -n '`:39`' security_reviewer.md` | ✓ code_reviewer's equivalent phrasing | ✓ the sentence it sits in | n/a | none |
| M-17 | ✓ `grep -n "^> " code_reviewer.md` — every gap block is emitted text | ✓ the other four gap blocks carry no meta-commentary | ✓ the block's closing line | n/a | none |
| M-18 | ✓ `grep -c "ALWAYS REPORT" validation-orchestrator/SKILL.md` — destinations checked | ✓ `criteria-enforcer`'s line names its destination | ✓ the pass-2 block above | ✓ the validation report's gap section is the destination | none |
| M-19 | ✓ `grep -n "visual regression" code_reviewer.md validator.md` — both declare it | ✓ the NOTE in code_reviewer says the same thing | ✓ the pass-2 rationale | n/a | none |
| M-20 | ✓ `grep -n "BASELINE-CREATED"` across the three surfaces | ✓ validator + framework doc | ✓ the UI verdict list | n/a | none |
| M-21 | ✓ `grep -n "validator LAST"` → 1 site | n/a | ✓ the ordering block | n/a | none |
| M-22 | ✓ `grep -n "spawned by protocol" audit.md` | ✓ D7.5's list vs the actual protocol-spawned set | ✓ D7.4 above it | n/a | none |
| M-25 | ✓ `grep -n '"When it runs"' audit.md` → 2 sites | ✓ both cite the same target | ✓ Phase 0's table and D10.6 | n/a | the second site was worded differently |
| M-26 | ✓ `grep -n "carried:" audit.md` | ✓ Phase 2 header and Recommended Fixes together | ✓ the group list above | ✓ two new lanes added | none |
| M-30 | ✓ read D8.7's SANCTIONED and RED lists side by side | n/a | ✓ D8.8 inherits the rule | n/a | none |
| M-37 | ✓ `grep -n "accepted-risk" audit.md maintenance.md` — consumer had no author | ✓ Audit intake is where the owner's decision arrives | ✓ step 5's deferral clause above | ✓ the five fields are the descending artifact | none |
| M-40 | ✓ `grep -n "production-financial" README.md` → 2 sites | ✓ both fixed in one pass | ✓ the footnote explaining the range | n/a | the second site |
| M-41 | ✓ line-length scan over `CLAUDE.md` | n/a | ✓ the whole paragraph rewrapped | n/a | none |
| M-42 | ✓ `grep -n "6 implementation" docs/modules/skills/README.md` | ✓ compared against `bootstrap.md:346-352`'s categorisation | ✓ the breakdown sums to 12 still | n/a | none |
| M-43 | ✓ `grep -rc "a hand-rolled date helper" --include=*.md .` → 0 after, 2 before | ✓ both sites in one pass | ✓ each commit-message example reads correctly | n/a | none — owner decision |
| M-44 | ✓ `git grep` over history for the residue | ✓ same five-field form as E-1's record | ✓ placed beside the E-1 record | n/a | none — owner decision |
| M-45 | ✓ `grep -c "added by Run 5"` → 0 after | ✓ both errata | ✓ Errata 1 above them | n/a | none |
| M-46 | ✓ `grep -rn "four documented\|four times"` over the three executed surfaces | ✓ all three updated to five | ✓ the counting rule beside each | n/a | the series needed Run 5's figure too |
| M-47 | ✓ `grep -coE "\b(ALWAYS\|NEVER\|MUST)\b" commit/SKILL.md` → 5 before, 11 after | ✓ compared against `session-end`'s shape | ✓ steps 1, 4, 6 already had them | n/a | none |
| M-48 | ✓ `for d in docs/modules/skills/*/; do grep -coE "\b(ALWAYS\|NEVER\|MUST)\b"` → 3 skills at 0 | ✓ all three fixed in one pass | ✓ each skill's opening section | ✓ each gained a report line too | none |
| M-49 | ✓ `grep -rn "silently skipped" docs/modules/` → 1 site | ✓ step 1 in the same file models the shape | ✓ step 0's check list | n/a | none |
| M-50 | ✓ `grep -c "ALWAYS REPORT" skill-gate/SKILL.md` | ✓ the observation-mode block has one | ✓ the post-promotion section | n/a | none |
| M-51 | ✓ re-counted the 2026-08-31 ledger row by row | ✓ both prior errata | ✓ placed above Errata 1 | n/a | none |
| M-52 | ✓ `grep -n "item [0-9]'s" maintenance.md` | ✓ the Workflow line was already fixed by L-27 | ✓ the Authorized-operations block | n/a | none |
| M-53 | ✓ `grep -n "exists/missing" EPA` | ✓ bootstrap's Step 15 has both verdicts | ✓ the Reading Report's other slots | n/a | none |
| M-54 | ✓ read items 1 and 2's report mandates | ✓ compared against items 3-9, which state their units | ✓ each item's own body | n/a | none |
| M-55 | ✓ diffed Phase 3's list against the PRD template's headings | ✓ bootstrap reads all four | ✓ the "what is PLANNED" block below | n/a | none |
| M-56 | ✓ `grep -rn "git push" --include=*.md .` — every string governed project repos or forbade it | ✓ CLAUDE.md and maintenance.md together | ✓ the Repository Lifecycle bullets | n/a | none — owner decision |
| M-12 | ✓ re-ran all 8 fence extractions with and without `sed '1d;$d'` | n/a | ✓ the receipt line it corrects | n/a | none |
| M-34 | ✓ `grep -n "RESOLVED FORWARD"` → 1 site | ✓ compared against L-8's enumeration | ✓ the E-1 row | n/a | none |
| M-38 | ✓ `grep -c "| open |"` before and after | ✓ L-19's row and the header claim | ✓ the application-status line | n/a | none |

Row count: **43**. Findings applied this session: **43**. Equal — L-17's row-count check passes.

**back-sweep:** 3 rules promoted, applied backward in the same session with the mechanical form
M-39 introduced.

| Rule promoted | Shape it governs | Grep that enumerates the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| "a declaring component, not 'the reviewer'" (M-57) | reviewer-only formulations in both component-design twins and the chain greps | `grep -ci "the reviewer cannot call\|echo the reviewer's gap\|when \[reviewer\] declares"` + `grep -c "validator.md"` on both commands | **2** (1 per twin, after the first pass had been read as complete) | 2 | **0 remaining** |
| "the third blocklist source is executed, not just declared" (M-36) | doc-vs-check splits in D16 | `grep -n "CODE identifier\|THREE sources\|settings.local.json" .claude/commands/audit.md` | 2 | 2 | 0 remaining |
| "push is owner-authorised and D16-gated" (M-56) | every surface that governs pushing | `grep -rn "git push" --include=*.md .` | 2 (CLAUDE.md, maintenance.md) | 2 | 0 remaining |

**liveness:** 25 components OK — full YAML parse over every raw and fenced frontmatter in
`docs/modules/agents/` and `docs/modules/skills/`. The bundled guard reached 1 component in
`modo estrutural — js-yaml ausente`, so the full parse ran alongside it. Raw agent templates: OK.

**negation proof:** N/A — no new self-check written this session. M-39's back-sweep table is a
mandated FORM, not a grep, and the three greps inside its first use are pre-existing patterns; each
was run against both the pre-fix state (non-zero) and the post-fix state (zero), which is recorded
in the table above.

**version:** v2.13.0 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.

**classification:** 29 add, 14 substitute, 0 delete, 0 relocate. Every SUBSTITUTE replaced text that
was factually false (M-40, M-46, M-19, M-42), scoped to two sources where three exist (M-13, M-57),
inverted (M-10), or in the wrong home (M-6, M-17). No RELOCATE, so §5's grep is N/A.

**new component:** none this session.

**verification audit:** proposed — trigger (d), and trigger (b) since the bump is MINOR. One
verification run satisfies both.
