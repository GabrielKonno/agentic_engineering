# Framework Audit Report

**Date:** 2026-09-22
**Framework version:** v2.32.0
**Run mode:** `baseline` — triggers (a) upstream absorption and (b) MINOR bump, both of which map to baseline (Phase 0). No batch of `applied` findings exists to verify.
**Post-push D16:** log cc3e14b..ece8b08 → 2 commits scanned, 0 hits — origin/main at ece8b08
**Dimensions checked:** 17
**Agents dispatched:** 6

**carried: 55 open + 0 escalated from `audit-2026-09-21.md`** — AD-1, AD-9, AD-10, AD-14, AD-15, AD-16, AA-3, AA-9, AA-10, AA-12, AA-13, AA-16, AB-2, AB-3, AC-3, AC-6, AC-7, AC-8, AC-10, AC-12, AC-13, AC-20, AC-21, AC-22, A-14, A-15, A-16, A-17, A-20, A-21, A-22, A-23, A-25, A-26, A-34, A-35, A-37, A-39, C-3, C-4, C-5, C-8, C-10, C-12, C-14, C-15, C-21, C-22, D-4, D-5, D-7, D-8, D-9, D-10, D-11. Every one is carried as a LEDGER ROW below (Phase 3 item 3), read from the previous report's rows, never from its `carried:` sentence.

**Accepted-risk OBSERVATIONS (reported once, never re-opened):**
- **E-1** (`audit-2026-09-02.md:603`) — source-project identifiers in PUSHED history. Agent 1's history scan surfaced 9 residual hits of one source-project filename (filed as L-20, fix-forward applied, 0 hits in tracked files today). Covered by E-1's standing instruction; NOT re-opened.
- **M-44** (`audit-2026-09-02.md:1020`), **N-53** (`audit-2026-09-03.md:613`), **T-11** (`audit-2026-09-09.md:279`), **T-36** (`audit-2026-09-09.md:302`), **U-48** (`audit-2026-09-10.md:504`), **B-17** (`audit-2026-09-15.md:2043`) — no new occurrence this run.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 module skills, 1 runtime skill (`cross-cutting-analysis`), also present in `docs/modules/` |
| C | Instruction style | Quality | 5 | PASS | 0 live anti-pattern instances in the sample; the 20 heuristic hits are all second sentences of already-dedicated mandates |
| E | Version consistency | Currency | 5 | PASS | canonical v2.32.0 on all 6 stamped surfaces; every other hit is lineage (E.5 exempt) |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 listed = 15 on disk; the 7 `user` / 8 `inline` split verified per frontmatter |
| D5 | Bootstrap file refs | References | 2 | PASS | 30/30 framework-relative paths resolve; both guard extractions syntax-checked |
| D6 | Gap vocabulary | References | 3 | PASS | 10 gaps, 3 declarers, install rows present in BOTH twins; batch touched 0 gap lines |
| D7 | Pushy Description | Quality | 3 | PASS | 20/20 example specialists COMPLIANT; 14/14 regression cases correct on both twins |
| D8 | Orchestrator refs | Process | 4 | PASS | every skill/agent name resolves; all §heading citations exact or sanctioned |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | every entry resolves incl. the new template; all count claims correct |
| D10 | Command → skill paths | References | 4 | FAIL | 3 self-check findings (AE-10, AE-11, AE-12); references themselves all resolve |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 15 primary + 10 sub + Setup; the qualified "15-step pipeline" claim is accurate; 1 flagged forward ref |
| D12 | Examples conventions | Currency | 5 | PASS | the new rules-convention claim verified 11/11 in BOTH directions; 1 LOW structural gap (AE-4) |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | bootstrap reads no section the PRD lacks |
| D14 | Bootstrap output | Process | 2 | PASS | 15/15 template references traced to a step; both guards in the Step 15 report |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 inaccuracies across 21 checked claims |
| D16 | Project-info isolation | Privacy | 1 | PASS | 3-source blocklist; 0 hits on tracked files, `.claude/docs/`, agent memory; history residue covered by E-1 |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | 4 findings (AE-5..AE-9) — the commit-body receipts home is authorised but unreadable by every mechanism built on the report home |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AE-1 | LOW | `README.md:198` | **Introduced by this batch.** The guard's row says it fails "on the ignored `applies_to:` key **and on dead globs**". The shipped contract is the opposite — `docs/modules/templates/check_rules_paths.md:24-26` and the script header state "Dead globs are REPORTED, never failed", and the ASSERTS list excludes them. README is the surface an owner reads to decide whether to wire the CI stage. | applied `s7be4aea` |
| AE-2 | LOW | `.claude/commands/bootstrap.md:454-456` | **Introduced by this batch.** The new guard-extraction fence carries neither its own `mkdir -p projects/$ARGUMENTS/scripts` nor a Setup-guard backstop; it depends on the preceding fence having run. The EPA twin gives its equivalent fence both (`existing_project_adaptation.md:815`). Same class as V-12. | applied `s7be4aea` |
| AE-3 | LOW | `.claude/commands/bootstrap.md` Step 5.7 heading | The heading ("Copy pre-built process skills, process agents, and session rules") now omits a fourth artifact class the step copies at ALL tiers: two guard scripts. | applied `s7be4aea` |
| AE-4 | LOW | `examples/README.md:173-179` | The per-kind **Rules** section states no frontmatter contract at all. The `domain:` / `paths:` / `# Scope:` requirement lives only in the preamble at `:88-92`, which itself instructs the reader to "Read the per-kind sections and measure before citing any of these as universal" — so a reader who obeys that routing learns nothing about `paths:`, this batch's central change. | applied `s7be4aea` |
| AE-5 | MEDIUM | `.claude/commands/maintenance.md:208`, `:1579`, Upstream intake items 5 and 8 | **Marking a PROJECT's evolution doc `upstreamed` from the mother repo has no written exception path.** The no-touch rule is stated absolutely in three places; this session did it under explicit owner authorization. A grep for `exception\|owner authoriz\|explicit authoriz` near the no-touch rule returns nothing. Item 8 still asserts "the no-touch rule stops this repo from marking it", now false in one authorised case. The precedent lives only in an unindexed lineage doc. | applied `s7be4aea` |
| AE-6 | MEDIUM | `.claude/commands/maintenance.md:59-70` | **A push whose receipts live in a COMMIT BODY has no executable recording home.** "WHEN A PUSH LANDS, ALWAYS UPDATE EVERY HASH-ENDING KEY IN THE SECTION" is unexecutable against an immutable commit message. Recording the push as a follow-up commit has happened 4× (3 into a report, 1 into a lineage doc); only the report home is documented. | applied `s7be4aea` |
| AE-7 | MEDIUM | `.claude/commands/maintenance.md:104-133` | **The recorded push endpoint is unreadable by its own detector.** The detector scans only the newest `assets/docs/audit-*.md`; this session recorded `Post-push D16 … origin/main at ece8b08` in a LINEAGE doc, so the detector returns `advanced cc3e14b..ece8b08` on a state already scanned GREEN. Fails SAFE (over-fires), but the record is inert, and the KNOWN LIMIT paragraph enumerates only the commit-body case, which produces a RED — a lineage home produces a silent `advanced`. | applied `s7be4aea` |
| AE-8 | LOW | `assets/docs/framework-evolution-upstream-2026-09-22-path-scoped-rules.md` (anchors table) | **Introduced by this batch.** The efficacy-anchor table credits `scripts/check-rules-paths.mjs` "at every tier" — true for projects, false for the mother copy of `component-design.md`, which gained `paths:` here and has no guard, no tier and no invoker in this repo. Per `maintenance.md` Upstream intake item 4 that anchor must read `unmeasured — no measurer in this repo`. | applied `s7be4aea` |
| AE-9 | MEDIUM | `.claude/commands/maintenance.md:345-347` (heading forms) + the `sec()` extractor | **The commit-body receipts home degrades every mechanism built on the report home.** All three authorised `## Post-change checklist receipts (…)` completions require a hash that does not exist when a commit body is written; measured against `ece8b08`'s body the fence-aware extractor returns **0 lines**, and only the UNAUTHORISED no-paren form (which this batch used) extracts. The keys are all present and score 27/27 when extracted that way — so the home works in practice and is invalid on paper. | applied `s7be4aea` |
| AE-10 | MEDIUM | `.claude/commands/maintenance.md:662-679` | **A mandatory control declares two halves and ships one command.** The LATERAL "redirect with no `mkdir`" fence carries `# HALF 1` and `# HALF 2` comments and a single loop; the stated expectation is "no output from EITHER half". `grep -n 'HALF\|half [12]'` over both command files returns only those comment lines — half 2 (a path mandated by a shipped artifact that no command mentions at all, the half that found the real defect in 2026-09-11) has no command anywhere, and the discharge reads GREEN either way. | applied `s7be4aea` |
| AE-11 | MEDIUM | `ece8b08` commit body, `verifier rounds:` and `verification audit:` | **Introduced by this batch.** Two `$` lines are angle-bracket descriptions (`$ <bash -n ratchet, item 10>`, `$ <Cycle governance rule 1 command>`). `maintenance.md:391` expects 0 and returns 2 over this batch; the rule requires naming any survivor "with the reason it cannot be written literally", and neither is named. The check itself is correct and fires. | applied `sWB` |
| AE-12 | LOW | `ece8b08` commit body; `.claude/commands/maintenance.md:331-334` | **Introduced by this batch.** Six keys discharge with `$ n/a`, while the rule authorises that only for `classification:` and `new component:`. `awk … /^ *\$ /` counts them as satisfied and the anti-decoration grep anchors only `<` and an ellipsis, so `$ n/a` is a third way to satisfy the `$`-line rule without measuring anything — the shape B-9 was filed to close. | applied `s7be4aea` |
| AE-13 | LOW | `README.md:382` | The `/audit` row garbles CLAUDE.md:154: "after a maintenance batch applied an audit batch" vs "after a `/maintenance` session applies an audit batch". The event SET is identical, so D10.6 parity stays GREEN; the wording is wrong. | applied `s7be4aea` |
| AE-14 | LOW | `assets/docs/audit-2026-09-21.md:122` (AC-6) | **A carried finding is falsified at the tip.** AC-6's premise is that the receipts self-check "structurally cannot pass" against a commit-message body; run over `ece8b08`'s body every one of the 27 keys scores `keys 1 \| $-line ≥1`. AC-6 should be NARROWED (it still holds for `adc7481`'s plain-form body) or closed. See also AE-9, which is the half that is genuinely broken. | applied `s7be4aea` |

### Carried forward from `audit-2026-09-21.md`

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AD-1 | HIGH | `assets/docs/audit-2026-09-20.md:14` and `:411` | **The carry-forward chain broke between two runs of the same file.** Run 3 declared `carried: 81 open` in PROSE and wrote no ledger ROW for any of them; Run 4 then read the ledger rows and declared `carried: 8`. Mechanically: 41 open/escalated findings across `audit-2026-09-16.md` and `audit-2026-09-19.md` have **zero** `^\| ID \|` rows in `audit-2026-09-20.md` (the A/C/D rows present there are Part 1 *verdict* rows, not carry-forward status rows). Phase 3 item 3's re-verification never ran over them. This run's own first carry-forward measurement read 24 and would have propagated the loss. **HALF-FIXED in `280c2f2`:** item 3 now requires ROWS and says to read the previous report's rows, not its `carried:` sentence; the 41 rows are restored. NOTHING COUNTS THEM — the reconciliation was drafted and cut (it read GREEN on this finding's own data, and the moratorium forbids a new control in that file). Stays `open`. | open |
| AD-9 | MEDIUM | `.claude/commands/maintenance.md` Gate 3 / Audit intake item 4 | Nothing asserts that an audit-applying session wrote a receipts SECTION into the report file, so the 27-key loop — self-scoped to "this run's section" — is skipped silently when the section is absent. Measured receipt-key coverage per commit body: `28b157a 0/27 · 847d18f 0/27 · 1b0bb67 1/27 · c9355b0 3/27 · 34a6c97 5/27 · 455c299 5/27 · 0bb7949 6/27 · 34a7c1c 9/27 · 961244b 27/27`. `34a7c1c` is the **v2.30.0 MINOR bump** — 9 of 27 keys, `verifier rounds:` written as prose (invisible to the loop's `^\*\*key:` anchor), zero `$` lines in the whole body. No control noticed. **HALF-FIXED in `280c2f2`:** the gap is now STATED in Gate 3, with both cut forms and why each failed. No check exists. Stays `open`. | open |
| AD-10 | LOW | `.claude/commands/bootstrap.md` (Steps 3, 4 vs `:449`) | `.claude/phases/` is never `mkdir`'d before Steps 3 and 4 write into it; the only creation sits inside Step 5.8's `internal-tool`+ branch, which never runs at `prototype`. Every other directory is explicitly created (`:378`, `:409`, `:421`, `:816`). Benign — the Write tool creates parents — but it is the one directory the command relies on implicitly. | open |
| AD-14 | LOW | `framework-audit/SKILL.md`, cited from `autonomous-loop/SKILL.md` and `session_rules.md` | `framework-audit` → Q4 is not a markdown heading in the target's body: it is the numbered list item `4. **Aspirational-vs-real audit**`; the only `### Q4 …` line sits INSIDE the fenced report template. Unambiguous and present, so not a broken reference — but it fails D8.7's "real markdown heading" test, and D8.7 says a bold/list lead-in is not a heading. | open |
| AD-15 | LOW | `.claude/commands/audit.md` D6.7 | D6.7's literal instruction — "`diff` the two install BLOCKS — the table AND the prose around it … expected: empty" — is **unsatisfiable by design**. The two commands deliberately diverge on mode-specific prose (bootstrap's Step 7/8/10 parenthetical; the `created:` lineage stamp), and the repo bounded the byte-equivalence claim to the `TWINNED SPAN` markers precisely to make it falsifiable (`/audit` 2026-09-10 U-24). Both bounded diffs are empty. **The check text is stale, not the repo** — a check that fires on the healthy state, the K-8/L-7 inversion. | open |
| AD-16 | LOW | `.claude/commands/maintenance.md:427` | "**ALWAYS report the harvest with its command and stdout**" names no key and is not in the 27-key registry, so the "every key exactly 1" loop cannot miss it. Measured: reported in 1 of the last 9 batches (`1b0bb67`); before that only in `audit-2026-09-15.md:566-568`, folded under `control back-sweep:`. component-design §10's own class inside the framework's apparatus. | open |
| AA-3 | LOW | `assets/docs/audit-2026-09-19.md:742`; harvesters at `.claude/commands/maintenance.md:97`, `.claude/commands/audit.md:111` | The `push:` receipt does not end with `— origin/main at sHASH` (a parenthetical follows), against `maintenance.md:50`. The harvester is `$`-anchored and takes `tail -1`, so it silently discarded that key and read the earlier `push decay:` line instead; its only RED fires when NOTHING matches. Harmless here (both lines carry the same hash), blind by construction. | open |
| AA-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227` vs `docs/modules/rules/session_rules.md:278-283` | A-18's schema/migration extra-receipt rule exists only inside the executed component; the section that owns the deploy moment ("Deploy gates"), which `:169` names as the gate's caller, never mentions it — component-design §9. | open |
| AA-10 | LOW | `.claude/commands/maintenance.md:939-942` vs `:949-950` | C-18's third element: step 4's mandated `probe-sandbox:` line has no slot in the `control back-sweep:` report form, which asks for "the command and stdout from step 1". It is reported only if the executor volunteers it. | open |
| AA-12 | LOW | `.claude/commands/audit.md:125`, `.claude/commands/maintenance.md:111` | Both state expectations (`origin/main at sNEW`, `unchanged at sHASH`) carrying an `s` prefix their fences never print; live output is `origin/main at af974ec`. The harvester tolerates it (`s?`), but a reader comparing stated-to-actual sees a mismatch on the healthy state — D10.0's own class. | open |
| AA-13 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:145` | `HOOKS=$(grep -hsE '"command" *:' .claude/settings.json)` is line-based: with a single-line or minified `settings.json` the whole file lands in `$HOOKS` and a `permissions.allow` entry counts as an invoker again, re-opening C-33. Confirmed in the sandbox with pretty-printed vs one-line JSON. | open |
| AA-16 | LOW | `.claude/commands/audit.md:548-572` (Agent 3 FILES TO READ) | D7.6 names `.claude/scripts/probe-sandbox.sh` and mandates running it, but the file appears in no agent contract's list — violating the list's own closing rule ("Every file a CHECK below names MUST appear in this list"). Repo-wide: 0 hits for `probe-sandbox` inside any FILES TO READ / INPUTS block. Same class as the open Z-18 (`d16-gate.sh`), so the Z-18 fix should carry this sweep. | open |
| AB-2 | MEDIUM | `.claude/commands/existing_project_adaptation.md` | The COUPLED REFRESH mandate has no slot in the final report; the guard slot offers refreshed/kept and cannot hold a coupled decision | open |
| AB-3 | MEDIUM | `docs/modules/rules/session_rules.md` | "NEVER DOWNGRADE a component's model…" is a banned process anti-pattern with no mechanical self-check (component-design section 6) — see AC-7, which refutes the stated deferral reason | open |
| AC-3 | MEDIUM | commit `adc7481` body vs `assets/docs/` | AB-1..AB-11 were filed only in a commit message. A grep for the ledger row shape over every report returns 0. Three readers are keyed to `assets/docs/audit-*.md` and none reads a commit body: the ID collision check, Phase 3's carry-forward, and trigger (d)'s `OBS` term. AB-2, AB-3 and AB-7 are `open` and in no ledger. Deposited into THIS report's ledger below as the remedy for the instance; the class needs a second form on `maintenance.md` item 10. | open |
| AC-6 | MEDIUM | `.claude/commands/maintenance.md` receipts form vs its self-check | The file authorizes the commit message body as a receipts home; the mandatory receipt self-check extracts a `## Post-change checklist receipts (…)` heading from a REPORT FILE and asserts a `$` line per key. Measured against `adc7481`'s body: 0 bold-form keys, 0 `$` lines, while all 27 keys are present in plain form. One authorized receipts home has a self-check that structurally cannot pass. The push-decay detector carries a written KNOWN LIMIT for this same form; this check carries none. | open |
| AC-7 | MEDIUM | `docs/modules/rules/session_rules.md` → the NEVER DOWNGRADE line | AB-3 was left open on the reason "a check would need value history, not presence". The reason does not survive: its two neighbouring NEVERs ARE mechanically guarded, and value history is what a git repo has — the framework already ships a base-vs-tip set-comparison ratchet (the bash-syntax ratchet). A `git diff` over `.claude/agents` scoped to removed/added `model:` pairs moving down the tier order is the direct analogue. | open |
| AC-8 | MEDIUM | `.claude/commands/maintenance.md` checklist item 6 | `model:` on the 30 shipped agent templates is verified by nobody in THIS repo. Item 6's guard command scans `.claude/skills/` and `.claude/agents/` (this repo has no `.claude/agents/`); its raw-template loop globs the module skills and agents and asserts only `name:`. `examples/` appears in no glob in that command file. All 30 carry it today; a future edit dropping it ships to every bootstrapped project and is caught only there, after the fact — the `criteria_enforcer.md` shape item 6 itself cites. | open |
| AC-10 | MEDIUM | `.claude/commands/audit.md` D6.7 | The mandated install-BLOCK diff states "expected: empty" over a span that legitimately diverges on both paths (bootstrap pre-selection vs EPA already-exists; the two lineage stamps). It can never return empty, so every run hand-adjudicates. The block carries no TWINNED SPAN markers, which is the mechanism the repo already uses for the Step 12.5b criteria; the span boundaries are described in prose, so two auditors extract two different spans. | open |
| AC-12 | MEDIUM | `.claude/commands/bootstrap.md` vs the EPA twin | EPA says "Then RUN it once now"; bootstrap names `check-agent-frontmatter.mjs` four times and never executes it, and Step 15 has no guard verdict slot. Bootstrap is the run that hand-writes `model:` into six agents from templates it adapts, then commits at Step 14.5 — and per component-design section 8 the break cannot manifest in the session that wrote it. At `prototype` the CI floor is skipped, so first detection is session 1.  **CUT from the 2026-09-21 backlog batch 4** — the "Then RUN it once now" mandate is sound and the step order checks out, but reporting the run needs named outcomes (pass / fail on exit 1 / skipped without Node) that no step defines yet. Destination first, then mandate (component-design §10). | open |
| AC-13 | MEDIUM | commit `adc7481` vs Cycle governance rule 2 | AA-13 lives at `docs/modules/skills/codebase-audit/SKILL.md:145`. `adc7481` edited that file. Rule 2: "ALWAYS apply a LOW finding when this session already edits its file for another reason, and say so in the commit." It was neither applied nor named; the commit records `class sweep: N/A`. | open |
| AC-20 | LOW | `.claude/commands/maintenance.md`, the push-decay KNOWN LIMIT | The documented failure mode ("the next session sees RED: no recorded origin/main hash") does not occur: the extraction returns a live hash three commit-body sessions stale, so the detector prints a plausible `advanced …`. It FAILS SAFE (over-scans an already-scanned range), but a reader relying on that sentence will not recognize the state they are in. | open |
| AC-21 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:246` | A nested bold marker inside a bolded mandate inverts the rendering: the outer bold closes at the inner marker, leaving a second distinct mandate UNbolded and a dangling trailing marker. Section 6 property 2 (dedicated line) plus a rendering defect. | open |
| AC-22 | LOW | `docs/modules/skills/sprint-proposer/SKILL.md:144-145` | Announces "(expected result stated)" and states neither a command nor a count — it cannot go red. The same file's Step 1 and `session-end` both give a real countable grep. | open |
| A-14 | LOW | `docs/modules/skills/session-log-creator/scripts/create-log.sh:26-27` vs `docs/modules/skills/session-log-creator/SKILL.md:46` | The generator writes the `## Review receipts` slot with no pre-deploy gate line in either the GREEN or the RED form. A log created by the script cannot carry the gate result the skill requires. B-23's and B-26's twin, not swept (847d18f did not touch the script). | open |
| A-15 | LOW | `.claude/scripts/d16-gate.sh:216-221` (`dir` scope); `.claude/commands/audit.md` D16.3b | `dir` matches paths as given. The memory directory can only be passed as an absolute path, so every ancestor directory name is scanned, and an identifier matching a word in the owner's home path hits every file although the contents are clean. Real run: 12 hits over 12 clean files. Probe: absolute path 2 hits, relative 0. D16.3b does not mention it. | open |
| A-16 | LOW | `autonomous-loop/SKILL.md:630-631` vs `:668-671`, `:313-315` | A mid-task marker removal by another session makes the closure check RED. The closure-RED rule then requires writing `State: stopped`, which needs the marker, and the revocation branch says NEVER re-create the marker. No precedence line. Introduced by X-13's fix. | open |
| A-17 | LOW | `autonomous-loop/SKILL.md:675-677`, `:706`, `:736-738` | The C5 digest offers "triage held/deferred" as an owner answer, but neither the resume line (`:675-677`) nor C6's reset (`:736-738`) says whether it resumes or resets. X-19's class, not swept; the receipt's claim about `:675-677` is false. | open |
| A-20 | LOW | `docs/modules/agents/diff_pattern_extractor.md:87-92`; `autonomous-loop/SKILL.md:505-509`; `codebase-audit/SKILL.md:110-112`, `:147-148`; `framework-audit/SKILL.md:81-82` | Five behavioural requirements added by the two batches are buried in note sentences, KNOWN LIMIT paragraphs or sentence tails ("— and ALWAYS file ONE task", "MUST carry its own", "say so, and record the step as ⏭️", "and say so in the report", "say so when one does"). Third consecutive batch pair needing an instruction-style follow-up. | open |
| A-21 | LOW | `autonomous-loop/SKILL.md:353-354`, `:494` vs `:366-373` | The every-subagent resource declaration still omits the MUTATOR/READER class and the scratchpad. `READERS` at C1 `:494` means "never adds a task", a different sense from READER at `:366-373`, so `criteria-enforcer` (writes `pendencias.md`) is classed both ways. B-12's class plus a new defect. | open |
| A-22 | LOW | `autonomous-loop/SKILL.md:421-424`, `:148` vs `:641` | "Emit the lessons block before `/session-end`" exists only under C5 (continuous). Segment-mode continuation and exception stops run `/session-end` with no such step, so their orchestration lessons never reach the log. B-19 D9, not swept. | open |
| A-23 | LOW | `autonomous-loop/SKILL.md:608-627` | The C4 closure check's first-parent traversal reads OK for a `--no-ff` merge whose side branch carries code committed after the counter moved (throwaway case P4); the KNOWN LIMIT (`:626-627`) does not declare it. Introduced by X-9's fix. | open |
| A-25 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 (`:232-233`) | The moratorium self-check counts receipt keys (27) and numbered items (14) only. A new prose-embedded control reads 27/14 (planted), so 742a691's new "expected ≥1 hit in EACH" check and D15.2's new coverage check passed unseen. It does not cover `audit.md`, and batch 2's receipts neither run nor report it. | open |
| A-26 | LOW | `autonomous-loop/SKILL.md:5-19`, `:536`, `:664`; `sprint-proposer/SKILL.md:68-102` | "cancel continuous mode" is read only in the autonomous-loop body. A session without that skill loaded is never told to remove the marker. X-8 residue. | open |
| A-34 | LOW | `.claude/commands/existing_project_adaptation.md:598-600` vs `:856-864` | Step 2.9 takes the refresh/keep decision for `session-log-creator` before the profile exists; the Step 2.9b pairing rule ("at `production`+, ALWAYS PRESENT THEM TOGETHER" with framework-audit and autonomous-loop) then cannot apply to a decision already taken. Introduced by 5ecefa9 (B-11 fix). | open |
| A-35 | LOW | `.claude/commands/existing_project_adaptation.md:1358`, `:1377-1382` | The EPA report has no slot for `.claude/docs/` (created at `:682`) or `.claude/skill-gate/review_reports/` (created at `:819`); bootstrap reports both (`:944`, `:984`). Twin asymmetry, W-18's class.  **CUT from the 2026-09-21 backlog batch 4** — slots were drafted with verdicts `created` / `already present`, but both directories are made by a bare SILENT `mkdir -p` and no step emits either value (and the report retires `already present` two lines below). Closing it needs the fences to `test -d` and echo a verdict FIRST, then the slot copies it. | open |
| A-37 | LOW | `.claude/commands/existing_project_adaptation.md:517`, `:595`, `:862`, `:894`, `:1121`; `.claude/commands/bootstrap.md:113`, `:342`, `:892` | V-17's class, not swept: a rationale clause fused with its ALWAYS in one sentence survives at 8 sites. Two of them (`EPA:595`, `:862`) are invisible to the style regex because `**` precedes the space, and the batch 2 receipt marked the twin "n/a — prose only". | open |
| A-39 | LOW | `.claude/commands/audit.md`, `.claude/commands/maintenance.md:1416-1424`; lineage docs' efficacy-anchor tables | B-11's mother-side half: the maintenance writer must name a measurer, but no `/audit` or `/maintenance` step ever reads an anchor's measured result back (`grep -ciE 'efficacy\|hypothes\|measurer' audit.md` → 3 unrelated hits). An anchor whose measurer silently breaks (A-3) is never noticed from here. | open |
| C-3 | MEDIUM | `assets/docs/audit-2026-09-15.md:1336`, `:1349`, `:1915`, `:1928`; enumerator `assets/docs/audit-2026-09-16.md:380` | A-2's class is not swept. The two persisted receipt generators execute the fail-open np1/np2 (`bash "$NP"`) under `$` lines that still say "run from the repo root", with no erratum. The batch's back-sweep enumerator reads `0` over them because it matches only literal git write verbs inside a fence. Agent 6's stand-in probe: np1 still commits (213 → 218), pushes and rewires origin when its scratch clone fails.  **CUT from the 2026-09-21 backlog batch 1** — three errata were written and all three withdrawn: one was spliced INSIDE the bash fence it annotated (breaking its parse), and the replacement set over-applied to a third generator that uses neither `bash -c "$cmd" 2>/dev/null` nor `$NP`. Needs errata for exactly the TWO generators this finding names. | open |
| C-4 | MEDIUM | `.claude/commands/audit.md:1437-1441` (Phase 3 item 7) vs `:36-38` | Item 7's fence `git add`s only the report, and the rewritten self-check is scoped to the report path. When item 6 has appended a defect-series row to `audit.md`, that row stays uncommitted while the check prints `report committed`. Sandbox: `report committed: sH` then `uncommitted after the fence: M .claude/commands/audit.md`. U-47's residue, adjacent to A-31. | open |
| C-5 | MEDIUM | `.claude/commands/audit.md:18-31`, `:223-236` | Whether a check WRITES — and therefore goes to the sandbox — is decided by whoever runs it, and nothing compares the live repo before dispatch with the live repo after the agents return. A writing check misrouted in place (the incident's own shape) is still caught by nothing: `grep -c fingerprint .claude/commands/audit.md` → 0. Concrete instance: C-13. | open |
| C-8 | LOW | `.claude/commands/audit.md:1439-1441` | A same-day re-run whose HEAD is the previous run's `docs(audit): persist` commit to the same file reads `report committed` before this run's report exists (sandbox: `report committed: sH` with no Run 2 content). Phase 3 item 1 (`:1368-1371`) calls same-day runs the normal shape. A-31/A-41 class, one case over. | open |
| C-10 | LOW | `.claude/commands/audit.md:852` (D10.0) vs `.claude/commands/maintenance.md:874-875`, `:1106` and `audit.md:47` | A-42 and A-46 were not swept into D10.0. D10.0 mandates `run --staged` for every writing check, including item 5's pre-fix reconstruction, which maintenance says must NEVER combine `--staged` with a checkout. D10.0's run-every-check mandate also reaches item 10's `--keep` recipe, which `audit.md:47` now forbids agents. | open |
| C-12 | LOW | `.claude/commands/audit.md:674-686` (D7.6 steps 1-2) | The steps require a synthetic project in a scratch directory, run in the sandbox, whose working directory is the clone. They never say that the extracted loop resolves `projects/$ARGUMENTS/.claude/agents` relative to the current directory (`bootstrap.md:697`), so the auditor must work out the `cd` and `ARGUMENTS` alone. | open |
| C-14 | LOW | `assets/docs/audit-2026-09-16.md:374-376` (`fences:` receipt) | The `$` line is the mandated command with a `| awk` summariser appended, so the pasted stdout (`zero: 0 of 12`) is not the mandated command's literal output — against `maintenance.md`'s "THE `$` LINE MUST BE THE MANDATED COMMAND, BYTE-FOR-BYTE". | open |
| C-15 | LOW | `.claude/commands/maintenance.md:1022`; `.claude/commands/audit.md:137` | Cross-file citations `/audit` → Authorized operations (a bold lead-in; added by 194f44a for A-48) and `maintenance.md` → the `push:` rule (a bullet) point at non-headings, which D8.7 marks RED.  **CUT from the 2026-09-21 backlog batch 2 after three verifier rounds** — the D8.7 amendment (sanctioning a self-declared non-heading citation) is the right idea: two such citations were already live and resolving, and unamended D8.7 flags both. But every implementation left a gap — a citation with a FALSE "where" fell outside BOTH the sanctioned and the RED list, letting a citer silence the check on an unverified assertion. Reverted whole. | open |
| C-21 | LOW | `.claude/commands/audit.md:226-232`, `:20-30` | The isolation rule sends "every probe" to the sandbox and forbids ever putting a read-only scan (`d16-gate.sh … dir`) there, with no tie-break for a probe that is itself a read-only scan (e.g. a planted `D16_EXTRA` over `dir`, which Agent 1 had to run in place to see `.claude/docs/`). | open |
| C-22 | LOW | `.claude/commands/audit.md:229` vs D16.2 (`:318-320`) | D16.2 harvests "repo URLs", but the verbatim rule bans `git remote`, the only direct source, and neither names a sanctioned read-only route (e.g. `git config --get-regexp '^remote\..*\.url'`). Agent 1 ran `git -C projects/<each>/ remote -v` read-only to fill the class. | open |
| D-4 | LOW | `.claude/commands/existing_project_adaptation.md:773-777` vs `:897-953` | A-8's residue: the PHASE-3 RE-CHECK (risk profile, and now the Red/Blue Team decision) is mandated only inside Phase-2 Step 2.9b, and Phase 3 never points back to it (0 grep hits for `2.9b|2.6|profile|re-check`). The moment has no invoker (component-design §9.1). | open |
| D-5 | LOW | `.claude/commands/existing_project_adaptation.md:1215-1365` (Step 5.1) vs `:877-878` | A-8's residue: nothing mechanically checks that `.claude/agents/red-team.md` exists when `code-reviewer.md` carries "red-team is MANDATORY on every diff that touches a money path". Step 5.1's fence has 0 red-team lines although it checks validator and arbitrator existence (`:1246`), so the defect A-8 named can still ship, caught only by prose (§9.2). | open |
| D-7 | LOW | `.claude/commands/existing_project_adaptation.md:617-623` (added by 87210b1) | The v2.27.0 migration note has three defects: its headline says "two pairs travel together" and names an ALWAYS for one; it claims an older `session-log-creator` "only lacks the pathspec-RED slot" when the pre-batch slot also lacked the `… K without receipt — deploy blocked` form; and its "ALWAYS offer `session-log-creator` with it" is buried mid-paragraph with no executing moment — `validation-orchestrator` is asked in Step 2.9b's coupled-set question (`:599-603`) while `session-log-creator` is asked in Step 2.9, so the pairing crosses that split (A-34's class). | open |
| D-8 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227-229`, `:231` | A-18's new ALWAYS line (a schema or migration commit also needs an adversarial or data-reviewer receipt) has no output format and no stated-skip form, and the ALWAYS REPORT line covers only the gate's four strings — so whether the deploying session performed the check is structurally invisible (component-design §6 property 3, §9.3). | open |
| D-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:218-219` | "A verdict the list does not know BLOCKS — extend `PASS`, never the reverse" is a behavioural requirement for every project that customises the gate, buried in lowercase at the end of a rationale paragraph (§6 C.4). | open |
| D-10 | LOW | `.claude/commands/maintenance.md:1112-1115` | C-19's fix introduced a defect: `rm -rf "${M%/r}"` now sits in the same executable fence that creates `$M`, so a verbatim run deletes the scratch mother repo before the verifier receives it, and "run it when the verifier returns" lives only in a `#` comment. Shell state does not persist between calls, so a deferred run has no `$M` either.  **CUT from the 2026-09-21 backlog batch 1** — the fix hoisted the deletion’s timing out of a `#` comment, and a verifier proved the mandate unexecutable: `$M` does not survive between shell invocations, so the deferred `rm -rf` is a silent no-op and the temp dir leaks. Closing it needs the recipe to persist the path, not a better sentence. | open |
| D-11 | LOW | `docs/modules/templates/metrics_md.md:21` vs `:14-17` | A-7's class, unswept to the twin template: the CODE-series baseline row reads `COMPLETE` with no `steps:` line although its own prose says Status is followed by the per-step line, and `framework_metrics_md.md:30` was updated. The same self-check gives 1 on the sibling row and 0 on this one. | open |

Re-verification method (Phase 3 item 3): every carried row keeps its ORIGINAL ID and status. The LOW findings whose files this batch did not touch were re-verified MECHANICALLY — `git diff --name-only ece8b08~1..e895711 -- <file>` empty means still true by construction. `AE-14` records the one carried finding this run found FALSIFIED at the tip (`AC-6`), which is filed as a new finding rather than silently closed.

## Detailed Findings

## Agent 1: Structural Sync

### PART 1 — `Part 1: N/A — baseline mode`

### [A] Dual Placement
- **Status: PASS** — `docs/modules/skills/` holds 15 folders; `.claude/skills/` holds exactly 1 (`cross-cutting-analysis/`), which also exists at `docs/modules/skills/cross-cutting-analysis/`. It is the documented runtime skill (PRD sessions), so A.4 holds. Findings: none.

### [D4] Skills README vs Disk
- **Status: PASS** — README lists 15 / disk has 15. Missing from README: none. Missing from disk: none.
- Count claims verified: `:8-13` "15 skills: 12 lifecycle … plus 3 tier-gated" (12+3=15; lifecycle breakdown 1+5+3+1+1+1 = 12); `:38` "7 declare `user` … 8 declare `inline`" verified against every SKILL.md frontmatter, and the 7 named match exactly; `:5-7` the 3 process agents all present; `:76` tier-gated removal list matches disk. Every `name:` frontmatter matches its directory (no §8 vanish risk introduced by the batch).

### [D9] CLAUDE.md Diagram vs Disk
- **Status: PASS** — In diagram but not on disk: none (including the batch's new `check_rules_paths.md`). On disk but not in diagram: none significant.
- Counts verified: 6 slash commands; 15 process skills / 3 tier-gated; templates 8 = 8; agents 10 = 10; rules 5 = 5; examples 20 / 9 / 11; `.claude/scripts/` 2 = 2.

### [D16] Project-Information Isolation
- **Status: PASS** (live surfaces 0 hits; one history-only OBSERVATION, pre-existing and already triaged)
- Blocklist derived — all three sources: (1) 3 project folder names, expanded by `d16-gate.sh` into variants, separator forms and ≥4-char PARTS at letter boundaries and CamelCase; (2) 13 identifiers from project DOCS (4 deployment domains, 1 sub-domain, 1 e-mail, 1 phone, 1 city, 1 person-name variant, 3 project-specific doc filenames, 1 config identifier); (3) **375 framework-layer code identifiers** cross-grepped against **130,959** identifiers from **6,734** project sources — intersection 119, ALL standard industry / framework-native vocabulary; zero hand-rolled source-project names. 30 verbatim framework→project copy paths derived from `bootstrap.md` (threshold "at least 5") and excluded. 12 value-shape EREs, written outside the repo, never printed.
- Surfaces scanned: tracked files (`staged`, 124 files, 0 hits) | `.claude/docs/` (`dir`, 4 files, 0 hits) | `docs/modules/**` + `examples/**` double scrutiny (88 files, base 0) | agent memory (18 files, 0 hits on all three classes) | git history (`log --all`, 255 commits, base 0 hits).
- Triage of non-base hits: a `user:pass@` DSN shape at `examples/agents/secrets-scanner.md:84` (a detection-regex example, explicitly exempt under D16.2b); the `Sessão 90` pair in the D16.2c instruction's OWN examples (already ruled legitimate); one over-broad blocklist PART that is an ordinary Portuguese word (false positive of the harvest, not a leak); 522 history hits that are the repo owner's own commit author e-mail (out of class — D16 governs PROJECT information).
- Commit-hash rule (D16.3b): 38 distinct hex tokens in memory after excluding UUIDs and URLs; **38 of 38 resolve** via `git cat-file -e <hash>^{commit}` in this repo. 0 unresolved.
- **OBSERVATION — history-only, pre-existing:** one source-project filename returns 9 residual hits in PUSHED history (filed as L-20, fix-forward applied, 0 hits in tracked files today). Covered by the E-1 accepted-risk decision; reported as an OBSERVATION, NOT re-opened. `audit-2026-09-21`'s history scan reported 2 hits because its blocklist did not carry this identifier — these 9 are newly VISIBLE, not newly introduced.
- Unpushed-vs-pushed: the batch under audit (`ece8b08`) and the lineage commit (`e895711`) contribute 0 hits on the base gate.

## Agent 2: Bootstrap Integrity

### PART 1 — `Part 1: N/A — baseline mode`

### [D5] File References
- **Status: PASS** (1 cross-surface accuracy finding, outside bootstrap.md → AE-1)
- Total distinct paths extracted: 88. Framework-relative sources: **30 / 30 resolved** (10 agents, 5 rules, 8 templates incl. the new `check_rules_paths.md` at `:455`, 3 tier-gated skills, `examples/`, both command twins). Project-relative targets: 58, correctly absent here (TIER OF REFERENCE). Unresolved: 0.
- Fence-extraction integrity verified mechanically: all 9 sed-extracted templates carry exactly 2 four-backtick fences with the correct language tag; the 4 read-and-adapt templates correctly carry none. Both guard extractions executed and syntax-checked:
```
rules-paths bytes: 6796   | SYNTAX OK: check-rules-paths.mjs
agent-frontmatter bytes: 9753 | SYNTAX OK: check-agent-frontmatter.mjs
probe-sandbox: GREEN — live repo unchanged; command exit 0
```
- D5.4 naming convention: 17 / 17 correct. TWINNED SPAN diff (bootstrap 12.5b ↔ EPA 5.1): identical, exit 0.
- FINDING (LOW → AE-1): `README.md:198` states the guard fails on dead globs; the template and the shipped script state the opposite.
- FINDING (LOW → AE-2): the new fence at `bootstrap.md:454-456` carries neither its own `mkdir -p` nor a Setup-guard backstop; the EPA twin gives its equivalent both.
- INFO (→ AE-3): Step 5.7's heading omits a fourth artifact class — two guard scripts.

### [D11] Step Sequencing
- **Status: PASS** — Primary steps: 15. Sub-steps: 10 (+ 1 unnumbered `## Setup`, + 2 sub-sub-steps). Total discrete: 26 (28 counting 12.5a/12.5b).
- Claimed vs actual: the concepts doc's "15-step pipeline (an unnumbered Setup phase …, 15 primary steps 1-15, Step 0.5 …, and sub-steps such as 5.7, 5.8, 14.5)" is **accurate** — explicitly qualified, and 15 primary steps exist. No unqualified "15 steps" claim anywhere.
- ASCII pipeline diagram vs command file: **26 / 26 rows match**, in order; the batch correctly added the `check_rules_paths.md → scripts/check-rules-paths.mjs` row.
- Forward references: 1 flagged and correctly paired (`bootstrap.md:466` → Step 14.2, which does wire both guards); EPA carries 4 sanctioned flags; 5 further consume-later references are handled with receiver mandates. 0 unflagged-and-unhandled.
- All 8 `ALWAYS REPORT` keys have a Step 15 slot. No orphan mandate, no orphan slot.

### [D13] PRD ↔ Bootstrap Compatibility
- **Status: PASS** — bootstrap reads a section the PRD lacks: **NONE**. Every parenthetical heading matches the template verbatim, and the Changelog is a real table.
- INFO (PRD defines, bootstrap never reads): 1.4 Competitive Differentiator; 6.1–6.3 and 7.1–7.3 individually; **8. High-Level Roadmap** (bootstrap derives phases from 5.4 Build Order).
- EPA twin: GREEN — Phase 3 enumerates every template section including the four M-55 additions and the Changelog.

### [D14] Output Completeness
- **Status: PASS** — Template references: 15. Traced: 15 / 15. Untraced: 0.
- Both self-referential placeholders carry explicit replacement mandates — the MIRROR defect correctly mitigated: a shipped CLAUDE.md citing a bootstrap step would be unresolvable in-project, and both are mandated away.
- The template's `v2.32.0` / `~90 lines` header is accurate: the fenced body is exactly 90 lines.
- D14.4: Step 15 lists every created file; both guards appear, including the new one with its CI-stage note.

## Agent 3: Activation Chain

### PART 1 — `Part 1: N/A — baseline mode`

### [D6] Vocabulary Alignment
- **Status: PASS.** The batch touched **zero** gap-bearing and **zero** `description:` lines. Mechanical proof: `git show ece8b08 -- docs/modules/agents examples/agents | grep -E "^[+-]" | grep -i "gap"` → empty; the same grep for `description:|USE PROACTIVELY|declares a|Produces .*Report|Without this` → empty. `--numstat` shows 1 line changed per agent template — the single `Rules files` context bullet.
- `declarers extracted: 3 of 3` — `code_reviewer.md` (accessibility `:214`, performance `:222`, concurrency `:230`, visual regression `:237`, data integrity `:249`), `security_reviewer.md` (static analysis `:200` and `:241`, secrets coverage `:250`, federation protocol `:258`, compliance `:268`, infrastructure security `:276`), `validator.md` (visual regression, bold form, `:150`).
- Specialist matches — all 10 gaps PASS, each with a bootstrap install row AND an EPA install row:

| Gap | Declared by | Specialist file | Phrase match | bootstrap row | EPA row |
|---|---|---|---|---|---|
| accessibility | code-reviewer | accessibility-checker.md | exact | :669 | :1290 |
| performance | code-reviewer | performance-auditor.md | exact | :670 | :1291 |
| concurrency | code-reviewer | concurrency-tester.md | exact | :671 | :1292 |
| visual regression | code-reviewer + validator | visual-regression-tester.md | exact (names BOTH declarers) | :672 | :1293 |
| data integrity | code-reviewer | data-integrity-checker.md | exact | :673 | :1294 |
| static analysis | security-reviewer | sast-scanner.md | exact | :674 | :1295 |
| secrets coverage | security-reviewer | secrets-scanner.md | exact | :675 | :1296 |
| federation protocol | security-reviewer | oauth-flow-tester.md | exact | :676 | :1297 |
| compliance | security-reviewer | compliance-auditor.md | exact | :677 | :1298 |
| infrastructure security | security-reviewer | iac-scanner.md | exact | :678 | :1299 |

- Install-block diff (D6.7), table + surrounding prose, 21 lines each side: two hunks, both legitimate twin divergence (step numbers and lineage wording). **The 10 table rows are byte-identical.** TWINNED SPAN diff: empty (exit 0).
- Fourth-surface check (D6.9): `docs/agentic_engineering_framework.md:998` names THREE declaring components and TWO passes, agreeing with component-design §1/§3 in BOTH twins (§1–§3 diff empty) and with `validation-orchestrator/SKILL.md:129`. No contradiction.
- Gap SOURCES found: 3 · Broken links: 0 · Orphaned specialists: 0.

### [D7] Pushy Description Compliance
- **Status: PASS.** All 20 `examples/agents/*.md` COMPLIANT (core function line 20–32 words before the trigger block, plus all four trigger elements). All 10 `docs/modules/agents/*.md` PARTIAL — protocol-spawned, **sanctioned, never a finding** — except `skill_reviewer.md`, which is COMPLIANT.
- `activation chains: 10 verified, 0 broken, 17 info` — **bootstrap**
- `activation chains: 10 verified, 0 broken, 17 info` — **EPA**
- `twins agree: yes` (counts AND info strings identical on the baseline and on all 15 planted states; loops extracted verbatim, 78 lines each, byte-identical)

| # | Case | Expected | Literal output (identical on both twins) |
|---|---|---|---|
| 1 | `declares a coverage gap for X` | BREAK | `BROKEN CHAIN: bogus-one declares 'coverage gap' but no declaring component declares it` → `10 verified, 1 broken, 17 info` |
| 2 | hyphenated `visual-regression gap` | VERIFY | `11 verified, 0 broken, 17 info` |
| 3 | slashed `data/integrity gap` | VERIFY | `11 verified, 0 broken, 17 info` |
| 4 | TAB-indented fold | VERIFY | `11 verified, 0 broken, 17 info` |
| 5 | no-article `declares performance gap` | VERIFY | `11 verified, 0 broken, 17 info` |
| 6 | second bogus gap, same agent | BREAK on 2nd only | `BROKEN CHAIN: two-gaps declares 'quantum gap' …` → `11 verified, 1 broken, 17 info` |
| 7 | sentence-initial `Declares` | VERIFY | `11 verified, 0 broken, 17 info` |
| 8 | space-fold break inside phrase | VERIFY | `11 verified, 0 broken, 17 info` |
| 9 | gap phrase in BODY only | no BREAK | `INFO: body-only has no gap phrase …` → `10 verified, 0 broken, 18 info` |
| 10 | NEGATED body sentence | no BREAK | `INFO: negated has no gap phrase …` → `10 verified, 0 broken, 18 info` |
| 11 | digit in domain | never silent | `BROKEN CHAIN: digit-dom declares 'oauth2 gap' …` → `10 verified, 1 broken, 17 info` |
| 12 | dot in domain | never silent | `BROKEN CHAIN: dot-dom declares 'web.security gap' …` → `10 verified, 1 broken, 17 info` |
| 13 | validator-only declarer (bold) | VERIFY | `activation chains: 1 verified, 0 broken, 16 info` |
| 14 | all declarers removed | `none installed`, exit 0 | `activation chains: none installed - no declaring component in projects/synth/.claude/agents  exit=0` |

- probe (D7.6): synthetic project under the sandbox's temp directory, outside the repo | `probe-sandbox: GREEN — live repo unchanged; command exit 0`
- Planted-specialist probe (D7.8): `BROKEN CHAIN: ghost-prober declares 'poltergeist gap' but no declaring component declares it` — emitted on BOTH twins. Not zero output.
- Invariant proof (D7.5): `files: 10 | output: 0`
- Anti-pattern instances: none.
- Collateral observations, all clean: (1) the rewritten context bullet is verbatim-identical across the 4 templates and the 7 generic examples, and the 3 with a domain-specific tail append it with balanced parens — the batch's self-reported V7 defect is repaired; (2) back-sweep complete, no stale read-all-rules instruction survives in agents or skills, and the two examples untouched by the rewrite correctly needed none; (3) `security_reviewer.md` states `static analysis` in two forms, which the loop's case-insensitive de-duplication collapses to one domain.

## Agent 4: Orchestration & Commands

### PART 1 — `Part 1: N/A — baseline mode`

### [D8] Orchestrator References
- **Status: PASS** — every skill/agent name resolves: session-end's 6 skills plus `diff-pattern-extractor`; validation-orchestrator's skills plus `criteria-enforcer` and `code-reviewer`; sprint-proposer's 3 skills plus `prd-sync-checker`; autonomous-loop; skill-gate, codebase-audit and framework-audit; and the batch-touched `rules-agents-updater:47,92` → `check_rules_paths.md`, whose fence extracts 164 lines.
- Cross-section citations (D8.7/D8.8): 14 citation sites checked, every one **exact or sanctioned** (leading ordinal, prefix of a `## Name — subtitle` heading, or trailing parenthetical). D8.8's `codebase-audit` → `code_reviewer.md` `### Periodic review — the triage VERDICTS` resolves exactly. `skill-gate`'s citation of a bold lead-in explicitly LABELS it as a lead-in rather than claiming it is a heading.
- Unresolved: none.
- LOW (D8.7-adjacent), `validation-orchestrator:305-306`: the quoted fragment paraphrases `session_rules.md:139` rather than quoting it verbatim. The heading resolves, so not RED.

### [D10] Command → Skill Paths
- **Status: FAIL** (references GREEN; three self-check findings → AE-10, AE-11, AE-12)
- References: `cross-cutting-analysis` from both PRD commands resolves, including its two mode headings. All D10.5 citations resolve — "New component creation", "Version bumps", "Audit intake", "Upstream intake", `/audit` Phase 0 and Phase 3, "The defect series", "Cycle governance" rules 1 and 3, CLAUDE.md triggers (a)-(d), and `session_rules` → "Execution proof".
- Self-checks executed verbatim (D10.0):

| Check | file:line | Stated expectation | Actual | Match? | RED? | GREEN? |
|---|---|---|---|---|---|---|
| apparatus key count | maintenance:263 | 27 | `27` | yes | ratchet | yes |
| checklist item count | maintenance:264 | 14 | `14` | yes | ratchet | yes |
| 27-key receipts loop (over `ece8b08` body) | maintenance:361-379 | every key exactly 1 | 27 keys, all `1` | yes | yes | yes |
| `$`-line >= 1, same loop | maintenance:383-387 | every key >= 1 | all >= 1 | yes | yes | yes |
| angle/ellipsis anchored grep | maintenance:391 | 0 | **2** | NO | yes | yes |
| ALWAYS-REPORT harvester | maintenance:406-431 | harvested < registered | `harvested 18 of 27`; 18/18 present in the loop | yes | yes | yes |
| fence-width loop | maintenance:565-572 | every template non-zero | 13 of 13 non-zero | yes | yes | yes |
| back-sweep LATERAL | maintenance:662-679 | "no output from either half" | no output; **only one command exists** | partial | half 2 cannot | n/a |
| per-fix table pipe count | maintenance:697-698 | three identical numbers | `8 8 8` | yes | yes | yes |
| item 6 frontmatter guard (sandboxed) | maintenance:1043-1045 | exit 0, mode stated | `1 componente(s) OK … [modo estrutural — js-yaml ausente]`, exit 0 — `probe-sandbox: GREEN — live repo unchanged; command exit 0` | yes | yes | yes |
| item 6 raw-template validator | maintenance:1053-1068 | `N validated of M globbed, S skipped, OK` | `19 validated of 25 globbed, 6 skipped (fenced), OK`, exit 0 | yes | yes | yes |
| version residue | maintenance:1264 | ZERO hits after bump | 0 hits | yes | yes | yes |
| canonical surfaces at v2.32.0 | maintenance:1122 | every surface hit | README, claude_md, EPA, audit.md all stamped | yes | yes | yes |
| D16 `staged` / `log` / `dir` (in place) | maintenance:607-610 | `…, 0 hits` | `124 files, 0 hits`; `2 commits, 0 hits`; `.claude/docs` 4 files 0 hits; memory 18 files 0 hits (`dir` with no path fails closed) | yes | yes | yes |
| defect-series parity | audit:1546-1550 | GREEN, both sides computed | `GREEN 18+2=20` | yes | yes | yes |
| Phase 3 item 7 report-commit | audit:1566-1568 | `report committed: sHASH` | `NOT committed — last report commit none, HEAD e895711` (correct: today's report did not exist yet) | yes | proven RED | yes |

- Runtime dual copy (D10.4): **byte-identical: yes** (`diff -r` → no output).
- Trigger-list parity (D10.6) — THREE surfaces: README.md `1` (line 382) · CLAUDE.md `1` (line 154) · audit.md Phase 0 table `1` (row (d)). **All three name the same event set — GREEN.**
- Findings: AE-10, AE-11, AE-12, AE-13, and the AC-6 falsification recorded as AE-14.

## Agent 5: Document Accuracy

### PART 1 — `Part 1: N/A — baseline mode`

### [C] Instruction Style
- **Status: PASS** — 0 live anti-pattern instances. Files sampled: session-end, sprint-proposer and validation-orchestrator SKILL.md, plus `code_reviewer.md` and `validator.md`.
- Every behavioral requirement sampled carries all three §6 properties. session-end's steps 1–6 each own an `ALWAYS REPORT … NEVER emit nothing.` line, closed by a self-check with an explicit expected result. The batch's new agent line ("ALWAYS Read every file under review before judging it") is imperative and compliant. The only descriptive sentence adjacent to behavior is `sprint-proposer:31`, which is correctly descriptive — and it is the line this batch fixed from the false `applies_to:` claim.
- The heuristic grep returned 20 hits across the 5 files; all 20 are the SECOND sentence of an already-bold, already-dedicated mandate, not buried prose.

### [E] Version Consistency
- **Status: PASS** — 0 mismatches. Canonical: `v2.32.0` (README.md:1).
- Stamped surfaces at v2.32.0: README.md:1 and :151, `docs/modules/templates/claude_md.md:4`, `.claude/commands/audit.md:379`, `existing_project_adaptation.md:260` and `:609`.
- Every other hit is lineage and E.5-exempt: the dated `**vX.Y.Z migration —` headings in EPA (v2.1.0, v2.25.0, v2.26.0, v2.27.0, v2.28.0, v2.29.0), `created: framework-v2.2.1` in `commit/SKILL.md:10`, the historical note in `docs/modules/skills/README.md:35`, and maintenance.md's bump-rule EXAMPLES. `docs/agentic_engineering_framework.md` and `CLAUDE.md` carry no version string at all, consistent with prior runs.

### [D12] Examples Convention Compliance
- **Status: PASS** on the measured claims; 1 LOW structural gap (AE-4).
- The new rules-convention claim at `examples/README.md:88-92`, measured over the FULL set in BOTH directions: "11 of 11 carry `domain:`" → 11/11; "`paths:`" → 11/11; "(plus a `# Scope:` comment)" → 11/11, all at line 3; "with no `name:`, `effort:`, `invocation:` or `model:` field" → 0/11 carry any of the four; and the reverse direction, no rules example carries the dead `applies_to:` → 0 hits.
- Other full-set truth-tests all hold: 20/20 agent examples carry `## Output Format`; 10 also carry a shorter `## Output`; the 1-of-11 and 5-of-11 checklist claims measure correctly and are indeed different sets; `model:` present on 20/20 agents and absent from 0 of 9 example skills and 0 of 15 module skills.

### [D15] Factual Accuracy
- **Status: PASS** — 0 inaccuracies across 21 checked claims, including every count this batch changed: README's "8 doc templates" (8 on disk), README:152's "(8, incl. the frontmatter and rules-scope guards)", the concepts doc's "8 document and config blueprints" naming both guards, `docs/modules/README.md:7`'s templates list, and CLAUDE.md's templates tree.
- Unchanged counts re-verified: agents 10, rules 5, skills 15 (12 + 3 tier-gated), examples 20 / 9 / 11, 6 slash commands, the qualified 15-step pipeline claim, and `docs/modules/skills/README.md:38`'s 7 `user` / 8 `inline` split.
- D15.1 — the BANNED sense of "inline": 0 survivors. The only tracked hits are the check's own definition and the historical finding J-14.
- D15.2 — coverage: all 5 `### Level N` headings of the Maturity Model have a README row; all 6 command files appear in BOTH README command tables.
- Cross-check on the batch's own claim: the commit message says 10 agent examples were re-routed; disk confirms 10 touched, in two wordings of one contract, with zero surviving read-all-rules lines in tracked surfaces.

## Agent 6: Process Coverage (meta)

### PART 1 — `Part 1: N/A — baseline mode`

### [D17] Process Coverage
- **Status: FINDINGS** — 4 findings (AE-5, AE-6, AE-7, AE-8), 1 observation.
- Commits classified (40): `docs(audit)` 17 (persistence, write-back, errata, triage, push records) · `fix` 13 (audit-fix application, factual corrections) · `feat` 6 (version bumps / upstream absorption) · `chore` 3 (placeholder substitution) · `docs(lineage)` 1 (push record). Operation types WITH a written home: audit intake, upstream intake, version bumps, placeholder substitution, and push recording into the newest report.
- **Undocumented executed flows:** AE-5 (marking a project's evolution doc `upstreamed` from the mother repo, under owner authorization, has no written exception path — the no-touch rule is stated absolutely in three places and the precedent lives only in an unindexed lineage doc); AE-6 (a push whose receipts live in a commit body has no executable recording home; the follow-up-commit form has happened 4×, only one of which is documented); AE-7 (the endpoint recorded in a lineage doc is invisible to the detector, which returns `advanced` on a range already scanned GREEN — fails safe, but the record is inert).
- **Referenced-but-unowned conventions: none.** Checked `evolution_policy.md:140-168` and `framework-audit/SKILL.md:171-174` (both owned by maintenance.md's Upstream intake), the new guard template's named invokers (`rules-agents-updater` at all tiers, the CI guards stage — both present), and the lineage's `DIFFERS from framework` promise (owned by `existing_project_adaptation.md:826`).
- **Aspirational mechanisms:** AE-8 — this repo's own `component-design.md` gained `paths:` (10 globs, all live) with no measurer HERE; the lineage's anchor table credits a script this repo does not have. The batch found a dead glob here by running the guard ad hoc from a sandbox, and nothing repeats that. Other framework-level cadence claims have owners (freshness → Step 0.5 in both twins; `/audit` triggers → maintenance item 7 + Cycle governance; defect series → audit.md Phase 3).
- **Observation (not a finding):** this repo's `.claude/rules/component-design.md` was loaded in full into the session prompt despite carrying `paths:`. The lazy-load premise is therefore unverified for the mother copy; host configuration cannot be isolated read-only.
- **Unprovable self-checks (D17.4)** — division of labour stated: D10 asks whether a check's stated expectation matches today's output; D17.4 asks only whether RED and GREEN are both reachable. Verdicts below are the provability axis; where the expectation itself is at issue, per D10.

| Check | Can go RED? | Can go GREEN? | Verdict |
|---|---|---|---|
| Receipts key loop + `$`-line assert, over a REPORT file | yes | yes | provable |
| Same, over the COMMIT-BODY home | always RED | **no** | **BROKEN** — all three authorised heading completions require a hash that cannot exist at write time; the fence-aware extractor returns 0 lines from `ece8b08`'s body. Only the unauthorised no-paren form extracts, and then all 27 keys score exactly 1 (AE-9) |
| Push endpoint detector | yes | yes | provable, **mis-scoped** (AE-7) |
| `check-rules-paths.mjs` asserts (new this batch) | yes — negation-proved | yes — healthy exit 0 | provable both directions |
| its "no dead glob" sub-claim as worded in bootstrap's guard list | **no** — dead globs are reported, never failed | yes | decoration as worded (AE-1's class, second surface) |
| `rules-agents-updater` invoker line (new this batch) | yes | yes | provable but **unreported** — no `ALWAYS REPORT ran/skipped` attached, so a silent skip is indistinguishable from a run |
| `version:` self-check | yes | yes | provable |

- **Self-reported controls (D17.5):**

| Control | Verified by whom | Persisted where | Gate? |
|---|---|---|---|
| All 27 receipt keys (ece8b08) | the author only — no `/audit` had run | the commit body (authorised) | **no** — immutable, un-appendable, unreadable by the push detector and by Audit intake, which reads `assets/docs/audit-*.md` |
| `negation proof:` | author, but sandboxed and reproducible | commit body | **yes** — the strongest control in the batch |
| `isolation:` D16 staged | author; the log form was declared owed post-commit and discharged in `e895711` | commit body + lineage | partial |
| `class sweep:` | author | commit body, `$ n/a` | **no** — `$ n/a` is authorised for two keys only |
| `receipt generator:` | author, `$ n/a` | commit body | **no** — this key exists BECAUSE it could not go RED; `$ n/a` restores that state |
| `verifier rounds:` | the author's own verifier; not filed | commit body only | partial — the 7 defects exist nowhere a later session can grep |

- **Convergence assessment (D17.6):**

| Batch | shipped (docs/modules + examples) | apparatus (.claude + assets/docs + CLAUDE.md) |
|---|---|---|
| 87210b1 | +77 −21 | +97 −35 |
| c38caa2 | +50 −15 | +96 −23 |
| adc7481 | +142 −11 | +39 −3 |
| 34a7c1c | +38 −1 | +40 −2 |
| 961244b | +26 −2 | +31 −7 |
| 280c2f2 | **+2 −2** | **+344 −42** |
| 1487052 | **+1 −1** | +10 −4 |
| **ece8b08** | **+341 −45** | +193 −10 |
| **HEAD~20..HEAD** | **+379 −53** | **+1266 −45** |

Over the last 20 commits apparatus outweighs shipped 3.3:1 — but `ece8b08` alone supplies 341 of the 379 shipped lines. Strip it and the other 19 commits ship **+38** against **+1073** apparatus: **28:1**. Every one of those 19 originates INSIDE the loop (audit → fix → write-back → errata → triage). The only batch that moved the surface bootstrapped projects receive came from OUTSIDE evidence — a production project's measured defect. That is precisely the premise the 2026-09-20 trigger-(d) exit was built on, and this batch honoured it mechanically (`verification audit: not triggered — no evidence from outside the loop`). **So the control added does address the cause** — it is the first one in this series that REMOVES work rather than adding a step, and its first live application held. The residual cost is AE-6, AE-7 and AE-9: the commit-body receipts home is authorised but has no working guard, no writable push slot and no downstream reader. Fixing that is a deletion-shaped fix — make the commit-body home produce a stub report row, or retire it — not another control.

## Recommended Fixes

0. **LOW BACKLOG — never a session on its own.** AE-1 (`README.md`), AE-2 and AE-3 (`bootstrap.md`), AE-4 (`examples/README.md`), AE-8 (the lineage doc), AE-12 and AE-13, plus the 55 carried findings. Per `/maintenance` → "Cycle governance" rule 2 these are applied only when a later session already edits their file. **AE-1 is the exception worth naming:** it is LOW by mechanism but it misstates a shipped contract on the surface an owner reads to decide whether to wire a CI stage, and `README.md` is a one-line edit.
1. **Quick fixes** — AE-1 (one clause), AE-13 (one clause), AE-3 (a heading), AE-8 (one table cell).
2. **Structural fixes** — AE-9 and AE-6/AE-7 travel together: the commit-body receipts home needs either a fourth authorised heading completion with no hash, or retirement in favour of always writing a stub report. AE-10 (the LATERAL check's missing half 2) is the other structural one, and it is a control that reads GREEN while half of it does not exist.
3. **Quality improvements** — AE-11 and AE-12 are about this batch's own receipts: name the two angle-bracket survivors with their reason, and stop discharging `$ n/a` on keys the rule does not authorise.
4. **ESCALATED — owner decision required** — none this run.
5. **ACCEPTED-RISK — no action** — E-1, M-44, N-53, T-11, T-36, U-48, B-17 (listed in the header; each cites its record and none is re-opened).

## Meta-observation

**prediction: 3 clauses evaluated — (1) failed, (2) failed, (3) void.**
- **(1)** AD-1's fix did not ship and no `carried:`-vs-rows check was added, yet this run carries 55 findings as 55 ledger ROWS and its `carried:` count equals that row count — which is the falsifier the clause itself named. The clause predicted the drop would recur; it did not. What made the difference was not a control but the run reading the previous report's ROWS instead of its sentence, exactly as Phase 3 item 3 instructs. **The gap AD-1 names is still open: nothing counts the rows.**
- **(2)** AD-9's fix did not ship and this batch's receipts went into a commit body, yet all 27 keys score 1 with a `$` line each. The predicted symptom did not appear. The gap did not close, though: nothing would have fired had the keys been missing, and AE-9 shows the extractor authorised for that home returns zero lines — so the home passes by accident, under an unauthorised heading form.
- **(3)** AD-4 and AD-5 moved in the SAME batch (`280c2f2`), which the clause named as its falsifier. Void: the antecedent never occurred.

**The recurring class this run saw: a control whose ALTERNATIVE PATH was authorised in prose and never built.** Three findings are the same shape one level apart. The receipts checklist authorises two homes — a report file and a commit body — and every mechanism downstream (the fence-aware extractor, the push-endpoint detector, Audit intake's reader) was built for the first only, so the second passes or fails by accident (AE-9, AE-6, AE-7). The LATERAL sweep authorises two halves and ships one command, so half a mandatory control is undischargeable and reads GREEN either way (AE-10). The `$`-line rule authorises `n/a` for two keys and the checking grep cannot tell those two from the other twenty-five (AE-12). In each case the prose is careful, enumerates the alternative, and nothing was built to read it. This is distinct from the class the previous runs kept naming (a mandate with no destination): here the destination exists, and the READER does not.

**Prediction, stated so it can fail.**
- **(1)** If AE-9 is fixed by adding a fourth authorised heading completion WITHOUT changing the extractor, the next commit-body batch will still extract zero lines, because the extractor anchors on the parenthesis. **What would falsify this:** the heading set is extended and a commit-body batch then extracts a non-empty section; or the fix changes the extractor instead.
- **(2)** If AE-6/AE-7 are fixed only by documenting the lineage-doc home rather than by making the detector read it, the next session after a push recorded that way will again re-run D16 over a range already scanned. **What would falsify this:** the detector gains the second source, or the lineage home is retired.
- **(3)** The next batch that ships to `docs/modules/` will again come from a project evolution doc, not from this report: of the 14 findings filed today, 12 are apparatus-side and the 2 shipped-side ones (AE-3, AE-4) are single-line documentation edits. **What would falsify this:** a batch derived from this report moves more than 50 lines under `docs/modules/` or `examples/`.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan — starting with AE-1, which is a one-line correction of a shipped contract claim, and then the AE-9/AE-6/AE-7 cluster, which should be fixed by DELETING a path rather than adding a control.

---

**Report status:** COMPLETE — all 6 agents returned, all 17 dimensions evaluated, no `probe-sandbox: RED`.
**defect series:** N/A — baseline mode.
*(superseded)* **Application status:** PENDING
**Application status:** APPLIED — 14 of 14 (13 in `s7be4aea`, 1 in `sWB`)

## Post-change checklist receipts (`s7be4aea` · `sWB`)

**THIS SECTION IS IN A REPORT FILE, WHICH IS NOW THE ONLY HOME** — the commit-message body was
retired by this very batch (AE-9/AE-6/AE-7). The batch is two commits: `s7be4aea` (the 13 fixes) and
`sWB` (this write-back, which also carries AE-11's correction).

**isolation:** D16 log (patches + messages + paths) — see below
    $ bash .claude/scripts/d16-gate.sh log 7be4aea~1..7be4aea
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits
**fences:** 13 of 13 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; echo "$(basename $t) -> $(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l)"; done | tr '\n' ' '
    check_agent_frontmatter.md -> 218 check_rules_paths.md -> 164 claude_md.md -> 92 framework_metrics_md.md -> 28 metrics_md.md -> 13 pendencias_md.md -> 37 project_md.md -> 31 settings_json.md -> 40 component_design.md -> 355 evolution_policy.md -> 168 ops_rules.md -> 58 quality_budgets.md -> 28 session_rules.md -> 329
**references:** every cited section resolved — denominator from the batch diff
    $ git diff 7be4aea~1..7be4aea | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l
    4
**instruction style:** added / rewritten / buried over the batch range; the 1 buried is a line that OPENS with its own mandate (`**THEREFORE: ALWAYS …`), eyeballed — the documented false-positive mode
    $ D="git diff 7be4aea~1..7be4aea"; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried" 
    10 added, 0 rewritten, 1 buried
**liveness:** structural-only (js-yaml absent) + the raw-template validator
    $ sed -n '/^````js$/,/^````$/p' docs/modules/templates/check_agent_frontmatter.md | sed '1d;$d' > /tmp/g.mjs && node /tmp/g.mjs | tail -1
    ✓ check-agent-frontmatter: 1 componente(s) OK (frontmatter parseável, name casa o arquivo) [modo estrutural — js-yaml ausente].
**version:** none — apparatus and documentation only; the canonical stamp stays v2.32.0
    $ grep -rn "v2.32.0" README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l
    7
**gates:** twin parity + receipt-rows-vs-diff + row-count + slots
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis >/dev/null && echo "twin parity PASS"; diff <(sed -n '/^```json$/,/^```$/p' docs/modules/templates/settings_json.md | sed '1d;$d' | tr -d '\r') <(sed -n "/<< 'SETTINGS'/,/^SETTINGS$/p" .claude/commands/existing_project_adaptation.md | sed '1d;$d' | tr -d '\r') >/dev/null && echo "settings twin PASS"; git show --name-only --format="" 7be4aea | wc -l
    twin parity PASS
    settings twin PASS
    7
**verifier rounds:** 1 round, 5 defects reproduced, 5 fixed before commit — filed as V1..V5 inside this section; trigger and ratchet below
    $ git diff --name-only 7be4aea~1..7be4aea -- .claude/commands .claude/scripts docs/modules | wc -l
    3
**control back-sweep:** the enumerator over both command files plus the docs/modules files this batch changed
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md $(git diff --name-only 7be4aea~1..7be4aea -- docs/modules); do inf=$(awk '/^```/{g=!g;next} g' "$f" | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 7 fenced + 30 inline = 37
    .claude/commands/audit.md: 10 fenced + 43 inline = 53
**defect series:** N/A — no /audit run in this session; the baseline run earlier today appended nothing (baseline mode)
    $ grep -c '^| [0-9]* | `audit-' .claude/commands/audit.md
    20
**report deletions:** none — the gate over both report files this batch touched
    $ for f in $(git diff --name-only 7be4aea~1..7be4aea -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]{1,2}-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:' '^```'; do b=$(git show 7be4aea~1:"$f" 2>/dev/null | awk '/^```/{g=!g;print;next} !g' | grep -cE "$pat"); a=$(awk '/^```/{g=!g;print;next} !g' "$f" | grep -cE "$pat"); [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat $b -> $a"; done; done; echo "no RED line above = no structural decrease"; 
    no RED line above = no structural decrease
**placeholder:** 1 token in use (`sWB`), substituted by the follow-up commit
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING|WB)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING|WB)`? *\|' assets/docs/audit-2026-09-22.md | wc -l
    0
**measurer:** N/A — no lineage doc written this session (the 2026-09-22 lineage gained a row, checked below)
    $ grep -ciE 'próxima auditoria deve|the next audit (must|should) measure' assets/docs/framework-evolution-upstream-2026-09-22-path-scoped-rules.md
    0
**verification audit:** decided by Cycle governance rule 1
    $ PEND=0; for d in $(grep -L "STATUS.*upstreamed" projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); [ "$PEND" -gt 0 ] || [ "$OBS" -gt 0 ] && echo "trigger (d): fires — pending $PEND, HIGH observed $OBS" || echo "trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog" 
    trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog
**push decay:** unchanged since this session's own push
    $ git fetch -q --prune origin; git rev-parse --short refs/remotes/origin/main
    ab90b2c
**inventory sweep:** N/A — no artifact added, removed, renamed or split, and no user-visible capability added; the batch amends existing controls and corrects existing prose
    $ git show --name-only --format="" 7be4aea
    (7 files, all pre-existing — listed under `gates:` above)
**class sweep:** 13 fixes × 4 directions, 2 extra instances found and fixed (the `commit body` survivors swept out of the KNOWN LIMIT paragraph; the `audit.md` carry-forward reader swept for the receipts-only file kind)
    $ grep -cE '^\| AE-[0-9]+ \| `s' assets/docs/audit-2026-09-22.md   # rows in the per-fix table below
    14

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| AE-1 | `s7be4aea` | `README.md` | ✓ `grep -rn "dead glob" README.md CLAUDE.md docs/` | ✓ concepts doc + CLAUDE.md already accurate | ✓ row re-read whole | n/a | none |
| AE-2 | `s7be4aea` | `.claude/commands/bootstrap.md` | ✓ `grep -n "mkdir -p projects" bootstrap.md` | ✓ EPA twin's backstop copied as the model | ✓ comment rewritten with the fence | n/a | none |
| AE-3 | `s7be4aea` | `.claude/commands/bootstrap.md` | ✓ `grep -n "^### Step 5.7"` | ✓ EPA has no equivalent heading | ✓ step body re-read | n/a | none |
| AE-4 | `s7be4aea` | `examples/README.md` | ✓ `grep -n "Frontmatter" examples/README.md` | ✓ agent/skill blocks already carry theirs | ✓ preamble routing line re-read | n/a | none |
| AE-5 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ `grep -rn "no-touch" .claude/commands/ CLAUDE.md` | n/a — no twin | ✓ item 8 swept in the same edit | ✓ closing-report line named | item 8 asserted the rule unconditionally |
| AE-6 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ `grep -rn "commit body" .claude/commands/` | n/a | ✓ push rule re-read whole | ✓ the slot it needs is the retired-home fix | same edit as AE-9 |
| AE-7 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ same grep | n/a | ✓ KNOWN LIMIT rewritten | ✓ detector's own `$` line re-read | the verifier found the rewrite OVERSTATED it; corrected |
| AE-8 | `s7be4aea` | lineage doc | ✓ `grep -n "at every tier" assets/docs/` | n/a | ✓ anchor table re-read | n/a | none |
| AE-9 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ `grep -rn "commit message body\|no report file" .claude/ CLAUDE.md` → only the retirement text | n/a | ✓ heading-forms paragraph re-read | ✓ Audit intake step 1 + `/audit` Phase 3 swept for the new file kind | 2 readers resolving the same glob |
| AE-10 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ `grep -n "HALF" maintenance.md` | n/a | ✓ the fence's expectation line re-read | n/a | none |
| AE-12 | `s7be4aea` | `.claude/commands/maintenance.md` | ✓ `grep -n 'n/a' maintenance.md` | n/a | ✓ the anti-decoration check re-read | n/a | none |
| AE-13 | `s7be4aea` | `README.md` | ✓ `grep -rn "outside the loop" README.md CLAUDE.md` | ✓ the other two trigger surfaces already correct | ✓ row re-read | n/a | none |
| AE-14 | `s7be4aea` | `assets/docs/audit-2026-09-21.md` | ✓ `grep -c '^> \*\*Errata ' assets/docs/*.md` | n/a | ✓ AC-6 row re-read | n/a | none |
| AE-11 | `sWB` | `assets/docs/audit-2026-09-22.md` | ✓ this section's own `$` lines | n/a | ✓ the two survivors named below | n/a | none |

**back-sweep:** 1 rule promoted (receipts live in a report file, always), 2 artifacts fixed of 2 found, re-run clean
    $ grep -rniE "commit (message )?body|no report file" .claude/commands/ CLAUDE.md | grep -vc "retir\|NEVER the receipts\|SUMMARISE\|silent"
    0

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| Receipts live in a dated report file, always | any surface still offering the commit body as a home, or reading receipts from one | `grep -rniE "commit (message )?body\|no report file" .claude/commands/ CLAUDE.md` minus the retirement text | 2 | 2 | 0 remaining |

**negation proof:** LATERAL half 2 → red as expected; the per-key `$ n/a` check → red as expected
    $ bash .claude/scripts/probe-sandbox.sh run --staged -- bash "$T/np_half2.sh"
    HALF2 RED: .claude/telemetry/
    probe-sandbox: GREEN — live repo unchanged; command exit 0
    $ awk '/^\*\*[a-z][a-z ._-]*:/{k=$0; sub(/^\*\*/,"",k); sub(/:.*/,"",k)} /^ *\$ *n\/a/{print k}' <planted section> | sort -u
    gates push
    (the healthy section prints only: classification new component)

**applied-proof:** 13 of 13 IDs resolve to a commit touching their line (form A with a pre-batch anchor: 4; form A with an ADDED-TEXT anchor, the only form that can fire on an insert-only fix: 3; form B / file-level for the 3 IDs whose ledger Location names no line, plus 3 more: 6), 0 forced open
    $ anchor=$(git show 7be4aea~1:<file> | sed -n '<line>p'); git log -S"$anchor" --oneline 7be4aea~1..7be4aea -- <file>
    AE-1 form A -> 7be4aea · AE-7 form A -> 7be4aea · AE-9 form A -> 7be4aea · AE-13 form A -> 7be4aea
    AE-5 / AE-6 / AE-10 form A (added-text anchor) -> 7be4aea (a pre-batch anchor CANNOT fire on an insert-only fix — stated, not hidden)
    AE-2 / AE-4 / AE-14 form B -> ok · AE-3 / AE-8 / AE-12 file-level -> touched
**classification:** 11 add, 3 substitute (the receipts-home bullet pair → one home; the KNOWN LIMIT paragraph; the `$ n/a` count → a per-key form), 1 delete (the commit-body home), 0 relocate
    $ n/a — judgement, not measurement
**new component:** none this session
    $ n/a — judgement, not measurement
**audit:** ran — this batch APPLIES the 2026-09-22 baseline run; no bump, so trigger (b) does not fire
    $ ls assets/docs/audit-2026-09-22.md
    assets/docs/audit-2026-09-22.md
**commit correction:** none — AE-11 is corrected HERE rather than by amending `ece8b08` (Audit intake item 8 forbids amending; its receipts are in an immutable commit body, which this batch retires)
    $ git log -1 --format=%s ece8b08
    feat: upstream path-scoped rules — paths: replaces the ignored applies_to (v2.32.0)
**report restore:** none
    $ git log --diff-filter=D --oneline -- 'assets/docs/audit-*.md'
    (no output — no report file was ever deleted)
**receipt generator:** pasted below — 73 lines, 15 keys generated, hand-typed: inventory sweep, class sweep, back-sweep, negation proof, applied-proof, classification, new component, audit, commit correction, report restore, receipt generator, push
    $ PYTHONIOENCODING=utf-8 python receipt_generator.py   # the script pasted below, run from the repo root; it also writes receipts_generated.md, which is spliced into this section

```python
"""Receipt generator for the 2026-09-22 findings application.
Run from the repo root:  PYTHONIOENCODING=utf-8 python receipt_generator.py > receipts.md
Every `$` line it emits is the command it actually ran; every block beneath one is that
command's literal stdout. Keys it does NOT generate are listed at the end, on stderr."""
import subprocess, sys, io, re

FIX = "7be4aea"
RANGE = f"{FIX}~1..{FIX}"
out = []


def sh(cmd):
    r = subprocess.run(["bash", "-lc", cmd], capture_output=True, text=True, encoding="utf-8", errors="replace")
    return (r.stdout + r.stderr).strip()


def key(name, verdict, cmd=None):
    out.append(f"**{name}:** {verdict}")
    if cmd:
        out.append(f"    $ {cmd}")
        for line in sh(cmd).split("\n"):
            out.append("    " + line)


# --- mechanical keys, each with the command that produced its number -----------------
isolation = sh(f"bash .claude/scripts/d16-gate.sh log {RANGE}")
key("isolation", isolation.split(":")[0] + " — see below", f"bash .claude/scripts/d16-gate.sh log {RANGE}")

key("fences", "13 of 13 templates extract non-empty",
    """for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; echo "$(basename $t) -> $(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l)"; done | tr '\\n' ' '""")

key("references", "every cited section resolved — denominator from the batch diff",
    f"""git diff {RANGE} | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\\.md`|component-design §[0-9]+|D[0-9]+\\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l""")

key("instruction style", "added / rewritten / buried over the batch range; the 1 buried is a line that OPENS with its own mandate (`**THEREFORE: ALWAYS …`), eyeballed — the documented false-positive mode",
    f"""D="git diff {RANGE}"; echo "$($D | grep '^+' | grep -cE '\\b(ALWAYS|MUST|NEVER)\\b') added, $($D | grep '^-' | grep -cE '\\b(ALWAYS|MUST|NEVER)\\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\\**(ALWAYS|MUST|NEVER)\\b') buried" """)

key("liveness", "structural-only (js-yaml absent) + the raw-template validator",
    """sed -n '/^````js$/,/^````$/p' docs/modules/templates/check_agent_frontmatter.md | sed '1d;$d' > /tmp/g.mjs && node /tmp/g.mjs | tail -1""")

key("version", "none — apparatus and documentation only; the canonical stamp stays v2.32.0",
    """grep -rn "v2.32.0" README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l""")

key("gates", "twin parity + receipt-rows-vs-diff + row-count + slots",
    """diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis >/dev/null && echo "twin parity PASS"; diff <(sed -n '/^```json$/,/^```$/p' docs/modules/templates/settings_json.md | sed '1d;$d' | tr -d '\\r') <(sed -n "/<< 'SETTINGS'/,/^SETTINGS$/p" .claude/commands/existing_project_adaptation.md | sed '1d;$d' | tr -d '\\r') >/dev/null && echo "settings twin PASS"; git show --name-only --format="" """ + FIX + """ | wc -l""")

key("verifier rounds", "1 round, 5 defects reproduced, 5 fixed before commit — filed as V1..V5 inside this section; trigger and ratchet below",
    f"""git diff --name-only {RANGE} -- .claude/commands .claude/scripts docs/modules | wc -l""")

key("control back-sweep", "the enumerator over both command files plus the docs/modules files this batch changed",
    f"""for f in .claude/commands/maintenance.md .claude/commands/audit.md $(git diff --name-only {RANGE} -- docs/modules); do inf=$(awk '/^```/{{g=!g;next}} g' "$f" | grep -cE '^\\s*(\\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{{4,}}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done""")

key("defect series", "N/A — no /audit run in this session; the baseline run earlier today appended nothing (baseline mode)",
    """grep -c '^| [0-9]* | `audit-' .claude/commands/audit.md""")

key("report deletions", "none — the gate over both report files this batch touched",
    f"""for f in $(git diff --name-only {RANGE} -- 'assets/docs/audit-*.md'); do for pat in '^#{{1,3}} ' '^\\| [A-Z]{{1,2}}-[0-9]+ \\|' '^> \\*\\*Errata ' '^ *\\$ ' '^\\*\\*[a-z][a-z ._-]*:' '^```'; do b=$(git show {FIX}~1:"$f" 2>/dev/null | awk '/^```/{{g=!g;print;next}} !g' | grep -cE "$pat"); a=$(awk '/^```/{{g=!g;print;next}} !g' "$f" | grep -cE "$pat"); [ "${{a:-0}}" -lt "${{b:-0}}" ] && echo "RED $f: $pat $b -> $a"; done; done; echo "no RED line above = no structural decrease"; """)

key("placeholder", "1 token in use (`sWB`), substituted by the follow-up commit",
    """grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING|WB)`?|\\| *`?s(BATCH|GROUP[0-9]+|PENDING|WB)`? *\\|' assets/docs/audit-2026-09-22.md | wc -l""")

key("measurer", "N/A — no lineage doc written this session (the 2026-09-22 lineage gained a row, checked below)",
    """grep -ciE 'próxima auditoria deve|the next audit (must|should) measure' assets/docs/framework-evolution-upstream-2026-09-22-path-scoped-rules.md""")

key("verification audit", "decided by Cycle governance rule 1",
    """PEND=0; for d in $(grep -L "STATUS.*upstreamed" projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\\| [A-Z]{1,2}-[0-9]+ \\| HIGH \\|' assets/docs/audit-*.md | grep -c '\\[observed in use\\]'); [ "$PEND" -gt 0 ] || [ "$OBS" -gt 0 ] && echo "trigger (d): fires — pending $PEND, HIGH observed $OBS" || echo "trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog" """)

key("push decay", "unchanged since this session's own push",
    """git fetch -q --prune origin; git rev-parse --short refs/remotes/origin/main""")

sys.stderr.write("keys NOT generated (hand-typed): inventory sweep, class sweep, back-sweep, negation proof, classification, new component, audit, commit correction, applied-proof, report restore, receipt generator, push\n")
io.open("receipts_generated.md", "w", encoding="utf-8").write("\n".join(out) + "\n")
print("\n".join(out))
```

**push:** requested and done earlier this session — D16 GREEN over 2 unpushed commits → pushed ece8b08..ab90b2c; this batch's two commits are NOT yet pushed — origin/main at ab90b2c
    $ git rev-parse --short refs/remotes/origin/main
    ab90b2c

### AE-11 — the correction this write-back owes

`ece8b08`'s receipts carry two `$` lines that are angle-bracket descriptions rather than commands:
`$ <bash -n ratchet, item 10>` under `verifier rounds:` and `$ <Cycle governance rule 1 command>`
under `verification audit:`. The rule requires NAMING any survivor with the reason it cannot be
written literally, and neither was named. **Neither has a reason: both commands are perfectly
writable**, and they appear literally in this section (`verifier rounds:` and `verification audit:`
above). The receipts they sit in are in an immutable commit body — which is exactly why this batch
retired that home. Six keys in the same body also used `$ n/a`, four of them unauthorised (AE-12);
the per-key check added by this batch would have gone RED on them, and its output is pasted above.

### Independent pre-commit verifier — the 5 defects it reproduced, fixed before commit

- **V1 (MEDIUM)** — the amended `$ n/a` check could not enforce its "ONLY under" half: two escapes
  under two UNAUTHORISED keys scored identically to the healthy two. Replaced the count with the
  per-key `awk` form.
- **V2 (MEDIUM)** — the `mkdir -p` added for AE-2 falsified the V-12 comment directly above it
  (`mkdir -p` creates every parent, which IS V-12's defect). Replaced with the EPA twin's real
  backstop: name check, project-folder check, then the mkdir.
- **V3 (MEDIUM)** — the rewritten KNOWN LIMIT overstated the retirement. The detector picks the
  newest FILENAME, so a push recorded correctly in a NON-newest report is still missed, silently.
  The paragraph now states that limit and mandates writing the `push:` line into the newest-dated
  report file.
- **V4 (MEDIUM)** — the new receipts-only file kind enters the `assets/docs/audit-*.md` glob that
  two READERS resolve for findings. Both readers (`maintenance.md` Audit intake step 1 and
  `audit.md` Phase 3 carry-forward) now skip a `receipts-only` header.
- **V5 (LOW)** — the Errata block named the run that FILED AE-14 rather than the application that
  authored it. Corrected to "the 2026-09-22 findings application".
