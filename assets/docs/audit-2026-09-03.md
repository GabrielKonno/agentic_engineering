# Framework Audit Report — 2026-09-03

**Date:** 2026-09-03
**Framework version:** v2.13.0 (`README.md:1`), HEAD `04cb256`, `origin/main` == HEAD
**Run mode:** `verification (over a2f4890)` — CLAUDE.md trigger (d); trigger (b) satisfied by the same run
**Under verification:** the 57 findings disposed across `d4d3c33`, `a2f4890` and `04cb256`
**Dimensions checked:** 17 + a Part 1 fix-verification pass + a receipt-verification pass
**Agents dispatched:** 6

**carried: 0 open + 2 accepted-risk** from `audit-2026-09-02.md`. Both re-measured and reported as
OBSERVATIONS below, never re-opened.

> **ID scheme.** `F-*`/`G-*` (2026-08-31), `H-*` (09-01), `J-*` (Run 2), `K-*` (Run 3), `L-*` (Run 4),
> `M-*` (Run 5). This run uses `N-*`. New file rather than an append: the date changed.

**What was new about this run.** Three things were tested for the first time: the **three-source D16
blocklist** as an executed check (340 identifier candidates × 6,687 project code files → 124
intersections); the **mechanical back-sweep table** M-39 introduced; and the **two-commit split**
Run 5's evidence recommended. All three produced results, and two produced findings against
themselves.

---

## Part 1 — Verification ledger

**Score, counted over the FINDINGS: 28 clean, 23 not clean, 6 not covered by any agent's
dimensions.** Of the 23: **11 introduced-a-defect**, **6 class-not-swept**, **6 partially-fixed**,
and **1 declared-applied-but-never-made**.

### Clean (28)
`M-1` (de-duplication verified against `d4d3c33^`: nothing lost, 30 steps monotonic, 0 duplicate
numbers) · `M-7` · `M-8` · `M-11` · `M-13` (one LOW residue → N-33) · `M-17` (0 meta-commentary in
any of the ten gap blockquotes) · `M-19` · `M-21` · `M-22` (→ N-31 for the residue) · `M-25` ·
`M-26` · `M-27` (run: returns 1 in both sections; negation holds) · `M-30` · `M-31` (12/12 GREEN in
both sections — the check that failed on the healthy state last run now passes) · `M-32` · `M-33` ·
`M-34` · `M-42` (category-by-category 1:1 against `bootstrap.md:346-352`) · `M-44` (five fields
present; 226 occurrences across exactly 4 files — the figure is EXACT) · `M-45` · `M-46` (all five
defect figures independently recounted; trigger attribution verified by `git log -S`) · `M-50`
(cosmetic → N-38) · `M-51` (4 of 15 recounted exact) · `M-52` · `M-53` · `M-54` · `M-56` ·
**VERSION** (v2.13.0 on 3/3 surfaces, 5 occurrences; 0 of 9 lineage fields rewritten; MINOR correct).

### Not clean (23)

| ID | Verdict | Pointer |
|----|---------|---------|
| M-2 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | → N-21 |
| M-6 | **PARTIALLY-FIXED** | → N-23 |
| M-9 | **PARTIALLY-FIXED** | → N-22 |
| M-10 | **PARTIALLY-FIXED — the fix is INVERTED** | → N-24 |
| M-12 | **FIXED-BUT-CLASS-NOT-SWEPT** | → N-25 |
| M-14 | **PARTIALLY-FIXED** | → N-29 |
| M-15 | **INTRODUCED A DEFECT** | → N-30 |
| M-18 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | → N-1 |
| M-20 | **CONFIRMED-FIXED — CLASS NOT SWEPT** | → N-32 |
| M-23 | **PARTIALLY-FIXED** | → N-2 |
| M-24 | **CONFIRMED-FIXED — the verifying sweep is broken** | → N-42 |
| M-36 | **PARTIALLY-FIXED — INTRODUCED FOUR DEFECTS** | → N-10, N-11, N-12, N-13 |
| M-37 | **PARTIALLY-FIXED — back-sweep MISS** | → N-43 |
| M-38 | **CONFIRMED-FIXED — residue** | → N-39 |
| M-39 | **CONFIRMED-FIXED — INTRODUCED A DEFECT** | → N-40 |
| M-40 | **PARTIALLY-FIXED** | → N-36 |
| M-41 | **PARTIALLY-FIXED — INTRODUCED A DEFECT** | → N-14 |
| M-43 | **FIXED-BUT-CLASS-NOT-SWEPT (HIGH)** | → N-15, N-16, N-7 |
| M-47 | **CONFIRMED-FIXED (text) — receipt FALSE** | → N-35 |
| M-48 | **CONFIRMED-FIXED — BROKE THE TWIN PAIR** | → N-6 |
| **M-49** | **NOT FIXED — the status is FALSE** | → **N-34** |
| M-55 / M-56 | disposed but **no ledger row, no status column** | → N-4 |
| M-57 | **FIXED-BUT-CLASS-NOT-SWEPT** | → N-28 |

### Not covered
`M-3`, `M-4`, `M-5`, `M-16`, `M-28`, `M-29`, `M-35` — disposed in `d4d3c33` and inside no agent's
Part-1 assignment this run. Stated rather than silently counted as clean.

---

## Part 1b — Receipt verification

Third run with auditable receipts. The pattern is now stable and worth stating as a result rather
than a list: **every receipt claim whose value a stated command reproduces was TRUE; every claim
whose value the author supplied from reading was FALSE or overstated.**

| Receipt claim | Re-measured | Verdict |
|---|---|---|
| `version: v2.13.0 … 5 occurrences / 3 surfaces; 0 lineage rewritten` | exact | **TRUE** |
| back-sweep **M-57 row**: `N found 2 / N fixed 2 / 0 remaining` | greps re-run: 0 per twin today | **TRUE — the one row that works** |
| M-44 record: `226 occurrences across 4 files` | 226 / 4 | **TRUE** |
| `liveness: 25 components OK` | re-run | **TRUE** |
| Key-presence and item-9 checks | 12/12 and 1/1 in both sections | **TRUE — GREEN on the healthy state** |
| back-sweep **M-36 and M-56 rows**: `0 remaining` | the named greps return **2** and **13/50**; they enumerate PRESENCE, not residue, so 0 is unproducible | **FALSE → N-40** |
| `class sweep: Row count 8. Findings applied 8. Equal` (`sd4d3c33`) | **14** findings carry `applied sd4d3c33`; 6 have no row | **FALSE → N-5** |
| `references: 4 cited sections resolved` | ≥35 distinct citation targets in the added lines alone | **FALSE — undercounts ~9× → N-3** |
| `instruction style: 34 … 0 rewritten … all on their own line` | 50 added imperative lines; **19 removed lines carried imperatives**; ≥6 buried mid-paragraph | **FALSE ×3 → N-37** |
| `isolation: 20 files scanned, 1 hit` | 23 touched files across the batch's commits; a fresh D16 finds a **second** hit | **FALSE ×2 → N-20** |
| `inventory sweep: … 2 stale claims fixed` | **1** README site edited; a third corrected count uncounted | **FALSE → N-36** |
| `M-49 \| ✓ grep -rn "silently skipped" docs/modules/ → 1 site` | the grep still returns that site; **the file is in no commit of this batch** | **FALSE → N-34** |
| `M-40 \| ✓ both fixed in one pass` | 1 of 2 sites | **FALSE → N-36** |
| `M-47 \| grep -coE … → 5 before, 11 after` | lines 4→7; occurrences 5→10 | **FALSE → N-35** |
| `M-43 \| grep -rc "a hand-rolled date helper" → 0 after` | 3 framework-layer hits remain | **FALSE → N-7, N-16** |
| `M-12 \| ✓ re-ran all 8 fence extractions` | the receipt two lines above still says **6 templates**; `metrics_md` (9) unmeasured again | **SELF-CONTRADICTORY → N-25** |
| `M-36 \| ✓ D16.2b/D16.3 renumbered around the insert` | pure insert; `D16.2c` sits **before** `D16.2b` | **FALSE → N-10** |
| `M-48 \| ✓ all three fixed in one pass` | swept the three skills, never the twin pair | **FALSE → N-6** |
| `M-23 \| Re-test: 6/6 PASS` | 3 of 6 fail | **FALSE → N-2** |
| L-17's `✓ must quote a pattern` | `sd4d3c33`: 21 of 28 unquoted; `sa2f4890`: 97 of 137 unquoted | **RED in both** |
| L-17's `✗ with no reason is RED` | 0 `✗` cells in 51 rows across both receipts | vacuously satisfied |

---

## New findings

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| N-34 | `docs/modules/skills/sprint-proposer/SKILL.md:37` | **M-49 was declared applied, receipted, and never made.** `silently skipped` is still there; `git log -1` on the file returns `a28f661`, **two batches earlier**; the file appears in no commit of this batch. The ledger says `applied sa2f4890`, the commit message lists it, and the receipt asserts a grep that still returns the site today. **The row-count self-check passed at 43 = 43 because a ROW existed — the control is satisfiable without the work.** | applied `s1c7c1f9` |
| N-8 | `.claude/commands/maintenance.md:127-136` | **`d4d3c33` pasted a five-line paragraph TWICE** — byte-identical, confirmed by `grep -c` → 2. This is M-1's exact class, committed by the small, isolated, careful commit **whose entire stated purpose was de-duplicating M-1**, into the file that hosts the ADJACENT-direction rule. It then survived `a2f4890` and `04cb256`, two inventory sweeps and two class sweeps. | applied s0052074 |
| N-6 | `.claude/skills/cross-cutting-analysis/SKILL.md` vs `docs/modules/skills/…` | **The repo's only twin pair is no longer byte-identical.** M-48's fix edited the `docs/modules/` copy only; `cmp` exits 1 at byte 739. Breaks D10.4, dimension A, CLAUDE.md's dual-placement rule and checklist item 8's twin obligation in one edit. Run 5 measured this GREEN. The framework's own PRD sessions now run a skill without the report mandate every project receives. | applied s0052074 |
| N-2 | `audit.md` Agent 1 / Agent 5 / Agent 6 blocks | **The guard rule fails on 3 of 6 blocks**, against `d4d3c33`'s "Re-test: 6/6". Agent 1 omits the `projects/` folder listing D16.1 builds the blocklist from; Agent 5 omits the `docs/modules/skills/` and `docs/modules/agents/` listings D15.5/D15.6 mandate — **and Agent 5 is the block M-23 named**; Agent 6's INPUTS scope `docs/modules/` while D17.3 greps `docs/`. The back-sweep grep was written for one shape (named files) and never re-run for the shape the rule governs (surfaces a check reads). | applied s6f4bc1a |
| N-15 | `docs/modules/rules/session_rules.md:161` | **A hand-rolled source-project function name ships in a template every project receives** — and it sits on the line **immediately above** the one M-43 fixed. Confirmed present in the source project's code. D16.4 gives templates double scrutiny; the receipt cell for M-43 reads `✓`. | applied s81cad36 |
| N-16 | `assets/docs/framework-base-upgrade.md:110` | **The identifier M-43's receipt claims returns 0 survives in a tracked lineage doc**, which D16.5 declares NOT exempt. `04cb256`'s commit message also now carries it, and `origin/main == HEAD`, so per D16.3c that occurrence is **PUSHED**. | applied s81cad36 |
| N-40 | `assets/docs/audit-2026-09-02.md` back-sweep table | **2 of the 3 rows in M-39's first execution report a `0 remaining` their own named grep cannot return.** The M-36 and M-56 rows cite PRESENCE greps (returning 2 and 13/50); only a RESIDUE grep has a GREEN value of 0. The form mandates "`N found` comes from RUNNING the grep, never from reading" — and two rows report a number the grep never produced, inside the item written to eliminate exactly that. | applied s6f4bc1a |
| N-5 | `assets/docs/audit-2026-09-02.md` `sd4d3c33` receipt | **8 rows for 14 findings**, asserting "Row count: 8. Findings applied: 8. Equal — the self-check passes." M-3, M-4, M-5, M-28, M-29, M-35 have no row. Item 4's own stated remedy ("give them one row each and write 'same edit as [ID]'") was available and unused. | applied `s1c7c1f9` |
| N-28 | `rules-agents-updater/SKILL.md:117-119`, `agentic_engineering_framework.md:1740-1746`, `EPA:948-955` | **M-57's sweep stopped at the two files it had just edited.** Three formulations survive in five files. The worst: **`rules-agents-updater` — the shipped skill every project executes when adding a specialist** — whose three chain checks are entirely two-source and which points at "component-design sections 1-3", now saying something different. Second: a **fourth verbatim copy** of §2's canonical Pushy template in the framework doc. Third: EPA's **executable** Step 5.1 loop still greps only the two reviewers — the two prose checks were fixed and the one that runs was not. | applied s6f4bc1a |
| N-41 | `CLAUDE.md:108` | **M-56's own gate has no invoker, no report key and no receipt.** "ALWAYS run D16 over the unpushed commits and get a GREEN before asking to push" — `grep -c "push:" maintenance.md` → 0; the checklist's 12 keys contain none. Textbook component-design §9, introduced by the batch that catalogued §9's class. It went unhonoured on the very next push, **108 seconds after the commit that created it**. | applied `sd38fd04` |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| N-1 | `validation-orchestrator/SKILL.md:135` vs `:172-198` | M-18's fix replaced "no destination" with a destination that **does not exist in the citer's own report format**; it resolves only in `validator.md`, a different template. The half of M-18 that said "the skill's own report format has no coverage-gaps slot" is untouched, and the fix added a D8.7 RED. | applied `s1c7c1f9` |
| N-3 | `sa2f4890` receipt | `references: 4` where ≥35 distinct citation targets exist. Run 5 flagged the predecessor at ≥3×; the count went **down** (8→4) as the batch grew. Item 3's `references:` receipt still has no stated unit — the M-54 fix reached items 1 and 2 and not item 3. | applied `s1c7c1f9` |
| N-4 | `audit-2026-09-02.md:1056` | **M-55 and M-56 have no ledger row and no status column**, yet both count toward "57 of 57 disposed". Violates Phase 3 item 2. The `sa2f4890` header also says "Findings applied: 43" while counting M-44, whose status is `accepted-risk` — which item 2 forbids marking `applied`. | applied `s1c7c1f9` |
| N-7 | `sa2f4890` receipt, M-43 row | The claim `grep -rc "a hand-rolled date helper" → 0 after` was false when written: `framework-base-upgrade.md:110` predates the commit. The receipt also re-introduced the identifier into a tracked file. | applied `s1c7c1f9` |
| N-10 | `audit.md:167-191` | **`D16.2c` was inserted BEFORE `D16.2b`.** Disk order: D16.1, D16.2, D16.2c, D16.2b, D16.3. A pure insert with no renumber, while the receipt's ADJACENT cell claims "✓ D16.2b/D16.3 renumbered around the insert". | applied `s1c7c1f9` |
| N-11 | `audit.md:127-131` | **A forward reference whose receiver arrived and was not told.** Agent 1's list still says "That source is NOT yet a numbered check in D16 below" — written by `d4d3c33`, never swept when `a2f4890` installed D16.2c forty lines below. | applied `s1c7c1f9` |
| N-12 | `audit.md:247` | D16's REPORT FORMAT still reads `Blocklist derived: [N project names + M harvested identifiers]` — two sources, no slot for the third. DESCENDING gap on M-36. | applied s81cad36 |
| N-13 | `audit.md:172-181` | **D16.2c's harvest scope excludes where the hits actually are.** It reads `docs/modules/**`, `examples/**`, `.claude/**` — omitting `assets/docs/**` and root-level docs, which is exactly where N-16, N-17 and E-1 live and which D16.5 declares NOT exempt. | applied s81cad36 |
| N-17 | `assets/docs/framework-base-upgrade.md:158`, `:181` | Source-project table and column names, a source file name, and **source-project session numbers** (`a source-project session number`, `a source-project session number`, a source-project session number) in a tracked lineage doc. E-1's own record states that an identifier "of a different class … is NOT covered by this record and remains a finding" — this is that class. | applied s81cad36 |
| N-18 | `docs/agentic_engineering_framework.md:1289-1291` | A source-project rules-file name plus single-project domain vocabulary and a currency figure, in an example — which `CLAUDE.md` bans without qualification. | applied s81cad36 |
| N-20 | `sa2f4890` receipt | `isolation: 20 files scanned, 1 hit` — the batch touched **23** files across its commits, and item 3 mandates "every touched file"; a fresh D16 over that set finds a **second** hit (N-15). Understates scope and overstates cleanliness. | applied `s1c7c1f9` |
| N-21 | `EPA:279`/`:333` vs `:709` | **M-2 recreated between steps the contradiction M-1 removed between copies.** Step 4.1b says "THIS step writes the concerns in"; the declarations say "ALWAYS write in every concern Step 4.1b routed here". Two steps told to perform the same write. It also created **two new forward references** — Phase-2 steps consuming Phase-4 output. | applied `s1c7c1f9` |
| N-22 | `EPA:829-844` | **Step 4.6 still does not receive** what M-9 re-pointed at it. Its body mentions cross-cutting CODE concerns and the retroactive PRD; it never consumes Step 2.7.1's deferred matches and carries no `N/N deferred created` line to close 2.7.1's report. | applied `s1c7c1f9` |
| N-23 | `bootstrap.md:184` → Step 15 | **The `PRD pointer:` verdict M-6 created has no Step 15 slot.** `grep -c "PRD pointer"` over the report range → 0, while every other `ALWAYS REPORT` in the file has one. The same DESCENDING class M-8 was raised on, introduced by M-8's own commit. | applied `s1c7c1f9` |
| N-24 | `EPA:286`, `:340` | **M-10's fix is inverted.** The false justification was corrected in the two clauses where it was TRUE (Steps 2.4/2.5) and left standing in the two the finding named (Steps 2.2/2.3), where Step 5.1 never reads `project.md` or `pendencias.md`. | applied `s1c7c1f9` |
| N-25 | `sa2f4890` receipt | **M-12's denominator half was applied to the historical receipt and repeated in the one being authored.** The new receipt says `6 templates`; bootstrap performs **8**; `metrics_md` (9 lines) unmeasured for the third consecutive batch. | applied `s1c7c1f9` |
| N-27 | `EPA` final report | The three `cross-cutting received: R/R · A/A · T/T` verdicts M-2 mandated have **no slot** — `grep -c "received"` over the report range → 0, while bootstrap's twin carries "Delivered by receivers". | applied `s1c7c1f9` |
| N-48 | `EPA` | **EPA has no `done_tasks.md` create step.** Bootstrap creates it unconditionally and calls it load-bearing ("without it, the task lifecycle breaks silently"); EPA only checks for it in a Step 2.3 checklist item. An adapted project can leave `pendencias-updater` with no destination. | applied `s1c7c1f9` |
| N-49 | `maintenance.md` push bullet | **The push gate has no exemption for the one D16 hit it reliably produces.** Running N-41's new gate on this very batch turned D16 RED on a single hit: the audit report naming its own OPEN finding. That hit is the point of the report, and the identifier is already reachable through the instance the finding names — but the gate as written admits no way to say so, so an honest executor either blocks forever or reads RED as GREEN in silence. | LOW | Stated explicitly in the `push:` line contract. | applied `sd38fd04` |
| N-50 | `maintenance.md` post-change item 3 | **The fence check cannot return a true value on 4 of the 7 templates it sweeps.** Its command hard-codes a 4-backtick fence; `claude_md.md`, `pendencias_md.md`, `project_md.md` and `settings_json.md` carry 3-backtick fences, so it returns 0 lines on each and the item reports a pass over surfaces it never read — the exact failure the item's own closing sentence warns about, living in the item's own command. The 8 files bootstrap actually extracts by `sed` all carry 4 backticks, so **nothing ships broken**: the defect is confined to the verification. | LOW | Command made width-agnostic (reads each template's own opening fence), widened to `rules/`, receipt now states a denominator. 12 of 12 non-empty; was 3 of 7. | applied `seb402a9` |
| N-51 | `maintenance.md` post-change item 6 | **The largest verbatim-copied surface in the framework is machine-validated by nothing.** Item 6's raw-template check hard-codes 4 agent paths; `bootstrap.md:350` copies all 15 `docs/modules/skills/*/SKILL.md` with `cp -r`, so their frontmatter must be valid AT REST here, and no check reads them. A hard-coded list also goes stale the moment a component is added — the same defect as any enumerated set. Found while verifying N-9, which edited one of those very files' frontmatter. | MEDIUM | Replaced the list with a glob over both directories; it now also asserts `name:` matches the file/dir. 19 validated, was 4. Negation-proved by injecting the FRAMEWORK-AGENT-YAML-01 colon-space class → exit 1. | applied `s1c7c1f9` |
| N-52 | `audit.md` errata section | **The errata rule N-45 installed has no collision check, and the very next batch to use it collided twice.** Group 4 wrote a second `Errata 2` and a second `Errata 3` in the 2026-09-02 report. The series is numbered per FILE and the blocks do not appear in numeric order in it, so reading gives the wrong next number — which is exactly why the sibling "ID allocation" section mandates a grep. That section was written in the same commit and this one did not inherit it. | LOW | Renumbered to 4 and 5; added the pre-write grep with its expected result, proved both ways (free number → 0, taken number → 1). | applied `s1c7c1f9` |
| N-53 | `assets/docs/audit-2026-09-03.md:477` | **A back-sweep receipt spelled out the source-project identifiers it was hunting, in a persisted report, and the report itself had already established the opposite convention** — `L-19`'s row reads "identifier withheld here per the isolation rule — see the fix commit for the before/after". A grep pattern that enumerates the identifiers defeats D16.2c as surely as the leak it hunts: the rule is that the name must not appear in a framework-layer artifact, and a detection pattern is not exempt from it. D16.2b's scanner-regex exemption covers VALUE shapes (secrets, PII), not CODE identifiers. Found by running D16 over the pushed range while reconciling an unexpected change in `origin/main`, NOT by any sweep this run had planned. | MEDIUM | Identifier list withheld and replaced by a pointer, exactly as `L-19` does; the count and the re-run result stay, so the receipt remains auditable. Already pushed → **accepted-risk record below**, per the standing instruction from `E-1` and `M-44`. | applied `sbb8cbec` + accepted-risk |
| N-54 | `maintenance.md` accepted-risk check | **N-43's five-field check anchors on a bare `Accepted-risk`, so it counts a section that is not a record.** The 2026-09-03 report carries an `Accepted-risk OBSERVATIONS` heading — observations, not closures — which the check reads as a record with 0 of 5 fields and reports RED. A check that goes red on a healthy state is as useless as one that never goes red at all, and it was installed one batch ago. | LOW | Anchor tightened to the exact prefix `^#{2,3} Accepted-risk record `. Re-run: all three real records 5/5, the observations section correctly ignored. | applied `sbb8cbec` |
| N-29 | `agentic_engineering_framework.md:966-972` | M-14 declared THREE surfaces and the ALWAYS clause immediately below still binds **two** ("add it to BOTH lists"). Six category/vocabulary divergences remain in the third surface, including the missing `Coverage Gap Declaration` slot N-1 depends on. | applied `s1c7c1f9` |
| N-30 | `security_reviewer.md:282-283` | **M-15's fix duplicated its own clause** — "…and **the report schema at the top of this file** already declares the field as `[None \| list of gaps]` **in the report schema at the top of this file**." Inside a fenced report-instruction block. The ADJACENT direction is exactly what would have caught it. | applied `s1c7c1f9` |
| N-31 | `audit.md` D7.5 | **M-22's class survived inside M-22's fix.** The exemption list went 5 → 7; the actual protocol-spawned set is **9** — `code-reviewer` and `security-reviewer` are also protocol-spawned and still read as unexplained PARTIALs. | applied `s1c7c1f9` |
| N-35 | `sa2f4890` receipt, M-47 row | The cited grep returns **4→7** by lines, **5→10** by occurrences; the receipt says 5→11. Neither reading produces it. | applied `s1c7c1f9` |
| N-36 | `README.md:35` + `sa2f4890` receipt | **M-40 edited 1 of 2 sites.** `README.md:33-36` is byte-identical to the parent commit and still attributes the maximum to `production-financial`. The receipt claims "→ 2 sites \| ✓ both fixed in one pass". | applied `s1c7c1f9` |
| N-37 | `sa2f4890` receipt | `instruction style: 34 … 0 rewritten … all on their own line` — measured **50** added imperative lines, **19** removed lines carrying imperatives, and ≥6 buried mid-paragraph. Three false claims in one receipt line, and "0 rewritten" is the same half Run 5's M-54 pass already caught at 13. | applied `s1c7c1f9` |
| N-42 | `maintenance.md:168-169` vs its executions | **The ordinal sweep cannot measure the state it reports GREEN on.** Its executed path list omits `assets/docs/` — where one of M-24's own two instances lived — and its executed pattern under-matches its own mandate by roughly 60% (`item 5`, `item 6`, `item 1` are invisible to it). Both receipts report 7; the measured values are 9 at `d4d3c33` and 5 at HEAD. | applied `sd38fd04` |
| N-43 | `audit-2026-09-02.md:603-628` | **M-37's rule was never applied backward to the one artifact it governs.** E-1's record has four fields, not five: field 3 is labelled `History` rather than `Residue`, and "What WOULD re-open it" has no field — its content is buried in the Standing-instruction bullet. M-44's record asserts "same five fields as E-1" and the receipt asserts "✓ same five-field form as E-1's record"; **both are false.** No mechanical check exists for the five fields. | applied `sd38fd04` |
| N-44 | `04cb256` | **Splitting one audit batch across commits has no written home**, and every downstream contract broke on it: the `Application status` value (`APPLIED — 56 of 57 in sd4d3c33 + sa2f4890`) is outside its own enumeration on two counts; `04cb256` carries **no receipts section** while editing four files; and it carries **no `bump:` line at all** though it changed a shipped template — a direct violation of the Version-bumps mandate, in the commit that closed the run. | applied `sd38fd04` |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| N-9 | `codebase-audit/SKILL.md:16` | `derived_from: framework-base-upgrade.md §2.1` — "2.1" is a list item, not a heading, and the target lives in `assets/docs/`, never copied to projects. A shipped template's lineage pointer that dangles in every bootstrapped project. Pre-existing. | applied `s1c7c1f9` |
| N-14 | `CLAUDE.md:153` | **M-41's rewrap lengthened one of the three lines it was fixing** — the 131-char line is now 152, the M-46 citation appended without re-wrapping. File norm is 76-97. | applied `s1c7c1f9` |
| N-19 | `audit-2026-09-02.md` E-1 record | The record states 38 revisions; the measured figure is **39**. Accurate when written; drifted since. A record with a count needs a re-measure clause or the count should be replaced with "every revision of that file". | applied `s1c7c1f9` — no edit needed: N-43's record repair in `sd38fd04` had already replaced the drifting count with "every revision of that one file". Dispositioned HERE, where the re-verification found it, so `sd38fd04`'s receipt count stays 9 rows for 9 findings |
| N-26 | `audit.md:14-15` | **`git reflog` is executed by audits and is not in the authorized read-only git list.** M-56's evidence and this run's own mandated INPUTS both use it. The batch swept the list for `git push` and not for the command that produced the `git push` finding. | applied `sd38fd04` |
| N-32 | `agentic_engineering_framework.md:952`, `validation-orchestrator:186` | M-20's reachability clause reached **1 of 3** surfaces; the framework doc still states BASELINE-CREATED unqualified and the orchestrator's UI row has no BASELINE-CREATED token at all. | applied `s1c7c1f9` |
| N-33 | `audit.md` D6.1/D6.2 | D6.1/D6.2 remain the only extraction steps and still name only the two reviewers; no step tells the auditor to extract the third declarer's gap phrases. D6.7 patches over it by phrase; D6.3 does not. | applied `s1c7c1f9` |
| N-38 | `skill-gate/SKILL.md:163-164` | Nested backticks inside a backticked span — the inner `` `invocation: subagent` `` terminates the outer code span, so the mandated report string renders broken. | applied `s1c7c1f9` |
| N-39 | `audit-2026-09-02.md:572` | M-38's fix left L-19's row stale in the opposite direction: it still reads "the blocklist-derivation half stays `open`" with no note that M-36 closed it in the same commit. | applied `s1c7c1f9` |
| N-45 | `audit-2026-09-02.md` | **`Errata N` blocks are unowned** — 11 uses in the report file, 0 mentions in any command. Already the direct cause of two findings (M-45 mislabelling, M-51 uncorrected errata). | applied `sd38fd04` |
| N-46 | `audit.md:862` | **ID-series allocation and collision detection are unowned.** "A stable ID" is mandated; how the per-run letter is allocated and how a reuse is detected is not. Run 5 produced a real collision (`M-16` used twice), costing a re-file and an errata; nothing was added to prevent recurrence. | applied `sd38fd04` |
| N-47 | `audit-2026-09-02.md:1139` | The `*(superseded)*` status-preservation marker has no owner. | applied `sd38fd04` |

### Accepted-risk OBSERVATIONS — not findings, never re-opened

| Item | Re-measured | Note |
|---|---|---|
| **E-1** | Working tree **0** ✓; history **39 revisions** of one file (record says 38 → N-19); 0 message hits | Five fields **NOT** all present → N-43. Hits N-16/N-17 sit in the same file but are a **different class**, which the record's own clause excludes from coverage. |
| **M-44** | Working tree **0** ✓; **226 occurrences across exactly 4 files** ✓ — figure EXACT | Five fields present and correctly labelled ✓ |

---

## Summary matrix (Part 2 — the 17 dimensions)

| # | Dimension | Agent | Status | Finding |
|---|-----------|-------|--------|---------|
| A | Dual placement | 1 | **FAIL** | The only twin pair diverged this batch → N-6 |
| C | Instruction style | 5 | **FAIL** | No module skill at 0 imperatives (floor is 1) ✓ — but N-34, N-8, N-30, N-38 |
| E | Version consistency | 5, 1 | PASS | v2.13.0 on 3/3, 5 occurrences; 0 lineage rewritten; MINOR correct |
| D4 | Skills README vs disk | 1 | PASS | 15=15 names 1:1; breakdown re-labelled and 1:1 with bootstrap; 7/8 `invocation:` exact |
| D5 | Bootstrap file refs | 2 | PASS | 41/41 framework paths resolve; **8/8 fence extractions RUN, none empty**; 16/16 mappings |
| D6 | Gap vocabulary | 3 | **FAIL** | 10/10 phrases exact, **install-table diff EMPTY**, 3 declarers confirmed — but N-28, N-33 |
| D7 | Pushy Description | 3 | PASS | 30/30 parse; 20/20 specialists complete; 0 anti-patterns → N-31 |
| D8 | Orchestrator refs | 4 | **FAIL** | All names resolve; **D8.7 run: 0 false positives on ~20 sanctioned sites**, 1 genuine RED → N-1 |
| D9 | CLAUDE.md diagram | 1 | PASS | Every path and every count correct, re-verified after this batch's edits |
| D10 | Command → skill paths | 4 | **FAIL** | All citations resolve; **D10.6 parity 4/4 both directions** — but D10.4 `cmp` FAILS → N-6 |
| D11 | Bootstrap sequencing | 2 | **FAIL** | bootstrap clean (25 steps, monotonic, 0 forward refs); EPA → N-21, N-22 |
| D12 | Examples conventions | 5, 1 | PASS | **0 violations; 16 universal claims truth-tested across the full set, all TRUE** |
| D13 | PRD ↔ bootstrap/EPA | 2 | PASS | 19 of 19 template sections now covered by EPA Phase 3 |
| D14 | Bootstrap output | 2 | **FAIL** | 15/15 pointers traced (the `PRD:` line closed) — but N-23, N-27, N-48 |
| D15 | Doc factual accuracy | 5 | **FAIL** | **Every count claim TRUE; all five defect figures recounted exact; banned-"inline" survivors 0** — but 4 new inaccuracies |
| D16 | Project-info isolation | 1 | **FAIL** | Source 1/2: **0 hits** across 107 files, 10 memory files, 119 commits. **Source 3, first full run: 340 candidates × 6,687 project files → 124 intersections, 119 legitimate, 5 hand-rolled** → N-15, N-16, N-17, N-18 |
| D17 | Process coverage (meta) | 6 | **FINDINGS** | `git push` newly homed ✓ — but N-41, N-44, N-26, N-45, N-46, N-47 |

---

## Recommended fixes

### 0. The three one-line gates — write these FIRST, before any content fix
Every HIGH in this run would have gone red against one of them, before commit:
- **`cmp` the twin pair** → catches N-6.
- **`git show --name-only <commit>` vs the files each receipt row names** → catches N-34 (a row naming a file absent from the diff) and N-36.
- **`grep -c "applied sHASH"` vs the receipt's row count** → catches N-5.

None requires judgement. All three are one line. **Add them to item 4 as mandatory pre-commit
checks, and make the per-fix receipt name the FILE each row edited.**

### 1. The two that make the back-sweep real
`N-40` — the Re-run column must cite a **residue** grep whose GREEN value is 0, never a presence
grep whose GREEN value is N. One sentence in item 5.
`N-2`, `N-28` — re-run the two sweeps against the SHAPE, not the paths edited.

### 2. Isolation — the first three-source run's harvest
`N-15` (template, HIGH), `N-16`, `N-17`, `N-18`, plus `N-13` (D16.2c's scope excludes where the
hits are) and `N-12` (its report format has no third slot).

### 3. The process gaps
`N-41` (M-56's gate has no invoker), `N-44` (the split has no home; `04cb256` has no receipts and
no bump), `N-43` (E-1's record fails M-37's own rule), `N-42` (the ordinal sweep's scope and
pattern), `N-45`, `N-46`, `N-26`, `N-47`.

### 4. The rest
`N-1`, `N-3`, `N-4`, `N-7`, `N-9`, `N-10`, `N-11`, `N-14`, `N-19`, `N-21` … `N-39`.

---

## Meta-observation

**The result this run establishes is not a defect count — it is a partition.** Across three
batches of auditable receipts, the split is now clean enough to state as a rule:

> **Every receipt claim whose value a stated command reproduces was TRUE. Every claim whose value
> the author supplied from reading was FALSE or overstated.**

TRUE this run: version parity (5/3), row counts where they were computed, liveness, key presence,
fence line values, the M-44 record's 226/4, and the one back-sweep row whose grep was a residue
grep. FALSE this run: `references: 4` (≥35), `instruction style: 34 / 0 rewritten` (50 / 19),
`isolation: 20 files, 1 hit` (23 / ≥2), `6/6 blocks` (3/6), `0 after` (3), `5→11` (4→7), `both
fixed` (1 of 2), and two of three `0 remaining`.

**Batch size was never the variable, and the split proved it.** `d4d3c33` was 8 findings,
deliberately isolated, self-verified — and it produced ≥3 defects (~37%, indistinguishable from
the large-batch rate), including **pasting a paragraph twice in the commit whose purpose was to
un-paste a paragraph pasted twice**. The split also *created* two defects that could not exist
without it: a status value outside its own enumeration, and a commit with neither receipts nor a
bump line.

**M-39 is the first control to attack the real variable, and its first execution shows exactly how
far it got.** Row 1 did genuine work — the grep found a survivor per twin that reading had approved,
the re-run confirmed 0, and this audit reproduced it. Rows 2 and 3 report a `0` their greps cannot
return. The difference between the working row and the broken ones is one property: **row 1's grep
counts violations; rows 2 and 3 count presence.** That is the whole fix, and it is one sentence.

**What is still unowned, stated so the next run can check it:** nothing verifies that a receipt row
corresponds to a change in the commit. `N-34` is the proof — a fix declared, receipted, counted in
a passing self-check, and never made. Until a row is tied to a file in the diff, the receipt
measures its own existence.

**Suggestion:** Run `/maintenance` with **Group 0 alone** — the three one-line gates — commit them,
and only then take the content fixes. This time the recommendation is not about batch size: it is
that the gates must exist before the batch they would have caught.

---

**Report status:** `COMPLETE` — all 6 agents returned; 17 dimensions plus a Part 1 verification pass
over 51 of 57 disposed findings (6 uncovered, named) and a receipt-verification pass over both
persisted receipt sections.
**Application status:** `APPLIED — 54 of 54 (2 in `s0052074`, 3 in `s6f4bc1a`, 6 in `s81cad36`, 9 in `sd38fd04`, 1 in `seb402a9`, 31 in `s1c7c1f9`, 2 in `sbb8cbec`)` — the seven counts sum to 54. `N-53` is applied in the working tree and closed as an accepted risk for the pushed history. **An earlier form of this line named 4 of the 7 hashes, attributing 20 of 54 dispositions, and nothing failed** — which is why `/audit` 2026-09-03 P-33 now requires the parenthesised counts to SUM to the total. (52, not 48: `N-49` and `N-50` were filed by Group 3 and `N-51` and `N-52` by Group 4 — each of the four found by RUNNING a control this run installs or discharges, never by re-reading one. Every ID was collision-checked before it was written, per `N-46`.) (48, not 47: one finding had been filed off-scheme as `N-28b`; renamed to `N-48` here — the ID-allocation class N-46 names, caught by running Gate 3.) **Group 0 (the three
pre-commit gates) is INSTALLED**, and N-6 + N-8 closed with it: N-6 because **Gate 1 blocked this
very commit** and the rule forbids committing on a RED, N-8 because it sat in the block directly
above the edit (the ADJACENT direction). ***(superseded)* `37 remain `open`` — 0 findings carry `open`; the run closed complete** — Groups 1-4, deferred by owner
decision, NOT dropped. The 2 accepted-risk observations are unchanged.


---

## Post-change checklist receipts (`s0052074`)

*Group 0 only. Every `ALWAYS REPORT` key appears below exactly once, anchored at line start.*

**inventory sweep:** N/A — no artifact added, removed, renamed or split. Three checklist GATES were
added to an existing item; the 7 inventory surfaces (`CLAUDE.md`, `README.md`,
`docs/modules/README.md`, `docs/agentic_engineering_framework.md`, `examples/README.md`, this
command file, the sibling command twin) carry no count of gates. **ORDINAL SWEEP: N/A** — no
numbered item was inserted or reordered; the gates sit inside item 4.

**instruction style:** 11 new behavioral instructions checked, 0 rewritten. **Unit:** a line
carrying a CAPS imperative a session must execute. All 11 on dedicated lines; 4 carry an explicit
expected result and 1 an explicit report format.

**references:** 2 cited sections resolved — `## Post-change checklist receipts` (3 sites, all `##`)
and CLAUDE.md's dual-placement rule. *Observation for the next run: "dual-placement rule" is a
conceptual nickname used at 3 sites in this file for a rule `CLAUDE.md` states in prose without
that name — it resolves conceptually, not by grep. Two of the three citations pre-date this
session; left unchanged rather than expanding Group 0's scope.*

**fences:** 5 rules templates + the js template extract non-empty — 273, 149, 53, 23, 216 markdown
lines and 185 js, measured with `sed '1d;$d'`. **Denominator stated (N-25's class): bootstrap
performs 8 extractions; `metrics_md` (9) and one 23-line template are outside this session's
edits and were not re-measured.**

**isolation:** 5 files scanned, 0 hits.

**class sweep:** 3 fixes × 4 directions, 0 extra instances found.

| Fix | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra |
|-----|--------------|---------|----------|----------|------------|-------|
| Group 0 (gates) | `.claude/commands/maintenance.md` | ✓ `grep -n "Mechanical self-check" maintenance.md` — compared every existing check for the presence-vs-residue defect N-40 names; none of the 3 new gates uses a presence grep | ✓ `audit.md` has no per-fix receipt, so no twin obligation — confirmed by `grep -c "per-fix" .claude/commands/audit.md` → 0 | ✓ read item 4 end to end; the `File touched` column is the ADJACENT change the gates require | ✓ the receipt table below carries the new column — this row is the proof | none |
| N-6 | `.claude/skills/cross-cutting-analysis/SKILL.md` | ✓ `ls .claude/skills/` → 1 entry, so this is the repo's only twin pair | ✓ `diff -r` over the pair, both directions | ✓ `cmp` exits 0 after | n/a | none |
| N-8 | `.claude/commands/maintenance.md` | ✓ `grep -c "Evidence this is not hypothetical"` → 2 before, 1 after; same grep repo-wide finds no other duplicated paragraph | n/a — no twin | ✓ read `:100-140` around both copies; the surviving one is the later, complete text | n/a | none |

Row count: **3**. Findings disposed this session: **3** (Group 0, N-6, N-8). Equal.

**back-sweep:** 1 rule promoted — *"a mandated check states an expected result a stated command
reproduces"*, embodied as the three gates.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| gates state a command + an expected result | every `Mechanical self-check` in this file | `grep -c "Mechanical self-check (expected result stated)"` → 4 pre-existing, all already stating a command and an expectation | 0 violations | 0 | 0 remaining |

*(N-40 — the two back-sweep rows that used presence greps rather than residue greps — is Group 1
and stays open. The three gates written here deliberately avoid that shape: each returns a value
that is 0 or empty ONLY when the state is correct.)*

**liveness:** 26 components OK — full YAML parse over every raw and fenced frontmatter in
`docs/modules/agents/`, `docs/modules/skills/` and `.claude/skills/`. The bundled guard reached 1
component in `modo estrutural — js-yaml ausente`; the full parse ran alongside it. Raw agent
templates: OK.

**negation proof:** **Gate 1 → RED, on its first execution, against this commit.** Not a synthetic
negation — the twin pair had genuinely diverged in `a2f4890`, `diff -r` printed the three added
lines, and the rule's own "a RED gate BLOCKS the commit" forced N-6 to be fixed here rather than
deferred to Group 2. Re-run after the fix: PASS, `cmp` exit 0. Gates 2 and 3 ran GREEN and are
trivially red-able (a row naming an untouched file; unequal counts).

**version:** v2.14.0 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.

**classification:** 2 add, 0 substitute, 1 delete, 0 relocate. The DELETE is N-8's duplicated
paragraph — justified under §5 as provably redundant (byte-identical to its neighbour). No
RELOCATE, so §5's grep is N/A.

**new component:** none this session.

**verification audit:** proposed — trigger (d), and trigger (b) since the bump is MINOR. One
verification run satisfies both.


---

## Post-change checklist receipts (`s6f4bc1a`)

*Group 1 only — 3 findings. Every `ALWAYS REPORT` key appears below exactly once, anchored at line
start.*

**inventory sweep:** N/A — no artifact added, removed, renamed or split. Surfaces confirmed
unaffected by grep, not by assumption: `CLAUDE.md`, `README.md`, `docs/modules/README.md`,
`docs/agentic_engineering_framework.md`, `examples/README.md`, this command file, the sibling
command twin — none carries a count of agent-block list entries or of back-sweep columns.
**ORDINAL SWEEP: RUN** — Agent 1's FILES TO READ gained an entry, renumbering items 5-10.
`grep -rn "item [0-9]\|D16\.[0-9]" .claude/commands/audit.md` → every citation of D16.1/D16.2c/D16.3
still names its own sub-check; no ordinal cites Agent 1's list positions.

**instruction style:** 6 new behavioral instructions checked, 0 rewritten. **Unit:** a line
carrying a CAPS imperative a session must execute. All 6 on dedicated lines; 2 carry an explicit
expected result.

**references:** 4 cited sections resolved — `D16.1`, `D15.5`, `D17.3` (all in `audit.md`, each
cited by the list entry added for it) and `Coverage Gap Declaration` (`validator.md`).

**fences:** 5 rules templates extract non-empty — 273, 149, 53, 23, 216 markdown lines, measured
with `sed '1d;$d'`. **Denominator: bootstrap performs 8 extractions; the 3 not listed
(`check_agent_frontmatter` js, `metrics_md`, `framework_metrics_md`) are outside this session's
edits and were not re-measured** (N-25's class, stated rather than silently omitted).

**isolation:** 6 files scanned, 0 hits.

**class sweep:** 3 fixes × 4 directions, 4 extra instances found and fixed.

| Fix | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra |
|-----|--------------|---------|----------|----------|------------|-------|
| N-40 | `.claude/commands/maintenance.md` | ✓ `grep -n "Mechanical self-check" maintenance.md` — checked all 5 for the presence-vs-residue defect; the 3 gates and item 7's version grep are residue-shaped, item 8's RELOCATE grep is a definition count with an explicit GREEN of "one per twin-set" | n/a — `audit.md` has no back-sweep item (`grep -c "back-sweep" .claude/commands/audit.md` → 0) | ✓ read item 5 end to end; the denominator and re-run sentences sit directly below the new rule | ✓ the table's `Re-run` column already expects `0 remaining`, which only a residue grep can produce | none |
| N-2 | `.claude/commands/audit.md` | ✓ per-block test measuring SURFACES not filenames — the previous test's blind spot | ✓ all 6 blocks tested, not the 3 the finding named | ✓ Agent 1's items 5-10 renumbered after the insert; ordinal sweep run | ✓ the guard sentence already names `FILES TO READ` / `INPUTS` both | none |
| N-28 | `docs/agentic_engineering_framework.md`, `docs/modules/skills/rules-agents-updater/SKILL.md`, `.claude/commands/bootstrap.md`, `.claude/commands/existing_project_adaptation.md` | ✓ residue grep `"when \[reviewer\] declares\|echo the reviewer's gap\|to the appropriate reviewer\|at least one reviewer"` run repo-wide, not on the paths last edited | ✓ both command twins swept together | ✓ EPA's executable loop AND its comment; bootstrap's three adjacent prose lines | ✓ the EPA shell loop is the DESCENDING surface — the two prose checks were fixed a batch earlier and the one that runs was not | 4 files, 6 violations |

Row count: **3**. Findings disposed this session: **3**. Equal.

**back-sweep:** 1 rule promoted — *"a back-sweep grep counts VIOLATIONS, never presence"* (N-40) —
applied backward in the same session to the one artifact it retroactively governs.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| back-sweep greps count violations | reviewer-only formulations across the framework layer (the N-28 sweep, written under the new rule) | `grep -rc "when \[reviewer\] declares\|echo the reviewer's gap\|to the appropriate reviewer\|at least one reviewer" --include=*.md docs/ .claude/` | **6 across 4 files** (measured at HEAD via `git stash`, per N-40's "run it against the UNFIXED state first") | 6 | **0 remaining** |

**liveness:** 26 components OK — full YAML parse over every raw and fenced frontmatter in
`docs/modules/agents/`, `docs/modules/skills/` and `.claude/skills/`. The bundled guard reached 1
component in `modo estrutural — js-yaml ausente`; the full parse ran alongside it.

**negation proof:** N-40's rule proved in BOTH directions on its first use — the N-28 residue grep
returned **6 across 4 files** against the unfixed state (`git stash`) and **0** after. That is
precisely what the two broken rows of the previous execution could not do, and it is why the rule
now mandates the pre-fix run.

**version:** v2.14.1 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.

**classification:** 5 add, 4 substitute, 0 delete, 0 relocate. The SUBSTITUTEs replaced text that
was two-source-scoped where three sources exist (N-28 ×4). No RELOCATE, so §5's grep is N/A.

**new component:** none this session.

**verification audit:** proposed — trigger (d). The bump is PATCH, so trigger (b) does not fire.

**gates:** twin parity **PASS** (`diff -r` no output, re-run after the version bump) |
receipt-rows-vs-diff **3/3** (every `File touched` cell appears in `git show --name-only s6f4bc1a`) |
row-count **3 rows vs 3 disposed**.


---

## Post-change checklist receipts (`s81cad36`)

*Group 2 — isolation, 6 findings. Every `ALWAYS REPORT` key appears below exactly once, anchored at
line start.*

**inventory sweep:** N/A — no artifact added, removed, renamed or split. The 7 surfaces
(`CLAUDE.md`, `README.md`, `docs/modules/README.md`, `docs/agentic_engineering_framework.md`,
`examples/README.md`, this command file, the sibling command twin) carry no count affected by an
isolation rewrite; confirmed by grep, not assumed. **ORDINAL SWEEP: N/A** — no numbered item
inserted or reordered.

**instruction style:** 4 new behavioral instructions checked, 0 rewritten. **Unit:** a line
carrying a CAPS imperative a session must execute. All 4 in D16.2c/D16's report format, each on a
dedicated line; 1 states an explicit failure condition ("a two-number report means the third
source was not run").

**references:** 2 cited sections resolved — `D16.5` (cited by D16.2c's new scope clause) and
`CLAUDE.md`'s isolation rule (which bans source-project session numbers by name, the authority for
the 59-occurrence normalisation).

**fences:** 5 rules templates extract non-empty — 273, 149, 53, 23, 216 markdown lines, `sed
'1d;$d'`. **Denominator: bootstrap performs 8; the 3 not listed are outside this session's edits
and were not re-measured.**

**isolation:** 7 files scanned, **0 hits** — and this is the first session in which the D16 residue
greps return 0 for BOTH classes across the whole framework layer: vocabulary 0, session numbers 0.

**class sweep:** 6 fixes × 4 directions, 2 extra instance-sets found and fixed.

| Fix | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra |
|-----|--------------|---------|----------|----------|------------|-------|
| N-15 | `docs/modules/rules/session_rules.md` | ✓ residue grep for hand-rolled identifiers across `docs/ .claude/ assets/docs/` | ✓ the sibling commit-message example one line below was already generic | ✓ read `:158-165`; the two examples now describe behaviour, not code | n/a | none |
| N-16 | `assets/docs/framework-base-upgrade.md` | ✓ same residue grep — found the identifier the previous receipt claimed was at 0 | ✓ checked both lineage docs, not the one named | ✓ the surrounding phase narrative | n/a | none |
| N-17 | `assets/docs/framework-base-upgrade.md` | ✓ `grep -oE "S[0-9]{1,3}"` — **48 occurrences over 34 lines, where the finding named 2** | ✓ **the sibling `framework-base-deepdive.md` carried 11 more** | ✓ the "Fase 0..5" labels already carried the ordering, so the numbers were redundant | n/a | **59 total, both docs** |
| N-18 | `docs/agentic_engineering_framework.md` | ✓ `grep -c earmark` → 5 pre-fix, not the 3 the finding cited | n/a — single doc | ✓ the WEAK/STRONG example pair below the source line | n/a | **2 extra `earmark`** |
| N-13 | `.claude/commands/audit.md` | ✓ compared D16.2c's scope against D16.5's own non-exemption clause | n/a — one check | ✓ D16.2b/D16.3 read around the edit | ✓ N-12 is the descending half | N-12 |
| N-12 | `.claude/commands/audit.md` | ✓ `grep -n "Blocklist derived"` → 1 site | n/a | ✓ D16's REPORT FORMAT block | ✓ this IS the descending fix for N-13 | none |

Row count: **6**. Findings disposed this session: **6**. Equal.

**back-sweep:** 2 rules promoted, both applied backward with residue greps run against the UNFIXED
state first, per N-40.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| no source-project CODE identifiers or single-project vocabulary in any framework-layer file | hand-rolled names and domain vocabulary across `docs/`, `.claude/`, `assets/docs/` | `grep -rcE "<the 9-identifier list — WITHHELD here per the isolation rule; recoverable from the removing commit, exactly as `L-19`'s row does>" --include=*.md docs/ .claude/ assets/docs/` | **13** across 4 files (measured via `git stash`) | 13 | **0 remaining** |
| no source-project SESSION NUMBERS anywhere in the framework layer | `S<N>` tokens in lineage and concept docs | `grep -roE "S[0-9]{1,3}" --include=*.md docs/ assets/docs/` | **59** — 48 in one lineage doc, 11 in its sibling (the finding named 2) | 59 | **0 remaining** |

**liveness:** skipped — no frontmatter touched (none of the 7 edited files is a component).

**negation proof:** both back-sweep greps proved in BOTH directions on the unfixed state
(13 and 59 respectively, via `git stash`) and after (0 and 0). The 59 is the number that matters:
the finding named 2 sites, reading would have closed it at 2, and only running the grep against
the shape produced the real denominator — twice, since the second measurement found the sibling
document the first had not covered.

**version:** v2.14.2 applied to 3/3 canonical surfaces (5 occurrences); 0 lineage fields rewritten.

**classification:** 2 add, 4 substitute, 0 delete, 0 relocate. The SUBSTITUTEs replaced text
carrying source-project identifiers (N-15, N-16, N-17, N-18) — justified under §5 as content the
isolation rule bans outright, with no information lost: the phase labels already carried the
ordering the session numbers encoded. No RELOCATE, so §5's grep is N/A.

**new component:** none this session.

**verification audit:** proposed — trigger (d). The bump is PATCH, so trigger (b) does not fire.

**gates:** twin parity **PASS** | receipt-rows-vs-diff **6/6** (every `File touched` cell appears in
`git show --name-only s81cad36`) | row-count **6 rows vs 6 disposed**.

> **Errata 1 (added by the Run 6 Group 3 application, finding N-43).** The commit subject of
> `d38fd04` reads `10 findings`; **9 distinct IDs** were applied in it. The subject counted N-43
> twice — once for the accepted-risk record it repaired and once for the mechanical check it
> installed — which are one finding. The receipts below, the ledger and the row-count gate all
> use the distinct-ID count of 9 and are correct; only the subject line overstates. Recorded rather
> than rewritten, per the same reasoning as the E-1 and M-44 accepted-risk records: an unpushed
> commit message is cheap to amend, but amending it would rewrite a commit an already-committed
> write-back depends on, and the erratum is the cheaper true record.

## Post-change checklist receipts (sd38fd04)

Run 6 Group 3 — process gaps. 9 findings: N-41, N-49, N-42, N-43, N-44, N-45, N-46, N-47, N-26.

**inventory sweep:** 7 surfaces checked — `CLAUDE.md`, `README.md`, `docs/modules/README.md`,
`docs/agentic_engineering_framework.md`, `examples/README.md`, `maintenance.md` (its own
Authorized-operations list and the header above the checklist), and the sibling command twin
(`bootstrap.md` ↔ `existing_project_adaptation.md`). 1 stale claim fixed: the self-check key list
went from 12 to 14 keys (`gates`, `push`). No artifact added, removed or renamed — two sections
were added INSIDE `audit.md`, which is not an inventory-bearing artifact.
ORDINAL SWEEP — ran, in the widened form N-42 installs (`item N`, `item N's`, `Phase N item M`,
across `.claude/`, `docs/`, `CLAUDE.md` AND `assets/docs/`): 193 hits repo-wide, 11 of the form
`Phase N item M` and 3 of the form `Audit intake step N`. The only list this batch renumbered is
Audit intake, where item 6 was INSERTED (old 6 → 7). All 3 live `Audit intake step N` citations
name **step 4** (the write-back), which did not move; the one citation of the inserted region cites
by NAME (`Audit intake's write-back step`) and is immune. **Every hit still names the content it
meant.**

**instruction style:** 14 new behavioral instructions checked, 0 rewritten. Unit counted: *a line
carrying a CAPS imperative (ALWAYS/MUST/NEVER) that a session must execute every time.* They are
the `push:` report mandate, its NEVER-emit-nothing clause, the D16-report exemption, the two
ordinal-sweep widenings, the per-commit `bump:` mandate, the per-commit receipts mandate, the
`Application status` multi-hash form, the five-field grep, the errata form, the NEVER-label-with-a-
future-run clause, the superseded marker, the ID pre-write collision grep, and the sequential-no-
suffix rule.

**references:** 4 cited sections resolved — `Errata and superseded status` (audit.md, 1),
`ID allocation` (audit.md, 1), `Version bumps` (maintenance.md, 2), `Post-change checklist`
(maintenance.md, 4). Every section this batch's new text cites exists under the name it cites.

**fences:** 3 of 7 templates extracted non-empty on the command as written — **RED**, and the
cause was the command, not the templates (→ N-50, filed and fixed in the write-back commit).
Re-run with the width-agnostic form over templates AND rules: **12 of 12 non-empty.**

**isolation:** 6 files scanned (the commit's full name-only list), 0 hits.

**class sweep:** 9 fixes × 4 directions, 2 extra instances found and fixed.

| Fix ID | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|---|---|---|---|---|---|---|
| N-41 | `.claude/commands/maintenance.md` | ✓ `grep -n "ALWAYS REPORT" maintenance.md` — 14 mandates, each checked for a key | ✓ no twin: the push bullet exists only in `maintenance.md` (`grep -l "git push" .claude/commands/*.md`) | ✓ read the Authorized-operations bullets above and below | ✓ the key list gained `push` and `gates` | N-49 — running the new gate produced a D16 RED the gate could not resolve |
| N-49 | `.claude/commands/maintenance.md` | ✓ same grep as N-41 | n/a — same bullet as N-41 | ✓ same edit as N-41 | ✓ covered by N-41's key-list row | same edit as N-41 |
| N-42 | `.claude/commands/maintenance.md` | ✓ `grep -rn "step [0-9]\|item [0-9]" --include=*.md .claude/ docs/ assets/docs/` — 193 hits triaged | ✓ no sibling ordinal sweep exists (`grep -c "ORDINAL SWEEP" .claude/commands/*.md` → 1) | ✓ read item 1's other bullets — the prose-count clause directly above | ✓ the receipt form now names the three citation shapes | none |
| N-43 | `.claude/commands/maintenance.md`, `assets/docs/audit-2026-09-02.md` | ✓ `grep -cE "^- \*\*(Decision\|Working tree\|Residue\|Standing instruction\|What WOULD re-open it)"` per record | ✓ BOTH accepted-risk records swept, not only the one the finding named | ✓ the `accepted-risk` status clause directly above | ✓ the check is stated with its expected result (`exactly 5`) | **M-44's record was also RED (4/5)** — the finding named only E-1 |
| N-44 | `.claude/commands/maintenance.md` | ✓ `grep -n "Application status" audit.md maintenance.md` — both enumerations checked | ✓ `/audit` Phase 3's status vocabulary re-read against the new multi-hash form | ✓ read Audit-intake items 5 and 7 around the insertion | ✓ ordinal sweep run for the insert (see inventory sweep) | none |
| N-45 | `.claude/commands/audit.md` | ✓ `grep -c "Errata" assets/docs/audit-*.md` — 11 uses, all now covered by a written form | ✓ checked `maintenance.md` for a competing errata convention → none | ✓ read the `## Meta-observation` block the new sections precede | ✓ the form names the authoring run, which the report header supplies | none |
| N-46 | `.claude/commands/audit.md` | ✓ `grep -oE "^\| N-[0-9]+ \|"` → highest 48, no gaps, no suffixes | ✓ same series check run on the 2026-09-02 report | ✓ same insertion block as N-45/N-47 | ✓ the pre-write collision grep was RUN before filing N-49 and N-50 (both returned 0) | none |
| N-47 | `.claude/commands/audit.md` | ✓ `grep -c "superseded" assets/docs/audit-*.md` | ✓ same insertion block as N-45 | ✓ same block | ✓ the marker's shape is stated verbatim | none |
| N-26 | `.claude/commands/audit.md` | ✓ `grep -n "git rev-list\|git reflog\|git check-ignore" .claude/commands/*.md` | ✓ `maintenance.md`'s own git authorization re-read — `reflog` not needed there (it audits nothing) | ✓ read the authorized-operations bullets around it | ✓ no report field depends on `reflog` | none |

Row count self-check: **9 rows vs 9 findings applied in `sd38fd04`** — equal.

**back-sweep:** 2 rules, 2 artifacts fixed of 2 found, re-run clean.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| N-41 — every `ALWAYS REPORT` mandate in the checklist must have a key in the self-check list | A mandated report key absent from the loop the self-check greps | `LIST=$(sed -n '/^for k in "inventory sweep"/,/^done$/p' maintenance.md \| grep -oE '"[a-z][a-z -]*"' \| tr -d '"')`, then each mandated key tested with `grep -qxF` | 2 (`gates`, `push`) | 2 | **0 remaining** — 14 keys, every mandated key present |
| N-43 — an accepted-risk record must carry all five fields | A record in any audit report with ≠ 5 anchored field bullets | `python -c "… re.split(r'^#{2,3} ') … re.findall(r'^- \*\*(Decision\|Working tree\|Residue\|Standing instruction\|What WOULD re-open it)')"` per record | 2 of 2 records RED | 2 | **0 remaining** — `[5, 5]` |

Both greps count VIOLATIONS, not presence (N-40), and both were run against the UNFIXED state
first. **Two measurement bugs were caught this way and are worth recording**, because in each case
the first result was a FALSE GREEN that a reading would have accepted:
- the first key-list grep used the class `[a-z ]+`, which does not cover the hyphen in
  `"back-sweep"`; the regex then matched the quote pair `" "` offset by one and swallowed
  `"version"`, reporting it missing when it was present all along.
- the first five-field script split the report on `^## ` in a file whose records are `###`, so it
  examined **zero records** and reported `0 non-conforming` — a pass computed over an empty set.

**liveness:** guard run over `.claude/` — 1 component OK (frontmatter parseable, `name:` matches the
file). No component frontmatter was edited this session; the guard was run anyway because
`claude_md.md` was touched.

**negation proof:** RUN, on the five-field check, against `d38fd04~1`: **`[2, 4]` fields, 2 RED**
— the check goes red on the unfixed state, so it is a control and not decoration. After the fix,
`[5, 5]`, 0 RED. Note that E-1 carried **2** conforming bullets, not the 4 a reading had assumed;
the number came from running the command.

**version:** `v2.14.2 → v2.15.0` (MINOR). Canonical set updated: `README.md` (title + structure
diagram, 2 hits), `docs/modules/templates/claude_md.md` (1),
`.claude/commands/existing_project_adaptation.md` (2). `created: framework-vX.Y.Z` lineage fields
rewritten: **0** (verified by `git diff --unified=0 | grep -c '^[-+]created:'` → 0). MINOR is
correct: two new mandated report keys, a new Audit-intake item, two new `audit.md` sections and a
new self-check are contract additions, not corrections.

**classification:** all 9 are ADD (new sections, new bullets, new mandates) except N-43's record
repair, which is SUBSTITUTE — the `History:` label became `Residue:` and `Decision` was promoted
from a bold line to a bullet. No information was lost: both changes make an existing statement
findable by the grep that governs it.

**new component:** N/A — no skill, agent or rule created. The two `audit.md` sections are sections
of an existing command, not components.

**gates:** twin parity PASS (`diff -r .claude/skills/cross-cutting-analysis
docs/modules/skills/cross-cutting-analysis` → no output) | receipt-rows-vs-diff 9/9 (every
`File touched` cell appears in `git show --name-only d38fd04`) | row-count 9 vs 9.

**push:** not requested for this batch. Per M-56 the push waits for the owner's explicit word AND
a GREEN D16 over the unpushed set.


### Accepted-risk record — N-53 (standing instruction, 2026-09-03)

- **Decision:** apply the standing instruction the owner set for `E-1` and reaffirmed for `M-44` —
  fix forward in the working tree, record the residue, **do NOT rewrite history**. No new owner
  decision was sought because the class, the reasoning and the disposition are identical, and the
  standing instruction exists precisely so that each recurrence does not become a new question.
- **Working tree:** clean. The literal identifier list at `:477` is replaced by a withheld-reference
  pointer, the same form `L-19`'s row uses. The receipt keeps its count (13), its denominator and
  its `0 remaining` re-run, so it stays auditable without naming anything.
- **Residue:** one source-project function name and four schema/file identifiers remain in the
  revisions of this one report file that are reachable from `origin/main`. They entered in
  `e35ea68` and were pushed before this run's D16 covered the range.
- **Standing instruction:** a back-sweep grep that hunts identifiers **NEVER spells them out in the
  persisted receipt** — cite the removing commit instead. This is now the rule for every future
  receipt of this shape, and `N-53`'s fix is its first application.
- **What WOULD re-open it:** a NEW literal occurrence in the working tree, or any identifier of a
  different class — project name, client, person, deployment domain, infrastructure id, secret value
  or PII. Those are not covered by this record and remain findings.

> **Errata 2 (added by the Run 7 application, findings P-22, P-4).** Three figures in the
> receipts below are wrong, independently re-measured, and they share the cause this run
> identified: **the mandate named a unit but neither a command nor a denominator.**
> - `instruction style: 11 new behavioral instructions checked, 0 rewritten` — the diff added
>   **29** lines carrying ALWAYS/MUST/NEVER and removed **12** that carried them, and **8**
>   of the added ones open a new sentence mid-line, which is the §6 anti-pattern. Three false
>   claims in one line, and the same half `N-37` had already caught one batch earlier.
> - `references: 9 of 9 cited sections resolved` — measured **11** distinct file-path
>   citations, and a broader citation unit gives more. Off by 2 against a predecessor off by
>   ~31: this key had received a command AND a denominator in the same batch, and the
>   improvement is the evidence behind `P-35`.
> - `isolation: 13 files scanned, 0 hits` — the commit touched **15** files, and item 3
>   mandates every touched file. The two omitted were the report files, one of which held a
>   live D16 hit at that moment — later filed as `N-53`. **The understated denominator is the
>   mechanism, not a bookkeeping nit.**
>
> Also recorded: `eb402a9`, `bb8cbec` and the two hash-substitution commits carry no receipts
> section, violating Audit-intake item 6 four times inside the batch that installed it
> (`P-4`). Run 7 gave the substitution commits a written exemption and a report key; the
> other two remain violations of record.

## Post-change checklist receipts (s1c7c1f9)

Run 6 Group 4 — the closing batch. 31 findings: the 29 that were open, plus N-51 and N-52, both
found by RUNNING a control this session installs or discharges.

**inventory sweep:** 7 surfaces checked — `CLAUDE.md`, `README.md`, `docs/modules/README.md`,
`docs/agentic_engineering_framework.md`, `examples/README.md`, `maintenance.md` (its own
Authorized-operations list and the header above the checklist), and the sibling command twin. 1
stale claim fixed: `README.md:33-36` mis-attributed the full tier set to `production-financial`
(N-36) — the second of the two sites M-40 was supposed to fix. No artifact added, removed or
renamed: every change is a section, bullet or slot inside an existing artifact.
ORDINAL SWEEP — **n/a**: no numbered item was inserted or reordered this session. `D16.2c` was
MOVED to sit after `D16.2b` (N-10), which is a reorder of two *lettered* sub-steps whose labels
travel with them, so no citation's target changed; verified by
`grep -rn "D16\.2[bc]" --include=*.md .` → every citation names the sub-step by its letter, not by
position.

**instruction style:** 11 new behavioral instructions checked, 0 rewritten. Unit counted: *a line
carrying a CAPS imperative (ALWAYS/MUST/NEVER) that a session must execute every time.* They are:
the `deferred domain rules:` report mandate (N-22), the `done_tasks.md:` report mandate (N-48), the
two-line `cross-cutting routed:`/`written:` mandate (N-21), the `audit cadence:` per-check report
mandate (N-34), the three-surface update mandate and its grep (N-29), D6.2b's `declarers extracted:`
mandate (N-33), D7.5's DERIVE-don't-enumerate mandate and its proof command (N-31), item 6's
GLOB-them-never-list-them mandate and its `ALWAYS REPORT N` (N-51), and the errata pre-write grep
(N-52).

**references:** 9 of 9 cited sections resolved — the unit is *a distinct section or file path cited
by text this session WROTE*, and the denominator is every such citation in the diff. They are
`component-design.md` §1 and §9 (N-33, N-34), `bootstrap.md:350` (N-51), `docs/modules/agents/validator.md`
Output template (N-29, N-32), `validation-orchestrator/SKILL.md` report list (N-29), `audit.md`
"ID allocation" (N-52), `/audit` Phase 3 item 2 (N-4), EPA Step 2.7.1 (N-22) and EPA Step 4.1b
(N-21). Each was grepped; each resolves.

**fences:** 12 of 12 templates and rules extract non-empty, using the width-agnostic form N-50
installed.

**isolation:** 13 files scanned, **0 hits**. Blocklist derived from ALL THREE sources: 3 project
folder names → 10 tokens after excluding generic words; identifiers harvested from project docs;
and source-project CODE identifiers plus session numbers (D16.2c). Two candidates were inspected
and both are covered by exemptions the rule already states: `appointments` in `bootstrap.md:622` is
canonical scheduling-domain vocabulary (D16.2c's "standard industry names are LEGITIMATE"), and
`S12` / `Sessão 90` at `audit.md:191` is the RULE stating the shape to detect (D16.2b's
scanner-regex exemption). **Negation-proved:** the same check run against
`.claude/settings.local.json` — the file that is EXEMPT precisely because it contains project paths
— returns hits, so a 0 on the real diff is a measurement and not a silence.

**class sweep:** 31 fixes × 4 directions, 4 extra instances found and fixed.

| Fix ID | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|---|---|---|---|---|---|---|
| N-1 | `validation-orchestrator/SKILL.md` | ✓ `grep -rn "Coverage Gap Declaration" docs/` — every citer checked for a matching slot | ✓ `validator.md`'s template already had the section | ✓ read the report block above and below the insert | ✓ the citer's mandate now resolves in its own format | none |
| N-3 | `.claude/commands/maintenance.md` | ✓ `grep -n "ALWAYS STATE THE UNIT\|ALWAYS NAME THE SURFACES" maintenance.md` — items 1, 2, 3 all now state a unit | n/a — the receipt items have no twin | ✓ read items 1 and 2, which M-54 had reached | ✓ the report line now carries `N of M` | historical half recorded as Errata 5 |
| N-4 | `assets/docs/audit-2026-09-02.md` | ✓ `grep -c "^| M-5[0-9] |"` over the ledger — M-55/M-56 were the only two missing | ✓ checked the 2026-09-03 ledger for the same gap → none | ✓ the `Application status` line directly below | ✓ the status line now separates applied from accepted-risk | the header counted an `accepted-risk` finding as applied |
| N-5 | `assets/docs/audit-2026-09-02.md` | ✓ `grep -c "^| M-" ` per receipt section vs disposed count | ✓ all three receipt sections in the file checked | ✓ the row-count claim directly below the table | n/a — a record, nothing descends from it | recorded as Errata 4 |
| N-7 | `assets/docs/audit-2026-09-02.md` | ✓ same errata as N-20/N-25/N-35/N-37 | ✓ same section | ✓ same block | n/a | recorded as Errata 5 |
| N-9 | `docs/modules/skills/codebase-audit/SKILL.md` | ✓ `grep -rn "derived_from:" docs/modules/` — every lineage pointer checked for a path a project cannot open | ✓ no other skill cites an `assets/docs/` path | ✓ read the whole frontmatter block | ✓ frontmatter still parses — see **liveness** | none |
| N-10 | `.claude/commands/audit.md` | ✓ `grep -n "D16\.[0-9]" audit.md` — disk order now D16.1, .2, .2b, .2c, .3 | n/a — D16 exists in one file | ✓ read D16.2 and D16.3 around the move | ✓ no report field keys on the order | none |
| N-11 | `.claude/commands/audit.md` | ✓ `grep -rn "NOT yet a numbered check" .claude/` → 0 | ✓ checked Agent 5 and Agent 6 lists for the same stale shape | ✓ read the surrounding numbered list | ✓ the sentence now names the receiver | none |
| N-14 | `CLAUDE.md` | ✓ `awk 'length($0)>120'` — 14 → 13 long lines, the named one 152 → 73 chars | n/a | ✓ read the paragraph either side of the wrap | n/a | 13 pre-existing long lines remain — NOT this finding's scope, reported as an observation |
| N-19 | none — already satisfied | ✓ re-read the E-1 record | ✓ M-44's record carries no count at all | ✓ read all five fields | n/a | closed by `sd38fd04`'s N-43 repair; dispositioned here so that receipt stays 9-for-9 |
| N-20 | `assets/docs/audit-2026-09-02.md` | ✓ same errata as N-7 | ✓ same section | ✓ same block | n/a | recorded as Errata 5 |
| N-21 | `.claude/commands/existing_project_adaptation.md` | ✓ `grep -n "ALWAYS write in every concern" EPA` → 1 (was 3) | ✓ bootstrap's Steps 3/4/13 re-read — theirs are genuine receivers, correctly unchanged | ✓ read Step 4.1b's routing table and its report block | ✓ the report gained both the routed and the written line | **a stale M-2 tail survived my own edit** — it still claimed "EPA's three had none" after two were removed |
| N-22 | `.claude/commands/existing_project_adaptation.md` | ✓ `grep -n "DEFER the copying to Step 4.6" EPA` — the pointer that had no receiver | ✓ bootstrap has no deferral (Step 13 runs after the copy) | ✓ read Step 4.6's existing receiver declaration | ✓ the final report gained a `Deferred domain rules` slot | none |
| N-23 | `.claude/commands/bootstrap.md` | ✓ `grep -n "ALWAYS REPORT" bootstrap.md` cross-checked against Step 15's slots | ✓ EPA's report checked for the same class → covered by N-27 | ✓ read the freshness slot directly above | ✓ the slot enumerates all three verdicts Step 2 can produce | none |
| N-24 | `.claude/commands/existing_project_adaptation.md` | ✓ `grep -n "4.6.5 and 5.1 read it unconditionally"` → 0 | ✓ both clauses fixed, not one | ✓ read Steps 2.4/2.5, where M-10 had wrongly applied the fix | ✓ consumers verified by parsing each Step's body, not by reading | Step 5.1 reads neither file; 4.6.5 reads only `pendencias.md` — established mechanically |
| N-25 | `assets/docs/audit-2026-09-02.md` | ✓ same errata as N-7 | ✓ same section | ✓ same block | n/a | recorded as Errata 5 |
| N-27 | `.claude/commands/existing_project_adaptation.md` | ✓ `grep -c "cross-cutting" EPA` — every mandated verdict traced to a slot | ✓ bootstrap's twin slot re-read as the model | ✓ read the report blocks either side | ✓ same edit as N-21's report half | none |
| N-29 | `docs/agentic_engineering_framework.md` | ✓ `grep -c "BASELINE-CREATED"` across all three surfaces → all non-zero | ✓ all three surfaces, not two | ✓ read the paragraph that declares three surfaces, directly above | ✓ the clause now names each surface explicitly | none |
| N-30 | `docs/modules/agents/security_reviewer.md` | ✓ `grep -c "report schema at the top of this file" security_reviewer.md` → 1 (was 2 in one sentence) | ✓ `code_reviewer.md` checked — its twin clause is clean | ✓ read the gap-declaration block above | n/a | none |
| N-31 | `.claude/commands/audit.md` | ✓ enumeration replaced by an invariant + proof command | ✓ `examples/agents/` checked as the contrast set | ✓ read D7.4 and the report format below | ✓ the check's expected result is stated | **the finding's own count of 9 was short by one** — the real set is all 10, which is why the fix derives instead of counting |
| N-32 | `docs/agentic_engineering_framework.md`, `validation-orchestrator/SKILL.md` | ✓ `grep -rn "BASELINE-CREATED"` — 3 surfaces, all now carry the reachability clause | ✓ both non-validator surfaces fixed | ✓ read the UI row's neighbours in each file | ✓ same grep as N-29 | none |
| N-33 | `.claude/commands/audit.md` | ✓ `grep -n "D6\.[0-9]" audit.md` — extraction steps now cover 3 of 3 declarers | ✓ D6.7's phrase-level patch re-read; it no longer papers over a hole | ✓ read D6.2 and D6.3 around the insert | ✓ `declarers extracted: 3 of 3` is a mandated report line | none |
| N-34 | `docs/modules/skills/sprint-proposer/SKILL.md` | ✓ `grep -rn "silently skipped" docs/` → 0 | ✓ `autonomous-loop` checked for the same silent-skip shape → none | ✓ read the two cadence bullets below | ✓ the skip must now be SAID, per component-design §9 item 3 | none |
| N-35 | `assets/docs/audit-2026-09-02.md` | ✓ same errata as N-7 | ✓ same section | ✓ same block | n/a | recorded as Errata 5 |
| N-36 | `README.md` | ✓ `grep -n "production-financial" README.md` → both sites now agree | ✓ the `:157` site re-read as the correct model | ✓ read the tier-gating note around it | n/a | recorded as Errata 5 too, since the receipt claimed both |
| N-37 | `assets/docs/audit-2026-09-02.md` | ✓ same errata as N-7 | ✓ same section | ✓ same block | n/a | recorded as Errata 5 |
| N-38 | `docs/modules/skills/skill-gate/SKILL.md` | ✓ `grep -rn '`[^`]*`[^`]*`' docs/modules/skills/` for nested-span shapes | ✓ no sibling skill has the same construct | ✓ read the eval block around it | ✓ the mandated string now renders | none |
| N-39 | `assets/docs/audit-2026-09-02.md` | ✓ `grep -n "stays \`open\`"` over the ledger | ✓ checked M-36's and M-38's rows for the mirror defect | ✓ read the L-19 row's own verdict cell | n/a | none |
| N-48 | `.claude/commands/existing_project_adaptation.md` | ✓ `grep -rn "done_tasks" .claude/commands/` — bootstrap creates, EPA only checked | ✓ bootstrap's Step 4 re-read as the model | ✓ same block as N-24b | ✓ a `done_tasks.md:` report line was added | none |
| N-51 | `.claude/commands/maintenance.md` | ✓ `grep -rn "for p in \['docs/modules" .claude/` → 0 hard-coded lists remain | ✓ both the `.claude/` guard and the raw check now cover their whole directories | ✓ read item 6's guard command above | ✓ the check reports its denominator | 15 SKILL.md templates entered the check's scope for the first time |
| N-52 | `.claude/commands/audit.md`, `assets/docs/audit-2026-09-02.md` | ✓ `grep -c "^> [*][*]Errata N "` per N, 1..6 | ✓ the sibling "ID allocation" section re-read as the model it should have followed | ✓ read the superseded-marker bullet below | ✓ the expected result is stated | **my own collision** — Group 4 wrote a second Errata 2 and 3 before the check existed |

Row count self-check: **31 rows vs 31 findings disposed in this commit** — equal.

**back-sweep:** 3 rules, 6 artifacts fixed of 6 found, re-run clean.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| N-31/N-51 — DERIVE a set from the directory; never enumerate it | A hard-coded list in a command that shadows a directory's contents | `grep -rn "for p in \['docs/modules\|prd-sync-checker, criteria-enforcer" .claude/commands/*.md` | 2 (D7.5's agent list, item 6's file list) | 2 | **0 remaining** |
| N-52 — an errata number must be grepped before it is written | An errata block whose number is already taken in the same file | `for n in $(seq 1 9); do grep -c "^> [*][*]Errata $n " <report>; done` — any value >1 is a violation | 2 (a second `Errata 2`, a second `Errata 3`) | 2 | **0 remaining** — 1..5 each exactly 1 |
| N-3 — a receipt figure must state its unit AND its denominator | A receipt key in the checklist with no stated unit | `grep -n "ALWAYS REPORT" maintenance.md` cross-read against items 1-3 | 2 (`references:` had neither; `fences:` had no denominator until N-50) | 2 | **0 remaining** — items 1, 2 and 3 all state a unit |

All three greps count VIOLATIONS, not presence (N-40). **Two false greens were caught by running
them**, and both would have read as a pass:
- the D16 isolation check first reported `0 hits` while looping over a file list that did not
  exist — a pass computed over an empty set, caught by an assertion that the list is non-empty.
- a first, looser blocklist split project folder names on `-`/`_` and kept every part longer than
  four characters, which put a generic English word in the blocklist and produced 24 "hits" across
  13 files. Neither number was real; the precise run returns 0 and is negation-proved.

**liveness:** guard run over `.claude/` — 1 component OK. Raw-template check, in the widened form
N-51 installs: **19 validated, OK** (15 skills + 4 verbatim-copied agents; the previous form
validated 4). N-9 edited frontmatter, so this was the load-bearing check of the batch:
`derived_from` is now a QUOTED scalar containing `:` and `(` — the exact
`FRAMEWORK-AGENT-YAML-01` shape — and it parses, with `name:` intact and all six keys present.

**negation proof:** RUN, on three checks this session installs or relies on.
- N-51's widened frontmatter check: injected an unquoted `last_eval:` scalar carrying colon-space
  into a real SKILL.md → `1 BROKEN`, exit 1; restored → `19 validated, OK`, exit 0; tree clean.
- N-52's errata grep: a free number returns 0, a taken number returns 1. It would have caught the
  collision this batch actually made.
- The D16 blocklist: run against `.claude/settings.local.json`, the file that is EXEMPT precisely
  because it contains project paths, it returns hits — so the 0 on the real diff is a measurement.

**version:** `v2.15.1 → v2.16.0` (MINOR). Canonical set updated: `README.md` (2),
`docs/modules/templates/claude_md.md` (1), `.claude/commands/existing_project_adaptation.md` (2).
`created: framework-vX.Y.Z` lineage fields rewritten: **0**. MINOR is correct — five new mandated
report lines, a new `audit.md` extraction sub-step (D6.2b), two new EPA report slots and a new
bootstrap report slot are contract additions. **audit: proposed** — a `verification`-mode `/audit`
over this commit is trigger (d), and a MINOR bump is trigger (b); both point at the same run.

**classification:** 24 ADD (new sections, slots, mandates, ledger rows and errata blocks), 6
SUBSTITUTE, 1 RELOCATE. The substitutions: D7.5's enumeration → an invariant plus a proof command
(N-31); item 6's file list → a glob (N-51); the two EPA justifications → the real consumer lists
(N-24); the receiver declarations in EPA Steps 2.2/2.3 → artifact-owner declarations (N-21); the
duplicated clause in `security_reviewer.md` → one clause (N-30); `codebase-audit`'s `derived_from`
path → the concept it named (N-9). Each is strictly better and loses nothing: in every case the
replaced text's information survives in the replacement or was false. The RELOCATE: `D16.2c` moved
below `D16.2b` (N-10) — nothing was added or removed, only ordered; no invoker cites either by
position, verified by grep.

**new component:** N/A — no skill, agent or rule created.

**gates:** twin parity PASS (`diff -r` → no output) | receipt-rows-vs-diff 31/31 (every
`File touched` cell appears in `git show --name-only`, and the one row reading `none — already
satisfied` is N-19, which by design touched nothing) | row-count 31 vs 31.

**push:** not requested for this batch. Per M-56 the push waits for the owner's explicit word AND
a GREEN D16 over the unpushed set.

---

# Run 7 — 2026-09-03

**Framework version:** v2.16.0
**Run mode:** `verification (over s1c7c1f9 + sbb8cbec, and the five earlier commits of the Run 6 batch)`
**Dimensions checked:** 17 + a Part 1 fix-verification pass
**Agents dispatched:** 6

**carried: 0 open + 0 escalated from Run 6** — Run 6 closed at `APPLIED — 54 of 54`, nothing open.
**Accepted-risk OBSERVATIONS (re-measured, never re-opened):**
- **E-1** (`audit-2026-09-02.md`) — working-tree occurrences of the E-1 identifier class: **0**; commit-message hits: **0**. Record stands closed.
- **M-44** (`audit-2026-09-02.md`) — re-measured **226 occurrences across exactly 4 files**; the figure is still EXACT. Five fields present. Record stands closed.
- **N-53** (`audit-2026-09-03.md`) — working tree clean at the named lines; **no new identifier-spelling pattern was introduced by `s1c7c1f9`/`sbb8cbec`**. Record stands closed. Its standing instruction is honoured; the *sibling* row on the same table under-scoped its grep, which is `N-17`'s problem and is filed below as `P-29`.

**Scope note.** Run 6 IS this file. Its 54 findings were applied across seven commits inside the
authoring session and **none had been through an independent verification pass**. Part 1 therefore
covers all 54, not only the last commit's 31 — verifying only the newest batch would have left the
21 findings of Groups 0-3 permanently in the state "applied, never checked".

## Part 1 — Verification ledger for the Run 6 batch

**Score, counted over the FINDINGS (denominator stated): of 54 findings, 45 received an explicit
verdict and 9 were not reached by any agent's dimensions. Of the 45 verdicted: 24 clean, 21 not
clean** — 9 partially-fixed, 8 fixed-but-class-not-swept, 4 introduced-a-defect.

| ID | Verdict | Evidence (structural) |
|----|---------|------|
| N-1 | CONFIRMED-FIXED | `validation-orchestrator/SKILL.md:191` — the section the mandate at `:135` cites now exists in the citer's own report format; vocabulary byte-exact against `validator.md:105`, `code_reviewer.md:206`, `security_reviewer.md:219` |
| N-2 | FIXED-BUT-CLASS-NOT-SWEPT | The guard sentence is present in all six agent blocks, but is now FALSE of Agent 2 and Agent 3: `audit.md:272-282` omits EPA from FILES TO READ while Run 6's matrix reports EPA coverage in D11/D13; `audit.md:359-386` is behind the prompt this run executed → `P-12`, `P-20` |
| N-3 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | Unit and denominator landed (`maintenance.md:216-223`) and the effect is measurable: the predecessor was ~9× wrong, this receipt ~1.2×. But the one-line format it mandates scores 1/0/0 against the file's own line-anchored key check → `P-5` |
| N-6 | CONFIRMED-FIXED | `cmp` on the twin pair → exit 0; `diff -r` → empty |
| N-9 | FIXED-BUT-CLASS-NOT-SWEPT | `codebase-audit/SKILL.md:16` is a quoted scalar naming the concept ✓. `framework-audit/SKILL.md:15` carries the identical defect UNQUOTED and with TWO `assets/docs/` paths. The receipt asserted "no other skill cites an `assets/docs/` path" → `P-23` |
| N-10 | CONFIRMED-FIXED | Disk order is D16.1, .2, .2b, .2c, .3, .3b, .3c, .4, .5 — monotonic |
| N-11 | CONFIRMED-FIXED | `audit.md:129-133` names the receiver; `grep -rn "NOT yet a numbered check" .claude/` → 0 |
| N-12 | CONFIRMED-FIXED | `audit.md:257` carries all three blocklist slots plus the two-number clause |
| N-13 | CONFIRMED-FIXED (rule text) | Harvest scope now names every framework-layer surface. **Caveat that became `P-27`:** the greps executed in the same commit did not adopt the widened scope |
| N-14 | CONFIRMED-FIXED | `CLAUDE.md:153` is 73 chars; sentence and markdown intact |
| N-15 | FIXED-BUT-CLASS-NOT-SWEPT | Named site clean and the replacement identifier is absent from all project code ✓. Three hand-rolled source-project function names survive in `examples/` — a DOUBLE-SCRUTINY surface the executed grep never read → `P-28` |
| N-16 | CONFIRMED-FIXED | The identifier is replaced by a description |
| N-17 | FIXED-BUT-CLASS-NOT-SWEPT — INTRODUCED-A-DEFECT | (a) the substitution left three identical tokens where three distinct ones stood, so the clause now says nothing, plus an ungrammatical fragment; (b) the `0 remaining` re-run is not reproducible; (c) only the uppercase half of the shape was enumerated → `P-25`, `P-29` |
| N-18 | FIXED-BUT-CLASS-NOT-SWEPT | Named site clean. Source-project vocabulary survives in `README.md` and a lineage doc — neither path is in the executed grep's list → `P-27` |
| N-19 | CONFIRMED-FIXED | The drifting count is gone |
| N-21 | CONFIRMED-FIXED | `grep -c "ALWAYS write in every concern"` → 1, the one genuine receiver. Steps 2.2/2.3 declare ownership without a write mandate; Step 4.1b claims the write; the routing table and the prose are reconciled |
| N-22 | CONFIRMED-FIXED | Pointer and receiver now name each other in both directions; report slot present |
| N-23 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | The slot landed in the right place but enumerates THREE verdicts where Step 2 defines TWO; the third bled in from Step 1.1's vocabulary → `P-16` |
| N-24 | PARTIALLY-FIXED | The two false clauses are gone, but the replacement consumer lists are themselves wrong: two named steps touch neither file, and the step that actually reads and writes both is absent from both lists. The receipt claimed the lists were "established mechanically" → `P-19` |
| N-25 | CONFIRMED-FIXED | All 8 `sed` extractions bootstrap performs run non-empty; the 9-line template is the complete table, not a truncation |
| N-26 | CONFIRMED-FIXED | `git reflog` present in the authorized list |
| N-27 | CONFIRMED-FIXED — INTRODUCED-A-DEFECT | The three verdicts have a home, but a cross-cutting report section **already existed** and the fix added a second instead of extending it → `P-18` |
| N-28 | PARTIALLY-FIXED — CLASS-NOT-SWEPT | The shipped skill's items 1-2 and the framework doc's template are fixed. The two command twins diverged; the ONE executable check still greps two declarers; residue survives two and nine lines from the fix → `P-8`, `P-9`, `P-13` |
| N-29 | PARTIALLY-FIXED | The "BOTH lists → ALL THREE SURFACES" half landed; the divergence half is untouched — the third surface still lacks three categories and diverges on two verdict vocabularies, and the mandated check greps only one token and cannot see any of it → `P-14` |
| N-30 | CONFIRMED-FIXED | The clause appears once; the twin is independently clean |
| N-31 | PARTIALLY-FIXED | The invariant HOLDS on disk (proof run: `0`, nothing else, 10/10 agents), no agent is mis-classified, no information lost. But the proof command's output has **no slot in `audit.md`'s own REPORT FORMAT** → `P-11` |
| N-32 | CONFIRMED-FIXED | Reachability clause present on all three surfaces |
| N-33 | PARTIALLY-FIXED | `D6.2b` exists and is correctly ordered, but its mandated line has no slot in the D6 REPORT FORMAT, and the value is a hard-coded `3` in the file that forbids hard-coded sets → `P-11` |
| N-34 | CONFIRMED-FIXED — CLASS-NOT-SWEPT | `grep -rn "silently skipped"` → 0 and the mandate is explicit. But it names no destination and the proposal format has no slot for it → `P-6` |
| N-36 | PARTIALLY-FIXED — INTRODUCED-A-DEFECT | Both sites now agree with what bootstrap gates ✓, but the fix added an unqualified clause that three bootstrap lines contradict → `P-24` |
| N-38 | CONFIRMED-FIXED | Double-backtick spans; the nested span renders |
| N-41 | FIXED-BUT-CLASS-NOT-SWEPT | The key list is 14 and both post-mandate receipts return 14/14 ✓. But TWO other mandated keys have no entry in the loop, and both went missing or buried in the very receipts the check passed → `P-1` |
| N-42 | PARTIALLY-FIXED | Paths and forms widened, but the "MATCH ALL THREE FORMS" mandate **drops `step N`** — and the one citation this batch actually broke is a `step N` → `P-3` |
| N-43 | CONFIRMED-FIXED | Both records 5/5 under the anchored labels |
| N-44 | INTRODUCED-A-DEFECT (×3) | The rule landed, but the file that OWNS the status enumeration still forbids the multi-hash form; the report's own status line names 4 of 7 hashes; and two commits in the same batch carry no receipts section → `P-2`, `P-4`, `P-33` |
| N-45 | CONFIRMED-FIXED — CLASS-NOT-SWEPT | Correct heading level, cited by name, no stale reference. An UNNUMBERED errata block predating the rule violates its form and is invisible to the collision grep → `P-7` |
| N-46 | CONFIRMED-FIXED | Run over the whole series: 54 distinct IDs, no gaps, no suffixes, no reuse |
| N-47 | CONFIRMED-FIXED | The marker is defined and used |
| N-48 | PARTIALLY-FIXED | Create step and mandate landed; the two paths agree on the artifact. **The mandated report line has no slot in EPA's final report**, while bootstrap's twin has one — the identical class fixed two rows above it in the same receipt table → `P-17` |
| N-49 | CONFIRMED-FIXED | The exemption is stated and must be SAID rather than read as GREEN |
| N-50 | CONFIRMED-FIXED (RUN) | Command extracted verbatim and executed: 12 of 12 non-empty; all 8 paths bootstrap `sed`-extracts are inside the 12 |
| N-51 | FIXED-BUT-CLASS-NOT-SWEPT | Glob replaced the list, `19 validated, OK` reproduced exactly. But the two globs cover **25** files and 6 are silently skipped — including all three declaring components — and the reported figure is a numerator with no denominator, against the item's own mandate → `P-34` |
| N-52 | FIXED-BUT-CLASS-NOT-SWEPT | Collisions gone, negation holds. An unnumbered block is structurally invisible to the check → `P-7` |
| N-53 | CONFIRMED-FIXED | Withheld-reference pointer in the established form; count, denominator and re-run retained |
| N-54 | CONFIRMED-FIXED | Tight anchor: 3 records, 5/5 each; the observations heading correctly excluded; provable both ways |
| N-4, N-5, N-7, N-8, N-20, N-35, N-37, N-39, N-40 | **NOT REACHED** | No agent's dimensions covered these nine. Most are errata-recorded receipt corrections and ledger repairs. **Stated rather than silently omitted** — an unverified finding reported as verified is the defect this mode exists to catch |

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 skills on disk, 1 runtime twin, byte-identical |
| C | Instruction style | Quality | 5 | FAIL | 7 mandates appended to rationale sentences rather than given their own line |
| E | Version consistency | Currency | 5 | PASS | v2.16.0 across 5 occurrences / 3 surfaces; 0 lineage fields rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15, breakdown sums, `invocation:` split exact |
| D5 | Bootstrap file refs | References | 2 | PASS | 53 paths resolve; 8/8 fence extractions non-empty; 18/18 naming mappings |
| D6 | Gap vocabulary | References | 3 | FAIL | 10/10 phrases and install rows align, but one twin, the executable loop and a shipped skill still name two declarers |
| D7 | Pushy Description | Quality | 3 | PASS | Invariant holds 10/10; 20/20 specialists complete; 0 unsanctioned PARTIALs |
| D8 | Orchestrator refs | Process | 4 | PASS | 25 references and 22 cross-section citations, 0 RED |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | Every path resolves; every count matches |
| D10 | Command → skill paths | References | 4 | PASS | All resolve; twin byte-identical; trigger parity 4/4 both directions |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | Bootstrap clean (15 primary / 25 total, claim accurate); EPA carries 3 pre-existing forward references |
| D12 | Examples conventions | Currency | 5 | PASS | 0 violations; all 7 README truth-tests measured across the full set |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | 19 sections defined, none read that is not defined |
| D14 | Bootstrap output | Process | 2 | FAIL | 16/16 template references traced; 3 mandated verdicts have no report slot |
| D15 | Doc factual accuracy | Currency | 5 | FAIL | Every count claim true; 5 non-count claims false or stale |
| D16 | Project-info isolation | Privacy | 1 | **FAIL** | 10 hit classes; root cause is a rule widened without widening the grep that enforces it |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | 3 undocumented executed flows; the three gates verified as genuine gates |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| P-1 | HIGH | `maintenance.md:119-121` vs `:475`, `:578` | The 14-key self-check omits `verification audit:` and `audit:`, both mandated with "NEVER emit nothing". Both post-fix receipts miss or bury them while the check reports 14/14. N-41's class surviving N-41's fix. | open |
| P-2 | HIGH | `audit-2026-09-03.md:283` | The multi-hash `Application status` N-44 created lists **4 of 7 hashes** — 20 of 54 dispositions attributed, omitting the largest commit. The same line still asserts "37 remain `open`" while claiming `APPLIED — 54 of 54`; zero findings carry `open`. | open |
| P-8 | HIGH | `bootstrap.md:606` vs `EPA:900` | N-28's twins diverged **in the fix**: EPA says "in the declaring component", bootstrap still says "in code-reviewer.md or security-reviewer.md", against a receipt asserting both twins were swept together. On the bootstrap path a validator-only declaration reads as a broken chain. | open |
| P-9 | HIGH | `EPA:982-984` | N-28's own diagnosis repeated inside N-28's fix: the **comments** were corrected and the **two lines that execute** were not. The framework's only executable activation-chain check still greps two declarers and its failure string still says "no reviewer". | open |
| P-10 | HIGH | `EPA:979` | The executable loop's domain regex requires a literal `declares a ` and a single-token domain, so it fails on **7 of the 10 real gap phrases** (every multi-word domain, and every `declares an …`). It reports correctly-wired chains as broken — including the one that has broken twice. Pre-existing, but N-28 declared this loop swept. | open |
| P-17 | HIGH | `EPA:347-352` vs `:994-1092` | N-48's mandated `done_tasks.md:` report line has **no slot** in EPA's final report, while bootstrap's twin has one. The receipt's DESCENDING cell claimed the line was added — adding the line IS the fix; the descending obligation is the slot. | open |
| P-22 | HIGH | `audit-2026-09-03.md` receipts for `s1c7c1f9` | Three receipt figures are false, re-measured independently: `instruction style: 11 … 0 rewritten` against **29** added imperative lines and **12** removed ones; `references: 9 of 9` against **11** distinct file-path citations (one agent counting a broader citation unit measured ≥31 — the two units disagree and both exceed 9); `isolation: 13 files scanned` against **15** touched. | open |
| P-27 | HIGH | `README.md`, `examples/**` ×3, two lineage docs | **D16 FAIL.** N-13 widened D16.2c's *rule* to every framework-layer surface; the back-sweep greps executing in the same commit stayed at three directories and uppercase-only. Every surviving hit lives in that gap. Three sit in `examples/` — a double-scrutiny surface copied into every bootstrapped project. All are reachable from `origin/main`. | open |
| P-3 | MEDIUM | `maintenance.md:171-182`; `audit-2026-09-02.md:108` | N-42's widened form drops `step N` / `step N's`, and the citation this batch actually broke is a `step N`: one `Audit intake step 6` citation meant "propose a verification audit" and now names "a batch MAY be split". Six such citations on disk, not the three the receipt reports. | open |
| P-4 | MEDIUM | `eb402a9`, `bb8cbec`, `f086041`, `cea7428` | Audit-intake item 6 is violated four times inside the batch that installed it. One commit bumped a version and edited three shipped surfaces with receipts only in its message (the OR-branch is for "no report file", and it edited the report file) and only 6 of 14 keys; another has neither. | open |
| P-11 | MEDIUM | `audit.md:474-486`, `:494-499` | Both report lines this batch mandated — `declarers extracted: 3 of 3` and the D7.5 invariant proof output — exist only inside the CHECK text and have **no slot** in the corresponding REPORT FORMAT. Identical to N-12/N-23/N-27, filed in this same run. The `3` is also a hard-coded constant in the file that forbids hard-coded sets. | open |
| P-12 | MEDIUM | `audit.md:359-386` (Agent 3 block) | Working checks are living in the invoking prompt rather than in `audit.md` — the exact failure the block's own guard sentence names. Three divergences: a FILES TO READ entry naming the two files where N-1 and N-28 live, D6.7's updated wording, and the two report slots `P-11` says are missing. | open |
| P-13 | MEDIUM | `rules-agents-updater/SKILL.md:118`, `:127`; `bootstrap.md:596`; `EPA:893` | N-28's class survives two and nine lines from its own fix, in the shipped skill every project runs: one sentence's second half still says "the reviewer", and a Step-7 loop still enumerates two declarers. N-28's residue grep matches neither formulation, so its `6 → 0 remaining` row measured a pattern narrower than its own mandate. | open |
| P-14 | MEDIUM | `agentic_engineering_framework.md:933-934` | The third surface still describes TWO declarers and ONE pass, contradicting the orchestrator's own "THREE components … read in TWO passes; a single pass structurally cannot reach the third". N-29's fix reached the report-format slot 24 lines below and not the handling paragraph. | open |
| P-16 | MEDIUM | `bootstrap.md:742` vs `:184-185` | N-23's slot enumerates three verdicts where the step it serves defines two; the third bled in from a neighbouring step's vocabulary. A slot offering a verdict its owning step cannot emit is the mirror of the gap it was fixing. | open |
| P-18 | MEDIUM | `EPA:1003-1004` and `:1045-1052` | N-27 added a second `### Cross-cutting concerns` report section without seeing that one already existed. Two homes for one number is the divergence class the design policy names. | open |
| P-19 | MEDIUM | `EPA:288`, `:343` | N-24's replacement consumer lists are wrong: two named steps touch neither file, and the step that actually reads `project.md`'s risk profile, writes back to it and registers a task into `pendencias.md` appears in neither list. The receipt claimed the lists were established mechanically. | open |
| P-20 | MEDIUM | `audit.md:272-282`, `:302`, `:308-314` | Agent 2's block names 9 files and omits EPA; D11.4 and D13 carry no EPA clause. Run 6's matrix nevertheless reported EPA coverage in both — coverage that existed only in the dispatch prompt. Same class as `P-12`, different block. | open |
| P-23 | MEDIUM | `framework-audit/SKILL.md:15` | N-9's class, unswept and doubled: an UNQUOTED `derived_from:` naming **two** files that live only in `assets/docs/` and are never copied to projects. The receipt asserted no other skill cites such a path. | open |
| P-24 | MEDIUM | `README.md:37` | N-36's fix added an unqualified clause — "nothing is reserved for `production-financial` alone" — contradicted by three bootstrap lines that do reserve reconciliation queries, mandatory red-team and a tighter audit cadence for that tier alone. | open |
| P-25 | MEDIUM | `assets/docs/framework-base-upgrade.md:110` | N-17's substitution replaced three DISTINCT tokens with three IDENTICAL ones, so the clause now carries no information, and left an ungrammatical fragment on the same line. A privacy fix that broke the sentence it was protecting. | open |
| P-26 | MEDIUM | `CLAUDE.md:143-147`; `audit.md:37-38` | Both say the verification mode "has run five times" and list five figures. This is the sixth. `s1c7c1f9` rewrapped a line **inside that very paragraph** without updating the count — an ADJACENT miss at the narrowest possible range. | open |
| P-28 | MEDIUM | `examples/skills/…`, `examples/rules/…` ×2 | N-15's class survives in three `examples/` files — hand-rolled source-project database function names, on a DOUBLE-SCRUTINY surface. The executed grep's path list omitted `examples/` entirely. | open |
| P-29 | MEDIUM | two lineage docs, ~25 sites | N-17's `59 found / 59 fixed / 0 remaining` is a false green: the grep was uppercase-only and two-directory, while the rule it enforces names the spelled-out form explicitly. Lowercase and spelled-out session numbers, source-project rules-file names and an incident identifier all survive. | open |
| P-30 | MEDIUM | agent memory, 1 file | A source-project session number and two source-project commit hashes in the agent's persistent memory. **Not in git — locally fixable with no history rewrite.** D16.3b covers this surface; no sweep this batch ran reached it. | open |
| P-31 | MEDIUM | `maintenance.md` → Audit intake | The hash chicken-and-egg has no written home. Receipts must name a commit that does not exist until the commit is made, so the batch writes a placeholder and substitutes it in a follow-up commit — done twice here. `grep -rn "placeholder" .claude/commands/*.md` → 0. It also interacts with item 6: those commits change files and carry no receipts. | open |
| P-32 | MEDIUM | `maintenance.md:18-30`; `CLAUDE.md:103-110` | "Someone else pushed while the session was mid-batch" is undocumented and silently voids the push gate. Six commits are now pushed whose receipts read `push: not requested`, meaning the mandated GREEN D16 never ran over any of them. This is precisely how N-53's identifier crossed the boundary and became an accepted risk instead of a local fix. | open |
| P-33 | MEDIUM | `audit.md:961-962` | The file that OWNS the `Application status` enumeration still forbids the multi-hash form `maintenance.md` now mandates — "NEVER invent a value outside this set". N-44's receipt says the vocabulary was "re-read", not updated. | open |
| P-34 | MEDIUM | `maintenance.md:366-384` | N-51's glob covers 25 files and validates 19; the 6 skipped are fenced templates — and they include **all three declaring components**. The item's own mandate is "ALWAYS REPORT N, because a silent drop in the denominator is how a whole directory left the check's scope unnoticed", and the reported figure is a numerator with no denominator. (All six parse today; the gap is coverage, not a live break.) | open |
| P-35 | MEDIUM | `maintenance.md` item 2 | `instruction style:` is the **only** receipt key in the checklist with neither a command nor a denominator, and it is the only one that failed re-measurement. Item 3 received both in this batch and went from ~9× wrong to ~1.2× wrong; item 2 received only a unit and stayed exactly as wrong. | open |
| P-36 | MEDIUM | `audit.md:182-198` (D16.2c) | **D16.2c's cross-grep is structurally prone to FALSE POSITIVES, and the check text does not say so.** Source 3 harvests identifiers from framework-layer surfaces and cross-greps them against `projects/**`. But bootstrap Step 1.5 copies `examples/` into every project as `assets/examples/`, so the framework's OWN template identifiers are present under `projects/` in all 3 installed projects — a match there means the identifier propagated framework → project, the OPPOSITE direction from a leak. `grep -c "assets/examples" audit.md` → **0**: nothing excludes that directory from the comparison set. The risk runs both ways — a false hit filed, or a real hit dismissed as "probably our own template". Found by the follow-up verification that cleared one candidate identifier on exactly these grounds. | open |
| P-5 | LOW | `maintenance.md:216-217` | The one-line `references: … \| fences: … \| isolation: …` format the item mandates scores 1/0/0 against the file's own line-anchored key check — a format-compliant discharge reads RED. | open |
| P-6 | LOW | `sprint-proposer/SKILL.md:39-40`, `:75` | `audit cadence:` and `loop marker:` are mandated with no stated destination, and the proposal format has no slot — the "a report mandate without a destination is owed by nobody" class N-1 closed elsewhere in the same commit. | open |
| P-7 | LOW | `audit-2026-09-02.md:477` | An UNNUMBERED errata block predates the rule, violates its mandated form, and is structurally invisible to N-52's collision grep. | open |
| P-15 | LOW | `security_reviewer.md:284` | Asserts "the two are deliberately identical" of a pair a `diff` refutes — one writes a phrase bolded, the twin writes it plain. | open |
| P-21 | LOW | `bootstrap.md:816-817`, `:813-814`; `EPA` Step 2.1 | Two further mandated verdicts are only half-slotted (an MCP "none installed" verdict and a Hooks "none — no formatter" verdict), and bootstrap's PRD-bracket deletion mandate has no EPA counterpart. | open |


> **Note on `P-36` (added after this report was first persisted in `s75533b3`).** Agent 1's verification continued in the background and returned one further finding plus two confirmations. No Part 1 verdict changed and no D16 hit was added or withdrawn — the ledger score stands at 24 clean of 45 verdicted. Two negative results are worth recording because they were **tested rather than assumed**: one candidate identifier was cleared (it originates in the framework's own templates, which is what produced `P-36`), and two schema names were deliberately NOT filed under D16.2c's "standard industry names are LEGITIMATE" exemption. A source-project rule/incident identifier was re-confirmed, so `P-29`'s hit stands. `s75533b3`'s commit message says "35 findings open"; the correct figure is **36**, and the message is left as written rather than rewritten.

## Recommended Fixes

**1. Quick fixes** — `P-24` (delete one false clause), `P-25` (restore the broken sentence with distinct generic tokens), `P-26` (update two "five times" claims to six and add this run's figure), `P-15`, `P-7` (number the orphan errata block), `P-2` (complete the hash list and delete the stale "37 remain open").

**2. Structural fixes** — `P-9` and `P-10` first: they are the only *executable* check in the activation chain and it is both under-scoped and regex-broken. Then `P-8`, `P-13`, `P-14` (finish N-28's sweep across the twin, the shipped skill and the framework doc); `P-17`, `P-16`, `P-18`, `P-19`, `P-21` (the EPA/bootstrap report slots and consumer lists); `P-23`, `P-34` (the frontmatter class and the check's own denominator); `P-1`, `P-3`, `P-5`, `P-6`, `P-11`, `P-33` (report keys, ordinal forms and slots); `P-12`, `P-20` (move the checks that live in the prompt into `audit.md`).

**3. Quality improvements** — `P-35` (give item 2 a command and a denominator — the single highest-leverage fix in this list), the seven instruction-style instances, `P-31`, `P-32`, `P-4` (write down the three executed-but-unowned flows).

**4. ESCALATED — owner decision required**

`P-27`, `P-28`, `P-29` are a **pushed privacy residue** and this repo cannot close them alone.
- **What is affected:** one root document, three `examples/` templates (copied into every bootstrapped project), two lineage docs, across commits reachable from `origin/main`.
- **What is NOT affected:** the four unpushed commits are **clean** — they introduce no hit. The push gate's own condition (D16 GREEN over the unpushed range) is satisfied.
- **The decision:** the standing instruction from `E-1` and `M-44` is *fix forward, record, never rewrite history*. Applying it here is mechanical and needs no new decision. **What is genuinely new** is that three of these hits sit in `examples/` templates, which are not a historical record but live content copied into future projects — fixing those forward changes what every future bootstrap receives, and that is worth stating rather than assuming.
- `P-30` (agent memory) is **not in git** and is fixable locally with no history implication at all.

**5. ACCEPTED-RISK — no action**
- **E-1** — re-measured, 0 working-tree and 0 message hits. Closed.
- **M-44** — re-measured at exactly the figure its record states. Closed.
- **N-53** — working tree clean, no new instance introduced by this batch. Closed.

## Meta-observation

**The recurring class this run is a rule widened without widening the command that enforces it.**
It is not a new class — it is `N-40`'s ("count violations, never presence") one level up, and it
produced the single FAIL that matters. `N-13` correctly widened D16.2c's *rule* to every
framework-layer surface; the back-sweep greps that executed in the same commit stayed at three
directories and uppercase-only. Every one of the ten surviving isolation hits lives in exactly that
gap, and both `0 remaining` rows in that table are false greens. The same shape appears three more
times independently: `N-42` widened the ordinal sweep's paths but dropped a citation form, and the
one citation that broke is in the dropped form; `N-41` widened the key list to 14 but two other
mandated keys have no entry; `N-51` replaced a hard-coded list with a glob and the glob silently
skips six files. **In all four the missing case was reachable by the control's own command, run one
notch wider.**

**The second class is a mandated report line with no slot in the report format** — `P-11`, `P-17`,
`P-6`, `P-21`, and `P-16` in its inverted form (a slot offering a verdict its step cannot emit).
This is the third consecutive run in which it appears, and it is now unmistakably the dominant
ungated defect: the same commit fixed six instances of it correctly (`N-1`, `N-22`, `N-23`,
`N-27`, `N-29`, `N-32`) and committed four new ones. A control exists for artifacts (the inventory
sweep) and for point fixes (the class sweep); none exists for *the descending direction of a new
mandate*. One line of grep would close it: for every `ALWAYS REPORT` key added this session, grep
the same file's report format for a slot.

**On Run 6's own conclusion, which this run set out to test.** Run 6 claimed: *every receipt claim
whose value a stated command reproduces was TRUE; every claim the author supplied from reading was
FALSE.* **It holds, and the mechanism is now sharper than Run 6 could state it.** Every
command-derived claim in the 31-finding commit reproduced exactly — 19 validated, 12 of 12, 31/31
rows-vs-diff, 31 vs 31, twin parity, 14/14 keys, errata 1..5, five-field 5/5/5, the D7.5 invariant
in both directions. What separates the two halves is not *command vs reading*: it is **whether the
mandate names a command AND a denominator.** Item 3 (`references:`) received both in this batch and
went from roughly ninefold wrong to roughly 1.2-fold wrong. Item 2 (`instruction style:`) received
only a unit — no command, no denominator — and stayed exactly as wrong as before, at 11 claimed
against 29 measured. It is now the only receipt key in the checklist with neither, and the only one
that failed re-measurement. That is a testable claim, and `P-35` is the experiment.

**Batch size is exonerated for the third time.** The 31-finding commit is the cleanest in this
file's history on every gated key. **The three pre-commit gates are genuine gates:** Gate 1 has
blocked a real commit, and Gate 2 was independently reproduced from outside for all 51 receipt rows
— the first time any receipt claim in this repository has been reproducible end-to-end by a party
other than its author.

**Prediction for the next batch, so it is checkable next run:** the defects will NOT be in the
gated keys, and they will NOT scale with batch size. They will be (a) in the descending direction
of newly added mandates, until a slot-check gate exists, and (b) in `instruction style:`, until it
is given a command and a denominator. If the next verification run finds its defects somewhere
else, this model is wrong and should be replaced rather than defended.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; 45 of 54 Run 6
findings verdicted and the 9 unreached ones named explicitly.
**Application status:** `PENDING`
