# Framework Audit Report

**Date:** 2026-09-15
**Framework version:** v2.23.1 (README.md:1)
**Run mode:** `verification (over 6b4baa8 · 30c3d6e · d156dc4)`
**Trigger:** (d). `/maintenance` applied 17 IDs of `audit-2026-09-14.md` Run 3 in 6b4baa8, 30c3d6e and d156dc4; the write-back is 8644903. The batch range is `6b4baa8~1..8644903`, PUSHED (`origin/main` = 8644903).
**Dimensions checked:** 17, plus the Part 1 fix-verification pass
**Agents dispatched:** 6. Each agent extracted its contract verbatim from `.claude/commands/audit.md` with `sed` and received the Part 1 mandate and a privacy rule (counts only, invented names for adversarial probes).
**Part 1 input:** `audit-2026-09-14.md` intact at 8644903 (Run 3 ledger, the carried X/W rows, the receipts section `Run 3 batch`). No reconstruction was needed.
**ID series:** `Z`. Collision check before writing: `grep -h '^| Z-' assets/docs/audit-*.md | wc -l` returned 0.
**What this run tests:** the first isolation gate specified as ONE script that reads git's object model (Run 3's falsifier), verified before commit by two diff-only verifier rounds.

**carried: 38 open + 0 escalated from `audit-2026-09-14.md` (Run 3 application status `PARTIAL — 17 of 55`).** Every one re-verified on disk as still true: Y-8, Y-9, X-2..X-5, X-23, W-14, W-15, W-17..W-19 (Agent 2); W-24, W-28, W-31 (Agent 3); Y-12, Y-15, X-7, X-8, X-11, X-15, W-21..W-23, W-25, W-26 (Agent 4); X-12, W-27, W-29 (Agent 5); X-17, X-18, Y-16 (Agent 6); X-9, X-10, X-13, X-19 (files untouched by the batch except `autonomous-loop/SKILL.md:55`; `git diff -U0 6b4baa8~1..8644903 -- docs/modules/skills/autonomous-loop` has one hunk, `@@ -55 +55 @@`). **Recurred:** Y-14 and X-16.
**OBSERVATIONS — accepted-risk, not re-opened:** E-1, M-44, N-53, T-11, T-36, U-48. Agent 1: the unreachable commit 584be08 accounts for every hit outside reachable history (1 object hit, 3 through reflog entries); 0 of the other 107 unreachable/reflog commits hit. U-48's record covers it.
**Post-push D16, persisted here** (8644903 was pushed without a persisted gate over it — X-16): `bash .claude/scripts/d16-gate.sh log c4595dd..8644903` → `4 commits scanned, 0 hits` (Agents 4 and 6, independently). `log --all` → `194 commits scanned, 0 hits`; `staged` → `114 files scanned, 0 hits`; `selftest` → `95 of 95`.

## Part 1 — Verification ledger for 6b4baa8 · 30c3d6e · d156dc4

NOT-FIXED was settled first, mechanically: the receipt's pickaxe loop re-run verbatim returns a commit for all 17 IDs (Agents 1, 4, 6). **No ID is NOT-FIXED.** Where agents disagreed, the verdict below is the most severe one backed by a reproduced probe.

| ID | Verdict | Evidence (file:line — structural) |
|----|---------|------|
| Y-1 | CONFIRMED-FIXED | `d16-gate.sh:107` passes patterns only through `-f "$pf" -- "$1"`; paths come from a file, never argv. Selftest "leak under a path starting -" (`:233`) passes. `maintenance.md:439-448` points and restates no pipeline (`grep -rl "paste -sd'\|'" .claude/commands` → 0). (Agents 1, 4, 6.) |
| Y-2 | CONFIRMED-FIXED | `index_text` (`d16-gate.sh:119-122`) reads index blobs via `ls-files -s` + `cat-file --batch`. Selftest "staged then cleaned on disk" (`:235-236`) passes. (Agents 1, 4, 6.) |
| Y-3 | FIXED-BUT-CLASS-NOT-SWEPT | The named forms hit (`:77-91`, selftest `:255-257`, `:301-303`); `audit.md:225-232` names them. **Not swept:** parts and full-name variants split only on `_`, `-`, space (`:78-82`). Invented folder names containing `.` or `+`: the name as written hits, its snake, spaced and bare-part forms read 0 (Agent 6); a dotted name reads 0 on 4 of 4 forms (Agent 1). Latent: 0 of 3 real folders. (Agents 1, 6; Agent 4 read CONFIRMED.) → Z-4 |
| Y-4 | FIXED-BUT-CLASS-NOT-SWEPT | Paths are counted in every gate scope (`:145`, `:174`, `:188`); `audit.md:287-288` requires PATHS in D16.3. **Not swept:** the D16.3c hand-scan bullets (`audit.md:314-315`) — the half that carries the D16.2/D16.2c identifiers the gate does not derive — still scan contents and messages only, with `git grep -I`, and read 0 on paths, binary-marked files, NUL bytes, UTF-16 and author e-mails. (Agents 1, 4.) → Z-3 |
| Y-5 | PARTIALLY-FIXED | Behaviour correct: `ESC` (`:69`), fail-closed empty blocklist (`:74`); 9 metacharacter folder names read RED 9/9 on a leak, GREEN 9/9 on clean text, 0 stderr (Agent 1). **Missing:** the selftest (`:227-310`) has no metacharacter-name, missing-`projects/` or bad-range case — against `maintenance.md:475` "ALWAYS ADD a selftest case in the same edit that teaches the gate a new form"; the negation rests on unpersisted verifier seeds. (Agents 1, 4; Agent 6 read CONFIRMED.) → Z-6 |
| Y-6 | FIXED-BUT-CLASS-NOT-SWEPT | Hand seeds replaced by the selftest (`maintenance.md:457-474`); swap case at `:228`. **Not swept — the selftest's seeds come from the gate's own derivation** (`d16-gate.sh:224-226`, `:249-250` repeat `:76`, `:82-83`): a synthetic mother repo with an uppercase-run CamelCase and a dotted folder reads `selftest: 66 of 66` while the gate reads 0 on their snake/spaced forms; a mutation removing the letter-boundary fix (`:85`) still reads `37 of 37`; the green controls (`:305`, `:309-310`) cannot fail under invented names. (Agent 6; Agent 1 read CONFIRMED.) → Z-2 |
| Y-7 | FIXED-BUT-CLASS-NOT-SWEPT + INTRODUCED-A-DEFECT | Placeholder gone (`maintenance.md:26-28`); `=` compare blocks on a `D16 RED:` line. **Introduced:** the scan reads `origin/main..HEAD` but the push publishes `main` — HEAD on a clean branch with a leak on `main`: snippet `0 commits scanned, 0 hits` → push fires, `log origin/main..main` → 2 hits (Agents 1, 4). **Not swept:** with `push.followTags=true` an annotated tag naming the project on a range commit reads 0 → push fires, `log --all` → 2 hits; `d16-gate.sh:36-37` KNOWN LIMITS asserts "a branch push publishes no tag" and `:24-25` claims every flippable git option is pinned, while the push command pins none (Agent 4). (Agent 6 read CONFIRMED with the HEAD/main residue noted.) → Z-1 |
| Y-10 | PARTIALLY-FIXED | Count fixed: `existing_project_adaptation.md:591` "FOUR DIRECTORIES THIS FENCE CREATES" matches the mkdir at `:600`. **Not fixed:** `:592` keeps "THREE THIS COMMAND WRITES INTO AND NEVER CREATED" for directories `:549` and `:600` create — the "appended a caveat instead of correcting it" complaint recurs; the receipt's self-check (`grep 'THREE DIRECTORIES'` → 0) matches only the old word order. `:592-593` "`cp` to a missing directory FAILS" heads a `sed … >` fence. (Agent 2.) → Z-11 |
| Y-11 | PARTIALLY-FIXED | Slot `maintenance.md:432` equals the script's line; the `log` form re-runs after commit (`3 commits scanned, 0 hits` reproduced). **Not fixed:** receipts living in a commit body (`maintenance.md:204`) cannot run `log <first>~1..<last>` before that commit exists — 6b4baa8's and 30c3d6e's bodies discharged with the `staged` form and a file count; the persisted `**isolation:**` receipt paraphrases the line (`D16 log over the whole batch, 0 hits`) instead of pasting it. (Agents 4, 5; Agents 1, 6 read CONFIRMED.) → Z-9 |
| Y-13 | FIXED-BUT-CLASS-NOT-SWEPT | "No trend" line fixed (`audit.md:155-156`). **Not swept:** Phase 3 item 6 still states `GREEN 9+2=11 today` (`audit.md:1317`) while its command returns `GREEN 11+2=13` — a copied run count, the class Phase 0 forbids; the Y-13 receipt's ADJACENT cell claims Phase 3 item 6 was inspected. (Agent 4.) → Z-10 |
| W-16 | CONFIRMED-FIXED | `bootstrap.md:363` "extracted unconditionally below (this step's "Session rules" fence)"; the fence is at `:398-405` in the same Step 5.7; `grep 'unconditionally above' .claude docs` → 0. (Agent 2.) |
| W-20 | CONFIRMED-FIXED | `maintenance.md:792-793` 31 + 35 = 66; the enumerator returns `5 fenced + 26 inline = 31` and `7 fenced + 28 inline = 35`. (Agent 4.) |
| W-30 | CONFIRMED-FIXED | `examples/README.md:69` names `/bootstrap` Step 1.5 (`bootstrap.md:177`); d156dc4 fixed the 4 lateral lines in the concepts doc; `session0` residue 0. (Agent 5.) |
| X-14 | CONFIRMED-FIXED | `autonomous-loop/SKILL.md:55` "If NONE of the four paths applies" under the four-row table (`:48-53`); `NEITHER happened` residue 0. (Agent 5.) |
| Y-17 | FIXED-BUT-CLASS-NOT-SWEPT | The seven named classes are selftest cases (`:237-247`, `:267-278`). **Not swept:** (a) the D16.3c hand scan still uses `git grep -I` and an interpolated `-E "<…>"` (`audit.md:314`) and reads 0 files on UTF-16, NUL and `binary`-marked files for an invented identifier (Agent 1) → Z-3; (b) content transformed before it reaches grep — gzip, base64, a deflated `.docx`, an HTML entity — reads 0 and is not a KNOWN LIMIT (Agents 1, 6) → Z-5. (Agent 4 read CONFIRMED.) |
| Y-18 | FIXED-BUT-CLASS-NOT-SWEPT | Fixed classes are selftest cases (`:271-291`, `:298-306`); the sparse-index limit reproduces as declared. **Not swept:** the CamelCase split `([a-z0-9])([A-Z])` (`:76`) never splits an uppercase run (an acronym-prefixed name): snake, spaced and part forms read 0 on 3 of 3; the D16.3c messages form (`%h\|%s\|%b`, `audit.md:315`) reads 0 on an author e-mail and never reads tags; `log --all` reads no ref NAMES (branch, lightweight tag). (Agents 1, 6; Agent 4 read CONFIRMED.) → Z-3, Z-4, Z-8 |
| Y-19 | CONFIRMED-FIXED | 0 non-comment `-i` in any scan (`grep -c 'grep -a.* -i ' .claude/scripts/d16-gate.sh` → 0); `FOLD` at `:63-68`; full selftest passes on GNU grep 3.0. (Agents 1, 4, 6.) |

**Score over the 17 applied FINDINGS (denominator 17): 7 of 17 clean, 10 not clean.** Of the not-clean: 3 partially-fixed (Y-5, Y-10, Y-11) · 7 class-not-swept (Y-3, Y-4, Y-6, Y-7, Y-13, Y-17, Y-18) · 0 not-fixed · 1 introduced-a-defect (Y-7, combined).
**Severity of what was introduced or left:** 0 HIGH · 3 MEDIUM (Z-1 push scans a different ref than it publishes; Z-2 the selftest's oracle is the derivation it tests; Z-3 the D16.3c hand-scan half is blind to every class Y-17/Y-18 fixed in the gate).

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` byte-identical |
| C | Instruction style | Quality | 5 | FAIL | 2 new batch-text burials (Z-15); X-12, W-27, W-29 remain |
| E | Version consistency | Currency | 5 | PASS | v2.23.1 on 6 of 6 stamps; `maintenance.md:970` is lineage |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15 |
| D5 | Bootstrap file refs | References | 2 | PASS | every framework-source path resolves |
| D6 | Gap vocabulary | References | 3 | PASS | 10 exact; declarers 3 of 3; install tables, TWINNED SPAN and loops byte-identical to 6b4baa8~1 |
| D7 | Pushy Description | Quality | 3 | PASS | both twins `10 verified, 0 broken, 17 info`; 15/15 regressions; planted probe named |
| D8 | Orchestrator refs | Process | 4 | PASS | every name and §heading citation resolves |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | `.gitattributes` and `.claude/scripts/` now in the tree |
| D10 | Command → skill paths | References | 4 | FAIL | push gate reads another ref (Z-1); version-residue check fires on healthy text (Z-12); LATERAL half 1 has no command (Z-13); Gate 3 still 20 vs 17 (Y-12) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | carried only (W-14, W-15, W-17, X-2, X-3) |
| D12 | Examples conventions | Currency | 5 | PASS | full-set truth tests 20/9/11 |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | untouched |
| D14 | Bootstrap output | Process | 2 | FAIL | carried only (W-18, W-19, X-5, Y-9) |
| D15 | Doc factual accuracy | Currency | 5 | FAIL | CLAUDE.md:7 "Only documentation…" false; concepts-doc tree lacks `.gitattributes` (Z-14) |
| D16 | Project-info isolation | Privacy | 1 | PASS | 0 hits on every surface incl. unreachable objects and reflog; gate/scan defects Z-1..Z-8 |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | selftest oracle self-referential (Z-2); no GREEN floor (Z-7); no independent selftest runner (Z-17); shipped share ≈0.9% |

## Findings ledger — Run 1

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| Z-1 | MEDIUM | `.claude/commands/maintenance.md:26-28`; `.claude/scripts/d16-gate.sh:24-25`, `:36-37` | The push gate scans `origin/main..HEAD` but runs `git push origin main`: with HEAD on another branch, a leak on `main` reads `0 hits` and the push fires (2 hits over `origin/main..main`). Under `push.followTags=true` an annotated tag naming a project is published unscanned, while KNOWN LIMITS asserts "a branch push publishes no tag" and the header claims every flippable git option is pinned. Introduced by the Y-7 fix. | applied `safa8ef0` |
| Z-2 | MEDIUM | `.claude/scripts/d16-gate.sh:224-260`, `:305`, `:309-310` | The selftest generates its seeds with the gate's own derivation, so a derivation gap is invisible to it: over a synthetic mother repo with an uppercase-run CamelCase and a dotted folder it reads `66 of 66` while the gate reads 0 on their variant forms; with the letter-boundary fix mutated out it still reads `37 of 37`. Its green controls are five fixed words that cannot fail under a different folder set. Y-6's class restated at the oracle. | open |
| Z-3 | MEDIUM | `.claude/commands/audit.md:314-315` (D16.3c) | The hand-scan half of D16.3c — the only scan for D16.2/D16.2c identifiers (person names, domains, code identifiers) — still uses `git grep -I -c -E "<…>"` and `git log --format="%h\|%s\|%b"`: it reads 0 on UTF-16, NUL-byte and `binary`-attributed files, on paths, on author/committer e-mails and on tag objects, and interpolates the pattern. Every class Y-4/Y-17/Y-18 fixed in the gate survives here. | applied `safa8ef0` |
| Z-4 | LOW | `.claude/scripts/d16-gate.sh:76-83`, `:32-37` | Name derivation splits only on `_`, `-`, space and on lower→Upper: a folder name with `.` or `+`, or an uppercase-run CamelCase (acronym prefix), derives no working variants or parts (snake, spaced and part forms read 0). Neither fails closed nor appears in KNOWN LIMITS. Latent: 0 of 3 real folders. | open |
| Z-5 | LOW | `.claude/scripts/d16-gate.sh:32-37` | Content transformed before it reaches grep — gzip, base64, a deflated `.docx`, an HTML entity inside a part — reads 0 hits (plain text: 2) and is not a declared KNOWN LIMIT. Latent: no tracked binary/encoded files today. | open |
| Z-6 | LOW | `.claude/scripts/d16-gate.sh` selftest (`:227-310`) vs `.claude/commands/maintenance.md:475` | No selftest case for a regex-metacharacter folder name, a missing `projects/` or a bad revision range, although the batch's own rule requires a case for every form the gate is taught; those negations rest on unpersisted verifier seeds. | open |
| Z-7 | LOW | `.claude/scripts/d16-gate.sh:129-133`, `:310` | GREEN has no floor on units scanned: `0 commits scanned` / `0 files scanned` exits 0; `log HEAD..HEAD`, a mistyped receipt range, or `GIT_INDEX_FILE` pointing at an empty index turn a leak (2 hits) into `0 hits`; the selftest treats an empty index as a GREEN control. | open |
| Z-8 | LOW | `.claude/scripts/d16-gate.sh:36-37`; `.claude/commands/audit.md:317` | `log --all` reads no ref NAMES: a branch name, a lightweight tag name, and a lightweight tag pointing at a leaking blob each read 0. KNOWN LIMITS mentions tag objects only. Real ref names are clean (17 lines, 0 hits). | open |
| Z-9 | LOW | `.claude/commands/maintenance.md:204`, `:432`, `:454-456`; `audit-2026-09-14.md` receipts `isolation:` | A receipt that lives in a commit body cannot run the mandated `log <first>~1..<last>` before the commit exists: 6b4baa8 and 30c3d6e discharged with the `staged` form and a file count, the unit Y-11 removed. The persisted report receipt paraphrases the gate line instead of pasting it. | open |
| Z-10 | LOW | `.claude/commands/audit.md:1317` | Phase 3 item 6 states `GREEN 9+2=11 today` beside a command that returns `GREEN 11+2=13`: a copied run count in the defect-series section, the class Y-13 fixed two paragraphs away. | open |
| Z-11 | LOW | `.claude/commands/existing_project_adaptation.md:592-593` | The rules-fence header still claims "THREE THIS COMMAND WRITES INTO AND NEVER CREATED" for directories `:549` and `:600` create, and says "`cp` to a missing directory FAILS" over a `sed … >` fence; the Y-10 receipt's `grep 'THREE DIRECTORIES'` cannot see the reordered text. | open |
| Z-12 | LOW | `.claude/commands/maintenance.md:980-985` | The version-bump residue check expects "ZERO hits" after a bump but excludes only `created: framework-v`; the lineage sentence at `:970` ("v2.23.0 shipped continuous mode") reads 1 on a healthy state, so the Run 3 receipt narrated the exception instead of the check stating it. | open |
| Z-13 | LOW | `.claude/commands/maintenance.md:503-519` | The LATERAL "redirect with no mkdir" control mandates "BOTH HALVES" but its fence carries a command for half 2 only; half 1 is described in a comment and can never go RED. | open |
| Z-14 | LOW | `CLAUDE.md:7`; `docs/agentic_engineering_framework.md:204-222` | The inventory class was not swept: CLAUDE.md:7 "Only documentation, templates, examples, and project folders" became false with `.claude/scripts/d16-gate.sh`, and the concepts doc's framework-repo tree lists `.gitignore` but not `.gitattributes` (and has no `.claude/` block) — while the batch's `inventory sweep:` receipt names that doc as checked. | open |
| Z-15 | LOW | `.claude/commands/maintenance.md:415-417`; `.claude/commands/audit.md:232` | Two batch-written requirements are not dedicated imperative lines: the D16 gate mandate is the third clause of the fence-failure sentence, and "Extend by hand only with the D16.2 / D16.2c identifiers" is a lowercase constraint after the ALWAYS line. The commit claimed `0 buried` (the B regex cannot see either shape). | open |
| Z-16 | LOW | `.claude/commands/bootstrap.md:369-373`, `:439-442`; `existing_project_adaptation.md:553`, `:673-675`, `:838` | The 6 shipped `.sh` skill scripts (one wired as a PostToolUse hook) get no LF protection in bootstrapped or adapted projects: neither twin writes a `.gitattributes` or mentions eol, while the mother repo's new `.gitattributes` names exactly the CRLF failure and protects only its own checkout. | open |
| Z-17 | LOW | `.claude/commands/maintenance.md:458-461`; `.claude/commands/audit.md` (0 `selftest` references) | The gate's negation proof runs only in a session that edits the script; `/audit` D16 runs the gate but never its selftest, so git/grep drift after the edit (the Y-19 class) goes unproven between edits. | open |
| Z-18 | LOW | `.claude/commands/audit.md:175-192` (Agent 1 FILES TO READ) | D16.1 (`:229`) and D16.3c (`:317`) name `.claude/scripts/d16-gate.sh`, but Agent 1's FILES TO READ does not list it — the K-15/L-3 class; this run supplied it from the dispatch prompt. | open |
| Z-19 | LOW | `.claude/commands/audit.md` D17.6 command | The shipped/apparatus pathspecs cover neither `README.md`, `docs/agentic_engineering_framework.md` nor `.gitattributes`: 17 lines of this batch, all of d156dc4, are counted nowhere. Extends Y-16. | open |
| Z-20 | MEDIUM | `.claude/commands/existing_project_adaptation.md` Step 2.9 / 2.9b (first draft); `.claude/scripts/d16-gate.sh` D16_EXTRA | [filed by the maintenance session applying Run 1, on 2026-09-15, from its independent pre-commit verifier] Round 1 over the draft: `refresh_agent` onto a directory, rules/guard fences reading DIFFERS for a directory target, `refresh_skill` running `rm -rf` before checking a template exists (empty and template-less names wiped project data), the in-fence name guard presented as stopping injection it cannot stop, silent Step 2.9b metrics/ops copies, a stale guard slot with no `refresh_guard`, and D16_EXTRA reading 0 over a BOM, UTF-16 or padded file. Fixed before commit. | applied `safa8ef0` |
| Z-21 | MEDIUM | `.claude/commands/existing_project_adaptation.md` Step 2.9 / 2.9b (second draft); `.claude/scripts/d16-gate.sh` D16_EXTRA | [filed by the maintenance session applying Run 1, on 2026-09-15, from its independent pre-commit verifier] Round 2 over the fixed draft: `refresh_skill ../agents` deleted `.claude/agents/` (path traversal), Step 2.9b FAILED lines had no STOP rule or slot, a 0-byte metrics.md was kept forever, D16_EXTRA read 0 over a later-line BOM, CR-only endings and empty pattern files, a `^` pattern made every line a hit, `cmp`/`diff` errors read DIFFERS, and the 2.9b report slot could not receive the new verdicts. Fixed before commit; the gate cases are selftest cases. | applied `safa8ef0` |

**Carried forward from `audit-2026-09-14.md` (re-verified on disk, all still true):** Y-8, Y-9, Y-12, Y-14 (recurred), Y-15, Y-16 · X-2, X-3, X-4, X-5, X-7, X-8, X-9, X-10, X-11, X-12, X-13, X-15, X-16 (recurred), X-17, X-18, X-19, X-23 · W-14, W-15, W-17, W-18, W-19, W-21, W-22, W-23, W-24, W-25, W-26, W-27, W-28, W-29, W-31. Their ledger rows stand unchanged in that file with status `open`.
**Observation, not a finding (Agent 5, low confidence):** the batch was bumped PATCH as "a mother-repo-only control"; `maintenance.md` "Version bumps" scopes MINOR to "a new rule/section/template … or a schema change to a document projects receive", and the qualifier binds only the schema clause. The policy neither supports nor excludes PATCH for new mother-repo rules.

## Recommended Fixes

1. **Quick fixes:** Z-10, Z-11, Z-12, Z-14, Z-15, Z-18.
2. **Structural fixes:**
   - **Z-1** — make the push gate scan exactly what the push command publishes (the pushed ref's range, plus tags when tags can travel).
   - **Z-3** — route the D16.3c hand-scan half through the same NUL-stripped object reads, or state it as a limit.
   - Z-16, Z-13, Z-9; then the carried delivery-path items Y-8, Y-9, X-2, X-3, X-4, X-5, X-23, W-14, W-17, W-18, W-19.
3. **Process controls:** **Z-2** (give the selftest an oracle independent of `build()`), Z-6, Z-7, Z-17, Z-19; carried X-17, Y-15, Y-12, Y-14, Y-16, X-16, X-18.
4. **Quality improvements:** Z-4, Z-5, Z-8; carried X-7..X-13, X-15, X-19, W-15, W-21..W-29, W-31.
5. **ESCALATED — owner decision required:** none. No privacy hit on any surface.
6. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48.

## Meta-observation

`prediction: 3 clauses evaluated — void, failed, void`
- "If the Y batch patches the isolation gate case by case again, the next verification will find at least one further input class that reads 0 over a real leak" → **void**. The antecedent did not hold: the gate became one script reading git's object model, patterns via `-f`, verified by diff-only rounds.
- The falsifier — "a change of SPECIFICATION … If the next verification then finds no input class reading 0 over a real leak, this prediction fails" → **failed** (the prediction was not falsified). All three spec conditions held (object-model reads `d16-gate.sh:119-122`, `:159-165`; `-f` at `:107`; diff-only verifiers, claimed at the receipts but unpersisted), and this run still found input classes reading 0: dotted/`+` and uppercase-run folder names (Z-4), encoded content (Z-5), ref names (Z-8), a pushed ref the scan does not read (Z-1).
- "The prediction also fails if the case-by-case patch happens to leave no residue" → **void** (no case-by-case patch was made).

**The recurring class this run saw: THE CONTROL'S SCOPE IS NOT THE THING IT GATES — one layer out from where the last fix landed.** Run 3's residues all sat in the READ layer, and this batch closed that layer: not one finding this run is about how bytes are read from git (Agents 1, 4, 6 agree). The residue moved to the edges of the specification:
- **The ref layer:** the push gate reads `HEAD`'s range and publishes `main` and, under config, tags (Z-1); `log --all` reads objects but not ref names (Z-8).
- **The derivation layer:** the blocklist is still built by enumerating name shapes (`_`, `-`, space, lower→Upper) — the case-by-case method Run 3 named, one function further in (Z-4).
- **The oracle layer:** the selftest proves the reader and cannot see the derivation, because it seeds from the same derivation (Z-2).
- **The twin layer:** the gate's half was specified; the hand-scan half of the same dimension kept every blind spot (Z-3).

The rate reads 10 of 17 not clean (59%), 0 HIGH, 3 MEDIUM. The batch is **adequate as a gate** — the first isolation control that is mechanical, fails closed, and was re-run to the same number by three agents other than its author — and **inadequate as a loop**: D17.6 measures 6 shipped lines against 646 apparatus lines (≈0.9%), after 0% in Run 2's batch and 39% in Run 1's, while 11 carried findings sit on the delivery path that projects actually run (Y-8, Y-9, X-2..X-5, X-23, W-14, W-17..W-19). **The loop should be re-pointed at the shipped surface**; none of Z-4..Z-8 (all LOW, 0 of 3 real folders exposed) justifies another apparatus-first batch.

**Prediction, stated so it can fail.** If the next batch fixes Z-2/Z-4 by adding name shapes to `build()` while the selftest still derives its seeds from `build()`, the verification after it will find at least one further folder-name shape for which the selftest reads `K of K` and the gate reads 0 on a variant form. **What would falsify this:** the selftest gains a FIXED corpus of invented folder names with hand-written expected hits and misses, never produced by `build()`; if that corpus and the next verification both find no derivation gap, this prediction fails. **Second clause:** if the next batch is again under 10% shipped surface, its not-clean rate will be at least 50%; a batch that is mostly delivery-path fixes (the list above) and scores under 50% falsifies it.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings

### Agent 1: Structural Sync
- **Part 1:** Y-1, Y-2, Y-3 (named forms), Y-4, Y-6, Y-11, Y-19 CONFIRMED; Y-5 PARTIALLY-FIXED (no selftest case); Y-7 INTRODUCED (HEAD vs `main`); Y-17, Y-18 CLASS-NOT-SWEPT (D16.3c hand scans; dotted and acronym-CamelCase names). CLAUDE.md tree lines CONFIRMED (`:18`, `:31-32`; `.gitattributes` gives `i/lf w/lf attr/text eol=lf`).
- **[A] PASS** · **[D4] PASS** (15 = 15; "15 skills: 12 lifecycle + 3 tier-gated") · **[D9] PASS** (6 commands, 7 templates, 10 agents, 5 rules, 15 skills, examples 20/9/11).
- **[D16] PASS.** Blocklist: 3 folder names (variants derived by the gate) + 14 high-signal doc identifiers (plus 3,647 single-project capitalised tokens) + 110 shared source-code identifiers (30 copy paths derived, ≥5 expected); 0 session-number shapes in the batch lines. Surfaces: tracked files (`staged` 114 files, 0); `.claude/docs/` (3 files, 0); agent memory (10 files, 0; 13 hash-shaped tokens outside UUIDs/URLs, 13 resolve); history (`log --all` 194 commits, 0; high-signal 0 revision:file pairs, 0 metadata lines incl. author/committer); unreachable objects and reflog (U-48 only). Batch-added lines: 0 high-signal, 0 value shapes; 2 capitalised tokens are sentence-initial ordinary words; 1 shared code identifier is common library vocabulary. Whole tree: 1 `user:pass` placeholder in a scanner example (legitimate). Unpushed: 0 commits.
- Proposed findings → Z-1, Z-3, Z-4, Z-5, Z-6, Z-8, Z-18.

### Agent 2: Bootstrap Integrity
- **Part 1:** W-16 CONFIRMED; Y-10 PARTIALLY-FIXED.
- **`bash -n`:** bootstrap 18 fences (1 placeholder), EPA 22 fences; 0 failures at HEAD and at 6b4baa8~1.
- **[D5] PASS** (bootstrap 52 paths / EPA 49; every framework-source path resolves; D5.4 naming consistent) · **[D11] FAIL** — carried only (15 primary + 10 sub-steps = 25 plus Setup, matching the concepts doc) · **[D13] PASS** · **[D14] FAIL** — carried only (12 template references, all traced).
- No shipped surface wrongly cites the gate (`grep d16\|isolation gate\|gitattributes` over `docs/modules`, `examples`, concepts doc → 0).
- Proposed findings → Z-11, Z-16. Carried W-14, W-15, W-17, W-18, W-19, X-2, X-3, X-4, X-5, X-23, Y-8, Y-9 all still true.

### Agent 3: Activation Chain
- **Part 1:** no applied ID in D6/D7. The batch's four hunks in the command twins (`bootstrap.md:363`, EPA `:238`, `:542`, `:591-592`) sit outside the install tables (10 rows), the TWINNED SPAN (13 lines) and the loops (71 lines), all `cmp`-identical to 6b4baa8~1 and to each other.
- **[D6] PASS** — 10 gaps, exact phrase matches, install rows in both twins; declarers extracted 3 of 3; fourth surface names 3; whole-block diff rc=1 is W-24's known structural state.
- **[D7] PASS** — 30 agents; `docs/modules/agents` PARTIALs sanctioned; 20 examples COMPLIANT. `activation chains: 10 verified, 0 broken, 17 info` in both twins, `twins agree: yes` (full outputs `cmp`-identical over 17 runs); regression rows 1-14 plus 13b as expected; planted probe names `widget-telemetry-auditor`; invariant `files: 10 | output: 0`. Synthetic project in `mktemp -d`, deleted; `git status --porcelain` unchanged.
- Carried W-24, W-28, W-31 still true. No new findings.

### Agent 4: Orchestration & Commands
- **Part 1:** Y-1, Y-2, Y-3, Y-6, W-20, Y-17, Y-18, Y-19 CONFIRMED; Y-4, Y-5, Y-13 CLASS-NOT-SWEPT; Y-7 CLASS-NOT-SWEPT + INTRODUCED; Y-11 PARTIALLY-FIXED.
- **[D8] PASS** — every skill/agent name resolves (15 folders, 10 agents); 20 §heading citations exact or sanctioned.
- **[D10] FAIL.** D10.0: 44 self-checks executed. Mismatches: push decay (legitimate, pushed), push-gate snippet (Z-1), LATERAL half 1 has no command (Z-13), Gate 3 whole-file 20 vs 17 (Y-12), version residue fires on lineage text (Z-12), defect-series stated figure stale (Z-10); copy-path derivation 30 vs ≥5 (W-23). D10.4 byte-identical; D10.6 trigger parity yes.
- **Receipts re-run:** every `$` line of the Run 3 batch section reproduces except the time-dependent `push decay` and `push`. Format defects recur as Y-14: 31 of 51 `✓` cells quote no command; the `liveness:` `$` line is a diff grep, not the mandated guard; `control back-sweep:` reads "2 of 66"; `push:` carries a trailing clause.
- Carried Y-12, Y-15, X-7, X-8, X-11, X-15, W-21, W-22, W-23, W-25, W-26 still true; Y-14, X-16 recurred.

### Agent 5: Document Accuracy
- **Part 1:** W-30, X-14 CONFIRMED; v2.23.1 stamps CONFIRMED (6 of 6); tree additions CLASS-NOT-SWEPT (concepts doc); CLAUDE.md:7 INTRODUCED (Z-14); new normative text PARTIALLY compliant (Z-15); `isolation:` slot matches the script, the persisted receipt paraphrases it (Z-9).
- **[C] FAIL** — 2 new burials; X-12, W-27, W-29 still true. **[E] PASS** — 0 mismatches; `maintenance.md:970`, `:961-963`, `:872`, EPA `:262`, `:266`, skills README `:35`, autonomous-loop `:21`, `:28`, `:246`, sprint-proposer `:277` are lineage or illustrative; `commit/SKILL.md:10` `created:` not rewritten. **[D12] PASS** — 20 agents, 9 skills, 11 rules meet every convention. **[D15] FAIL** — 2 inaccuracies (Z-14); every count claim (6 commands, 15 skills = 12 + 3, 7 user / 8 inline, 10 agents, 7 templates, 5 rules, examples 20/9/11, 15-step pipeline, Steps 1.5 and 12.5) accurate; W-28 carried.

### Agent 6: Process Coverage (meta)
- **Part 1:** Y-1, Y-2, Y-4, Y-5, Y-7 (HEAD/main residue noted), Y-11, Y-19 CONFIRMED; Y-3, Y-6, Y-17, Y-18 CLASS-NOT-SWEPT.
- **D17.1:** 40 commits → audit-fix application 17, audit persist 6, write-back 6, errata/correction 5, maintenance correction 4, feature 2 (bumps embedded in 4 fixes). Undocumented executed flows: diff-only verifier rounds (2 sessions, 4 rounds; X-17, Y-15); script-generated receipts (3 executions, generator never persisted; Y-15); push after write-back with no persisted gate over the write-back commit (8644903; X-16's third occurrence).
- **D17.2:** none new. **D17.3:** "NEGATION-PROVE THE GATE WITH ITS OWN SELFTEST" is owned only by sessions that edit the script (Z-17).
- **D17.4:** `staged`, `log`, push snippet — RED and GREEN reachable, but GREEN on 0 units (Z-7); `selftest` RED reachable for a broken reader, blind to derivation (Z-2); green controls unprovable under a changed folder set.
- **D17.5:** `isolation:` gate — yes (reproduced); push gate — partial (8644903 ungated in-session); selftest — weak (self-referential oracle, no independent runner); verifier rounds — no (unpersisted); receipt generator — no (scratchpad); `/audit` D16 — yes.
- **D17.6:**

  | Batch | Range | Shipped (`docs/modules` + `examples`) | Apparatus (`.claude`, `assets/docs`, `CLAUDE.md`) | In neither set | Shipped share |
  |---|---|---|---|---|---|
  | Run 1 | ecace7a~1..12afa4a | 143 | 223 | 0 | 39% |
  | Run 2 | 700a382~1..8f190b9 | 0 | 208 | 0 | 0% |
  | Run 3 | 6b4baa8~1..8644903 | 6 | 646 (script 324, write-back 195, delivery commands 9) | 17 | ≈0.9% |

  **Judgement:** adequate as a gate; the loop is not converging on what projects receive — re-point it at the shipped surface. Proposed findings → Z-2, Z-4, Z-5, Z-7, Z-17, Z-19. Carried X-17, X-18, Y-15, Y-16 still true; X-16 recurred.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions and Part 1 (17 of 17 applied IDs verdicted) evaluated; 19 new findings (0 HIGH, 3 MEDIUM, 16 LOW) + 38 carried.
**defect series:** row 14 appended to the one table in `.claude/commands/audit.md` — 10 of 17; no other surface touched.
**Application status:** `PARTIAL — 15 of 59 applied in safa8ef0`

## Post-change checklist receipts (`safa8ef0`)

Applies Z-1, Z-3 of this report and the carried Y-8, Y-9, X-2, X-3, X-4, X-5, X-23, W-14, W-17, W-18, W-19 (their ledger rows live in `audit-2026-09-14.md` and were written back there), plus Z-20, Z-21 filed by this session. Owner scope decision (2026-09-15): the two MEDIUM gate findings plus the delivery path; everything else stays open.
Every `$` line in the generated keys was EXECUTED by `gen-receipts-2.sh` in this session's scratchpad and its stdout pasted; the generator is NOT persisted (Y-15, open). The per-fix TABLE and the judgement keys are typed by the author; every command they quote is re-runnable.
Before commit, two diff-only verifier rounds chose their own cases in scratch mother repos: round 1 found 7 defects in the draft (Z-20), round 2 confirmed 6 of 7 fixed plus the Step 2.8 change, found item 5 half-fixed and 6 new defects (Z-21). All were fixed before `afa8ef0`; the helper fixes were then probed by the author in a scratch mother repo.

**Not applied — 44 open, deferred by the owner's scope decision:** Z-2, Z-4, Z-5, Z-6, Z-7, Z-8, Z-9, Z-10, Z-11, Z-12, Z-13, Z-14, Z-15, Z-16, Z-17, Z-18, Z-19 · Y-12, Y-14, Y-15, Y-16 · X-7, X-8, X-9, X-10, X-11, X-12, X-13, X-15, X-16, X-17, X-18, X-19 · W-15, W-21, W-22, W-23, W-24, W-25, W-26, W-27, W-28, W-29, W-31.

**push decay:** origin/main unchanged (6a37653); unpushed at receipt time: afa8ef0
    $ git status -sb | head -1; git log -1 --format=%h origin/main
    ## main...origin/main [ahead 1]
    6a37653

**inventory sweep:** N/A — no artifact added, removed, renamed or split (helpers are functions inside existing fences; the gate gained options, not a file)
    $ git show --name-status --format= afa8ef0 | grep -cE "^[ADR]"
    0

**instruction style:** unit: a diff line carrying a CAPS imperative; range afa8ef0~1..afa8ef0
    $ D='git diff afa8ef0~1..afa8ef0 -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    33 added, 3 rewritten, 0 buried

**references:** the distinct citation targets below all resolve (each is an existing file or an audit.md D-item); the regex stays blind to quoted-heading citations (W-22, open)
    $ git diff afa8ef0~1..afa8ef0 | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | tr '\n' ' '; echo; grep -cE '^  D16\.(2|2b|2c|3c)\. ' .claude/commands/audit.md
    `bootstrap.md` `code-reviewer.md` `docs/modules/templates/pendencias_md.md` `existing_project_adaptation.md` `pendencias.md` D16.2 D16.2b D16.2c D16.3c 
    4

**fences:** 12 of 12 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE "^\`{3,5}[a-z]*" "$t"); c=${f%%[a-z]*}; sed -n "/^$f$/,/^$c$/p" "$t" | wc -l; done | awk "{n++; if(\$1>0)k++} END{print k\" of \"n\" non-empty\"}"
    12 of 12 non-empty

**isolation:** D16 log over the batch, 0 hits
    $ bash .claude/scripts/d16-gate.sh log afa8ef0~1..afa8ef0
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**liveness:** skipped — no frontmatter touched
    $ git diff afa8ef0~1..afa8ef0 | grep -cE '^[-+](name|description):'
    0

**negation proof:** the gate's selftest reads RED on every seeded form (including the 11 new D16_EXTRA / D16_EXTRA_RE cases) and GREEN on every control; the EPA helper fixes were negation-probed in a scratch mother repo (traversal, empty and template-less names FAILED with nothing removed; a 0-byte metrics.md replaced), command in the session transcript
    $ bash .claude/scripts/d16-gate.sh selftest 2>/dev/null | tail -1
    selftest: 106 of 106 cases as expected

**version:** v2.23.2 applied to 6 of 6 stamps across the 4 canonical surfaces; old-label residue 0
    $ grep -rn "v2\.23\.2" README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l; grep -rn "v2\.23\.1" README.md docs/ .claude/ CLAUDE.md | grep -v "created: framework-v" | wc -l
    6
    0

**control back-sweep:** 2 controls amended (the push-gate snippet; the gate's input surface); both re-run with negation — push gate by verifier rounds 1-2 (HEAD on another branch, followTags, missing origin/main), gate by its selftest; the other controls were NOT re-run; baseline re-measured below
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md; do inf=$(awk "/^\`\`\`/{g=!g;next} g" "$f" | grep -cE "^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )"); inl=$(grep -ohE "\`(grep|git|diff|awk|sed|node|python|ls) [^\`]{4,}\`" "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 5 fenced + 27 inline = 32
    .claude/commands/audit.md: 7 fenced + 28 inline = 35

**applied-proof:** 15 of 15 IDs resolve to afa8ef0 (form A on pre-batch text: 11; form A on text the fix added, for additive fixes: 4 — Y-9, W-17, Z-20, Z-21), 0 forced to open
    $ for x in 'Z-1|.claude/commands/maintenance.md|log origin/main..HEAD)   # ONE line' 'Z-3|.claude/commands/audit.md|- Contents: `git grep -I -c -E' 'Y-8|.claude/commands/existing_project_adaptation.md|mkdir -p projects/$ARGUMENTS/.claude/rules projects/' 'Y-9|.claude/commands/existing_project_adaptation.md|copy failures: none' 'X-2|.claude/commands/existing_project_adaptation.md|**Verify lineage fields** on all agents/skills:' 'X-3|.claude/commands/existing_project_adaptation.md|[ ! -d "$PROJ/.claude/skills/codebase-audit" ] && cp -r' 'X-4|.claude/commands/existing_project_adaptation.md|ASK the owner, per skill, whether to refresh it' 'X-5|.claude/commands/existing_project_adaptation.md|[refreshed as a set | kept as a set | not installed]' 'X-23|.claude/commands/existing_project_adaptation.md|  if [ ! -f "projects/$ARGUMENTS/.claude/agents/$dest_name.md" ]; then' 'W-14|.claude/commands/bootstrap.md|note in `code-reviewer.md` that red-team is' 'W-17|.claude/commands/existing_project_adaptation.md|SPECIFIED HERE AND WRITTEN IN STEP 4.2' 'W-18|.claude/commands/bootstrap.md|- [or `none — PRD has no Cross-cutting Concerns section`]' 'W-19|.claude/commands/existing_project_adaptation.md|### MCPs: [list with status]' 'Z-20|.claude/commands/existing_project_adaptation.md|refresh_extract()' 'Z-21|.claude/commands/existing_project_adaptation.md|is not a skill name — nothing removed'; do id=${x%%|*}; r=${x#*|}; f=${r%%|*}; a=${r#*|}; echo "$id $(git log -S"$a" --format=%h afa8ef0~1..afa8ef0 -- "$f" | tr '\n' ' ')"; done
    Z-1 afa8ef0 
    Z-3 afa8ef0 
    Y-8 afa8ef0 
    Y-9 afa8ef0 
    X-2 afa8ef0 
    X-3 afa8ef0 
    X-4 afa8ef0 
    X-5 afa8ef0 
    X-23 afa8ef0 
    W-14 afa8ef0 
    W-17 afa8ef0 
    W-18 afa8ef0 
    W-19 afa8ef0 
    Z-20 afa8ef0 
    Z-21 afa8ef0 

**gates:** twin parity PASS | receipt-rows-vs-diff 15/15 (every row's file is in afa8ef0's list) | row-count below | slots 2/2 (copy failures:, MCP: — both slotted)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis; echo "twin rc=$?"; git show --name-only --format= afa8ef0 | tr '\n' ' '; echo; git diff afa8ef0~1..afa8ef0 -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:'; sed -n '/^\*\*Step 5.2 — Produce the adaptation report:\*\*/,$p' .claude/commands/existing_project_adaptation.md | grep -cE '^### (Copy failures|MCPs) \('
    twin rc=0
    .claude/commands/audit.md .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md .claude/commands/maintenance.md .claude/scripts/d16-gate.sh README.md docs/modules/templates/claude_md.md 
    ALWAYS REPORT — `copy failures:
    ALWAYS REPORT — `MCP:
    2

**placeholder:** none
    $ cat assets/docs/audit-2026-09-14.md assets/docs/audit-2026-09-15.md | grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' | wc -l
    0

**class sweep:** 15 fixes × 4 directions, 6 extra instances found and fixed (bootstrap's paraphrased MCP slot; EPA's missing money-path note; the stale guard slot; Step 2.9b's four silent template copies; the 2.9 skills/agents loops folding a compare error into DIFFERS; a 0-byte metrics leftover never repaired), 1 class left by precondition (the unquoted `projects/$ARGUMENTS` expansions, covered by the Setup name guard, not edited line by line)
    $ grep -c "READING THE NAME BEFORE RUNNING ANY FENCE" .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md; grep -c "not installed]" .claude/commands/existing_project_adaptation.md; grep -c "per skill, whether to refresh" .claude/commands/existing_project_adaptation.md; grep -c "PRD has no Cross-cutting Concerns section" .claude/commands/bootstrap.md
    .claude/commands/bootstrap.md:1
    .claude/commands/existing_project_adaptation.md:1
    0
    0
    0

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| Z-1 | safa8ef0 | .claude/commands/maintenance.md | ✓ `grep -rn "origin/main..HEAD" .claude/commands CLAUDE.md` → quotations of the old form only | ✓ `.claude/scripts/d16-gate.sh` KNOWN LIMITS rewritten in the same commit | ✓ the `=`-compare line kept on its own line | ✓ this section's `push:` key runs `log origin/main..main` | none |
| Z-3 | safa8ef0 | .claude/commands/audit.md | ✓ `grep -rn "git grep -I" .claude/commands` → only the NEVER line quoting it | ✓ D16.1 now routes the identifiers through `D16_EXTRA` | ✓ the D16.3c reachability bullets kept | ✓ the gate selftest gained 11 `D16_EXTRA` / `D16_EXTRA_RE` cases | none |
| Y-8 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "READING THE NAME BEFORE RUNNING ANY FENCE" .claude/commands/bootstrap.md` → 1 | ✓ bootstrap Setup carries the same prose guard | ✓ the refresh helpers validate names before `rm -rf` | ✓ the Step 2.9 backstop prints `FAILED` | the unquoted expansions (class left by precondition, see the total) |
| Y-9 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "FAILED to" .claude/commands/existing_project_adaptation.md` — every emitter sits under the STOP rule | n/a — bootstrap's copies print no verdicts | ✓ the STOP rule extended to Step 2.9b | ✓ slot `Copy failures (Steps 2.9 / 2.9b)` | Step 2.9b's FAILED lines had no owner (Z-21) |
| X-2 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "Verify lineage fields" .claude/commands/existing_project_adaptation.md` → the one rule | n/a — bootstrap installs fresh copies | ✓ the flat→folder migration below left untouched | n/a — no report field | the rule named all 10 agents; narrowed to the 4 compared (Z-20) |
| X-3 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "] && cp" .claude/commands/existing_project_adaptation.md` → 0 | n/a — bootstrap copies into a fresh project | ✓ metrics / framework-metrics / ops-rules / quality-budgets moved to `tier_tmpl` | ✓ the MACRO skeleton slot copies the helpers' verdicts | 4 silent `sed` copies (Z-20); a 0-byte leftover (Z-21) |
| X-4 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "per skill, whether to refresh" .claude/commands/existing_project_adaptation.md` → 0 | ✓ `autonomous-loop` C1 "refreshes these writers as one set" is now executable | ✓ refresh helpers, one-answer rule, rules verdicts, `origin:` legend in Step 2.3 | ✓ rules and guard slots take the verdicts | path traversal in `refresh_skill` (Z-21) |
| X-5 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "not installed]" .claude/commands/existing_project_adaptation.md` → 0 | n/a — bootstrap has no coupled-set slot | ✓ agents and rules slots copied from their loops | ✓ the guard slot (Z-20) | the stale guard slot (Z-20) |
| X-23 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "a directory occupies the target" .claude/commands/existing_project_adaptation.md` — agents loop, `tier_agent`, `refresh_agent`, `refresh_extract` | n/a | ✓ rules and guard fences check `-f` before comparing | n/a | the same class in three helpers (Z-20) |
| W-14 | safa8ef0 | .claude/commands/bootstrap.md | ✓ `grep -rn "money path" .claude/commands` → bootstrap Step 7 and EPA Step 2.9b | ✓ EPA carried no such note — added | ✓ the Step 5.8 production-financial paragraph | ✓ the Step 15 MACRO slot line "red-team mandatory on money-paths" unchanged | EPA's missing note |
| W-17 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "SPECIFIED HERE AND WRITTEN IN STEP 4.2" .claude/commands/existing_project_adaptation.md` | n/a | ✓ the Step 4.2 receiver sentence | n/a — no report field | none |
| W-18 | safa8ef0 | .claude/commands/bootstrap.md | ✓ `grep -c "PRD has no Cross-cutting Concerns section" .claude/commands/bootstrap.md` → 0 | ✓ EPA's cross-cutting slot already carried its own step's verdicts | ✓ the MCP slot enumeration copied from Step 5 (W-19 lateral) | ✓ Files created, Risk profile and Specialist slots | bootstrap's paraphrased MCP slot |
| W-19 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -rn "none installed — placeholder replaced" .claude/commands` → mandate and slot in both twins | ✓ bootstrap slot made identical to its mandate | ✓ the Hooks mandate in Step 4.2 | ✓ both slots headed "ALWAYS report, never omit" | none |
| Z-20 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "refresh_extract" .claude/commands/existing_project_adaptation.md` — one extraction helper for rule and guard | n/a | ✓ `.claude/scripts/d16-gate.sh` extras normalised in the same commit | ✓ selftest cases for BOM, padding and UTF-16 | none |
| Z-21 | safa8ef0 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c "FAILED to compare" .claude/commands/existing_project_adaptation.md` — every compare distinguishes exit 2 | n/a | ✓ the MACRO slot receives the 2.9b verdicts | ✓ selftest cases for a later-line BOM, CR-only endings, an empty RE file and a `^` pattern | none |

**back-sweep:** 2 rules promoted, residue re-measured below; 1 exception named
    $ for r in afa8ef0~1 afa8ef0; do echo "$r: silent-or-folded copies in EPA $(git show $r:.claude/commands/existing_project_adaptation.md | grep -cE 'elif (cmp -s|diff -rq)|\] && (cp|sed) ') · in bootstrap $(git show $r:.claude/commands/bootstrap.md | grep -cE 'elif (cmp -s|diff -rq)|\] && (cp|sed) ') · name guards $(git show $r:.claude/commands/bootstrap.md | grep -c 'READING THE NAME BEFORE RUNNING ANY FENCE')+$(git show $r:.claude/commands/existing_project_adaptation.md | grep -c 'READING THE NAME BEFORE RUNNING ANY FENCE')"; done
    afa8ef0~1: silent-or-folded copies in EPA 10 · in bootstrap 0 · name guards 0+0
    afa8ef0: silent-or-folded copies in EPA 0 · in bootstrap 0 · name guards 1+1

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| A copy verdict follows a successful non-empty write, and a compare error is `FAILED`, never `DIFFERS` | a boolean `elif cmp -s` / `elif diff -rq`, or a silent `] && cp` / `] && sed` copy | the first count of the `$` line above, per twin | 10 | 10 | 0 remaining |
| The project name is refused by reading it before any fence | a command Setup with no name guard | the last two counts of the `$` line above | 2 | 2 | 0 remaining (both twins carry it) |

Exception: bootstrap's own `sed … >` extractions into a FRESH project print no verdict at all; they are not a boolean compare and were not in this batch's scope — named here, not claimed.

**classification:** 9 add (the Setup name guards, the STOP rule, the refresh and tier helpers, the `origin:` legend rule, the Step 4.2 receiver, the Step 7 money-path line, the EPA money-path line, the `D16_EXTRA` options, the Z-1 rationale), 15 substitute (the push snippet, KNOWN LIMITS, the D16.3c bullets, the rules / guard / agents / skills compares, the Step 2.8 rule, the DIFFERS ask, the Step 2.9b copies, the report slots in both twins — each strictly more correct, no information lost), 0 delete, 1 relocate (the red-team-on-money-paths note, from bootstrap Step 5.8 to Step 7; the source now points and restates nothing)
    $ n/a — judgement, not measurement

**new component:** none this session
    $ n/a — judgement, not measurement

**report deletions:** none — 0 structural decreases across 2 report files
    $ for f in $(git diff --cached --name-only -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:'; do b=$(git show HEAD:"$f" 2>/dev/null | grep -cE "$pat"); if [ -f "$f" ]; then a=$(grep -cE "$pat" "$f"); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat"; done; done | wc -l
    0

**report restore:** none
    $ echo "n/a — no destroyed report"
    n/a — no destroyed report

**commit correction:** none
    $ echo "n/a — no false claim in a made commit"
    n/a — no false claim in a made commit

**audit:** N/A — PATCH bump (v2.23.2); trigger (b) does not apply
    $ echo "n/a — PATCH"
    n/a — PATCH

**verification audit:** proposed — trigger (d), in this session's closing report
    $ echo "n/a — owner decision"
    n/a — owner decision

**defect series:** N/A — maintenance session, no row
    $ echo "n/a — maintenance session"
    n/a — maintenance session

**push:** requested this session (owner: "Pode fazer push e executar o maintenance" covered the audit report `6a37653`, pushed); not requested for this batch — D16 over the pushed ref's unpushed range at receipt time:
    $ bash .claude/scripts/d16-gate.sh log origin/main..main
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits
