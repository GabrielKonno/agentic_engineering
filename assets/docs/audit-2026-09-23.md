# Framework maintenance receipts — 2026-09-23
> **Errata 1 (added by the 2026-09-23 `/audit` Run 1, finding AF-1).** An audit DID run this day: `# Run 1` at the end of this file, a baseline with a full findings ledger. The `receipts-only` header below is superseded so that the readers that skip such a file (`/maintenance` Audit intake step 1, `/audit` Phase 3 item 3) read this file's ledger instead of falling back to `audit-2026-09-22.md`. The receipts above Run 1 are unchanged.
*(superseded)* **Run mode:** receipts-only — no audit ran this day
**Run mode:** `baseline` — the audit is `# Run 1` below; the receipts section above predates it

Batch: `2cb4ede` — upstream absorption of the multi-agent reporting pilot, v2.33.0. Lineage: `assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md`. Open finding filed by this batch: `AE-15`, in the ledger of `assets/docs/audit-2026-09-22.md`.

## Post-change checklist receipts (`s2cb4ede`)

**inventory sweep:** N/A — no artifact added, removed, renamed or split, and no user-visible capability added (0 A/D/R paths under the shipped roots; the one new file is a lineage record under `assets/docs/`). Surfaces checked for stale counts anyway: CLAUDE.md, README.md, docs/modules/README.md, docs/agentic_engineering_framework.md, examples/README.md, this command, the twin — 0 stale claims (no count changed).
    $ git diff --name-status --diff-filter=ADR 2cb4ede~1 2cb4ede -- docs/modules .claude/commands .claude/skills .claude/rules .claude/scripts examples | wc -l
    0

**instruction style:** 10 added, 0 rewritten, 0 buried — range `2cb4ede~1..2cb4ede`; unit: an added line carrying a CAPS imperative a session must execute. (Pre-fix the index read 2 buried, both §8 rule 4; fixed before commit.)
    $ D='git diff 2cb4ede~1..2cb4ede -U0'; echo "instruction style: $($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    instruction style: 10 added, 0 rewritten, 0 buried

**references:** 10 of 11 cited targets resolve in their tier — the 9 tracked paths plus `framework-evolution-2026-09-23-business-team-pilot.md`, which resolves in the PROJECT tier (the source doc, cited only by the lineage record); the 11th, `team.md`, is named as the Phase 3 manifest that does not exist yet, never as a reference
    $ git diff 2cb4ede~1..2cb4ede | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l
    11
    $ git diff 2cb4ede~1..2cb4ede | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | tr -d '`' | while read r; do case "$r" in *.md) n=$(git ls-files | grep -c "$(basename "$r")"); echo "$r -> tracked matches $n";; *) echo "$r -> section cite";; esac; done
    .claude/rules/component-design.md -> tracked matches 1
    docs/modules/agents/security_reviewer.md -> tracked matches 1
    docs/modules/rules/component_design.md -> tracked matches 1
    docs/modules/rules/evolution_policy.md -> tracked matches 1
    docs/modules/rules/ops_rules.md -> tracked matches 1
    docs/modules/rules/session_rules.md -> tracked matches 1
    docs/modules/skills/validation-orchestrator/SKILL.md -> tracked matches 25
    existing_project_adaptation.md -> tracked matches 1
    framework-evolution-2026-09-23-business-team-pilot.md -> tracked matches 0
    team.md -> tracked matches 2
    component-design §9 -> section cite

**fences:** 13 of 13 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; echo "$t -> $(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l) lines"; done
    docs/modules/templates/check_agent_frontmatter.md -> 218 lines
    docs/modules/templates/check_rules_paths.md -> 164 lines
    docs/modules/templates/claude_md.md -> 92 lines
    docs/modules/templates/framework_metrics_md.md -> 28 lines
    docs/modules/templates/metrics_md.md -> 13 lines
    docs/modules/templates/pendencias_md.md -> 37 lines
    docs/modules/templates/project_md.md -> 31 lines
    docs/modules/templates/settings_json.md -> 40 lines
    docs/modules/rules/component_design.md -> 368 lines
    docs/modules/rules/evolution_policy.md -> 172 lines
    docs/modules/rules/ops_rules.md -> 59 lines
    docs/modules/rules/quality_budgets.md -> 28 lines
    docs/modules/rules/session_rules.md -> 339 lines

**isolation:** D16 log (patches + messages + paths): 1 commits scanned, 0 hits
    $ bash .claude/scripts/d16-gate.sh log 2cb4ede~1..2cb4ede
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**class sweep:** N/A — no point fixes this session: no audit or owner finding was applied (0 IDs in an `Applies` line). The verifier's 10 defects were fixed inside the uncommitted batch and are receipted under `verifier rounds:`.
    $ git log -1 --format=%B 2cb4ede | awk '/^Applies /{f=1} f&&/^$/{exit} f' | grep -oE '\b[A-Z]{1,2}-[0-9]+\b' | sort -u | wc -l
    0

**back-sweep:** 3 rules, 0 artifacts fixed of 0 found, re-run clean — table below. The first grep's 2 raw hits are both the wrapped second line of an existing PROHIBITION (`never a silent / fallback to a general-purpose agent`, component_design.md:270 and autonomous-loop SKILL.md:417); the pre-batch tree returns the same 2. The third row is a PRESENCE grep and measures nothing: the snapshot rule condemns project reconciliation queries, and none ships (`Stack content: [fill]`).
    $ git grep -niE '(spawn|use|fall ?back to) (a )?general-purpose' 2cb4ede -- docs/modules .claude/commands | grep -viE 'never|not |no silent|instead of' | wc -l
    2
    $ git grep -niE '(summari[sz]e|translate|condense) (the|its) (review|reviewer|subagent)' 2cb4ede -- docs/modules | wc -l
    0
    $ git grep -niE 'snapshot' 2cb4ede -- docs/modules/rules/ops_rules.md | wc -l
    1

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| component-design §8 rule 4 — no general-purpose fallback on a same-session `not found` | an instruction permitting that fallback | the first `$` line above | 0 (2 raw, both a wrapped prohibition; pre-batch also 2) | 0 | 0 remaining |
| validation-orchestrator — verbatim means untransformed | an instruction to summarize/translate a reviewer's report | the second `$` line above | 0 | 0 | 0 remaining |
| ops-rules §6 — snapshot oracle reserved to the checker | a shipped reconciliation query reading the snapshot | the third `$` line above (PRESENCE — measures nothing; no query ships) | 0 | 0 | 0 remaining |

**liveness:** 1 component OK [structural-only — js-yaml absent] over this repo's `.claude/`, plus raw templates 19 validated of 25 globbed, 6 skipped (fenced), OK
    $ sed -n '/^````js$/,/^````$/p' docs/modules/templates/check_agent_frontmatter.md | sed '1d;$d' > "$TMP/guard.mjs" && node "$TMP/guard.mjs" | tail -3
    ✓ check-agent-frontmatter: 1 componente(s) OK (frontmatter parseável, name casa o arquivo) [modo estrutural — js-yaml ausente].
    $ python -c "import textwrap; exec(textwrap.dedent('''
      import io,yaml,glob,os,sys
      bad=0; n=0; sk=0
      for p in sorted(glob.glob('docs/modules/skills/*/SKILL.md'))+sorted(glob.glob('docs/modules/agents/*.md')):
          s=io.open(p,encoding='utf-8').read().replace(chr(13)+chr(10),chr(10))
          if not s.startswith('---'): sk+=1; continue
          n+=1
          try:
              d=yaml.safe_load(s[4:s.index(chr(10)+'---',4)+1])
              exp=os.path.basename(os.path.dirname(p)) if p.endswith('SKILL.md') else os.path.basename(p)[:-3].replace('_','-')
              if d.get('name')!=exp: bad+=1; print('FAIL',p,'name=',d.get('name'),'expected',exp)
          except Exception as e: bad+=1; print('FAIL',p,type(e).__name__)
      print('raw templates: %d validated of %d globbed, %d skipped (fenced),' % (n, n+sk, sk), 'OK' if not bad else str(bad)+' BROKEN'); sys.exit(1 if bad else 0)
      '''))"
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** N/A — no new self-check written (0 added command lines in the shipped/command diff)
    $ git diff 2cb4ede~1..2cb4ede -- .claude/commands docs/modules .claude/rules | grep -E '^\+' | grep -cE '^\+ *(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_]+ in )'
    0

**version:** v2.33.0 applied to 6/6 enumerated stamp locations (README x2, claude_md, audit.md, EPA x2) + the new EPA migration heading = 7 hits; residue 1 = `existing_project_adaptation.md:711`, READ: inside the v2.33.0 migration note body (lineage, the known residual)
    $ grep -rn 'v2\.33\.0' README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l
    7
    $ grep -rn 'v2.32.0' README.md docs/ .claude/ CLAUDE.md 2>/dev/null | grep -v 'created: framework-v' | grep -vE 'v[0-9.]+ migration|pre-v[0-9.]+' | wc -l
    1

**classification:** 10 add, 2 substitute, 0 delete, 0 relocate — ADD: six template additions, the §8 rule 4 mirror in this repo's own `.claude/rules/component-design.md`, the EPA v2.33.0 migration note, the lineage record, the AE-15 ledger row; SUBSTITUTE: the version stamps (justified: the bump) and the hypothesis sentence "no run has measured it" → "ONE run has measured it (n=1)" (justified: it became false; nothing lost)
    $ n/a — judgement, not measurement

**new component:** none this session
    $ n/a — judgement, not measurement

**gates:** twin parity PASS | receipt-rows-vs-diff 0/0 (no per-fix table — class sweep N/A) | row-count 0 vs 0 (no finding written `applied`) | slots 0/0 (no `ALWAYS REPORT` key added)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo twin-parity-PASS
    twin-parity-PASS
    $ diff <(sed -n '/^```json$/,/^```$/p' docs/modules/templates/settings_json.md | sed '1d;$d' | tr -d '\r') <(sed -n "/<< 'SETTINGS'/,/^SETTINGS$/p" .claude/commands/existing_project_adaptation.md | sed '1d;$d' | tr -d '\r') && echo settings-twin-PASS
    settings-twin-PASS
    $ git diff 2cb4ede~1..2cb4ede -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:' | wc -l
    0
    $ grep -cE 'applied `?s2cb4ede' assets/docs/audit-*.md | awk -F: '{s+=$2} END{print s+0}'
    0

**push:** not requested — origin/main at c98db39
    $ git fetch -q --prune origin && git rev-parse --short refs/remotes/origin/main && git status -sb | head -1
    c98db39
    ## main...origin/main [ahead 1]

**audit:** proposed — before the MINOR bump the owner chose "bump now, audit later"; it has not run, and no push should precede it
    $ git log -1 --format=%B 2cb4ede | grep -E '^(bump|audit):'
    bump: v2.33.0 — MINOR (upstream absorption, new checklist section).
    audit: proposed — owner chose "bump now, audit later"; do not push before it runs.

**verification audit:** not triggered — no evidence from outside the loop; this session absorbed the only pending upstream doc
    $ PEND=0; for d in $(grep -L 'STATUS.*upstreamed' projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); [ "$PEND" -gt 0 ] || [ "$OBS" -gt 0 ] && echo "trigger (d): fires — pending upstream docs $PEND, HIGH observed in use $OBS" || echo "trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog"
    trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog

**placeholder:** none — no placeholder token used (receipts written after the hash existed)
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-22.md assets/docs/audit-2026-09-23.md | wc -l
    0

**control back-sweep:** N/A — no control touched (numerator 0 over `.claude/commands/` + `docs/modules/`)
    $ git diff 2cb4ede~1..2cb4ede -U0 .claude/commands/ docs/modules/ | grep '^+' | grep -cE '(grep|python -c|diff -r|git (diff|show|status)|node|sed -n)'
    0

**commit correction:** none
    $ git log --format=%s c98db39..2cb4ede
    feat: absorb the multi-agent reporting pilot upstream (v2.33.0)

**defect series:** unchanged — no `/audit` ran; 0 rows appended to its table
    $ git diff 2cb4ede~1..2cb4ede -- .claude/commands/audit.md | grep -cE '^\+\|'
    0

**applied-proof:** N/A — 0 IDs written `applied` (AE-15 filed `open`)
    $ git diff 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md' | grep -cE '^\+.*\| applied'
    0

**report deletions:** none — 0 reports deleted; `audit-2026-09-22.md` +1/-0 (the AE-15 row)
    $ git diff --name-status --diff-filter=D 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md' | wc -l; git diff --numstat 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md'
    0
    1	0	assets/docs/audit-2026-09-22.md

**push decay:** advanced df26d7e..c98db39 — D16 re-run over the newly-published range: GREEN — origin/main at c98db39
    $ bash .claude/scripts/d16-gate.sh log df26d7e..c98db39
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**report restore:** none
    $ git log --diff-filter=D --name-only --format= 2cb4ede~1..2cb4ede -- assets/docs | wc -l
    0

**verifier rounds:** 1 round, 10 defects reproduced, 9 fixed before commit — filed: AE-15 (open); trigger 9 files; bash -n ratchet GREEN
    $ git diff --name-only 2cb4ede~1 2cb4ede -- .claude/commands .claude/scripts docs/modules | wc -l
    9
    $ A=2cb4ede~1; B=2cb4ede; T=$(mktemp -d); for f in $(git diff --name-only "$A" "$B" -- .claude/commands); do for side in A B; do r=${!side}; git show "$r:$f" 2>/dev/null | awk '/^ *```bash *$/{b=1;s="";next} b&&/^ *```$/{b=0;printf "%s%c", s, 0;next} b{s=s $0 "\n"}' | while IFS= read -r -d '' body; do printf '%s' "$body" | bash -n 2>/dev/null || printf '%s' "$body" | grep -qE '<[a-z][^>]*>' || printf '%s' "$body" | cksum; done | sort > "$T/$side"; done; echo "$f base=$(wc -l < "$T/A") tip=$(wc -l < "$T/B") new=$(comm -13 "$T/A" "$T/B" | wc -l)"; done; rm -rf "$T"
    .claude/commands/audit.md base=0 tip=0 new=0
    .claude/commands/existing_project_adaptation.md base=0 tip=0 new=0

Reproduced-defect list (counts and invented names only), with disposition:
1. `component_design.md` §8 — the existing "registry reloads when the file is READ" line contradicted the new rule 4 → FIXED: rule 4 states the reload concerns an agent the registry already listed.
2. §8 rule 4 "ALWAYS first spawn in the NEXT session" contradicted shipped same-session creation evals (bootstrap, EPA, agent templates, rules-agents-updater) → FIXED: rule 4 rewritten as failure handling (a same-session `not found` defers the spawn, creation eval included).
3. `session_rules.md` — the topology ALWAYS/NEVER was violated on arrival by two shipped `sonnet` defaults and contradicted "decided by risk class, written once" → FIXED by CUT: the reading stays as n=1 evidence inside the HYPOTHESIS blockquote.
4. EPA v2.33.0 note "NO coordinated set" contradicted validation-orchestrator's membership in two coupled sets → FIXED: the note says this version adds no NEW coupling and a validation-orchestrator refresh stays governed by the existing sets.
5. `security_reviewer.md` new block — no invoker routes a hook/permission diff to the security reviewer (component-design §9) → FILED `AE-15`, open.
6. `security_reviewer.md` — `Sections checked` lists NUMBERED sections, so an unnumbered subsection of §2 could never be reported; "What this review covered" not updated → FIXED: the block is now §13 with its covered-list line.
7. `validation-orchestrator` — "save the transformed copy and ask the producer to audit it" had no destination → FIXED by CUT.
8. `evolution_policy.md` — "ALWAYS declared BEFORE the pilot ran" had no producing step → FIXED: the doc records the condition and WHEN it was declared.
9. `security_reviewer.md` — "the negation proof" named a mother-repo-only term → FIXED: cites the shipped component-design §9 rule 4.
10. Three new ALWAYS lines buried mid-line (§8 rule 4 in both copies, validation-orchestrator, evolution_policy), and rule 4 said "component" where the evidence is about agents → FIXED (removed or given their own line; scope narrowed to agents).

**receipt generator:** pasted below — 89 lines, 25 keys generated, hand-typed: liveness (its raw-template `$` line), placeholder (re-run after this file existed), classification, new component
    $ wc -l < "$SCRATCH/receipt_gen.sh"
    89
```bash
#!/usr/bin/env bash
# Receipt generator for the v2.33.0 upstream batch (2cb4ede). Run from the repo root.
# Prints, per key, "@@<key>" then each command as "    $ <cmd>" and its literal stdout indented.
R=2cb4ede~1..2cb4ede
run() { printf '    $ %s\n' "$1"; bash -c "$1" 2>&1 | sed 's/^/    /'; }
k() { printf '@@%s\n' "$1"; }

k "inventory sweep"
run "git diff --name-status --diff-filter=ADR 2cb4ede~1 2cb4ede -- docs/modules .claude/commands .claude/skills .claude/rules .claude/scripts examples | wc -l"

k "instruction style"
run "D='git diff 2cb4ede~1..2cb4ede -U0'; echo \"instruction style: \$(\$D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, \$(\$D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, \$(\$D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried\""

k "references"
run "git diff 2cb4ede~1..2cb4ede | grep '^+' | grep -ohE '\`[A-Za-z0-9_./-]+\.md\`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l"
run "git diff 2cb4ede~1..2cb4ede | grep '^+' | grep -ohE '\`[A-Za-z0-9_./-]+\.md\`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | tr -d '\`' | while read r; do case \"\$r\" in *.md) n=\$(git ls-files | grep -c \"\$(basename \"\$r\")\"); echo \"\$r -> tracked matches \$n\";; *) echo \"\$r -> section cite\";; esac; done"

k "fences"
run "for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=\$(grep -m1 -oE '^\`{3,5}[a-z]*' \"\$t\"); c=\${f%%[a-z]*}; echo \"\$t -> \$(sed -n \"/^\$f\$/,/^\$c\$/p\" \"\$t\" | wc -l) lines\"; done"

k "isolation"
run "bash .claude/scripts/d16-gate.sh log 2cb4ede~1..2cb4ede"

k "class sweep"
run "git log -1 --format=%B 2cb4ede | awk '/^Applies /{f=1} f&&/^\$/{exit} f' | grep -oE '\b[A-Z]{1,2}-[0-9]+\b' | sort -u | wc -l"

k "back-sweep"
run "git grep -niE '(spawn|use|fall ?back to) (a )?general-purpose' 2cb4ede -- docs/modules .claude/commands | grep -viE 'never|not |no silent|instead of' | wc -l"
run "git grep -niE '(summari[sz]e|translate|condense) (the|its) (review|reviewer|subagent)' 2cb4ede -- docs/modules | wc -l"
run "git grep -niE 'snapshot' 2cb4ede -- docs/modules/rules/ops_rules.md | wc -l"

k "liveness"
run "sed -n '/^\`\`\`\`js\$/,/^\`\`\`\`\$/p' docs/modules/templates/check_agent_frontmatter.md | sed '1d;\$d' > \"\$TMP/guard.mjs\" && node \"\$TMP/guard.mjs\" | tail -3"

k "negation proof"
run "git diff 2cb4ede~1..2cb4ede -- .claude/commands docs/modules .claude/rules | grep -E '^\+' | grep -cE '^\+ *(\\\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_]+ in )'"

k "version"
run "grep -rn 'v2\.33\.0' README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l"
run "grep -rn 'v2.32.0' README.md docs/ .claude/ CLAUDE.md 2>/dev/null | grep -v 'created: framework-v' | grep -vE 'v[0-9.]+ migration|pre-v[0-9.]+' | wc -l"

k "gates"
run "diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo twin-parity-PASS"
run "diff <(sed -n '/^\`\`\`json\$/,/^\`\`\`\$/p' docs/modules/templates/settings_json.md | sed '1d;\$d' | tr -d '\r') <(sed -n \"/<< 'SETTINGS'/,/^SETTINGS\$/p\" .claude/commands/existing_project_adaptation.md | sed '1d;\$d' | tr -d '\r') && echo settings-twin-PASS"
run "git diff 2cb4ede~1..2cb4ede -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^\`]*\`[A-Za-z][A-Za-z0-9 ._-]*:' | wc -l"
run "grep -cE 'applied \`?s2cb4ede' assets/docs/audit-*.md | awk -F: '{s+=\$2} END{print s+0}'"

k "push"
run "git fetch -q --prune origin && git rev-parse --short refs/remotes/origin/main && git status -sb | head -1"

k "audit"
run "git log -1 --format=%B 2cb4ede | grep -E '^(bump|audit):'"

k "verification audit"
run "PEND=0; for d in \$(grep -L 'STATUS.*upstreamed' projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql \"\$(basename \"\$d\")\" assets/docs/framework-evolution-upstream-*.md || PEND=\$((PEND+1)); done; OBS=\$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); [ \"\$PEND\" -gt 0 ] || [ \"\$OBS\" -gt 0 ] && echo \"trigger (d): fires — pending upstream docs \$PEND, HIGH observed in use \$OBS\" || echo \"trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog\""

k "placeholder"
run "grep -nE 'applied \`?s(BATCH|GROUP[0-9]+|PENDING)\`?|\| *\`?s(BATCH|GROUP[0-9]+|PENDING)\`? *\|' assets/docs/audit-2026-09-22.md assets/docs/audit-2026-09-23.md | wc -l"

k "control back-sweep"
run "git diff 2cb4ede~1..2cb4ede -U0 .claude/commands/ docs/modules/ | grep '^+' | grep -cE '(grep|python -c|diff -r|git (diff|show|status)|node|sed -n)'"

k "commit correction"
run "git log --format=%s c98db39..2cb4ede"

k "defect series"
run "git diff 2cb4ede~1..2cb4ede -- .claude/commands/audit.md | grep -cE '^\+\|'"

k "applied-proof"
run "git diff 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md' | grep -cE '^\+.*\| applied'"

k "report deletions"
run "git diff --name-status --diff-filter=D 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md' | wc -l; git diff --numstat 2cb4ede~1..2cb4ede -- 'assets/docs/audit-*.md'"

k "push decay"
run "bash .claude/scripts/d16-gate.sh log df26d7e..c98db39"

k "report restore"
run "git log --diff-filter=D --name-only --format= 2cb4ede~1..2cb4ede -- assets/docs | wc -l"

k "verifier rounds"
run "git diff --name-only 2cb4ede~1 2cb4ede -- .claude/commands .claude/scripts docs/modules | wc -l"
run "A=2cb4ede~1; B=2cb4ede; T=\$(mktemp -d); for f in \$(git diff --name-only \"\$A\" \"\$B\" -- .claude/commands); do for side in A B; do r=\${!side}; git show \"\$r:\$f\" 2>/dev/null | awk '/^ *\`\`\`bash *\$/{b=1;s=\"\";next} b&&/^ *\`\`\`\$/{b=0;printf \"%s%c\", s, 0;next} b{s=s \$0 \"\n\"}' | while IFS= read -r -d '' body; do printf '%s' \"\$body\" | bash -n 2>/dev/null || printf '%s' \"\$body\" | grep -qE '<[a-z][^>]*>' || printf '%s' \"\$body\" | cksum; done | sort > \"\$T/\$side\"; done; echo \"\$f base=\$(wc -l < \"\$T/A\") tip=\$(wc -l < \"\$T/B\") new=\$(comm -13 \"\$T/A\" \"\$T/B\" | wc -l)\"; done; rm -rf \"\$T\""

k "receipt generator"
run "wc -l < \"\$SCRATCH/receipt_gen.sh\""

k "measurer"
run "grep -ciE 'próxima auditoria deve|the next audit (must|should) measure' assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md"
```

**measurer:** 6 anchors — 1 named, 5 unmeasured
    $ grep -ciE 'próxima auditoria deve|the next audit (must|should) measure' assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md
    0


---

# Run 1

# Framework Audit Report

**Date:** 2026-09-23
**Framework version:** v2.33.0
**Run mode:** `baseline` — trigger (a) after the upstream absorption `2cb4ede` (v2.33.0), which also discharges trigger (b) for that MINOR bump; Phase 0 maps both to baseline. No batch of `applied` findings exists to verify.
**Post-push D16:** log c98db39..c98db39 → 0 commits scanned, 0 hits — origin/main at c98db39
**Dimensions checked:** 17
**Agents dispatched:** 6

**This run lands in a `receipts-only` file.** Phase 3 item 1 says to APPEND to today's file, and the carry-forward and Audit-intake readers SKIP a file whose header says `receipts-only`. So this run's ledger would have been invisible to both (AF-1). The instance is remedied above with the report's own correction forms: Errata 1 and a `*(superseded)*` header line. The class is open as AF-1.

**carried: 104 open + 0 escalated from `audit-2026-09-22.md`, plus 48 recovered from `audit-2026-09-14.md` / `audit-2026-09-15.md`**:
- From the rows of `audit-2026-09-22.md` (56): AE-15, AD-1, AD-9, AD-10, AD-14, AD-15, AD-16, AA-3, AA-9, AA-10, AA-12, AA-13, AA-16, AB-2, AB-3, AC-3, AC-6, AC-7, AC-8, AC-10, AC-12, AC-13, AC-20, AC-21, AC-22, A-14, A-15, A-16, A-17, A-20, A-21, A-22, A-23, A-25, A-26, A-34, A-35, A-37, A-39, C-3, C-4, C-5, C-8, C-10, C-12, C-14, C-15, C-21, C-22, D-4, D-5, D-7, D-8, D-9, D-10, D-11.
- **RECOVERED (48)** under Phase 3 item 3's broken-chain rule. `audit-2026-09-20.md:14` carried these in PROSE ("the 48 older rows"). AD-1's recovery walked back only as far as `audit-2026-09-16.md`, so neither `audit-2026-09-21.md` nor `audit-2026-09-22.md` has a row for any of them. The IDs: B-1, B-4, B-15, B-20, B-25, V-1, V-2, V-3, V-4, V-5, V-6, V-7, V-8, V-10, V-14, V-18, V-19, W-15, W-21, W-22, W-23, W-24, W-25, W-26, W-27, W-29, W-31, X-15, X-18, Y-12, Y-14, Y-16, Z-2, Z-4, Z-5, Z-6, Z-7, Z-8, Z-9, Z-10, Z-12, Z-13, Z-14, Z-15, Z-16, Z-17, Z-18, Z-19.
- The break itself is filed as AF-2.
- Every carried ID has a LEDGER ROW below. These were read from the reports' ROWS, never from a `carried:` sentence.
- How the recovered set was derived (mechanical):
  - Step 1: for every `^| ID |` status row in every `assets/docs/audit-*.md`, take the LAST row's status column. 114 IDs end `open`.
  - Step 2: minus the 56 with rows in `audit-2026-09-22.md`.
  - Step 3: minus 10 R-series IDs (R-9, R-13, R-15, R-19, R-34..R-38, R-42). Their last status row is `open` in `audit-2026-09-04.md`, but they were verdicted NOT-FIXED and consolidated into T-10 (`audit-2026-09-09.md:134`, `applied s51f395c`), so they are superseded, not dropped.
  - That leaves 48.
  - Agent 6 also named 11 older candidates (K-4, L-8, L-10, L-19, P-2, R-39, T-1, T-4, T-28, T-49). None of them has an `open` LAST status row, so they are NOT recovered.

**Accepted-risk OBSERVATIONS (reported once, never re-opened):**
- **E-1** (`audit-2026-09-02.md:603`): PUSHED-history residue. Agent 1 counted 9 hits of one source-project doc filename (the residue already reported on 2026-09-22) and 2 hits of one single-project domain term in `3e581b1` and its removal in `11ce6e9`. The second is newly visible only because this run's harvest carried the term. Tracked files: 0. Covered by E-1's standing instruction and NOT re-opened.
- **M-44**, **N-53**, **T-11**, **T-36**, **U-48**, **B-17**: no new occurrence.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 module skills; 1 runtime skill (`cross-cutting-analysis`), also in `docs/modules/` |
| C | Instruction style | Quality | 5 | FAIL | Added lines of `2cb4ede`: 0 buried, 1 compound checkbox (AF-5). 7 pre-existing instances outside W-29's list (AF-15) |
| E | Version consistency | Currency | 5 | PASS | v2.33.0 on every stamped surface. EPA:711's v2.32.0 is lineage inside the migration note. No `created:` field rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 listed = 15 on disk; 7 `user` / 8 `inline` verified |
| D5 | Bootstrap file refs | References | 2 | PASS | 56/56 framework-relative references resolve; every template extraction non-empty |
| D6 | Gap vocabulary | References | 3 | PASS | 10 gaps, `declarers extracted: 3 of 3`; §13 added no gap phrase; install rows present in both twins |
| D7 | Pushy Description | Quality | 3 | PASS | 20/20 specialists COMPLIANT; both twins `10 verified, 0 broken, 17 info`; 14/14 regression cases correct |
| D8 | Orchestrator refs | Process | 4 | PASS | Every name resolves. AD-14 still RED. Two new bold-lead-in citations (AF-11, AF-12) |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | Every entry resolves; all count claims correct |
| D10 | Command → skill paths | References | 4 | FAIL | Receipts-only collision (AF-1, AF-6); C-8 reproduced live (`report committed: s9b0fffa` before this report existed) |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 15 primary + 10 sub + Setup; qualified "15-step" claim accurate; v2.33.0 note adds no forward reference |
| D12 | Examples conventions | Currency | 5 | PASS | Full-set truth tests hold (20 agents / 9 skills / 11 rules) |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | Bootstrap reads no section the PRD lacks |
| D14 | Bootstrap output | Process | 2 | PASS | 17/17 template references traced; Step 15 reports every artifact |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 inaccuracies across the checked claims; D15.2 coverage holds |
| D16 | Project-info isolation | Privacy | 1 | PASS | 3-source blocklist (3 + 48 + 540→111 legit); 0 hits on tracked files, memory and the unpushed range. 1 LOW in a gitignored note (AF-13) |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | Receipts-only collision; 48 dropped carry-forward rows; W-26 recurring |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AF-1 | HIGH | `.claude/commands/audit.md` Phase 3 item 1 vs item 3 and `.claude/commands/maintenance.md` Audit intake step 1 / the receipts-home paragraph | **An audit run on a day that already has a `receipts-only` file is invisible to every reader of findings.** Phase 3 item 1 mandates APPENDING a `# Run N` to today's existing file. Both findings readers SKIP any file "whose header says `receipts-only`". Result: the next `/maintenance` falls back to the previous report (here `audit-2026-09-22.md`, already `APPLIED — 14 of 14`), and the next carry-forward drops this run's ledger — the "falsely clean backlog" the header rule exists to prevent. The `7be4aea` sweep reached the 2 READERS and not the WRITER. "Header" is also undefined once a file holds two runs. Instance remedied in this run by Errata 1 plus a `*(superseded)*` header line; the rule is unchanged. (Agents 4 and 6.) | applied `s3030551` |
| AF-2 | HIGH | `assets/docs/audit-2026-09-20.md:14` → `audit-2026-09-21.md` → `audit-2026-09-22.md` | **The carry-forward chain dropped 48 still-open findings. AD-1's recovery missed them.** `audit-2026-09-20.md` carried "the 48 older rows" in prose. AD-1's walk-back stopped at `audit-2026-09-16.md`, so B-, V-, W-, X-, Y- and Z-series findings last recorded `open` in `audit-2026-09-14.md` / `-15.md` have had no row for two runs. Measured: 114 IDs end `open`; 56 are in 09-22's rows, 10 are superseded R-series, 48 dropped. **Recovered into this run's ledger** (rows below). AD-1's mechanism (nothing COUNTS rows) is unchanged, so the class stays open. (Agent 6.) | open |
| AF-3 | MEDIUM | `.claude/commands/existing_project_adaptation.md:490` (Step 2.5) | Step 2.5 upgrades an existing security-reviewer "against the full checklist (9 sections)". The template has **13** (`grep -cE '^## [0-9]+\.' docs/modules/agents/security_reviewer.md` → 13). So §§10-12 (SAST, rate limiting, data validation) never reach an adapted project's existing agent. The v2.33.0 note patches only §13, and from Step 2.9 rather than from the step that owns the upgrade. The line dates to `7ed6315` and survived §§10-12 landing in `183177e`. (Agent 2.) | applied `s3030551` |
| AF-4 | MEDIUM | `.claude/commands/existing_project_adaptation.md:257-294` and the v2.32.0 migration note | v2.32.0 added a behavioural Session Protocol line to the CLAUDE.md template (`claude_md.md:30`: domain rules are PATH-SCOPED, so ALWAYS Read the rules file directly when planning without opening code). EPA's checklist tests only section PRESENCE, and its v2.32.0 note never mentions CLAUDE.md. A project whose rules Step 2.7 converts to `paths:` therefore never receives the compensating line that bootstrapped projects get. `EPA:290` still says the Session Protocol "is 5 lines of pointers" against 8 bullets in the template. (Agent 2.) | applied `s3030551` |
| AF-5 | MEDIUM | `docs/modules/agents/security_reviewer.md:226` (§13, added by `2cb4ede`) | §13's last checkbox asks the reviewer to confirm (a) that a real call was refused in the live harness and (b) that the owner requested the probe in the session. That is two requirements in one box, and the agent's `## Input` (diff, stack skill, rules, CLAUDE.md) carries neither fact. Its BOUNDARIES forbid reading the logs where either could be recorded. The item can only be skipped or rubber-stamped. Distinct from AE-15 (no invoker). (Agent 5.) | applied `s3030551` |
| AF-6 | MEDIUM | `.claude/commands/audit.md:1405-1407` (prior-prediction grep) and `:316` (Agent 1 FILES TO READ item 11) | The receipts-only kind was swept into 2 readers of "the previous report" and missed these two. Pointed at the newest file, the prediction command returns `0` (measured over `audit-2026-09-23.md`; `1` over `audit-2026-09-22.md`). The rule maps that to `prediction: n/a — the previous run stated none`, a false n/a on the healthy state. This run resolved "previous" by hand. (Agents 4 and 6.) | applied `s3030551` |
| AF-7 | LOW | `docs/modules/rules/session_rules.md:231`, `:236` | The new first-measurement text says "the oracle" / "An oracle", a term defined nowhere in the blockquote or the section. The sibling `ops_rules` §6 item says "SNAPSHOT". (Agent 5.) | applied `sefca24e` |
| AF-8 | LOW | `docs/modules/rules/session_rules.md:218` vs `:222` | One HYPOTHESIS blockquote now says both "ONE run has measured it (n=1, below)" and "**unmeasured — no measurer below `production`, AND none above it either**". The two are reconcilable (a hand measurement vs no mechanical measurer), but the text never says which is meant. (Agent 5.) | applied `sefca24e` |
| AF-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:176-179` | The new "VERBATIM MEANS UNTRANSFORMED" rule is scoped to saved REVIEWER verdicts. Its own evidence is a different case: an orchestrator relaying a subagent's ANALYSIS to the next agent. That general handoff is still uncovered, and "found the transcript faithful" weakens the cited evidence. (Agent 5.) | applied `sefca24e` |
| AF-10 | LOW | `assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md:44` | The lineage record still says "no coordinated refresh set". The pre-commit verifier's defect #4 corrected exactly that formulation in the EPA note ("adds NO new coupling … STAYS governed by the coupled sets"). The fix was not swept to its sibling surface. (Agent 6.) | applied `sefca24e` |
| AF-11 | LOW | `assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md:44-45` | Introduced by `2cb4ede`: cites `existing_project_adaptation.md` → "v2.33.0 migration", whose target (`EPA:700`) is a bold lead-in, not a heading (D8.7 RED). The batch's `references:` receipt counts file targets only, so it cannot see it. (Agent 4.) | applied `sefca24e` |
| AF-12 | LOW | `.claude/commands/existing_project_adaptation.md:1135-1136` | Cites `/maintenance` → "Gate 1 (TWIN PARITY)" as a section; the target is the bold lead-in `**Gate 1 — TWIN PARITY.**` (`maintenance.md:794`). C-15's class, from `961244b`, in no ledger. (Agent 4.) | applied `s3030551` |
| AF-13 | LOW | `.claude/docs/idea-team-modules.md:203` (gitignored) | A local framework note names one of the production project's pilot agents by its literal name where a role descriptor would do. Its direction (spec → project or project → spec) cannot be read from the note. Not published: the file is gitignored and appears in no commit. (Agent 1, D16.3.) | open — fixed 2026-09-26 in the WORKING TREE only: the file is gitignored, so no commit can carry it and applied-proof cannot resolve it; re-verify by grep |
| AF-14 | LOW | `.claude/commands/audit.md:379-384` (D16.2c) | D16.2c tells auditors to exclude all of `projects/*/scripts/` as a copy path, but bootstrap copies only two files there (`check-agent-frontmatter.mjs`, `check-rules-paths.mjs`). The exclusion drops 34 genuine project sources from the comparison set, and 10 intersection identifiers were reachable only through them (all legitimate this run). (Agent 1.) | applied `s3030551` |
| AF-15 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:97`, `:163`, `:305`, `:342`; `docs/modules/skills/sprint-proposer/SKILL.md:219`, `:257`; `docs/modules/agents/code_reviewer.md:292` | W-29's class (instruction style), at seven sites outside W-29's list: behavioural requirements with no CAPS verb, buried mid-step, or with no output format. Pre-existing; none added by `2cb4ede`. (Agent 5, [C].) | applied `sefca24e` |
| AF-16 | MEDIUM | `.claude/commands/audit.md` Phase 3 item 1 (as first drafted in this batch) | The drafted supersede rule added a second column-0 `**Run mode:**` line beside the appended run's own, double-counting the run in "The defect series" verification grep (planted in a sandbox: RED). Fixed before commit: NEVER add a second line. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-17 | MEDIUM | `.claude/commands/audit.md:316` (Agent 1 FILES TO READ item 11, as drafted) | The drafted text cited "Phase 3 item 3", which Agent 1's prompt never carries; the agent would read only "skip receipts-only" and match a superseded header — AF-1's class in the D16 reader. Fixed before commit: the definition is inlined. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-18 | LOW | `.claude/commands/maintenance.md:353-355`; `.claude/commands/audit.md` Phase 3 item 1 | Reader count stale: "two readers … Both" while four sites skip receipts-only files after this batch. Fixed before commit. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-19 | MEDIUM | `.claude/commands/bootstrap.md` (existing CLAUDE.md branch; Step 8 existing security-reviewer branch) | Twin parity: EPA gained bullet-by-bullet Session Protocol and 13-section checks; bootstrap's existing-file branches had neither. Fixed before commit. Residual, not in this batch: Step 8 still checks a "tiered security testing model reference" the template does not contain (pre-existing). [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-20 | LOW | `.claude/commands/existing_project_adaptation.md` report template (CLAUDE.md and security-reviewer.md lines) | The new in-place-edit offers, the 13-section count and the bullet comparison had no report slot. Fixed before commit by amending the two existing slots (no new key). [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-21 | LOW | `.claude/commands/existing_project_adaptation.md` Step 2.5 count check | "equal to the number of □ above" counted 20 boxes (7 frontmatter + 13 sections) against 13. Fixed before commit: NUMBERED boxes of the section list. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `s3030551` |
| AF-22 | LOW | `docs/agentic_engineering_framework.md` (security-reviewer trigger line; `Security reviewer:` row provenance) | The widened skip verdict named the tool-gate trigger while the two lines explaining the row still named client-bound data only. Fixed before commit. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |
| AF-23 | LOW | `.claude/commands/existing_project_adaptation.md` report template (code-reviewer.md, security-reviewer.md lines) | The v2.34.0 in-place edits had no report slot. Fixed before commit by amending the two existing slots. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |
| AF-24 | MEDIUM | `docs/modules/agents/code_reviewer.md` Known Bug Patterns rule (as drafted) | The drafted rewrite added a second record format (`pattern · where found · grep used · N instances`) incompatible with the section's `[added | triggered | false-positive]` entries that codebase-audit reads. Fixed before commit: "in the entry format above". [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |
| AF-25 | MEDIUM | `docs/modules/skills/validation-orchestrator/SKILL.md` agent-tool-gate trigger (as drafted) | The drafted trigger located permission rules only; hooks and agent-confined scripts had no recognisable path. Fixed before commit: three enumerated shapes (settings keys, a hook's script, a runner named in `permissions.allow` or an agent prompt). [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |
| AF-26 | LOW | `.claude/commands/existing_project_adaptation.md` v2.34.0 migration note (as drafted) | The note sat between v2.33.0 and v2.33.1 and named only the continuous-mode set while three changed skills fall under "the coupled sets". Fixed before commit. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |
| AF-27 | LOW | `docs/modules/rules/session_rules.md` hypothesis blockquote | Cited ops-rules §6 with no tier qualifier in a rule shipped at every tier; ops-rules installs at `production`+. Fixed before commit. The pre-existing unqualified citation further down the file is not in this batch. [filed by /maintenance on 2026-09-26, from its independent pre-commit verifier] | applied `sefca24e` |

VISIBILITY check: `grep -c '^| AF-[0-9]' <this report>` → 15, equal to the 15 rows written. COLLISION check before writing: `grep -h "^| AF-" assets/docs/audit-*.md | wc -l` → 0.

**Cited, not re-filed:**
- AE-15: §13 has no invoker.
- AC-10 / AD-15: D6.7's install-block diff cannot return empty; still exits 1 on the two legitimate hunks.
- AD-14: the Q4 citation.
- C-15: non-heading citations.
- C-8: reproduced live by Agent 4.
- AA-12: `s`-prefix mismatch.
- W-27: session-end's three minimums, which Agent 5 re-found at `:112/:123/:125`.
- W-26: the bump/audit ordering. Agent 6 re-found it recurring and its row is annotated below.

### Carried forward from `audit-2026-09-22.md`

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AE-15 | MEDIUM | `docs/modules/agents/security_reviewer.md` §13; `docs/modules/skills/validation-orchestrator/SKILL.md` Routes 1-2 | **A new checklist section has no invoker.** v2.33.0 added §13 "AI-Agent Tool Gates" (check when a diff adds or edits a hook, a permission rule or an agent-confined script), but neither the agent's own "When this agent is invoked" list nor any validation-orchestrator route sends such a diff to the security reviewer, so the section runs only when the reviewer happens to be spawned for another reason (component-design §9). Routing is a behaviour change in the orchestrator and was left out of the absorption. [filed by the 2026-09-23 maintenance session on 2026-09-23, from its independent pre-commit verifier] | applied `sefca24e` |
| AD-1 | HIGH | `assets/docs/audit-2026-09-20.md:14` and `:411` | **The carry-forward chain broke between two runs of the same file.** Run 3 declared `carried: 81 open` in PROSE and wrote no ledger ROW for any of them; Run 4 then read the ledger rows and declared `carried: 8`. Mechanically: 41 open/escalated findings across `audit-2026-09-16.md` and `audit-2026-09-19.md` have **zero** `^\| ID \|` rows in `audit-2026-09-20.md` (the A/C/D rows present there are Part 1 *verdict* rows, not carry-forward status rows). Phase 3 item 3's re-verification never ran over them. This run's own first carry-forward measurement read 24 and would have propagated the loss. **HALF-FIXED in `280c2f2`:** item 3 now requires ROWS and says to read the previous report's rows, not its `carried:` sentence; the 41 rows are restored. NOTHING COUNTS THEM — the reconciliation was drafted and cut (it read GREEN on this finding's own data, and the moratorium forbids a new control in that file). Stays `open`. | open |
| AD-9 | MEDIUM | `.claude/commands/maintenance.md` Gate 3 / Audit intake item 4 | Nothing asserts that an audit-applying session wrote a receipts SECTION into the report file, so the 27-key loop — self-scoped to "this run's section" — is skipped silently when the section is absent. Measured receipt-key coverage per commit body: `28b157a 0/27 · 847d18f 0/27 · 1b0bb67 1/27 · c9355b0 3/27 · 34a6c97 5/27 · 455c299 5/27 · 0bb7949 6/27 · 34a7c1c 9/27 · 961244b 27/27`. `34a7c1c` is the **v2.30.0 MINOR bump** — 9 of 27 keys, `verifier rounds:` written as prose (invisible to the loop's `^\*\*key:` anchor), zero `$` lines in the whole body. No control noticed. **HALF-FIXED in `280c2f2`:** the gap is now STATED in Gate 3, with both cut forms and why each failed. No check exists. Stays `open`. | open |
| AD-10 | LOW | `.claude/commands/bootstrap.md` (Steps 3, 4 vs `:449`) | `.claude/phases/` is never `mkdir`'d before Steps 3 and 4 write into it; the only creation sits inside Step 5.8's `internal-tool`+ branch, which never runs at `prototype`. Every other directory is explicitly created (`:378`, `:409`, `:421`, `:816`). Benign — the Write tool creates parents — but it is the one directory the command relies on implicitly. | open |
| AD-14 | LOW | `framework-audit/SKILL.md`, cited from `autonomous-loop/SKILL.md` and `session_rules.md` | `framework-audit` → Q4 is not a markdown heading in the target's body: it is the numbered list item `4. **Aspirational-vs-real audit**`; the only `### Q4 …` line sits INSIDE the fenced report template. Unambiguous and present, so not a broken reference — but it fails D8.7's "real markdown heading" test, and D8.7 says a bold/list lead-in is not a heading. | open |
| AD-15 | LOW | `.claude/commands/audit.md` D6.7 | D6.7's literal instruction — "`diff` the two install BLOCKS — the table AND the prose around it … expected: empty" — is **unsatisfiable by design**. The two commands deliberately diverge on mode-specific prose (bootstrap's Step 7/8/10 parenthetical; the `created:` lineage stamp), and the repo bounded the byte-equivalence claim to the `TWINNED SPAN` markers precisely to make it falsifiable (`/audit` 2026-09-10 U-24). Both bounded diffs are empty. **The check text is stale, not the repo** — a check that fires on the healthy state, the K-8/L-7 inversion. | open |
| AD-16 | LOW | `.claude/commands/maintenance.md:427` | "**ALWAYS report the harvest with its command and stdout**" names no key and is not in the 27-key registry, so the "every key exactly 1" loop cannot miss it. Measured: reported in 1 of the last 9 batches (`1b0bb67`); before that only in `audit-2026-09-15.md:566-568`, folded under `control back-sweep:`. component-design §10's own class inside the framework's apparatus. | open |
| AA-3 | LOW | `assets/docs/audit-2026-09-19.md:742`; harvesters at `.claude/commands/maintenance.md:97`, `.claude/commands/audit.md:111` | The `push:` receipt does not end with `— origin/main at sHASH` (a parenthetical follows), against `maintenance.md:50`. The harvester is `$`-anchored and takes `tail -1`, so it silently discarded that key and read the earlier `push decay:` line instead; its only RED fires when NOTHING matches. Harmless here (both lines carry the same hash), blind by construction. | open |
| AA-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227` vs `docs/modules/rules/session_rules.md:278-283` | A-18's schema/migration extra-receipt rule exists only inside the executed component; the section that owns the deploy moment ("Deploy gates"), which `:169` names as the gate's caller, never mentions it — component-design §9. | open |
| AA-10 | LOW | `.claude/commands/maintenance.md:939-942` vs `:949-950` | C-18's third element: step 4's mandated `probe-sandbox:` line has no slot in the `control back-sweep:` report form, which asks for "the command and stdout from step 1". It is reported only if the executor volunteers it. | open |
| AA-12 | LOW | `.claude/commands/audit.md:125`, `.claude/commands/maintenance.md:111` | Both state expectations (`origin/main at sNEW`, `unchanged at sHASH`) carrying an `s` prefix their fences never print; live output is `origin/main at af974ec`. The harvester tolerates it (`s?`), but a reader comparing stated-to-actual sees a mismatch on the healthy state — D10.0's own class. | open |
| AA-13 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:145` | `HOOKS=$(grep -hsE '"command" *:' .claude/settings.json)` is line-based: with a single-line or minified `settings.json` the whole file lands in `$HOOKS` and a `permissions.allow` entry counts as an invoker again, re-opening C-33. Confirmed in the sandbox with pretty-printed vs one-line JSON. | open |
| AA-16 | LOW | `.claude/commands/audit.md:548-572` (Agent 3 FILES TO READ) | D7.6 names `.claude/scripts/probe-sandbox.sh` and mandates running it, but the file appears in no agent contract's list — violating the list's own closing rule ("Every file a CHECK below names MUST appear in this list"). Repo-wide: 0 hits for `probe-sandbox` inside any FILES TO READ / INPUTS block. Same class as the open Z-18 (`d16-gate.sh`), so the Z-18 fix should carry this sweep. | open |
| AB-2 | MEDIUM | `.claude/commands/existing_project_adaptation.md` | The COUPLED REFRESH mandate has no slot in the final report; the guard slot offers refreshed/kept and cannot hold a coupled decision | open |
| AB-3 | MEDIUM | `docs/modules/rules/session_rules.md` | "NEVER DOWNGRADE a component's model…" is a banned process anti-pattern with no mechanical self-check (component-design section 6) — see AC-7, which refutes the stated deferral reason | open |
| AC-3 | MEDIUM | commit `adc7481` body vs `assets/docs/` | AB-1..AB-11 were filed only in a commit message. A grep for the ledger row shape over every report returns 0. Three readers are keyed to `assets/docs/audit-*.md` and none reads a commit body: the ID collision check, Phase 3's carry-forward, and trigger (d)'s `OBS` term. AB-2, AB-3 and AB-7 are `open` and in no ledger. Deposited into THIS report's ledger below as the remedy for the instance; the class needs a second form on `maintenance.md` item 10. | open |
| AC-6 | MEDIUM | `.claude/commands/maintenance.md` receipts form vs its self-check | The file authorizes the commit message body as a receipts home; the mandatory receipt self-check extracts a `## Post-change checklist receipts (…)` heading from a REPORT FILE and asserts a `$` line per key. Measured against `adc7481`'s body: 0 bold-form keys, 0 `$` lines, while all 27 keys are present in plain form. One authorized receipts home has a self-check that structurally cannot pass. The push-decay detector carries a written KNOWN LIMIT for this same form; this check carries none. | open |
| AC-7 | MEDIUM | `docs/modules/rules/session_rules.md` → the NEVER DOWNGRADE line | AB-3 was left open on the reason "a check would need value history, not presence". The reason does not survive: its two neighbouring NEVERs ARE mechanically guarded, and value history is what a git repo has — the framework already ships a base-vs-tip set-comparison ratchet (the bash-syntax ratchet). A `git diff` over `.claude/agents` scoped to removed/added `model:` pairs moving down the tier order is the direct analogue. | open |
| AC-8 | MEDIUM | `.claude/commands/maintenance.md` checklist item 6 | `model:` on the 30 shipped agent templates is verified by nobody in THIS repo. Item 6's guard command scans `.claude/skills/` and `.claude/agents/` (this repo has no `.claude/agents/`); its raw-template loop globs the module skills and agents and asserts only `name:`. `examples/` appears in no glob in that command file. All 30 carry it today; a future edit dropping it ships to every bootstrapped project and is caught only there, after the fact — the `criteria_enforcer.md` shape item 6 itself cites. | open |
| AC-10 | MEDIUM | `.claude/commands/audit.md` D6.7 | The mandated install-BLOCK diff states "expected: empty" over a span that legitimately diverges on both paths (bootstrap pre-selection vs EPA already-exists; the two lineage stamps). It can never return empty, so every run hand-adjudicates. The block carries no TWINNED SPAN markers, which is the mechanism the repo already uses for the Step 12.5b criteria; the span boundaries are described in prose, so two auditors extract two different spans. | open |
| AC-12 | MEDIUM | `.claude/commands/bootstrap.md` vs the EPA twin | EPA says "Then RUN it once now"; bootstrap names `check-agent-frontmatter.mjs` four times and never executes it, and Step 15 has no guard verdict slot. Bootstrap is the run that hand-writes `model:` into six agents from templates it adapts, then commits at Step 14.5 — and per component-design section 8 the break cannot manifest in the session that wrote it. At `prototype` the CI floor is skipped, so first detection is session 1.  **CUT from the 2026-09-21 backlog batch 4** — the "Then RUN it once now" mandate is sound and the step order checks out, but reporting the run needs named outcomes (pass / fail on exit 1 / skipped without Node) that no step defines yet. Destination first, then mandate (component-design §10). | open |
| AC-13 | MEDIUM | commit `adc7481` vs Cycle governance rule 2 | AA-13 lives at `docs/modules/skills/codebase-audit/SKILL.md:145`. `adc7481` edited that file. Rule 2: "ALWAYS apply a LOW finding when this session already edits its file for another reason, and say so in the commit." It was neither applied nor named; the commit records `class sweep: N/A`. | open |
| AC-20 | LOW | `.claude/commands/maintenance.md`, the push-decay KNOWN LIMIT | The documented failure mode ("the next session sees RED: no recorded origin/main hash") does not occur: the extraction returns a live hash three commit-body sessions stale, so the detector prints a plausible `advanced …`. It FAILS SAFE (over-scans an already-scanned range), but a reader relying on that sentence will not recognize the state they are in. | open |
| AC-21 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:246` | A nested bold marker inside a bolded mandate inverts the rendering: the outer bold closes at the inner marker, leaving a second distinct mandate UNbolded and a dangling trailing marker. Section 6 property 2 (dedicated line) plus a rendering defect. | open |
| AC-22 | LOW | `docs/modules/skills/sprint-proposer/SKILL.md:144-145` | Announces "(expected result stated)" and states neither a command nor a count — it cannot go red. The same file's Step 1 and `session-end` both give a real countable grep. | open |
| A-14 | LOW | `docs/modules/skills/session-log-creator/scripts/create-log.sh:26-27` vs `docs/modules/skills/session-log-creator/SKILL.md:46` | The generator writes the `## Review receipts` slot with no pre-deploy gate line in either the GREEN or the RED form. A log created by the script cannot carry the gate result the skill requires. B-23's and B-26's twin, not swept (847d18f did not touch the script). | open |
| A-15 | LOW | `.claude/scripts/d16-gate.sh:216-221` (`dir` scope); `.claude/commands/audit.md` D16.3b | `dir` matches paths as given. The memory directory can only be passed as an absolute path, so every ancestor directory name is scanned, and an identifier matching a word in the owner's home path hits every file although the contents are clean. Real run: 12 hits over 12 clean files. Probe: absolute path 2 hits, relative 0. D16.3b does not mention it. | open |
| A-16 | LOW | `autonomous-loop/SKILL.md:630-631` vs `:668-671`, `:313-315` | A mid-task marker removal by another session makes the closure check RED. The closure-RED rule then requires writing `State: stopped`, which needs the marker, and the revocation branch says NEVER re-create the marker. No precedence line. Introduced by X-13's fix. | open |
| A-17 | LOW | `autonomous-loop/SKILL.md:675-677`, `:706`, `:736-738` | The C5 digest offers "triage held/deferred" as an owner answer, but neither the resume line (`:675-677`) nor C6's reset (`:736-738`) says whether it resumes or resets. X-19's class, not swept; the receipt's claim about `:675-677` is false. | open |
| A-20 | LOW | `docs/modules/agents/diff_pattern_extractor.md:87-92`; `autonomous-loop/SKILL.md:505-509`; `codebase-audit/SKILL.md:110-112`, `:147-148`; `framework-audit/SKILL.md:81-82` | Five behavioural requirements added by the two batches are buried in note sentences, KNOWN LIMIT paragraphs or sentence tails ("— and ALWAYS file ONE task", "MUST carry its own", "say so, and record the step as ⏭️", "and say so in the report", "say so when one does"). Third consecutive batch pair needing an instruction-style follow-up. | open |
| A-21 | LOW | `autonomous-loop/SKILL.md:353-354`, `:494` vs `:366-373` | The every-subagent resource declaration still omits the MUTATOR/READER class and the scratchpad. `READERS` at C1 `:494` means "never adds a task", a different sense from READER at `:366-373`, so `criteria-enforcer` (writes `pendencias.md`) is classed both ways. B-12's class plus a new defect. | open |
| A-22 | LOW | `autonomous-loop/SKILL.md:421-424`, `:148` vs `:641` | "Emit the lessons block before `/session-end`" exists only under C5 (continuous). Segment-mode continuation and exception stops run `/session-end` with no such step, so their orchestration lessons never reach the log. B-19 D9, not swept. | open |
| A-23 | LOW | `autonomous-loop/SKILL.md:608-627` | The C4 closure check's first-parent traversal reads OK for a `--no-ff` merge whose side branch carries code committed after the counter moved (throwaway case P4); the KNOWN LIMIT (`:626-627`) does not declare it. Introduced by X-9's fix. | open |
| A-25 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 (`:232-233`) | The moratorium self-check counts receipt keys (27) and numbered items (14) only. A new prose-embedded control reads 27/14 (planted), so 742a691's new "expected ≥1 hit in EACH" check and D15.2's new coverage check passed unseen. It does not cover `audit.md`, and batch 2's receipts neither run nor report it. | open |
| A-26 | LOW | `autonomous-loop/SKILL.md:5-19`, `:536`, `:664`; `sprint-proposer/SKILL.md:68-102` | "cancel continuous mode" is read only in the autonomous-loop body. A session without that skill loaded is never told to remove the marker. X-8 residue. | open |
| A-34 | LOW | `.claude/commands/existing_project_adaptation.md:598-600` vs `:856-864` | Step 2.9 takes the refresh/keep decision for `session-log-creator` before the profile exists; the Step 2.9b pairing rule ("at `production`+, ALWAYS PRESENT THEM TOGETHER" with framework-audit and autonomous-loop) then cannot apply to a decision already taken. Introduced by 5ecefa9 (B-11 fix). | open |
| A-35 | LOW | `.claude/commands/existing_project_adaptation.md:1358`, `:1377-1382` | The EPA report has no slot for `.claude/docs/` (created at `:682`) or `.claude/skill-gate/review_reports/` (created at `:819`); bootstrap reports both (`:944`, `:984`). Twin asymmetry, W-18's class.  **CUT from the 2026-09-21 backlog batch 4** — slots were drafted with verdicts `created` / `already present`, but both directories are made by a bare SILENT `mkdir -p` and no step emits either value (and the report retires `already present` two lines below). Closing it needs the fences to `test -d` and echo a verdict FIRST, then the slot copies it. | open |
| A-37 | LOW | `.claude/commands/existing_project_adaptation.md:517`, `:595`, `:862`, `:894`, `:1121`; `.claude/commands/bootstrap.md:113`, `:342`, `:892` | V-17's class, not swept: a rationale clause fused with its ALWAYS in one sentence survives at 8 sites. Two of them (`EPA:595`, `:862`) are invisible to the style regex because `**` precedes the space, and the batch 2 receipt marked the twin "n/a — prose only". | open |
| A-39 | LOW | `.claude/commands/audit.md`, `.claude/commands/maintenance.md:1416-1424`; lineage docs' efficacy-anchor tables | B-11's mother-side half: the maintenance writer must name a measurer, but no `/audit` or `/maintenance` step ever reads an anchor's measured result back (`grep -ciE 'efficacy\|hypothes\|measurer' audit.md` → 3 unrelated hits). An anchor whose measurer silently breaks (A-3) is never noticed from here. | open |
| C-3 | MEDIUM | `assets/docs/audit-2026-09-15.md:1336`, `:1349`, `:1915`, `:1928`; enumerator `assets/docs/audit-2026-09-16.md:380` | A-2's class is not swept. The two persisted receipt generators execute the fail-open np1/np2 (`bash "$NP"`) under `$` lines that still say "run from the repo root", with no erratum. The batch's back-sweep enumerator reads `0` over them because it matches only literal git write verbs inside a fence. Agent 6's stand-in probe: np1 still commits (213 → 218), pushes and rewires origin when its scratch clone fails.  **CUT from the 2026-09-21 backlog batch 1** — three errata were written and all three withdrawn: one was spliced INSIDE the bash fence it annotated (breaking its parse), and the replacement set over-applied to a third generator that uses neither `bash -c "$cmd" 2>/dev/null` nor `$NP`. Needs errata for exactly the TWO generators this finding names. | open |
| C-4 | MEDIUM | `.claude/commands/audit.md:1437-1441` (Phase 3 item 7) vs `:36-38` | Item 7's fence `git add`s only the report, and the rewritten self-check is scoped to the report path. When item 6 has appended a defect-series row to `audit.md`, that row stays uncommitted while the check prints `report committed`. Sandbox: `report committed: sH` then `uncommitted after the fence: M .claude/commands/audit.md`. U-47's residue, adjacent to A-31. | open |
| C-5 | MEDIUM | `.claude/commands/audit.md:18-31`, `:223-236` | Whether a check WRITES — and therefore goes to the sandbox — is decided by whoever runs it, and nothing compares the live repo before dispatch with the live repo after the agents return. A writing check misrouted in place (the incident's own shape) is still caught by nothing: `grep -c fingerprint .claude/commands/audit.md` → 0. Concrete instance: C-13. | open |
| C-8 | LOW | `.claude/commands/audit.md:1439-1441` | A same-day re-run whose HEAD is the previous run's `docs(audit): persist` commit to the same file reads `report committed` before this run's report exists (sandbox: `report committed: sH` with no Run 2 content). Phase 3 item 1 (`:1368-1371`) calls same-day runs the normal shape. A-31/A-41 class, one case over. | open |
| C-10 | LOW | `.claude/commands/audit.md:852` (D10.0) vs `.claude/commands/maintenance.md:874-875`, `:1106` and `audit.md:47` | A-42 and A-46 were not swept into D10.0. D10.0 mandates `run --staged` for every writing check, including item 5's pre-fix reconstruction, which maintenance says must NEVER combine `--staged` with a checkout. D10.0's run-every-check mandate also reaches item 10's `--keep` recipe, which `audit.md:47` now forbids agents. | open |
| C-12 | LOW | `.claude/commands/audit.md:674-686` (D7.6 steps 1-2) | The steps require a synthetic project in a scratch directory, run in the sandbox, whose working directory is the clone. They never say that the extracted loop resolves `projects/$ARGUMENTS/.claude/agents` relative to the current directory (`bootstrap.md:697`), so the auditor must work out the `cd` and `ARGUMENTS` alone. | open |
| C-14 | LOW | `assets/docs/audit-2026-09-16.md:374-376` (`fences:` receipt) | The `$` line is the mandated command with a `| awk` summariser appended, so the pasted stdout (`zero: 0 of 12`) is not the mandated command's literal output — against `maintenance.md`'s "THE `$` LINE MUST BE THE MANDATED COMMAND, BYTE-FOR-BYTE". | open |
| C-15 | LOW | `.claude/commands/maintenance.md:1022`; `.claude/commands/audit.md:137` | Cross-file citations `/audit` → Authorized operations (a bold lead-in; added by 194f44a for A-48) and `maintenance.md` → the `push:` rule (a bullet) point at non-headings, which D8.7 marks RED.  **CUT from the 2026-09-21 backlog batch 2 after three verifier rounds** — the D8.7 amendment (sanctioning a self-declared non-heading citation) is the right idea: two such citations were already live and resolving, and unamended D8.7 flags both. But every implementation left a gap — a citation with a FALSE "where" fell outside BOTH the sanctioned and the RED list, letting a citer silence the check on an unverified assertion. Reverted whole. | open |
| C-21 | LOW | `.claude/commands/audit.md:226-232`, `:20-30` | The isolation rule sends "every probe" to the sandbox and forbids ever putting a read-only scan (`d16-gate.sh … dir`) there, with no tie-break for a probe that is itself a read-only scan (e.g. a planted `D16_EXTRA` over `dir`, which Agent 1 had to run in place to see `.claude/docs/`). | open |
| C-22 | LOW | `.claude/commands/audit.md:229` vs D16.2 (`:318-320`) | D16.2 harvests "repo URLs", but the verbatim rule bans `git remote`, the only direct source, and neither names a sanctioned read-only route (e.g. `git config --get-regexp '^remote\..*\.url'`). Agent 1 ran `git -C projects/<each>/ remote -v` read-only to fill the class. | open |
| D-4 | LOW | `.claude/commands/existing_project_adaptation.md:773-777` vs `:897-953` | A-8's residue: the PHASE-3 RE-CHECK (risk profile, and now the Red/Blue Team decision) is mandated only inside Phase-2 Step 2.9b, and Phase 3 never points back to it (0 grep hits for `2.9b|2.6|profile|re-check`). The moment has no invoker (component-design §9.1). | open |
| D-5 | LOW | `.claude/commands/existing_project_adaptation.md:1215-1365` (Step 5.1) vs `:877-878` | A-8's residue: nothing mechanically checks that `.claude/agents/red-team.md` exists when `code-reviewer.md` carries "red-team is MANDATORY on every diff that touches a money path". Step 5.1's fence has 0 red-team lines although it checks validator and arbitrator existence (`:1246`), so the defect A-8 named can still ship, caught only by prose (§9.2). | open |
| D-7 | LOW | `.claude/commands/existing_project_adaptation.md:617-623` (added by 87210b1) | The v2.27.0 migration note has three defects: its headline says "two pairs travel together" and names an ALWAYS for one; it claims an older `session-log-creator` "only lacks the pathspec-RED slot" when the pre-batch slot also lacked the `… K without receipt — deploy blocked` form; and its "ALWAYS offer `session-log-creator` with it" is buried mid-paragraph with no executing moment — `validation-orchestrator` is asked in Step 2.9b's coupled-set question (`:599-603`) while `session-log-creator` is asked in Step 2.9, so the pairing crosses that split (A-34's class). | open |
| D-8 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227-229`, `:231` | A-18's new ALWAYS line (a schema or migration commit also needs an adversarial or data-reviewer receipt) has no output format and no stated-skip form, and the ALWAYS REPORT line covers only the gate's four strings — so whether the deploying session performed the check is structurally invisible (component-design §6 property 3, §9.3). | open |
| D-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:218-219` | "A verdict the list does not know BLOCKS — extend `PASS`, never the reverse" is a behavioural requirement for every project that customises the gate, buried in lowercase at the end of a rationale paragraph (§6 C.4). | open |
| D-10 | LOW | `.claude/commands/maintenance.md:1112-1115` | C-19's fix introduced a defect: `rm -rf "${M%/r}"` now sits in the same executable fence that creates `$M`, so a verbatim run deletes the scratch mother repo before the verifier receives it, and "run it when the verifier returns" lives only in a `#` comment. Shell state does not persist between calls, so a deferred run has no `$M` either.  **CUT from the 2026-09-21 backlog batch 1** — the fix hoisted the deletion’s timing out of a `#` comment, and a verifier proved the mandate unexecutable: `$M` does not survive between shell invocations, so the deferred `rm -rf` is a silent no-op and the temp dir leaks. Closing it needs the recipe to persist the path, not a better sentence. | open |
| D-11 | LOW | `docs/modules/templates/metrics_md.md:21` vs `:14-17` | A-7's class, unswept to the twin template: the CODE-series baseline row reads `COMPLETE` with no `steps:` line although its own prose says Status is followed by the per-step line, and `framework_metrics_md.md:30` was updated. The same self-check gives 1 on the sibling row and 0 on this one. | open |

### Recovered — dropped from the chain after `audit-2026-09-20.md` (AF-2)

B-, V- and Z-series rows are copied verbatim from `audit-2026-09-15.md`; W-, X- and Y-series from `audit-2026-09-14.md`. Each keeps its ORIGINAL ID and status. W-26 carries a dated annotation.

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| B-1 | LOW | `.claude/commands/existing_project_adaptation.md:829-833`, `:805-813`, `:582` | A leftover coupled-set member (`codebase-audit` or `skill-gate`) at a tier below internal-tool gets no verdict from any fence in Steps 2.9 or 2.9b (probe E2: zero lines for a locally-modified leftover). The coupled-set question and slot can therefore read "no member differed" while one differs, or the set refresh can overwrite it without the diff being shown. Introduced by the V-9 fix. | open |
| B-4 | LOW | `.claude/commands/existing_project_adaptation.md:819`, `:1341` vs `.claude/commands/bootstrap.md:464-465` | EPA copies `ops-rules.md` but never fills the ops-rules §6 reconciliation queries for production-financial, which bootstrap Step 5.8 does; EPA's money-path slot carries only the red-team half. V-13's class, not swept. | open |
| B-15 | LOW | `.claude/commands/existing_project_adaptation.md:805-813`, `:1339` | The Step 2.9b tier fence prints nothing at prototype tier and also nothing for a PROFILE left as the placeholder or mistyped (probe G: `production_financial`), so a typo reads as prototype; the slot value `not copied (tier)` is printed by no fence. Pre-existing, in no earlier ledger. | open |
| B-20 | LOW | `.claude/commands/existing_project_adaptation.md` Step 2.9b tier fence (`tier_skill framework-audit` only in the `production` branch) | [filed by the maintenance session applying Run 3, on 2026-09-16, from its independent pre-commit verifier, round 1] A leftover `framework-audit` copy in a project now below `production` gets no verdict line, so the new `framework-audit` + `session-log-creator` ↔ `autonomous-loop` pairing rule cannot see it. B-1's class (leftover tier-gated members), not in this session's scope. | open |
| B-25 | MEDIUM | `docs/modules/rules/evolution_policy.md` → Exemption; `docs/modules/skills/session-end/SKILL.md` gate activity; `docs/modules/skills/framework-audit/SKILL.md` Q5; `.claude/commands/existing_project_adaptation.md` | [filed by the maintenance session applying the 2026-09-16 upstream and the carried X, W, V and Z findings, on 2026-09-16, from its independent pre-commit verifier, round 3] The exemption's commit-message line has no writer at the moment of an upstream copy (the adaptation command copies templates and never mentions it) and no reader: session-end counts only `promotion.log`, and nothing detects "a new file with neither that line nor a promotion.log entry", so an exempt file and a silent bypass look the same. A mechanical detector also cannot tell a copied rules template from a new rule: skill templates carry `created: framework-v…`, rules templates carry no stamp. | open |
| V-1 | MEDIUM | `.claude/commands/audit.md:233`, `:315-325` (D16.1, D16.3c); `.claude/scripts/d16-gate.sh` | The one-pass D16.3c procedure's `expected: …, 0 hits` is unreachable on a healthy repo: D16.2c identifiers are harvested from framework surfaces and read RED by construction (113 → `staged` 362 hits), D16.2b shapes the dimension calls legitimate read RED (e-mail → `log --all` 808 from author lines and the mandated trailer; `user:pass@` → 1 scanner example), the gate has no allowlist and prints one total, and "NEVER by printing the matched line" leaves D16.5's file:line and D16.2b's legitimacy judgement no sanctioned route. Introduced by the Z-3 fix. | open |
| V-2 | MEDIUM | `.claude/commands/maintenance.md:18-28`, `:438` vs `CLAUDE.md:111` | The push gate and the `isolation:` receipt run the gate on folder names only and never feed `D16_EXTRA`/`D16_EXTRA_RE`, while CLAUDE.md mandates D16 — all three blocklist sources — over the unpushed commits before every push; person names, domains and code identifiers in an unpushed commit are scanned only if `/audit` happens to run first. Z-3's class, not swept. | open |
| V-3 | LOW | `.claude/commands/audit.md:285` (D16.3), `:293` (D16.3b), `:192`, `:367` (Agent 1 FILES TO READ, REPORT FORMAT) | D16.3 and D16.3b still prescribe hand greps for the blocklist and value shapes over tracked files and memory, and Agent 1's input list and report format still describe the banned "contents + messages" shape with no paths, tags or `D16_EXTRA` proof. Z-3's class, not swept. | open |
| V-4 | LOW | `.claude/scripts/d16-gate.sh:17-21`, `:112-123` | Nothing proves the extras were loaded: the output line is identical with and without them; `D16_EXTRA=` set to an empty string is treated as unset and reads GREEN (the header says a set variable fails closed); a PCRE escape (`\d`) in `D16_EXTRA_RE` silently matches nothing; there is no minimum identifier length (a 2-letter identifier gives 25 false hits); the identifier files are unnamed and unpersisted, so a second reader cannot re-run the scan. | open |
| V-5 | LOW | `.claude/scripts/d16-gate.sh:14`, `:185`, `:29-30`; `.claude/commands/maintenance.md:26-28` | Z-1's class not swept: the script's usage line and error text still offer `origin/main..HEAD` as the example range; and the snippet's refnames are unqualified — a local branch `refs/heads/origin/main` at `main`'s tip with `core.warnAmbiguousRefs=false` makes the gate read `0 commits scanned` while the push publishes the leak. | open |
| V-6 | LOW | `.claude/scripts/d16-gate.sh` selftest (`:226-379`) vs `.claude/commands/maintenance.md:482` | The Z-21 fail-closed branches — empty or whitespace-only `D16_EXTRA`, NUL bytes or an unreadable `D16_EXTRA_RE`, an invalid ERE — have no selftest case, although the commit says "the gate cases are selftest cases". Extends Z-6. | open |
| V-7 | LOW | `.claude/commands/bootstrap.md:27-34`; `.claude/commands/existing_project_adaptation.md:27-34`, `:577-578` | Both Setup guards state "Every fence in this command expands `projects/$ARGUMENTS`" (false for 6 of 18 bootstrap fences and at least 4 EPA fences); the EPA backstop runs after 7 fences that already expand the name without saying so; bootstrap has no mechanical backstop. Introduced by the Y-8 fix. | open |
| V-8 | LOW | `.claude/commands/existing_project_adaptation.md:709-714`, `:577-578` | The Step 2.9 STOP rule bans continuing past a FAILED component with no mechanical self-check (component-design §6), offers no reportable value when the owner declines a fix (§9.3), and does not cover the backstop's `FAILED: project name…` lines. Y-9 residue. | open |
| V-10 | LOW | `.claude/commands/maintenance.md:978` | "Version bumps" still says `/existing_project_adaptation` refreshes an existing skill "only on the owner's per-skill decision"; the batch made EPA ask one question for the whole coupled set. X-4's lateral, not swept (the receipt's LATERAL grep covered EPA only). | open |
| V-14 | LOW | `.claude/commands/bootstrap.md:902` vs `:249-250`, `:269-270` | The Step 15 cross-cutting slot copies the Step 1.1 and Step 13 verdicts but still paraphrases the Step 3 and Step 4 receivers ("A/A decisions · T/T tasks") against their mandated `cross-cutting received: …` lines. W-18's class, not swept. | open |
| V-18 | LOW | `.claude/commands/maintenance.md:966-973` ("Version bumps") | The PATCH/MINOR policy does not classify additions to the command pipeline, and afa8ef0 was labelled PATCH while adding new contract — a line written into adapted projects' `code-reviewer.md`, new report sections, new Setup and STOP mandates, a new gate interface — which exempted it from trigger (b). Second consecutive batch with the ambiguity (Run 1's observation). | open |
| V-19 | LOW | `.claude/commands/maintenance.md:786-791`, `:802-804` | The control-enumerator's inline shape counts any backticked `git …` span, so the "+1" recorded in the re-measured baseline is `` `git push origin main` `` — the gated action, not a control; the baseline comment calls it "one inline control". | open |
| W-15 | LOW | `.claude/commands/bootstrap.md:145-156` | Step 1.2 says "Steps 2 and 3 ALWAYS confirm receipt", but neither receiver mentions the risk profile. The mandate lives only in the sender (component-design §9). | open |
| W-21 | LOW | `.claude/commands/maintenance.md:236`, :177 | The elision grep catches only a LEADING `<` on a `$` line. It returned 0 on d812c07 while two mid-line angle-bracket `$` lines existed (a looser form returns 2). The "stdout must be literal, never a parenthetical" rule has no mechanical check. | open |
| W-22 | LOW | `.claude/commands/maintenance.md:420` | The references denominator regex cannot see `→ "Heading"` citations, the class d812c07 added (5 distinct), so `references:` under-counts its own stated unit. | open |
| W-23 | LOW | `.claude/commands/audit.md:243-246` | The copy-path derivation returns 29 lines against a prose list of exactly 5; an "at least 5" threshold cannot realistically go RED. | open |
| W-24 | LOW | `.claude/commands/audit.md` D6.7 | "diff the two install BLOCKS — table AND prose — expected: empty" no longer fits the commands, which scope parity to the marked TWINNED SPAN (U-24). The whole-block diff is structurally non-empty (rc=1), while the TWINNED SPAN diff and the loops are identical. | open |
| W-25 | LOW | `.claude/commands/maintenance.md` Audit intake item 8, :537 | A correction-only follow-up commit has no stated receipt form. Its three executions (d41022b, b9d2ee2, e82176f) used three forms. Item 7's exemption regex covers bumps and substitutions, not corrections. | open |
| W-26 | LOW | `.claude/commands/maintenance.md` "Version bumps"; CLAUDE.md trigger (b) | "PROPOSE /audit BEFORE a MINOR bump" only proposes and nothing gates it. d812c07 applied the bump before the audit. Whether an unpushed bump counts as "not yet a bump" is undocumented. **Recurred — re-measured 2026-09-23 (`/audit` Run 1, Agent 6):** the "bump committed, `/audit` after, push held" ordering has now run at least 4 more times (`adc7481` v2.29.0; `ece8b08` v2.32.0, PUSHED at 15:19 in `e895711` before its audit persisted at 15:37 in `ab90b2c`; `2cb4ede` v2.33.0, "do not push before it runs"). The hold lives only in a commit body, a home retired on 2026-09-22, and the push gate (`maintenance.md:18-28`) checks D16 only. Recovered here rather than re-filed. | applied `sefca24e` |
| W-27 | LOW | `docs/modules/skills/session-end/SKILL.md:112` vs :125 | "Items 1-3 are the critical minimum" vs "at minimum items 2, 3, and 6". | open |
| W-29 | LOW | session-end:125; sprint-proposer:199-203, :214, :240; validation-orchestrator:104, :145; validator.md:147-148, :164-166; code_reviewer.md:208-210, :264, :291 | Pre-existing instruction-style instances (lowercase or title-case imperatives, fields buried in prose). | open |
| W-31 | LOW | `docs/modules/skills/rules-agents-updater/SKILL.md:119` | The vocabulary-alignment check is still a whole-file `grep -l`, the T-9 false-match shape the activation loop was fixed to avoid. | open |
| X-15 | LOW | `assets/docs/audit-2026-09-14.md` Run 1 receipts (`:379`, `:411-425`, `:456`, `:498`, `:531`) | The batch's receipts repeat the evidence-form class W-11 corrected: 20 `✓` cells with no quoted command; `push:` outside the enumerated verdicts; `control back-sweep:` not in `K of N` form; `negation proof:` points at a command-less commit body; `:438`'s negation is claimed with no `$` line; "no figure below was typed" is false. | open |
| X-18 | LOW | `.claude/commands/maintenance.md:931-935` | The new "ALWAYS NAME THE MIGRATION PATH" rule has no receipt key and no mechanical check. | open |
| Y-12 | LOW | `.claude/commands/maintenance.md:638` | Gate 3 run verbatim over the report returns 7 against 6: `audit-2026-09-14.md:901`, a receipt quoting the pattern, self-matches. The section-scoping rule at :647-653 is prose; the command itself is unscoped. | open |
| Y-14 | LOW | `audit-2026-09-14.md` receipts (`s700a382`, `:810-964`) | X-15 recurs. 8 of 15 `✓` cells quote no runnable command (one records the X-23 defect under `✓`). `push:` is outside the enumerated form. `control back-sweep:` reads "2 of 63 re-run" against the mandate to run every control. `negation proof:` gives no `$` line for the gate seeds. The `liveness:` `$` line is a rewritten variant of the mandated command. | open |
| Y-16 | LOW | `.claude/commands/audit.md` D17.6 | The shipped/apparatus split is by path, so `existing_project_adaptation.md` and `bootstrap.md` — the delivery path of the shipped contract — count as apparatus. X-1 (HIGH, 0 process agents delivered to existing projects) sat in a "0% shipped" batch. | open |
| Z-2 | MEDIUM | `.claude/scripts/d16-gate.sh:224-260`, `:305`, `:309-310` | The selftest generates its seeds with the gate's own derivation, so a derivation gap is invisible to it: over a synthetic mother repo with an uppercase-run CamelCase and a dotted folder it reads `66 of 66` while the gate reads 0 on their variant forms; with the letter-boundary fix mutated out it still reads `37 of 37`. Its green controls are five fixed words that cannot fail under a different folder set. Y-6's class restated at the oracle. | open |
| Z-4 | LOW | `.claude/scripts/d16-gate.sh:76-83`, `:32-37` | Name derivation splits only on `_`, `-`, space and on lower→Upper: a folder name with `.` or `+`, or an uppercase-run CamelCase (acronym prefix), derives no working variants or parts (snake, spaced and part forms read 0). Neither fails closed nor appears in KNOWN LIMITS. Latent: 0 of 3 real folders. | open |
| Z-5 | LOW | `.claude/scripts/d16-gate.sh:32-37` | Content transformed before it reaches grep — gzip, base64, a deflated `.docx`, an HTML entity inside a part — reads 0 hits (plain text: 2) and is not a declared KNOWN LIMIT. Latent: no tracked binary/encoded files today. | open |
| Z-6 | LOW | `.claude/scripts/d16-gate.sh` selftest (`:227-310`) vs `.claude/commands/maintenance.md:475` | No selftest case for a regex-metacharacter folder name, a missing `projects/` or a bad revision range, although the batch's own rule requires a case for every form the gate is taught; those negations rest on unpersisted verifier seeds. | open |
| Z-7 | LOW | `.claude/scripts/d16-gate.sh:129-133`, `:310` | GREEN has no floor on units scanned: `0 commits scanned` / `0 files scanned` exits 0; `log HEAD..HEAD`, a mistyped receipt range, or `GIT_INDEX_FILE` pointing at an empty index turn a leak (2 hits) into `0 hits`; the selftest treats an empty index as a GREEN control. | open |
| Z-8 | LOW | `.claude/scripts/d16-gate.sh:36-37`; `.claude/commands/audit.md:317` | `log --all` reads no ref NAMES: a branch name, a lightweight tag name, and a lightweight tag pointing at a leaking blob each read 0. KNOWN LIMITS mentions tag objects only. Real ref names are clean (17 lines, 0 hits). | open |
| Z-9 | LOW | `.claude/commands/maintenance.md:204`, `:432`, `:454-456`; `audit-2026-09-14.md` receipts `isolation:` | A receipt that lives in a commit body cannot run the mandated `log <first>~1..<last>` before the commit exists: 6b4baa8 and 30c3d6e discharged with the `staged` form and a file count, the unit Y-11 removed. The persisted report receipt paraphrases the gate line instead of pasting it. | open |
| Z-10 | LOW | `.claude/commands/audit.md:1317` | Phase 3 item 6 states `GREEN 9+2=11 today` beside a command that returns `GREEN 11+2=13`: a copied run count in the defect-series section, the class Y-13 fixed two paragraphs away. | open |
| Z-12 | LOW | `.claude/commands/maintenance.md:980-985` | The version-bump residue check expects "ZERO hits" after a bump but excludes only `created: framework-v`; the lineage sentence at `:970` ("v2.23.0 shipped continuous mode") reads 1 on a healthy state, so the Run 3 receipt narrated the exception instead of the check stating it. | open |
| Z-13 | LOW | `.claude/commands/maintenance.md:503-519` | The LATERAL "redirect with no mkdir" control mandates "BOTH HALVES" but its fence carries a command for half 2 only; half 1 is described in a comment and can never go RED. | open |
| Z-14 | LOW | `CLAUDE.md:7`; `docs/agentic_engineering_framework.md:204-222` | The inventory class was not swept: CLAUDE.md:7 "Only documentation, templates, examples, and project folders" became false with `.claude/scripts/d16-gate.sh`, and the concepts doc's framework-repo tree lists `.gitignore` but not `.gitattributes` (and has no `.claude/` block) — while the batch's `inventory sweep:` receipt names that doc as checked. | open |
| Z-15 | LOW | `.claude/commands/maintenance.md:415-417`; `.claude/commands/audit.md:232` | Two batch-written requirements are not dedicated imperative lines: the D16 gate mandate is the third clause of the fence-failure sentence, and "Extend by hand only with the D16.2 / D16.2c identifiers" is a lowercase constraint after the ALWAYS line. The commit claimed `0 buried` (the B regex cannot see either shape). | open |
| Z-16 | LOW | `.claude/commands/bootstrap.md:369-373`, `:439-442`; `existing_project_adaptation.md:553`, `:673-675`, `:838` | The 6 shipped `.sh` skill scripts (one wired as a PostToolUse hook) get no LF protection in bootstrapped or adapted projects: neither twin writes a `.gitattributes` or mentions eol, while the mother repo's new `.gitattributes` names exactly the CRLF failure and protects only its own checkout. | open |
| Z-17 | LOW | `.claude/commands/maintenance.md:458-461`; `.claude/commands/audit.md` (0 `selftest` references) | The gate's negation proof runs only in a session that edits the script; `/audit` D16 runs the gate but never its selftest, so git/grep drift after the edit (the Y-19 class) goes unproven between edits. | open |
| Z-18 | LOW | `.claude/commands/audit.md:175-192` (Agent 1 FILES TO READ) | D16.1 (`:229`) and D16.3c (`:317`) name `.claude/scripts/d16-gate.sh`, but Agent 1's FILES TO READ does not list it — the K-15/L-3 class; this run supplied it from the dispatch prompt. | open |
| Z-19 | LOW | `.claude/commands/audit.md` D17.6 command | The shipped/apparatus pathspecs cover neither `README.md`, `docs/agentic_engineering_framework.md` nor `.gitattributes`: 17 lines of this batch, all of d156dc4, are counted nowhere. Extends Y-16. | open |

**Re-verification method (Phase 3 item 3).** The audited range is `c98db39..9b0fffa`; `git diff --name-only` over it lists 14 files. For each carried or recovered finding:
- **File not in that list:** still true by construction (the mechanical LOW rule), and the MEDIUMs were carried the same way.
- **File in the list** (`audit.md`, `existing_project_adaptation.md`, `session_rules.md`, `evolution_policy.md`, `validation-orchestrator/SKILL.md`, `security_reviewer.md`, `component_design.md`): the hunks were read.
  - `audit.md` changed one version string (`:379`).
  - EPA gained an inserted migration note at `:700-712`. That shifts every EPA line cited after `:697` by +13, but no finding's text is touched.
  - The other five gained only the six v2.33.0 text additions, and none of them overlaps a carried finding's location.

**Re-checked live by agents this run:**
- **AE-15:** §13 still has no invoker (Agent 3).
- **AC-10 / AD-15:** the D6.7 diff exits 1 on the two legitimate hunks (Agent 3).
- **AD-14** (Agent 4).
- **C-8:** reproduced live, `report committed: s9b0fffa` with HEAD the receipts commit (Agent 4).
- **AA-12** (Agent 4).
- **C-15** (Agent 4).
- **W-27** (Agent 5).
- **W-26:** recurring (Agent 6).
- **Z-6 / V-6:** the D16 selftest case count was not re-measured this run. They stand as filed, with the note from `audit-2026-09-20.md:14`.

## Detailed Findings

## Agent 1: Structural Sync

### PART 1 — `Part 1: N/A — baseline mode`

### [A] Dual Placement
- Status: PASS
- 15 folders in `docs/modules/skills/`. The 1 folder in `.claude/skills/` (`cross-cutting-analysis/`) also exists there and is the documented runtime skill for `/prd_planning` and `/prd_change` (A.4). Findings: none.

### [D4] Skills README vs Disk
- Status: PASS
- README lists 15 skills; disk has 15 folders.
- Missing from README: none. Missing from disk: none.
- Count claims correct:
  - `:8-13` "15 = 12 lifecycle + 3 tier-gated" (1+5+3+1+1+1).
  - `:38` "7 declare `user` … 8 declare `inline`": `grep '^invocation:'` gives 7 user (autonomous-loop, codebase-audit, commit, context-recovery, framework-audit, session-end, sprint-proposer) and 8 inline, names matching.

### [D9] CLAUDE.md Diagram vs Disk
- Status: PASS
- In diagram but not on disk: none. On disk but not in diagram: none significant (`assets/docs/` holds 15 audit files and 8 lineage/base docs, all covered by the generic entry, incl. the new 2026-09-23 lineage doc).
- Counts verified:
  - 6 commands (5 + 1), `.claude/scripts/` 2, `.claude/rules/` 1;
  - templates 8, module agents 10, module rules 5, skills 15 (12 + 3);
  - examples 20 / 9 / 11.
- Mismatches: none.

### [D16] Project-Information Isolation
- Status: PASS. Live published surfaces are clean. 1 LOW is in a gitignored note (AF-13); history residue is E-1.
- **Blocklist derived from all three sources:**
  - **3** project folder names (variants and ≥4-char PARTs derived by `d16-gate.sh`).
  - **48** identifiers harvested from project DOCS: domains, a personal e-mail, a phone, Supabase refs, a city, person names, a client org, a product name, single-project domain terms, source doc filenames, pilot agent/hook/runner names, task-ID prefixes, a table and a constant name, log filenames, a screenshot prefix, program identifiers.
  - **540** camelCase/snake_case identifiers harvested from every framework-layer surface (D16.2c), cross-grepped against 11,218 identifiers from 1,014 project sources, excluding the `bootstrap.md`-derived copy paths plus `node_modules/`, `.next/` and `out/`. Intersection: **111**, all standard industry/framework/platform vocabulary; the hand-rolled-looking ones are canonical e-commerce names in `examples/` dating to `3e581b1` (v1.0). 0 source-project session numbers.
- **Value shapes (D16.2b):** 11 EREs (JWT, key prefixes, `user:pass@` DSN, long Bearer, BR phone, CPF, CNPJ, personal-provider e-mail, gmail sweep).
- **Surfaces scanned:**
  - **Tracked files** (`staged`, 127 files): base 0 hits. With extras, 1 hit: the exempt detection example at `examples/agents/secrets-scanner.md:84`.
  - **`.claude/docs/`** (`dir`, 4 files): base 0. With identifiers, 1 hit (AF-13).
  - **Agent memory** (20 files): 0 hits on all three classes. 42 of 42 hex tokens outside UUIDs and URLs resolve via `git cat-file -e`.
  - **Git history** (`log --all`, 263 commits: patches, messages, raw objects, tags, paths): base 0. With extras, 12 hits: 1 D16.2b-exempt DSN example in `3e581b1`, and 11 under E-1 (9 of one source doc filename, 2 of one domain term in `3e581b1` / `11ce6e9`).
  - **Unpushed range** (`log origin/main..HEAD`, 2 commits): 0 hits on all classes.
- The new lineage doc and the `2cb4ede` diff were hand-reviewed as well: they use role descriptors only.
- **Unpushed-vs-pushed:** every history hit is PUSHED and covered by E-1 or D16.2b. The push-gate range is GREEN.
- No sandbox used (every check read-only, in place).

## Agent 2: Bootstrap Integrity

### PART 1 — `Part 1: N/A — baseline mode`

### [D5] File References
- Status: PASS
- 56 framework-resolvable references (28 `docs/modules/` sources, 26 `examples/` references, the 2 command twins at `:712`): **56/56 resolve**. Project-tier targets resolve in the project tier.
- Cited headings exist: `session_rules.md` → "Execution proof" (`:125`) and "Model by risk class" (`:184`); `settings_json.md` "Merge rule" (`:75`).
- Fence extraction: all 9 sed-extracted templates carry exactly 2 four-backtick fences, and every extraction is non-empty (incl. the 4 rule templates this batch edited: `session_rules` 337 lines, `evolution_policy` 170, `component_design` 366, `ops_rules` 57).
- D5.4 naming consistent.
- Twin diffs: TWINNED SPAN rc=0; activation-chain loop rc=0; gap install tables rc=0.
- Unresolved: none.

### [D11] Step Sequencing
- Status: PASS
- 15 primary + 10 sub-steps (0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5) = 25, or 26 with Setup.
- Claim at `docs/agentic_engineering_framework.md:50`, `:389`: qualified "15-step pipeline", which is accurate. The ASCII diagram matches 26/26 rows in order.
- Forward references: one flagged and paired (`:472` → Step 14.2 `guards`, received at `:910`); every other later-step dependency is a declared hand-off.
- The EPA v2.33.0 note (`:700-711`) points only backward, and its targets all exist.
- Version stamps agree (`claude_md.md:4`, EPA `:260` / `:609`, README `:1` / `:151`).

### [D13] PRD ↔ Bootstrap Compatibility
- Status: PASS
- The PRD template defines Cross-cutting Concerns, §§1.1-9 and the Changelog. Bootstrap reads no section the PRD lacks; every parenthetical heading matches verbatim.
- INFO (defined but ignored): 1.4, 7.2 / 7.3 individually, 8.
- The EPA twin's Phase 3 lists every template section.

### [D14] Output Completeness
- Status: PASS
- 17 distinct references in the fenced body of `claude_md.md` (90 lines): **17/17 traced**. Both step placeholders have replacement mandates (`:357`, `:885`).
- Step 15 (`:1006-1104`) lists every artifact from Setup through 14.5, and all 8 `ALWAYS REPORT` keys have a slot.
- No writing command run; no `probe-sandbox:` line.

## Agent 3: Activation Chain

### PART 1 — `Part 1: N/A — baseline mode`

### [D6] Vocabulary Alignment
- Status: PASS
- **Gap declarations found:**
  - code-reviewer: accessibility, performance, concurrency, visual regression, data integrity (`code_reviewer.md:214,222,230,237,249`).
  - security-reviewer: static analysis, secrets coverage, federation protocol, compliance, infrastructure security (`security_reviewer.md:250,259,267,277,285`, plus the bold form at `:200`).
  - validator: visual regression, bold form (`validator.md:150`).
- **§13 (the batch)** added no gap phrase; the `description:` is unchanged. §8 rule 4 is identical in both twins and adds no gap vocabulary.
- **Specialist matches:** all 10 gaps match exactly (accessibility-checker, performance-auditor, concurrency-tester, visual-regression-tester, data-integrity-checker, sast-scanner, secrets-scanner, oauth-flow-tester, compliance-auditor, iac-scanner). Each has an install row in bootstrap (from `:674`) and EPA (from `:1302`).
- **Install-block diff (D6.7):** exit 1, two hunks, both the known path-specific ones:
  - `1c1`: bootstrap's Step 7/8/10 parenthetical vs EPA's "ALREADY EXISTS".
  - `17c17`: the lineage stamp.
  
  The table rows are byte-identical; TWINNED SPAN exit 0. This is AC-10 / AD-15, not re-filed.
- **Fourth-surface check (D6.9):** `docs/agentic_engineering_framework.md:998-1006` names 3 declarers; policy §1/§3 names 3; match: **yes**.
- `declarers extracted: 3 of 3`
- **Gap SOURCES (D6.8):** code-reviewer, security-reviewer, validator. Policy (both twins): the same set; match: yes.
- Broken links: none. Orphaned specialists: none.

### [D7] Pushy Description Compliance
- Status: PASS
- 30 agents checked:
  - 10 in `docs/modules/agents/`: 9 PARTIAL (sanctioned, protocol-spawned) and `skill_reviewer` COMPLIANT.
  - 20 in `examples/agents/`: all COMPLIANT.
- **Activation chains:**
  - bootstrap twin: `activation chains: 10 verified, 0 broken, 17 info`
  - EPA twin: `activation chains: 10 verified, 0 broken, 17 info`
  - `twins agree: yes` (loops byte-identical, 78 lines; INFO lines identical)
- **Regression set, 14/14 correct on both twins:**
  - #1 BREAK; #2-#5 VERIFY; #6 BREAK on the second; #7-#8 VERIFY; #9-#10 INFO, no BREAK;
  - #11 `OAuth2 federation gap` → BROKEN (seen); #12 `Node.js runtime gap` → BROKEN (seen);
  - #13 validator-only → VERIFY; #14 → `none installed`, exit 0.
- probe (D7.6): synthetic project under `…/scratchpad/agent3/work`, run by absolute path in the sandbox clone. `probe-sandbox: GREEN — live repo unchanged; command exit 0`
- **Planted-specialist probe (D7.8):** `BROKEN CHAIN: zz-planted-gap declares 'quantum entanglement gap' …` plus `INFO: zz-planted-plain …`, giving `10 verified, 1 broken, 18 info` on both twins. GREEN.
- **Invariant proof (D7.5):** `files: 10 | output: 0`, which is GREEN.
- Anti-pattern instances: none.

## Agent 4: Orchestration & Commands

### PART 1 — `Part 1: N/A — baseline mode`

### [D8] Orchestrator References
- Status: PASS on name resolution. Every skill and agent name referenced by session-end, validation-orchestrator, sprint-proposer, autonomous-loop, skill-gate, codebase-audit and framework-audit resolves. codebase-audit's `data-integrity-checker` / `performance-auditor` are conditional by design.
- **Cross-section citations:** every citation resolves exactly or as a sanctioned prefix / ordinal / parenthetical, incl. the new ones from `2cb4ede`:
  - §13 → component-design §9 rule 4
  - §8 rule 4 → rule 3 / "Autonomous loop watchdog"
  - EPA `:700-711` → six targets
  - D8.8 `codebase-audit:174` → `code_reviewer.md:278`: exact
- **RED:**
  - AD-14: known.
  - Lineage doc `:44-45` → "v2.33.0 migration": a bold lead-in (AF-11).
  - EPA `:1135-1136` → "Gate 1 (TWIN PARITY)": a bold lead-in (AF-12).

### [D10] Command → Skill Paths
- Status: FAIL (D10.0 only).
- Every D10.1-D10.5 reference resolves: cross-cutting-analysis generation/consultation modes; component-design §5/§6/§8/§9; "New component creation", "Version bumps", "Audit intake", "Upstream intake", "Cycle governance"; `/audit` Phase 0 / Phase 3 / defect series / ID allocation / Errata; CLAUDE.md (a)-(d); "Execution proof". C-15 (known) covers the non-heading citations.
- **Self-checks executed (D10.0):** ~40 checks from `maintenance.md` and `audit.md`, read-only ones in place and writing ones in the sandbox. All matched their stated expectation except those below.
  - **Matched, notably:**
    - push-decay `unchanged at c98db39`; trigger (d) does not fire; moratorium 27/14;
    - receipts loop 27 × 1/≥1 over the 09-23 section; instruction-style receipt reproduced (`10 added, 0 rewritten, 0 buried`); fences 13/13;
    - D16 staged `127 files scanned, 0 hits`;
    - Gate 1 exit 0; raw-template YAML `19 validated of 25 globbed, 6 skipped (fenced), OK`;
    - version residue 1 (EPA:711, the stated residual); defect series `GREEN 18+2=20`;
    - D10.6 parity 1/1/1; D7 invariant `0`; copy-path derivation ≥5.
  - **Negation-proved this run:** moratorium (planted item → 15), Gate 1 (planted byte → exit 1), D10.6 (README plant → 0), defect series (row deletion → `RED 18+2 vs 19`).
  - **Exceptions:**
    - **report-committed** (`audit.md:1569-1570`): fires `report committed: s9b0fffa` BEFORE this run's report exists. C-8, reproduced live.
    - **prediction grep** (`audit.md:1406-1407`): 0 over the newest file (receipts-only) vs 1 over 09-22 (AF-6).
    - **references denominator:** counts file targets only, so AF-11 is invisible to it.
    - **push command:** the push half was NOT RUN (forbidden to agents).
    - D16 `log --all` with extras and D7.6 were delegated to Agents 1 and 3.
  - `probe-sandbox:` lines (liveness; ratchet; enumerator at base; negation plants): each `probe-sandbox: GREEN — live repo unchanged; command exit 0`.
- **Runtime dual copy (D10.4):** `cross-cutting-analysis` present in both; byte-identical: yes (`diff -r` / `cmp` exit 0, 7985 bytes).
- **Trigger-list parity (D10.6):** CLAUDE.md (a)-(d) | `audit.md` Phase 0 (a)-(d) | README `/audit` row: all four with the outside-the-loop condition. Match: yes.

## Agent 5: Document Accuracy

### PART 1 — `Part 1: N/A — baseline mode`

### [C] Instruction Style
- Status: FAIL
- Sample: the contract's 5 files, plus every added line of `git diff 2cb4ede~1 2cb4ede -- docs/modules .claude/rules`.
- **Added lines:**
  - §8 rule 4 (both twins), the validation-orchestrator VERBATIM line and evolution_policy: clean, dedicated bold lines.
  - ops_rules §6: a checklist question, like its neighbours.
  - session_rules: descriptive text inside the HYPOTHESIS blockquote (correct form).
  - security_reviewer §13 last item: a compound check (AF-5).
- **9 anti-pattern instances:**

  | File | Line | Issue | Where filed |
  |---|---|---|---|
  | security_reviewer.md | 226 | two requirements in one box | AF-5 |
  | validation-orchestrator | 97 | mobile viewport / browser automation mid-step | AF-15 |
  | validation-orchestrator | 163 | 3-part prompt content in a paragraph | AF-15 |
  | validation-orchestrator | 305 | lowercase "Never" in a legend | AF-15 |
  | validation-orchestrator | 342 | step with no output format | AF-15 |
  | sprint-proposer | 257 | context health check | AF-15 |
  | sprint-proposer | 219 | "note this in Risks" | AF-15 |
  | validator.md | 148-149 | responsive / cross-browser prose | covered by W-29 |
  | code_reviewer.md | 292 | per-bug rule | AF-15; W-29 lists `:291` |

### [E] Version Consistency
- Status: PASS
- Canonical: v2.33.0 (`README.md:1`).
- Matches: README `:1` / `:151`; `claude_md.md:4`; EPA `:260`, `:609`, `:700`; `audit.md:379`.
- Exempt:
  - EPA:711 v2.32.0, inside the v2.33.0 note (lineage).
  - `docs/modules/skills/README.md:35` v2.1.0 (history).
  - 20 `created: framework-v*` fields (E.5).
- `git show 2cb4ede | grep 'created: framework-v'` → nothing, so no field was rewritten. Mismatches: 0.

### [D12] Examples Convention Compliance
- Status: PASS
- Sample: `concurrency-tester`, `load-tester`, `secrets-scanner`, `nextjs-supabase`, `ci-cd-pipeline`, `auth-rules`, `resilience-rules`.
- Full-set truth tests:
  - **agents 20/20:** frontmatter incl. `model: inherit`; `## Output Format` / `## Input` / `## When this agent is invoked` / `## BOUNDARIES`; checklists.
  - **skills 9/9:** folder/SKILL.md, `invocation: inline`, 0 `model:`; the 3 stack skills have `## Testing`.
  - **rules 11/11:** `domain:` / `paths:` / `# Scope:`; 0 forbidden keys; "1 of 11" and "5 of 11" claims verified.
- Violations: none.

### [D15] Factual Accuracy
- Status: PASS
- Every numeric claim checked in README, the concepts doc, `docs/modules/README.md`, `docs/modules/skills/README.md` and CLAUDE.md matches disk:
  - 8 templates / 10 agents / 5 rules / 15 skills (12 + 3) / examples 20-9-11;
  - 6 commands; 17 dimensions / 6 agents; 15-step pipeline; 3 process agents; 7 user / 8 inline;
  - validator 14 categories / 10 verdict tokens.
- D15.2 coverage: maturity Levels 1-5 all have README rows; all 6 commands are in both README tables.
- Banned "inline" sense: 0 survivors.
- Inaccuracies: 0.

## Agent 6: Process Coverage (meta)

### PART 1 — `Part 1: N/A — baseline mode`

### [D17] Process Coverage
- Status: FINDINGS
- **Commits classified: 40 (`d3d76cf..9b0fffa`):**
  - audit-fix application 7; write-back 6; maintenance feature/fix 5;
  - placeholder substitution 4; commit correction 4; push record 4 (`e895711` went into a lineage doc, now forbidden, `maintenance.md:62`);
  - audit persist 3; upstream absorption 2; errata 2; receipts-only persist 1;
  - backlog triage 1 and owner-decision record 1 (each once, below the 2+ threshold).
- **Undocumented executed flows:** "bump committed first, `/audit` afterwards, push held until it runs". Instances: `adc7481` v2.29.0; `d812c07` v2.23.0; v2.24.0 pushed before its audit (`audit-2026-09-15.md:746`); `ece8b08` v2.32.0 PUSHED 15:19 (`e895711`) with its audit persisted 15:37 (`ab90b2c`), no `audit:` line; `2cb4ede` v2.33.0.
  - `maintenance.md:1278-1281` and `CLAUDE.md:153` say the audit runs BEFORE the bump; neither says the bump is published at the push.
  - The hold lives in a commit body, a retired home, and the push gate checks D16 only.
  - This is **W-26**, recovered and annotated. Natural home: Version bumps, plus a clause in the push rule.
- **Referenced-but-unowned conventions:** the new `evolution_policy.md:138-141` asks a pilot doc to record its cut condition, and no Upstream intake step reads it (`grep -ci "cut condition\|pilot"` → 0). It was consumed by judgement this time. Below the finding bar: the template promises no mother-side behaviour.
- **Aspirational mechanisms:** trigger (b)'s "before every MINOR/MAJOR bump" has an invoker that only proposes; practice over 5 bumps is audit-after-commit (W-26). Other framework-level cadence claims have owners.
- **Unprovable self-checks (D17.4)** — the D10 verdicts are Agent 4's, cited:

  | Check | Can go RED? | Can go GREEN? | Verdict |
  |---|---|---|---|
  | push-decay detector | yes | yes (reads the receipts-only file → `unchanged at c98db39`) | provable |
  | Phase 0 post-push D16 | yes | yes | provable |
  | trigger (d) PEND/OBS | yes | yes | provable |
  | ID collision | yes | yes | provable |
  | defect-series parity | yes | yes | provable |
  | prior-prediction grep | yes | yes over 09-22 | wrong input: no receipts-only skip (AF-6) |
  | receipts-only skip | no check exists (prose "header says") | n/a | unprovable; "header" undefined once a file holds two runs (AF-1) |
  | `measurer:` N/M/U figures | `$` grep only | yes | low, not filed |
  | `2cb4ede` new checks | none added | — | no new unprovable check |

- **Self-reported controls (D17.5):**

  | Control | Verified by | Persisted | Gate? |
  |---|---|---|---|
  | `audit:` "do not push before it runs" | nobody at push time | commit body + 09-23 receipts | **No** |
  | receipts-only header / skip | nobody | the header | No |
  | hand-typed keys (4) | author | 09-23, named | No (disclosed) |
  | pre-commit verifier (10 defects, 9 fixed, 1 filed) | independent agent | 09-23 list | Yes |
  | generator keys (25 of 27) | re-runnable | 09-23 fence | Yes |
  | `verification audit:` | command-decided | 09-23 | Yes |

- **Convergence (D17.6), measured** — shipped = `docs/modules` + `examples`; apparatus = `.claude` + `assets/docs` + `CLAUDE.md`:

  | Range | Kind | Shipped | Apparatus |
  |---|---|---|---|
  | `79fbd19~1..3ea9ece` | audit-fix | +1/-1 | +241/-28 |
  | `34a7c1c~1..c9355b0` | feature | +38/-1 | +41/-3 |
  | `961244b` | feature | +26/-2 | +31/-7 |
  | `280c2f2~1..f48b421` | audit-fix | +2/-2 | +362/-42 |
  | `7899e99~1..08e2123` | backlog | +13/-7 | +188/-30 |
  | `ece8b08~1..ece8b08` | upstream | **+341/-45** | +193/-10 |
  | `ab90b2c..c16b5d4` | audit-fix | +10 | +326/-30 |
  | `d925474~1..c98db39` | corrections | 0 | +27/-10 |
  | `2cb4ede~1..2cb4ede` | upstream | **+43/-2** | +93/-3 |
  | `2cb4ede~1..9b0fffa` | upstream + receipts | +43/-2 | +371/-3 |

  - **Mechanical judgement:** shipped content comes from upstream absorptions and features. Audit-fix batches return 1-13 shipped lines against 188-362 apparatus lines. **The audit-fix loop consumes more than it returns.** That is the already-governed state (Cycle governance; `cc3e14b`), and nothing here contradicts it.
  - **`2cb4ede`'s own controls are adequate:** 0 new self-checks or report keys, and 2 defects fixed by CUTTING mandates with no producer. The residual cost is receipts: 278 lines to certify 43 shipped lines (6.5×). For upstream batches, receipts are now the dominant apparatus term.

## Recommended Fixes

0. **LOW BACKLOG — never a session on its own.** Applied only when a later session already edits their file (`/maintenance` → "Cycle governance"):
   - AF-7 and AF-8 (`session_rules.md`);
   - AF-9 and AF-15 (validation-orchestrator, sprint-proposer, code_reviewer);
   - AF-10 and AF-11 (the 2026-09-23 lineage doc);
   - AF-12 (EPA);
   - AF-13 (a gitignored note);
   - AF-14 (`audit.md`);
   - plus the carried and recovered LOWs.
1. **Quick fixes:**
   - AF-3 is a one-token correction ("9 sections" → derive the count or cite 13) on a SHIPPED path, which is why it is MEDIUM.
   - AF-10 is one clause.
   - AF-6's two readers take the same skip clause the other two already carry.
2. **Structural fixes:**
   - **AF-1 first.** The next `/maintenance` would otherwise read `audit-2026-09-22.md` as "the most recent" report.
     - This run's instance is already remedied by the superseded header, so the fix is to the RULE.
     - The natural shape is DELETION rather than a new control: Phase 3 item 1 converts an existing `receipts-only` file (supersede its header) instead of appending under it.
     - The alternative is to define "header" as the file's LAST `**Run mode:**` line.
   - **AF-2's remainder is AD-1's:** rows are now required and recovered, and nothing counts them. Do not add a control for it while the moratorium stands.
   - **AF-4** (EPA must carry the v2.32.0 Session Protocol line to adapted projects) and **AF-5** (§13's last box needs an evidence source or a split) both change SHIPPED behaviour.
   - **W-26**, recurring, is an owner decision in all but name: either the push gate reads a pending trigger-(b) audit, or CLAUDE.md's "before" is reworded to "before the bump is PUSHED".
3. **Quality improvements:** AF-15 and the W-29 set.
4. **ESCALATED — owner decision required:** none this run. W-26's wording choice is flagged for the owner under item 2.
5. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48, B-17 (header OBSERVATIONS).

## Meta-observation

**prediction: 3 clauses evaluated — (1) void, (2) void, (3) held.** Scoped to `audit-2026-09-22.md`, the previous report with a ledger. The literal "newest file" resolves to this receipts-only file and returns 0 (AF-6).
- **(1) void.** AE-9 was fixed by RETIRING the commit-body receipts home (`maintenance.md:359-365`), not by adding a heading completion. The antecedent never occurred.
- **(2) void.** The lineage push home was retired too ("NEVER record a push anywhere else", `maintenance.md:62`), the route the clause named as its falsifier. The "documented only" antecedent never occurred.
- **(3) held.** The next batch shipping to `docs/modules/` (`2cb4ede`, +43/-2) came from a project evolution doc. The report-derived batch moved 10 lines under `docs/modules`, below the >50 falsifier.

**The recurring class this run saw: a new FILE KIND, or a new LIST MEMBER, swept to the readers someone remembered and not to all of them.**
- The `receipts-only` file kind was swept into 2 of at least 5 places that resolve "the report". It missed:
  - the WRITER that appends to it (AF-1);
  - the prediction reader and Agent 1's input (AF-6).
- The carry-forward walk-back reached the reports someone remembered and stopped short of two more (AF-2).
- A migration note patched the one security-reviewer section it was about, while the step that owns the upgrade still counts 9 of 13 (AF-3).
- A template line added in v2.32.0 reached bootstrapped projects and not adapted ones (AF-4).

In each case the change was correct where it landed, and nothing ENUMERATED the full set of places it had to reach. This is not new; it is the class-sweep problem the checklist names. The difference this run is that the missed members are READERS of a glob and SIBLING PATHS (bootstrap vs EPA), not text instances, so a text grep for the changed phrase cannot find them.

**Prediction, stated so it can fail.**
- **(1)** If AF-1 is fixed by adding a third skip-or-append rule rather than by removing the special file kind (converting or superseding the header), the next same-day audit after a maintenance session will hit a new variant: a reader added after the fix that neither skips nor converts. **Falsifier:** the fix removes the distinction (one report kind, or the reader keys on the LAST run header), or two same-day audit-after-receipts cycles pass with every reader seeing the ledger.
- **(2)** The next upstream absorption that changes a SHIPPED template will again reach bootstrapped projects and miss the EPA path in at least one place, because nothing mechanically diffs "what bootstrap delivers" against "what EPA re-checks" outside the TWINNED SPAN. **Falsifier:** a next absorption whose EPA migration note is shown, by a mechanical comparison, to cover every changed template line.
- **(3)** The W-26 ordering will recur on the next MINOR bump: bump committed, audit after, push held by prose. **Falsifier:** the next MINOR bump is either preceded by its `/audit`, or held by a mechanism the push rule reads.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan — starting with AF-1 (so the next reader sees this ledger), then AF-3/AF-4/AF-5, which are the SHIPPED-surface findings. Decide W-26's wording as an owner call.

---

**Report status:** COMPLETE — all 6 agents returned, all 17 dimensions evaluated, no `probe-sandbox: RED`.
**defect series:** N/A — baseline mode.
**Application status:** PARTIAL — 25 of 27 applied (13 in `s3030551`, 12 in `sefca24e`); AE-15 and W-26 (carried rows) also applied in `sefca24e`. AF-13 fixed in the working tree only (gitignored). Open: AF-2 (its remainder needs a control the moratorium forbids), plus the carried and recovered rows.
