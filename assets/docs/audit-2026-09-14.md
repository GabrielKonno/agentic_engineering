# Framework Audit Report — 2026-09-14

**Date:** 2026-09-14
**Framework version:** v2.23.0 (README.md:1)
**Run mode:** `baseline` — trigger (b), before the MINOR bump to v2.23.0. The bump was already
committed (d812c07) and is UNPUSHED; the owner chose "commit + bump now, /audit before any push",
so this run is in practice pre-PUSH, not pre-bump (see W-26).
**Dimensions checked:** 17
**Agents dispatched:** 6
**Batch in scope of the push gate:** `origin/main..HEAD` = d812c07, 1c3bc46, e82176f
**ID series:** `W`. `V` was skipped: `audit-2026-09-10.md` already uses `### V-A`…`### V-E` as
section labels, and a `V-` ledger series would collide with every grep over them.

**carried: 0 open + 0 escalated from audit-2026-09-10.md** (98 ledger rows; 0 `open`, 0 `escalated`).
**OBSERVATIONS — accepted-risk, not re-opened:** E-1 (`audit-2026-09-02.md` record), M-44
(`audit-2026-09-02.md`), N-53 (`audit-2026-09-03.md`), T-11 and T-36 (`audit-2026-09-09.md`), U-48
(`audit-2026-09-10.md`). Agent 1 re-measured all six. None was found again in the working tree,
agent memory or the 182 reachable commits. U-48's orphan object is unreachable from any ref, which
puts it out of D16.3c scope by that record's own standing instruction.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` is the only runtime skill and exists in both places |
| C | Instruction style | Quality | 5 | FAIL | 11 instances; 4 are in the new Continuous-mode text (W-10), 7 predate it (W-29) |
| E | Version consistency | Currency | 5 | PASS | v2.23.0 on every current-version surface; no `created:` field was rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15 |
| D5 | Bootstrap file refs | References | 2 | FAIL | 56 of 57 resolve; the risk-profile check greps the wrong project.md path (W-13) |
| D6 | Gap vocabulary | References | 3 | PASS | 10 gaps, 3 of 3 declarers, 0 broken, 0 orphaned; D6.7 wording drift (W-24) |
| D7 | Pushy Description | Quality | 3 | PASS | 30 agents; both twins print `10 verified, 0 broken, 17 info`; 14 of 14 regressions behave as expected |
| D8 | Orchestrator refs | Process | 4 | PASS | every name and citation resolves; 2 stale counts in the batch (W-8, W-9) |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | no mismatches |
| D10 | Command → skill paths | References | 4 | FAIL | references resolve; 3 check defects (W-12, W-20, W-11) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | step count is consistent (25 + Setup); 4 forward references are not flagged (W-14..W-17) |
| D12 | Examples conventions | Currency | 5 | PASS | 20/9/11 checked; no violations |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | nothing bootstrap reads is missing from the PRD template |
| D14 | Bootstrap output | Process | 2 | FAIL | 15 of 15 template refs traced; the Step 15 report lacks slots (W-18); EPA has slots no step fills (W-19) |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 numeric errors; 1 stale term (W-30) |
| D16 | Project-info isolation | Privacy | 1 | PASS | 3 sources (12 + 16 + 112); 0 hits; **push gate over origin/main..HEAD: GREEN** |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | the batch promises behaviour nothing owns; 2 checklist checks cannot see the defects that recurred |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| W-1 | HIGH | `docs/modules/skills/pendencias-updater/SKILL.md:45` | pendencias-updater writes every task discovered during a session (session-end:67 hands discoveries to it) but has no `origin: discovered` mandate. Continuous mode therefore reads those tasks as `owner` (admitted with no cap), and the discovery brake under-counts. The d812c07 back-sweep missed it because its "enumerating grep" was a hand-typed 5-file `$W` list, which cannot find a writer outside the list. skill-gate:74/83/84/129 also register pendencies untagged, and it is not stated whether they are held. | applied `secace7a` |
| W-2 | HIGH | `.claude/commands/existing_project_adaptation.md:546-553`; `docs/modules/skills/autonomous-loop/SKILL.md` C3 | Existing projects cannot receive the v2.23.0 contract. EPA Step 2.9 copies a skill only if it is missing ("SKIPPED (already exists)"), and no version-delta list exists. Partial refreshes fail unsafe: untagged = `owner` = admitted uncapped, so a project that hand-refreshes only autonomous-loop, without the tagging writers, admits every AI-filed task. d812c07 declared a MINOR with no migration statement. | applied `secace7a` |
| W-3 | MEDIUM | `autonomous-loop/SKILL.md` C5 (:519, :528), C6; `sprint-proposer/SKILL.md` Step 1 | "END THE RECURRING TRIGGER (for `/loop`: stop the loop)" names no mechanism: a self-paced loop needs `stop: true`, a fixed-interval loop needs `CronDelete`, and loops are session-scoped. A revocation in another session removes the marker but cannot end the loop, and each later firing falls through to a full sprint proposal (Step 1 finds no marker, and no branch ends the trigger). The C6 idle claim "approval stands … next re-entry picks up" lapses silently when the session closes. | applied `secace7a` |
| W-4 | MEDIUM | `autonomous-loop/SKILL.md` C4 (:506) | The `Window: closed N` check expects "previous boundary + 1", but the previous value is not persisted: the report lives only in the transcript, and the marker is overwritten in the same closure. After an autocompact or re-entry, the cases closure condition 3 exists for, the check cannot go RED. A disk anchor exists: `git show HEAD~1:.claude/phases/project.md` (the tree must be clean at the boundary). | applied `secace7a` |
| W-5 | MEDIUM | `autonomous-loop/SKILL.md` C5 (:518 vs :528) | "Every stop … ALWAYS does all four: write `**State:**` …" contradicts the Revocation bullet, which removes the marker, so there is no State field left to write. | applied `secace7a` |
| W-6 | LOW | `autonomous-loop/SKILL.md` C5 (:531) / C6 | The window-counter reset on "continue" is stated only in C5. It is absent from C6's re-entry sequence, which is the path the owner's digest answer arrives through. | applied `secace7a` |
| W-7 | LOW | `autonomous-loop/SKILL.md` :534, :550 (and segment :151-152, :580); `sprint-proposer/SKILL.md` Step 0 | Audit-cadence equivalence (`ceil(closed/3)`, and "one per phase" in segment mode) has no reader: sprint-proposer Step 0 counts Progress Log rows, and C6's per-idle `/session-end` writes rows of its own. Segment mode already had the gap; the batch extends it. | applied `secace7a` |
| W-8 | LOW | `autonomous-loop/SKILL.md:46` | "Two legitimate paths in:" sits above a table that has 4 rows since d812c07. | applied `secace7a` |
| W-9 | LOW | `sprint-proposer/SKILL.md:268` | "a mode whose ~150 lines" is stale (autonomous-loop is 593 lines). | applied `secace7a` |
| W-10 | LOW | `autonomous-loop/SKILL.md` :562-564, :531-532, :534-536, :470 | Behavioural rules in the new text with no CAPS verb or no dedicated line: C6 pacing, C5 counter reset, C5 cadence computation, C3 held-chain rule. | applied `secace7a` |
| W-11 | LOW | commit bodies d812c07, 1c3bc46, e82176f | Receipt evidence form. `class sweep:` has no `$` line in d812c07 or 1c3bc46 (RED under maintenance.md:232). 1c3bc46's `instruction style:` and `inventory sweep:` stdout slots are parentheticals (T-24 form). e82176f carries a duplicate `commit correction:` key and 0 of the other keys. | applied `secace7a` |
| W-12 | MEDIUM | `.claude/commands/maintenance.md:435-438` | The isolation command's comment says "WORD BOUNDARIES ARE MANDATORY", but the command is `grep -oniE "($P)"`, with no `-w` or `\b`. Run as written over tracked files it returns 3 hits in 3 files; with `-w` it returns 0. The push gate therefore fires on a healthy state. e238330's subject claims a fix that did not land. | applied `secace7a` |
| W-13 | MEDIUM | `.claude/commands/bootstrap.md:153` | The risk-profile receipt check greps `projects/$ARGUMENTS/project.md`, but Step 3 creates `.claude/phases/project.md` (:227, :234). "Expected: `1` for BOTH files" cannot pass on a correct bootstrap. | applied `secace7a` |
| W-14 | MEDIUM | `.claude/commands/bootstrap.md:455-457` | Step 5.8 (production-financial) says to note in `code-reviewer.md`, which Step 7 creates. Step 7 never picks the note up, and its "if it already exists: do NOT overwrite" branch would skip the template if the note left a stub. | open |
| W-15 | LOW | `.claude/commands/bootstrap.md:145-156` | Step 1.2 says "Steps 2 and 3 ALWAYS confirm receipt", but neither receiver mentions the risk profile. The mandate lives only in the sender (component-design §9). | open |
| W-16 | LOW | `.claude/commands/bootstrap.md:362-363` | The comment says `evolution-policy.md` "is extracted unconditionally above"; the extraction is below (:403). | applied `s6b4baa8` |
| W-17 | LOW | `.claude/commands/existing_project_adaptation.md:330-355` | EPA Step 2.2 writes a "retroactive PRD created" Progress row and a session log before Phase 3 creates the PRD, and into `.claude/logs/` before Step 4.2 creates it. Neither is flagged. | open |
| W-18 | LOW | `.claude/commands/bootstrap.md` Step 15 | The final report has no slot for `.claude/docs/` (Step 5.7), for the Step 12.5a "kept gap, no example → pendencias" outcome, or for the Step 1.2 grep result. Two slot enumerations are paraphrased rather than copied (Step 1.1, Step 13). | open |
| W-19 | LOW | `.claude/commands/existing_project_adaptation.md` Step 5.2 | The report offers MCP `none installed — placeholder replaced` and a Hooks slot, but Steps 4.2 and 4.3 never require replacing those placeholders, so no step produces those verdicts. | open |
| W-20 | LOW | `.claude/commands/maintenance.md:757-758` | The control baseline records "4 fenced + 23 inline = 27 … TOTAL 62"; today it returns 5 + 23 = 28, TOTAL 63 (the rise came at 512f07b). | applied `s30c3d6e` |
| W-21 | LOW | `.claude/commands/maintenance.md:236`, :177 | The elision grep catches only a LEADING `<` on a `$` line. It returned 0 on d812c07 while two mid-line angle-bracket `$` lines existed (a looser form returns 2). The "stdout must be literal, never a parenthetical" rule has no mechanical check. | open |
| W-22 | LOW | `.claude/commands/maintenance.md:420` | The references denominator regex cannot see `→ "Heading"` citations, the class d812c07 added (5 distinct), so `references:` under-counts its own stated unit. | open |
| W-23 | LOW | `.claude/commands/audit.md:243-246` | The copy-path derivation returns 29 lines against a prose list of exactly 5; an "at least 5" threshold cannot realistically go RED. | open |
| W-24 | LOW | `.claude/commands/audit.md` D6.7 | "diff the two install BLOCKS — table AND prose — expected: empty" no longer fits the commands, which scope parity to the marked TWINNED SPAN (U-24). The whole-block diff is structurally non-empty (rc=1), while the TWINNED SPAN diff and the loops are identical. | open |
| W-25 | LOW | `.claude/commands/maintenance.md` Audit intake item 8, :537 | A correction-only follow-up commit has no stated receipt form. Its three executions (d41022b, b9d2ee2, e82176f) used three forms. Item 7's exemption regex covers bumps and substitutions, not corrections. | open |
| W-26 | LOW | `.claude/commands/maintenance.md` "Version bumps"; CLAUDE.md trigger (b) | "PROPOSE /audit BEFORE a MINOR bump" only proposes and nothing gates it. d812c07 applied the bump before the audit. Whether an unpushed bump counts as "not yet a bump" is undocumented. | open |
| W-27 | LOW | `docs/modules/skills/session-end/SKILL.md:112` vs :125 | "Items 1-3 are the critical minimum" vs "at minimum items 2, 3, and 6". | open |
| W-28 | LOW | `docs/modules/agents/code_reviewer.md:41`; `validation-orchestrator/SKILL.md:119` | The reviewer schema field is `### Coverage gaps declared:`, while the orchestrator's Pass 1 tells the reader to find "the Coverage Gap Declaration section". | open |
| W-29 | LOW | session-end:125; sprint-proposer:199-203, :214, :240; validation-orchestrator:104, :145; validator.md:147-148, :164-166; code_reviewer.md:208-210, :264, :291 | Pre-existing instruction-style instances (lowercase or title-case imperatives, fields buried in prose). | open |
| W-30 | LOW | `examples/README.md:69` | "The session0 prompt copies this entire directory" uses a stale term; `/bootstrap` Step 1.5 does the copy. | applied `s6b4baa8` |
| W-31 | LOW | `docs/modules/skills/rules-agents-updater/SKILL.md:119` | The vocabulary-alignment check is still a whole-file `grep -l`, the T-9 false-match shape the activation loop was fixed to avoid. | open |

## Recommended Fixes

1. **Quick fixes:** W-8, W-9, W-16, W-20, W-30, W-27.
2. **Structural fixes:**
   - **W-1 and W-2 first.** Both are in the unpushed batch, and both make the new mode fail open.
   - Then W-3, W-4, W-5, W-6, W-12, W-13, W-14, W-17, W-18, W-19, W-28.
3. **Quality improvements:** W-7, W-10, W-11, W-15, W-21, W-22, W-23, W-24, W-25, W-26, W-29, W-31.
4. **ESCALATED — owner decision required:**
   - **Push decision.** The owner authorized the push on the condition that the audit passes. D16 is GREEN, but W-1 and W-2 (HIGH) are defects in the unpushed batch itself. Publishing v2.23.0 now would ship a contract that admits AI-filed tasks uncapped. The owner decides: fix first (recommended) or push as is.
5. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48 (see header).

## Meta-observation

`prediction: 3 clauses evaluated — void, void, held`
- "If a Run 12 remediation batch is run, the not-clean rate will land between 40% and 55%" → **void**. No remediation batch ran.
- "the dominant class will again be receipts whose numbers do not reproduce" → **void**, since its condition did not occur. It was observed anyway at small scale: 1c3bc46's `6 added` did not reproduce (5), caught by the author, not by a second actor.
- "If instead the next session is spent on `docs/modules/` and `examples/`, this prediction is void and that is the better outcome" → **held**. The batch is 95% shipped surface (294 of 308 lines, Agent 6).

**The recurring class this run saw: behaviour PROMISED by new shipped text that no mechanism OWNS, and propagation that no path DELIVERS.** The receipt apparatus did its job on form: the author's re-runs caught a wrong number and two non-literal `$` lines. But not one checklist control measures the things this batch actually got wrong:
- a back-sweep bounded by a hand list (W-1);
- a MINOR contract with no route into existing projects (W-2);
- a stop instruction that names no stop mechanism (W-3);
- a closure check whose red state is unreachable exactly when it matters (W-4).

Every checklist control passed, and every such defect was found only by a reader asking "who does this, and how would it fail?" This is the first batch in six to change the shipped surface meaningfully. Agent 6 measured Runs 8-11 at 0.2-1.4% shipped share, and the defect series tracks newly added normative text. So it is where defects were expected, and it is where they are.

**Prediction, stated so it can fail.** If the next maintenance batch fixes W-1..W-7 with the post-change checklist as its only control, at least one of those fixes will introduce a new owner-less promise or unreachable check in `autonomous-loop`. The checklist inspects the shape of text, not the behaviour it describes. What would falsify this is a behavioural probe: a synthetic project run through a continuous-mode cycle (register a task mid-run, revoke from another session, autocompact between boundaries), in the style of `d27e09f`'s scoped verification. If that probe is run and finds nothing, this prediction fails, and that is the better outcome.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings

### Agent 1: Structural Sync

**Part 1:** N/A — baseline mode.

**[A] Dual Placement — PASS.** `.claude/skills/` has 1 folder (`cross-cutting-analysis`), which also exists in `docs/modules/skills/` (15 folders). It is the only runtime skill, as expected.

**[D4] Skills README vs Disk — PASS.** README lists 15 (rows 1-15, `docs/modules/skills/README.md:45-59`) and disk has 15. Nothing is missing either way. `README.md:8` "15 skills: 12 lifecycle + 3 tier-gated" is correct: 1 + 5 + 3 + 1 + 1 + 1 = 12, plus 3 tier-gated.

**[D9] CLAUDE.md Diagram vs Disk — PASS.** Every entry in the diagram exists: 7 templates, 10 agent templates, 5 rules templates, 6 command files. Counts match: 6 slash commands; 15 process skills (12 + 3); "(9 more)" = 15 − 6 named; examples 20/9/11. The individual files in `.claude/docs/` (3) and `assets/docs/` (11) are covered by directory-level comments.

**[D16] Project-Information Isolation — PASS.**
- **Blocklist, from three sources:**
  - 12 folder-name patterns (3 folders with their variants, word-bounded, stoplisted);
  - 16 doc identifiers. Classes: a person's name, a personal domain, a platform subdomain, a hosting-app domain, a phone number, a personal e-mail, a city, a street address, a client business name, two product-module names, a product title;
  - 112 code identifiers shared between the 335 identifiers harvested from framework-layer surfaces and 6,698 project source files. Copy paths were excluded, and the derivation returned well over 5 paths.
  - The pattern was derived at runtime and never printed.
- **Surfaces scanned:**
  - all tracked files;
  - `.claude/docs/` (3 files);
  - agent memory (10 files including MEMORY.md);
  - git history, 182 commits, contents and messages. Contents were scanned with a per-revision loop.
- **Negation proofs:** a seeded folder name returns 1; a known token over 3 revisions returns 48; a seeded doc identifier returns 1.
- **Hits:** none.
  - The only value-shape match is the generic test fixture `t@t.com` (prd_planning.md:339,341; framework doc :1264,:1273,:1840,:1890), which is legitimate.
  - Memory: 8 commit-hash tokens, all resolved with `git cat-file -e`; 0 unresolved.
  - Range D16.2c: the only shared identifier is `done_tasks`, framework vocabulary.
- **Pushed OBSERVATION, not a finding:** 9 schema- or helper-shaped names in 10 shipped files, all on origin/main and none added in the range. They are standard e-commerce, multi-tenancy and test vocabulary, under the industry-name exemption already noted in `audit-2026-09-04.md:364` and `audit-2026-09-10.md:20`. Re-measured, not re-opened.
- **Push gate — verdict over origin/main..HEAD: GREEN, 0 hits.**
  - Contents: 0 file-hits over 182 revisions, which include the 3 unpushed.
  - Messages: `git log --all --format='%h|%s|%b' | grep -ciwE "(<derived pattern>)"` → 0; the doc-identifier file form → 0.
  - Value shapes in `git diff origin/main..HEAD`: 0. Messages carry only the attribution address.

### Agent 2: Bootstrap Integrity

**Part 1:** N/A — baseline mode.

**[D5] File References — FAIL.**
- 57 paths found, 56 resolved.
- Unresolved: `bootstrap.md:153` (see W-13).
- D5.4 naming: consistent in both twins. The `````js`/`````markdown` fences each appear exactly once in their source.

**[D11] Step Sequencing — FAIL.**
- 15 primary steps + 10 sub-steps (0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5) = 25, or 26 with Setup. This matches the framework doc's "15 primary steps + Setup + 0.5 + sub-steps" and its ASCII diagram.
- Forward references:
  - W-14 (Step 5.8 → Step 7);
  - W-15 (Step 1.2 receiver mandate);
  - W-16 (5.7 comment "above" vs below);
  - W-17 (EPA 2.2 → Phase 3 / Step 4.2).
- Info: the twinned-span marker and loop comment say "EPA Step 5.1", but the criteria span is in 4.6.5. Both loops carry the leftover comment "candidate replacement for …".
- Handled correctly: 5.5→14, Step 2 placeholders (5/6/14), 1.1→3/4/13, EPA 2.6/2.7.1/2.9b, EPA 4.1b.
- The TWINNED SPAN diff is empty.

**[D13] PRD ↔ Bootstrap Compatibility — PASS.**
- Bootstrap reads Cross-cutting Concerns, 1.1-1.3, 2.1-2.3, 3.x, 4, 5 (5.1/5.2/5.3), 6, 7, 9 and the Changelog version. EPA Phase 3 reads the full set.
- Nothing bootstrap reads is missing from the template. Minor wording drift: EPA "5.1 Stack" vs the template's "5.1 Suggested Stack".
- Info: bootstrap ignores 1.4, 8, 7.2/7.3, 3.x acceptance criteria / edge cases / main flow, and header Status.

**[D14] Output Completeness — FAIL.**
- 15 template references, all 15 traced: session-rules (5.7), prd (Setup/Step 2), project.md (3), pendencias (4), `.claude/logs/` (14), six skills (5.7), `.claude/skills/` and `.claude/agents/` (5.7), and the Step 5/Step 14 placeholders.
- D14.4: see W-18. Outside the dimension: W-19.
- The new `origin:` field is compatible with bootstrap Step 4 (untagged = owner, "or at bootstrap").

### Agent 3: Activation Chain

**Part 1:** N/A — baseline mode.

**[D6] Vocabulary Alignment — PASS.**
- **Declarers:**
  - code-reviewer: Accessibility, Performance, Concurrency, Visual regression, Data integrity;
  - security-reviewer: Static analysis, Secrets coverage, Federation protocol, Compliance, Infrastructure security;
  - validator: visual regression (bold form).
- `declarers extracted: 3 of 3` (M derived = 3).
- **Specialist matches:** all 10 exact, each with a bootstrap row and an EPA row:
  - accessibility-checker, performance-auditor, concurrency-tester;
  - visual-regression-tester ("code-reviewer or validator declares");
  - data-integrity-checker, sast-scanner, secrets-scanner, oauth-flow-tester, compliance-auditor, iac-scanner.
- **Install-block diff (D6.7):** table diff empty; TWINNED SPAN diff empty; loops identical (71 lines, `cmp`). The whole-block diff is rc=1 because of step structure only: lead-in wording, `created:` lineage, the bootstrap-only `#### Step 12.5b` heading, and the loop's placement. See W-24.
- **D6.9:** framework doc `:944-950` names 3 declarers, policy §1/§3 names 3 → match. validation-orchestrator (113-129) and rules-agents-updater (117, 127) name 3.
- **D6.8:** sources are {code-reviewer, security-reviewer, validator}. blue_team and red_team use "gap" only in unrelated senses. The §1-§3 twin diff is empty → match.
- Broken links: none. Orphaned specialists: none.
- Info: the install table header says "Gap declaration (in reviewer)", but the validator is not a reviewer. rules-agents-updater:119 uses whole-file `grep -l` (W-31).

**[D7] Pushy Description Compliance — PASS.**
- 30 agents checked: 10 module agents are PARTIAL (sanctioned) except skill-reviewer, which is FULL; all 20 examples are FULL.
- Synthetic project: `…/scratchpad/audit-a3/projects/base`. Repo `git status --porcelain` was 0 lines before and after.
- `activation chains`: bootstrap twin `10 verified, 0 broken, 17 info` (exit 0); EPA twin `10 verified, 0 broken, 17 info` (exit 0).
- `twins agree: yes`, including all 17 INFO strings byte-identical.
- **Regression set, identical in both twins:**
  1. `coverage gap for X` → BROKEN (`10 verified, 1 broken`)
  2. hyphenated → VERIFY (11/0)
  3. slashed → VERIFY
  4. TAB fold → VERIFY
  5. no article → VERIFY
  6. bogus second gap → BROKEN on the second gap only (11/1)
  7. `Declares` → VERIFY
  8. space fold → VERIFY
  9. body only → INFO, no BREAK (10/0/18)
  10. negated body → INFO, no BREAK
  11. `OAuth2 federation gap` → BROKEN (seen)
  12. `Node.js runtime gap` → BROKEN (seen)
  13. validator as sole declarer → VERIFY
  14. all declarers removed → `activation chains: none installed - no declaring component in projects/t14/.claude/agents`, exit 0
- **Planted probe (D7.8):** `BROKEN CHAIN: widget-telemetry-auditor declares 'widget telemetry gap' but no declaring component declares it`, plus `INFO: bespoke-helper has no gap phrase and is not a shipped example - verify it is protocol-spawned`; `10 verified, 1 broken, 18 info`.
- **Invariant (D7.5):** `files: 10 | output: 0`.
- Anti-pattern instances: none.

### Agent 4: Orchestration & Commands

**Part 1:** N/A — baseline mode.

**[D8] Orchestrator References — PASS.**
- **Names:** every skill and agent name in session-end, validation-orchestrator, sprint-proposer, autonomous-loop, skill-gate, codebase-audit and framework-audit resolves.
- **Cross-section citations, all resolved:**
  - "Continuous mode" → C1/C3/C4/C6: sanctioned prefix of `## Continuous mode — …`;
  - "Resource contention", "Task closure", "Continuation across sessions", "Cadence integrity", "Autonomous loop watchdog", "Execution proof": sanctioned prefixes;
  - "Loop guardrails", "Between Tasks", "Session archetypes", "Framework-evolution docs — the upstream lifecycle": sanctioned (parenthetical dropped);
  - "Exception stops", "Final report", "Before Implementing", "In sprint-approved mode", "Signals of exceeding", §4d, §1b, §3a, component-design §5-§9, D8.8 `### Periodic review — the triage VERDICTS`: exact;
  - "MODEL SWITCH entries": sanctioned (ordinal dropped).
- Unresolved: none.
- LOW: W-8, W-9; `autonomous-loop:349` "Bounded re-sequencing" is word-truncated but resolves uniquely (pre-existing, bddb018).

**[D10] Command → Skill Paths — FAIL.**
- All references in prd_planning, prd_change, maintenance and audit resolve.
- D10.4: cross-cutting-analysis is byte-identical in both placements.
- D10.6: CLAUDE.md (a)-(d) = audit.md Phase 0 (a)-(d).
- **Self-checks run verbatim, on HEAD e82176f; staged-diff checks over HEAD~3..HEAD:**
  - push decay (status half; fetch skipped): `## main...origin/main [ahead 3]` ✓
  - Step 0 channel: 2 of 3 without a channel, correctly RED; pending 0
  - receipt keys: d812c07 24/24, 1c3bc46 24/24, e82176f 0/24
  - `$`-line ≥1: **`class sweep` = 0 in d812c07 and 1c3bc46 → RED** (W-11)
  - angle/ellipsis grep 0 (but see W-21)
  - harvest `harvested 14 of 24 registered` ✓
  - instruction style d812c07 37/4/2 ✓; 1c3bc46 5/1/0 (corrected in e82176f), with a parenthetical stdout
  - fences 12 of 12 ✓
  - references 3 and 1 match, but the regex under-counts (W-22)
  - **isolation as written 3 hits in 3 files; with `-w` 0 → fires on healthy state** (W-12)
  - mandated-never-created: no output ✓
  - pipe count `8 8 8` ✓
  - Gate 1 twin parity: no output ✓
  - Gate 4 `admission:` has a slot ✓
  - **control enumeration 28 + 35 = 63 vs baseline 62 → stale** (W-20)
  - control numerator 0 (N/A)
  - liveness `1 componente(s) OK … [modo estrutural — js-yaml ausente]`, exit 0 (degraded mode)
  - raw templates `19 validated of 25 globbed, 6 skipped (fenced), OK` ✓
  - version surfaces ✓; residue 0 ✓
  - report deletions: vacuous
  - accepted-risk: 6 records × 5 fields ✓
  - placeholder: none ✓
  - copy-path derivation: 29 lines vs 5 in prose (W-23)
  - declarer M 3 ✓
  - D7.5 `files: 10 | output: 0` ✓
  - errata-number and ID collisions 0 ✓
  - prior prediction: 1 hit ✓
  - run-mode lines 9 ✓
  - defect-series parity `GREEN 9+2=11` (dropping a row → RED) ✓
- **Not executed:**
  - the `git fetch` half (it writes refs);
  - Gates 2 and 3 (no applied ledger in baseline);
  - RELOCATE (none in range);
  - Phase 0 reconstruction (verification mode only);
  - D16 history greps (Agent 1);
  - D7.6 (Agent 3).

### Agent 5: Document Accuracy

**Part 1:** N/A — baseline mode.

**[C] Instruction Style — FAIL.**
- 11 instances. The 4 in the new text are W-10; the 7 pre-existing are W-29.
- Excluded as the mandated shape: every "`…`. NEVER emit nothing." report line, the C2 admission line, and the C4/C5 blocks.
- Cross-dimension observations: W-27 (session-end minimums), W-5 (revocation vs "all four"), W-28 (coverage-gap section name).

**[E] Version Consistency — PASS.**
- Canonical v2.23.0 at README:1, README:146, audit.md:246, EPA:238/:542 and claude_md.md:4.
- Historical or illustrative strings, correctly left alone: EPA:262/:266 migration history; maintenance.md:836/:925/:927; skills/README:35; autonomous-loop:21/:28/:246; sprint-proposer:268.
- `created:` lineage fields were not reported (E.5). `git diff HEAD~3 HEAD` changed only the six current-version lines and rewrote no `created:` field.

**[D12] Examples Convention Compliance — PASS.**
- Agents 20/20: all frontmatter fields present and `name:` = filename; Input / BOUNDARIES / When invoked / Output Format sections; Input immediately followed by an output heading; verdict line.
- Skills 9/9: folder/SKILL.md, `invocation: inline`, no `last_eval`; all 3 stack skills have Testing and pitfalls sections.
- Rules 11/11: `domain:` / `applies_to:`, no `name:`; checklist claims match README:170.
- Violations: none.

**[D15] Factual Accuracy — PASS.**
- Disk counts:
  - 15 skills (12 + 3), 10 agents, 5 rules, 7 templates, 6 commands, examples 20/9/11;
  - invocation fields 7 `user` / 8 `inline`;
  - 17 dimensions over 6 agents; bootstrap Steps 1-15.
- Every numeric claim checked matches disk:
  - framework.md :45/:47/:48/:49/:50/:346/:213/:296/:362/:175-178/:171-173/:1232;
  - README :21-24/:22-23/:55/:123/:138/:147-156/:162/:182/:218/:399;
  - modules/README:10; skills/README:8-9/:38/:43-59; bootstrap 5.7; CLAUDE.md :19/:36/:68/:141; examples/README :12/:33/:52.
- No banned sense of "inline" found.
- Stale term: W-30.

### Agent 6: Process Coverage (meta)

**Part 1:** N/A — baseline mode.

**[D17] Process Coverage — FINDINGS.**

**Commits classified: 40 commits, 9 operation types.**
- audit-fix group application: 17
- standalone control/pattern corrections: 7
- audit report persist: 4
- audit write-back: 4
- commit/receipt correction follow-ups: 3 (d41022b, b9d2ee2, e82176f)
- owner-requested shipped-feature evolution: 2 (d812c07, 1c3bc46)
- placeholder substitution: 1
- version-bump chore: 1
- scoped verification record: 1 (d27e09f)

**Undocumented executed flows:** W-25 (correction-commit receipt form, 3 executions with 3 forms). The scoped verification (d27e09f) ran once, which is below the threshold; it is noted as the only behavioural probe in 40 commits.

**Referenced-but-unowned conventions:**
- Template → mother-repo promises: none. The evolution_policy upstream lifecycle is owned by maintenance.md Step 0 and Upstream intake, and the channel was created in 512f07b.
- Continuous-mode promises with no owner: W-3 (stopping the trigger, revoking from another session, idle lapsing), W-6 (counter reset missing from C6), W-2 (existing projects cannot receive the MINOR; fail-open default), W-7 (cadence equivalence has no reader).
- Back-sweep gap: W-1.

**Aspirational mechanisms:** W-26 (trigger (b) is proposed, not gated). CLAUDE.md:214 and :162 are owned; README :176/:344/:381 are project-level.

**D17.4 provability:**

| Check | Can go RED? | Can go GREEN? | Verdict |
|---|---|---|---|
| Elision grep | Only for a leading `<` | Yes | Blind to the shape that recurred (W-21) |
| Literal-stdout rule | No check exists | n/a | Rule without a mechanical check (W-21) |
| Receipt key loop | Yes | No, on a correction commit | Fires on a healthy but unexempted class (W-25) |
| Back-sweep via `$W` | Cannot find writers outside the list | Yes | Decoration for completeness (W-1) |
| C4 `Mode` line | Yes (negation-proved) | Yes | Provable |
| C4 `Window` +1 | Only in-context | Yes | Red unreachable where it matters (W-4) |
| `Last closed` | No check | n/a | Decoration |
| sprint-proposer marker count | Yes | Yes | Provable |
| Version residue | Yes | Yes | Provable |

**D17.5 self-reported controls:**

| Control | Verified by whom | Output persisted where | Gate? |
|---|---|---|---|
| All 24 keys | Author only, until this audit | Commit bodies | No |
| Independent re-runs | This agent | This report | Reproduced: 37/4/2; 5/1/0; references 3; `origin: discovered` = 1 × 5; residue 0; 24 keys. Not reproducible: 1c3bc46 `inventory sweep:` stdout |
| Twin parity | Author (mechanical) | Commit body | Closest to a gate |
| `audit:` trigger (b) | Owner decision | Commit body | No; proposed, then bumped |
| Project-side continuous closure outputs and `admission:` | The orchestrator itself | Transcript only; digest keeps only "N boundaries, all OK" | No |

**D17.6 convergence:**

| Batch | Range | Shipped (docs/modules + examples) | Apparatus | Shipped share |
|---|---|---|---|---|
| B0 (this) | e5613f4..HEAD (3) | 294 | 14 | 95% |
| B1 | d27e09f..e5613f4 (4) | 2 | 152 | 1.3% |
| B2, Run 11 | 7c1ec64..d27e09f (10) | 11 | 1058 | 1.0% |
| B3, Run 10 | 84f094d..7c1ec64 (8) | 4 | 1795 | 0.2% |
| B4, Run 9 | fd3d027..84f094d (5) | 4 | 1261 | 0.3% |
| B5, Run 8 | 2ba16ee..fd3d027 (9) | 12 | 856 | 1.4% |

Judgement:
- Across B1-B5, shipped delta fell to near zero while the defect rate did not fall. The loop returned about 33 shipped lines for about 5,100 apparatus lines.
- This batch reverses the split, but through owner-directed work, not through the loop being re-pointed.
- The controls are adequate for receipt FORM and inadequate for shipped-surface SEMANTICS. The loop should be re-pointed at shipped behaviour, and a documented behavioural verification step is the natural vehicle.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; 31 findings (2 HIGH, 6 MEDIUM, 23 LOW).
**defect series:** `N/A — baseline mode`
*(superseded)* **Application status:** `PENDING`
**Application status:** `PARTIAL — 13 of 31 applied in secace7a`

## Post-change checklist receipts (`secace7a`)

Applies W-1..W-13 of this report. Receipts GENERATED by a script that runs each command and pastes its stdout — no figure below was typed.

**push decay:** origin/main unchanged (e5613f4)
    $ git status -sb | head -1; git log -1 --format=%h origin/main
    ## main...origin/main [ahead 5]
    e5613f4

**inventory sweep:** N/A — no artifact added, removed, renamed or split in ecace7a
    $ git show --name-status --format= ecace7a | grep -cE '^[ADR]'
    0

**instruction style:** 36 added, 8 rewritten, 2 buried — unit: a diff line carrying a CAPS imperative; range ecace7a~1..ecace7a; both buried lines are the sanctioned `NEVER emit nothing` tail
    $ D='git diff ecace7a~1..ecace7a -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    36 added, 8 rewritten, 2 buried

**references:** 0 of 0 path/§ citations added; the quoted-heading targets the batch cites (C1-C6 under "Continuous mode") resolve — 6 headings
    $ git diff ecace7a~1..ecace7a | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l; grep -cE '^### C[1-6]\.' docs/modules/skills/autonomous-loop/SKILL.md
    0
    6

**fences:** 12 of 12 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; n=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$n" -gt 0 ] && echo ok || echo "ZERO $t"; done | sort | uniq -c
         12 ok

**isolation:** 7 files scanned, 0 hits (pattern derived from disk at runtime, never echoed)
    $ git diff --name-only ecace7a~1..ecace7a | xargs grep -oniwE "($P)" | wc -l
    0

**class sweep:** 13 fixes × 4 directions, 3 extra instances found and fixed (maintenance.md:1116 and bootstrap.md:153 control bytes; skill-gate untagged writer), 1 found and NOT fixed (assets/docs/audit-2026-09-03.md, 2 lines — an append-only persisted report). Control-byte residue in shipped surfaces:
    $ git ls-files -z -- .claude docs examples CLAUDE.md README.md | xargs -0 grep -lP '[\x00-\x08\x0B\x0C\x0E-\x1F]' | wc -l
    0

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| W-1 | secace7a | docs/modules/skills/pendencias-updater/SKILL.md | ✓ widened writer grep over docs/modules — the `PAT` in the back-sweep `$` line below | n/a — skills have no command twin | ✓ "### 3. Add new items" field list extended | ✓ C1 writer check lists it | skill-gate/SKILL.md untagged — fixed |
| W-2 | secace7a | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "SKIPPED (already exists)"` → 4 sites; skills + agents loops fixed, rules/guard left (not in the coupled set) | n/a — bootstrap creates new projects, no existing skills | ✓ Step 5.2 report slots added | ✓ autonomous-loop C1 fails closed; maintenance "Version bumps" migration rule | none |
| W-3 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "RECURRING TRIGGER"` and `grep -n "/loop /sprint-proposer"` → C1, C5, C6 updated | ✓ sprint-proposer Step 1 no-marker branch | ✓ digest `### Trigger:` slot | ✓ Step 1 report enumeration + slot widened | none |
| W-4 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "previous boundary"` → 1 site | n/a | ✓ closure prose requires the marker change in HEAD | ✓ C6 reset committed alone | the fix's own first draft read OK on code-without-marker (probe P7) — fixed before commit |
| W-5 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "all four"` → 1 site | n/a | ✓ revocation bullet rewritten | n/a | none |
| W-6 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "resets the"` → 0 after | n/a | ✓ C5 resume paragraph points at C6 | ✓ C4 counts from the committed 0 | none |
| W-7 | secace7a | docs/modules/skills/sprint-proposer/SKILL.md | ✓ `grep -rn "counts as" docs/modules` → autonomous-loop Step 2, C5, final report | n/a | ✓ Step 0 INCOMPLETE paragraph | ✓ C5 writes the string the reader greps | none |
| W-8 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -rn "legitimate paths" docs/` → 1 | n/a | ✓ table rows = 4 | n/a | none |
| W-9 | secace7a | docs/modules/skills/sprint-proposer/SKILL.md | ✓ `grep -rn "~[0-9]* lines" docs/modules/skills` → 0 after | n/a | ✓ paragraph re-read | n/a | none |
| W-10 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ staged instruction-style B → only NEVER-emit-nothing tails | n/a | ✓ 2 further buried lines split (C1 unavailable, C5 trigger owner) | n/a | 2 buried lines in this batch's own draft — fixed |
| W-11 | secace7a | none — commit bodies are immutable; correction written in ecace7a's body | n/a — no file | n/a | n/a | ✓ this section uses the `$`-line form | none |
| W-12 | secace7a | .claude/commands/maintenance.md | ✓ `grep -lP '[\x00-\x08\x0B\x0C\x0E-\x1F]'` over tracked files → 4 files | ✓ bootstrap.md:153 | ✓ the comment above line 438 is now true | ✓ receipts discharged with `-w` | maintenance.md:1116, bootstrap.md:153 fixed; audit-2026-09-03.md recorded, not edited |
| W-13 | secace7a | .claude/commands/bootstrap.md | ✓ `grep -rn "ARGUMENTS/project.md" .claude/commands` → 0 after | ✓ EPA reads `project.md` Overview by name, no path grep | ✓ "Expected: 1 for BOTH files" now reachable | n/a | none |

**back-sweep:** 1 rule, 2 artifacts fixed of 2 found, re-run clean — PRE measured at ecace7a~1, POST at the tip
    $ PAT='(add|register|route|file|write|create)[^.]{0,60}(pendencias|Next Steps|a pendency|as tasks?)|(new task|task discovered|findings?)[^.]{0,60}(pendencias|Next Steps)'
      echo PRE:; for f in $(git ls-tree -r --name-only ecace7a~1 docs/modules | grep -E '(SKILL\.md|agents/.*\.md|rules/.*\.md)$'); do s=$(git show ecace7a~1:"$f"); echo "$s" | grep -qiE "$PAT" && ! echo "$s" | grep -q "origin: discovered" && echo "UNTAGGED $f"; done
      echo POST:; for f in $(git ls-files docs/modules | grep -E '(SKILL\.md|agents/.*\.md|rules/.*\.md)$'); do grep -qiE "$PAT" "$f" && ! grep -q "origin: discovered" "$f" && echo x; done | wc -l
    PRE:
    UNTAGGED docs/modules/skills/pendencias-updater/SKILL.md
    UNTAGGED docs/modules/skills/skill-gate/SKILL.md
    POST:
    0

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| every AI task writer tags `origin: discovered` | a shipped skill/agent/rule instructing the AI to add tasks or pendencies to pendencias | the `PAT` residue loop in the `$` line above (pattern-derived — replaces d812c07's hand-typed `$W` list, which is how W-1 escaped) | 2 | 2 | 0 remaining |

**liveness:** 19 components OK [full parse] — raw templates 19 validated of 25 globbed, 6 skipped (fenced)
    $ python -c "import io,yaml,glob,os,sys
      bad=0; n=0; sk=0
      for p in sorted(glob.glob('docs/modules/skills/*/SKILL.md'))+sorted(glob.glob('docs/modules/agents/*.md')):
          s=io.open(p,encoding='utf-8').read().replace(chr(13)+chr(10),chr(10))
          if not s.startswith('---'): sk+=1; continue
          n+=1
          try:
              d=yaml.safe_load(s[4:s.index(chr(10)+'---',4)+1])
              exp=os.path.basename(os.path.dirname(p)) if p.endswith('SKILL.md') else os.path.basename(p)[:-3].replace('_','-')
              if d.get('name')!=exp: bad+=1; print('FAIL',p,'name=',d.get('name'),'expected',exp)
          except Exception as e: bad+=1; print('FAIL',p,type(e).__name__)
      print('raw templates: %d validated of %d globbed, %d skipped (fenced),' % (n, n+sk, sk), 'OK' if not bad else str(bad)+' BROKEN'); sys.exit(1 if bad else 0)"
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** repaired :1116 counts 2 Applies IDs and ignores 2 cited ones; maintenance.md carried 2 control-byte lines before ecace7a and 0 after; the C1 writer check, C4 closure check and EPA DIFFERS branch went RED on their seeded states (probe in ecace7a's body)
    $ printf 'Applies W-1, W-12 from x\n\nCites U-48 and T-10\n' | awk '/^Applies /{f=1} f&&/^$/{exit} f' | grep -oE '\b[A-Z]-[0-9]+\b' | sort -u | wc -l; git show ecace7a~1:.claude/commands/maintenance.md | grep -cP '\x08'; git show ecace7a:.claude/commands/maintenance.md | grep -cP '\x08'
    2
    2
    0

**version:** none — corrects the unpushed v2.23.0 (d812c07); old-label residue 0
    $ grep -rn "v2\.22\.0" README.md docs/ .claude/ CLAUDE.md 2>/dev/null | grep -v 'created: framework-v' | wc -l
    0

**classification:** 13 add, 11 substitute, 0 delete, 0 relocate — ADD: C1 writer check, trigger-mechanism block, C6 reset, idle-lapse line, cadence Progress-Log mandate, digest Trigger + Tagging-writers slots, Step 1 no-marker branch, Step 0 reader, pendencias-updater origin, skill-gate tag, EPA DIFFERS + coupled set, EPA report slots, maintenance migration rule. SUBSTITUTE (strictly more correct, no information lost): C4 closure block, C5 stop intro, revocation bullet, resume paragraph, C3 chain line, C6 pacing, path count, "~150 lines", :438 -w, :1116 \b, :153 \b + path
    $ echo "n/a — judgement, not measurement"
    n/a — judgement, not measurement

**new component:** none this session
    $ git show --name-status --format= ecace7a | grep -c '^A'
    0

**gates:** twin parity PASS | receipt-rows-vs-diff 12/12 file-bearing rows (W-11 names no file) | row-count 13 vs 13 | slots 1/1 (`tagging writers:` → C1 policy + digest)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo PASS; git show --name-only --format= ecace7a | sort | tr '\n' ' '; echo; grep -cE '^\| W-([1-9]|1[0-3]) \|.*applied `secace7a` \|$' assets/docs/audit-2026-09-14.md
    PASS
    .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md .claude/commands/maintenance.md docs/modules/skills/autonomous-loop/SKILL.md docs/modules/skills/pendencias-updater/SKILL.md docs/modules/skills/skill-gate/SKILL.md docs/modules/skills/sprint-proposer/SKILL.md 
    13

**applied-proof:** 13 of 13 IDs resolve to a commit touching their line (form A: 11, form B: 1 — W-1, whose pre-batch line was kept and the fix added below it; n/a: 1 — W-11, commit bodies), 0 forced to open
    $ for x in "W-2|.claude/commands/existing_project_adaptation.md|553" "W-3|docs/modules/skills/autonomous-loop/SKILL.md|519" "W-4|docs/modules/skills/autonomous-loop/SKILL.md|506" "W-5|docs/modules/skills/autonomous-loop/SKILL.md|518" "W-6|docs/modules/skills/autonomous-loop/SKILL.md|531" "W-7|docs/modules/skills/autonomous-loop/SKILL.md|534" "W-8|docs/modules/skills/autonomous-loop/SKILL.md|46" "W-9|docs/modules/skills/sprint-proposer/SKILL.md|269" "W-10|docs/modules/skills/autonomous-loop/SKILL.md|562" "W-12|.claude/commands/maintenance.md|438" "W-13|.claude/commands/bootstrap.md|153"; do IFS='|' read id f l <<< "$x"; a=$(git show ecace7a~1:"$f" | sed -n "${l}p" | sed 's/[[:space:]]*$//'); echo "$id form-A $(git log -S"$a" --oneline ecace7a~1..ecace7a -- "$f" | wc -l)"; done
      echo "W-1 form-B $(git log -L 53,53:docs/modules/skills/pendencias-updater/SKILL.md --oneline ecace7a~1..ecace7a | grep -cE '^[0-9a-f]{7} ')"
      echo "W-11 n/a $(git log -1 --format=%B ecace7a | grep -c '^- W-11 commit correction')"
    W-2 form-A 1
    W-3 form-A 1
    W-4 form-A 1
    W-5 form-A 1
    W-6 form-A 1
    W-7 form-A 1
    W-8 form-A 1
    W-9 form-A 1
    W-10 form-A 1
    W-12 form-A 1
    W-13 form-A 1
    W-1 form-B 1
    W-11 n/a 1

**control back-sweep:** 4 controls touched, all re-run with negation (maintenance.md:438 isolation, :1116 Applies-ID count, bootstrap.md:153 risk-profile check, autonomous-loop C1 writer check + C4 closure check); 1 defect found — the first draft of C4 could not go RED on code committed without the marker (probe P7), fixed before ecace7a. Denominator:
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md; do inf=$(awk '/^```/{g=!g;next} g' "$f" | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 5 fenced + 23 inline = 28
    .claude/commands/audit.md: 7 fenced + 28 inline = 35

**commit correction:** d812c07, 1c3bc46, e82176f corrected by ecace7a — receipt evidence form (W-11); verdicts unchanged
    $ git log -1 --format=%B ecace7a | grep -c '^- W-11 commit correction'
    1

**placeholder:** none — the write-back follows the fix commit, so every hash was known
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-14.md | wc -l
    0

**report deletions:** none
    $ for f in $(git diff --cached --name-only -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:'; do b=$(git show HEAD:"$f" 2>/dev/null | grep -cE "$pat"); if [ -f "$f" ]; then a=$(grep -cE "$pat" "$f"); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat"; done; done; echo "structural decreases listed above: none if this is the only line"
    structural decreases listed above: none if this is the only line

**report restore:** none
    $ echo "n/a — no report destroyed in history this session"
    n/a — no report destroyed in history this session

**audit:** N/A — no MINOR/MAJOR bump in ecace7a; the baseline /audit owed by v2.23.0 is this report
    $ echo "n/a — no bump in ecace7a"
    n/a — no bump in ecace7a

**verification audit:** proposed — trigger (d); the owner decides when
    $ echo "n/a — owner decision"
    n/a — owner decision

**defect series:** N/A — maintenance session; this report is a baseline run and appends no row
    $ echo "n/a — baseline run, maintenance session"
    n/a — baseline run, maintenance session

**push:** requested by the owner — D16 over the unpushed range measured below BEFORE the write-back commit; the push is gated on a re-run after that commit (branch on the count, never on exit status)
    $ c1=$(git diff --name-only origin/main..HEAD | sort -u | while read f; do [ -f "$f" ] && grep -oniwE "($P)" "$f"; done | wc -l); c2=$(git log --format=%B origin/main..HEAD | grep -oniwE "($P)" | wc -l); c3=$(git diff origin/main..HEAD | grep '^+' | grep -cE 'eyJ[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{20,}'); echo "commits $(git log --oneline origin/main..HEAD | wc -l) | contents $c1 | messages $c2 | value-shapes $c3 | total $((c1+c2+c3))"
    commits 5 | contents 0 | messages 0 | value-shapes 0 | total 0



# Run 2 — verification pass

**Date:** 2026-09-14
**Framework version:** v2.23.0 (README.md:1)
**Run mode:** `verification (over ecace7a)`
**Trigger:** (d). `/maintenance` applied W-1..W-13 in ecace7a; the write-back is 12afa4a. The batch range is `ecace7a~1..12afa4a` and it is PUSHED (`origin/main` = 12afa4a).
**Dimensions checked:** 17, plus the Part 1 fix-verification pass
**Agents dispatched:** 6
**Part 1 input:** Run 1 of this file, intact at 12afa4a (31 ledger rows, W-1..W-13 `applied`). No reconstruction was needed.
**ID series:** `X` (Run 1 used `W`). Collision check before writing: `grep -c '^| X-'` over every report returned 0.

**carried: 18 open + 0 escalated from Run 1 of this file (W-14..W-31)**. Each was re-verified against the disk and is still true (see the carried-forward rows).
**OBSERVATIONS — accepted-risk, not re-opened:** E-1, M-44, N-53, T-11, T-36, U-48. Agent 1 re-measured all of them over 185 commits, memory and the tracked tree: 0 recurrences.

**The post-push D16 re-run is persisted here.** 12afa4a promised a "re-run after that commit" gating the push, and wrote its output nowhere (X-16). Independent re-runs in this pass:
- Agent 4, over `e5613f4..HEAD`: `commits 6 | contents 0 | messages 0 | value-shapes 0 | total 0`.
- Agent 1: a full-name scan (all 12 folder-name variants as plain substrings, 21 doc identifiers, value shapes) over 185 commits, messages, memory and tracked files returned **0 hits**.

The batch is GREEN on privacy, but the gate that certified it could not see 2 of 3 folder names (X-6).

## Part 1 — Verification ledger for ecace7a

> **Errata 1 (added by the 2026-09-14 Run 2 persistence, finding X-19).** The W-6 row below says the "adjust the policy" observation was "filed as X-20"; it is **X-19**. X-20 is the full-folder-name seed rule.

NOT-FIXED was settled first, mechanically. Every ID returns at least one commit in the range: pickaxe on the pre-batch text, or `-L` on the tip line for W-1 and W-4, or the body grep for W-11. **No ID is NOT-FIXED.** Where agents disagreed, the verdict below is the most severe one backed by structural evidence, and the disagreement is noted.

| ID | Verdict | Evidence (file:line — structural) |
|----|---------|------|
| W-1 | FIXED-BUT-CLASS-NOT-SWEPT | pendencias-updater:51-53 and skill-gate:88 are now tagged. The receipt's "pattern-derived" `PAT` misses `docs/modules/agents/prd_sync_checker.md:52` ("add new tasks for new features": no tag, and no stated origin class). Its whole-file tag test also hides a second writer in an already-tagged file: `codebase-audit/SKILL.md:125` (the REOPEN task) is masked by the tag at :105. C1's check (`autonomous-loop/SKILL.md:431-437`) is a hand-typed list of 7 paths with a whole-file `grep -q`, the very class W-1 removed from the receipt, now shipped. → X-10 (Agent 6) |
| W-2 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | The skills loop (`existing_project_adaptation.md:546-557`) is correct and passes `bash -n`. The process-agents loop at **:579-580 reads `else` followed by `elif`**. Extracted verbatim, `bash -n` gives `syntax error near unexpected token 'elif'` (rc=2); at ecace7a~1 it gives rc=0. So the block aborts before copying any process agent. That includes `diff-pattern-extractor`, which is in the coupled set. The author's probe exercised only the skills loop. Also: `SKIPPED (identical)` is unreachable after Step 2.8's lineage edits; tier-gated coupled members (Step 2.9b, :668-671) get no verdict; the rest of the v2.23.0 contract (`session_rules.md`, the pendencias `origin:` legend) still has no route into existing projects; "refresh" has no command; the report slots do not match the verdicts. Agents 2, 3, 4 and 6 found the syntax error independently. → X-1..X-5 |
| W-3 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | C5's mechanism list (autonomous-loop:561-569), the no-marker branch (sprint-proposer:83-86) and consistent naming of the `continuous` argument all landed. **Introduced:** Step 1's own self-check still says `` `0` means continue below `` (sprint-proposer:92-94), contradicting :83-86. `/sprint-proposer continuous` also collides with a manual start of the mode (sprint-proposer:283; autonomous-loop:463, :627). **Partial:** the no-marker branch names no mechanism, and the list lives only in autonomous-loop, which is never loaded without a marker. A scheduled session is never told to carry `continuous`. Revocation has no digest to name the mechanism in. "cancel continuous mode" has no reader outside autonomous-loop. → X-7, X-8 (Agents 4, 5, 6) |
| W-4 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | Agent 6's independent probe ran the check verbatim across 18 cases. The disk anchor holds for autocompact, a fresh shell, the first boundary after C1 (committed or not), reset, amend, restart after revocation and CRLF. **Introduced:** `HEAD == lastc` (:541) gives a **false RED on a healthy state** in two cases: an owner commit landing after the task's last commit (case G, although C2 :475-476 names the owner a concurrent writer), and a merge commit (case J). A closure RED has no disposition. It is not a C5 stop or a guardrail, so no `State` is written and the trigger is not ended. C6 re-entry (:632-639) never re-runs the closure check. → X-9 |
| W-5 | INTRODUCED-A-DEFECT | The "all four" contradiction is gone (:557-558, :577-578). But "EXCEPT revocation" also exempts revocation from "ends at a TASK boundary", and nothing restates that. Probe case R (marker removed mid-task) gives `0` and `RED — window 1 -> none`, and no branch owns that outcome. Revocation silently drops the digest, and the digest's Stop reason field has no revocation value. → X-13 (Agents 5, 6) |
| W-6 | CONFIRMED-FIXED | The reset is in C6 (:632-634), committed on its own; C5 (:583-585) points at it. Probe K returns `OK — window 0 -> 1`; L (reset folded into the task commit) returns RED, as specified. No conflict with "one task in flight". Observation, filed as X-20: the "adjust the policy" answer does not trigger the reset. |
| W-7 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | The continuous writer strings (:590-592) match the reader (sprint-proposer:60-62) byte for byte. **Introduced:** the reader cites `autonomous-loop` → Step 2 as a Progress Log writer. Step 2 (:151-152, :649) writes only into the final report, a transcript, and project-md-updater (:21-27) has no such field. **Partial:** segment mode still counts 1 per session, and the finding named that half. Brake, guardrail and revocation `/session-end` entries carry no `counts as`, and the C6 reset then erases the window. The idle `counts as 0` mandate sits in C5, not in C6's idle step, which owns that moment. → X-11 (Agents 4, 5, 6) |
| W-8 | FIXED-BUT-CLASS-NOT-SWEPT | :46 now reads "Four legitimate paths in — two per mode" above a 4-row table. But :55 still reads "**If NEITHER happened**", a two-way quantifier. (Agent 4: CONFIRMED; Agent 5 found :55.) → X-14 |
| W-9 | CONFIRMED-FIXED | sprint-proposer:278 now reads "full text would load". The only other `~N lines` hit is `skill-gate/rubric.md:43`, which is a size threshold rather than a claim. (Agents 4 and 5 agree.) |
| W-10 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | The four named lines are fixed (:494, :588-593, :629-634). **Introduced:** the mandate that "the trigger MUST carry the `continuous` argument (C6)" is buried in the Revocation rationale sub-bullet (:579-581), and C6 (:622-628) states the argument only descriptively. The idle `counts as 0` instruction points at C6, which never carries it (§9). → X-12 (and X-11) |
| W-11 | FIXED-BUT-CLASS-NOT-SWEPT | The ecace7a body corrects d812c07, 1c3bc46 and e82176f, and the new section passes the mechanical key checks (24/24, `$` lines, 0 elisions, pipes `8`). The evidence-form class survives in the batch's own receipts: 20 per-fix cells read `✓` with no quoted command (RED per maintenance.md:518-522); the `push:` verdict is outside the enumerated set and its gating re-run is persisted nowhere; `control back-sweep:` is not in `K of N` form; `negation proof:` points at a prose commit body with no commands; and the header "no figure below was typed" is false (the class table and several counts are typed). Agent 1 adds that the `:438` negation claimed at :498 has no `$` line. → X-15, X-16 (Agents 1, 4, 6) |
| W-12 | INTRODUCED-A-DEFECT (the fix landed) | The backspace bytes are gone from maintenance.md:438, :1116 and bootstrap.md:153; the C0 sweep of shipped surfaces returns 0; :1116 and :153 were proved by negation. **Introduced:** `P` (:433-434) holds only the PARTS of each folder name split on `_`/`-`, and `-w` treats `_` as a word character. Run verbatim against seeds holding each full folder name, **2 of 3 projects (both underscore-joined) return 0** (the pre-fix form returned 3 and 1). The push gate is blind to the most literal leak there is. Agent 4 rated this CONFIRMED on a seed made of a PART; Agent 1's full-name seed decides it. → X-6 |
| W-13 | CONFIRMED-FIXED | bootstrap.md:153 greps `.claude/phases/project.md` (Step 3's path, :227, :234) with `\b`. A synthetic project rendered from both templates gives `1/1` resolved, `0/0` for the placeholder, and `0` for `productionX`; the pre-batch line gives "No such file". No `ARGUMENTS/project.md` left in `.claude/commands`. |

**Score over the 13 applied FINDINGS (denominator 13): 3 of 13 clean, 10 not clean.** Of the not-clean, by primary verdict: 5 partially-fixed · 3 class-not-swept · 0 not-fixed · 2 introduced-a-defect alone. Counting combinations, 7 of 13 introduced a defect (W-2, W-3, W-4, W-5, W-7, W-10, W-12).

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` byte-identical in both places |
| C | Instruction style | Quality | 5 | FAIL | 2 new buried mandates in the batch text (X-12, X-11); W-29's 7 remain |
| E | Version consistency | Currency | 5 | PASS | v2.23.0 on every current surface; no `created:` rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15 |
| D5 | Bootstrap file refs | References | 2 | PASS | all batch paths resolve; 3 regex false positives |
| D6 | Gap vocabulary | References | 3 | PASS | 10 gaps, 3 of 3 declarers, 0 broken/orphaned; neighbourhood undisturbed |
| D7 | Pushy Description | Quality | 3 | PASS | both twins `10 verified, 0 broken, 17 info`; 14/14 regressions; planted probe named |
| D8 | Orchestrator refs | Process | 4 | FAIL | names resolve; the Step 1 self-check contradicts the new branch (X-7); the cadence reader cites a non-writer (X-11) |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | no mismatches |
| D10 | Command → skill paths | References | 4 | FAIL | references resolve; stale control baseline, blind references regex, unfalsifiable copy-path threshold remain (W-20, W-22, W-23) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | W-14..W-17 still open; new EPA 2.8→2.9 ordering and 2.9→2.9b "when installed" gaps (X-2, X-3) |
| D12 | Examples conventions | Currency | 5 | PASS | 20/9/11, no violations |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | unchanged |
| D14 | Bootstrap output | Process | 2 | FAIL | W-18, W-19 open; EPA's new report slots do not match the verdicts (X-5) |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 numeric errors in scope |
| D16 | Project-info isolation | Privacy | 1 | PASS | 3 sources (12 variants + 21 + 56); 0 hits on every surface, 185 commits; but the gate's command is blind (X-6) |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | 1 of 8 applied IDs in scope clean; behavioural probing has no maintenance home (X-18) |

## Findings ledger — Run 2

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| X-1 | HIGH | `.claude/commands/existing_project_adaptation.md:579-580` | The Step 2.9 process-agents loop reads `else` then `elif`, so it fails `bash -n` and aborts without copying any process agent, `diff-pattern-extractor` included. Introduced by the W-2 fix (ecace7a) and PUSHED; the author's probe ran only the skills loop. | applied `s700a382` |
| X-2 | MEDIUM | `existing_project_adaptation.md:525` (Step 2.8) vs `:549-585` (Step 2.9) | `SKIPPED (identical)` cannot be reached for framework-installed components: Step 2.8 adds missing lineage fields (`fixes:`, `last_eval:`) to every agent and skill before Step 2.9's `diff`/`cmp`, so every pristine copy reports DIFFERS. | open |
| X-3 | MEDIUM | `existing_project_adaptation.md:564-570`, `:668-671`; `autonomous-loop/SKILL.md` C1 | Two coupled-set members (`codebase-audit`, `skill-gate`) are copied in Step 2.9b with a silent `[ ! -d ] && cp` and get no verdict, so "refresh ALL or NONE" cannot be executed for them. C1's claim that `/existing_project_adaptation` "refreshes these writers as one set" is false for these two. | open |
| X-4 | LOW | `existing_project_adaptation.md:603`, Step 2.3 (`:358-399`); `:560` | The rest of the v2.23.0 contract still cannot reach existing projects: the rules loop prints `SKIPPED (already exists) — verify manually` for `session_rules.md`, and no step adds the pendencias `origin:` legend. "Refresh" names no command, and reusing `cp -r` onto an existing directory nests it (`dest/x/x/SKILL.md`). The per-skill ASK (:560) contradicts "ALL or NONE" (:564). | open |
| X-5 | LOW | `existing_project_adaptation.md:1180-1181`, `:1188` | The report slots do not match the verdicts: the coupled-set slot has no "copied fresh / identical / mixed" value and offers an impossible `not installed`; `:1188` still reads "skipped (already existed)"; no slot receives the agents loop's Copied/SKIPPED output. | open |
| X-6 | MEDIUM | `.claude/commands/maintenance.md:433-438` | The `-w` isolation grep matches only the PARTS of folder names, and `_` is a word character, so it returns 0 on an underscore-joined folder name. It gives a false GREEN for 2 of 3 current projects, on the command that gates every push. Introduced by the W-12 fix; its negation proof used a part as the seed. | applied `s700a382` |
| X-7 | MEDIUM | `docs/modules/skills/sprint-proposer/SKILL.md:83-86` vs `:92-94`, `:283` | Step 1's self-check still reads `` `0` means continue below ``, contradicting the new "no marker on a continuous firing → end the trigger" branch. `/sprint-proposer continuous` also collides with an owner manually starting the mode, and Step 1 cannot tell a trigger firing from a manual call. | open |
| X-8 | LOW | `sprint-proposer/SKILL.md:83-84`; `autonomous-loop/SKILL.md:561-564`, `:577-581`, `:627-628` | The no-marker branch says "ENDS ITS TRIGGER" without the mechanism, which lives only in autonomous-loop and is never loaded on that path. A scheduled session is never told to carry `continuous`, so after a revocation it proposes a sprint unattended. Revocation emits no digest to name the mechanism in. "cancel continuous mode" has no reader outside autonomous-loop. | open |
| X-9 | MEDIUM | `autonomous-loop/SKILL.md:541`, `:551`, `:632-639` | The C4 closure check gives a false RED on a healthy state when the owner (a declared concurrent writer) commits after the task's last commit, or when HEAD is a merge. A closure RED has no disposition (no `State`, trigger not ended), and C6 re-entry never re-runs the check, so the next firing walks past it. Probe cases G and J. | open |
| X-10 | MEDIUM | `autonomous-loop/SKILL.md:431-437`; `docs/modules/agents/prd_sync_checker.md:52`; `docs/modules/skills/codebase-audit/SKILL.md:125` | The C1 tagging-writer check is a hand-typed 7-path list with a whole-file tag test: a writer outside the list, or a second untagged writer in an already-tagged file, reads GREEN (probe cases 3 and 4). The receipt's `PAT` misses `prd_sync_checker.md:52`, whose origin class is undeclared, and cannot see `codebase-audit:125`. | open |
| X-11 | MEDIUM | `sprint-proposer/SKILL.md:60-62`; `autonomous-loop/SKILL.md:151-152`, `:590-593`, `:615-618`, `:649`; `project-md-updater/SKILL.md:21-27` | The audit-cadence reader cites `autonomous-loop` → Step 2 as a Progress Log writer, but Step 2 writes only the final report, so segment mode still counts 1 per session. Brake, guardrail and revocation `/session-end` entries carry no `counts as`, and the C6 reset then erases the window. The idle `counts as 0` mandate lives in C5, not in C6's idle step (§9). | open |
| X-12 | MEDIUM | `autonomous-loop/SKILL.md:579-581` vs `:622-628` | "the trigger MUST carry the `continuous` argument (C6)" is buried in a rationale sub-bullet, and C6 states the argument only descriptively ("The recommended trigger is …"). There is no dedicated ALWAYS line (§6). | open |
| X-13 | LOW | `autonomous-loop/SKILL.md:557`, `:577-578`, `:599` | "EXCEPT revocation" also exempts revocation from ending at a task boundary, and a marker removed mid-task (another session) has no owning branch (probe case R). Revocation drops the digest without saying so, and the digest's Stop reason field has no revocation value. | open |
| X-14 | LOW | `autonomous-loop/SKILL.md:55` | "If NEITHER happened" is a two-way quantifier under the four-path table the W-8 fix corrected. | applied `s6b4baa8` |
| X-15 | LOW | `assets/docs/audit-2026-09-14.md` Run 1 receipts (`:379`, `:411-425`, `:456`, `:498`, `:531`) | The batch's receipts repeat the evidence-form class W-11 corrected: 20 `✓` cells with no quoted command; `push:` outside the enumerated verdicts; `control back-sweep:` not in `K of N` form; `negation proof:` points at a command-less commit body; `:438`'s negation is claimed with no `$` line; "no figure below was typed" is false. | open |
| X-16 | MEDIUM | `audit-2026-09-14.md:531`; `origin/main` = 12afa4a | The push happened, and the "re-run after that commit" that gated it was persisted nowhere. It is recorded in THIS run's header (GREEN, total 0), so the evidence now exists. The process gap does not close: no receipt slot or step persists a post-commit gate result. | open |
| X-17 | MEDIUM | `.claude/commands/maintenance.md` post-change checklist (no hit for `probe\|synthetic\|scratch\|bash -n`) | A behavioural probe in a scratch project was executed 7 times (86ed15e, e4e93de, 0738f28, d27e09f, e238330, d812c07, ecace7a) with no maintenance home: no rule for when to probe, how to extract checks verbatim, what coverage is required, or where the script and output persist. No control syntax-checks the bash embedded in commands. X-1 passed every checklist key. | open |
| X-18 | LOW | `.claude/commands/maintenance.md:931-935` | The new "ALWAYS NAME THE MIGRATION PATH" rule has no receipt key and no mechanical check. | open |
| X-19 | LOW | `autonomous-loop/SKILL.md:583-585` vs `:632-634` | C5 says resuming "ALWAYS runs C6's reset step first", but C6 triggers the reset only "On an owner 'continue'". An "adjust the policy" answer is covered by neither. | open |
| X-20 | LOW | `audit-2026-09-14.md` Run 1 ledger W-12 row vs `maintenance.md:440` | The "negation-prove it against a seeded file" instruction does not say the seed must be a FULL folder name. A part seed passes a pattern that is blind to the whole name, which is exactly how W-12's fix was certified (X-6). | applied `s700a382` |
| X-21 | MEDIUM | `.claude/commands/existing_project_adaptation.md:545-557` | [filed by the maintenance session applying Run 2, on 2026-09-14, from its independent pre-commit verifier] Step 2.9 skills loop never created `.claude/skills/` and echoed `Copied skill` even when `cp -r` failed — 12 printed, 0 installed on a project with partial structure (the bootstrap twin creates it at Step 5.7). | applied `s700a382` |
| X-22 | LOW | `.claude/commands/existing_project_adaptation.md:574-584` vs `:595` | [filed by the maintenance session applying Run 2, on 2026-09-14, from its independent pre-commit verifier] Step 2.9 agents loop ran before any `mkdir` of `.claude/agents/` and echoed success on a failed `cp`; the X-1 fix is what made this code reachable. | applied `s700a382` |
| X-23 | LOW | `.claude/commands/existing_project_adaptation.md` Step 2.9 agents loop (`[ ! -f ]`) | [filed by the maintenance session applying Run 2, on 2026-09-14, from its independent pre-commit verifier] When a DIRECTORY sits at an agent's target path, `[ ! -f ]` passes and `cp` writes the file INTO it, printing `Copied` for a file not at its path; with `ARGUMENTS` unset or misspelled the new `mkdir -p` makes both loops succeed into `projects/.claude/` or a new folder instead of failing. Contrived; not fixed. | open |
| X-24 | MEDIUM | `.claude/commands/maintenance.md` post-change checklist item 3 isolation gate | [filed by the maintenance session applying Run 2, on 2026-09-14, from its independent pre-commit verifier] The gate fed `git diff --cached --name-only` through whitespace-splitting `xargs`, and git escapes non-ASCII paths: a leak staged under a path with a space, a quote or an accent made grep error to stderr and the count read 0 over 2 real leaks (HEAD identical). | applied `s700a382` |

**Carried forward from Run 1 (re-verified against the current disk, all still true):**

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| W-14 | MEDIUM | `.claude/commands/bootstrap.md:455-457` | Step 5.8 notes into `code-reviewer.md`, which Step 7 creates; Step 7 never picks the note up (Agent 2) | open |
| W-15 | LOW | `bootstrap.md:145-156` | The Step 1.2 receiver mandate is not echoed in Steps 2/3 (Agent 2) | open |
| W-16 | LOW | `bootstrap.md:362-363` | "extracted unconditionally above", but the extraction is below (Agent 2) | applied `s6b4baa8` |
| W-17 | LOW | `existing_project_adaptation.md:333-355` | Step 2.2 writes the PRD row and log before they exist (Agent 2) | open |
| W-18 | LOW | `bootstrap.md` Step 15 | Report slots missing (Agent 2) | open |
| W-19 | LOW | `existing_project_adaptation.md` Step 5.2 | MCP/Hooks slots with no producing step (Agent 2) | open |
| W-20 | LOW | `maintenance.md:757-758` | Control baseline 62, today 63 (Agent 4) | applied `s30c3d6e` |
| W-21 | LOW | `maintenance.md:236`, :177 | Elision grep sees a leading `<` only; no literal-stdout check (Agent 4) | open |
| W-22 | LOW | `maintenance.md:420` | The references regex is blind to quoted-heading citations; it returned 0 on ecace7a (Agent 4) | open |
| W-23 | LOW | `audit.md:243-246` | Copy-path derivation returns 30 vs "at least 5"; unfalsifiable (Agent 4) | open |
| W-24 | LOW | `audit.md` D6.7 | Whole-block diff expectation vs TWINNED SPAN scope; still rc=1 structural (Agent 3) | open |
| W-25 | LOW | `maintenance.md` Audit intake item 8 | Correction-commit receipt form undocumented; ecace7a did not touch it | open |
| W-26 | LOW | `maintenance.md` "Version bumps" | Trigger (b) proposed, not gated; untouched | open |
| W-27 | LOW | `session-end/SKILL.md:112` vs :125 | Minimum-items conflict (Agent 5) | open |
| W-28 | LOW | `code_reviewer.md:41`; `validation-orchestrator/SKILL.md:118` | Coverage-gap section name mismatch (Agent 5) | open |
| W-29 | LOW | session-end, sprint-proposer, validation-orchestrator, validator, code_reviewer | Pre-existing instruction-style instances (Agent 5) | open |
| W-30 | LOW | `examples/README.md:69` | "session0 prompt" (Agent 5) | applied `s6b4baa8` |
| W-31 | LOW | `rules-agents-updater/SKILL.md:119` | Whole-file `grep -l` vocabulary check (Agent 3) | open |

## Recommended Fixes

1. **Quick fixes:**
   - **X-1 first.** Delete the stray `else` at `existing_project_adaptation.md:579`. It is live on `origin/main`, and any `/existing_project_adaptation` run today installs no process agents.
   - X-14, W-16, W-20, W-30.
2. **Structural fixes:**
   - **X-6** — the push gate. Add full folder-name variants to `P`, or drop `-w` for the full-name alternatives.
   - X-7, X-9, X-10, X-11, X-2, X-3, W-14, W-17, W-18, W-19.
3. **Process controls (the cause, per the meta-observation):**
   - **X-17** — give the behavioural probe a maintenance home, and add a `bash -n` pass over every fenced shell block the batch touched.
   - X-20, X-16, X-18.
4. **Quality improvements:** X-4, X-5, X-8, X-12, X-13, X-15, X-19, W-15, W-21..W-29, W-31.
5. **ESCALATED — owner decision required:** none. No privacy hit on any surface.
6. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48.

## Meta-observation

`prediction: 3 clauses evaluated — void, held, held`
- "If the next maintenance batch fixes W-1..W-7 with the post-change checklist as its only control" → **void**. The checklist was not the only control, because the author also ran a behavioural probe.
- "at least one of those fixes will introduce a new owner-less promise or unreachable check in `autonomous-loop`" → **held**. W-4's closure RED has no disposition and fires on healthy concurrent commits (X-9). Mid-task revocation is unowned (X-13). The trigger-end promise in sprint-proposer points at a mechanism never loaded on that path (X-8). C1 cannot go RED for writers outside its list (X-10).
- "What would falsify this is a behavioural probe … if that probe is run and finds nothing, this prediction fails" → **held**. The probe ran and found one defect (the P7 draft), so it did not find nothing. Agent 6's independent probe found more.

**The recurring class this run saw: AUTHOR-SELECTED COVERAGE.** Every defect this pass found sits exactly where the author's own verification did not reach, and next to where it did:
- The author probed the EPA **skills** loop and shipped a syntax error in the **agents** loop eleven lines below it (X-1).
- The author negation-proved `-w` with a folder-name **part** as the seed, and the gate is blind to the **whole** name (X-6).
- The author probed the closure check across the author's own commit sequences and never tried a concurrent owner commit, which the same skill declares will happen (X-9).
- The author back-swept writers with a regex, and missed the phrasing the regex was not written for (X-10).

Generating receipts by script removed transcription error, and every re-run in this pass reproduced. It did not remove selection error, because the author still picked the commands. Seven of thirteen fixes introduced a defect: that is a rate of 77% not clean, the second highest in the series. It happened on a batch that was 80% shipped surface and carried more verification than any before it. More author-run verification does not converge. What found these defects was a **different actor choosing the cases**, the same conclusion the 2026-09-10 prediction stated.

**Prediction, stated so it can fail.** If the X-series batch is applied with its verification designed and run by the same session that writes the fixes, at least one fix touching a fenced shell block will ship a block that `bash -n` rejects or a branch no case exercised, and it will be found by the next independent verification. **What would falsify this:** before commit, the applying session spawns an independent subagent that receives only the diff and the finding IDs, chooses its own probe cases and runs `bash -n` over every touched fence. If that subagent's cases find nothing and the next verification also finds no defect in an X fix, this prediction fails.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings (Run 2)

### Agent 1: Structural Sync
- **Part 1:**
  - **W-12 INTRODUCED-A-DEFECT:** the fix landed, but `-w` over the split parts is blind to underscore-joined full names (2 of 3 projects); see X-6.
  - The W-12 receipts are PARTIALLY-FIXED: the `:438` negation is claimed with no `$` line (X-15).
- **[A] PASS:** `diff -r` identical. **[D4] PASS:** 15/15, 7 `user` / 8 `inline`. **[D9] PASS:** 6 commands, 7 templates, 10 agents, 5 rules, 15 skills, examples 20/9/11.
- **[D16] PASS.**
  - Blocklist: 3 folder names → 12 full-name variants + 5 stoplisted parts; 21 doc identifiers (person names, domains, repo handle and URLs, phone, e-mail, city, street, design tokens, module, package/DB name, a single-project third-party API); 56 code identifiers + 2 session-number tokens.
  - Copy-path derivation returned 30 paths.
  - Negation proofs: seeded identifier → 1; seeded part → 1; known token across revisions → 1,594.
  - Surfaces: tracked files, `.claude/docs/` (3), memory (10 files; 10 hashes, all resolving), 185 commits (contents + messages).
  - Hits: none. Value shapes are all legitimate fixtures, scanner regexes or attribution. The 56 code identifiers are industry vocabulary and none was added in the range. `audit.md:264` session-number examples are rule text, already ruled legitimate.
  - The 12afa4a receipts show the pattern only as `$P` (no U-48 recurrence). 0 unpushed commits.

### Agent 2: Bootstrap Integrity
- **Part 1:**
  - **W-13 CONFIRMED-FIXED.** Probe: resolved `1/1`, placeholder `0/0`, mixed `0/1`, old path "No such file", `productionX` `0`.
  - **W-2 PARTIALLY-FIXED + INTRODUCED-A-DEFECT.** Findings (a)–(f) map to X-1..X-5.
- **[D5] PASS:** bootstrap 46 path tokens, 2 regex false positives; EPA 38, 1.
- **[D11] FAIL:** 15 primary + 10 sub-steps = 25 (26 with Setup), matching the doc. W-14..W-17 open, plus X-2 and X-3 ordering.
- **[D13] PASS**, unchanged.
- **[D14] FAIL:** W-18, W-19, X-5.
- **Carry-forward:** W-14..W-19 all still true, with line evidence.

### Agent 3: Activation Chain
- **Part 1:**
  - 0 applied IDs in D6/D7.
  - Neighbourhood undisturbed: the bootstrap install block and loop match d3748fd under `cmp`; the EPA 4.6.5 span and the 5.1 loop match under `cmp`.
  - Independently found the EPA agents-loop `bash -n` failure (X-1).
- **[D6] PASS:**
  - declarers extracted: 3 of 3; 10 gaps all exact, with install rows in both twins.
  - Table diff and TWINNED SPAN diff empty; loops identical (71 lines); whole-block rc=1, structural only (W-24).
  - The framework doc names 3 declarers; the policy twins diff empty.
  - 0 broken, 0 orphaned.
- **[D7] PASS:**
  - 20 examples fully Pushy; 10 module agents sanctioned PARTIAL.
  - `files: 10 | output: 0`.
  - Both twins `activation chains: 10 verified, 0 broken, 17 info`; twins agree on counts and all 17 INFO strings.
  - Regression 1..14 all as expected; case 13b (validator only) gives `1 verified, 9 broken`, with visual regression verified.
  - Planted probe: `BROKEN CHAIN: custom-quantum declares 'quantum decoherence gap'…`.
  - Repo porcelain unchanged.
- **Carry-forward:** W-24 and W-31 still true.

### Agent 4: Orchestration & Commands
- **Part 1:**
  - W-6, W-8, W-9 CONFIRMED.
  - **W-3 and W-7 PARTIALLY-FIXED + INTRODUCED-A-DEFECT** (X-7, X-8, X-11).
  - **W-11 FIXED-BUT-CLASS-NOT-SWEPT** (X-15).
  - W-12 CONFIRMED on a part seed; superseded by Agent 1's full-name seed.
  - The C0 sweep over `.claude/`, `docs/`, `examples/`, CLAUDE.md and README (tracked and untracked) found 0 files; the only survivor is `audit-2026-09-03.md`.
  - Independently found X-1.
- **[D8] FAIL:**
  - Every name resolves, and every cross-section citation is a real heading, exact or a sanctioned prefix.
  - RED in substance: sprint-proposer:61 cites Step 2 as a Progress Log writer (X-11); the Step 1 self-check contradicts :83-86 (X-7).
- **[D10] FAIL:**
  - References resolve; D10.4 byte-identical; D10.6 parity yes.
  - D10.0: 33 checks executed.
    - Mismatches: push decay (origin moved, legitimate), references regex blind (W-22), stale control baseline 62→63 (W-20), unfalsifiable copy-path (30), Step 1 self-check vs branch (X-7).
    - Liveness ran in structural mode (js-yaml absent).
    - Defect-series parity `GREEN 9+2=11` before this run's append.
- **Receipts reproduction:** every numeric stdout reproduced. The exceptions are legitimate time-dependent keys: push decay, push (origin now equals HEAD; the equivalent re-run over `e5613f4..HEAD` gives total 0) and report deletions (nothing staged).

### Agent 5: Document Accuracy
- **Part 1:**
  - W-9 CONFIRMED.
  - W-8 FIXED-BUT-CLASS-NOT-SWEPT (:55 "NEITHER").
  - **W-10 PARTIALLY-FIXED + INTRODUCED-A-DEFECT** (buried `continuous` mandate; idle `counts as 0` not in C6).
  - New: 5a→X-12, 5b/5c→X-11, 5d→X-13, 5e→X-14.
- **[C] FAIL:** 2 new instances in batch text. Everything else in ecace7a's added text is clean. W-29's 7 remain, shifted about +9 lines in sprint-proposer.
- **[E] PASS:**
  - v2.23.0 on README ×2, audit.md, EPA ×2 and the claude_md template.
  - maintenance.md:934's "v2.23.0 shipped…" is historical and correct.
  - No `created:` rewritten.
- **[D12] PASS:** full-set truth tests 20/9/11.
- **[D15] PASS:** every numeric claim matches the disk, including the Level 5 continuous "every 10 tasks".
- **Carry-forward:** W-27..W-30 still true.

### Agent 6: Process Coverage (meta)
- **Part 1:**
  - W-6 CONFIRMED.
  - W-1 class-not-swept.
  - **W-2, W-3 introduced-a-defect.**
  - **W-4 partial + introduced.**
  - W-5 introduced.
  - W-7 partial.
  - W-11 class-not-swept.
- **[D17] FINDINGS:**
  - **Undocumented executed flow:** the behavioural probe, 7 executions with no maintenance home and no `bash -n` control (X-17). Script-generated receipts: 1 execution, below threshold.
  - **Referenced-but-unowned:** trigger-end mechanism (X-8), other-session revocation reader (X-8), closure-RED disposition (X-9), prd-sync-checker origin class (X-10).
  - **Aspirational:** the migration rule has no receipt key (X-18); the push gate re-run is unpersisted (X-16).
  - **D17.4 verdicts:**
    - C1 false GREEN outside its list;
    - `N checked` not printed by the check;
    - C4 provable but RED on healthy states;
    - no-marker branch unprovable (the argument is invisible to any grep);
    - EPA agents loop dead;
    - skills loop, :438, :1116 and :153 provable;
    - `PAT` incomplete.
  - **D17.5:** generated receipts, the author probe and the post-commit gate are all author-verified or unpersisted, so none is a gate. Script generation narrows error to command SELECTION.
  - **Probe** (checks extracted verbatim from HEAD, scratch repo): C4 cases A–R with literal outputs (G and J false RED; R unowned); C1 cases 1–5 (3 and 4 false GREEN); EPA agents loop `bash -n` rc=2, rc=0 at ecace7a~1.
  - **D17.6:**

    | Batch | Range | Shipped | Apparatus | Shipped share |
    |---|---|---|---|---|
    | this (fix + write-back) | ecace7a~1..12afa4a | 143 | 223 | 39% |
    | fix commit alone | ecace7a~1..ecace7a | 143 | 35 | 80% |
    | feature | e5613f4..e82176f | 294 | 14 | 95% |
    | B1 | d27e09f..e5613f4 | 2 | 152 | 1.3% |
    | Run 11 | 7c1ec64..d27e09f | 11 | 1058 | 1.0% |
    | Run 10 | 84f094d..7c1ec64 | 4 | 1795 | 0.2% |
    | Run 9 | fd3d027..84f094d | 4 | 1261 | 0.3% |
    | Run 8 | 2ba16ee..fd3d027 | 12 | 856 | 1.4% |

    Shipped delta no longer trends to zero, but the rate did not fall. **Inadequate:** the only behavioural control has no home, no persisted script and no coverage rule.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions and Part 1 (13 of 13 applied IDs verdicted) evaluated; 20 new findings (1 HIGH, 10 MEDIUM, 9 LOW) + 18 carried.
**defect series:** row 12 appended to the one table in `.claude/commands/audit.md` — 10 of 13; no other surface touched.
*(superseded)* **Application status:** `PENDING`
**Application status:** `PARTIAL — 6 of 24 applied in s700a382`

## Post-change checklist receipts (`s700a382`)

Applies X-1, X-6, X-20, X-21, X-22, X-24 of Run 2. Every `$` line below was EXECUTED by a script and its stdout pasted. The two TABLES are typed by the author; their quoted commands are re-runnable.

**push decay:** origin/main unchanged (12afa4a); unpushed: the 2 Run 2 report commits + 700a382
    $ git status -sb | head -1; git log -1 --format=%h origin/main
    ## main...origin/main [ahead 3]
    12afa4a

**inventory sweep:** N/A — no artifact added, removed, renamed or split
    $ git show --name-status --format= 700a382 | grep -cE '^[ADR]'
    0

**instruction style:** counts below — unit: a diff line carrying a CAPS imperative; range 700a382~1..700a382; any buried line is printed beneath for inspection
    $ D='git diff 700a382~1..700a382 -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"; $D | grep '^+' | grep -E '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b' | cut -c1-90
    3 added, 2 rewritten, 0 buried

**references:** 0 path/§ citations added (the batch cites only finding IDs)
    $ git diff 700a382~1..700a382 | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l
    0

**fences:** 12 of 12 templates extract non-empty (no template touched)
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; n=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$n" -gt 0 ] && echo ok || echo "ZERO $t"; done | sort | uniq -c
         12 ok

**isolation:** 2 files scanned, 0 hits — the NEW gate form (F substring + P word), range-scoped; patterns derived at runtime, never echoed
    $ F=$(ls -d projects/*/ | xargs -n1 basename | awk '{print; a=$0; gsub(/_/,"-",a); print a; b=$0; gsub(/-/,"_",b); print b}' | sort -u | paste -sd'|' -)
      P=$(ls -d projects/*/ | xargs -n1 basename | tr '_-' '\n\n' | awk 'length($0)>=4' | grep -vxE 'system|page|site|core|base|main|data|admin|trabalho|projeto' | sort -u | paste -sd'|' -)
      n() { git -c core.quotepath=off diff --name-only -z 700a382~1 700a382 | xargs -0 -r grep -oni"$1"E "($2)" | wc -l; }
      echo "files $(git diff --name-only 700a382~1 700a382 | wc -l) | hits $(( $(n '' "$F") + $(n w "$P") ))"
    files 2 | hits 0

**class sweep:** 6 fixes × 4 directions, 3 extra instances found and fixed (X-21, X-22, X-24 — found by the independent verifier), 1 found and left open (X-23)
    $ echo "LATERAL X-1/X-21/X-22 — copy echoes: $(grep -c 'echo "Copied' .claude/commands/existing_project_adaptation.md) | conditional: $(grep -c 'then echo "Copied' .claude/commands/existing_project_adaptation.md)"
      echo "LATERAL X-6/X-24 — other xargs greps over a staged/diff file list: $(grep -rnE -- '--name-only[^|]*\| *xargs [^-]' .claude/commands | wc -l)"
      echo "PARALLEL — bootstrap mkdir of skills+agents before its copy: $(grep -c 'mkdir -p projects/$ARGUMENTS/.claude/skills projects/$ARGUMENTS/.claude/agents' .claude/commands/bootstrap.md)"
      echo "PARALLEL — other copies of the isolation pattern: $(grep -rln 'grep -oni' .claude docs CLAUDE.md README.md | wc -l) file(s)"
    LATERAL X-1/X-21/X-22 — copy echoes: 4 | conditional: 2
    LATERAL X-6/X-24 — other xargs greps over a staged/diff file list: 0
    PARALLEL — bootstrap mkdir of skills+agents before its copy: 1
    PARALLEL — other copies of the isolation pattern: 1 file(s)

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| X-1 | s700a382 | .claude/commands/existing_project_adaptation.md | ✓ `bash -n` over every bash fence in both commands: 0 new failures | ✓ `grep -n "elif" .claude/commands/bootstrap.md` — no if/elif copy chain in the twin | ✓ `bash -n` plus a verbatim mirror run of the skills fence above (verifier rounds 1-2) | ✓ `grep -n "DIFFER from the framework" .claude/commands/existing_project_adaptation.md` — report slot unchanged | X-22 (agents loop reachable, no mkdir) — fixed |
| X-21 | s700a382 | .claude/commands/existing_project_adaptation.md | ✓ `grep -c 'echo "Copied'` — 4 sites; the 2 cp-based loops now conditional, rules/guard write via redirect into mkdir-created dirs | ✓ bootstrap Step 5.7 `mkdir -p .../.claude/skills .../.claude/agents` exists | ✓ `grep -n "ALSO created at the top of the skills fence" .claude/commands/existing_project_adaptation.md` — rules-fence comment updated | ✗ a `FAILED to copy` verdict has no Step 5.2 slot — already inside open X-5 (report slots) | none |
| X-22 | s700a382 | .claude/commands/existing_project_adaptation.md | same edit as X-21 | same edit as X-21 | ✓ `[ ! -f ]` with a directory at the target reads Copied | n/a | X-23 — left open (contrived) |
| X-6 | s700a382 | .claude/commands/maintenance.md | ✓ `grep -rln 'grep -oni' .claude docs CLAUDE.md README.md` — only maintenance.md carries the gate | n/a — /audit D16.1 derives variants in its own agent prose | ✓ negation instruction (X-20) and the `NEVER echo` line | ✓ `**isolation:**` receipt format unchanged | X-24 (xargs whitespace / quotepath) — fixed |
| X-20 | s700a382 | .claude/commands/maintenance.md | ✓ `grep -rn "Negation-prove" .claude/commands` — 1 site | n/a | ✓ `grep -n "extend THAT list" .claude/commands/maintenance.md` — still holds (full names bypass the stoplist, stated) | n/a | none |
| X-24 | s700a382 | .claude/commands/maintenance.md | ✓ the name-only-to-xargs grep in the class-sweep `$` line above — 0 other sites | n/a | ✓ empty staged list: `-r` keeps grep off stdin (gate returns 0) | n/a | none |

**back-sweep:** 1 rule (a staged file list is fed NUL-separated), 1 artifact fixed of 1 found, re-run clean
    $ for rev in 700a382~1 700a382; do echo "$rev whitespace-split staged-list greps: $(git show $rev:.claude/commands/maintenance.md | grep -cE -- '--name-only \| xargs grep')"; done
    700a382~1 whitespace-split staged-list greps: 1
    700a382 whitespace-split staged-list greps: 0

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| a staged/diff file list feeding grep is NUL-separated (`-z` plus `xargs -0`) | a name-only file list piped into a whitespace-splitting `xargs grep` in an executed command | the `$` line above, run at `700a382~1` and `700a382` | 1 | 1 | 0 remaining |

**liveness:** skipped — no frontmatter touched; raw-template parse re-run as a control
    $ python -c "import io,yaml,glob,os
      bad=n=sk=0
      for p in sorted(glob.glob('docs/modules/skills/*/SKILL.md'))+sorted(glob.glob('docs/modules/agents/*.md')):
          s=io.open(p,encoding='utf-8').read().replace(chr(13)+chr(10),chr(10))
          if not s.startswith('---'): sk+=1; continue
          n+=1; d=yaml.safe_load(s[4:s.index(chr(10)+'---',4)+1])
          exp=os.path.basename(os.path.dirname(p)) if p.endswith('SKILL.md') else os.path.basename(p)[:-3].replace('_','-')
          bad+= d.get('name')!=exp
      print('raw templates: %d validated of %d globbed, %d skipped (fenced),'%(n,n+sk,sk),'OK' if not bad else 'BROKEN')"
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** the X-1 fence goes RED at 700a382~1 and GREEN at 700a382; the gate's seed negations (full/glued/part/space-path) are in 700a382's body and were independently reproduced by two verifier rounds
    $ T="${TMPDIR:-$TEMP}"; git show 700a382~1:.claude/commands/existing_project_adaptation.md | awk '/^\*\*Copy process agents/{s=1} s&&/^```bash$/{f=1;next} f&&/^```$/{exit} f{print}' > "$T/ag0.sh"; git show 700a382:.claude/commands/existing_project_adaptation.md | awk '/^\*\*Copy process agents/{s=1} s&&/^```bash$/{f=1;next} f&&/^```$/{exit} f{print}' > "$T/ag1.sh"
      bash -n "$T/ag0.sh" 2>/dev/null && echo "agents fence at 700a382~1: parses" || echo "agents fence at 700a382~1: SYNTAX ERROR (red, as expected)"; bash -n "$T/ag1.sh" 2>/dev/null && echo "agents fence at 700a382: parses" || echo "agents fence at 700a382: SYNTAX ERROR"
    agents fence at 700a382~1: SYNTAX ERROR (red, as expected)
    agents fence at 700a382: parses

**version:** none — PATCH-class corrections to the published v2.23.0; old-label residue 0
    $ grep -rn "v2\.22\.0" README.md docs/ .claude/ CLAUDE.md 2>/dev/null | grep -v 'created: framework-v' | wc -l
    0

**classification:** 2 add (EPA mkdir line, rules-fence note), 5 substitute (agents else removal, 2 conditional cp echoes, gate F/P block, negation-seed rule) — each strictly more correct, no information lost; 0 delete, 0 relocate
    $ echo "n/a — judgement, not measurement"
    n/a — judgement, not measurement

**new component:** none this session
    $ git show --name-status --format= 700a382 | grep -c '^A'
    0

**gates:** twin parity PASS | receipt-rows-vs-diff 6/6 | row-count 6 vs 6 | slots 0/0 (no report key added)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo "twin parity PASS"; git show --name-only --format= 700a382 | tr '\n' ' '; echo; echo "applied rows s700a382: $(grep -cE '^\| X-[0-9]+ \|.*applied `s700a382` \|$' assets/docs/audit-2026-09-14.md)"; echo "new ALWAYS REPORT keys: $(git diff 700a382~1..700a382 -U0 | grep '^+' | grep -coE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:')"
    twin parity PASS
    .claude/commands/existing_project_adaptation.md .claude/commands/maintenance.md 
    applied rows s700a382: 6
    new ALWAYS REPORT keys: 0

**applied-proof:** 6 of 6 IDs resolve to a commit touching their line (form A: 0, form B: 6 — every fix replaced its pre-batch line wholesale or removed a non-unique one, so each is anchored on the TIP line of its new text), 0 forced to open
    $ B=700a382~1; E=.claude/commands/existing_project_adaptation.md; M=.claude/commands/maintenance.md
      pf() { l=$(grep -nF -- "$3" "$2" | head -1 | cut -d: -f1); s=$((l-${4:-0})); c=$(git log -L "$s,$l:$2" --oneline $B..700a382 | grep -cE '^[0-9a-f]{7} '); echo "$1 form-B tip-lines $s-$l commits $c"; }
      pf X-1 "$E" 'elif cmp -s "docs/modules/agents/' 1   # a DELETION: span the line above the elif, where the stray else was
      pf X-6 "$M" 'F=$(ls -d projects/*/ | xargs -n1 basename \'
      pf X-20 "$M" 'ALWAYS seed ALL THREE forms'
      pf X-21 "$E" 'then echo "Copied skill'
      pf X-22 "$E" 'then echo "Copied agent'
      pf X-24 "$M" 'core.quotepath=off diff --cached --name-only -z'
    X-1 form-B tip-lines 580-581 commits 1
    X-6 form-B tip-lines 441-441 commits 1
    X-20 form-B tip-lines 455-455 commits 1
    X-21 form-B tip-lines 554-554 commits 1
    X-22 form-B tip-lines 580-580 commits 1
    X-24 form-B tip-lines 451-451 commits 1

**control back-sweep:** 2 of 63 controls touched and re-run with negation (the isolation gate; its seed-negation rule), 0 defects left — the verifier rounds found 2 defects in the gate's own earlier draft (X-6 residual, X-24), both fixed before 700a382. Denominator:
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md; do inf=$(awk '/^```/{g=!g;next} g' "$f" | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 5 fenced + 23 inline = 28
    .claude/commands/audit.md: 7 fenced + 28 inline = 35

**commit correction:** none
    $ echo "n/a — no prior commit message corrected"
    n/a — no prior commit message corrected

**placeholder:** none
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-14.md | wc -l
    0

**report deletions:** none
    $ for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:'; do b=$(git show HEAD:assets/docs/audit-2026-09-14.md | grep -cE "$pat"); a=$(grep -cE "$pat" assets/docs/audit-2026-09-14.md); [ "$a" -lt "$b" ] && echo "RED $pat"; done; echo "structural decreases: none listed above"
    structural decreases: none listed above

**report restore:** none
    $ echo "n/a — no destroyed report"
    n/a — no destroyed report

**audit:** N/A — no MINOR/MAJOR bump
    $ echo "n/a — no bump"
    n/a — no bump

**verification audit:** proposed — trigger (d); two independent verifier rounds already ran BEFORE commit
    $ echo "n/a — owner decision"
    n/a — owner decision

**defect series:** N/A — maintenance session, no row
    $ echo "n/a — maintenance session"
    n/a — maintenance session

**push:** requested — D16 GREEN over the unpushed range at receipt time (new F+P gate); the write-back commit carrying this section is re-gated by the same command immediately before the push, branching on its count
    $ F=$(ls -d projects/*/ | xargs -n1 basename | awk '{print; a=$0; gsub(/_/,"-",a); print a; b=$0; gsub(/-/,"_",b); print b}' | sort -u | paste -sd'|' -)
      P=$(ls -d projects/*/ | xargs -n1 basename | tr '_-' '\n\n' | awk 'length($0)>=4' | grep -vxE 'system|page|site|core|base|main|data|admin|trabalho|projeto' | sort -u | paste -sd'|' -)
      c1=$(git -c core.quotepath=off diff --name-only -z origin/main HEAD | xargs -0 -r grep -oniE "($F)" | wc -l); c1w=$(git -c core.quotepath=off diff --name-only -z origin/main HEAD | xargs -0 -r grep -oniwE "($P)" | wc -l)
      c2=$(git log --format=%B origin/main..HEAD | grep -oniE "($F)" | wc -l); c2w=$(git log --format=%B origin/main..HEAD | grep -oniwE "($P)" | wc -l)
      c3=$(git diff origin/main HEAD | grep '^+' | grep -cE 'eyJ[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{20,}')
      echo "commits $(git log --oneline origin/main..HEAD | wc -l) | contents $((c1+c1w)) | messages $((c2+c2w)) | value-shapes $c3 | total $((c1+c1w+c2+c2w+c3))"
    commits 3 | contents 0 | messages 0 | value-shapes 0 | total 0



# Run 3 — verification pass

**Date:** 2026-09-15
**Framework version:** v2.23.0 (README.md:1)
**Run mode:** `verification (over 700a382)`
**Trigger:** (d). `/maintenance` applied X-1, X-6, X-20, X-21, X-22 and X-24 in 700a382; the write-back is 8f190b9. The batch range is `700a382~1..8f190b9`, PUSHED (`origin/main` = 8f190b9).
**Dimensions checked:** 17, plus the Part 1 fix-verification pass
**Agents dispatched:** 6
**Part 1 input:** Run 2 of this file, intact at 8f190b9. Its ledger rows X-1..X-24 are present, and 6 are `applied s700a382`. No reconstruction was needed.
**ID series:** `Y`. Collision check before writing: `grep -c '^| Y-'` returned 0 across every report.
**What this run tests:** this is the first batch verified BEFORE commit by independent subagents that chose their own cases, as Run 2's prediction proposed.

**carried: 36 open + 0 escalated from Run 2 of this file.** That is 18 X (X-2..X-5, X-7..X-19, X-23) plus 18 W (W-14..W-31), every one re-verified on disk as still true. X-15 and X-16 also **recurred** in this batch.
**OBSERVATIONS — accepted-risk, not re-opened:**
- E-1, M-44, N-53, T-11, T-36, U-48.
- Agent 1 re-measured 189 reachable commits, their messages and paths, the tracked tree, `.claude/docs/` and 10 memory files: **0 hits**.
- The only unreachable object carrying part tokens is orphan commit 584be08. U-48's record covers it.

**The post-push D16 re-run is persisted here**, because 8f190b9's claimed pre-push re-gate left no output (X-16 recurred):
- Agent 4, over `12afa4a..8f190b9`: contents F 0 / P 0, messages F 0 / P 0 — **GREEN**.
- Agent 1's independent full-variant scan also returned **0**. Its variants are: full name, both swaps, space-joined, separator-less, plus parts.

## Part 1 — Verification ledger for 700a382

NOT-FIXED was settled first, mechanically: every ID returns 1 commit (700a382) with `-L` on its tip lines or a pickaxe on its pre-batch text. **No ID is NOT-FIXED.** Where agents disagreed, the verdict below is the most severe one backed by a reproduced probe.

| ID | Verdict | Evidence (file:line — structural) |
|----|---------|------|
| X-1 | CONFIRMED-FIXED | `existing_project_adaptation.md:577-586` now reads `if / elif cmp / else / fi`. `bash -n` fails at 700a382~1 and passes at the tip. All 40 bash fences in both command twins parse at the tip (bootstrap 18/0, EPA 22/0). Run verbatim in three independent mirrors: 3 agents on disk; SKIPPED (identical); DIFFERS; FAILED when the target path is a file. (Agents 2, 3, 6 agree.) |
| X-6 | FIXED-BUT-CLASS-NOT-SWEPT + INTRODUCED-A-DEFECT | F (`maintenance.md:441-442`) now catches full, swapped, glued, uppercase and space-joined names for 3 of 3 projects; the old gate caught 0/1/0 on the full name. **Class not swept:** the comment names the cause (`-w` treats `_` as a word character, :435-437) and applies the remedy only to F. P (`n w "$P"`, :451) still reads **0 on 3 of 3** for a part glued by `_` or in CamelCase. A separator-less full name reads 0 on 3 of 3 (every real folder has a separator). The auditor twin `audit.md:223-224` lists only "hyphen/underscore forms". **Introduced:** a staged path beginning with `-` is parsed by grep as options. The count reads **0**, and **both patterns land on stderr, all 3 full folder names included**; the pre-batch gate leaked only parts. That breaks :454 "NEVER echo `$F` or `$P`" — U-48's class. (Agents 1, 4, 6.) → Y-1, Y-3 |
| X-20 | FIXED-BUT-CLASS-NOT-SWEPT | :454-459 requires full-name, `_`-glued and surviving-part seeds, and discriminates (breaking F turns the glued seed RED). It omits the `_`↔`-` swapped forms F claims to cover. Negation-proved by Agent 4: a gate with the swap `awk` removed passes all mandated seeds and reads 0 on project 2's swapped form. It also never says to STAGE the seeds (unstaged seeds read 0), and has no separator-less seed. (Agents 4, 5, 6; Agent 1 read CONFIRMED.) → Y-6 |
| X-21 | FIXED-BUT-CLASS-NOT-SWEPT + INTRODUCED-A-DEFECT | The skills fence is fixed (:549 mkdir, :554 conditional echo): 12 skills installed from no `.claude/`; FAILED when the target is a file. **Class not swept:** the rules fence (:603-604) and the guard fence (:615-616) still print `Copied` after a redirect that can fail. A file at `.claude/rules` gives 3 Copied and 0 files; a renamed template fence gives `Copied guard` with 0 bytes. Their mkdirs leave `$ARGUMENTS` unquoted, so a project name with a space yields 3 Copied, 0 files and stray `my/` `proj/` directories. Receipt :855's claim that these fences write "into mkdir-created dirs" is disproved. **Introduced:** the new `FAILED to copy …` verdict has no disposition (the prose at :563-573 covers only DIFFERS) and no Step 5.2 slot. Receipt :855 folded it into X-5, whose text does not name it. (Agents 2, 5, 6.) → Y-8, Y-9 |
| X-22 | INTRODUCED-A-DEFECT (fixed at the named lines) | The mkdir at :549 precedes the agents loop and :580 is conditional: 3 agents from no `.claude/`, 3 FAILED when `.claude/agents` is a file. The :597-598 note is accurate. It shares the ownerless `FAILED` verdict with X-21 (same new finding). X-23's residue reproduces as filed: a directory at the target reads Copied; unset or misspelled `ARGUMENTS` fills `projects/.claude/` or creates a folder, gated upstream by the `:27` prose. (Agents 2, 6.) → Y-9 |
| X-24 | FIXED-BUT-CLASS-NOT-SWEPT | Paths with a space, an accent or a single quote now read 1 with 0 stderr; the old gate read 0 with stderr 3/1/1. An empty staged list gives 0. The same symptom (a path's shape silently zeroing the count) survives for a **path beginning with `-`**: no `--`/`-e` before the pattern, `xargs -0` passes it as an option, and the count is 0 with 2 stderr lines. Also unswept: the gate greps the WORKING TREE, not the index (a leak staged and then cleaned reads 0), and a folder name holding a regex metacharacter makes grep error and read 0. (Agents 1, 4, 6.) → Y-1, Y-2, Y-5 |

**Score over the 6 applied FINDINGS (denominator 6): 1 of 6 clean, 5 not clean.** By primary verdict: 0 partially-fixed · 4 class-not-swept · 0 not-fixed · 1 introduced-a-defect alone. Counting combinations, 3 of 6 introduced a defect (X-6, X-21, X-22).
**Severity of what was introduced:** 0 HIGH (Run 2: 1 HIGH, the EPA syntax error), 1 MEDIUM (Y-1, full names on stderr for a leading-dash path), 1 LOW (Y-9).

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` byte-identical |
| C | Instruction style | Quality | 5 | FAIL | 4 new batch-text instances (Y-9, Y-10, Y-6, Y-11); W-29 and X-12 remain |
| E | Version consistency | Currency | 5 | PASS | v2.23.0; the batch touched no version or `created:` line |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15 |
| D5 | Bootstrap file refs | References | 2 | PASS | 42 + 37 path tokens resolve |
| D6 | Gap vocabulary | References | 3 | PASS | 10 exact, 3 of 3 declarers; install tables, TWINNED SPAN and loops byte-identical to 700a382~1 |
| D7 | Pushy Description | Quality | 3 | PASS | both twins `10 verified, 0 broken, 17 info`; 14/14 regressions; planted probe named |
| D8 | Orchestrator refs | Process | 4 | PASS | every name and citation resolves; X-7, X-11 still open |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | no mismatches |
| D10 | Command → skill paths | References | 4 | FAIL | isolation gate RED-blind on 4 input classes; Gate 3 self-matches; control baseline stale (W-20) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | carried only (W-14..W-17, X-2, X-3); the batch's mkdir interacts cleanly with 2.8, 2.9b and 4.6.5 |
| D12 | Examples conventions | Currency | 5 | PASS | full-set truth tests 20/9/11 |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | untouched |
| D14 | Bootstrap output | Process | 2 | FAIL | `FAILED` verdict has no slot (Y-9); W-18, W-19, X-5 |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 numeric errors |
| D16 | Project-info isolation | Privacy | 1 | PASS | 0 hits on every surface, including paths and unreachable objects; the GATE is blind on several forms (Y-1..Y-5) |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | independent verification has no home (X-17); push scan is a placeholder (Y-7); shipped/apparatus split misclassifies EPA (Y-16) |

## Findings ledger — Run 3

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| Y-1 | MEDIUM | `.claude/commands/maintenance.md:451` | A staged path beginning with `-` is taken by grep as options: the count reads 0 and both patterns — all 3 full folder names — are printed to stderr, breaking :454 "NEVER echo `$F` or `$P`" (U-48 class). There is no `--`/`-e` before the pattern. Introduced by the X-6 F pattern; X-24's class not swept. | applied `s6b4baa8` |
| Y-2 | MEDIUM | `.claude/commands/maintenance.md:441-452` | The gate greps the WORKING TREE, not the index. A leak staged and then cleaned on disk without re-staging reads 0 while the staged blob carries it; a clean staged file with a leak only on disk reads a false RED. It is most likely right after a RED, when the author edits and re-runs. Pre-existing. | applied `s6b4baa8` |
| Y-3 | MEDIUM | `.claude/commands/maintenance.md:443-451`; `.claude/commands/audit.md:223-224` | X-6's class is not swept. P keeps `-w`, so a surviving part glued by `_` (`<part>_table`) or in CamelCase reads 0 on 3 of 3 projects, which is exactly a snake_case code identifier. The separator-less full name reads 0 on 3 of 3. The D16.1 auditor twin names only hyphen/underscore variants. | applied `s6b4baa8` |
| Y-4 | LOW | `.claude/commands/maintenance.md:451`; `.claude/commands/audit.md:276-278` | The gate and D16.3 scan CONTENTS only. A folder name present only in a staged file or directory PATH reads 0 on 3 of 3 (the M-44 class). | applied `s6b4baa8` |
| Y-5 | LOW | `.claude/commands/maintenance.md:441-452` | A folder name containing a regex metacharacter (an unbalanced `(`, `[1]`) makes grep error to stderr and the count reads 0 over a real leak. With no `projects/` directory, F and P are empty and the gate prints 0 with 6 stderr lines, passing with nothing checked; no precondition is stated. | applied `s6b4baa8` |
| Y-6 | LOW | `.claude/commands/maintenance.md:454-459` | The mandated negation seeds omit the `_`↔`-` swapped forms: a gate with the swap removed passes every mandated seed. The rule never says to STAGE the seeds (unstaged seeds read 0) and has no separator-less seed. | applied `s6b4baa8` |
| Y-7 | MEDIUM | `.claude/commands/maintenance.md:26`; `audit-2026-09-14.md` receipts `:956-963` | The push gate's scan is still the placeholder `hits=$(<the D16 scan>)`. The 8f190b9 receipt improvised a net-tip form (`git diff --name-only origin/main HEAD \| xargs grep`) that returns 0 over a leak added and removed inside the unpushed range, while `git log -p` finds 2. That range history is exactly what a push publishes. | applied `s6b4baa8` |
| Y-8 | LOW | `.claude/commands/existing_project_adaptation.md:599`, `:603-604`, `:613-616` | X-21's class is not swept. The rules and guard fences echo `Copied` after a `sed … >` that can fail: a file at the rules or scripts path, or an empty extraction (0-byte guard). Their mkdirs leave `$ARGUMENTS` unquoted: a space in the name gives 3 Copied, 0 files and stray directories. | open |
| Y-9 | LOW | `.claude/commands/existing_project_adaptation.md:554`, `:580` vs `:563-573`, `:1182-1191` | The `FAILED to copy skill/agent` verdict added by X-21/X-22 has no disposition (stop, retry, ask) and no Step 5.2 report slot. The receipt folded it into X-5, whose text does not name it. | open |
| Y-10 | LOW | `.claude/commands/existing_project_adaptation.md:591` vs `:597-599` | The rules-fence header still reads "THREE DIRECTORIES THIS COMMAND WRITES INTO AND NEVER CREATED"; the batch appended a caveat instead of correcting it, and the mkdir it heads creates four directories. | applied `s6b4baa8` |
| Y-11 | LOW | `.claude/commands/maintenance.md:169`, `:210`, `:424` vs `:451` | The `**isolation:** N files scanned, 0 hits` slot asks for a file count the gate does not print. The gate reads `--cached`, so run verbatim after commit it scans an empty list and reads a vacuous 0. Receipts must therefore re-scope the `$` line and hand-add N, contradicting the byte-for-byte rule. | applied `s6b4baa8` |
| Y-12 | LOW | `.claude/commands/maintenance.md:638` | Gate 3 run verbatim over the report returns 7 against 6: `audit-2026-09-14.md:901`, a receipt quoting the pattern, self-matches. The section-scoping rule at :647-653 is prose; the command itself is unscoped. | open |
| Y-13 | LOW | `.claude/commands/audit.md:154` | The "No trend" line lists 11 percentages for a table of 12 rows. Row 12 was appended without its figure, and the line is a copy of the ONE table inside that table's own section. | applied `s6b4baa8` |
| Y-14 | LOW | `audit-2026-09-14.md` receipts (`s700a382`, `:810-964`) | X-15 recurs. 8 of 15 `✓` cells quote no runnable command (one records the X-23 defect under `✓`). `push:` is outside the enumerated form. `control back-sweep:` reads "2 of 63 re-run" against the mandate to run every control. `negation proof:` gives no `$` line for the gate seeds. The `liveness:` `$` line is a rewritten variant of the mandated command. | open |
| Y-15 | LOW | `.claude/commands/maintenance.md` (no home) | Script-generated receipts have now been executed twice (12afa4a, 8f190b9) with no written rule and no persisted generator, so "EXECUTED by a script" cannot be verified. The independent pre-commit verifier (1 session, 2 rounds, credited with X-21, X-22, X-24) has no home and no persisted output — attached to X-17. | open |
| Y-16 | LOW | `.claude/commands/audit.md` D17.6 | The shipped/apparatus split is by path, so `existing_project_adaptation.md` and `bootstrap.md` — the delivery path of the shipped contract — count as apparatus. X-1 (HIGH, 0 process agents delivered to existing projects) sat in a "0% shipped" batch. | open |
| Y-17 | MEDIUM | `.claude/scripts/d16-gate.sh` (first draft) | [filed by the maintenance session applying Run 3, on 2026-09-15, from its independent pre-commit verifier] Round 1: the first draft of the new gate read 0 over a real leak in seven input classes, none writing to stderr — a UTF-16 file, a `binary` .gitattributes entry, a NUL byte, `color.ui=always`, `log.showRoot=false`, a diff textconv, `i18n.logOutputEncoding`. Fixed before commit: NUL-stripped `cat-file` reads, pinned git options, `--root --no-textconv --encoding=UTF-8`; each is a selftest case. | applied `s6b4baa8` |
| Y-18 | MEDIUM | `.claude/scripts/d16-gate.sh` (second draft) | [filed by the maintenance session applying Run 3, on 2026-09-15, from its independent pre-commit verifier] Round 2: 6 false GREENs and 2 false-RED/could-not-check classes — a CamelCase folder name, an all-caps part, `i18n.commitEncoding`, `git replace` on a blob or commit, an author e-mail and an annotated tag message, a hidden folder, and names shorter than 4 characters / a sparse index. Fixed before commit (raw commit and tag objects, `GIT_NO_REPLACE_OBJECTS=1`, CamelCase split, hidden glob, non-ASCII names fail closed); the short-name and sparse-index classes fail closed and are declared as KNOWN LIMITS in the script header. | applied `s6b4baa8` |
| Y-19 | LOW | `.claude/scripts/d16-gate.sh` (third draft) | [filed by the maintenance session applying Run 3, on 2026-09-15, from running its own gate] GNU grep 3.0 (Git for Windows) aborts with rc 134 on `-i` with several literal patterns under `LC_ALL=C`, for `-F` and `-E`; every scope failed closed and the gate could never pass. Case folding moved into the pattern (`[aA]`). | applied `s6b4baa8` |

**Carried forward from Run 2 (re-verified on disk, all still true):** X-2, X-3, X-4, X-5 (Agent 2) · X-7, X-8, X-11, X-15 (recurred as Y-14), X-16 (recurred; re-run persisted in this header) (Agent 4) · X-9, X-10, X-13, X-19 (untouched files) · X-12, X-14 (Agent 5) · X-17 (Agent 6; the probe has now run 8 times) · X-18 · X-23 (Agents 2, 6: reproduces as filed) · W-14..W-19 (Agent 2) · W-20..W-23, W-25, W-26 (Agent 4) · W-24, W-31 (Agent 3) · W-27..W-30 (Agent 5). Their ledger rows stand unchanged in Run 2 and Run 1 with status `open`.

## Recommended Fixes

1. **Quick fixes:** Y-10, Y-13, W-16, W-20, W-30, X-14.
2. **Structural fixes — the isolation gate, as ONE redesign rather than more patches:** Y-1, Y-2, Y-3, Y-4, Y-5, Y-6, Y-11, Y-7 (see the meta-observation). Then Y-8, Y-9, X-7, X-9, X-10, X-11, X-2, X-3.
3. **Process controls:** X-17 plus Y-15 (give independent pre-commit verification and the probe a written home with persisted output); Y-12, Y-14, Y-16, X-16.
4. **Quality improvements:** X-4, X-5, X-8, X-12, X-13, X-15, X-18, X-19, X-23, W-15, W-21..W-29, W-31.
5. **ESCALATED — owner decision required:** none. No privacy hit on any surface.
6. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48.

## Meta-observation

`prediction: 3 clauses evaluated — void, void, void`
- "If the X-series batch is applied with its verification designed and run by the same session that writes the fixes, at least one fix touching a fenced shell block will ship a block that `bash -n` rejects or a branch no case exercised" → **void**. The antecedent did not hold: two independent subagent rounds chose their own cases before commit. For the record, no fence fails `bash -n`, but unexercised branches did ship (Y-1..Y-5, Y-8).
- "and it will be found by the next independent verification" → **void**, by the same antecedent. The unexercised branches were found by this run.
- The falsifier ("if that subagent's cases find nothing and the next verification also finds no defect in an X fix, this prediction fails") → **void**. Its first condition did not occur: the subagents found X-21, X-22, X-24 and an X-6 residual. **The falsifier was badly designed** (Agent 6): it could not distinguish a working independent control that catches defects from a failing author-only process, because both leave it untriggered.

**The recurring class this run saw: THE INDEPENDENT VERIFIER INHERITS THE FINDING'S FRAME.** The pre-commit verifiers were independent of the author, but they received the finding TEXTS and probed the named symptom and its immediate neighbours. What they tested, and what this run found next to it:
- **Paths with spaces, accents, quotes** → a path beginning with `-` (Y-1).
- **Full names glued by `_`** → parts glued by `_` and the separator-less name (Y-3).
- **The command as it reads the file list** → the working tree versus the index (Y-2), and paths versus contents (Y-4).

Every residue is an INPUT CLASS of the same shell pipeline, not a new mechanism. The rate reads 5 of 6 not clean (83%) over a small denominator, but the distribution moved: 0 HIGH (Run 2: 1), 0 fences failing `bash -n` (Run 2: 1), and one MEDIUM introduced defect against Run 2's seven introduced defects of every severity. The verifier narrowed the error; it did not close it. **The gate is being specified by enumerating shell edge cases, and each batch adds one more case, which is why each verification finds the next one.**

**Prediction, stated so it can fail.** If the Y batch patches the isolation gate case by case again (adding `--`, adding a separator-less alternative, adding `-e`), the next verification will find at least one further input class that reads 0 over a real leak. **What would falsify this is a change of SPECIFICATION, not another case:**
- the gate reads what git will publish, from git's object model — `git grep --cached` over staged blobs for contents, `git diff --cached --name-only` for paths, and per-commit `git log -p` over the unpushed range for the push gate;
- with the pattern passed via `-e`/`-f` from a file, never interpolated as a regex;
- and an independent verifier given only the diff (not the finding text).

If the next verification then finds no input class reading 0 over a real leak, this prediction fails, and that is the better outcome. The prediction also fails if the case-by-case patch happens to leave no residue.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings (Run 3)

### Agent 1: Structural Sync
- **Part 1:** X-6 FIXED-BUT-CLASS-NOT-SWEPT (separator-less name and `_`-glued part read 0 on 3/3; audit.md D16.1 twin); X-20 CONFIRMED (caveat: its seeds are exactly the forms the gate catches); X-24 CONFIRMED (`é q'.md` reads 1/2/1, stderr 0). New: working tree versus index (Y-2), `_`-glued part and separator-less name (Y-3), path-only names (Y-4).
- **Structural checks:** [A] PASS; [D4] PASS, 15/15, 7 `user` / 8 `inline`; [D9] PASS (6 commands, 7/10/5/15, examples 20/9/11, `assets/docs/` 12 files).
- **[D16] PASS.**
  - Blocklist: 12 full-name variants + 5 parts; project docs 28 tokens + 2 package names + 486 single-project capitalised tokens (148 in framework files, all generic words or public products); code 307 identifiers, 107 shared, 16 of 78 single-project traced to framework-authored examples.
  - Surfaces: 112 tracked + `.claude/docs/` + 10 memory files + 189 commits (contents, messages, paths) + reflog (229) + unreachable objects (9 commits, 42 blobs).
  - Hits: 0 reachable. Unreachable 584be08 is covered by U-48. Memory: 12 hashes resolve, the other 12 are UUID/URL fragments (excluded). The receipts show only `$F`/`$P` and an `old_<name>` placeholder.
  - Gate-vs-scan table: the gate reads 0 on the separator-less name, a `_`-glued part, a path-only name and a staged-then-cleaned leak, where the independent scan finds each.

### Agent 2: Bootstrap Integrity
- **Part 1:** X-1 CONFIRMED; X-21 FIXED-BUT-CLASS-NOT-SWEPT + INTRODUCED; X-22 CONFIRMED at the named lines + INTRODUCED (shared).
- **EPA probe cases:**
  - C1: only `src/`, so 12 + 3 installed.
  - C2 and C3: a file at the target path, so FAILED ×3 / ×12.
  - C4: a directory at an agent target reads Copied (X-23).
  - C5 and C6: unset or misspelled `ARGUMENTS` (X-23).
  - C7: `"my proj"` in the rules fence gives Copied ×3, 0 files, stray directories (Y-8).
  - C8: Step 2.8 then 2.9 gives DIFFERS (X-2).
  - C9b: full-order run, 0 errors.
  - C11: Step 5.1 cannot see X-23.
  - C12: guard with a space gives `Copied guard`, 0 files.
- **bash -n:** bootstrap 18/0/0; EPA 22, 1 at 700a382~1, 0 at the tip.
- **Dimensions:** [D5] PASS; [D11] FAIL (carried; 2.9b depends on 2.9's mkdir, INFO); [D13] PASS; [D14] FAIL (Y-9, W-18, W-19, X-5).
- **Carry-forward:** X-2, X-3, X-4, X-5, X-23 and W-14..W-19 still true.

### Agent 3: Activation Chain
- **Part 1:** 0 IDs in D6/D7. bootstrap.md untouched (rc 0). EPA edited only in Step 2.9. Install tables 11=11, TWINNED SPAN 13=13 and loops 71=71 are byte-identical to 700a382~1. The new mkdir helps Step 4.6.5.
- **[D6] PASS:** 10 gaps exact, install rows in both twins; table and TWINNED SPAN diffs empty; whole block rc=1, structural (W-24); framework doc names 3 declarers; policy twins identical; 0 broken, 0 orphaned.
- **[D7] PASS:**
  - 20 examples FULL; 9 module agents PARTIAL (sanctioned); skill_reviewer FULL.
  - Both twins `activation chains: 10 verified, 0 broken, 17 info`; twins agree, byte-identical.
  - Regressions 1-14 as expected, including 13b (`1 verified, 9 broken`, visual regression still verified) and 14 (`none installed`, exit 0).
  - Planted probe named both agents. Invariant `files: 10 | output: [0]`.
- **Carry-forward:** W-24, W-31 still true.

### Agent 4: Orchestration & Commands
- **Part 1:** X-6 class-not-swept + INTRODUCED (Y-1); X-20 class-not-swept (swap removed passes the mandated seeds); X-24 class-not-swept (leading-dash path).
- **Gate probe** (verbatim, old vs new):
  - Full name: new 1/2/1 vs old 0/1/0. Swapped: 4/1/2 vs 3/0/1. Glued: 1 each vs 0 each.
  - `_`-glued part and CamelCase: 0 each in both. Space/accent/quote path: new 1 with stderr 0, old 0 with stderr.
  - **Leading-dash path: 0, stderr prints both patterns in full.**
  - Staged-then-cleaned: 0. Clean staged + dirty working tree: false 1. No `projects/`: 0 with stderr 6.
- **[D8] PASS:** every name and citation resolves.
- **[D10] FAIL:** 32 D10.0 checks executed. Mismatches: push decay (legitimate: pushed), the isolation gate's RED-blind classes, Gate 3's self-match (Y-12), control baseline (W-20), copy-path unfalsifiable (W-23). "No trend" lists 11 of 12 (Y-13).
- **Receipts:** 24/24 keys, all numeric stdouts reproduce except the time-dependent `push decay` and `push` (legitimate). Format defects as Y-14. Post-push re-run over 12afa4a..8f190b9 is GREEN.
- **Carry-forward:** W-20..W-23, W-25, W-26, X-7, X-8, X-11 true; X-15, X-16 recurred.

### Agent 5: Document Accuracy
- **Part 1:** 0 IDs in C/E/D12/D15.
- **Batch text:** "TWO PATTERNS… SUMMED" matches the code; "Bootstrap's twin creates them at 5.7" is true.
- **New instances:**
  - 5a: FAILED verdict ownerless (→ Y-9).
  - 5b: "THREE DIRECTORIES" header stale (→ Y-10).
  - 5c: seed rule omits the swapped forms (→ Y-6).
  - 5d: `N files scanned` slot unproducible (→ Y-11).
- **Dimensions:** [E] PASS (the batch touched no version or `created:`); [D12] PASS (full set); [D15] PASS (0 numeric errors).
- **Carry-forward:** W-27..W-30, X-12, X-14 true.

### Agent 6: Process Coverage (meta)
- **Part 1:** X-1, X-22 CONFIRMED; X-21 class-not-swept (rules/guard fences); X-6 class-not-swept (concatenated form, working tree vs blob, metacharacter folder); X-20 CONFIRMED with provability notes; X-24 CONFIRMED. The G/H half of X-23 was introduced by the new mkdir, already filed, gated by `:27`.
- **Undocumented flows:** the probe has run 8 times with no home (X-17); script-generated receipts 2× (→ Y-15); the independent pre-commit verifier 1 session, 2 rounds, no home.
- **Referenced-but-unowned:** the push-gate scan placeholder at `maintenance.md:26` (→ Y-7), proved in a scratch repo where a leak added and removed inside the range reads 0 net against 2 in `git log -p`.
- **Aspirational:** the "re-gated immediately before the push" claim has no output (X-16).
- **D17.4:** the gate is GREEN and RED reachable, but blind on concatenated / space-of-stoplisted / staged blob / metacharacter. The negation-seed rule discriminates. EPA rules/guard `Copied` can NEVER go RED (Y-8). `SKIPPED` is unreachable after Step 2.8 (X-2).
- **D17.5:** generated receipts, verifier rounds and the post-commit re-gate are all author-spawned or unpersisted, so none is a gate.
- **D17.6:**

  | Batch | Range | Shipped | Apparatus | of which commands | Shipped share |
  |---|---|---|---|---|---|
  | this | 700a382~1..8f190b9 | 0 | 208 | 39 (EPA 13) | 0% — ≈33% of the fix is contract delivery by effect |
  | Run 2's batch | ecace7a~1..12afa4a | 143 | 223 | 35 | 39% |
  | feature | e5613f4..e82176f | 294 | 14 | 12 | 95% |
  | Runs 8-11 | — | — | — | — | 0.2%–1.4% |

  The split misclassifies delivery commands (Y-16). **Judgement: adequate for this batch** (rate 10/13 → 2/6 by Agent 6's own verdicts, 0 introduced by its reading), **inadequate as a process** (the control that helped has no home, no persisted output, no coverage rule).

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions and Part 1 (6 of 6 applied IDs verdicted) evaluated; 16 new findings (0 HIGH, 4 MEDIUM, 12 LOW) + 36 carried.
**defect series:** row 13 appended to the one table in `.claude/commands/audit.md` — 5 of 6; no other surface touched.
**Application status:** `PARTIAL — 17 of 55 applied (16 in s6b4baa8, W-20 in s30c3d6e)`

## Post-change checklist receipts (Run 3 batch — `s6b4baa8` · `s30c3d6e` · `sd156dc4`)

Applies Y-1, Y-2, Y-3, Y-4, Y-5, Y-6, Y-7, Y-10, Y-11, Y-13, W-16, W-20, W-30, X-14 of this file, plus Y-17, Y-18, Y-19 filed by this session. Owner scope decision (2026-09-15): the Recommended Fixes quick fixes plus the isolation-gate redesign; everything else stays open.
Every `$` line in the generated keys below was EXECUTED by a script and its stdout pasted; the generator is `gen-receipts.sh` in this session's scratchpad and is NOT persisted (Y-15, open). The per-fix TABLE and the judgement keys are typed by the author; every command they quote is re-runnable.
Before commit, two independent verifier rounds received ONLY the staged diff (never the finding texts) and chose their own cases — the falsifier Run 3's prediction named. Round 1 found 7 input classes, round 2 found 8 (Y-17, Y-18); running the gate itself found Y-19. All were fixed before `6b4baa8` and each is now a selftest case.

**Not applied — 38 open, deferred by the owner's scope decision:** Y-8, Y-9, Y-12, Y-14, Y-15, Y-16 · X-2, X-3, X-4, X-5, X-7, X-8, X-9, X-10, X-11, X-12, X-13, X-15, X-16, X-17, X-18, X-19, X-23 · W-14, W-15, W-17, W-18, W-19, W-21, W-22, W-23, W-24, W-25, W-26, W-27, W-28, W-29, W-31.

**push decay:** origin/main unchanged (c4595dd); unpushed at receipt time: 6b4baa8, 30c3d6e, d156dc4
    $ git status -sb | head -1; git log -1 --format=%h origin/main
    ## main...origin/main [ahead 3]
    c4595dd

**inventory sweep:** 7 surfaces checked (CLAUDE.md, README.md, docs/modules/README.md, docs/agentic_engineering_framework.md, examples/README.md, maintenance.md authorized-ops, the command twin), 2 stale claims fixed (the CLAUDE.md and README.md trees gained .gitattributes and .claude/scripts/); the script is mother-repo only, so the twin installs nothing
    $ git show --name-status --format= 6b4baa8 30c3d6e d156dc4 | grep -E "^[ADR]"; grep -c "d16-gate\|gitattributes" CLAUDE.md README.md docs/modules/README.md docs/agentic_engineering_framework.md examples/README.md
    A	.claude/scripts/d16-gate.sh
    A	.gitattributes
    CLAUDE.md:2
    README.md:2
    docs/modules/README.md:0
    docs/agentic_engineering_framework.md:0
    examples/README.md:0

**instruction style:** unit: a diff line carrying a CAPS imperative; range 6b4baa8~1..d156dc4
    $ D='git diff 6b4baa8~1..d156dc4 -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    18 added, 6 rewritten, 0 buried

**references:** 3 of 3 cited sections resolved (component-design §5, D16.2, D16.2c — the two resolution counts follow); unit: a distinct § / D-item / .md path the batch wrote; the regex stays blind to quoted-heading and inline-path citations (W-22, open)
    $ git diff 6b4baa8~1..d156dc4 | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l; grep -c '^## 5\. Content Modification' .claude/rules/component-design.md; grep -cE '^  D16\.2c?\. ' .claude/commands/audit.md
    5
    1
    2

**fences:** 12 of 12 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE "^\`{3,5}[a-z]*" "$t"); c=${f%%[a-z]*}; sed -n "/^$f$/,/^$c$/p" "$t" | wc -l; done | awk "{n++; if(\$1>0)k++} END{print k\" of \"n\" non-empty\"}"
    12 of 12 non-empty

**isolation:** D16 log over the whole batch, 0 hits
    $ bash .claude/scripts/d16-gate.sh log 6b4baa8~1..d156dc4
    D16 log (patches + messages + paths): 3 commits scanned, 0 hits

**back-sweep:** 1 rule (the isolation gate has ONE definition; no command restates its pipeline) — 1 artifact of 1 found (maintenance.md, 2 pipeline lines at 6b4baa8~1) fixed, re-run clean
    $ git show 6b4baa8~1:.claude/commands/maintenance.md | grep -c "paste -sd'|'"; grep -rl "paste -sd'|'" .claude/commands docs CLAUDE.md README.md | wc -l
    2
    0

**liveness:** skipped — no frontmatter touched
    $ git diff 6b4baa8~1..d156dc4 | grep -cE '^[-+](name|description):'
    0

**negation proof:** the gate's own selftest reads RED on every seeded form and GREEN on both controls; the pre-batch gate read 0 on the Y-1, Y-2, Y-3 (x2) and Y-4 seeds (measured this session in a scratch repo)
    $ bash .claude/scripts/d16-gate.sh selftest 2>/dev/null | tail -1
    selftest: 95 of 95 cases as expected

**version:** v2.23.1 applied to 6 of 6 stamps across the 4 canonical surfaces; old-label residue 1 — the line below is a LINEAGE sentence ("v2.23.0 shipped continuous mode"), not a stamp
    $ grep -rn "v2\.23\.1" README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | wc -l; grep -rn "v2\.23\.0" README.md docs/ .claude/ CLAUDE.md | grep -v "created: framework-v" | cut -d: -f1,2
    6
    .claude/commands/maintenance.md:970

**control back-sweep:** 2 of 66 controls re-run with negation (the isolation gate via its 95-case selftest; the push-gate snippet, parsed on every output shape by verifier round 2); the other 64 were NOT re-run; 0 defects left in the 2
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md; do inf=$(awk "/^\`\`\`/{g=!g;next} g" "$f" | grep -cE "^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )"); inl=$(grep -ohE "\`(grep|git|diff|awk|sed|node|python|ls) [^\`]{4,}\`" "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 5 fenced + 26 inline = 31
    .claude/commands/audit.md: 7 fenced + 28 inline = 35

**applied-proof:** 17 of 17 IDs resolve to a commit touching their line (form A: 17, form B: 0), 0 forced to open; each anchor is the PRE-batch text of the finding's line (Y-17..Y-19: text the fix introduced into the new file)
    $ for x in 'Y-1|.claude/commands/maintenance.md|n() { git -c core.quotepath=off diff --cached --name-only -z' 'Y-2|.claude/commands/maintenance.md|# NUL-SEPARATED, UNQUOTED PATHS.' 'Y-3|.claude/commands/audit.md|hyphen/underscore forms, with and without suffixes' 'Y-4|.claude/commands/audit.md|directory NAME is a hit' 'Y-5|.claude/commands/maintenance.md|   # P = the PARTS (split on' 'Y-6|.claude/commands/maintenance.md|ALWAYS seed ALL THREE forms' 'Y-7|.claude/commands/maintenance.md|hits=$(<the D16 scan>)' 'Y-10|.claude/commands/existing_project_adaptation.md|# THREE DIRECTORIES THIS COMMAND WRITES INTO AND NEVER CREATED' 'Y-11|.claude/commands/maintenance.md|`**isolation:** N files scanned, 0 hits`' 'Y-13|.claude/commands/audit.md|**No trend** (27%' 'W-16|.claude/commands/bootstrap.md|is extracted unconditionally above' 'W-20|.claude/commands/maintenance.md|audit.md 7 + 28 = 35; TOTAL 62.' 'W-30|examples/README.md|The session0 prompt copies' 'X-14|docs/modules/skills/autonomous-loop/SKILL.md|**If NEITHER happened**' 'Y-17|.claude/scripts/d16-gate.sh|i18n.logOutputEncoding (round 1)' 'Y-18|.claude/scripts/d16-gate.sh|an annotated tag message, a hidden folder (round 2)' 'Y-19|.claude/scripts/d16-gate.sh|GNU grep 3.0 (Git for Windows) ABORTS'; do id=${x%%|*}; r=${x#*|}; f=${r%%|*}; a=${r#*|}; echo "$id $(git log -S"$a" --format=%h 6b4baa8~1..d156dc4 -- "$f" | tr '\n' ' ')"; done
    Y-1 6b4baa8 
    Y-2 6b4baa8 
    Y-3 6b4baa8 
    Y-4 6b4baa8 
    Y-5 6b4baa8 
    Y-6 6b4baa8 
    Y-7 6b4baa8 
    Y-10 6b4baa8 
    Y-11 6b4baa8 
    Y-13 6b4baa8 
    W-16 6b4baa8 
    W-20 6b4baa8 
    W-30 6b4baa8 
    X-14 6b4baa8 
    Y-17 6b4baa8 
    Y-18 6b4baa8 
    Y-19 6b4baa8 

**gates:** twin parity PASS | receipt-rows-vs-diff 17/17, each row's file in its own commit's list below | row-count 17 IDs vs 17 table findings | slots 0/0 (no report key added)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis; echo "twin rc=$?"; for h in 6b4baa8 30c3d6e d156dc4; do echo "$h: $(git show --name-only --format= $h | tr '\n' ' ')"; done; git diff 6b4baa8~1..d156dc4 -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:' | wc -l
    twin rc=0
    6b4baa8: .claude/commands/audit.md .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md .claude/commands/maintenance.md .claude/scripts/d16-gate.sh .gitattributes CLAUDE.md README.md docs/modules/skills/autonomous-loop/SKILL.md docs/modules/templates/claude_md.md examples/README.md 
    30c3d6e: .claude/commands/maintenance.md 
    d156dc4: docs/agentic_engineering_framework.md 
    0

**placeholder:** none
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-14.md | wc -l
    0

**class sweep:** 17 fixes × 4 directions, 5 extra instances found and fixed (4 stale `session0` references, in d156dc4; 1 stale control baseline written by this batch, in 30c3d6e), 0 left open
    $ grep -rni 'session0' examples docs .claude/commands README.md CLAUDE.md | wc -l; grep -rln 'xargs -0 -r grep' .claude/commands docs CLAUDE.md README.md | wc -l; grep -rn 'THREE DIRECTORIES\|unconditionally above\|NEITHER happened' .claude/commands docs | wc -l
    0
    0
    0

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| Y-1 | s6b4baa8 | .claude/commands/maintenance.md | ✓ `grep -rln 'xargs -0 -r grep' .claude/commands docs CLAUDE.md README.md` → 0 | ✓ `grep -n 'd16-gate.sh log --all' .claude/commands/audit.md` → the audit twin cites the same script | ✓ the push gate above item 3 runs the same script | ✓ selftest case "leak under a path starting -" | none |
| Y-2 | s6b4baa8 | .claude/commands/maintenance.md | same edit as Y-1 | same edit as Y-1 | ✓ `grep -c 'cat-file --batch' .claude/scripts/d16-gate.sh` — `staged` reads index blobs | ✓ selftest case "staged then cleaned on disk" | none |
| Y-3 | s6b4baa8 | .claude/commands/audit.md | ✓ `grep -n 'hyphen/underscore' .claude/commands/*.md` → only the rewritten D16.1 | ✓ maintenance.md item 3 points at the same script | ✓ D16.3 and D16.3c in the same block | ✓ selftest cases "part glued by _", "part in CamelCase", "all-caps part in CamelCase" | CamelCase and all-caps FOLDER names (Y-18) — fixed |
| Y-4 | s6b4baa8 | .claude/commands/audit.md | ✓ `grep -c 'count_file "$T/paths"' .claude/scripts/d16-gate.sh` → every scope counts paths | same edit as Y-3 | ✓ D16.3b (memory) is scanned by `dir` | ✓ selftest case "full name in a PATH only" | none |
| Y-5 | s6b4baa8 | .claude/commands/maintenance.md | ✓ `grep -c 'sed "$ESC"' .claude/scripts/d16-gate.sh` → folder names and parts both escaped | same edit as Y-1 | ✓ a missing `projects/` fails closed in `build` | ✓ verifier round 2: names with `] } ( ) ' + $` — a leak hits, clean text reads 0 | non-ASCII names now fail closed (Y-18) |
| Y-6 | s6b4baa8 | .claude/commands/maintenance.md | ✓ `grep -n 'hand-picked seed' .claude/commands/maintenance.md` → the only seed rule left | n/a — /audit carries no seed list | ✓ the new line "ALWAYS ADD a selftest case" | ✓ `selftest` prints `K of K`, the slot the rule cites | none |
| Y-7 | s6b4baa8 | .claude/commands/maintenance.md | ✓ `grep -rn '<the D16 scan>' .claude/commands` → 1, the quotation in the new rationale | ✓ audit.md D16.3c: `log --all` | ✓ the `=` vs `-eq` compare, now on its own line | ✓ the `push:` key below runs the same `log` scope | author e-mail and tag objects (Y-18) — fixed |
| Y-10 | s6b4baa8 | .claude/commands/existing_project_adaptation.md | ✓ `grep -rn 'THREE DIRECTORIES' .claude/commands` → 0 | ✓ `grep -n 'DIRECTORIES' .claude/commands/bootstrap.md` → no count claim | ✗ the unquoted `$ARGUMENTS` in the mkdir below it is Y-8 — deferred by the owner's scope | n/a — a comment | none |
| Y-11 | s6b4baa8 | .claude/commands/maintenance.md | ✓ `grep -rn 'isolation:\*\* N files scanned' .claude/commands` → 0 | n/a — /audit has no `isolation:` key | ✓ item 3's fence sentence and Upstream intake step 6 now name the gate | ✓ this section's `isolation:` is discharged with the `log` form | none |
| Y-13 | s6b4baa8 | .claude/commands/audit.md | ✓ `grep -rnE '\(27%, 54%' .claude CLAUDE.md README.md docs` → 0 | ✓ CLAUDE.md cites the table without figures | ✓ Phase 3 item 6's check still anchors on `^\*\*No trend` | n/a | none |
| W-16 | s6b4baa8 | .claude/commands/bootstrap.md | ✓ `grep -rn 'unconditionally above' .claude/commands docs` → 0 | ✓ `grep -n 'unconditionally' .claude/commands/existing_project_adaptation.md` → no such claim | ✓ the fence it heads | n/a | none |
| W-20 | s30c3d6e | .claude/commands/maintenance.md | ✓ `grep -rn 'TOTAL 6[0-9]' .claude/commands CLAUDE.md` → 1, the baseline itself | n/a | ✓ re-measured at the final tip: 5 + 26 = 31 | ✓ `control back-sweep:` above prints the same figures | this batch's own first write (65, in 6b4baa8) was stale — fixed in 30c3d6e |
| W-30 | s6b4baa8 | examples/README.md | ✓ `grep -rni 'session0' examples docs .claude/commands README.md CLAUDE.md` → 4 after 6b4baa8, 0 after d156dc4 | ✓ the concepts doc — fixed in d156dc4 | ✓ the Step 12.5 paragraph below already names the command | n/a | 4 in docs/agentic_engineering_framework.md — fixed in d156dc4 |
| X-14 | s6b4baa8 | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -rn 'NEITHER happened' docs .claude` → 0 | n/a — autonomous-loop has no `.claude/skills/` twin | ✓ the four-path table above it | n/a | none |
| Y-17 | s6b4baa8 | .claude/scripts/d16-gate.sh | ✓ `grep -c 'git grep' .claude/scripts/d16-gate.sh` inspected — only the comment explaining why it is not used | n/a — one definition | ✓ the header lists the seven classes | ✓ one selftest case per class | none |
| Y-18 | s6b4baa8 | .claude/scripts/d16-gate.sh | ✓ `grep -c 'GIT_NO_REPLACE_OBJECTS=1' .claude/scripts/d16-gate.sh` → 1 | n/a — one definition | ✓ the KNOWN LIMITS block names what still fails closed | ✓ one selftest case per fixed class, including a synthetic mother repo | short names and sparse index — declared, fail closed |
| Y-19 | s6b4baa8 | .claude/scripts/d16-gate.sh | ✓ `grep -c 'grep -a.* -i ' .claude/scripts/d16-gate.sh` → 0, no `-i` in any scan | n/a | ✓ the FOLD comment | ✓ all 95 selftest cases run under LC_ALL=C | none |

**classification:** 3 add (d16-gate.sh, .gitattributes, the two tree lines), 13 substitute (the Y-1..Y-7 and Y-11 prose and fence, Y-3/Y-4 D16 text, Y-10, Y-13, W-16, W-20, W-30 and its 4 lateral lines, X-14 — each strictly more correct, no information lost; Y-13 keeps its rationale and drops only a copied figure list), 0 delete, 1 relocate (the isolation pipeline moved from maintenance.md item 3 into `.claude/scripts/d16-gate.sh`; the source POINTS and restates nothing — `grep -rl "paste -sd'|'" .claude/commands` returns 0)
    $ n/a — judgement, not measurement

**new component:** `.claude/scripts/d16-gate.sh` — placed in this repo's own `.claude/scripts/` only (the framework needs it at runtime and no project receives it, so no `docs/modules/` copy), invoker `/maintenance` item 3 + its push gate + `/audit` D16.1/D16.3c, liveness n/a — a script with no frontmatter; its own selftest reads 95 of 95
    $ n/a — judgement, not measurement

**report deletions:** none — 0 structural decreases across 1 report file
    $ for f in $(git diff --cached --name-only -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:'; do b=$(git show HEAD:"$f" 2>/dev/null | grep -cE "$pat"); if [ -f "$f" ]; then a=$(grep -cE "$pat" "$f"); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat"; done; done | wc -l
    0

**report restore:** none
    $ echo "n/a — no destroyed report"
    n/a — no destroyed report

**commit correction:** none
    $ echo "n/a — no false claim in a made commit"
    n/a — no false claim in a made commit

**audit:** N/A — PATCH bump (v2.23.1); trigger (b) does not apply
    $ echo "n/a — PATCH"
    n/a — PATCH

**verification audit:** proposed — trigger (d), in this session's closing report
    $ echo "n/a — owner decision"
    n/a — owner decision

**defect series:** N/A — maintenance session, no row
    $ echo "n/a — maintenance session"
    n/a — maintenance session

**push:** not requested for this batch — the owner's push approval this session covered `c4595dd` only; D16 over the unpushed range at receipt time is GREEN
    $ bash .claude/scripts/d16-gate.sh log origin/main..HEAD
    D16 log (patches + messages + paths): 3 commits scanned, 0 hits
