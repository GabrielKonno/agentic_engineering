# Framework Audit Report — 2026-09-14

**Date:** 2026-09-14
**Framework version:** v2.23.0 (README.md:1)
**Run mode:** `baseline` — trigger (b), before the MINOR bump to v2.23.0. The bump was already
committed (d812c07) and is UNPUSHED; the owner chose "commit + bump now, /audit before any push",
so this run is in practice pre-PUSH, not pre-bump (see W-26).
**Dimensions checked:** 17
**Agents dispatched:** 6
**Batch in scope of the push gate:** `origin/main..HEAD` = d812c07, 1c3bc46, e82176f
**ID series:** `W`. `V` was skipped: `audit-2026-09-10.md` already uses `### V-A`…`### V-E` as
section labels, and a `V-` ledger series would collide with every grep over them.

**carried: 0 open + 0 escalated from audit-2026-09-10.md** (98 ledger rows; 0 `open`, 0 `escalated`).
**OBSERVATIONS — accepted-risk, not re-opened:** E-1 (`audit-2026-09-02.md` record), M-44
(`audit-2026-09-02.md`), N-53 (`audit-2026-09-03.md`), T-11 and T-36 (`audit-2026-09-09.md`), U-48
(`audit-2026-09-10.md`). Agent 1 re-measured all six. None was found again in the working tree,
agent memory or the 182 reachable commits. U-48's orphan object is unreachable from any ref, which
puts it out of D16.3c scope by that record's own standing instruction.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` is the only runtime skill and exists in both places |
| C | Instruction style | Quality | 5 | FAIL | 11 instances; 4 are in the new Continuous-mode text (W-10), 7 predate it (W-29) |
| E | Version consistency | Currency | 5 | PASS | v2.23.0 on every current-version surface; no `created:` field was rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15 |
| D5 | Bootstrap file refs | References | 2 | FAIL | 56 of 57 resolve; the risk-profile check greps the wrong project.md path (W-13) |
| D6 | Gap vocabulary | References | 3 | PASS | 10 gaps, 3 of 3 declarers, 0 broken, 0 orphaned; D6.7 wording drift (W-24) |
| D7 | Pushy Description | Quality | 3 | PASS | 30 agents; both twins print `10 verified, 0 broken, 17 info`; 14 of 14 regressions behave as expected |
| D8 | Orchestrator refs | Process | 4 | PASS | every name and citation resolves; 2 stale counts in the batch (W-8, W-9) |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | no mismatches |
| D10 | Command → skill paths | References | 4 | FAIL | references resolve; 3 check defects (W-12, W-20, W-11) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | step count is consistent (25 + Setup); 4 forward references are not flagged (W-14..W-17) |
| D12 | Examples conventions | Currency | 5 | PASS | 20/9/11 checked; no violations |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | nothing bootstrap reads is missing from the PRD template |
| D14 | Bootstrap output | Process | 2 | FAIL | 15 of 15 template refs traced; the Step 15 report lacks slots (W-18); EPA has slots no step fills (W-19) |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 numeric errors; 1 stale term (W-30) |
| D16 | Project-info isolation | Privacy | 1 | PASS | 3 sources (12 + 16 + 112); 0 hits; **push gate over origin/main..HEAD: GREEN** |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | the batch promises behaviour nothing owns; 2 checklist checks cannot see the defects that recurred |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| W-1 | HIGH | `docs/modules/skills/pendencias-updater/SKILL.md:45` | pendencias-updater writes every task discovered during a session (session-end:67 hands discoveries to it) but has no `origin: discovered` mandate. Continuous mode therefore reads those tasks as `owner` (admitted with no cap), and the discovery brake under-counts. The d812c07 back-sweep missed it because its "enumerating grep" was a hand-typed 5-file `$W` list, which cannot find a writer outside the list. skill-gate:74/83/84/129 also register pendencies untagged, and it is not stated whether they are held. | applied `secace7a` |
| W-2 | HIGH | `.claude/commands/existing_project_adaptation.md:546-553`; `docs/modules/skills/autonomous-loop/SKILL.md` C3 | Existing projects cannot receive the v2.23.0 contract. EPA Step 2.9 copies a skill only if it is missing ("SKIPPED (already exists)"), and no version-delta list exists. Partial refreshes fail unsafe: untagged = `owner` = admitted uncapped, so a project that hand-refreshes only autonomous-loop, without the tagging writers, admits every AI-filed task. d812c07 declared a MINOR with no migration statement. | applied `secace7a` |
| W-3 | MEDIUM | `autonomous-loop/SKILL.md` C5 (:519, :528), C6; `sprint-proposer/SKILL.md` Step 1 | "END THE RECURRING TRIGGER (for `/loop`: stop the loop)" names no mechanism: a self-paced loop needs `stop: true`, a fixed-interval loop needs `CronDelete`, and loops are session-scoped. A revocation in another session removes the marker but cannot end the loop, and each later firing falls through to a full sprint proposal (Step 1 finds no marker, and no branch ends the trigger). The C6 idle claim "approval stands … next re-entry picks up" lapses silently when the session closes. | applied `secace7a` |
| W-4 | MEDIUM | `autonomous-loop/SKILL.md` C4 (:506) | The `Window: closed N` check expects "previous boundary + 1", but the previous value is not persisted: the report lives only in the transcript, and the marker is overwritten in the same closure. After an autocompact or re-entry, the cases closure condition 3 exists for, the check cannot go RED. A disk anchor exists: `git show HEAD~1:.claude/phases/project.md` (the tree must be clean at the boundary). | applied `secace7a` |
| W-5 | MEDIUM | `autonomous-loop/SKILL.md` C5 (:518 vs :528) | "Every stop … ALWAYS does all four: write `**State:**` …" contradicts the Revocation bullet, which removes the marker, so there is no State field left to write. | applied `secace7a` |
| W-6 | LOW | `autonomous-loop/SKILL.md` C5 (:531) / C6 | The window-counter reset on "continue" is stated only in C5. It is absent from C6's re-entry sequence, which is the path the owner's digest answer arrives through. | applied `secace7a` |
| W-7 | LOW | `autonomous-loop/SKILL.md` :534, :550 (and segment :151-152, :580); `sprint-proposer/SKILL.md` Step 0 | Audit-cadence equivalence (`ceil(closed/3)`, and "one per phase" in segment mode) has no reader: sprint-proposer Step 0 counts Progress Log rows, and C6's per-idle `/session-end` writes rows of its own. Segment mode already had the gap; the batch extends it. | applied `secace7a` |
| W-8 | LOW | `autonomous-loop/SKILL.md:46` | "Two legitimate paths in:" sits above a table that has 4 rows since d812c07. | applied `secace7a` |
| W-9 | LOW | `sprint-proposer/SKILL.md:268` | "a mode whose ~150 lines" is stale (autonomous-loop is 593 lines). | applied `secace7a` |
| W-10 | LOW | `autonomous-loop/SKILL.md` :562-564, :531-532, :534-536, :470 | Behavioural rules in the new text with no CAPS verb or no dedicated line: C6 pacing, C5 counter reset, C5 cadence computation, C3 held-chain rule. | applied `secace7a` |
| W-11 | LOW | commit bodies d812c07, 1c3bc46, e82176f | Receipt evidence form. `class sweep:` has no `$` line in d812c07 or 1c3bc46 (RED under maintenance.md:232). 1c3bc46's `instruction style:` and `inventory sweep:` stdout slots are parentheticals (T-24 form). e82176f carries a duplicate `commit correction:` key and 0 of the other keys. | applied `secace7a` |
| W-12 | MEDIUM | `.claude/commands/maintenance.md:435-438` | The isolation command's comment says "WORD BOUNDARIES ARE MANDATORY", but the command is `grep -oniE "($P)"`, with no `-w` or `\b`. Run as written over tracked files it returns 3 hits in 3 files; with `-w` it returns 0. The push gate therefore fires on a healthy state. e238330's subject claims a fix that did not land. | applied `secace7a` |
| W-13 | MEDIUM | `.claude/commands/bootstrap.md:153` | The risk-profile receipt check greps `projects/$ARGUMENTS/project.md`, but Step 3 creates `.claude/phases/project.md` (:227, :234). "Expected: `1` for BOTH files" cannot pass on a correct bootstrap. | applied `secace7a` |
| W-14 | MEDIUM | `.claude/commands/bootstrap.md:455-457` | Step 5.8 (production-financial) says to note in `code-reviewer.md`, which Step 7 creates. Step 7 never picks the note up, and its "if it already exists: do NOT overwrite" branch would skip the template if the note left a stub. | open |
| W-15 | LOW | `.claude/commands/bootstrap.md:145-156` | Step 1.2 says "Steps 2 and 3 ALWAYS confirm receipt", but neither receiver mentions the risk profile. The mandate lives only in the sender (component-design §9). | open |
| W-16 | LOW | `.claude/commands/bootstrap.md:362-363` | The comment says `evolution-policy.md` "is extracted unconditionally above"; the extraction is below (:403). | open |
| W-17 | LOW | `.claude/commands/existing_project_adaptation.md:330-355` | EPA Step 2.2 writes a "retroactive PRD created" Progress row and a session log before Phase 3 creates the PRD, and into `.claude/logs/` before Step 4.2 creates it. Neither is flagged. | open |
| W-18 | LOW | `.claude/commands/bootstrap.md` Step 15 | The final report has no slot for `.claude/docs/` (Step 5.7), for the Step 12.5a "kept gap, no example → pendencias" outcome, or for the Step 1.2 grep result. Two slot enumerations are paraphrased rather than copied (Step 1.1, Step 13). | open |
| W-19 | LOW | `.claude/commands/existing_project_adaptation.md` Step 5.2 | The report offers MCP `none installed — placeholder replaced` and a Hooks slot, but Steps 4.2 and 4.3 never require replacing those placeholders, so no step produces those verdicts. | open |
| W-20 | LOW | `.claude/commands/maintenance.md:757-758` | The control baseline records "4 fenced + 23 inline = 27 … TOTAL 62"; today it returns 5 + 23 = 28, TOTAL 63 (the rise came at 512f07b). | open |
| W-21 | LOW | `.claude/commands/maintenance.md:236`, :177 | The elision grep catches only a LEADING `<` on a `$` line. It returned 0 on d812c07 while two mid-line angle-bracket `$` lines existed (a looser form returns 2). The "stdout must be literal, never a parenthetical" rule has no mechanical check. | open |
| W-22 | LOW | `.claude/commands/maintenance.md:420` | The references denominator regex cannot see `→ "Heading"` citations, the class d812c07 added (5 distinct), so `references:` under-counts its own stated unit. | open |
| W-23 | LOW | `.claude/commands/audit.md:243-246` | The copy-path derivation returns 29 lines against a prose list of exactly 5; an "at least 5" threshold cannot realistically go RED. | open |
| W-24 | LOW | `.claude/commands/audit.md` D6.7 | "diff the two install BLOCKS — table AND prose — expected: empty" no longer fits the commands, which scope parity to the marked TWINNED SPAN (U-24). The whole-block diff is structurally non-empty (rc=1), while the TWINNED SPAN diff and the loops are identical. | open |
| W-25 | LOW | `.claude/commands/maintenance.md` Audit intake item 8, :537 | A correction-only follow-up commit has no stated receipt form. Its three executions (d41022b, b9d2ee2, e82176f) used three forms. Item 7's exemption regex covers bumps and substitutions, not corrections. | open |
| W-26 | LOW | `.claude/commands/maintenance.md` "Version bumps"; CLAUDE.md trigger (b) | "PROPOSE /audit BEFORE a MINOR bump" only proposes and nothing gates it. d812c07 applied the bump before the audit. Whether an unpushed bump counts as "not yet a bump" is undocumented. | open |
| W-27 | LOW | `docs/modules/skills/session-end/SKILL.md:112` vs :125 | "Items 1-3 are the critical minimum" vs "at minimum items 2, 3, and 6". | open |
| W-28 | LOW | `docs/modules/agents/code_reviewer.md:41`; `validation-orchestrator/SKILL.md:119` | The reviewer schema field is `### Coverage gaps declared:`, while the orchestrator's Pass 1 tells the reader to find "the Coverage Gap Declaration section". | open |
| W-29 | LOW | session-end:125; sprint-proposer:199-203, :214, :240; validation-orchestrator:104, :145; validator.md:147-148, :164-166; code_reviewer.md:208-210, :264, :291 | Pre-existing instruction-style instances (lowercase or title-case imperatives, fields buried in prose). | open |
| W-30 | LOW | `examples/README.md:69` | "The session0 prompt copies this entire directory" uses a stale term; `/bootstrap` Step 1.5 does the copy. | open |
| W-31 | LOW | `docs/modules/skills/rules-agents-updater/SKILL.md:119` | The vocabulary-alignment check is still a whole-file `grep -l`, the T-9 false-match shape the activation loop was fixed to avoid. | open |

## Recommended Fixes

1. **Quick fixes:** W-8, W-9, W-16, W-20, W-30, W-27.
2. **Structural fixes:**
   - **W-1 and W-2 first.** Both are in the unpushed batch, and both make the new mode fail open.
   - Then W-3, W-4, W-5, W-6, W-12, W-13, W-14, W-17, W-18, W-19, W-28.
3. **Quality improvements:** W-7, W-10, W-11, W-15, W-21, W-22, W-23, W-24, W-25, W-26, W-29, W-31.
4. **ESCALATED — owner decision required:**
   - **Push decision.** The owner authorized the push on the condition that the audit passes. D16 is GREEN, but W-1 and W-2 (HIGH) are defects in the unpushed batch itself. Publishing v2.23.0 now would ship a contract that admits AI-filed tasks uncapped. The owner decides: fix first (recommended) or push as is.
5. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48 (see header).

## Meta-observation

`prediction: 3 clauses evaluated — void, void, held`
- "If a Run 12 remediation batch is run, the not-clean rate will land between 40% and 55%" → **void**. No remediation batch ran.
- "the dominant class will again be receipts whose numbers do not reproduce" → **void**, since its condition did not occur. It was observed anyway at small scale: 1c3bc46's `6 added` did not reproduce (5), caught by the author, not by a second actor.
- "If instead the next session is spent on `docs/modules/` and `examples/`, this prediction is void and that is the better outcome" → **held**. The batch is 95% shipped surface (294 of 308 lines, Agent 6).

**The recurring class this run saw: behaviour PROMISED by new shipped text that no mechanism OWNS, and propagation that no path DELIVERS.** The receipt apparatus did its job on form: the author's re-runs caught a wrong number and two non-literal `$` lines. But not one checklist control measures the things this batch actually got wrong:
- a back-sweep bounded by a hand list (W-1);
- a MINOR contract with no route into existing projects (W-2);
- a stop instruction that names no stop mechanism (W-3);
- a closure check whose red state is unreachable exactly when it matters (W-4).

Every checklist control passed, and every such defect was found only by a reader asking "who does this, and how would it fail?" This is the first batch in six to change the shipped surface meaningfully. Agent 6 measured Runs 8-11 at 0.2-1.4% shipped share, and the defect series tracks newly added normative text. So it is where defects were expected, and it is where they are.

**Prediction, stated so it can fail.** If the next maintenance batch fixes W-1..W-7 with the post-change checklist as its only control, at least one of those fixes will introduce a new owner-less promise or unreachable check in `autonomous-loop`. The checklist inspects the shape of text, not the behaviour it describes. What would falsify this is a behavioural probe: a synthetic project run through a continuous-mode cycle (register a task mid-run, revoke from another session, autocompact between boundaries), in the style of `d27e09f`'s scoped verification. If that probe is run and finds nothing, this prediction fails, and that is the better outcome.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings

### Agent 1: Structural Sync

**Part 1:** N/A — baseline mode.

**[A] Dual Placement — PASS.** `.claude/skills/` has 1 folder (`cross-cutting-analysis`), which also exists in `docs/modules/skills/` (15 folders). It is the only runtime skill, as expected.

**[D4] Skills README vs Disk — PASS.** README lists 15 (rows 1-15, `docs/modules/skills/README.md:45-59`) and disk has 15. Nothing is missing either way. `README.md:8` "15 skills: 12 lifecycle + 3 tier-gated" is correct: 1 + 5 + 3 + 1 + 1 + 1 = 12, plus 3 tier-gated.

**[D9] CLAUDE.md Diagram vs Disk — PASS.** Every entry in the diagram exists: 7 templates, 10 agent templates, 5 rules templates, 6 command files. Counts match: 6 slash commands; 15 process skills (12 + 3); "(9 more)" = 15 − 6 named; examples 20/9/11. The individual files in `.claude/docs/` (3) and `assets/docs/` (11) are covered by directory-level comments.

**[D16] Project-Information Isolation — PASS.**
- **Blocklist, from three sources:**
  - 12 folder-name patterns (3 folders with their variants, word-bounded, stoplisted);
  - 16 doc identifiers. Classes: a person's name, a personal domain, a platform subdomain, a hosting-app domain, a phone number, a personal e-mail, a city, a street address, a client business name, two product-module names, a product title;
  - 112 code identifiers shared between the 335 identifiers harvested from framework-layer surfaces and 6,698 project source files. Copy paths were excluded, and the derivation returned well over 5 paths.
  - The pattern was derived at runtime and never printed.
- **Surfaces scanned:**
  - all tracked files;
  - `.claude/docs/` (3 files);
  - agent memory (10 files including MEMORY.md);
  - git history, 182 commits, contents and messages. Contents were scanned with a per-revision loop.
- **Negation proofs:** a seeded folder name returns 1; a known token over 3 revisions returns 48; a seeded doc identifier returns 1.
- **Hits:** none.
  - The only value-shape match is the generic test fixture `t@t.com` (prd_planning.md:339,341; framework doc :1264,:1273,:1840,:1890), which is legitimate.
  - Memory: 8 commit-hash tokens, all resolved with `git cat-file -e`; 0 unresolved.
  - Range D16.2c: the only shared identifier is `done_tasks`, framework vocabulary.
- **Pushed OBSERVATION, not a finding:** 9 schema- or helper-shaped names in 10 shipped files, all on origin/main and none added in the range. They are standard e-commerce, multi-tenancy and test vocabulary, under the industry-name exemption already noted in `audit-2026-09-04.md:364` and `audit-2026-09-10.md:20`. Re-measured, not re-opened.
- **Push gate — verdict over origin/main..HEAD: GREEN, 0 hits.**
  - Contents: 0 file-hits over 182 revisions, which include the 3 unpushed.
  - Messages: `git log --all --format='%h|%s|%b' | grep -ciwE "(<derived pattern>)"` → 0; the doc-identifier file form → 0.
  - Value shapes in `git diff origin/main..HEAD`: 0. Messages carry only the attribution address.

### Agent 2: Bootstrap Integrity

**Part 1:** N/A — baseline mode.

**[D5] File References — FAIL.**
- 57 paths found, 56 resolved.
- Unresolved: `bootstrap.md:153` (see W-13).
- D5.4 naming: consistent in both twins. The `````js`/`````markdown` fences each appear exactly once in their source.

**[D11] Step Sequencing — FAIL.**
- 15 primary steps + 10 sub-steps (0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5) = 25, or 26 with Setup. This matches the framework doc's "15 primary steps + Setup + 0.5 + sub-steps" and its ASCII diagram.
- Forward references:
  - W-14 (Step 5.8 → Step 7);
  - W-15 (Step 1.2 receiver mandate);
  - W-16 (5.7 comment "above" vs below);
  - W-17 (EPA 2.2 → Phase 3 / Step 4.2).
- Info: the twinned-span marker and loop comment say "EPA Step 5.1", but the criteria span is in 4.6.5. Both loops carry the leftover comment "candidate replacement for …".
- Handled correctly: 5.5→14, Step 2 placeholders (5/6/14), 1.1→3/4/13, EPA 2.6/2.7.1/2.9b, EPA 4.1b.
- The TWINNED SPAN diff is empty.

**[D13] PRD ↔ Bootstrap Compatibility — PASS.**
- Bootstrap reads Cross-cutting Concerns, 1.1-1.3, 2.1-2.3, 3.x, 4, 5 (5.1/5.2/5.3), 6, 7, 9 and the Changelog version. EPA Phase 3 reads the full set.
- Nothing bootstrap reads is missing from the template. Minor wording drift: EPA "5.1 Stack" vs the template's "5.1 Suggested Stack".
- Info: bootstrap ignores 1.4, 8, 7.2/7.3, 3.x acceptance criteria / edge cases / main flow, and header Status.

**[D14] Output Completeness — FAIL.**
- 15 template references, all 15 traced: session-rules (5.7), prd (Setup/Step 2), project.md (3), pendencias (4), `.claude/logs/` (14), six skills (5.7), `.claude/skills/` and `.claude/agents/` (5.7), and the Step 5/Step 14 placeholders.
- D14.4: see W-18. Outside the dimension: W-19.
- The new `origin:` field is compatible with bootstrap Step 4 (untagged = owner, "or at bootstrap").

### Agent 3: Activation Chain

**Part 1:** N/A — baseline mode.

**[D6] Vocabulary Alignment — PASS.**
- **Declarers:**
  - code-reviewer: Accessibility, Performance, Concurrency, Visual regression, Data integrity;
  - security-reviewer: Static analysis, Secrets coverage, Federation protocol, Compliance, Infrastructure security;
  - validator: visual regression (bold form).
- `declarers extracted: 3 of 3` (M derived = 3).
- **Specialist matches:** all 10 exact, each with a bootstrap row and an EPA row:
  - accessibility-checker, performance-auditor, concurrency-tester;
  - visual-regression-tester ("code-reviewer or validator declares");
  - data-integrity-checker, sast-scanner, secrets-scanner, oauth-flow-tester, compliance-auditor, iac-scanner.
- **Install-block diff (D6.7):** table diff empty; TWINNED SPAN diff empty; loops identical (71 lines, `cmp`). The whole-block diff is rc=1 because of step structure only: lead-in wording, `created:` lineage, the bootstrap-only `#### Step 12.5b` heading, and the loop's placement. See W-24.
- **D6.9:** framework doc `:944-950` names 3 declarers, policy §1/§3 names 3 → match. validation-orchestrator (113-129) and rules-agents-updater (117, 127) name 3.
- **D6.8:** sources are {code-reviewer, security-reviewer, validator}. blue_team and red_team use "gap" only in unrelated senses. The §1-§3 twin diff is empty → match.
- Broken links: none. Orphaned specialists: none.
- Info: the install table header says "Gap declaration (in reviewer)", but the validator is not a reviewer. rules-agents-updater:119 uses whole-file `grep -l` (W-31).

**[D7] Pushy Description Compliance — PASS.**
- 30 agents checked: 10 module agents are PARTIAL (sanctioned) except skill-reviewer, which is FULL; all 20 examples are FULL.
- Synthetic project: `…/scratchpad/audit-a3/projects/base`. Repo `git status --porcelain` was 0 lines before and after.
- `activation chains`: bootstrap twin `10 verified, 0 broken, 17 info` (exit 0); EPA twin `10 verified, 0 broken, 17 info` (exit 0).
- `twins agree: yes`, including all 17 INFO strings byte-identical.
- **Regression set, identical in both twins:**
  1. `coverage gap for X` → BROKEN (`10 verified, 1 broken`)
  2. hyphenated → VERIFY (11/0)
  3. slashed → VERIFY
  4. TAB fold → VERIFY
  5. no article → VERIFY
  6. bogus second gap → BROKEN on the second gap only (11/1)
  7. `Declares` → VERIFY
  8. space fold → VERIFY
  9. body only → INFO, no BREAK (10/0/18)
  10. negated body → INFO, no BREAK
  11. `OAuth2 federation gap` → BROKEN (seen)
  12. `Node.js runtime gap` → BROKEN (seen)
  13. validator as sole declarer → VERIFY
  14. all declarers removed → `activation chains: none installed - no declaring component in projects/t14/.claude/agents`, exit 0
- **Planted probe (D7.8):** `BROKEN CHAIN: widget-telemetry-auditor declares 'widget telemetry gap' but no declaring component declares it`, plus `INFO: bespoke-helper has no gap phrase and is not a shipped example - verify it is protocol-spawned`; `10 verified, 1 broken, 18 info`.
- **Invariant (D7.5):** `files: 10 | output: 0`.
- Anti-pattern instances: none.

### Agent 4: Orchestration & Commands

**Part 1:** N/A — baseline mode.

**[D8] Orchestrator References — PASS.**
- **Names:** every skill and agent name in session-end, validation-orchestrator, sprint-proposer, autonomous-loop, skill-gate, codebase-audit and framework-audit resolves.
- **Cross-section citations, all resolved:**
  - "Continuous mode" → C1/C3/C4/C6: sanctioned prefix of `## Continuous mode — …`;
  - "Resource contention", "Task closure", "Continuation across sessions", "Cadence integrity", "Autonomous loop watchdog", "Execution proof": sanctioned prefixes;
  - "Loop guardrails", "Between Tasks", "Session archetypes", "Framework-evolution docs — the upstream lifecycle": sanctioned (parenthetical dropped);
  - "Exception stops", "Final report", "Before Implementing", "In sprint-approved mode", "Signals of exceeding", §4d, §1b, §3a, component-design §5-§9, D8.8 `### Periodic review — the triage VERDICTS`: exact;
  - "MODEL SWITCH entries": sanctioned (ordinal dropped).
- Unresolved: none.
- LOW: W-8, W-9; `autonomous-loop:349` "Bounded re-sequencing" is word-truncated but resolves uniquely (pre-existing, bddb018).

**[D10] Command → Skill Paths — FAIL.**
- All references in prd_planning, prd_change, maintenance and audit resolve.
- D10.4: cross-cutting-analysis is byte-identical in both placements.
- D10.6: CLAUDE.md (a)-(d) = audit.md Phase 0 (a)-(d).
- **Self-checks run verbatim, on HEAD e82176f; staged-diff checks over HEAD~3..HEAD:**
  - push decay (status half; fetch skipped): `## main...origin/main [ahead 3]` ✓
  - Step 0 channel: 2 of 3 without a channel, correctly RED; pending 0
  - receipt keys: d812c07 24/24, 1c3bc46 24/24, e82176f 0/24
  - `$`-line ≥1: **`class sweep` = 0 in d812c07 and 1c3bc46 → RED** (W-11)
  - angle/ellipsis grep 0 (but see W-21)
  - harvest `harvested 14 of 24 registered` ✓
  - instruction style d812c07 37/4/2 ✓; 1c3bc46 5/1/0 (corrected in e82176f), with a parenthetical stdout
  - fences 12 of 12 ✓
  - references 3 and 1 match, but the regex under-counts (W-22)
  - **isolation as written 3 hits in 3 files; with `-w` 0 → fires on healthy state** (W-12)
  - mandated-never-created: no output ✓
  - pipe count `8 8 8` ✓
  - Gate 1 twin parity: no output ✓
  - Gate 4 `admission:` has a slot ✓
  - **control enumeration 28 + 35 = 63 vs baseline 62 → stale** (W-20)
  - control numerator 0 (N/A)
  - liveness `1 componente(s) OK … [modo estrutural — js-yaml ausente]`, exit 0 (degraded mode)
  - raw templates `19 validated of 25 globbed, 6 skipped (fenced), OK` ✓
  - version surfaces ✓; residue 0 ✓
  - report deletions: vacuous
  - accepted-risk: 6 records × 5 fields ✓
  - placeholder: none ✓
  - copy-path derivation: 29 lines vs 5 in prose (W-23)
  - declarer M 3 ✓
  - D7.5 `files: 10 | output: 0` ✓
  - errata-number and ID collisions 0 ✓
  - prior prediction: 1 hit ✓
  - run-mode lines 9 ✓
  - defect-series parity `GREEN 9+2=11` (dropping a row → RED) ✓
- **Not executed:**
  - the `git fetch` half (it writes refs);
  - Gates 2 and 3 (no applied ledger in baseline);
  - RELOCATE (none in range);
  - Phase 0 reconstruction (verification mode only);
  - D16 history greps (Agent 1);
  - D7.6 (Agent 3).

### Agent 5: Document Accuracy

**Part 1:** N/A — baseline mode.

**[C] Instruction Style — FAIL.**
- 11 instances. The 4 in the new text are W-10; the 7 pre-existing are W-29.
- Excluded as the mandated shape: every "`…`. NEVER emit nothing." report line, the C2 admission line, and the C4/C5 blocks.
- Cross-dimension observations: W-27 (session-end minimums), W-5 (revocation vs "all four"), W-28 (coverage-gap section name).

**[E] Version Consistency — PASS.**
- Canonical v2.23.0 at README:1, README:146, audit.md:246, EPA:238/:542 and claude_md.md:4.
- Historical or illustrative strings, correctly left alone: EPA:262/:266 migration history; maintenance.md:836/:925/:927; skills/README:35; autonomous-loop:21/:28/:246; sprint-proposer:268.
- `created:` lineage fields were not reported (E.5). `git diff HEAD~3 HEAD` changed only the six current-version lines and rewrote no `created:` field.

**[D12] Examples Convention Compliance — PASS.**
- Agents 20/20: all frontmatter fields present and `name:` = filename; Input / BOUNDARIES / When invoked / Output Format sections; Input immediately followed by an output heading; verdict line.
- Skills 9/9: folder/SKILL.md, `invocation: inline`, no `last_eval`; all 3 stack skills have Testing and pitfalls sections.
- Rules 11/11: `domain:` / `applies_to:`, no `name:`; checklist claims match README:170.
- Violations: none.

**[D15] Factual Accuracy — PASS.**
- Disk counts:
  - 15 skills (12 + 3), 10 agents, 5 rules, 7 templates, 6 commands, examples 20/9/11;
  - invocation fields 7 `user` / 8 `inline`;
  - 17 dimensions over 6 agents; bootstrap Steps 1-15.
- Every numeric claim checked matches disk:
  - framework.md :45/:47/:48/:49/:50/:346/:213/:296/:362/:175-178/:171-173/:1232;
  - README :21-24/:22-23/:55/:123/:138/:147-156/:162/:182/:218/:399;
  - modules/README:10; skills/README:8-9/:38/:43-59; bootstrap 5.7; CLAUDE.md :19/:36/:68/:141; examples/README :12/:33/:52.
- No banned sense of "inline" found.
- Stale term: W-30.

### Agent 6: Process Coverage (meta)

**Part 1:** N/A — baseline mode.

**[D17] Process Coverage — FINDINGS.**

**Commits classified: 40 commits, 9 operation types.**
- audit-fix group application: 17
- standalone control/pattern corrections: 7
- audit report persist: 4
- audit write-back: 4
- commit/receipt correction follow-ups: 3 (d41022b, b9d2ee2, e82176f)
- owner-requested shipped-feature evolution: 2 (d812c07, 1c3bc46)
- placeholder substitution: 1
- version-bump chore: 1
- scoped verification record: 1 (d27e09f)

**Undocumented executed flows:** W-25 (correction-commit receipt form, 3 executions with 3 forms). The scoped verification (d27e09f) ran once, which is below the threshold; it is noted as the only behavioural probe in 40 commits.

**Referenced-but-unowned conventions:**
- Template → mother-repo promises: none. The evolution_policy upstream lifecycle is owned by maintenance.md Step 0 and Upstream intake, and the channel was created in 512f07b.
- Continuous-mode promises with no owner: W-3 (stopping the trigger, revoking from another session, idle lapsing), W-6 (counter reset missing from C6), W-2 (existing projects cannot receive the MINOR; fail-open default), W-7 (cadence equivalence has no reader).
- Back-sweep gap: W-1.

**Aspirational mechanisms:** W-26 (trigger (b) is proposed, not gated). CLAUDE.md:214 and :162 are owned; README :176/:344/:381 are project-level.

**D17.4 provability:**

| Check | Can go RED? | Can go GREEN? | Verdict |
|---|---|---|---|
| Elision grep | Only for a leading `<` | Yes | Blind to the shape that recurred (W-21) |
| Literal-stdout rule | No check exists | n/a | Rule without a mechanical check (W-21) |
| Receipt key loop | Yes | No, on a correction commit | Fires on a healthy but unexempted class (W-25) |
| Back-sweep via `$W` | Cannot find writers outside the list | Yes | Decoration for completeness (W-1) |
| C4 `Mode` line | Yes (negation-proved) | Yes | Provable |
| C4 `Window` +1 | Only in-context | Yes | Red unreachable where it matters (W-4) |
| `Last closed` | No check | n/a | Decoration |
| sprint-proposer marker count | Yes | Yes | Provable |
| Version residue | Yes | Yes | Provable |

**D17.5 self-reported controls:**

| Control | Verified by whom | Output persisted where | Gate? |
|---|---|---|---|
| All 24 keys | Author only, until this audit | Commit bodies | No |
| Independent re-runs | This agent | This report | Reproduced: 37/4/2; 5/1/0; references 3; `origin: discovered` = 1 × 5; residue 0; 24 keys. Not reproducible: 1c3bc46 `inventory sweep:` stdout |
| Twin parity | Author (mechanical) | Commit body | Closest to a gate |
| `audit:` trigger (b) | Owner decision | Commit body | No; proposed, then bumped |
| Project-side continuous closure outputs and `admission:` | The orchestrator itself | Transcript only; digest keeps only "N boundaries, all OK" | No |

**D17.6 convergence:**

| Batch | Range | Shipped (docs/modules + examples) | Apparatus | Shipped share |
|---|---|---|---|---|
| B0 (this) | e5613f4..HEAD (3) | 294 | 14 | 95% |
| B1 | d27e09f..e5613f4 (4) | 2 | 152 | 1.3% |
| B2, Run 11 | 7c1ec64..d27e09f (10) | 11 | 1058 | 1.0% |
| B3, Run 10 | 84f094d..7c1ec64 (8) | 4 | 1795 | 0.2% |
| B4, Run 9 | fd3d027..84f094d (5) | 4 | 1261 | 0.3% |
| B5, Run 8 | 2ba16ee..fd3d027 (9) | 12 | 856 | 1.4% |

Judgement:
- Across B1-B5, shipped delta fell to near zero while the defect rate did not fall. The loop returned about 33 shipped lines for about 5,100 apparatus lines.
- This batch reverses the split, but through owner-directed work, not through the loop being re-pointed.
- The controls are adequate for receipt FORM and inadequate for shipped-surface SEMANTICS. The loop should be re-pointed at shipped behaviour, and a documented behavioural verification step is the natural vehicle.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; 31 findings (2 HIGH, 6 MEDIUM, 23 LOW).
**defect series:** `N/A — baseline mode`
*(superseded)* **Application status:** `PENDING`
**Application status:** `PARTIAL — 13 of 31 applied in secace7a`

## Post-change checklist receipts (`secace7a`)

Applies W-1..W-13 of this report. Receipts GENERATED by a script that runs each command and pastes its stdout — no figure below was typed.

**push decay:** origin/main unchanged (e5613f4)
    $ git status -sb | head -1; git log -1 --format=%h origin/main
    ## main...origin/main [ahead 5]
    e5613f4

**inventory sweep:** N/A — no artifact added, removed, renamed or split in ecace7a
    $ git show --name-status --format= ecace7a | grep -cE '^[ADR]'
    0

**instruction style:** 36 added, 8 rewritten, 2 buried — unit: a diff line carrying a CAPS imperative; range ecace7a~1..ecace7a; both buried lines are the sanctioned `NEVER emit nothing` tail
    $ D='git diff ecace7a~1..ecace7a -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    36 added, 8 rewritten, 2 buried

**references:** 0 of 0 path/§ citations added; the quoted-heading targets the batch cites (C1-C6 under "Continuous mode") resolve — 6 headings
    $ git diff ecace7a~1..ecace7a | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l; grep -cE '^### C[1-6]\.' docs/modules/skills/autonomous-loop/SKILL.md
    0
    6

**fences:** 12 of 12 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; n=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$n" -gt 0 ] && echo ok || echo "ZERO $t"; done | sort | uniq -c
         12 ok

**isolation:** 7 files scanned, 0 hits (pattern derived from disk at runtime, never echoed)
    $ git diff --name-only ecace7a~1..ecace7a | xargs grep -oniwE "($P)" | wc -l
    0

**class sweep:** 13 fixes × 4 directions, 3 extra instances found and fixed (maintenance.md:1116 and bootstrap.md:153 control bytes; skill-gate untagged writer), 1 found and NOT fixed (assets/docs/audit-2026-09-03.md, 2 lines — an append-only persisted report). Control-byte residue in shipped surfaces:
    $ git ls-files -z -- .claude docs examples CLAUDE.md README.md | xargs -0 grep -lP '[\x00-\x08\x0B\x0C\x0E-\x1F]' | wc -l
    0

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| W-1 | secace7a | docs/modules/skills/pendencias-updater/SKILL.md | ✓ widened writer grep over docs/modules — the `PAT` in the back-sweep `$` line below | n/a — skills have no command twin | ✓ "### 3. Add new items" field list extended | ✓ C1 writer check lists it | skill-gate/SKILL.md untagged — fixed |
| W-2 | secace7a | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "SKIPPED (already exists)"` → 4 sites; skills + agents loops fixed, rules/guard left (not in the coupled set) | n/a — bootstrap creates new projects, no existing skills | ✓ Step 5.2 report slots added | ✓ autonomous-loop C1 fails closed; maintenance "Version bumps" migration rule | none |
| W-3 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "RECURRING TRIGGER"` and `grep -n "/loop /sprint-proposer"` → C1, C5, C6 updated | ✓ sprint-proposer Step 1 no-marker branch | ✓ digest `### Trigger:` slot | ✓ Step 1 report enumeration + slot widened | none |
| W-4 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "previous boundary"` → 1 site | n/a | ✓ closure prose requires the marker change in HEAD | ✓ C6 reset committed alone | the fix's own first draft read OK on code-without-marker (probe P7) — fixed before commit |
| W-5 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "all four"` → 1 site | n/a | ✓ revocation bullet rewritten | n/a | none |
| W-6 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -n "resets the"` → 0 after | n/a | ✓ C5 resume paragraph points at C6 | ✓ C4 counts from the committed 0 | none |
| W-7 | secace7a | docs/modules/skills/sprint-proposer/SKILL.md | ✓ `grep -rn "counts as" docs/modules` → autonomous-loop Step 2, C5, final report | n/a | ✓ Step 0 INCOMPLETE paragraph | ✓ C5 writes the string the reader greps | none |
| W-8 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ `grep -rn "legitimate paths" docs/` → 1 | n/a | ✓ table rows = 4 | n/a | none |
| W-9 | secace7a | docs/modules/skills/sprint-proposer/SKILL.md | ✓ `grep -rn "~[0-9]* lines" docs/modules/skills` → 0 after | n/a | ✓ paragraph re-read | n/a | none |
| W-10 | secace7a | docs/modules/skills/autonomous-loop/SKILL.md | ✓ staged instruction-style B → only NEVER-emit-nothing tails | n/a | ✓ 2 further buried lines split (C1 unavailable, C5 trigger owner) | n/a | 2 buried lines in this batch's own draft — fixed |
| W-11 | secace7a | none — commit bodies are immutable; correction written in ecace7a's body | n/a — no file | n/a | n/a | ✓ this section uses the `$`-line form | none |
| W-12 | secace7a | .claude/commands/maintenance.md | ✓ `grep -lP '[\x00-\x08\x0B\x0C\x0E-\x1F]'` over tracked files → 4 files | ✓ bootstrap.md:153 | ✓ the comment above line 438 is now true | ✓ receipts discharged with `-w` | maintenance.md:1116, bootstrap.md:153 fixed; audit-2026-09-03.md recorded, not edited |
| W-13 | secace7a | .claude/commands/bootstrap.md | ✓ `grep -rn "ARGUMENTS/project.md" .claude/commands` → 0 after | ✓ EPA reads `project.md` Overview by name, no path grep | ✓ "Expected: 1 for BOTH files" now reachable | n/a | none |

**back-sweep:** 1 rule, 2 artifacts fixed of 2 found, re-run clean — PRE measured at ecace7a~1, POST at the tip
    $ PAT='(add|register|route|file|write|create)[^.]{0,60}(pendencias|Next Steps|a pendency|as tasks?)|(new task|task discovered|findings?)[^.]{0,60}(pendencias|Next Steps)'
      echo PRE:; for f in $(git ls-tree -r --name-only ecace7a~1 docs/modules | grep -E '(SKILL\.md|agents/.*\.md|rules/.*\.md)$'); do s=$(git show ecace7a~1:"$f"); echo "$s" | grep -qiE "$PAT" && ! echo "$s" | grep -q "origin: discovered" && echo "UNTAGGED $f"; done
      echo POST:; for f in $(git ls-files docs/modules | grep -E '(SKILL\.md|agents/.*\.md|rules/.*\.md)$'); do grep -qiE "$PAT" "$f" && ! grep -q "origin: discovered" "$f" && echo x; done | wc -l
    PRE:
    UNTAGGED docs/modules/skills/pendencias-updater/SKILL.md
    UNTAGGED docs/modules/skills/skill-gate/SKILL.md
    POST:
    0

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| every AI task writer tags `origin: discovered` | a shipped skill/agent/rule instructing the AI to add tasks or pendencies to pendencias | the `PAT` residue loop in the `$` line above (pattern-derived — replaces d812c07's hand-typed `$W` list, which is how W-1 escaped) | 2 | 2 | 0 remaining |

**liveness:** 19 components OK [full parse] — raw templates 19 validated of 25 globbed, 6 skipped (fenced)
    $ python -c "import io,yaml,glob,os,sys
      bad=0; n=0; sk=0
      for p in sorted(glob.glob('docs/modules/skills/*/SKILL.md'))+sorted(glob.glob('docs/modules/agents/*.md')):
          s=io.open(p,encoding='utf-8').read().replace(chr(13)+chr(10),chr(10))
          if not s.startswith('---'): sk+=1; continue
          n+=1
          try:
              d=yaml.safe_load(s[4:s.index(chr(10)+'---',4)+1])
              exp=os.path.basename(os.path.dirname(p)) if p.endswith('SKILL.md') else os.path.basename(p)[:-3].replace('_','-')
              if d.get('name')!=exp: bad+=1; print('FAIL',p,'name=',d.get('name'),'expected',exp)
          except Exception as e: bad+=1; print('FAIL',p,type(e).__name__)
      print('raw templates: %d validated of %d globbed, %d skipped (fenced),' % (n, n+sk, sk), 'OK' if not bad else str(bad)+' BROKEN'); sys.exit(1 if bad else 0)"
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** repaired :1116 counts 2 Applies IDs and ignores 2 cited ones; maintenance.md carried 2 control-byte lines before ecace7a and 0 after; the C1 writer check, C4 closure check and EPA DIFFERS branch went RED on their seeded states (probe in ecace7a's body)
    $ printf 'Applies W-1, W-12 from x\n\nCites U-48 and T-10\n' | awk '/^Applies /{f=1} f&&/^$/{exit} f' | grep -oE '\b[A-Z]-[0-9]+\b' | sort -u | wc -l; git show ecace7a~1:.claude/commands/maintenance.md | grep -cP '\x08'; git show ecace7a:.claude/commands/maintenance.md | grep -cP '\x08'
    2
    2
    0

**version:** none — corrects the unpushed v2.23.0 (d812c07); old-label residue 0
    $ grep -rn "v2\.22\.0" README.md docs/ .claude/ CLAUDE.md 2>/dev/null | grep -v 'created: framework-v' | wc -l
    0

**classification:** 13 add, 11 substitute, 0 delete, 0 relocate — ADD: C1 writer check, trigger-mechanism block, C6 reset, idle-lapse line, cadence Progress-Log mandate, digest Trigger + Tagging-writers slots, Step 1 no-marker branch, Step 0 reader, pendencias-updater origin, skill-gate tag, EPA DIFFERS + coupled set, EPA report slots, maintenance migration rule. SUBSTITUTE (strictly more correct, no information lost): C4 closure block, C5 stop intro, revocation bullet, resume paragraph, C3 chain line, C6 pacing, path count, "~150 lines", :438 -w, :1116 \b, :153 \b + path
    $ echo "n/a — judgement, not measurement"
    n/a — judgement, not measurement

**new component:** none this session
    $ git show --name-status --format= ecace7a | grep -c '^A'
    0

**gates:** twin parity PASS | receipt-rows-vs-diff 12/12 file-bearing rows (W-11 names no file) | row-count 13 vs 13 | slots 1/1 (`tagging writers:` → C1 policy + digest)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo PASS; git show --name-only --format= ecace7a | sort | tr '\n' ' '; echo; grep -cE '^\| W-([1-9]|1[0-3]) \|.*applied `secace7a` \|$' assets/docs/audit-2026-09-14.md
    PASS
    .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md .claude/commands/maintenance.md docs/modules/skills/autonomous-loop/SKILL.md docs/modules/skills/pendencias-updater/SKILL.md docs/modules/skills/skill-gate/SKILL.md docs/modules/skills/sprint-proposer/SKILL.md 
    13

**applied-proof:** 13 of 13 IDs resolve to a commit touching their line (form A: 11, form B: 1 — W-1, whose pre-batch line was kept and the fix added below it; n/a: 1 — W-11, commit bodies), 0 forced to open
    $ for x in "W-2|.claude/commands/existing_project_adaptation.md|553" "W-3|docs/modules/skills/autonomous-loop/SKILL.md|519" "W-4|docs/modules/skills/autonomous-loop/SKILL.md|506" "W-5|docs/modules/skills/autonomous-loop/SKILL.md|518" "W-6|docs/modules/skills/autonomous-loop/SKILL.md|531" "W-7|docs/modules/skills/autonomous-loop/SKILL.md|534" "W-8|docs/modules/skills/autonomous-loop/SKILL.md|46" "W-9|docs/modules/skills/sprint-proposer/SKILL.md|269" "W-10|docs/modules/skills/autonomous-loop/SKILL.md|562" "W-12|.claude/commands/maintenance.md|438" "W-13|.claude/commands/bootstrap.md|153"; do IFS='|' read id f l <<< "$x"; a=$(git show ecace7a~1:"$f" | sed -n "${l}p" | sed 's/[[:space:]]*$//'); echo "$id form-A $(git log -S"$a" --oneline ecace7a~1..ecace7a -- "$f" | wc -l)"; done
      echo "W-1 form-B $(git log -L 53,53:docs/modules/skills/pendencias-updater/SKILL.md --oneline ecace7a~1..ecace7a | grep -cE '^[0-9a-f]{7} ')"
      echo "W-11 n/a $(git log -1 --format=%B ecace7a | grep -c '^- W-11 commit correction')"
    W-2 form-A 1
    W-3 form-A 1
    W-4 form-A 1
    W-5 form-A 1
    W-6 form-A 1
    W-7 form-A 1
    W-8 form-A 1
    W-9 form-A 1
    W-10 form-A 1
    W-12 form-A 1
    W-13 form-A 1
    W-1 form-B 1
    W-11 n/a 1

**control back-sweep:** 4 controls touched, all re-run with negation (maintenance.md:438 isolation, :1116 Applies-ID count, bootstrap.md:153 risk-profile check, autonomous-loop C1 writer check + C4 closure check); 1 defect found — the first draft of C4 could not go RED on code committed without the marker (probe P7), fixed before ecace7a. Denominator:
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md; do inf=$(awk '/^```/{g=!g;next} g' "$f" | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 5 fenced + 23 inline = 28
    .claude/commands/audit.md: 7 fenced + 28 inline = 35

**commit correction:** d812c07, 1c3bc46, e82176f corrected by ecace7a — receipt evidence form (W-11); verdicts unchanged
    $ git log -1 --format=%B ecace7a | grep -c '^- W-11 commit correction'
    1

**placeholder:** none — the write-back follows the fix commit, so every hash was known
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-14.md | wc -l
    0

**report deletions:** none
    $ for f in $(git diff --cached --name-only -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:'; do b=$(git show HEAD:"$f" 2>/dev/null | grep -cE "$pat"); if [ -f "$f" ]; then a=$(grep -cE "$pat" "$f"); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat"; done; done; echo "structural decreases listed above: none if this is the only line"
    structural decreases listed above: none if this is the only line

**report restore:** none
    $ echo "n/a — no report destroyed in history this session"
    n/a — no report destroyed in history this session

**audit:** N/A — no MINOR/MAJOR bump in ecace7a; the baseline /audit owed by v2.23.0 is this report
    $ echo "n/a — no bump in ecace7a"
    n/a — no bump in ecace7a

**verification audit:** proposed — trigger (d); the owner decides when
    $ echo "n/a — owner decision"
    n/a — owner decision

**defect series:** N/A — maintenance session; this report is a baseline run and appends no row
    $ echo "n/a — baseline run, maintenance session"
    n/a — baseline run, maintenance session

**push:** requested by the owner — D16 over the unpushed range measured below BEFORE the write-back commit; the push is gated on a re-run after that commit (branch on the count, never on exit status)
    $ c1=$(git diff --name-only origin/main..HEAD | sort -u | while read f; do [ -f "$f" ] && grep -oniwE "($P)" "$f"; done | wc -l); c2=$(git log --format=%B origin/main..HEAD | grep -oniwE "($P)" | wc -l); c3=$(git diff origin/main..HEAD | grep '^+' | grep -cE 'eyJ[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{20,}'); echo "commits $(git log --oneline origin/main..HEAD | wc -l) | contents $c1 | messages $c2 | value-shapes $c3 | total $((c1+c2+c3))"
    commits 5 | contents 0 | messages 0 | value-shapes 0 | total 0

