# Framework Audit Report

**Date:** 2026-09-01
**Framework version:** v2.8.0 (`README.md:1`), HEAD `bddb018`
**Dimensions checked:** 17
**Agents dispatched:** 6
**Trigger:** CLAUDE.md trigger (b) — before/after a MINOR bump (`bddb018`, v2.7.0 → v2.8.0),
proposed by the maintenance session that shipped the `autonomous-loop` extraction.

**carried: 1 open from `assets/docs/audit-2026-08-31.md`** (G-20 — re-verified, still true).

> **ID scheme.** The 2026-08-31 report used `F-*` (run 1) and `G-*` (run 2). This run continues
> the progression with `H-*` so IDs never collide across report files. Carried findings KEEP their
> original ID.

---

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 module skills; only `cross-cutting-analysis` in `.claude/skills/`, as intended |
| C | Instruction style | Quality | 5 | **FAIL** | 4 instances — 2 MED (misfiled trigger, mandate with no output slot), 2 LOW |
| E | Version consistency | Currency | 5 | PASS | v2.8.0 across all 5 canonical surfaces; 0 `created:` lineage fields rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 listed = 15 on disk; sub-breakdown sums to 12 inline |
| D5 | Bootstrap file refs | References | 2 | PASS | 47/47 paths resolve; all 7 sed fences extract non-empty |
| D6 | Gap vocabulary | References | 3 | **FAIL** | 11/11 phrases match exactly, but the visual-regression chain is broken at 3 links |
| D7 | Pushy Description | Quality | 3 | PASS | 20/20 specialists fully compliant; 9 protocol agents PARTIAL per D7.5 carve-out |
| D8 | Orchestrator refs | Process | 4 | PASS | sprint→loop handoff intact both directions; marker string identical in writer and readers |
| D9 | CLAUDE.md diagram | Currency | 1 | **FAIL** | 1 stale count (`CLAUDE.md:36` says 14 skills) |
| D10 | Command → skill paths | References | 4 | PASS | `cross-cutting-analysis` present in both required locations |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 15 primary + 8 sub = 23; zero forward references; 2 declared handoffs both received |
| D12 | Examples conventions | Currency | 5 | **FAIL** | 5 skills lack `## Testing`; `examples/README.md` omits `invocation: user` |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | every cited PRD section exists; 3 defined-but-unread (1 material — see H-23) |
| D14 | Bootstrap output | Process | 2 | PASS | 14/14 template refs traced; `autonomous-loop` covered at all 3 points |
| D15 | Doc factual accuracy | Currency | 5 | **FAIL** | 1 of 27 numeric claims wrong (same `CLAUDE.md:36`) |
| D16 | Project-info isolation | Privacy | 1 | PASS | 4 surfaces incl. agent memory + 103 commits: zero hits |
| D17 | Process coverage (meta) | Meta | 6 | **FINDINGS** | 6 gaps — extraction/creation flows unowned, bump unverified, checklist half-reported |

**Verdict:** 11 PASS, 5 FAIL, 1 FINDINGS. No structural breakage; the failures are one real
activation-chain defect, one stale count, and a cluster of process controls that are installed
without being reported.

---

## Findings Ledger

Status values: `open` / `applied sHASH` / `rejected — [reason]`.

### Carried forward

| ID | Sev | Location | Finding | Status |
|----|-----|----------|---------|--------|
| G-20 | LOW | `examples/agents/` (20 files) | `## Input` sits in the top 12–33% of the file in 10 examples and at 74–85% in the other 10. Re-verified 2026-09-01: still exactly 10/10. Deferred in run 2 because the variance is PRE-EXISTING (`## Output Format` already sat at the bottom in those 10) and normalising means relocating 10 report templates: real risk, cosmetic gain. | open |

### HIGH

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| H-1 | `docs/modules/agents/code_reviewer.md:235-245` | The `Visual regression gap` block was inserted UNDER the data-integrity trigger header (`**If diff modifies multi-table operations, cascading deletes, or denormalized data:**`), and the data-integrity block below it was left with NO header of its own. Net effect: code-reviewer declares a visual-regression gap on backend multi-table diffs and NEVER on the shared-component / CSS-variable / design-token diffs the specialist exists for. Its own trigger sentence is absent entirely. Only `validator.md:112` still fires this gap correctly. **Introduced by `9c3d90a` — the commit that applied G-13.** Found independently by Agent 3 (D6) and Agent 5 (C). | open |

### MEDIUM

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| H-2 | `.claude/commands/bootstrap.md:483-493` | Step 12.5a's install table maps 9 gaps → 9 specialists; there is no `visual regression gap → visual-regression-tester.md` row. A project that keeps the gap declares it with no specialist installed, and the step's fallback does not register it in `pendencias.md` either, because that fallback only fires for gaps whose example is MISSING from the library — this one is present. Same class as H-1: G-13's fix did not sweep the install path. | open |
| H-3 | `.claude/commands/bootstrap.md:394`, `.claude/commands/existing_project_adaptation.md:325` | Both say "Review the **four** optional gap sections (accessibility, performance, concurrency, data integrity)". There are **five** — visual regression is never offered for keep/remove pre-selection. The security side correctly says "five". Twin divergence + stale count, same G-13 class. | open |
| H-4 | `docs/modules/agents/validator.md:112, 133, 141` vs its output format at 45-88 | Three instructions write into `this report's Coverage Gap Declaration` (L112, `ALWAYS DECLARE`) and `"Items for human verification"` (L133, L141), but the Validation Report template in the same file defines NEITHER section. component-design §6 property 3 unmet: the omission is structurally invisible. This is the DESCENDING direction of the maintenance class sweep — a step that must report something, with no field to report it in. | open |
| H-5 | `.claude/commands/maintenance.md` — "Version bumps" section | The bump is an H2 **outside** the numbered post-change checklist, so the checklist's governing line ("Run EVERY numbered item below before committing") never reaches it; and unlike items 4/5/6 it carries **no `ALWAYS REPORT` line**. Evidence this is not hypothetical: `cc6e31d` exists solely because "the previous commit declared a bump it never applied". A silent bump is indistinguishable from a forgotten one. | open |
| H-6 | `.claude/commands/maintenance.md` — checklist items 1-3 | Items **4, 5 and 6 each carry an explicit `ALWAYS REPORT … / NEVER emit nothing` line**; items **1 (inventory sweep), 2 (instruction-style), 3 (reference & isolation)** carry none. Verified item-by-item. A session can silently skip 1-3 and produce output indistinguishable from one that ran them — the condition component-design §9 rule 3 forbids. The rule was applied to the three newest items and never back-swept onto the three original ones. | open |
| H-7 | `docs/modules/skills/skill-gate/SKILL.md:14` | `derived_from: skill-gate-spec v1.1 (.claude/docs/skill-gate-spec.md in the framework repo)` — but `.gitignore:4` ignores `.claude/docs/` and `git ls-files .claude/docs/` returns **0**. This SHIPPED template goes to every internal-tool+ project pointing at a mother-repo artifact that exists only on the owner's machine and is absent from every clone and fork. | open |
| H-8 | `.claude/rules/component-design.md:105` (§5), `docs/modules/skills/skill-gate/SKILL.md:26-30`, `.claude/commands/maintenance.md` item 1 | **Component EXTRACTION/SPLIT has no written home.** §5 offers exactly three verbs (ADD / SUBSTITUTE / DELETE) and no fourth for RELOCATE — content leaving one component to live in another. skill-gate's Scope check is strictly binary ("NEW → this skill applies; EXISTING → follow evolution-policy") and an extraction is simultaneously both, with no rule saying which branch wins. maintenance item 1 fires on "ADDS, REMOVES, or RENAMES" — a split is none of the three, and the extraction-specific surface (every component that INVOKED the moved content, per §9) is not in its fixed list. `bddb018` made the load-bearing pointer-vs-duplicate decision with no rule to anchor it, so the next extraction may legitimately choose duplication. *(1 occurrence, below the 2+ threshold — reported because the precedent is now unanchored.)* | open |
| H-9 | `.claude/commands/maintenance.md`, `CLAUDE.md:40-44` | **Creating a new framework component in the mother repo has no procedure and no gate.** Executed 4+ times (`ebac5f0` commit skill, `9ab0c06` codebase-audit + framework-audit, `7d2af19` skill-gate itself, `bddb018` autonomous-loop). The only written instruction is the PLACEMENT note in CLAUDE.md; nothing covers quality — no draft dir, no blind review, no rubric. The repo ships `skill-gate` to every internal-tool+ project as the mandatory creation gate and applies none of it to the components it authors itself. This may be a deliberate exemption; nothing anywhere SAYS it is. | open |

### LOW

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| H-10 | `CLAUDE.md:36` | "the agent templates, rules templates, and **14 process skills** live under `docs/modules/`" — actual is **15**. Partial-update artifact from `bddb018`: line 68 of the same file WAS updated to "15: 12 lifecycle/process + 3 tier-gated" and line 75's "(9 more)" is consistent with 15. The count lived in a prose NOTE paragraph, not in the tree, and the inventory sweep only swept the tree. The only "14" left anywhere in the repo. | open |
| H-11 | `.claude/commands/bootstrap.md` Step 5 (:224) vs Step 14 | Step 14 carries an explicit mandate to overwrite the Hooks placeholder ("a project whose CLAUDE.md still carries that line references a bootstrap step that does not exist in the project"). Step 5 has **no equivalent clause** for the `[Filled in Step 5 below]` MCP Servers placeholder, one section higher in the same template. A project installing no MCPs ships the placeholder. Twin defect, fixed on one side only. | open |
| H-12 | `.claude/commands/bootstrap.md:380` | Step 6 says 'Register in CLAUDE.md **"Skills"** section', but the template's section is `## Skills & Agents` (claude_md.md:53). Step 2 (:123) uses the correct name. Two instructions disagree on the target. | open |
| H-13 | `docs/agentic_engineering_framework.md:340-370` | The bootstrap pipeline diagram enumerates every Step 5.7 copy EXCEPT `modules/templates/check_agent_frontmatter.md → scripts/check-agent-frontmatter.mjs`. The guard is present in bootstrap and in the Step 15 report; only the concept doc's diagram omits it. | open |
| H-14 | `docs/modules/skills/validation-orchestrator/SKILL.md:~57` (`### 3. Git checkpoint`) | Steps 1 and 2 of "Before Implementing" both carry `ALWAYS …` + `ALWAYS REPORT … NEVER emit nothing`. Step 3 is a bare descriptive sentence with no CAPS verb and no report line — yet `autonomous-loop` §3a's ownership table treats it as an owned mandatory step. | open |
| H-15 | `docs/modules/skills/sprint-proposer/SKILL.md:~139` (`#### 4b`) | The 3-5 task cap and "1 if large" are load-bearing constraints (`autonomous-loop` spends a callout warning against carrying the number into a loop phase) but are written as descriptive bullets with no imperative verb. | open |
| H-16 | `examples/skills/` — ci-cd-pipeline, database-migration-guide, e-commerce-patterns, multi-tenancy-patterns, scheduling-patterns | 5 of 9 skill examples lack a `## Testing` section, which `examples/README.md` states unconditionally as a skill convention; database-migration-guide also lacks the Common Pitfalls table. The split is systematic — all 3 STACK skills + api-design-patterns have it, the 5 domain/process ones do not. The fix is either adding the sections or QUALIFYING the convention ("stack skills"), not a per-file patch. | open |
| H-17 | `examples/README.md` — Frontmatter → `invocation:` | Documents only `invocation: subagent` and `invocation: inline`, but the framework ships 7 skills declaring `invocation: user` (sprint-proposer, autonomous-loop, session-end, context-recovery, commit, codebase-audit, framework-audit). The convention doc is incomplete relative to shipped practice. | open |
| H-18 | `docs/agentic_engineering_framework.md:286`, `docs/modules/skills/README.md`, `README.md:170,206` | Three files call the 12 non-tier-gated skills "**12 inline** skills" while 5 of those 12 declare `invocation: user`. "Inline" is being used to mean "always copied, not tier-gated", which collides with the `invocation:` field's documented meaning. Terminology, not a count error. | open |
| H-19 | `docs/modules/skills/skill-gate/SKILL.md:118`, `docs/modules/rules/session_rules.md:67` | Two cross-skill citations name a target that is a **bold paragraph lead-in inside another section**, not a heading of its own: sprint-proposer cites skill-gate → "In sprint-approved mode", and autonomous-loop cites session-rules → "Signals of exceeding". Both resolve by grepping the quoted phrase; neither resolves by scanning headings. | open |
| H-20 | `CLAUDE.md:91` (+ `README.md:66-73`) | "**Update periodically:** `git pull`" has no owner, no trigger defining "periodically", and no verification that the local framework clone is current BEFORE `/bootstrap` stamps `framework-v2.8.0` into a new project or `/existing_project_adaptation` keys migration decisions to that label — a stale clone ships an old contract under a trusted version. Also: README recommends FORK, whose update command is `git fetch upstream && git merge upstream/main`, not the `git pull` CLAUDE.md names. The repo already treats this risk seriously in the other direction (`existing_project_adaptation.md` Step 1.0 mandates a freshness check on the PROJECT copy, born from a stale-copy incident) with no symmetric check on the FRAMEWORK copy. | open |
| H-21 | `.claude/commands/bootstrap.md` Step 13 | The table's "Example template" column uses framework-relative `examples/rules/i18n-rules.md` while the Guard clause and copy instruction directly below use project-relative `assets/examples/rules/`. Both resolve at runtime; the convention is inconsistent within one step. Step 12.5a is consistent. | open |
| H-22 | `.claude/commands/bootstrap.md` Step 15 | The "Files created" inventory names `.claude/skills/[domain]-test-patterns/SKILL.md` but not Step 12's `.claude/skills/[stack-name]/SKILL.md`. It IS reported in the later "Skills installed" block, so this is an inventory-list omission, not a silent artifact. | open |
| H-23 | `.claude/commands/prd_planning.md` (Cross-cutting Concerns section) ↔ `bootstrap.md` Steps 1-4 | The PRD's **Cross-cutting Concerns** section is written by `/prd_planning` Phase 4 and maintained by `/prd_change` Phase 3 — and **no bootstrap step reads it**. A project's transversal themes never reach `project.md` or the seeded rules. Of the three PRD sections bootstrap ignores (§1.4 Competitive Differentiator, §8 Roadmap, Cross-cutting Concerns), this is the only one with downstream consequences; the other two are correctly superseded by §5.4 Build Order. | open |

---

## Detailed Findings (agent reports)

### Agent 1 — Structural Sync (A, D4, D9, D16)

- **[A] PASS.** `docs/modules/skills/` holds 15 folders; `.claude/skills/` holds exactly 1
  (`cross-cutting-analysis`), which exists in both as required. No over-placement.
- **[D4] PASS.** README table lists 15, disk has 15, count claim "15 skills: 12 inline + 3
  tier-gated" correct, inline sub-breakdown (6+3+1+1+1) sums to 12.
- **[D9] FAIL — 1 count.** Every path in the tree resolves (6 commands, 7 templates, 10 agents,
  5 rules, 6 named skill folders, 3 example subdirs). Counts "6 slash commands", "(9 more)",
  examples 20/9/11 all correct. `CLAUDE.md:36` says 14 process skills → **H-10**.
- **[D16] PASS.** Blocklist: 3 project folder names + ~14 harvested identifiers (person/client
  tokens, 3 deployment domains, locality, brand, domain vocabulary, academic/app identifiers, 1
  BR phone value). Surfaces scanned: all tracked files, `.claude/docs/` (3 untracked notes),
  agent memory (10 files incl. MEMORY.md), git history (103 commits — contents via `git grep`
  over `git rev-list --all`, plus all subjects and bodies). **Zero hits.** Two greps matched and
  were cleared as legitimate: `audit.md:77` contains the literal `*.vercel.app` pattern as part
  of the instruction describing what to hunt for, and `load-tester.md:75` matched a substring
  inside "Grafana". Value-shape scan (JWT/`sk-`/`ghp_`/`AKIA`/`xox`/`user:pass@`/Bearer/BR
  phone/CPF/CNPJ) clean on all four surfaces; every e-mail is an obviously synthetic fixture.
  `git log --all --name-only` shows **0 paths ever under `projects/`** — the gitignore isolation
  has held for the entire 103-commit history. Remote exists, branch is 6 commits ahead.

### Agent 2 — Bootstrap Integrity (D5, D11, D13, D14)

- **[D5] PASS.** 47 static paths, 47 resolve. All 7 `sed` fence sources verified to open with a
  4-backtick fence (and `check_agent_frontmatter.md` with ` ```js `), so no extraction produces an
  empty file. All 12 underscore→hyphen renames correct. Cross-component step references resolve
  (sprint-proposer Step 0 / step 3, session-end item 1).
- **[D11] PASS.** 15 primary + 8 sub-steps = 23 discrete. The "15-step pipeline (15 primary plus
  sub-steps)" claim is worded honestly and is accurate. **Zero forward references.** Two
  deliberate forward HANDOFFS (5.5 → 14 `enabledPlugins`; 1.2c → 2/3 risk profile) are both
  explicitly declared AND received. All 26 `Step N` tokens resolve.
- **[D13] PASS.** Every PRD section bootstrap cites exists, including every sub-number. Three
  sections defined-but-unread: §1.4, §8 (correctly superseded by §5.4 Build Order), and
  Cross-cutting Concerns → **H-23**.
- **[D14] PASS.** 14/14 template references traced to a bootstrap step. **`autonomous-loop` is
  covered at all three points** — inside the Step 5.7 glob and NOT in the tier-gate `rm -rf` list,
  named in the Step 5.7 bullet list (L260), and named in the Step 15 report (L648) — with counts
  coherent across bootstrap (12), CLAUDE.md (15 = 12+3), modules README, README, and the pipeline
  diagram, and version v2.8.0 agreeing everywhere. Its frontmatter is registry-safe. Advisories →
  **H-11, H-12, H-13, H-21, H-22**.

### Agent 3 — Activation Chain (D6, D7)

- **[D6] FAIL.** code-reviewer declares **5** gaps (accessibility, performance, concurrency,
  visual regression, data integrity); security-reviewer declares 5 (static analysis, secrets
  coverage, federation protocol, compliance, infrastructure security); validator co-declares
  visual regression. **All 11 phrase links match exactly** — the vocabulary layer is intact. The
  chain breaks at three other links: **H-1** (trigger misfiled), **H-2** (install table),
  **H-3** (stale "four" count). No orphaned specialists: 10 of 20 examples carry a `declares a …
  gap` phrase and all 10 are declared by a reviewer; the other 10 are `USE PROACTIVELY`-activated
  by design.
- **[D7] PASS.** 30 agents checked. **20/20 specialists in `examples/agents/` carry all five
  elements.** The 9 PARTIAL rows in `docs/modules/agents/` are protocol-spawned or process agents
  falling under the D7.5 carve-out. **Zero anti-pattern instances** — every description states
  what the agent does; `criteria_enforcer` and `diff_pattern_extractor` merely place that sentence
  after the trigger rather than before it.
- **[Frontmatter liveness, supplementary] PASS.** All 30 frontmatter blocks parse under
  `yaml.safe_load`; `name:` matches the target filename in all 30; no key silently resolved to a
  nested map. The one scalar carrying a literal `: ` (`criteria_enforcer.md` → `receives:`) is
  correctly single-quoted — **confirming the fix applied in `bddb018` holds**.

### Agent 4 — Orchestration & Commands (D8, D10)

- **[D8] PASS.** 31 name references across 5 orchestrators, all resolve. All 10 agent files are
  referenced by at least one orchestrator — no orphan agents, no dangling references.
  **The 2026-09-01 sprint→loop handoff is intact in both directions:** every section heading cited
  across `sprint-proposer` ↔ `autonomous-loop` ↔ `validation-orchestrator` exists verbatim in its
  target, and the marker string `LOOP CONTINUATION — active` is byte-identical in the writer
  (`autonomous-loop:337`) and both readers (`sprint-proposer:75`, `autonomous-loop:261`), so the
  grep-based self-check actually fires. Two citations resolve to bold lead-ins rather than
  headings → **H-19**.
- **[D10] PASS.** Both PRD commands reference exactly one skill (`cross-cutting-analysis`),
  present in both `docs/modules/skills/` and `.claude/skills/` as the dual-placement rule
  requires. The two modes they cite match the modes the skill declares.

### Agent 5 — Document Accuracy (C, E, D12, D15)

- **[C] FAIL — 4 instances.** 2 MEDIUM (**H-1**, **H-4**), 2 LOW (**H-14**, **H-15**).
  Positive note recorded by the agent: `autonomous-loop/SKILL.md` is the strongest file in the
  sample — every behavioral step carries a CAPS imperative, a dedicated section, an explicit
  report line, and a mechanical check with a stated expected result. No anti-pattern in it.
- **[E] PASS.** Canonical v2.8.0 matches on all 5 surfaces. `git show HEAD --unified=0 | grep
  '^[-+]created:'` returns exactly one line — `+created: framework-v2.8.0` on the newly added
  skill. **No existing lineage field was rewritten by the bump.** Observation: `CLAUDE.md` and the
  concept doc carry no canonical version string at all; `README.md:1` is the sole anchor.
- **[D12] FAIL — LOW.** → **H-16**, **H-17**. Passes: all 20 agent examples carry every required
  frontmatter field and all four required sections (zero misses); all 9 skills use folder/SKILL.md
  and correctly omit `last_eval`; all 11 rules carry `domain:`/`applies_to:` + Inviolable Rules +
  verification.
- **[D15] FAIL — 1 of 27.** → **H-10**. Every other numeric claim across CLAUDE.md, README.md,
  the concept doc, both module READMEs and both command twins is correct, including
  "/audit — 17 dimensions" (A, C, E + D4–D17 = 17) and "6 parallel audit agents". Terminology
  note → **H-18**.

### Agent 6 — Process Coverage, meta (D17)

- **[D17] FINDINGS — 6.** 40 commits classified into 6 operation types. Four types have a written
  home and are correctly owned (template/command evolution ~21, audit-fix application ~11,
  upstream absorption 5, audit execution 1). Gaps: **H-8** (extraction/split, 1 occurrence),
  **H-9** (new-component creation, 4+ occurrences), **H-5** (version bump), **H-7**
  (referenced-but-unpublished spec), **H-6** and **H-20** (aspirational mechanisms).
- **Verified as properly owned (no finding):** every mother-repo behavior promised by
  `evolution_policy.md:100-151` has a mother-side instruction — the `/maintenance` upstream
  session (intake steps 1-4), the `upstreamed` back-handoff (intake step 8), the authoritative
  lineage disposition (intake step 4 + Step 0's filename cross-check), and `framework-audit`'s
  evolution-doc vehicle (Step 0's sweep). **This chain is closed in both directions** — the
  G-7/G-11 fixes from the previous run hold.

---

## Recommended Fixes

### 1. Structural — the activation chain (do these together, they are one class)
`H-1` → give `Visual regression gap` its own trigger header in `code_reviewer.md`
("If diff modifies shared UI components, CSS variables, design tokens, or global stylesheets")
and restore the data-integrity header above its block.
`H-2` → add the `visual regression gap → visual-regression-tester.md` row to bootstrap Step 12.5a.
`H-3` → "four optional gap sections" → "five …" in BOTH command twins.
`H-4` → add `Coverage Gap Declaration` and `Items for human verification` sections to
`validator.md`'s Validation Report template.

> These four are one G-13 aftershock. Applying H-1 alone leaves the specialist uninstalled (H-2)
> and unofferable (H-3).

### 2. Process controls — installed but not reported
`H-5` → promote the version bump to a numbered checklist item with
`version: vX.Y.Z applied to N/N canonical surfaces` / `version: none — [reason]`.
`H-6` → add the `ALWAYS REPORT … / NEVER emit nothing` line to checklist items 1, 2 and 3.
`H-8` → add a fourth verb (RELOCATE) to component-design §5 with the pointer-vs-duplicate rule;
one line in skill-gate's Scope check resolving the both-branches case; add SPLIT to item 1's verbs.
`H-9` → a "New component creation" section in maintenance.md, **or** an explicit written exemption.
Silence is the defect; either resolution closes it.
`H-20` → a framework-clone freshness check before bootstrap/adaptation stamp a version, and align
`CLAUDE.md:91`'s command with README's fork flow.

### 3. Quick fixes — single-line edits
`H-10` (CLAUDE.md:36 → 15), `H-7` (skill-gate `derived_from`), `H-11`, `H-12`, `H-13`, `H-14`,
`H-15`, `H-19`, `H-21`, `H-22`.

### 4. Convention decisions — need an owner's call, not a patch
`H-16` (add `## Testing` to 5 skills, or qualify the convention to stack skills),
`H-17` (document `invocation: user`), `H-18` ("inline" vs `invocation:` terminology),
`H-23` (should bootstrap read the PRD's Cross-cutting Concerns?), `G-20` (Input ordering).

---

## Meta-observation for the next maintenance session

Every HIGH and MEDIUM structural finding in this run (`H-1`, `H-2`, `H-3`, `H-4`) descends from a
single earlier fix — G-13 in `9c3d90a`. The 2026-08-31 run named its own recurring class as *"a
fix applied to the instance the report named, without sweeping the class"*, and answered it with
checklist item 4 (the four-direction class sweep). That item works on the class of the DEFECT; it
has no step that verifies **the correction itself landed where it was supposed to**. H-1 is a
correct fix inserted under the wrong header — item 4 could run perfectly and never catch it.

The natural home for that missing step is the Audit-intake section, which already mandates
re-verifying each finding against the current disk BEFORE applying it (step 2) but nothing AFTER.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

---

**Report status:** `status: COMPLETE` — all 6 agents returned; all 17 dimensions evaluated.
