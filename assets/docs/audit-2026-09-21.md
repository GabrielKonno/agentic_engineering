# Framework Audit Report

**Date:** 2026-09-21
**Framework version:** v2.31.0
**Run mode:** `baseline` (trigger (c) owner request, following a trigger (b) proposal before the v2.31.0 MINOR bump; the owner named no batch to verify)
**Post-push D16:** log 82f84e2..961244b → 9 commits scanned, 0 hits — origin/main at 961244b
**Dimensions checked:** 17
**Agents dispatched:** 6

**carried: 24 open + 0 escalated from `audit-2026-09-20.md` Run 4** — AA-3, AA-4, AA-9, AA-10,
AA-11, AA-12, AA-13, AA-16, AB-2, AB-3, AB-7, AC-3, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-12,
AC-13, AC-19, AC-20, AC-21, AC-22 (12 MEDIUM, 12 LOW).
**PLUS 41 findings this run RECOVERED from the two preceding reports, which the carry-forward chain
had dropped — see AD-1, the run's HIGH finding.**

**carry-forward integrity self-check (the control AD-1 recommends, run on THIS report):**
```bash
grep -cE '^\| [A-Z]{1,2}-[0-9]+ \|' assets/docs/audit-2026-09-21.md   # 81
```
**81 rows = 16 new (AD-1..AD-16) + 24 carried + 41 recovered**, counted over the FINDINGS LEDGER
only. A whole-file grep now returns 93, because the per-fix receipt table added below shares the
`| ID |` row shape — the exact conflation Gate 3 warns about ("count dispositions in the FINDINGS
ledger and rows inside THIS run's receipts section, never with one grep over the file"). Every ID
named in prose above has a ROW below. This report was written with the 65 in prose only, and the check caught it before the
commit — which is the same defect AD-1 files, one run later. The rows were recovered mechanically
from `audit-2026-09-16.md`, `audit-2026-09-19.md` and `audit-2026-09-20.md`; 65 of 65 resolved, none
had to be reconstructed by hand.

**OBSERVATIONS — accepted-risk, not re-opened:** E-1, M-44, N-53, T-11, T-36, U-48, B-17.
Agent 1's D16 history scan returned 2 hits over 236 commits; both are attributable to **E-1** (owner
decision 2026-09-02, `audit-2026-09-02.md:603`) — a source-project third-party payment-vendor
identifier, infra-reference class, introduced in a lineage doc at `9ab0c06` (2026-06-23, v2.3.0) and
fix-forwarded out of the working content at `dd98dea` (2026-09-02). The two residual hits are the
introducing patch and the removing patch. Both are PUSHED; removal requires history rewrite + force
push, which the owner has accepted. **E-1's reopening condition is satisfied as written** — "any
identifier of a different class remains a finding" — and no identifier of any other class was found
on any surface.

**OBSERVATION — push decay, discharged by this report.** The `maintenance.md` push-decay detector
fires `advanced 82f84e2..961244b`: nine commits were published after receipts written when they were
still unpushed. The mandated discharge was run in place and is persisted in this report's
**Post-push D16** header line above — `9 commits scanned, 0 hits`, GREEN. No further action is owed.

---

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 module skills, 1 runtime skill, correct single dual placement |
| C | Instruction style | Quality | 5 | PASS | 0 buried-requirement anti-patterns; 4 LOW style deviations |
| E | Version consistency | Currency | 5 | PASS | v2.31.0 on all 6 canonical surfaces; `v2.30.0` residue 0; no lineage field rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15/15; all four count claims correct |
| D5 | Bootstrap file refs | References | 2 | PASS | 85 of 85 paths resolve |
| D6 | Gap vocabulary | References | 3 | PASS | 3 of 3 declarers; 10 gaps, each with specialist + install row on both twins |
| D7 | Pushy Description | Quality | 3 | PASS | 20/20 examples FULL; 14/14 regression cases pass on both twins |
| D8 | Orchestrator refs | Process | 4 | PASS | all references resolve; 1 LOW citation (AD-14) |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | ~55 entries resolve; all count comments correct |
| D10 | Command → skill paths | References | 4 | PASS | 41 self-checks executed; byte-identity confirmed; trigger parity matches on all three surfaces |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 15 primary + 12 sub + Setup = 28; no undeclared forward reference |
| D12 | Examples conventions | Currency | 5 | PASS | 0 violations; full-set truth-tests all hold |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | every cited section defined at the cited numbering |
| D14 | Bootstrap output | Process | 2 | PASS | 16 of 16 template references traced |
| D15 | Doc factual accuracy | Currency | 5 | **FAIL** | 6 inaccuracies — every count correct, every defect a scope/coverage claim |
| D16 | Project-info isolation | Privacy | 1 | PASS | 0 hits on every live surface; 2 history hits = E-1, accepted-risk |
| D17 | Process coverage (meta) | Meta | 6 | **FINDINGS** | 2 new gaps; convergence verdict favourable |

**Cross-dimensional:** AD-1 (HIGH) was found by consolidation, not by a single agent — it is a
defect in the report chain itself.

---

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AD-1 | HIGH | `assets/docs/audit-2026-09-20.md:14` and `:411` | **The carry-forward chain broke between two runs of the same file.** Run 3 declared `carried: 81 open` in PROSE and wrote no ledger ROW for any of them; Run 4 then read the ledger rows and declared `carried: 8`. Mechanically: 41 open/escalated findings across `audit-2026-09-16.md` and `audit-2026-09-19.md` have **zero** `^\| ID \|` rows in `audit-2026-09-20.md` (the A/C/D rows present there are Part 1 *verdict* rows, not carry-forward status rows). Phase 3 item 3's re-verification never ran over them. This run's own first carry-forward measurement read 24 and would have propagated the loss. **HALF-FIXED in `280c2f2`:** item 3 now requires ROWS and says to read the previous report's rows, not its `carried:` sentence; the 41 rows are restored. NOTHING COUNTS THEM — the reconciliation was drafted and cut (it read GREEN on this finding's own data, and the moratorium forbids a new control in that file). Stays `open`. | open |
| AD-2 | MEDIUM | `.claude/commands/bootstrap.md:838` | The Step 14 merge note is a divergent local RESTATEMENT: "merge the keys rather than overwriting" — names no key and omits the never-replace clause both the template (`settings_json.md:75`) and EPA Step 4.2 (`:1048`) now carry. component-design §5: the source POINTS, never restates. Aggravating: the note explicitly covers `.claude/settings.local.json`, the file most likely to already hold an owner statusline. Missed by 961244b's inventory sweep, which fixed the enumeration 19 lines above. | applied `280c2f2` |
| AD-3 | MEDIUM | `.claude/commands/bootstrap.md:132-162`, Steps 2 (`:214-224`) and 3 (`:236-243`) | Step 1.2 asserts "RECEIVER MANDATE — Steps 2 and 3 ALWAYS confirm receipt" and annotates its own grep "run after Step 3", but `grep -n "Risk profile"` hits only Step 1.2 and the Step 15 slot. Neither receiver mentions it. Same §9 asymmetry the cross-cutting hand-off already fixed for Steps 3/4/13; U-28's repair added the grep to the DECLARING step only. | applied `280c2f2` |
| AD-4 | MEDIUM | `CLAUDE.md:146` | `/audit` is described as persisting a report "**(the only file it writes)**". FALSE: `audit.md:32-37` mandates appending a row to the defect-series table in `audit.md` itself AND committing that file. `/audit` writes two files. CLAUDE.md contradicts itself 16 lines later at `:162` ("`/audit` Phase 3 owns appending a row"). | applied `280c2f2` |
| AD-5 | MEDIUM | `docs/agentic_engineering_framework.md:49` | Same defect, second surface: "`/audit` … never modifies an audited file, and writes only its own dated report". FALSE on both halves — it modifies `.claude/commands/audit.md`, which D10/Agent 4 audits. Root cause shared with AD-4: `/audit` 2026-09-09 T-50 resolved the contradiction INSIDE `audit.md` and never swept the two external surfaces that describe its write scope. | applied `280c2f2` |
| AD-6 | MEDIUM | `docs/modules/skills/validation-orchestrator/SKILL.md:118`, `:284` | The `Security reviewer:` report category exists on **1 of the 3 twin surfaces** its own rule binds (`docs/agentic_engineering_framework.md:~1021-1030`: "when adding or renaming a category, update ALL THREE SURFACES … AND `validator.md`'s `produces:`"). Absent from the concepts-doc category list and from `validator.md`'s Output template and `produces:` (still "14 categories"). Consequence: on Route 2 the VALIDATOR writes the report from a template with no `Security reviewer:` slot while `:118` mandates `ALWAYS REPORT` into it — component-design §10, a mandate present, correct and **unexecutable**. | applied `280c2f2` |
| AD-7 | MEDIUM | `.claude/commands/audit.md` (absence) | The TIER-OF-REFERENCE rule reached this run's agents through the INVOKING PROMPT only. `grep -inE 'tier-of-reference\|FACTORY' .claude/commands/audit.md` returns nothing; its only home is `maintenance.md:1173`, scoped to the pre-commit verifier brief. Phase 1 mandates prepending the isolation rule and nothing else. The K-15 / L-3 class both the Agent 1 and Agent 4 contracts name: a working rule that lives in the prompt, so the next run gets it only if the invoker remembers. | applied `280c2f2` |
| AD-8 | MEDIUM | `.claude/commands/maintenance.md` Audit intake item 8 | A follow-up commit correcting the CONTENT of the immediately preceding commit has no written home, and no rule states what receipt set it owes. Item 8 is scoped by its own title to "A FALSE CLAIM IN AN ALREADY-MADE COMMIT **MESSAGE**". Four content-correction commits in the last 40: `28b157a`, `847d18f`, `34a6c97`, `455c299`. | applied `280c2f2` |
| AD-9 | MEDIUM | `.claude/commands/maintenance.md` Gate 3 / Audit intake item 4 | Nothing asserts that an audit-applying session wrote a receipts SECTION into the report file, so the 27-key loop — self-scoped to "this run's section" — is skipped silently when the section is absent. Measured receipt-key coverage per commit body: `28b157a 0/27 · 847d18f 0/27 · 1b0bb67 1/27 · c9355b0 3/27 · 34a6c97 5/27 · 455c299 5/27 · 0bb7949 6/27 · 34a7c1c 9/27 · 961244b 27/27`. `34a7c1c` is the **v2.30.0 MINOR bump** — 9 of 27 keys, `verifier rounds:` written as prose (invisible to the loop's `^\*\*key:` anchor), zero `$` lines in the whole body. No control noticed. **HALF-FIXED in `280c2f2`:** the gap is now STATED in Gate 3, with both cut forms and why each failed. No check exists. Stays `open`. | open |
| AD-10 | LOW | `.claude/commands/bootstrap.md` (Steps 3, 4 vs `:449`) | `.claude/phases/` is never `mkdir`'d before Steps 3 and 4 write into it; the only creation sits inside Step 5.8's `internal-tool`+ branch, which never runs at `prototype`. Every other directory is explicitly created (`:378`, `:409`, `:421`, `:816`). Benign — the Write tool creates parents — but it is the one directory the command relies on implicitly. | open |
| AD-11 | LOW | `README.md:131-163` | The "Repository Structure" tree omits `assets/` entirely, though README's own command table at `:124-125` names `assets/docs/` as the destination for `/audit` reports and `/maintenance` records. An omission is a finding under D15.2. | applied `280c2f2` |
| AD-12 | LOW | `README.md:139-145` | The `.claude/` subtree omits `.claude/docs/`, which exists on disk and IS listed in `CLAUDE.md`'s own tree ("Framework notes and ideas"). | applied `280c2f2` |
| AD-13 | LOW | `docs/agentic_engineering_framework.md:~243-261` | The "Framework repository" tree omits `.claude/`, `assets/`, `README.md` and `.gitattributes`. The same document's components table at `:49` documents `.claude/commands/`, so the tree contradicts the table two sections above it. | applied `280c2f2` |
| AD-14 | LOW | `framework-audit/SKILL.md`, cited from `autonomous-loop/SKILL.md` and `session_rules.md` | `framework-audit` → Q4 is not a markdown heading in the target's body: it is the numbered list item `4. **Aspirational-vs-real audit**`; the only `### Q4 …` line sits INSIDE the fenced report template. Unambiguous and present, so not a broken reference — but it fails D8.7's "real markdown heading" test, and D8.7 says a bold/list lead-in is not a heading. | open |
| AD-15 | LOW | `.claude/commands/audit.md` D6.7 | D6.7's literal instruction — "`diff` the two install BLOCKS — the table AND the prose around it … expected: empty" — is **unsatisfiable by design**. The two commands deliberately diverge on mode-specific prose (bootstrap's Step 7/8/10 parenthetical; the `created:` lineage stamp), and the repo bounded the byte-equivalence claim to the `TWINNED SPAN` markers precisely to make it falsifiable (`/audit` 2026-09-10 U-24). Both bounded diffs are empty. **The check text is stale, not the repo** — a check that fires on the healthy state, the K-8/L-7 inversion. | open |
| AD-16 | LOW | `.claude/commands/maintenance.md:427` | "**ALWAYS report the harvest with its command and stdout**" names no key and is not in the 27-key registry, so the "every key exactly 1" loop cannot miss it. Measured: reported in 1 of the last 9 batches (`1b0bb67`); before that only in `audit-2026-09-15.md:566-568`, folded under `control back-sweep:`. component-design §10's own class inside the framework's apparatus. | open |

| AD-17 | LOW | `docs/modules/skills/skill-gate/SKILL.md:31` | [filed by the 2026-09-21 backlog batch 2, from its independent pre-commit verifier] The citation `evolution-policy → "Component creation gate" → Exemption states` named a string that appears NOWHERE in the target; the real text is a bold lead-in. Found while proving D8.7 still had a reachable RED — it was the only non-resolving citation in the repo. | applied `3c24435` |

### Carried forward from `audit-2026-09-20.md` Run 4 — re-verified this run

All 24 re-verified. **AC-9 re-measured and its magnitude has grown**: `maintenance.md:966-973`
records the control back-sweep baseline as *maintenance 5+27=32; audit 7+28=35; TOTAL 67
(2026-09-15)*. Run verbatim today it returns **maintenance 6+30=36; audit 10+41=51; TOTAL 87**. The
stated RED condition is "lower than the previous batch reported", so **up to 20 controls could be
deleted and still read GREEN**. The rule's own text ("RE-MEASURE THIS AT THE FINAL TIP") has not
been honoured by any batch since 2026-09-15. Recurrence of `/audit` 2026-09-10 U-6 at larger
magnitude.

**All 24 re-verified this run. Rows re-emitted so the ledger grep can see them (AD-1).**

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AA-3 | LOW | `assets/docs/audit-2026-09-19.md:742`; harvesters at `.claude/commands/maintenance.md:97`, `.claude/commands/audit.md:111` | The `push:` receipt does not end with `— origin/main at sHASH` (a parenthetical follows), against `maintenance.md:50`. The harvester is `$`-anchored and takes `tail -1`, so it silently discarded that key and read the earlier `push decay:` line instead; its only RED fires when NOTHING matches. Harmless here (both lines carry the same hash), blind by construction. | open |
| AA-4 | LOW | `assets/docs/audit-2026-09-19.md`, the `control back-sweep:` `$` line | The line redirects to `"$TMP/x"` with `$TMP` defined nowhere in the fence, the command or the report. It reproduces on this host (Git Bash inherits `TMP`) and becomes `> "/x"` on a POSIX host where `TMP` is unset. C-13's fix landing on an undeclared variable. | applied `9a959e3` |
| AA-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227` vs `docs/modules/rules/session_rules.md:278-283` | A-18's schema/migration extra-receipt rule exists only inside the executed component; the section that owns the deploy moment ("Deploy gates"), which `:169` names as the gate's caller, never mentions it — component-design §9. | open |
| AA-10 | LOW | `.claude/commands/maintenance.md:939-942` vs `:949-950` | C-18's third element: step 4's mandated `probe-sandbox:` line has no slot in the `control back-sweep:` report form, which asks for "the command and stdout from step 1". It is reported only if the executor volunteers it. | open |
| AA-11 | LOW | `docs/modules/rules/component_design.md:285` | Cites `codebase-audit` → step 6 → "every guard script has an EXECUTABLE invoker"; the target (`codebase-audit/SKILL.md:133`) is a bold lead-in, not a heading. D8.7 RED, the same class as the carried C-15. | applied `908a53a` |
| AA-12 | LOW | `.claude/commands/audit.md:125`, `.claude/commands/maintenance.md:111` | Both state expectations (`origin/main at sNEW`, `unchanged at sHASH`) carrying an `s` prefix their fences never print; live output is `origin/main at af974ec`. The harvester tolerates it (`s?`), but a reader comparing stated-to-actual sees a mismatch on the healthy state — D10.0's own class. | open |
| AA-13 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:145` | `HOOKS=$(grep -hsE '"command" *:' .claude/settings.json)` is line-based: with a single-line or minified `settings.json` the whole file lands in `$HOOKS` and a `permissions.allow` entry counts as an invoker again, re-opening C-33. Confirmed in the sandbox with pretty-printed vs one-line JSON. | open |
| AA-16 | LOW | `.claude/commands/audit.md:548-572` (Agent 3 FILES TO READ) | D7.6 names `.claude/scripts/probe-sandbox.sh` and mandates running it, but the file appears in no agent contract's list — violating the list's own closing rule ("Every file a CHECK below names MUST appear in this list"). Repo-wide: 0 hits for `probe-sandbox` inside any FILES TO READ / INPUTS block. Same class as the open Z-18 (`d16-gate.sh`), so the Z-18 fix should carry this sweep. | open |
| AB-2 | MEDIUM | `.claude/commands/existing_project_adaptation.md` | The COUPLED REFRESH mandate has no slot in the final report; the guard slot offers refreshed/kept and cannot hold a coupled decision | open |
| AB-3 | MEDIUM | `docs/modules/rules/session_rules.md` | "NEVER DOWNGRADE a component's model…" is a banned process anti-pattern with no mechanical self-check (component-design section 6) — see AC-7, which refutes the stated deferral reason | open |
| AB-7 | MEDIUM | `.claude/commands/existing_project_adaptation.md:300` | `model:` is listed under "verified in Steps 2.4-2.8"; no step in that range verifies it | open |
| AC-3 | MEDIUM | commit `adc7481` body vs `assets/docs/` | AB-1..AB-11 were filed only in a commit message. A grep for the ledger row shape over every report returns 0. Three readers are keyed to `assets/docs/audit-*.md` and none reads a commit body: the ID collision check, Phase 3's carry-forward, and trigger (d)'s `OBS` term. AB-2, AB-3 and AB-7 are `open` and in no ledger. Deposited into THIS report's ledger below as the remedy for the instance; the class needs a second form on `maintenance.md` item 10. | open |
| AC-6 | MEDIUM | `.claude/commands/maintenance.md` receipts form vs its self-check | The file authorizes the commit message body as a receipts home; the mandatory receipt self-check extracts a `## Post-change checklist receipts (…)` heading from a REPORT FILE and asserts a `$` line per key. Measured against `adc7481`'s body: 0 bold-form keys, 0 `$` lines, while all 27 keys are present in plain form. One authorized receipts home has a self-check that structurally cannot pass. The push-decay detector carries a written KNOWN LIMIT for this same form; this check carries none. | open |
| AC-7 | MEDIUM | `docs/modules/rules/session_rules.md` → the NEVER DOWNGRADE line | AB-3 was left open on the reason "a check would need value history, not presence". The reason does not survive: its two neighbouring NEVERs ARE mechanically guarded, and value history is what a git repo has — the framework already ships a base-vs-tip set-comparison ratchet (the bash-syntax ratchet). A `git diff` over `.claude/agents` scoped to removed/added `model:` pairs moving down the tier order is the direct analogue. | open |
| AC-8 | MEDIUM | `.claude/commands/maintenance.md` checklist item 6 | `model:` on the 30 shipped agent templates is verified by nobody in THIS repo. Item 6's guard command scans `.claude/skills/` and `.claude/agents/` (this repo has no `.claude/agents/`); its raw-template loop globs the module skills and agents and asserts only `name:`. `examples/` appears in no glob in that command file. All 30 carry it today; a future edit dropping it ships to every bootstrapped project and is caught only there, after the fact — the `criteria_enforcer.md` shape item 6 itself cites. | open |
| AC-9 | MEDIUM | `.claude/commands/maintenance.md` control back-sweep, the written baseline | Baseline written as maintenance 5+27=32; audit 7+28=35; TOTAL 67 (re-measured 2026-09-15). Measured today: 36 / 48 / 84. The ratchet's RED condition tolerates deleting 17 controls and still reading GREEN. Two batches added controls without re-measuring, which the surrounding prose forbids. U-6's stale-threshold class. | applied `280c2f2` |
| AC-10 | MEDIUM | `.claude/commands/audit.md` D6.7 | The mandated install-BLOCK diff states "expected: empty" over a span that legitimately diverges on both paths (bootstrap pre-selection vs EPA already-exists; the two lineage stamps). It can never return empty, so every run hand-adjudicates. The block carries no TWINNED SPAN markers, which is the mechanism the repo already uses for the Step 12.5b criteria; the span boundaries are described in prose, so two auditors extract two different spans. | open |
| AC-11 | MEDIUM | `.claude/commands/bootstrap.md` Step 1.2 | The RECEIVER MANDATE asserts "Steps 2 and 3 ALWAYS confirm receipt, and this hand-off is not complete until they do." Measured: Step 2 returns 0 occurrences of "risk profile", Step 3 returns 0. Every other hand-off in the file plants the clause in the executing step. The mechanical net (the grep plus its Step 15 slot) exists, so this is not a silent hole — but the sentence asserts text that is not there. | applied `280c2f2` |
| AC-12 | MEDIUM | `.claude/commands/bootstrap.md` vs the EPA twin | EPA says "Then RUN it once now"; bootstrap names `check-agent-frontmatter.mjs` four times and never executes it, and Step 15 has no guard verdict slot. Bootstrap is the run that hand-writes `model:` into six agents from templates it adapts, then commits at Step 14.5 — and per component-design section 8 the break cannot manifest in the session that wrote it. At `prototype` the CI floor is skipped, so first detection is session 1. | open |
| AC-13 | MEDIUM | commit `adc7481` vs Cycle governance rule 2 | AA-13 lives at `docs/modules/skills/codebase-audit/SKILL.md:145`. `adc7481` edited that file. Rule 2: "ALWAYS apply a LOW finding when this session already edits its file for another reason, and say so in the commit." It was neither applied nor named; the commit records `class sweep: N/A`. | open |
| AC-19 | LOW | `docs/modules/rules/component_design.md:294` | The citation of `codebase-audit` step 6 quotes a bold lead-in as a section heading (the `step 6` half resolves). SECOND HALF, dimension A: the whole guard-script block is ABSENT from the `.claude/rules/component-design.md` twin (267 lines vs 305), so only the shipped template carries it. | applied `908a53a` |
| AC-20 | LOW | `.claude/commands/maintenance.md`, the push-decay KNOWN LIMIT | The documented failure mode ("the next session sees RED: no recorded origin/main hash") does not occur: the extraction returns a live hash three commit-body sessions stale, so the detector prints a plausible `advanced …`. It FAILS SAFE (over-scans an already-scanned range), but a reader relying on that sentence will not recognize the state they are in. | open |
| AC-21 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:246` | A nested bold marker inside a bolded mandate inverts the rendering: the outer bold closes at the inner marker, leaving a second distinct mandate UNbolded and a dangling trailing marker. Section 6 property 2 (dedicated line) plus a rendering defect. | open |
| AC-22 | LOW | `docs/modules/skills/sprint-proposer/SKILL.md:144-145` | Announces "(expected result stated)" and states neither a command nor a count — it cannot go red. The same file's Step 1 and `session-end` both give a real countable grep. | open |

The LOW ones whose files this cycle did not touch were re-verified MECHANICALLY per Phase 3 item 3
(`git diff --name-only 82f84e2..961244b -- <file>` empty -> still true by construction): AA-9, AA-10,
AA-11, AA-12, AA-13, AA-16, AC-19, AC-20, AC-21, AC-22.

 

### RECOVERED by AD-1 — 41 findings with no ledger row in the previous report

These are re-entered into the chain by this report and must be re-verified by the session that
applies it. They are NOT re-verified here — AD-1 is the finding that they were dropped, and
adjudicating 41 findings is the work of the applying session, not of the run that discovered the
break.

**Re-entered as ROWS, not prose — the whole point of AD-1. Not re-verified here; the applying session adjudicates each.**

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| A-12 | LOW | `validation-orchestrator/SKILL.md:135-136`; `docs/modules/agents/security_reviewer.md:41`; `docs/agentic_engineering_framework.md:991` | W-28's class, not swept: the parenthetical names only code-reviewer's `### Coverage gaps declared:` heading, which security-reviewer also uses. The concepts doc names only "the Coverage Gap Declaration section". | applied `3c24435` |
| A-14 | LOW | `docs/modules/skills/session-log-creator/scripts/create-log.sh:26-27` vs `docs/modules/skills/session-log-creator/SKILL.md:46` | The generator writes the `## Review receipts` slot with no pre-deploy gate line in either the GREEN or the RED form. A log created by the script cannot carry the gate result the skill requires. B-23's and B-26's twin, not swept (847d18f did not touch the script). | open |
| A-15 | LOW | `.claude/scripts/d16-gate.sh:216-221` (`dir` scope); `.claude/commands/audit.md` D16.3b | `dir` matches paths as given. The memory directory can only be passed as an absolute path, so every ancestor directory name is scanned, and an identifier matching a word in the owner's home path hits every file although the contents are clean. Real run: 12 hits over 12 clean files. Probe: absolute path 2 hits, relative 0. D16.3b does not mention it. | open |
| A-16 | LOW | `autonomous-loop/SKILL.md:630-631` vs `:668-671`, `:313-315` | A mid-task marker removal by another session makes the closure check RED. The closure-RED rule then requires writing `State: stopped`, which needs the marker, and the revocation branch says NEVER re-create the marker. No precedence line. Introduced by X-13's fix. | open |
| A-17 | LOW | `autonomous-loop/SKILL.md:675-677`, `:706`, `:736-738` | The C5 digest offers "triage held/deferred" as an owner answer, but neither the resume line (`:675-677`) nor C6's reset (`:736-738`) says whether it resumes or resets. X-19's class, not swept; the receipt's claim about `:675-677` is false. | open |
| A-20 | LOW | `docs/modules/agents/diff_pattern_extractor.md:87-92`; `autonomous-loop/SKILL.md:505-509`; `codebase-audit/SKILL.md:110-112`, `:147-148`; `framework-audit/SKILL.md:81-82` | Five behavioural requirements added by the two batches are buried in note sentences, KNOWN LIMIT paragraphs or sentence tails ("— and ALWAYS file ONE task", "MUST carry its own", "say so, and record the step as ⏭️", "and say so in the report", "say so when one does"). Third consecutive batch pair needing an instruction-style follow-up. | open |
| A-21 | LOW | `autonomous-loop/SKILL.md:353-354`, `:494` vs `:366-373` | The every-subagent resource declaration still omits the MUTATOR/READER class and the scratchpad. `READERS` at C1 `:494` means "never adds a task", a different sense from READER at `:366-373`, so `criteria-enforcer` (writes `pendencias.md`) is classed both ways. B-12's class plus a new defect. | open |
| A-22 | LOW | `autonomous-loop/SKILL.md:421-424`, `:148` vs `:641` | "Emit the lessons block before `/session-end`" exists only under C5 (continuous). Segment-mode continuation and exception stops run `/session-end` with no such step, so their orchestration lessons never reach the log. B-19 D9, not swept. | open |
| A-23 | LOW | `autonomous-loop/SKILL.md:608-627` | The C4 closure check's first-parent traversal reads OK for a `--no-ff` merge whose side branch carries code committed after the counter moved (throwaway case P4); the KNOWN LIMIT (`:626-627`) does not declare it. Introduced by X-9's fix. | open |
| A-25 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 (`:232-233`) | The moratorium self-check counts receipt keys (27) and numbered items (14) only. A new prose-embedded control reads 27/14 (planted), so 742a691's new "expected ≥1 hit in EACH" check and D15.2's new coverage check passed unseen. It does not cover `audit.md`, and batch 2's receipts neither run nor report it. | open |
| A-26 | LOW | `autonomous-loop/SKILL.md:5-19`, `:536`, `:664`; `sprint-proposer/SKILL.md:68-102` | "cancel continuous mode" is read only in the autonomous-loop body. A session without that skill loaded is never told to remove the marker. X-8 residue. | open |
| A-34 | LOW | `.claude/commands/existing_project_adaptation.md:598-600` vs `:856-864` | Step 2.9 takes the refresh/keep decision for `session-log-creator` before the profile exists; the Step 2.9b pairing rule ("at `production`+, ALWAYS PRESENT THEM TOGETHER" with framework-audit and autonomous-loop) then cannot apply to a decision already taken. Introduced by 5ecefa9 (B-11 fix). | open |
| A-35 | LOW | `.claude/commands/existing_project_adaptation.md:1358`, `:1377-1382` | The EPA report has no slot for `.claude/docs/` (created at `:682`) or `.claude/skill-gate/review_reports/` (created at `:819`); bootstrap reports both (`:944`, `:984`). Twin asymmetry, W-18's class. | open |
| A-37 | LOW | `.claude/commands/existing_project_adaptation.md:517`, `:595`, `:862`, `:894`, `:1121`; `.claude/commands/bootstrap.md:113`, `:342`, `:892` | V-17's class, not swept: a rationale clause fused with its ALWAYS in one sentence survives at 8 sites. Two of them (`EPA:595`, `:862`) are invisible to the style regex because `**` precedes the space, and the batch 2 receipt marked the twin "n/a — prose only". | open |
| A-39 | LOW | `.claude/commands/audit.md`, `.claude/commands/maintenance.md:1416-1424`; lineage docs' efficacy-anchor tables | B-11's mother-side half: the maintenance writer must name a measurer, but no `/audit` or `/maintenance` step ever reads an anchor's measured result back (`grep -ciE 'efficacy\|hypothes\|measurer' audit.md` → 3 unrelated hits). An anchor whose measurer silently breaks (A-3) is never noticed from here. | open |
| C-3 | MEDIUM | `assets/docs/audit-2026-09-15.md:1336`, `:1349`, `:1915`, `:1928`; enumerator `assets/docs/audit-2026-09-16.md:380` | A-2's class is not swept. The two persisted receipt generators execute the fail-open np1/np2 (`bash "$NP"`) under `$` lines that still say "run from the repo root", with no erratum. The batch's back-sweep enumerator reads `0` over them because it matches only literal git write verbs inside a fence. Agent 6's stand-in probe: np1 still commits (213 → 218), pushes and rewires origin when its scratch clone fails.  **CUT from the 2026-09-21 backlog batch 1** — three errata were written and all three withdrawn: one was spliced INSIDE the bash fence it annotated (breaking its parse), and the replacement set over-applied to a third generator that uses neither `bash -c "$cmd" 2>/dev/null` nor `$NP`. Needs errata for exactly the TWO generators this finding names. | open |
| C-4 | MEDIUM | `.claude/commands/audit.md:1437-1441` (Phase 3 item 7) vs `:36-38` | Item 7's fence `git add`s only the report, and the rewritten self-check is scoped to the report path. When item 6 has appended a defect-series row to `audit.md`, that row stays uncommitted while the check prints `report committed`. Sandbox: `report committed: sH` then `uncommitted after the fence: M .claude/commands/audit.md`. U-47's residue, adjacent to A-31. | open |
| C-5 | MEDIUM | `.claude/commands/audit.md:18-31`, `:223-236` | Whether a check WRITES — and therefore goes to the sandbox — is decided by whoever runs it, and nothing compares the live repo before dispatch with the live repo after the agents return. A writing check misrouted in place (the incident's own shape) is still caught by nothing: `grep -c fingerprint .claude/commands/audit.md` → 0. Concrete instance: C-13. | open |
| C-6 | LOW | `.claude/commands/audit.md:229-231` vs `:14-19` | A-1's class, not swept to its fetch half. The agent-facing isolation rule's in-place list omits `git fetch`, which only session text authorises, yet D10.0's two endpoint detectors fetch. An agent holding only the rule either sandboxes them (`RED: fetch failed`) or runs a command it cannot classify. | applied `3c24435` |
| C-8 | LOW | `.claude/commands/audit.md:1439-1441` | A same-day re-run whose HEAD is the previous run's `docs(audit): persist` commit to the same file reads `report committed` before this run's report exists (sandbox: `report committed: sH` with no Run 2 content). Phase 3 item 1 (`:1368-1371`) calls same-day runs the normal shape. A-31/A-41 class, one case over. | open |
| C-10 | LOW | `.claude/commands/audit.md:852` (D10.0) vs `.claude/commands/maintenance.md:874-875`, `:1106` and `audit.md:47` | A-42 and A-46 were not swept into D10.0. D10.0 mandates `run --staged` for every writing check, including item 5's pre-fix reconstruction, which maintenance says must NEVER combine `--staged` with a checkout. D10.0's run-every-check mandate also reaches item 10's `--keep` recipe, which `audit.md:47` now forbids agents. | open |
| C-11 | LOW | `.claude/commands/audit.md:46-47`; `.claude/scripts/probe-sandbox.sh:18`, `:45` | The A-46 ban ("agents never pass `--keep`") is a lowercase clause in a parenthetical, inside the Authorized-operations list that `:222-224` says no agent receives. The verbatim agent rule does not repeat it, and the script header offers `--keep`. | open |
| C-12 | LOW | `.claude/commands/audit.md:674-686` (D7.6 steps 1-2) | The steps require a synthetic project in a scratch directory, run in the sandbox, whose working directory is the clone. They never say that the extracted loop resolves `projects/$ARGUMENTS/.claude/agents` relative to the current directory (`bootstrap.md:697`), so the auditor must work out the `cd` and `ARGUMENTS` alone. | open |
| C-13 | LOW | `assets/docs/audit-2026-09-16.md:422` (`control back-sweep:` receipt) | The `$` line runs `git show $r:$f > x` in the working directory. Re-run verbatim from the repo root, it writes an untracked, un-ignored file `x` into the live repo (`git check-ignore x` → rc=1), against the batch's own "NEVER write a file inside the live repository". | applied `9a959e3` |
| C-14 | LOW | `assets/docs/audit-2026-09-16.md:374-376` (`fences:` receipt) | The `$` line is the mandated command with a `| awk` summariser appended, so the pasted stdout (`zero: 0 of 12`) is not the mandated command's literal output — against `maintenance.md`'s "THE `$` LINE MUST BE THE MANDATED COMMAND, BYTE-FOR-BYTE". | open |
| C-15 | LOW | `.claude/commands/maintenance.md:1022`; `.claude/commands/audit.md:137` | Cross-file citations `/audit` → Authorized operations (a bold lead-in; added by 194f44a for A-48) and `maintenance.md` → the `push:` rule (a bullet) point at non-headings, which D8.7 marks RED.  **CUT from the 2026-09-21 backlog batch 2 after three verifier rounds** — the D8.7 amendment (sanctioning a self-declared non-heading citation) is the right idea: two such citations were already live and resolving, and unamended D8.7 flags both. But every implementation left a gap — a citation with a FALSE "where" fell outside BOTH the sanctioned and the RED list, letting a citer silence the check on an unverified assertion. Reverted whole. | open |
| C-16 | LOW | `README.md:144`; `CLAUDE.md:33` | Both structure lines describe `probe-sandbox.sh` as where "checks" (CLAUDE.md: "the ONE place checks, probes and persisted proofs execute") run, dropping the "that WRITES" qualifier that `probe-sandbox.sh:2`, `:12-15` and `audit.md:27` keep — and those forbid read-only scans in the sandbox. | applied `3c24435` |
| C-17 | LOW | `.claude/commands/audit.md:43-44` vs `:15-18` | The unchanged line calling the report commit "the ONLY git operation that WRITES" now contradicts the batch's `git fetch` entry, which says fetch writes remote-tracking refs, `FETCH_HEAD` and objects. | applied `3c24435` |
| C-20 | LOW | `README.md:157` | The entry for `examples/README.md` says "Conventions for creating agents/skills", but that file also defines rules conventions (`examples/README.md:3`, `:167-172`). | applied `3c24435` |
| C-21 | LOW | `.claude/commands/audit.md:226-232`, `:20-30` | The isolation rule sends "every probe" to the sandbox and forbids ever putting a read-only scan (`d16-gate.sh … dir`) there, with no tie-break for a probe that is itself a read-only scan (e.g. a planted `D16_EXTRA` over `dir`, which Agent 1 had to run in place to see `.claude/docs/`). | open |
| C-22 | LOW | `.claude/commands/audit.md:229` vs D16.2 (`:318-320`) | D16.2 harvests "repo URLs", but the verbatim rule bans `git remote`, the only direct source, and neither names a sanctioned read-only route (e.g. `git config --get-regexp '^remote\..*\.url'`). Agent 1 ran `git -C projects/<each>/ remote -v` read-only to fill the class. | open |
| C-25 | LOW | `docs/agentic_engineering_framework.md:455-457`, `:463`, `:469-470` | The block "Why templates reference files that don't exist in this repo" is stale in four places:<br>• it quotes a PRD-sync line the CLAUDE.md template has not carried since `a40a56d`;<br>• it says agents are "created at Steps 7-11", but bootstrap also creates them at 5.7, 5.8 and 12.5;<br>• it lists `.claude/rules/*.md` as "not at bootstrap", but 5.7, 5.8 and 13 create them;<br>• it says logs are "first created at end of session 0", against `bootstrap.md:943` "session logs start from session 1". | open |
| D-3 | LOW | `.claude/commands/bootstrap.md:234` | C-24's twin: Step 3's "already exists" branch verifies three sections and never checks or writes `**PRD version:**`, which `prd_sync_checker.md:35` Check A compares against. Only the create branch (`:236-237`) writes it, and the C-24 receipt's PARALLEL cell covered only that branch. | open |
| D-4 | LOW | `.claude/commands/existing_project_adaptation.md:773-777` vs `:897-953` | A-8's residue: the PHASE-3 RE-CHECK (risk profile, and now the Red/Blue Team decision) is mandated only inside Phase-2 Step 2.9b, and Phase 3 never points back to it (0 grep hits for `2.9b|2.6|profile|re-check`). The moment has no invoker (component-design §9.1). | open |
| D-5 | LOW | `.claude/commands/existing_project_adaptation.md:1215-1365` (Step 5.1) vs `:877-878` | A-8's residue: nothing mechanically checks that `.claude/agents/red-team.md` exists when `code-reviewer.md` carries "red-team is MANDATORY on every diff that touches a money path". Step 5.1's fence has 0 red-team lines although it checks validator and arbitrator existence (`:1246`), so the defect A-8 named can still ship, caught only by prose (§9.2). | open |
| D-6 | LOW | `.claude/commands/existing_project_adaptation.md:494` vs `.claude/commands/bootstrap.md:553-560` | A-8's rewrite lists 5 Red/Blue Team indicators where the twin lists 7: "external API integrations with credentials" and "file uploads from users" are missing. | open |
| D-7 | LOW | `.claude/commands/existing_project_adaptation.md:617-623` (added by 87210b1) | The v2.27.0 migration note has three defects: its headline says "two pairs travel together" and names an ALWAYS for one; it claims an older `session-log-creator` "only lacks the pathspec-RED slot" when the pre-batch slot also lacked the `… K without receipt — deploy blocked` form; and its "ALWAYS offer `session-log-creator` with it" is buried mid-paragraph with no executing moment — `validation-orchestrator` is asked in Step 2.9b's coupled-set question (`:599-603`) while `session-log-creator` is asked in Step 2.9, so the pairing crosses that split (A-34's class). | open |
| D-8 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227-229`, `:231` | A-18's new ALWAYS line (a schema or migration commit also needs an adversarial or data-reviewer receipt) has no output format and no stated-skip form, and the ALWAYS REPORT line covers only the gate's four strings — so whether the deploying session performed the check is structurally invisible (component-design §6 property 3, §9.3). | open |
| D-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:218-219` | "A verdict the list does not know BLOCKS — extend `PASS`, never the reverse" is a behavioural requirement for every project that customises the gate, buried in lowercase at the end of a rationale paragraph (§6 C.4). | open |
| D-10 | LOW | `.claude/commands/maintenance.md:1112-1115` | C-19's fix introduced a defect: `rm -rf "${M%/r}"` now sits in the same executable fence that creates `$M`, so a verbatim run deletes the scratch mother repo before the verifier receives it, and "run it when the verifier returns" lives only in a `#` comment. Shell state does not persist between calls, so a deferred run has no `$M` either.  **CUT from the 2026-09-21 backlog batch 1** — the fix hoisted the deletion’s timing out of a `#` comment, and a verifier proved the mandate unexecutable: `$M` does not survive between shell invocations, so the deferred `rm -rf` is a silent no-op and the temp dir leaks. Closing it needs the recipe to persist the path, not a better sentence. | open |
| D-11 | LOW | `docs/modules/templates/metrics_md.md:21` vs `:14-17` | A-7's class, unswept to the twin template: the CODE-series baseline row reads `COMPLETE` with no `steps:` line although its own prose says Status is followed by the per-step line, and `framework_metrics_md.md:30` was updated. The same self-check gives 1 on the sibling row and 0 on this one. | open |


**A-39 was independently re-verified by Agent 6 this run and is still true:** `maintenance.md:1531`
makes the lineage writer NAME a measurer, but no `/audit` or `/maintenance` step ever READS an
efficacy anchor's result back.

---

## Detailed Findings

### Agent 1 (A, D4, D9, D16) — PASS on all four

**A.** `docs/modules/skills/` 15 folders; `.claude/skills/` 1 (`cross-cutting-analysis`). It is the
only runtime skill, exactly as CLAUDE.md's NOTE prescribes. No skill in `.claude/skills/` without a
`docs/modules/skills/` twin; no non-runtime skill duplicated.

**D4.** README lists 15, disk has 15, every `SKILL.md` `name:` matches its directory. All four count
claims correct: "15 skills"; "12 lifecycle = 1+5+3+1+1+1" sums to 12; "+3 tier-gated" → 15; "7
declare `user` / 8 declare `inline`" verified against frontmatter, and the seven named are exactly
the seven that declare `user`.

**D9.** ~55 enumerated entries all resolve, including `.gitattributes`, both `.claude/scripts/*.sh`,
6 commands, 7 templates, 10 agent templates, 5 rules templates. Nothing significant on disk is
missing from the diagram. Every count comment correct: 6 commands, 15 skills (3 tier-gated), 6
named + "(9 more)" = 15, examples 20/9/11, templates/agents/rules 7/10/5.

**D16 — blocklist from all three sources, counted separately:** 3 project folder names (expanded by
`d16-gate.sh` into full variants plus ≥4-char PARTS at letter boundaries and in CamelCase) + 22
identifiers from project DOCS + 6 source-project CODE identifiers blocklisted, on top of a full
D16.2c cross-grep (369 identifiers harvested from every framework-layer surface × 10,292 harvested
from 945 project sources, caches pruned) and a session-number sweep. Value shapes: 10 EREs. The 30
verbatim copy paths were derived from `bootstrap.md` (threshold "at least 5" — got 30) and excluded.

Cross-grep intersection: **71 shared identifiers, all standard-library / framework-native / canonical
industry vocabulary** (`useState`, `forEach`, `createServerClient`, `localStorage`, `created_at`,
`user_id`, `organization_id`, `organization_members`, plus `done_tasks` which is a FRAMEWORK file
name, and generic e-commerce nouns in archetype examples). **Zero hand-rolled source-project names.**
Session numbers: 18 matches, all legitimate — `Session 0/1/5` (framework terminology), `=S256`
(PKCE `code_challenge_method`), and the `S12`/`Sessão 90` pair which are the D16.2c instruction's OWN
examples.

Surfaces: tracked files `staged` **121 files → 0 hits** · `.claude/docs/` + agent memory `dir`
**19 files → 0 hits** · `docs/modules/**` + `examples/**` double scrutiny `dir` **87 files → 0
hits** · agent memory **16 files, 37 of 37 hex tokens resolve** via `git cat-file -e <hash>^{commit}`
after stripping URLs and UUIDs first · git history `log --all` **236 commits → 2 hits**, both E-1.
`.claude/settings.local.json` EXEMPT, not reported. Hits were located by narrowing scope only
(class-split → per-identifier bisect → 8×30-commit chunks → per-commit), never by printing a matched
line. `git status -sb` reads `## main...origin/main` — zero unpushed commits, so there is no unpushed
range to classify.

### Agent 2 (D5, D11, D13, D14) — PASS on all four

**D5.** 85 unique path strings, **85 resolved**. 42 framework-source paths exist; all 10 specialist
filenames in Step 12.5a's install table exist in `examples/agents/`; 42 project-target paths resolve
under the tier-of-reference rule, each tracing to a creating step. `~/.claude/plugins/installed_plugins.json`
is a user-machine path, exempt. `docs/modules/...` at `:209` is a prose ellipsis, not a path.
**D5.4 naming convention consistent** with no exception: 10 agents, 5 rules, 3 templates all map
underscore → hyphen; skill directories already hyphenated and copied as-is.

**D11.** 15 primary + 12 sub-steps + 1 unnumbered Setup = **28 discrete** (26 at `###` level). The
"15-step pipeline" claim at `framework.md:50` and `:385` is TRUE and correctly qualified; the two
occurrences agree. The pipeline ASCII diagram is accurate — row 5.7's "12 lifecycle skills" matches
15 minus the 3 tier-gated removed by its own `rm -rf`, and matches Step 5.7's 12-name bullet list
and Step 15's 12-name report section. **No undeclared forward reference**: every one names its
receiver (1.1→3/4/13, 1.2c→2/3, 2→5/6/14/14.2, 5.5→14, 5.8→7). Consumption is always backward.

**D13.** Every numbered PRD section bootstrap cites is defined at the exact numbering cited; the
`## Cross-cutting Concerns` heading matches verbatim. INFO only (defined, never read): 1.4, 8, and
several sub-sections read wholesale rather than individually. EPA Phase 3 enumerates every section
of the template with no gap.

**D14.** 16 template references, **16 traced**. The Step 14 write-back string matches verbatim on
both sides. Step 15's final report has a landing slot for every producing step; only the CI floor's
concrete path is unnamed, and it has a status slot, so nothing is owed.

**On 961244b:** both mechanical parity diffs run in place, exit 0 — template JSON fence vs EPA
heredoc byte-identical, and the twinned install spans identical. Step 15's slots are correct as-is:
Step 14 mandates an `ALWAYS REPORT` for hooks only and none for the statusline, and since the
template states the key is display-only with no control reading it, no statusline destination is
owed under §10.

### Agent 3 (D6, D7) — PASS on both, no defects

**D6.** `declarers extracted: 3 of 3`, M derived not typed. code-reviewer 5 gaps, security-reviewer
5, validator 1 (bold form). All 10 gaps have an exact or line-folded phrase match and an install row
on BOTH twins; `visual regression` correctly names both declarers (`code-reviewer or validator`).
Install TABLE diff empty (exit 0, 10 rows byte-identical); TWINNED SPAN diff empty. **D6.9
fourth-surface check: `framework.md:985` names 3, policy §1/§3 names 3 in both twins — match yes**;
two further surfaces checked as a bonus (validation-orchestrator, rules-agents-updater) also name 3.
No broken links, no orphaned specialists.

**D7.** 30 agents checked. 20/20 in `examples/agents/` are FULL on all five Pushy elements; the 9
PARTIALs are all protocol-spawned agents in `docs/modules/agents/` (sanctioned per D7.5, never a
finding); `skill_reviewer` is FULL. **Invariant proof: `files: 10 | output: 0`.** Anti-pattern
instances: none.

`activation chains: 10 verified, 0 broken, 17 info` on EACH twin; **twins agree: yes** — 261 output
lines each, compared line-by-line across all 15 runs, 0 differences; the activation-loop portions are
themselves byte-identical (78 lines each, diff exit 0). **All 14 regression cases PASS on both
twins**, including the substring false-PASS guard (case 1), the digit and dot domains (cases 11-12,
SEEN not silent), the validator-only bold form (case 13) and the empty state (case 14, `none
installed`, exit 0). Planted-specialist probe (D7.8): **not silent** — names the planted agent.
Probe built at a fresh `mktemp -d` inside the sandbox; final line
`probe-sandbox: GREEN — live repo unchanged; command exit 0`.

### Agent 4 (D8, D10) — PASS on both, 4 findings

**D8.** Every orchestrator reference resolves. ~30 cross-section citations adjudicated; all exact or
sanctioned-prefix except `framework-audit` → Q4 (AD-14). D8.8's `codebase-audit` → `code_reviewer.md`
→ `### Periodic review — the triage VERDICTS` is an **exact** heading match.

**D10.** **41 self-checks extracted from both command files and executed verbatim**, each with its
stated expectation beside its actual output. Highlights: moratorium `27` / `14`; harvest
`18 of 27 registered` with no `MISSING FROM LOOP` lines; per-fix pipe count `8/8/8`; fence extraction
12 templates none empty; D16 gate `121 files, 0 hits`, `dir .claude/docs` `3 files, 0 hits`;
raw-template validator `19 validated of 25 globbed, 6 skipped (fenced), OK`; receipts loop 27/27 keys
= 1 with every `$`-line ≥ 1; `bash -n` ratchet `base=0 tip=0 new=0` on all four; defect-series
consistency **`GREEN 18+2=20`**; ID collision `0` for `AD-1`; report-deletions gate no output on
healthy and **5 RED lines on a planted deletion**.

**Gate 1b (new in 961244b) — both directions judged.** GREEN on the live tip and on a clean clone.
RED in the sandbox: case 1 (template gains a key) → `14d13 < "newKey": 1,` exit 1; case 2 (EPA
diverges) → `13a14 > "newKey": 1,` exit 1; case 3 (restored) → exit 0. `settings_json.md` has
exactly one `json` fence and EPA exactly one `SETTINGS` heredoc (38 lines each), so neither `sed`
range can over-capture. **It fires symmetrically and is not decoration.**

**D10.4:** `cross-cutting-analysis` byte-identical — `diff -r` exit 0, `cmp` exit 0, 7985 bytes both.
**D10.6 trigger parity: match yes on all three surfaces** — `README.md:1`, `CLAUDE.md:1`, Phase-0
table `1`; all name the same four events including (d)'s outside-the-loop clause.

Three sandboxed runs, each ending `probe-sandbox: GREEN — live repo unchanged; command exit 0`.

Findings: AC-9 (re-measured, above), AD-7, AD-14, and the push-decay observation discharged in the
header.

### Agent 5 (C, E, D12, D15) — D15 FAIL

**C.** 0 buried-requirement anti-patterns; the mechanical scan for `**(Always|Never|Must|Do not)`
across 5 files returned exactly 1 hit. CAPS-imperative density measured per file. `session-end`
carries a self-consistent self-check: it mandates "exactly 6" report lines and contains exactly 6
`ALWAYS REPORT` mandates. 4 LOW style deviations, all in `validation-orchestrator` (`:93`, `:104`,
`:300`) and `sprint-proposer` (`:216-220`, a `should` for a hard cap stated imperatively 45 lines
earlier).

**E.** Canonical **v2.31.0**; `v2.30.0` residue outside `assets/docs/` = **0**; 961244b's "6 of 6
surfaces" receipt verified independently — exactly 6 current-version occurrences exist, all at
v2.31.0, and they are the same 6 the diff moved. 13 files carry `created: framework-vX.Y.Z` lineage
fields; **none rewritten** — 961244b touched 9 files, zero of which contain one. Migration-note
headings and historical narrative correctly retained. **Mismatches: 0.**

**D12.** 0 violations. Full-set truth-tests (not the sample): 20/20 agents carry `## Output Format`,
`## BOUNDARIES`, `## When this agent is invoked` and explicit `model:`; `## Input` immediately
precedes the output heading in 20/20; 20/20 frontmatter complete on all nine fields; 11/11 rules
carry `domain:`/`applies_to:` and **0/11** carry any of the four agent-kind fields; 9/9 skills use
folder/`SKILL.md`; the README's own "1 of 11" and "5 of 11" checklist claims verify exactly, and the
two sets are disjoint.

**D15.** Every count claim correct — 15 skills (12+3), 10 agents, 5 rules, 7 templates, 20/9/11
examples, 6 commands, 17 dimensions via 6 agents, "The 6 Evolutions", 20 TOC entries, the
`audit.md:353` copy-path claim (grep returns 30, all 5 named roots present). **The banned sense of
"inline" has 0 survivors.** D15.2 coverage: 5 Level headings → 5 README rows; 6 commands in BOTH
README tables. The 6 inaccuracies are AD-4, AD-5, AD-6, AD-11, AD-12, AD-13 — **every one a
scope/coverage claim, not a count.**

### Agent 6 (D17) — FINDINGS, favourable convergence verdict

**D17.1.** 40 commits classified into 10 operation types. Nine have a written home; **post-commit
CONTENT correction does not** (AD-8).

**D17.2.** No new referenced-but-unowned conventions. Every mother-repo behaviour the shipped
templates promise resolves: `evolution_policy.md:140-168` → `maintenance.md` "Upstream intake" +
CLAUDE.md §4; `component_design.md:334` (the slot gate, **correctly described-not-named**) →
`maintenance.md` Gate 4 at `:842`; `framework-audit/SKILL.md:171-174` → same intake section.
**A-39 carried forward and re-verified still true.**

**D17.3.** Every framework-repo-level cadence claim has an owner. The `periodic` hits in
`README.md:194,197` and `framework.md:650,2090` are project-level, owned by `codebase-audit` Step 6,
with the `prototype` gap stated explicitly — out of scope and correctly handled. One residual feeding
AD-16: the harvest mandate names no key.

**D17.4.** Provability table over 14 checks. All provable in both directions except the **`ALWAYS
REPORT` harvester's reported figure**, which is decoration as reported — the stated expectation is
"harvested LOWER than registered", so an unregistered mandate raises `harvested` by 1 and the line
still reads GREEN. The failable half is the unreported membership test, which Agent 6 ran: **0
unregistered, GREEN today.** Agent 6 states plainly that it could not cite Agent 4's D10 verdicts
(parallel dispatch) and that every row is a reachability judgement, not a re-run of D10.

**D17.5.** Of the checklist's controls, the real gates are: component liveness (script, exit-coded),
version bump (residue grep — **it caught `c9355b0`'s missing sixth surface**), the new-component gate
(`skill-reviewer`, blind), Gates 1-4, and **item 10's independent pre-commit verification, named the
strongest**. Not gates: inventory sweep, class/back-sweep/control back-sweep, classification — all
verified by their own author with no expected-result command. Instruction style is a gate ONLY when
the `$` line is run before composing; it **failed 3× in one session** (`c9355b0`, `0bb7949`,
`34a6c97`) and was fixed by `455c299`. The receipts themselves are verified by verification-mode
Part 1 — which reads the report file, **not a commit body** (AD-9).

**D17.6 — convergence.**

```
d3d76cf..79fbd19 shipped:  1 file,   1 +/-   | apparatus: 3 files, 76 +, 19 -
79fbd19..1b0bb67 shipped:  —                 | apparatus: 2 files, 189 +,  9 -
1b0bb67..c9355b0 shipped:  2 files, 38 +, 1 -| apparatus: 4 files, 42 +,  4 -
c9355b0..db6cb1d shipped:  —                 | apparatus: 2 files, 13 +
db6cb1d..455c299 shipped:  —                 | apparatus: 1 file,   9 +,  1 -
455c299..961244b shipped:  2 files, 26 +, 2 -| apparatus: 5 files, 31 +,  7 -

cycle  d3d76cf..HEAD : shipped 65 / apparatus 384 -> shipped share 14.5%
v2.31.0 455c299..961244b: shipped 28 / apparatus  38 -> shipped share 42.4%
```

**The loop is NOT consuming more than it returns, and re-pointing it at the shipped surface is NOT
recommended.** v2.31.0 is **42.4% shipped** — the highest single-batch share in the series bar
`adc7481` — against the **94/6** measured across Runs 5-9 and 52/48 on 2026-09-20. The cycle as a
whole is 14.5%, an order of magnitude above Runs 5-9. **The `b62ef33` exit is working mechanically:**
trigger (d)'s own command returns `PEND=0 OBS=0` → does not fire, correctly, on a batch that changed
a shipped template. Under the pre-`b62ef33` OR it would have fired and this would be the fifth
consecutive self-audit.

**961244b's control addresses the cause and is a gate, not process** — a `diff` with an expected
result, negation-proved both ways by its author and re-run green by Agent 6, which **amended** Gate 1
rather than adding a control (counters unchanged at 27/14).

**The honest counterweight:** of the cycle's 384 apparatus lines, `79fbd19..1b0bb67` (189) and the
three correction commits (≈64) were spent on defects the loop created in its own receipts — three
false receipts in one session, each one commit after the correction that stated the lesson.
`455c299`'s fix is mechanical and hit the cause; 961244b is the first batch under it, at 27/27 keys
with `$` lines. What remains uncovered is the layer above: AD-8 and AD-9.

---

## Recommended Fixes

### 0. LOW BACKLOG — never a session on its own

AD-10, AD-11, AD-12, AD-13, AD-14, AD-15, AD-16, plus the 12 carried LOW findings. **Apply only when
a later session already edits that file**, and say so in the commit (`/maintenance` → "Cycle
governance" rule 2). AD-11/AD-12/AD-13 cluster in two files (`README.md`, `framework.md`) and would
be cheap to take together whenever either is next opened.

### 1. Quick fixes — single-line edits

- **AD-4, AD-5** — delete or correct "the only file it writes" / "writes only its own dated report"
  on both surfaces. One sentence each; they are the same defect twice.
- **AD-2** — replace `bootstrap.md:838`'s restated merge note with a pointer to the template's
  Merge rule (component-design §5: point, never restate).

### 2. Structural fixes

- **AD-1 (HIGH — do this FIRST).** Restore the 41 recovered findings to the ledger AS ROWS and
  re-verify each against current disk. Then fix the mechanism: Phase 3 item 3's carry-forward must
  be satisfiable by the ledger grep, so a prose `carried: N` line with no rows must be RED. The
  natural home is a Phase 3 self-check counting `^\| ID \|` rows against the `carried:` number. This
  is AC-3's family and AA-5's prediction realised at scale — **without it, every future run inherits
  a truncated backlog and no later run can tell.**
- **AD-6** — extend the `Security reviewer:` category to the two twin surfaces its own rule binds,
  including `validator.md`'s Output template and `produces:` field (which then stops saying "14
  categories"). Until then the `:118` mandate is unexecutable.
- **AD-3** — add the receiver paragraph to bootstrap Steps 2 and 3, matching the shape already used
  for Steps 3/4/13.
- **AD-9** — amend Gate 3 or Audit intake item 4 so the receipts SECTION's presence is asserted.
  Agent 6 names this the single highest-value repair available, and it is an amendment, not a new
  control (the apparatus moratorium forbids the latter).
- **AD-8** — widen Audit intake item 8 from MESSAGE to CONTENT, and state the receipt obligation a
  correction commit owes.
- **AC-9 (carried)** — derive the control back-sweep floor at run time rather than writing today's
  figure into prose. Run 4 predicted the written-figure fix would recur within two bumps; the
  finding has instead grown from a 15-control gap to a **20-control** one while unfixed.

### 3. Quality improvements

- **AD-7** — move the TIER-OF-REFERENCE rule into `audit.md` Phase 1's prepended block, so it
  reaches every agent from the file rather than from whoever dispatches.
- The 4 LOW instruction-style deviations Agent 5 listed, when those files are next opened.

### 4. ESCALATED — owner decision required

**None new.** E-1 remains the only item in this class and it already carries an owner decision
(accepted-risk); it is reported above as an OBSERVATION and is not re-opened.

### 5. ACCEPTED-RISK — no action

E-1, M-44, N-53, T-11, T-36, U-48, B-17 — each decided, each cited, none re-opened.

---

## Post-change checklist receipts (`280c2f2`)

**Applied AD-2, AD-3, AD-4, AD-5, AD-6, AD-7, AD-8, AD-11, AD-12, AD-13 and the carried AC-9.
AD-1 and AD-9 stay `open` — both are HALF-FIXED by design, stated in the files themselves. AD-10,
AD-14, AD-15, AD-16 stay `open` as LOW backlog (Cycle governance rule 2).**

**inventory sweep:** 6 surfaces checked, 4 stale claims fixed — README's tree (`.claude/docs/` and
`assets/` both absent), the concepts-doc tree (`.claude/`, `assets/`, `README.md`, `.gitattributes`
absent), README's three `/audit` write-scope assertions, and the concepts-doc `.claude/` gloss.
`docs/modules/README.md` and `examples/README.md` were read and are unchanged — neither enumerates
anything this batch touched.

    $ grep -rin 'only file it writes\|exactly one file\|one dated report' README.md CLAUDE.md docs/ .claude/ | grep -v assets/docs | wc -l
    1

The single survivor is `audit.md:13`, which QUOTES the old text inside its own correction — a rule
naming a shape, not a claim asserting one. Same distinction D16 draws for its own pattern literals.

**instruction style:** 20 added, 6 rewritten, 2 buried — over the staged index. The unit is a line
carrying a CAPS imperative a session must execute. Both `buried` survivors were eyeballed and are
the framework's own sanctioned idiom; ONE REAL burial was caught by this check and fixed pre-commit.

    $ D='git diff --cached -U0'; echo "$($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    20 added, 6 rewritten, 2 buried

**references:** 5 of 5 cited sections resolved. The unit is a distinct section or file path cited by
text this session WROTE; M comes from the mandated command, never from the receipt's own list.

    $ git diff --cached | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l
    5

**fences:** 12 of 12 templates extract non-empty.

    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; L=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$L" -eq 0 ] && echo "ZERO: $t"; done; echo "done"
    done

**isolation:** both scopes GREEN. The `log` form re-runs verbatim after the commit and is the
one that counts; the pattern is derived from disk by the script and never typed here.

    $ bash .claude/scripts/d16-gate.sh log 280c2f2~1..280c2f2
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits
    $ bash .claude/scripts/d16-gate.sh staged
    D16 staged (index blobs + paths): 122 files scanned, 0 hits

**class sweep:** 11 fixes × 4 directions, 4 extra instances found and fixed — README's THREE
`/audit` write-scope assertions (LATERAL from AD-4/AD-5, which the report named on two surfaces
only) and `audit.md:4`'s own "exactly one file" (ADJACENT, seven lines above its own correction, in
the file being corrected — found by verifier round 3, not by this sweep).

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| AD-1 | `280c2f2` | `.claude/commands/audit.md` | ✓ `grep -n 'carried:' assets/docs/*.md` | n/a — no twin | ✓ items 3 and 4 read together | ✓ Phase 2's `carried:` slot swept to admit IDs | Phase 2 template `:1262` |
| AD-2 | `280c2f2` | `.claude/commands/bootstrap.md` | ✓ `grep -rn 'merge the keys'` | ✗ EPA still RESTATES — left open as backlog, its copy is currently accurate | ✓ Step 14 enumeration re-read | n/a | none |
| AD-3 | `280c2f2` | `.claude/commands/bootstrap.md` | ✓ `grep -n 'RECEIVER of'` (3 precedents) | ✓ EPA derives the profile, receives nothing — no twin owed | ✓ moved OUT of the does-not-exist branch after round 1 | ✓ Step 1.2c's grep already covers both files; no key minted | Step 2's branch placement |
| AD-4 | `280c2f2` | `CLAUDE.md` | ✓ same sentence class over 6 surfaces | n/a | ✓ `CLAUDE.md:162` re-read, now consistent | n/a | same edit as AD-5 |
| AD-5 | `280c2f2` | `docs/agentic_engineering_framework.md`, `README.md`, `.claude/commands/audit.md` | ✓ `grep -rin 'one dated report'` | n/a | ✓ `audit.md:4` vs `:11` | n/a | README ×3, `audit.md:4` |
| AD-6 | `280c2f2` | `docs/agentic_engineering_framework.md`, `docs/modules/skills/validation-orchestrator/SKILL.md` | ✓ `grep -c 'Security reviewer'` × 3 surfaces | ✓ `validator.md` correctly has NO slot (ORCHESTRATOR-ONLY) | ✓ twin rule's "one row" → "TWO such rows" | ✓ `produces: 14 categories` verified still correct | the twin rule's stale count |
| AD-7 | `280c2f2` | `.claude/commands/audit.md` | ✓ `grep -ciE 'tier-of-reference'` = 0 at HEAD | n/a | ✓ the isolation blockquote it joins | ✓ lead-in swept from "this isolation rule" to "BOTH RULES BELOW" | none |
| AD-8 | `280c2f2` | `.claude/commands/maintenance.md` | ✓ `grep -n 'amend'` | n/a | ✓ intake items 7 and 9 re-read | n/a | none |
| AD-11 | `280c2f2` | `README.md` | ✓ both trees compared to disk | ✓ concepts-doc tree fixed too (AD-13) | ✓ alignment derived from siblings, not guessed | n/a | same edit as AD-13 |
| AD-12 | `280c2f2` | `README.md` | ✓ `ls .claude/` vs tree | ✓ same | ✓ `.gitignore` checked — row marked git-ignored | n/a | the git-ignored marker |
| AD-13 | `280c2f2` | `docs/agentic_engineering_framework.md` | ✓ `ls` root vs tree | ✓ same | ✓ comment column derived from the file | n/a | one line I mis-aligned and restored |
| AC-9 | `280c2f2` | `.claude/commands/maintenance.md` | ✓ `grep -n 'TOTAL 6'` | n/a | ✓ step 1's enumerator read in full | ✓ its RED condition re-read against it | every written baseline deleted |

    $ awk -F'|' 'NF{print NF-2}' <the header, separator and first data row of the table above>
    8
    8
    8

**back-sweep:** 1 rule, 2 artifacts fixed of 2 found, re-run clean.

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| AD-1: a carried finding is a ROW, never a name in prose | a `carried:` format string that admits only a count | `grep -c 'escalated from \[previous' .claude/commands/audit.md` minus those admitting IDs | 2 | 2 | 0 remaining |

    $ grep -c 'escalated from \[previous.*\[the IDs\]' .claude/commands/audit.md
    2

**liveness:** skipped — no frontmatter touched.

    $ git diff --cached -U0 -- docs/modules/skills/validation-orchestrator/SKILL.md | grep -cE '^[+-](name|description|invocation|model|effort|created|last_eval):'
    0

**negation proof:** N/A — every self-check drafted this session was CUT before commit; none shipped.
Three were written and all three were killed by independent verifiers: a Gate 3 existence assertion
in two successive forms, and a duplicate control enumerator. The one mechanical CLAIM that survived
is AC-9's stated limit, negation-proved by round 3 in the sandbox (deleting 7 Gate command lines
left both file totals byte-identical, confirming the enumerator is blind to indented fences).

    $ git diff --cached -U0 -- .claude/commands docs/modules | grep '^+' | grep -cE '^\+ *(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'
    0

**version:** v2.31.1 applied to 6/6 canonical surfaces — PATCH (corrections adding no new contract).

    $ grep -rn "v2\.31\.0" README.md docs/ .claude/ CLAUDE.md | grep -v 'created: framework-v' | grep -vE 'v[0-9.]+ migration|pre-v[0-9.]+' | wc -l
    0

**classification:** 0 add, 9 substitute, 3 delete, 1 relocate.

    $ n/a — judgement, not measurement

SUBSTITUTE — the stale or false claims (AD-2, AD-4, AD-5 across four surfaces, AD-6's twin-rule
count) and the three trees. Each is strictly better: a false statement replaced by a measured one.
DELETE — three controls, each cut after an independent verifier proved it could not go RED: Gate 3's
existence assertion (two successive forms) and `audit.md` item 4's row-count fence and the duplicate
`count()` enumerator. Justification: a check that cannot fail is decoration, and one that greens on
the very incident it was written for is worse than none.
RELOCATE — the TIER-OF-REFERENCE rule, from the invoking prompt into `audit.md` Phase 1's prepended
block (AD-7). The source points and does not restate: `maintenance.md` keeps its verifier-brief
copy, which is a different moment with a different reader.

**new component:** none this session.

    $ git diff --cached --name-status | grep -c '^A'
    0

**gates:** twin parity PASS (both pairs) | receipt-rows-vs-diff 12/12 | row-count 11 vs 11 | slots
1/1 with one FALSE RED adjudicated aloud.

    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis; echo "exit=$?"
    exit=0

**Gate 4 FALSE RED, adjudicated rather than silently greened:** the harvester returns
``ALWAYS REPORT — `security-reviewer:`` from `validation-orchestrator/SKILL.md:118`, a MODIFIED line
that reads as added. Its slot exists at `:284` spelled `Security reviewer:` — capital and space — so
the key-form grep cannot match it. Not a missing slot; the documented case/spelling false RED.

    $ grep -c '^- Security reviewer:' docs/modules/skills/validation-orchestrator/SKILL.md
    1

**push:** requested — D16 GREEN over 4 unpushed commits → pushed 961244b..5e9bac5 — origin/main at 5e9bac5

**audit:** n/a — PATCH bump; trigger (b) governs MINOR and MAJOR only.

**verification audit:** not triggered — no evidence from outside the loop; this batch's findings are
backlog.

    $ PEND=0; for d in $(grep -L "STATUS.*upstreamed" projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); echo "pending $PEND, HIGH observed $OBS"
    pending 0, HIGH observed 0

**placeholder:** the literal token was used throughout this section and the ledger while the
hash did not yet exist; 28 occurrences were substituted with `280c2f2` in `8b57b0d`.
**THE SUBSTITUTION IS A BLIND TOKEN REPLACE AND IT REWRITES PROSE ABOUT THE CONVENTION TOO** —
it turned this very paragraph into a sentence saying a real hash "will be replaced by the
follow-up commit", which is nonsense. Caught by re-reading the key after substituting; the
residue grep cannot see it, because that grep anchors on the POSITION a hash belongs in and
this was prose. **ALWAYS RE-READ the placeholder key itself after substituting.**

    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-21.md; echo swept
    swept
    $ grep -cE 'applied `280c2f2`' assets/docs/audit-2026-09-21.md
    11

**control back-sweep:** 2 of 2 controls touched were re-run; 0 defects found by the sweep itself —
both were DELETED rather than amended, so the denominator did not move.

    $ for r in HEAD :0; do for f in .claude/commands/maintenance.md .claude/commands/audit.md; do a=$(git show "$r:$f" | awk '/^```/{g=!g;next} g' | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); b=$(git show "$r:$f" | grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' | wc -l); echo "$r $f $a fenced + $b inline"; done; done
    HEAD .claude/commands/maintenance.md 6 fenced + 30 inline
    HEAD .claude/commands/audit.md 10 fenced + 41 inline
    :0 .claude/commands/maintenance.md 6 fenced + 30 inline
    :0 .claude/commands/audit.md 10 fenced + 41 inline

Identical at base and tip — 87 each side. **STATED LIMIT, from round 3's sandbox proof: this
enumerator is blind to INDENTED fences, where all four Gates live**, so a flat total does NOT prove
no Gate command changed. It did not here, verified by reading — but the number alone could not have
told me, and that is now written into the control itself.

**commit correction:** none — this is the batch's first commit.

**defect series:** N/A — baseline mode; no verification run.

**applied-proof:** 12 of 12 IDs resolve to a commit touching their file (form A, file-level via
the per-fix table's `File touched` column), 0 forced to open. The ledger's `Location` cells are
PRE-BATCH line numbers, so the ID-to-file mapping comes from the receipt table, not from them — a
first attempt parsed the ledger cell positionally and resolved 3 of 11, which was a defect in the
MEASUREMENT, not in the fixes.

    $ touched=$(git show --name-only --format="" 280c2f2); sed -n '/^| Fix ID | Commit | File touched/,/^$/p' assets/docs/audit-2026-09-21.md | grep -E '^\| [A-Z]{1,2}-[0-9]+ \|' | while IFS= read -r row; do id=$(echo "$row" | awk -F'|' '{gsub(/ /,"",$2); print $2}'); for f in $(echo "$row" | awk -F'|' '{print $4}' | grep -oE '`[^`]+`' | tr -d '`'); do echo "$touched" | grep -qx "$f" || echo "RED $id $f"; done; done; echo swept
    swept

Line-level proof for the two IDs that changed PRE-EXISTING text, pickaxed on the pre-batch anchor:

    $ git log -S"only file it writes" --oneline 280c2f2~1..280c2f2 -- CLAUDE.md | wc -l; git log -S"writes only its own dated report" --oneline 280c2f2~1..280c2f2 -- docs/agentic_engineering_framework.md | wc -l
    1
    1

**report deletions:** none — every structural count in this report increased or held.

    $ for pat in '^#{1,3} ' '^\| [A-Z]{1,2}-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:' '^```'; do b=$(git show HEAD:assets/docs/audit-2026-09-21.md | awk '/^```/{g=!g;print;next} !g' | grep -cE "$pat"); a=$(awk '/^```/{g=!g;print;next} !g' assets/docs/audit-2026-09-21.md | grep -cE "$pat"); [ "$a" -lt "$b" ] && echo "RED $pat $b -> $a"; done; echo "swept"
    swept

**push decay:** unchanged at the time of the batch; the batch was then PUSHED in this same session — D16 re-run over the published range: GREEN, 4 commits scanned, 0 hits — origin/main at 5e9bac5

**report restore:** none.

**verifier rounds:** 3 rounds, 29 defects reproduced (12 → 9 → 8; blocking 3 → 3 → 2), all 29 fixed
or cut before commit; trigger 6 files; bash -n ratchet GREEN (`audit.md new=0`, `bootstrap.md
new=0`, `maintenance.md new=0`). **EVERY blocking defect across all three rounds was a NEW
MECHANICAL CONTROL this session authored.** Rounds 1 and 2 each repaired the previous round's
control and produced another that could not go RED; round 3 killed the prose explaining them, which
carried two measurably false claims. The batch was then cut to ZERO new controls — which is why
round 3's blockers were fixable by deletion alone and could not spawn a fourth generation.

    $ git diff --cached --name-only -- .claude/commands .claude/scripts docs/modules | wc -l
    6

**receipt generator:** none — receipts typed by hand; every key above was hand-typed. Every `$`
line was run in an invocation that made NO commit, its output read, and only then composed (the
rule shipped in `455c299`).

    $ n/a — no generator script exists to run

**measurer:** N/A — no lineage doc written this session.

    $ ls assets/docs/framework-evolution-upstream-*.md | wc -l; git diff --cached --name-only -- assets/docs | wc -l
    4
    0

## Triage of the open backlog — 2026-09-21, re-verified against disk

**64 open findings were re-read against the CURRENT tree by six independent agents working in
parallel. Verdict: 63 STILL-OPEN, 1 ALREADY-FIXED, 0 NEVER-VALID, 0 CANNOT-TELL.**

This was a READ-ONLY pass. Nothing was fixed by it; its output is the status of the backlog, which
was previously unknown.

**Why it was run.** The mechanical carry-forward test — "the file has not changed since the finding
was filed, so the finding is still true by construction" — resolved only 11 of 70. For the other
57 the file HAD changed, which proves nothing either way. The session that carried them forward
read that as "probably already fixed". It was wrong: of the 64 triaged, exactly ONE was fixed, and
that one was fixed by this same day's batch, incidentally.

| Verdict | N | Meaning |
|---|---|---|
| STILL-OPEN | 63 | the defect is present at the cited place today |
| ALREADY-FIXED | 1 | AC-11, closed by `280c2f2` — see below |
| NEVER-VALID | 0 | no finding mischaracterised its target |
| CANNOT-TELL | 0 | every `Location` cell resolved |

**`CANNOT-TELL = 0` is the load-bearing number.** No finding in this backlog is too vague to act
on. The backlog is actionable in full; it has simply never been worked.

### AC-11 — ALREADY-FIXED by `280c2f2`

Both receivers now name the risk profile: `.claude/commands/bootstrap.md:228-235` (Step 2) and
`:261-265` (Step 3). Verified independently — `git log -S"RECEIVER of Step 1.2's RISK PROFILE"`
returns `280c2f2` and nothing earlier. **AC-11 and AD-3 were the same defect filed by two different
runs.** Fixing AD-3 closed AC-11 without the session knowing, which is the argument for working
this backlog BY FILE or BY CLASS rather than by ID.

### Where the 63 live

    22  .claude/commands/audit.md
    15  .claude/commands/maintenance.md
     9  .claude/commands/existing_project_adaptation.md
     7  docs/modules/skills/autonomous-loop/SKILL.md
     7  .claude/commands/bootstrap.md
     5  docs/modules/skills/validation-orchestrator/SKILL.md
     4  docs/modules/skills/codebase-audit/SKILL.md
     4  .claude/scripts/probe-sandbox.sh
     3  docs/modules/rules/session_rules.md
     3  assets/docs/audit-2026-09-16.md

**37 of 63 sit in the two command files the apparatus moratorium protects** — so most of this
backlog must be worked by amending or DELETING existing controls, never by adding new ones.

### Three classes account for much of it

1. **A citation whose target is a bold lead-in, not a heading** — AA-11, AC-19, C-15 and others.
   `/audit` D8.7 already names this "the recurring defect"; the repo keeps producing it.
2. **A receipt fence that WRITES INTO THE LIVE REPO when re-run** — AA-4 (`$TMP` assigned nowhere),
   C-13 (`> x` in the CWD, confirmed not gitignored), D-10 (the scratch-repo deletion lives only in
   a `#` comment). Receipt fences exist to be re-run by whoever audits; these damage the tree.
3. **A mandate whose destination does not exist** — AA-10, AB-2, A-35, AD-9. This is
   component-design §10, a rule this repo SHIPS and keeps violating at home.

**Working by class should collapse the 63 well below 63.** AC-11 is the existence proof.

### What this says about the loop

Four batches touched these files between the filings and today — including this day's own — and
**not one of them landed on any of these findings.** Every batch was consumed by defects it had
itself created. That is the same conclusion `/audit` D17.6 reached from the shipped-vs-apparatus
split, arrived at from the other direction.

**Method note, for the next run:** three of the six triage agents returned a TALLY that disagreed
with their own table (one wrote "0 still-open" above a table of twelve). The consolidated figures
above were counted over the TABLE ROWS, never over a stated total — `/audit` Phase 0's rule about
counting the score over the FINDINGS rather than a narrated number, reproduced live.

## Meta-observation

**prediction: 2 clauses evaluated — void, void (with the second's consequent observed independently).**

Run 4's prediction was conditional on two fixes being MADE in a particular way. Neither AC-3 nor
AC-9 was fixed — both are carried forward as `open` — so both antecedents are untriggered and both
clauses are **void**, not held and not failed. Clause (2)'s *consequent* nevertheless materialised by
another route: Agent 4 re-measured the control back-sweep baseline and found it 20 controls stale,
worse than when the prediction was written. A written figure went stale without anyone having to
"fix it badly" first — it was enough to leave it alone.

**The recurring class this run: a record that is WRITTEN but not in the shape its READER greps for.**
It appears at four independent sites, and it is not the same as a missing record.

- **AD-1** is the pure case: Run 3 wrote `carried: 81 open` in prose, wrote no ledger rows, and Run 4's
  carry-forward — which reads rows — found 8. Nothing was omitted by anyone; 41 findings simply
  became invisible to the one grep that decides whether they survive.
- **AD-9**: receipts written into a commit body are real receipts, but Part 1 reads the report file,
  so a MINOR bump passed at 9 of 27 keys with zero `$` lines.
- **AD-16**: a harvest mandate written without a key cannot be missed by a loop that iterates keys.
- **AD-6**: a category written on the orchestrator surface but not in the template the validator
  actually fills.

In all four the text exists, is correct, and is addressed to a reader that cannot see it. This is
component-design §10 one level up: §10 says write the destination before the mandate — these say
**write it in the shape the destination's reader parses**. Four of the last five runs' HIGH/MEDIUM
findings are in this family, which suggests the repo's controls have outgrown "is the text present?"
as a test.

**Prediction, stated so it can fail.**

- **(1)** If AD-1 is fixed by restoring the 41 rows WITHOUT adding a check that compares the
  `carried:` number against the count of `^\| ID \|` rows in the same report, the next report that
  carries findings in prose will drop them again, and the drop will again be invisible until a run
  reads the PREVIOUS report's ledger directly. **What would falsify this:** the rows are restored with
  no such check and the next run's `carried:` number still equals its row count; equally, the check
  ships and a drop happens anyway.
- **(2)** If AD-9 is fixed by mandating a receipts section without making its ABSENCE produce output,
  the next batch receipted in a commit body will again score below 10 of 27 keys with no control
  firing. **What would falsify this:** a batch receipted in a commit body after the fix scores full
  keys, or the fix makes absence RED.
- **(3)** AD-4 and AD-5 are one defect on two surfaces. If only one is fixed, the next baseline run
  will report the other as a fresh finding. **What would falsify this:** both move in the same batch,
  or one moves and the next run does not re-file the other.

---

**Report status:** COMPLETE — all 6 agents returned, all 17 dimensions evaluated, no
`probe-sandbox: RED`.
**defect series:** N/A — baseline mode.
**Application status:** PENDING

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan —
**starting with AD-1**, because until the ledger carries the 41 recovered rows, every later run
inherits a truncated backlog and cannot tell that it did.
