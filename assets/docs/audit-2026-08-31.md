# Framework Audit — 2026-08-31

**Version audited:** v2.5.0 (bumped to v2.6.0 by the maintenance session that applied these findings)
**Head at audit time:** `d0b5bcb` · **Dimensions:** 17 · **Agents:** 6
**Result:** 14 PASS · 2 FAIL (D12, D14) · 1 FINDINGS (D17)
**Carried forward:** none — first persisted audit report (Phase 3 introduced by the session that
applied this report's own F-10).

> **The pattern, not the findings.** Eight of the fifteen are rules this repository wrote and did
> not apply to ITSELF. Section 9 of component-design ("activation instructions belong to the
> INVOKER") was promoted the same day and already had four local violations, one of them inside
> the very command that published it. The fourteen passing dimensions check claim-against-fact and
> are structurally blind to a rule nobody invokes: it breaks no reference and mismatches no count.
> The dimension that hunts ABSENCES (D17) found the whole family. The systemic fix is post-change
> checklist item 4 (back-sweep of every process rule promoted in the session), not the eight
> individual repairs.

## Summary matrix

| Dim | Dimension | Category | Agent | Status |
|-----|-----------|----------|:-----:|:------:|
| A | Dual placement | Structural | 1 | PASS |
| C | Instruction style | Quality | 5 | PASS (4 back-sweep observations) |
| E | Version consistency | Currency | 5 | PASS |
| D4 | Skills README vs disk | Currency | 1 | PASS |
| D5 | Bootstrap file references | References | 2 | PASS |
| D6 | Gap vocabulary alignment | References | 3 | PASS |
| D7 | Pushy Description | Quality | 3 | PASS |
| D8 | Orchestrator references | Process | 4 | PASS |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS |
| D10 | Command → skill paths | References | 4 | PASS |
| D11 | Bootstrap sequencing | Process | 2 | PASS (2 shell defects) |
| D12 | Examples conventions | Currency | 5 | **FAIL** |
| D13 | PRD ↔ bootstrap | Process | 2 | PASS |
| D14 | Bootstrap output | Process | 2 | **FAIL** |
| D15 | Doc factual accuracy | Currency | 5 | PASS |
| D16 | Project-information isolation | Privacy | 1 | PASS |
| D17 | Process coverage | Meta | 6 | **FINDINGS** |

## Findings ledger

Status values: `open` / `applied` / `rejected — reason`. Every finding below was re-verified
against disk before being accepted — no agent report was taken at face value.

| ID | Location | Finding | Status |
|----|----------|---------|--------|
| F-1 | `docs/modules/agents/security_reviewer.md:197` | Instructs a subagent to spawn another subagent — impossible per component-design §7. The correct gap-declaration route already existed in the same file. | applied — replaced with a DECLARE-the-gap instruction |
| F-2 | `.claude/commands/bootstrap.md:450, 545` | `mkdir -p` without the `projects/$ARGUMENTS/` prefix required by the file's own line 109 — would create directories inside the framework repo. | applied — prefix added; mechanical check now returns empty |
| F-3 | `.claude/commands/bootstrap.md:602` ↔ Step 15 | Step 14.5 routes commit failure to "Step 15's output", but Step 15's report template had no git slot — a failed initial commit was reported nowhere. | applied — Step 15 gained a mandatory "Initial commit" section |
| F-4 | `.claude/commands/bootstrap.md:233` ↔ Step 14 | `enabledPlugins` handoff had a sender and no receiver; the settings template lacks the key. | applied — Step 14 is now the explicit receiver |
| F-5 | `.claude/commands/maintenance.md` checklist | component-design §8 rule 2 mandates a frontmatter re-check "in maintenance sessions HERE", but the mandate lived in the rule file (the invoked), not the command (the invoker). §9 violation. | applied — checklist item 5 (component liveness) with an explicit expected result |
| F-6 | `docs/modules/skills/validation-orchestrator/SKILL.md:31` | criteria-enforcer invoker: bare sentence, no imperative, no say-the-skip. | applied — imperative + mandatory ran/skipped reporting |
| F-7 | `docs/modules/skills/session-end/SKILL.md:45` | diff-pattern-extractor invoker: same, and the archetype branch above legitimately skips it for 3 of 4 archetypes — the exact case §9 rule 3 exists for. | applied — imperative + mandatory ran/skipped reporting |
| F-8 | `docs/modules/agents/code_reviewer.md:259` | True orphan mandate: a "every 10 sessions" KBP review with no invoker anywhere. | applied — codebase-audit Step 6 is now the invoker; prototype's lack of coverage stated plainly |
| F-9 | versioning | Five bumps executed, no policy documented. `v2.5.0` was 11 commits and two upstream absorptions stale, while `existing_project_adaptation.md` keys migration decisions to that label. | applied — bump policy in maintenance.md + bumped to v2.6.0 across the canonical set |
| F-10 | `.claude/commands/audit.md` Phase 2 | The consolidated report was never persisted, yet cross-session deferral is routine (7 commits apply findings from earlier sessions). | applied — Phase 3 persists this file; stable IDs and carry-forward mandated |
| F-11 | `.claude/commands/maintenance.md:112` | The `upstreamed` back-handoff had no mother-side owner, and Step 0's sweep could not distinguish "not absorbed" from "absorbed, awaiting project-side discharge". | applied — intake step 7 + Step 0 lineage cross-check |
| F-12 | `.claude/commands/maintenance.md:44` | "the periodic `/audit`" — the justification for the entire post-change checklist — had no cadence, trigger, or record. | applied — event-based trigger list in CLAUDE.md |
| F-13 | `examples/agents/` (10 files) | 10 of 20 lacked `## Input`, 9 lacked `## BOUNDARIES` (the anti-bias firewall), trigger heading inconsistent, and `examples/README.md` claimed the conventions were "visible in all examples". | applied — 20/20 now conform; README claim corrected |
| F-14 | `framework-metrics.md` | The 2026-08-31 upstream mandated a `Status` column on a file whose row schema had no template anywhere in the repo. | applied — `framework_metrics_md.md` created and wired into both command twins |
| F-15 | three cosmetics | `.claude/phases/` written by shell redirect with no prior `mkdir -p`; `e-commerce-rules.md` missing `## Inviolable Rules`; `commit/SKILL.md` missing the `(pre-validated)` suffix. | applied |

## Correction to the audit's own D12 characterization

Agent 5 reported that 10 files "use `## When to invoke` instead of the canonical
`When this agent is invoked`", implying one section with inconsistent naming. That is wrong, and
acting on it literally would have merged two distinct sections into a duplicate heading — the
maintenance session's assertion caught it before any file was written.

`## When spawned` is the activation-chain section required by component-design §1 (which reviewer
gap routes here, what context the prompt carries, what main Claude does with each outcome).
`## When this agent is invoked` is the TRIGGER section (which diffs warrant the agent). Ten agents
legitimately carry both. Only the trigger heading had a real naming split (8 vs 3), and
`examples/README.md` never documented that the two sections are different — which is what made the
conflation available in the first place. That documentation gap is now closed.

**Lesson for future audits:** a finding phrased as "X should be renamed to Y" deserves a structural
read of the file before it is applied. Two headings that look like paraphrases of each other may be
carrying different contracts.
