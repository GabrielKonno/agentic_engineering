# Framework Audit Report

**Date:** 2026-09-16
**Framework version:** v2.26.0 (README.md:1)
**Run mode:** `verification (over 5ecefa9 · 28b157a · db70667 · 847d18f)`
**Trigger:** (d). Two `/maintenance` batches applied findings from `audit-2026-09-15.md` since its Run 3 (e4e3fb3): batch 1 `5ecefa9~1..28b157a` (write-back 1a35525) and batch 2 `db70667~1..847d18f` (write-back 22bc80e). Batch 2's receipt records `trigger (d): fires — shipped files 18, HIGH/MEDIUM applied 7`. Batch 2 was also an upstream absorption with a MINOR bump (triggers (a) and (b)). Verification mode adds to baseline, so all 17 dimensions ran as well.
**Post-push D16:** log s742a691..sfdb48bc → 4 commits scanned, 0 hits — origin/main at sfdb48bc
**Dimensions checked:** 17, plus the Part 1 fix-verification pass
**Agents dispatched:** 6. Each agent took its contract verbatim from `.claude/commands/audit.md` by `sed` line range and received the Part 1 mandate and a privacy rule (counts only, invented names in probes). **Agents 1, 3, 4 and 6 were re-dispatched after the incident in A-1.** The re-runs worked in an isolated clone under the session scratchpad: push URL disabled, a refusing pre-push hook, and `projects/` recreated as empty folders with the real names so the gate can derive its blocklist. Agents 2 and 5 are from the first dispatch; both cite evidence at fdb48bc, and both flagged the tree changing under them.
**Part 1 input:** `audit-2026-09-15.md` intact at fdb48bc (Run 3 ledger, the carried rows, both receipts sections). No reconstruction was needed.
**ID series:** `A`. Every single-letter series except `A`, `C` and `D` is already used, and all three collide with dimension names. The exhaustion is filed as A-38. Collision check before writing: `grep -h '^| A-[0-9]' assets/docs/audit-*.md | wc -l` returned 0.

**carried: 48 open + 0 escalated from `audit-2026-09-15.md` (Run 3 application status `PARTIAL — 20 of 26 applied (13 in s5ecefa9, 5 in sdb70667, 2 in s847d18f)`).** Each one was re-verified as still true. Re-read on disk: B-1, B-4, B-15, B-20, V-7, V-8, Z-16 (Agent 2) · W-24 (Agent 3) · V-10, V-18, V-19, W-21, W-22, W-23, W-25, W-26, X-18, Y-12, Z-10, Z-12 (recurred), Z-13, B-25 (Agent 4) · Z-14 (Agent 5) · V-1, V-2, V-3, Z-2, Z-8, Z-9, Z-15 (half), Z-17, Z-18 (Agent 1) · X-15, Y-14, Y-16, Z-19 (Agent 6). **Carried mechanically** (file untouched by `e4e3fb3..fdb48bc`, `git diff --name-only` empty): V-4, V-5, V-6, Z-4, Z-5, Z-6, Z-7 (`.claude/scripts/d16-gate.sh`) · V-14, W-15 (`.claude/commands/bootstrap.md`) · W-27 (`session-end`), W-31 (`rules-agents-updater`); W-29 was re-read (Agent 5) because two of its files were touched.
**OBSERVATIONS — accepted-risk, not re-opened:** E-1, M-44, N-53, T-11, T-36, U-48, **B-17** (closed in fdb48bc; record at the end of `audit-2026-09-15.md`). Agent 1's history pass found 68 vocabulary hits from 8 words in 0 tree files, all pushed. One is a public payment-gateway vendor term (E-1's class); the rest are ordinary Portuguese words, an AI model name and a messaging vendor. None identifies a client.

## Incident during this run — probe commits pushed to the real remote (owner-remediated)

- **What happened.** Between 22:24:37 and 22:26:52 (-0300), a first-dispatch agent (Agent 4) executed persisted negation-proof scripts as `audit.md` D10.0 directs, from the live repo.
  - The scripts' scratch step failed on this host, and they carried on in the live repo (A-2).
  - It committed `2c39e94 rec` (a planted `**push:**` line appended to `audit-2026-09-15.md`) and `eefd614 leak` (a planted one-line `leak.txt`), and **pushed both to `origin/main`**.
  - It then committed `73dabc6 readme`, `5473dec skill` and `1efd0ac broken` locally, and a `reset` returned local `main` to fdb48bc.
- **Damage measured, without printing contents.**
  - `bash .claude/scripts/d16-gate.sh log fdb48bc..eefd614` → `2 commits scanned, 0 hits`.
  - `leak.txt` is 18 bytes and matches no value shape. One of its two tokens appears in 7 tracked framework files; the other appears in 0 project files.
  - The planted `**push:**` line ended in a resolvable commit hash, so it would have misdirected the next run's Phase 0 endpoint.
- **Remediation — owner decision, this session.**
  - `git push --force-with-lease=main:eefd614… origin fdb48bc:main` → `+ eefd614...fdb48bc (forced update)`.
  - `git ls-remote origin refs/heads/main` → fdb48bc.
  - The five probe commits survive only as unreachable objects in the local reflog and were left for git's own expiry.
  - The four agents still running were stopped and re-dispatched isolated.
- **Why the controls did not stop it:** A-1 (no mechanical isolation requirement for probe execution) and A-2 (the persisted scripts fail open).

## Part 1 — Verification ledger for 5ecefa9 · 28b157a · db70667 · 847d18f

NOT-FIXED was settled first, mechanically, by every agent: `git log -L` (or `-S` on the pre-batch text for batch 1) returns a commit inside its batch range for all 31 IDs. **No ID is NOT-FIXED.**

| ID | Verdict | Evidence (file:line — structural) |
|----|---------|------|
| B-2 | CONFIRMED-FIXED | `EPA:872-874` "skip THIS CI floor — never the coupled-set question above", inside item 3 after the tier-labelled question (`:835-841`), pairing rule (`:856`) and financial line (`:866`). `grep 'of the above'` → 0 in EPA; bootstrap's `:469` covers copies only. (Agent 2) |
| B-3 | CONFIRMED-FIXED | `EPA:33-34` "opens EVERY writing fence of Steps 2.9–2.9b (forward reference, flagged)". All 7 fences (`@571, 621, 649, 669, 704, 772, 842`) carry the `case "$ARGUMENTS"` guard; all pass `bash -n`. (Agent 2) |
| B-5 | CONFIRMED-FIXED | `autonomous-loop/SKILL.md:189-193`: an in-segment dependency re-cuts; an out-of-segment unstarted dependency, or a task widening to LARGE or architecture/security, is a STOP. Consistent with the Step 1a gate (`:96-97`) and Bounded re-sequencing (`:329-335`); slots at `:193`, `:767`. (Agent 6) |
| B-6 | CONFIRMED-FIXED | `autonomous-loop:701` digest slot is typed exactly like the final report `:770` (mutator/reader overlap, scratchpad collision). Receivers exist: `session-log-creator:78-79,:103`, `framework-audit:74-75`, `sprint-proposer:270`. (Agent 5) |
| B-7 | CONFIRMED-FIXED | Push-decay detector (`maintenance.md:93-104`) and Phase 0 post-push fence (`audit.md:94-103`) run in throwaway copies with local bare remotes. Planted leak → `2 hits`, exit 1, no mislabel. Failed fetch, missing ref and no recorded hash each → their RED line. A same-clone push reads `unchanged` → `advanced`. A RED line ending in a hash and a mid-sentence hash are both ignored. Side defects filed as A-27 and A-28. (Agent 4) |
| B-8 | PARTIALLY-FIXED | In place: brief includes the report section (`maint:1078`), verifier output pasted (`:1085`), filing tag with date (`:1086`), narrowed exemption (`:287-289`), syntax ratchet (`:1089-1101`) GREEN on both batches with `new=1` on a planted break and a swap, and the scratch-mother recipe (`:1082-1083`). **Not fixed:** the trigger (`:1072-1073`) covers `docs/modules/skills` only. → A-5 (Agent 4) |
| B-9 | CONFIRMED-FIXED | Fence-language, interpreter and cwd rule (`maint:1109`); hand-typed keys named (`:1110`); exemption back to 2 keys (`:284-289`). The first receipt written under the rule does not re-run verbatim — filed separately as A-29. (Agent 4) |
| B-10 | CONFIRMED-FIXED | `sec()` is fence-aware (`maint:319`); over both receipts sections (532 and 550 lines) all 27 keys read 1 with a `$` line. Deletions gate (`:1223-1236`) in a copy: add-only 0; one generator line removed 1; H2 removed 1; whole generator 2; hollowed body 1. (Agent 4) |
| B-11 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT (by db70667) | **(a) Regression:** framework-audit's HYPOTHESIS listing `grep -rlE '^> .*kept as HYPOTHES'` (`framework-audit/SKILL.md:69`) matches autonomous-loop at 5ecefa9..742a691 and **nothing** from db70667 onward, because db70667 rewrapped "kept as" (`autonomous-loop:392`) / "HYPOTHESES." (`:393`). Run in a throwaway project: listing empty, counter `1` → `hypotheses: N/A` while an occurrence is logged. **(b) Mother side:** no step in `audit.md` or `maintenance.md` reads an efficacy anchor's result back; `maintenance.md:1416-1424` only makes the writer name a measurer. → A-3, A-39 (Agents 4, 5, 6) |
| B-12 | FIXED-BUT-CLASS-NOT-SWEPT + INTRODUCED-A-DEFECT | The role was added to the 3b declaration (`autonomous-loop:235-238`). The sibling every-subagent mandate (`:353-354`) still lists only file set, test env and phase docs — no MUTATOR/READER class, no scratchpad (`git log -L 353,354 7a7d1db~1..847d18f` empty). `READERS` at C1 `:494` means "never adds a task", so `criteria-enforcer` is a READER there and a MUTATOR under `:366-373`. → A-21 (Agent 6) |
| B-13 | CONFIRMED-FIXED | Lineage `framework-evolution-upstream-2026-09.md:56` "três ALWAYS" = three dedicated lines `autonomous-loop:380, :384, :385`. (Agent 5) |
| B-14 | CONFIRMED-FIXED | `framework-evolution-upstream-2026-09.md:13-16` replaced; consistent with `:83-91`. No other lineage doc carries the claim (`ficam só` / `only in this record` → 0). (Agent 1) |
| B-16 | CONFIRMED-FIXED | Prior-prediction check (`audit.md:1264-1265`) is scoped to the last run: healthy 1; Run 3 prediction line deleted → 0; Meta-observation deleted → 0; relabelled → 0. (Agent 4) |
| B-18 | INTRODUCED-A-DEFECT (by db70667) | 8 of 9 defects still hold as fixed on disk and by probe. Defect #7 (the HYPOTHESIS check) regressed through db70667's rewrap: `framework-audit:69` lists 1 component at 28b157a and 0 at 847d18f. Batch 2's negation proof never re-ran it. → A-3 (Agent 4) |
| B-19 | FIXED-BUT-CLASS-NOT-SWEPT | 11 of 12 hold, re-run in throwaway clones: RED line with hash skipped; no origin ref → RED; fetch fail → RED; hollowed generator → RED `501 -> 445`; ratchet `new=1` on planted and `new=0` healthy; missing metrics → RED; typed `none` not counted; lineage grep 0; KNOWN LIMIT `maint:114-118`. **D9 not swept:** "emit lessons before `/session-end`" (`autonomous-loop:641`) sits under C5 only; the segment continuation (`:421-424`) and exception stops have no such step. The round-1 #7 anchor regressed (B-18). → A-22, A-3 (Agent 6) |
| B-21 | CONFIRMED-FIXED | `EPA:788-789` sets the label conditionally. The verbatim fence `:773-830` probed with an invented project at production prints "(Step 2.9 coupled set)" for codebase-audit and skill-gate only. (Agent 2) |
| B-22 | PARTIALLY-FIXED + INTRODUCED-A-DEFECT | Of the 13 round-1 defects, 10 are fixed on disk, 2 are partly fixed and 1 is fixed only as printed text:<br>• **#2:** the RED line is printed but the script exits 0.<br>• **#7:** the guard-invoker loop misses settings hooks, `.husky` and `Makefile`.<br>• **#11:** framework-audit has no `steps:` line.<br>Receipt-gate fence extracted verbatim and run in a scratch repo:<br>• unreadable range → RED line, **exit 0**<br>• hash only in a session log → `NO RECEIPT`, exit 0<br>• healthy → GREEN<br>• BLOCK-only receipt → **false GREEN**<br>• hash only in another line's reason text → **false GREEN**<br>• code outside the `-- src` placeholder → **silently not counted**<br>→ A-6, A-7, A-9, A-10, A-11, A-13 (Agent 3) |
| B-23 | FIXED-BUT-CLASS-NOT-SWEPT | Exemption agrees in all three homes (`session_rules.md:281-283`, concepts doc `:975`, `validation-orchestrator:201-203`); self-check unanchored (`session_rules:256`); slot added to `session-log-creator/SKILL.md`. **Not swept:** the script twin `session-log-creator/scripts/create-log.sh:26-27` has no gate-line alternative. → A-14 (Agents 1, 5) |
| B-24 | CONFIRMED-FIXED | `skill-gate/SKILL.md:30-32` points at `evolution-policy → "Component creation gate" → Exemption` (heading `evolution_policy.md:51`, bullet `:59`) and never restates it; split branch `:33-40` limited to non-exempt splits; carve-out `:67-69`, entry-condition rule `:70-72`. No other surface still routes every split to the gate. (Agent 1) |
| B-26 | FIXED-BUT-CLASS-NOT-SWEPT | All three parts landed (`skill-gate:10-11`; `evolution_policy.md:61-62` with receiver `:67-69`; RED slot `session-log-creator/SKILL.md:46`). Not swept: `create-log.sh:26-27` (untouched by 847d18f) generates the slot with no gate line in either form. → A-14 (Agent 1) |
| V-17 | FIXED-BUT-CLASS-NOT-SWEPT | The named line is fixed (`EPA:618-620`, ALWAYS on its own line). The same buried-ALWAYS shape survives:<br>• the regex still hits `EPA:517`, `:894` and `:1121`;<br>• two more are invisible to it because `**` precedes the space: `EPA:595` (from afa8ef0) and `EPA:862` (from 5ecefa9);<br>• the twin carries it at `bootstrap.md:113`, `:342` and `:892`, which the receipt marked "n/a — prose only".<br>→ A-37 (Agent 2) |
| W-28 | FIXED-BUT-CLASS-NOT-SWEPT | `validation-orchestrator/SKILL.md:135-136` says only code-reviewer's format uses `### Coverage gaps declared:`; Pass 1 reads both reviewers (`:133`) and `security_reviewer.md:41` uses the same heading. The concepts doc `:991` still names only "the Coverage Gap Declaration section". → A-12 (Agent 3) |
| X-7 | CONFIRMED-FIXED | `sprint-proposer:100-102` self-check carries the continuous exception; manual-start collision resolved (`:88-90`, `autonomous-loop:730-732`); slot `:97/:187`; Step 4d exists (`:209`). (Agent 4) |
| X-8 | PARTIALLY-FIXED | Trigger-ending mechanism `sprint-proposer:91-93`; arming with `continuous` (`autonomous-loop:726-729`); revocation line (`:667`). **Not fixed:** "cancel continuous mode" is read only inside the autonomous-loop body (`:536, :664, :706, :754`), not in its description (`:5-19`) or in sprint-proposer. → A-26 (Agent 4) |
| X-9 | CONFIRMED-FIXED + INTRODUCED-A-DEFECT | C4 fence (`autonomous-loop:607-619`) run in throwaway repos:<br>• owner commit after the counter → OK<br>• merge HEAD → OK<br>• code after the counter → RED<br>• counter jump 1→3 → RED<br>• window-0 line `:747` reads 0 healthy and 1 without a counter<br>**Introduced:** first-parent traversal makes a `--no-ff` merge whose side branch carries code read OK, and the KNOWN LIMIT (`:626-627`) does not declare it. → A-23 (Agent 6) |
| X-10 | FIXED-BUT-CLASS-NOT-SWEPT | C1 fence (`:489-501`) on a full throwaway install:<br>• healthy → `writers checked: 9`<br>• planted untagged writer → caught<br>• `origin:` removed from sprint-proposer → caught<br>• empty install → `writers checked: 0`<br>**Not swept:** db70667 itself added an untagged write site, `codebase-audit/SKILL.md:123` ("each filed as a task"), inside an already-tagged file. The per-file limit reads it GREEN, against `autonomous-loop:506-508` written in the same commit. → A-24 (Agent 6) |
| X-11 | CONFIRMED-FIXED | Writer `project-md-updater:29-32`; reader `sprint-proposer:60-63`; Step 2 hands the count to `/session-end` (`autonomous-loop:154-157`); every C5 stop writes it (`:682-686`); idle `counts as 0` in C6 (`:715-717`). (Agent 4) |
| X-12 | CONFIRMED-FIXED | Dedicated lines `autonomous-loop:726-729` (ALWAYS ARM … `continuous`) and `:730-732` (NEVER on a manual start); C5 sub-bullet `:672-673` now points at C6; receiver `sprint-proposer:84-90, :100-102`. (Agent 5) |
| X-13 | CONFIRMED-FIXED + INTRODUCED-A-DEFECT | Revocation at a task boundary (`:638`), no digest with a report string (`:664-667`, `:693`), mid-task marker removal handled (`:668-671`). **Introduced:** that removal makes the closure marker line read 0 (`:313-315`) = RED. `:630-631` says a closure RED is a C5 STOP writing `State: stopped`, which needs the marker, and `:670-671` says NEVER re-create the marker. No precedence line. → A-16 (Agent 5) |
| X-19 | FIXED-BUT-CLASS-NOT-SWEPT | `:736-738` covers "continue OR a policy adjustment", but `:675-677` still says resuming "ALWAYS runs C6's reset step first" without naming it, and the digest (`:706`) offers a fourth answer, "triage held/deferred", covered by neither. The receipt's claim that the C5 resume line names "adjust the policy" is false. → A-17 (Agent 5) |
| Z-11 | CONFIRMED-FIXED | `EPA:670-673` "THREE THIS COMMAND WRITES INTO LATER" and "A write — `cp` or a `sed … >` redirect"; the mkdir at `:682` creates exactly the 4 named; no stale `THREE DIRECTORIES\|NEVER CREATED` text. (Agent 2) |

**Score, counted over the 31 FINDINGS (31 rows, no `VERSION` or carried rows in this ledger): 16 of 31 clean, 15 not clean.**
`16 clean · 4 partially-fixed · 8 class-not-swept · 0 not-fixed · 6 introduced-a-defect`

INTRODUCED-A-DEFECT combines with another verdict on B-11, B-12 and B-22 (and with a clean fix on X-9 and X-13), and stands alone on B-18. The not-clean set is therefore 4 + 8 + 3 = 15.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` is the only runtime skill; `diff -r` identical |
| C | Instruction style | Quality | 5 | FAIL | 7 buried imperatives in lines added by the two batches (A-18, A-19, A-20); W-29 carried |
| E | Version consistency | Currency | 5 | PASS | v2.26.0 on every stamp; `created:` fields are lineage, none rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 = 15; 12 + 3; 7 user / 8 inline |
| D5 | Bootstrap file refs | References | 2 | PASS | 44 of 44 bootstrap source paths; EPA 35 + 5 loop variables |
| D6 | Gap vocabulary | References | 3 | PASS | `declarers extracted: 3 of 3`; 10 gaps, all rows in both install tables; D6.9 match |
| D7 | Pushy Description | Quality | 3 | PASS | `activation chains: 10 verified, 0 broken, 17 info` in both twins; regression set 14/14; D7.8 GREEN; `files: 10 \| output: 0` |
| D8 | Orchestrator refs | Process | 4 | PASS | every name resolves; every §heading citation exact or sanctioned |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | no mismatch; counts correct |
| D10 | Command → skill paths | References | 4 | FAIL | references resolve and D10.4 is byte-identical, but D10.0 execution: the persisted proofs fail open (A-2); version residue fires on the healthy state (Z-12); defect-series example stale (Z-10); python check fails verbatim (A-30) |
| D11 | Bootstrap sequencing | Process | 2 | FAIL | 25 steps (15 primary + 10 sub), "15-step" claim qualified and accurate; EPA forward references with no receiver (A-8, A-33, A-34); W-15 carried |
| D12 | Examples conventions | Currency | 5 | PASS | full-set truth tests hold (20 agents / 9 skills / 11 rules) |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | no section read that the template lacks; 1.4, 7.3, 8 ignored (INFO) |
| D14 | Bootstrap output | Process | 2 | FAIL | bootstrap 16 of 16 traced; EPA report lacks two slots bootstrap has (A-35); B-4, B-15 carried |
| D15 | Doc factual accuracy | Currency | 5 | PASS | every count claim accurate; D15.2 coverage (Levels 1-5, 6 commands in both tables) holds |
| D16 | Project-info isolation | Privacy | 1 | PASS | 0 identifying hits on every surface; `.claude/docs/` scanned by the consolidator (below); the one pass over all three sources is RED by construction (V-1) |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | no mechanical isolation for probe execution (A-1); shipped-surface controls have no back-sweep (A-4); loop now produces shipped work (db70667 batch 77/23) |

## Findings ledger — Run 1

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| A-1 | HIGH | `.claude/commands/audit.md:808-813` (D10.0), `:7-32` (Authorized operations), `:201`, `:639-648` (D7.6); `.claude/commands/maintenance.md:867`, `:998-1002` | `/audit` orders agents to RUN every mechanical self-check in `maintenance.md` and `audit.md`, including ones that `git commit`, `git stash` and `git push` (`maint:28`, `audit:1386`, `maint:867`), and to prove controls by planting bad states. It requires no throwaway clone, no disabled push URL, and no before/after check of `HEAD` and `origin/main`. The only "NEVER push" (`:30`) is session-level text that no agent receives (`:201` "Each agent receives its full contract"; the agent contracts carry no read-only language). D7.6's isolation proof, `git status --porcelain` unchanged, cannot see a committed and pushed plant. The Authorized-operations list also omits `git fetch`, which Phase 0 runs, and scratch-directory writes, which D7.6 requires. **Evidence:** this run's incident (section above) — 2 probe commits pushed to `origin/main`. | applied `s194f44a` |
| A-2 | HIGH | `assets/docs/audit-2026-09-15.md:1019-1170` (np1: `:1023`, `:1034-1041`, `:1056-1065`) and `:1592-1704` (np2: `:1596`, `:1625`, `:1644-1651`, `:1680-1681`) | The negation-proof scripts persisted as receipt evidence never check that `mktemp` or the scratch `cd` succeeded, and use `&&` chains and `;` sequences with `set -u` and no `set -e`. When the scratch clone fails, their `git commit … && git push -q origin HEAD:main`, `git reset`, `git add -A` and `git remote remove origin` lines run in the INVOKING repo. The receipts' `$` lines say to run them "from the repo root", and both headers claim "the real repo is untouched". On this host the long scratch path makes the clone fail ("Filename too long"). **Reproduced** by Agents 4 and 6 in stand-in repos with local bare remotes: commits `rec`, `leak`, `readme`, `skill`, `broken`, pushes to the stand-in origin, origin rewired or deleted — the exact commit names of the incident. | applied `s194f44a` |
| A-3 | MEDIUM | `docs/modules/skills/autonomous-loop/SKILL.md:392-393` vs `docs/modules/skills/framework-audit/SKILL.md:69` | db70667 rewrapped the HYPOTHESES blockquote, splitting "kept as" from "HYPOTHESES.", so framework-audit's line-based listing matches 0 components. A production project running the loop gets `hypotheses: N/A` while an occurrence is logged. `git grep` lists autonomous-loop at 28b157a and nothing at 847d18f; restoring 742a691's text lists it again. Regression of B-18 #7 and B-11. | applied `s87210b1` |
| A-4 | MEDIUM | `.claude/commands/maintenance.md` control back-sweep (receipt `audit-2026-09-15.md:1741`) | The control back-sweep enumerates controls in `.claude/commands` only (71). A shipped control in `docs/modules` is never re-run when an edit changes the text it greps, which is how A-3 passed batch 2 as `N/A — numerator 0`. | applied `s87210b1` |
| A-5 | MEDIUM | `.claude/commands/maintenance.md:1072-1073` (item 10 trigger) | The independent-verifier trigger covers `docs/modules/skills` only. A batch that changes only `docs/modules/rules`, `templates` or `agents` reads `0 = N/A`; db70667 changed `session_rules.md` by 73 lines, where A-7 now lives. B-8 residue; Run 3 prediction clause (2) recurred one directory over. | applied `s87210b1` |
| A-6 | MEDIUM | `docs/modules/skills/validation-orchestrator/SKILL.md:186-196` | The pre-deploy receipt gate exits 0 on every verdict, including `RED:` and `NO RECEIPT`. Yet `session_rules.md:284` lets a project wire it into CI, and `session_rules.md:150` itself says a gate is safe only when its exit status is the verdict. Probes A, B1, B2: `exit=0`. | applied `s87210b1` |
| A-7 | MEDIUM | `docs/modules/rules/session_rules.md:233-257` vs `docs/modules/skills/framework-audit/SKILL.md:124`, `:150`; `docs/modules/templates/framework_metrics_md.md:22` | `session_rules` requires a per-step `steps:` line on every periodic mechanism's cadence entry, and its self-check expects exactly 1 match. framework-audit has none (`grep -c 'steps:'` = 0), nor does its metrics template, so every framework-audit entry is RED by construction. The unswept half of B-22 #11. | applied `s87210b1` |
| A-8 | MEDIUM | `.claude/commands/existing_project_adaptation.md:493`, `:866-867`, `:1362` | Step 2.6 defers creating the Red/Blue Team agents until "the PRD (once created in Phase 3)", but no later step returns to it and the report has no slot. On a production-financial project `:866-867` still writes "red-team is MANDATORY on every diff that touches a money path", so the adapted project can mandate an agent that was never installed (bootstrap Step 9 installs it). Pre-existing since f48010f5; in no earlier ledger. | applied `s87210b1` |
| A-9 | MEDIUM | `docs/modules/skills/validation-orchestrator/SKILL.md:193` | The receipt match reads neither the verdict nor the `commits:` field. A commit whose only receipt is `BLOCK` reads GREEN (probe D), and a hash that appears only in another line's reason text reads GREEN (probe F). A false GREEN on a deploy gate. | applied `s87210b1` |
| A-10 | LOW | `validation-orchestrator/SKILL.md:188-189`, `:195` vs `:207`; `session-log-creator/SKILL.md:46` | The gate prints `RED: range … unreadable — nothing checked` and `review receipts: $n commits in range, $k without receipt`. Both report slots mandate `review receipts: RED — range unreadable — deploy blocked` and `… K without receipt — deploy blocked`, strings the fence never prints (U-29's class). The "exactly one `review receipts:` line" expectation has no RED form. | applied `s87210b1` |
| A-11 | LOW | `validation-orchestrator/SKILL.md:183-184`, `:188` | With the `-- src` pathspec placeholder left in, commits outside it silently drop out of `n`, and the pathspec used is never reported. Probe E counted 2 of 3 commits. | applied `s87210b1` |
| A-12 | LOW | `validation-orchestrator/SKILL.md:135-136`; `docs/modules/agents/security_reviewer.md:41`; `docs/agentic_engineering_framework.md:991` | W-28's class, not swept: the parenthetical names only code-reviewer's `### Coverage gaps declared:` heading, which security-reviewer also uses. The concepts doc names only "the Coverage Gap Declaration section". | open |
| A-13 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:135-145` | The guard-invoker loop does not scan `.claude/settings.json` hooks, git hooks (`.husky/`) or a `Makefile`. Scratch probe: 3 guards with real invokers printed `NO INVOKER`, giving `guard invokers: 4 scripts checked, 4 without invoker`. B-22 #7 residue. | applied `s87210b1` |
| A-14 | LOW | `docs/modules/skills/session-log-creator/scripts/create-log.sh:26-27` vs `docs/modules/skills/session-log-creator/SKILL.md:46` | The generator writes the `## Review receipts` slot with no pre-deploy gate line in either the GREEN or the RED form. A log created by the script cannot carry the gate result the skill requires. B-23's and B-26's twin, not swept (847d18f did not touch the script). | open |
| A-15 | LOW | `.claude/scripts/d16-gate.sh:216-221` (`dir` scope); `.claude/commands/audit.md` D16.3b | `dir` matches paths as given. The memory directory can only be passed as an absolute path, so every ancestor directory name is scanned, and an identifier matching a word in the owner's home path hits every file although the contents are clean. Real run: 12 hits over 12 clean files. Probe: absolute path 2 hits, relative 0. D16.3b does not mention it. | open |
| A-16 | LOW | `autonomous-loop/SKILL.md:630-631` vs `:668-671`, `:313-315` | A mid-task marker removal by another session makes the closure check RED. The closure-RED rule then requires writing `State: stopped`, which needs the marker, and the revocation branch says NEVER re-create the marker. No precedence line. Introduced by X-13's fix. | open |
| A-17 | LOW | `autonomous-loop/SKILL.md:675-677`, `:706`, `:736-738` | The C5 digest offers "triage held/deferred" as an owner answer, but neither the resume line (`:675-677`) nor C6's reset (`:736-738`) says whether it resumes or resets. X-19's class, not swept; the receipt's claim about `:675-677` is false. | open |
| A-18 | LOW | `validation-orchestrator/SKILL.md:203-205` | The requirement that a schema or migration commit also carry an adversarial or data-reviewer receipt is buried at the tail of a descriptive paragraph. The gate (`:187-196`) does not check it, and neither `session_rules.md:278-284` nor the concepts doc `:975` mentions it. | applied `s87210b1` |
| A-19 | LOW | `docs/modules/rules/session_rules.md:256-257` | The steps "Self-check (execute and report)" lives in the rule file, and no executing step invokes it: codebase-audit step 5 (`:97-104`) and framework-audit (`:124`) never mention it (component-design §9). | applied `s87210b1` |
| A-20 | LOW | `docs/modules/agents/diff_pattern_extractor.md:87-92`; `autonomous-loop/SKILL.md:505-509`; `codebase-audit/SKILL.md:110-112`, `:147-148`; `framework-audit/SKILL.md:81-82` | Five behavioural requirements added by the two batches are buried in note sentences, KNOWN LIMIT paragraphs or sentence tails ("— and ALWAYS file ONE task", "MUST carry its own", "say so, and record the step as ⏭️", "and say so in the report", "say so when one does"). Third consecutive batch pair needing an instruction-style follow-up. | open |
| A-21 | LOW | `autonomous-loop/SKILL.md:353-354`, `:494` vs `:366-373` | The every-subagent resource declaration still omits the MUTATOR/READER class and the scratchpad. `READERS` at C1 `:494` means "never adds a task", a different sense from READER at `:366-373`, so `criteria-enforcer` (writes `pendencias.md`) is classed both ways. B-12's class plus a new defect. | open |
| A-22 | LOW | `autonomous-loop/SKILL.md:421-424`, `:148` vs `:641` | "Emit the lessons block before `/session-end`" exists only under C5 (continuous). Segment-mode continuation and exception stops run `/session-end` with no such step, so their orchestration lessons never reach the log. B-19 D9, not swept. | open |
| A-23 | LOW | `autonomous-loop/SKILL.md:608-627` | The C4 closure check's first-parent traversal reads OK for a `--no-ff` merge whose side branch carries code committed after the counter moved (throwaway case P4); the KNOWN LIMIT (`:626-627`) does not declare it. Introduced by X-9's fix. | open |
| A-24 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:123` | db70667 added a write site to `pendencias` ("SWEPT — N violations found, each filed as a task") with no `origin:` tag, inside an already-tagged file. C1 reads GREEN through its per-file limit, contradicting `autonomous-loop:506-508`, written in the same commit. X-10's class. | applied `s87210b1` |
| A-25 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 (`:232-233`) | The moratorium self-check counts receipt keys (27) and numbered items (14) only. A new prose-embedded control reads 27/14 (planted), so 742a691's new "expected ≥1 hit in EACH" check and D15.2's new coverage check passed unseen. It does not cover `audit.md`, and batch 2's receipts neither run nor report it. | open |
| A-26 | LOW | `autonomous-loop/SKILL.md:5-19`, `:536`, `:664`; `sprint-proposer/SKILL.md:68-102` | "cancel continuous mode" is read only in the autonomous-loop body. A session without that skill loaded is never told to remove the marker. X-8 residue. | open |
| A-27 | LOW | `.claude/commands/audit.md:95-99`; `.claude/commands/maintenance.md:94-99` | A successful fetch (no prune) from a remote with no `main` keeps a stale `refs/remotes/origin/main`. The post-push fence then reads `origin/main at fdb48bc`, `4 commits scanned, 0 hits`: a false GREEN over an endpoint the remote no longer has. | applied `s194f44a` |
| A-28 | LOW | `assets/docs/audit-2026-09-15.md:1806` | Batch 2's `**push:**` line ends with a parenthetical, not `— origin/main at sHASH`, breaking the hash-ending rule (`maintenance.md:50-54`). Both detectors skip it silently. | applied `s194f44a` |
| A-29 | LOW | `assets/docs/audit-2026-09-15.md:1313`, `:1344`, `:1429`, `:1081`, `:947` | Batch 1's persisted evidence does not re-run verbatim:<br>• a verdict needs `${GEN_LINES:-N}`, which its `$` line never sets;<br>• unescaped backticks in the `references` verdict execute as commands, and the persisted verdict at `:947` is mangled;<br>• np1's C3 RED case now reads 1, because its plant deletes a `Prediction` inside its own pasted fence;<br>• applied-proof form B reads working-tree line numbers, so B-19 now reads empty.<br>(The batch 2 generator fixed the first two.) | applied `s194f44a` |
| A-30 | LOW | `.claude/commands/maintenance.md:960-971` | The raw-template YAML python check, copied byte-for-byte from its list item, keeps the 3-space indent and fails with `IndentationError`; it works only after de-indenting. | applied `s194f44a` |
| A-31 | LOW | `.claude/commands/audit.md:1386-1389` (Phase 3 item 7) | The scoped `git status --porcelain <report>` reads EMPTY both for a committed report and for a report never written, so the self-check cannot tell "committed" from "not persisted". | applied `s194f44a` |
| A-32 | LOW | `.claude/commands/maintenance.md:216` (Cycle governance rule 1) | The HIGH/MEDIUM count reads one applied report; batch 2 disposed of findings from two reports and the check counted 2 where 7 were applied. Harmless while shipped files > 0; a false "does not fire" on an apparatus-only batch. | applied `s194f44a` |
| A-33 | LOW | `.claude/commands/existing_project_adaptation.md:519` vs `:962` | Step 2.7.1 matches domains from "the existing/retroactive PRD", while the receiver table at `:962` says Step 2.7.1 "is codebase-derived and runs before the PRD exists". An unflagged forward reference contradicting its own table. | applied `s87210b1` |
| A-34 | LOW | `.claude/commands/existing_project_adaptation.md:598-600` vs `:856-864` | Step 2.9 takes the refresh/keep decision for `session-log-creator` before the profile exists; the Step 2.9b pairing rule ("at `production`+, ALWAYS PRESENT THEM TOGETHER" with framework-audit and autonomous-loop) then cannot apply to a decision already taken. Introduced by 5ecefa9 (B-11 fix). | open |
| A-35 | LOW | `.claude/commands/existing_project_adaptation.md:1358`, `:1377-1382` | The EPA report has no slot for `.claude/docs/` (created at `:682`) or `.claude/skill-gate/review_reports/` (created at `:819`); bootstrap reports both (`:944`, `:984`). Twin asymmetry, W-18's class. | open |
| A-36 | LOW | `.claude/commands/bootstrap.md:172` | The ceremony matrix says "CI floor at t=0 (Step 14)"; the step that creates it is 14.2 (`:825`), which the Step 15 slot `:981` cites correctly. | applied `s87210b1` |
| A-37 | LOW | `.claude/commands/existing_project_adaptation.md:517`, `:595`, `:862`, `:894`, `:1121`; `.claude/commands/bootstrap.md:113`, `:342`, `:892` | V-17's class, not swept: a rationale clause fused with its ALWAYS in one sentence survives at 8 sites. Two of them (`EPA:595`, `:862`) are invisible to the style regex because `**` precedes the space, and the batch 2 receipt marked the twin "n/a — prose only". | open |
| A-38 | LOW | `.claude/commands/audit.md` Phase 2 → "ID allocation" | The single-letter scheme is exhausted: every letter except `A`, `C` and `D` is used (`E` by E-1), and all three collide with dimension names. The section says to skip letters that read as digits and `S`, and has no rule for exhaustion. This run chose `A` by judgement. | applied `s194f44a` |
| A-39 | LOW | `.claude/commands/audit.md`, `.claude/commands/maintenance.md:1416-1424`; lineage docs' efficacy-anchor tables | B-11's mother-side half: the maintenance writer must name a measurer, but no `/audit` or `/maintenance` step ever reads an anchor's measured result back (`grep -ciE 'efficacy\|hypothes\|measurer' audit.md` → 3 unrelated hits). An anchor whose measurer silently breaks (A-3) is never noticed from here. | open |
| A-40 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 | "Migrating prose controls into `.claude/scripts/` with selftests" is recorded as "owner-approved, not yet done" with no owner, trigger or step; `git diff --stat c2c226f..fdb48bc -- .claude/scripts` is empty. An aspirational mechanism (D17.3). | rejected — the owner withdrew the promise on 2026-09-20; the moratorium now points at no next step, so there is no aspirational mechanism left to own |
| A-41 | LOW | `.claude/commands/audit.md` Phase 3 item 7 self-check (as staged for this batch) | The rewritten self-check read `report committed` for a report NOT yet written when a same-day re-run followed a write-back commit to that file (last report commit = HEAD). A-31's class, one case over. Fixed: the HEAD commit's subject must be the `persist` form. [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-42 | LOW | `.claude/commands/maintenance.md` item 5 negation-proof line (as staged) | "Reconstruct the pre-fix state inside `run --staged` (check out `<commit>~1` there)" aborts: the staged files are dirty in the sandbox. Before the commit the pre-fix state is plain HEAD without `--staged`. [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-43 | MEDIUM | `.claude/commands/audit.md` D10 report format (as staged) | "each inside `probe-sandbox.sh run`" contradicted D10.0 and Authorized operations (read-only checks run in place). Followed literally, the push-decay detector in the sandbox reported the live LOCAL `main` as `origin/main` with a GREEN verdict. [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-44 | MEDIUM | `.claude/scripts/probe-sandbox.sh` (as staged) | Only the push URL was disabled. A clone of a local path names the live LOCAL branches `origin/*` and keeps the live path as fetch URL, so any `origin/*` read or fetch in the sandbox read the live unpushed branches as the remote. Undeclared in KNOWN LIMITS. Fixed: live remote-tracking refs mirrored, fetch URL cut; new selftest case. [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-45 | LOW | `.claude/scripts/probe-sandbox.sh` exit 126/127 verdict (as staged) | A command that RAN and exited 126/127 was reported "nothing was run". A false fail-closed wording, never a false GREEN (the live comparison runs first). [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-46 | LOW | `.claude/commands/audit.md` Authorized operations + Phase 1 agent rule (as staged) | "the sandbox's clone … is deleted when the command ends" was false with `--keep`, which the agent rule offered, and nothing told an agent to delete a kept clone. Fixed: agents never pass `--keep`. [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-47 | LOW | `.claude/commands/audit.md` Phase 1 closing line + Phase 3 `Report status:` (as staged) | INCOMPLETE was told to name "the RED line's parts", but a fail-closed RED names a reason, not parts (cross-surface question a). [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-48 | LOW | `.claude/commands/maintenance.md` item 6 (as staged) | `/audit`'s "NEVER run a READ-ONLY scan inside the sandbox" had no counterpart in `/maintenance`, whose items run the D16 gate beside sandboxed proofs (cross-surface question b). [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |
| A-49 | LOW | `.claude/commands/audit.md` Authorized operations (as staged) | Three added references pointed forward (Phase 0; D7.6 and D10.0; Phase 3) without the flag the file uses elsewhere (cross-surface question c). [filed by the /maintenance session on 2026-09-19, from its independent pre-commit verifier] | applied `s194f44a` |

**Carried forward from `audit-2026-09-15.md` (re-verified, all still true; rows stand unchanged with status `open` in that file):** B-1, B-4, B-15, B-20, B-25 · V-1, V-2, V-3, V-4, V-5, V-6, V-7, V-8 (2 of 3 parts), V-10, V-14, V-18, V-19 · W-15, W-21, W-22, W-23, W-24, W-25, W-26, W-27, W-29, W-31 · X-15, X-18 · Y-12, Y-14, Y-16 · Z-2, Z-4, Z-5, Z-6, Z-7, Z-8, Z-9, Z-10 (the example now reads `GREEN 14+2=16`), Z-12 (recurs: `OLD=2.25.0` → 1, an EPA lineage label), Z-13, Z-14, Z-15 (maintenance half), Z-16, Z-17, Z-18, Z-19. **48 findings.**

**Pointers from Part 1 (the originals keep `applied sHASH` in `audit-2026-09-15.md`):** B-8 → A-5 · B-11 → A-3, A-39 · B-12 → A-21 · B-18 → A-3 · B-19 → A-22, A-3 · B-22 → A-6, A-7, A-9, A-10, A-11, A-13 · B-23 → A-14 · B-26 → A-14 · V-17 → A-37 · W-28 → A-12 · X-8 → A-26 · X-9 → A-23 · X-10 → A-24 · X-13 → A-16 · X-19 → A-17.

**INFO, not findings:** the bootstrap Step 12.5b loop comment `:694` still says "candidate replacement…", and the twinned-span header says "EPA Step 5.1" while the span sits in 4.6.5 (already noted at `audit-2026-09-14.md:159`). The D6.7 install-block diff has two expected path-specific hunks while the twinned span diff is empty — that is W-24, carried. `28b157a`'s subject names "3 imperatives" and no finding count.

## Recommended Fixes

0. **LOW BACKLOG — never a session on its own.** A-10..A-38 (except A-37's fence-adjacent sites) and A-39, A-40 are applied only when a later session already edits their file (`/maintenance` → "Cycle governance"). By file: `validation-orchestrator` A-10, A-11, A-12, A-18 · `autonomous-loop` A-16, A-17, A-21, A-22, A-23, A-26 · `codebase-audit` A-13, A-24 · `session-log-creator/scripts/create-log.sh` A-14 · `session_rules` A-19 · several skills A-20 · `d16-gate.sh` A-15 · `maintenance.md` A-25, A-27, A-30, A-32, A-40 · `audit.md` A-27, A-31, A-38, A-39 · `audit-2026-09-15.md` A-28, A-29 · EPA A-33, A-34, A-35, A-37 · `bootstrap.md` A-36, A-37.
1. **FIRST — the isolation gap, before any further self-check or proof is executed by anyone (A-1, A-2).** Make probe execution mechanically isolated: a throwaway clone with its push URL disabled, a `set -e` or an explicit toplevel assertion before the first write, and a before/after comparison of `HEAD` and `origin/main` in the live repo. Put the requirement in the agent contracts that D10.0 and D7.6 dispatch, not only in the session's Authorized-operations list. Then neutralise the persisted np1/np2 scripts in `audit-2026-09-15.md` with an erratum, never by silent rewrite. A proof that can write to the repo that runs it should not be persisted as runnable text.
2. **Structural — shipped controls broken by shipped edits (A-3, A-4, A-5, A-7).** Restore the HYPOTHESIS listing (A-3), then extend the control back-sweep (A-4) and the item 10 trigger (A-5) to all of `docs/modules/`, so the class is covered, not just the instance. Give framework-audit its `steps:` line or scope the rule (A-7).
3. **Structural — the deploy gate (A-6, A-9)** and **the EPA red-team delivery path (A-8).**
4. **Quality improvements:** A-20 and the carried W-29.
5. **ESCALATED — owner decision required:** none open. The incident remediation (force-with-lease of `origin/main` back to fdb48bc) was decided by the owner this session.
6. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48, B-17.

## Meta-observation

`prediction: 2 clauses evaluated — void, void`
- **(1)** "Receipt controls amended without persisting a RED and a GREEN run each → at least one unprovable" → **void.** The antecedent did not arise: batch 1 persisted both runs for all 9 amended controls (`audit-2026-09-15.md:979-1017`). The falsifier did not arise either: every receipt control re-run at HEAD goes both RED and GREEN (Agents 4, 6). The one control that decayed (the HYPOTHESIS listing) is a shipped control outside the clause's scope, broken later by a text edit. **The persisted runs were themselves the hazard**: they were stored as runnable scripts that fail open (A-2).
- **(2)** "Item 10's trigger still excluding `docs/modules/` → a rule naming a field or role no step produces" → **void as stated.** B-8 widened the trigger to `docs/modules/skills`, so the antecedent is false. The class recurred exactly one directory over, in `docs/modules/rules/session_rules.md` (A-7) and in skills the verifier did see (A-21, A-24).

**The recurring class this run saw: EVIDENCE AND CONTROLS ARE PERSISTED AS EXECUTABLE TEXT WITH NO GUARD ON WHERE THEY EXECUTE OR WHAT THEY MATCH.** The class has two faces.
- **Where they execute.** The negation proofs that make a control provable were saved as runnable scripts. They inherit the privileges of whoever re-runs them, the `/audit` contract orders them re-run, and nothing confines the run. One re-run in this very audit pushed to the real remote (A-1, A-2), and a second persisted receipt does not reproduce verbatim (A-29).
- **What they match.** Line-anchored greps written into shipped skills are controls, but nothing tracks them as controls. A reflow of the text they read silently turns them GREEN or N/A (A-3), and no back-sweep reaches `docs/modules/` (A-4, A-5).

Both faces share the property that closed Run 3's class: the controls in `.claude/commands/` are now provable by negation (0 of 9 receipt controls unprovable). The defects moved to the places those negation proofs do not reach: the proofs' own execution context, and the shipped surface.

The loop is finally producing shipped work (D17.6: the db70667 batch split 501 shipped to 151 apparatus lines, the first shipped-majority batch since ecace7a), and **14 of the 15 not-clean verdicts in this run are on shipped surfaces or their delivery twins.** More shipped work means more shipped defects, which is the expected trade and not a regression.

**Prediction, stated so it can fail.**
- **(1)** If the next batch fixes A-3 by rejoining the line without extending the control back-sweep (A-4) to `docs/modules/`, the next verification will find at least one more line-anchored control in a shipped skill or rule whose target text that batch edited and that no longer matches, or matches on the wrong state. **What would falsify this:** the back-sweep is not extended and no such control is found; equally, the back-sweep is extended and one is found anyway.
- **(2)** If A-1 and A-2 are fixed by prose alone, with no guard the script itself executes before its first write (such as `set -e` or a toplevel assertion), then re-running any persisted proof in a stand-in repo with an unusable `TMPDIR` will still produce commits in the stand-in. **What would falsify this:** a prose-only fix whose persisted proofs leave the stand-in unchanged under that probe.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan.

## Detailed Findings

Condensed per agent. Each agent's Part 1 rows and every numbered finding above carry its evidence. Below are the mandatory report slots and measurements that did not fit the ledger.

### Agent 1: Structural Sync (isolated re-run)
- **[A]** PASS — `.claude/skills/` 1 folder, also in `docs/modules/skills/`, `diff -r` identical.
- **[D4]** PASS — README lists 15, disk 15; "12 lifecycle + 3 tier-gated" and "7 `user` / 8 `inline`" correct.
- **[D9]** PASS — no entry missing either way. Counts: 6 commands, 7 templates, 10 agents, 5 rules, 15 skills, examples 20/9/11. `.claude/docs/` and `settings.local.json` are gitignored, so absent from the clone by design.
- **[D16]** PASS (Agent 1 reported PARTIAL for `.claude/docs/` only; the consolidator closed it).
  - **Blocklist, three sources counted separately:**
    - 3 project folder names.
    - 22 identifiers harvested from project DOCS (domains, handles, an e-mail local part, 2 infra ids, 4 phone numbers, person/business/place names, remote owner/repo parts); a further 345 capitalised vocabulary words absent from the tree were harvested separately.
    - 73 source-project CODE identifiers (D16.2c) plus 261 session numbers.
    - Copy-path exclusion derived from `bootstrap.md`: 5 roots (expected ≥5).
  - **Surfaces:**
    - **Tracked files (`staged`, 117 files incl. paths):** folder names 0; plus doc identifiers 0; plus value shapes 1 (`examples/agents/secrets-scanner.md:83`, a detection example, legitimate under D16.2b); plus the 73 code identifiers 285 (standard vendor, library, schema or framework-originated names — V-1's over-match); vocabulary 0.
    - **`.claude/docs/`** (consolidator, read-only in the live repo, relative path per A-15): 3 files. Folder names 0; doc identifiers + value shapes 0; code identifiers 2 hits, from 1 identifier that also appears in 14 tracked framework files (shared vocabulary).
    - **Agent memory** (12 files): folder names, doc identifiers, value shapes and code identifiers 0 each. Vocabulary 12, all from one ancestor path word (A-15). 20 hash tokens outside UUIDs and URLs, 20 resolve in this repo.
    - **Git history** (`log --all`, 210 commits + 13 tags, identical in clone and live repo): folder names 0. Doc identifiers + value shapes (without the connection-string shape) 0. The mandated one pass over all three sources 509, RED by construction (V-1). Vocabulary 68 (OBSERVATIONS above). Batch range `e4e3fb3..fdb48bc` (9 commits): 0.
  - **Unpushed vs pushed:** 0 unpushed commits at dispatch.
  - **OBSERVATION:** the probe range `fdb48bc..eefd614` → `2 commits scanned, 0 hits` (consolidator).
- **Isolation:** clone status empty; live repo status empty, HEAD fdb48bc.

### Agent 2: Bootstrap Integrity (first dispatch; evidence at fdb48bc, EPA lines 1-1486 unchanged by the probe commits)
- **[D5]** PASS — bootstrap 44 of 44 framework-source paths resolve, 72 project-relative targets each with a producing step, 10 specialist examples and 11 rules templates exist. EPA 35 of 40 as written plus 5 loop variables (`:627, :632, :641, :658, :689`). D5.4 underscore→hyphen consistent (`tr '_' '-'` in EPA).
- **[D11]** FAIL — 15 primary + 10 sub-steps (0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5) = 25, plus 12.5a/12.5b and Setup. Claim "15-step pipeline (… 15 primary steps …, and sub-steps …)" (`docs/agentic_engineering_framework.md:50`, `:385`) is qualified and accurate. Flagged forward references all have receivers; unflagged: A-8, A-33, A-34. Twinned span and loop diffs rc 0.
- **[D13]** PASS — bootstrap reads Cross-cutting Concerns, 1.1-1.3, Changelog, 2.1-2.3, 3.x, 4, 5, 5.1-5.4, 6, 7, 9 — all defined; ignored (INFO): 1.4, 7.3, 8. EPA Phase 3 (`:892-933`) covers every top-level section.
- **[D14]** 16 unique template references, 16 traced; bootstrap Step 15 (`:929-990`) lists every created file. EPA twin: B-4, B-15 carried; A-35.
- **Probes:** B-1 (prototype with differing leftover members → 0 lines), B-15 (`prototype` and the typo `production_financial` → nothing), B-20 (internal-tool with leftover framework-audit → no line). V-7: 4 of 24 EPA fences and 6 of 18 bootstrap fences do not expand the placeholder.

### Agent 3: Activation Chain (isolated re-run)
- **[D6]** PASS.
  - **Declarers:** code-reviewer (accessibility, performance, concurrency, visual regression, data integrity); security-reviewer (static analysis, secrets coverage, federation protocol, compliance, infrastructure security); validator (visual regression, bold form `validator.md:149`).
  - **Gap → specialist:** all 10 gaps resolve to their specialist by exact phrase, with a row in both install tables.
  - **Install-block diff (D6.7):** exit 1, two hunks — the lead-in wording, and `created:` lineage `s0 (bootstrap …)` vs `adaptation (…)`. The table diff and the twinned span diff are empty (W-24).
  - **Fourth surface (D6.9):** `agentic_engineering_framework.md:985-986` names 3 declarers, policy names 3, match yes.
  - `declarers extracted: 3 of 3` (M derived = 3). Gap sources match both policy twins (§1..§3 twin diff rc 0).
  - Broken links: none. Orphaned specialists: none.
- **[D7]** PASS.
  - **Descriptions:** 30 agents; 20 examples + skill-reviewer COMPLIANT. arbitrator, blue-team, code-reviewer, red-team, security-reviewer, validator, criteria-enforcer, diff-pattern-extractor and prd-sync-checker are PARTIAL, sanctioned by D7.5.
  - **Activation chains:** synthetic project `scratchpad/agent3b/fw/projects/synth` (30 agents; clone `git status --porcelain` count 0 before and after); the two loops are byte-identical (49 lines).
    - bootstrap twin: `activation chains: 10 verified, 0 broken, 17 info`
    - EPA twin: `activation chains: 10 verified, 0 broken, 17 info`
    - `twins agree: yes` (counts and all 17 INFO strings)
  - **Regression set:** 14 of 14 as expected in both twins, exit 0 each.
    - Case 1 BREAK on `coverage gap`; case 6 BREAK on the bogus second gap.
    - Cases 9 and 10 INFO, no BREAK.
    - Cases 11 and 12 BREAK, seen, not silent.
    - Case 13 VERIFY via the validator bold form; case 14 `activation chains: none installed - …`, exit 0.
  - **Planted specialist (D7.8):** `BROKEN CHAIN: planted-specialist declares 'quantum entropy gap' but no declaring component declares it` and `INFO: planted-plain has no gap phrase and is not a shipped example …` → `10 verified, 1 broken, 18 info` (both twins). GREEN.
  - **Invariant (D7.5):** `files: 10 | output: 0` — GREEN.
  - Anti-pattern instances: none.
- **B-22 receipt-gate probe table:** A exit 0 on RED · B1/B2 `NO RECEIPT`, exit 0 · C GREEN · D BLOCK-only false GREEN · F reason-text false GREEN · E `2 commits in range` of 3.
- **Isolation:** clone status empty, HEAD fdb48bc; the live repo was never read.

### Agent 4: Orchestration & Commands (isolated re-run)
- **[D8]** PASS.
  - **Names:** every skill and agent named by session-end, validation-orchestrator, sprint-proposer, autonomous-loop, skill-gate, codebase-audit, framework-audit and cross-cutting-analysis resolves. Unresolved hyphenated words are prose (`mid-loop`, `non-loop`, `meta-audit`, `meta-loop`, `task-runner`).
  - **Cross-section citations — exact:**
    - "Exception stops", "Before Implementing", "Reasoning depth mechanisms (complementary)", "Final report"
    - D8.8: `### Periodic review — the triage VERDICTS` (`code_reviewer.md:277`)
  - **Sanctioned prefix / parenthetical / ordinal:**
    - "Between Tasks", "Autonomous loop watchdog", "Execution proof", "Cadence integrity", "Deploy gates"
    - "MODEL SWITCH entries" (ordinal), "Component creation gate", "Continuous mode" (+ C5/C6)
    - maintenance.md and audit.md internal sections
  - **RED:** none.
- **[D10]** FAIL.
  - **References:** `cross-cutting-analysis` referenced by `prd_planning.md:174` and `prd_change.md:141` resolves; D10.4 byte-identical yes.
  - **Section citations:** maintenance.md sections, component-design §5 §6 §8 §9 (both twins), Phase 0 (`:44`) and Phase 3 (`:1311`), and "Execution proof" (`session_rules.md:125`) all resolve. The CLAUDE.md trigger letters are a bold lead-in (`:149`), cited as such.
  - **Trigger-list parity (D10.6):** match yes, (a)-(d) plus the exit note (`audit.md:74-75`).
  - **D10.0, executed in throwaway copies (pushes replaced or blocked) — every check matched its stated expectation except:**
    - raw-template python check `maint:960-971` — `IndentationError` verbatim, `19 of 25, 6 skipped, OK` de-indented (A-30)
    - version residue `maint:1147-1148` — `OLD=2.25.0` → 1 on the healthy state (Z-12)
    - defect-series check `audit:1373-1379` — stated `GREEN 9+2=11`, actual `GREEN 14+2=16` (Z-10)
    - report commit status `audit:1389` — cannot tell never-written from committed (A-31)
    - item 10 trigger — misses rules, templates and agents (A-5)
    - post-push fence — stale-ref false GREEN (A-27)
    - persisted negation proofs and generators — commit and push in the invoking repo (A-2)
  - **Matched (selected outputs):**
    - push gate — WOULD PUSH / `BLOCKED … 2 hits` / `BLOCKED: D16 RED`
    - moratorium 27/14; key loop 27/27 in both sections; elision 1 named / 0; harvester `18 of 27`
    - instruction style b1 `47/11/4`, b2 `68/12/4`; fences 12 of 12; references b1 8 / b2 21
    - D16 staged 119/0, log b1 2/0, b2 2/0; selftest `48 of 48`
    - pipe count `8 8 8`; twin parity rc 0; applied-proof 9 of 9; gate 3 scoped 13/19
    - control enumerator 71; liveness guard rc 0; stamps 7 hits v2.26.0; deletions gate empty; accepted-risk fields 5 in 7 records
    - Applies counts 13=13, 16=16, 2=2; placeholder grep 0/0; measurer grep 0
    - syntax ratchet `new=0` with negation `new=1`; copy-path derivation 30 (≥5, W-23); agent invariant `0` (planted `0 1`); prior-prediction 1 (negations 0)
- **Isolation:** clone status empty, HEAD fdb48bc; 0 pushes left the machine.

### Agent 5: Document Accuracy (first dispatch; evidence at fdb48bc)
- **[C]** FAIL (LOW).
  - **Sampled:** session-end, sprint-proposer, validation-orchestrator, code_reviewer, validator, plus every line added under `docs/modules` by `5ecefa9~1..847d18f`.
  - **7 new instances:** A-18, A-19 and the five in A-20. The four lines 847d18f split out hold.
- **[E]** PASS.
  - **Canonical:** v2.26.0 at `README.md:1, :150`, `claude_md.md:4`, `EPA:247, :568, :612`.
  - **Historical labels:** `EPA:271, :275, :856`; `skills/README.md:35`.
  - **`created:` fields:** 18 lineage stamps, none rewritten in `5ecefa9~1..fdb48bc`. Mismatches 0.
- **[D12]** PASS.
  - **Sample:** concurrency-tester, load-tester, secrets-scanner; django-postgres, scheduling-patterns; auth-rules, resilience-rules.
  - **Full-set truth tests:**
    - agents 20/20 on every required field and heading; 10 `## When spawned`, matching 10 gap phrases
    - rules 11/11 `domain:` and `applies_to:`
    - skills 9/9 `invocation: inline`, folder format; the 5 without `## Testing` are on the exempt list
  - Violations: none.
- **[D15]** PASS.
  - **Disk:** 15 skills (12+3), 10 agents, examples 20/9/11, 7 templates, 5 rules, 6 commands, 17 dimensions, 6 agents, Steps 1-15, 7 user / 8 inline.
  - **Claims:** every count claim in README.md, CLAUDE.md, the concepts doc and both modules READMEs is accurate. "1900+ lines" vs 2120 is true. The banned sense of "inline": 0.
  - **Coverage:** Levels 1-5 all in README's table; all 6 commands in both command tables.
  - Inaccuracies 0.

### Agent 6: Process Coverage (isolated re-run)
- **Commits classified (40):** audit-fix application 14 · write-back 8 · audit report 7 · errata/receipt correction 3 · template feature 3 · upstream absorption with fixes 2 · governance 1 · README/concepts narrative 1 · accepted-risk record 1. Every type has a written home.
- **Undocumented executed flows:** planted-state negation probes, executed in 2 batches with no isolation home → A-1, A-2.
- **Referenced-but-unowned conventions:**
  - B-25 (carried): the exemption line has no writer or reader.
  - Efficacy anchors have no mother-side reader → A-39.
- **Aspirational mechanisms:** the prose-to-scripts migration → A-40; the persisted scripts' "real repo is untouched" claim → A-2.
- **D17.4 — provability** (D10's verdicts are Agent 4's; this axis only):
  - **Provable:** push-decay detector, prior-prediction, report-deletions gate, bash ratchet, trigger (d), C1 tagging writers (per-file limit, A-24), C4 closure (merge edge, A-23).
  - **Not provable:** moratorium counts — blind to a new prose control (A-25); HYPOTHESIS check — broken on shipped text (A-3).
- **D17.5 — who verifies:**
  - negation proof: author; persisted, but unsafe (A-2)
  - control back-sweep: author; `.claude/commands` only (A-4)
  - verifier rounds: subagent, pasted by author — partial gate
  - moratorium counts: nobody, persisted nowhere (A-25)
  - trigger (d): author, mechanical — gate
  - probe isolation: nobody, persisted nowhere (A-1)
- **D17.6 — shipped vs apparatus** (`git diff --numstat`; delivery twins counted inside apparatus; README and concepts doc uncounted, Z-19):

| Batch | Shipped (+/-) | Apparatus (+/-) | of which delivery twins | Uncounted |
|---|---|---|---|---|
| 0954d69 | 79/14 | 438/23 | 88/18 | 6/5 |
| a28f661 | 111/37 | 268/78 | 130/57 | 23/7 |
| afccff3 | 14/7 | 215/55 | 15/12 | 16/11 |
| f2fcccf | 73/28 | 355/81 | 123/13 | 13/9 |
| a2f4890 | 40/20 | 197/77 | 74/24 | 7/4 |
| 1c7c1f9..bb8cbec | 19/11 | 393/89 | 74/27 | 14/6 |
| ecace7a | 114/29 | 30/5 | 22/3 | 0 |
| 700a382 | 0/0 | 30/9 | 8/5 | 0 |
| 6b4baa8..d156dc4 | 3/3 | 404/47 | 5/4 | 11/6 |
| afa8ef0 | 1/1 | 305/62 | 216/49 | 2/2 |
| 7a7d1db | 43/6 | 207/28 | 73/25 | 2/2 |
| 5ecefa9..28b157a | 62/8 | 217/39 | 17/5 | 2/2 |
| c2c226f | 0/0 | 70/8 | 0 | 0 |
| 742a691 | 0/0 | 22/6 | 0 | 71/2 |
| db70667..847d18f | 428/73 | 136/15 | 23/14 | 7/3 |

Write-backs, all apparatus: d514555 241 · 1a35525 561 · 22bc80e 588 · fdb48bc 24.
- **Moratorium:** 27 keys / 14 items at every commit from c2c226f~1 through fdb48bc, so it held as far as it measures (A-25).
- **Convergence verdict:** after c2c226f the absorb-upstream-first rule turned the ratio around — the db70667 batch is 77/23 shipped. Counting its write-back, apparatus is still larger (739 vs 501). **The loop now produces shipped work; what it lacks is a verifier for controls that live in `docs/modules/`** (A-4, A-5).
- **Isolation:** clone status empty, HEAD fdb48bc; throwaway directories deleted; the live repo was never read.

----
**Report status:** `COMPLETE` — all 6 agents returned (Agents 1, 3, 4 and 6 on an isolated re-dispatch after the incident, Agents 2 and 5 from the first dispatch with evidence at fdb48bc); all 17 dimensions and Part 1 (31 of 31 applied IDs verdicted) evaluated; D16's `.claude/docs/` surface completed by the consolidator; 40 new findings (2 HIGH, 7 MEDIUM, 31 LOW; 0 escalated) + 48 carried.
**defect series:** row 17 appended to the one table in `.claude/commands/audit.md` — 15 of 31; no other surface touched.
*(superseded)* **Application status:** `PENDING`
*(superseded)* **Application status:** `PARTIAL — 18 of 49 applied (18 in s194f44a)` — A-1, A-2 (HIGH); A-27..A-32, A-38 (LOW, "Cycle governance" rule 2: their files were already being edited); A-41..A-49 (filed from this batch's pre-commit verifier and fixed in the same commit). Open: A-3..A-26, A-33..A-37, A-39, A-40 — A-25 and A-39 need a new control (moratorium, `/maintenance` → "Cycle governance" rule 3), A-40 awaits an owner decision, the MEDIUM ones (A-3..A-9) are the next batch, the rest are LOW backlog.
**Application status:** `PARTIAL — 33 of 49 applied (18 in s194f44a, 15 in s87210b1)` — the second batch applied the MEDIUM findings A-3..A-9 and, because it already edited their files, the LOW findings A-10, A-11, A-13, A-18, A-19, A-24, A-33, A-36 (receipts in `audit-2026-09-19.md` → "Post-change checklist receipts (`s87210b1`)"). Open: A-12, A-14..A-17, A-20..A-23, A-25, A-26, A-34, A-35, A-37, A-39, A-40.

## Post-change checklist receipts (`s194f44a`)

Batch: one fix commit, `194f44a` (range `f6b9bc6..194f44a`), plus this write-back. Every `$` line below was run in this session and its stdout is pasted unedited.

**push decay:** advanced fdb48bc..f6b9bc6 — D16 re-run over the newly-published range: GREEN (1 commit, 0 hits) — origin/main at f6b9bc6
    $ bash .claude/scripts/d16-gate.sh log fdb48bc..f6b9bc6
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**inventory sweep:** 9 surfaces checked, 0 stale claims — `probe-sandbox.sh` is a mother-repo runtime script, listed beside `d16-gate.sh` in the CLAUDE.md tree and the README structure line (both in the batch). It is absent from `docs/modules/README.md`, the concepts doc and `examples/README.md`, as `d16-gate.sh` is, and from both twins, because no project installs it
    $ for f in CLAUDE.md README.md docs/modules/README.md docs/agentic_engineering_framework.md examples/README.md .claude/commands/maintenance.md .claude/commands/audit.md .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md; do printf '%s %s\n' "$f" "$(grep -c 'probe-sandbox.sh' $f)"; done
    CLAUDE.md 1
    README.md 1
    docs/modules/README.md 0
    docs/agentic_engineering_framework.md 0
    examples/README.md 0
    .claude/commands/maintenance.md 5
    .claude/commands/audit.md 5
    .claude/commands/bootstrap.md 0
    .claude/commands/existing_project_adaptation.md 0

**instruction style:** 35 added, 4 rewritten, 0 buried — range f6b9bc6..194f44a; unit: an added line carrying a CAPS imperative a session must execute
    $ D='git diff f6b9bc6..194f44a -U0'; echo "instruction style: $($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    instruction style: 35 added, 4 rewritten, 0 buried

**references:** 3 of 3 cited sections resolved — `audit-2026-09-16.md` exists; `D7.6` and `D10.0` are defined in `.claude/commands/audit.md` (`[D10.0]` in the D10 block, `D7.6.` in Agent 3)
    $ git diff f6b9bc6..194f44a | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u | wc -l
    3

**fences:** 12 of 12 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; echo "$t -> $(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l) lines"; done | awk '/ 0 lines/{z++} END{print "zero:", z+0, "of", NR}'
    zero: 0 of 12

**isolation:** D16 log (patches + messages + paths): 1 commits scanned, 0 hits
    $ bash .claude/scripts/d16-gate.sh log f6b9bc6..194f44a
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**class sweep:** 18 fixes × 4 directions, 1 extra instance found and fixed (the `/maintenance` item 10 scratch-repo recipe ran `git clone -q .` with no push lock and was never deleted, so it was rerouted through the sandbox in the same edit as A-1). The per-fix table follows: rows 18 = findings applied 18, and the batch's commit count comes from git
    $ git log --oneline f6b9bc6..194f44a | wc -l; git log --format=%s f6b9bc6..194f44a | grep -cE '^(chore: bump to v|chore: substitute s)'
    1
    0

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| A-1 | s194f44a | .claude/commands/audit.md, .claude/commands/maintenance.md, .claude/scripts/probe-sandbox.sh, CLAUDE.md, README.md | ✓ `grep -nE 'git stash¦<commit>~1¦mktemp -d¦git clone -q \.¦planted' .claude/commands/*.md` (¦ = regex alternation) | ✓ maintenance items 5, 6, 10 ↔ audit Authorized operations, Phase 1, D7.6, D10.0 | ✓ the Authorized-operations list above (`git fetch`, sandbox writes outside the repo) | ✓ D7 slot `probe (D7.6)`, D10 paste line, `Report status: INCOMPLETE` wording | item 10 scratch-repo recipe (no push lock) — rerouted |
| A-2 | s194f44a | assets/docs/audit-2026-09-15.md | ✓ writing fences per report — the first back-sweep `$` line below: 2, both in this file | n/a — no twin | ✓ both proofs' "run from the repo root" headers, named in the errata | ✓ `/maintenance` item 6 now prescribes the sandboxed `$` line for a persisted proof | none |
| A-27 | s194f44a | .claude/commands/audit.md, .claude/commands/maintenance.md | ✓ `git grep -c 'git fetch -q origin;' -- .claude/commands` → 2 | ✓ both detectors fixed | ✓ the fail-closed rationale lines under each fence | ✓ `RED: no origin/main ref` already has a slot in both keys | none |
| A-28 | s194f44a | assets/docs/audit-2026-09-15.md | ✓ `grep -n '^\*\*push:\*\*'` over the report — 4 lines, 1 without a trailing hash | n/a | ✓ the key block around the line | n/a — a record, no report slot | none |
| A-29 | s194f44a | assets/docs/audit-2026-09-15.md | ✓ `grep -n 'GEN_LINES'` — 1 use, batch 1 only | n/a | ✓ the `receipt generator` key and its `$` line | n/a — a record | none |
| A-30 | s194f44a | .claude/commands/maintenance.md | ✓ `grep -n 'python -c "' .claude/commands/*.md` — 1 multi-line form | n/a — the check lives in one command | ✓ its expected-result paragraph, still true | n/a | none |
| A-31 | s194f44a | .claude/commands/audit.md | ✓ `grep -n 'status --porcelain' .claude/commands/*.md` — the other hit is D7.6, replaced under A-1 | n/a — Phase 3 is audit-only | ✓ the scoping parenthesis kept | ✓ `report committed:` slot unchanged | same edit as A-41 |
| A-32 | s194f44a | .claude/commands/maintenance.md | ✓ `grep -n '<the applied report>' .claude/commands/*.md` → 1 | n/a | ✓ rule 1's expected line | n/a | none |
| A-38 | s194f44a | .claude/commands/audit.md | ✓ `grep -n 'ID allocation' .claude/commands/*.md` | n/a | ✓ the collision-check rule below it | n/a | none |
| A-41 | s194f44a | .claude/commands/audit.md | ✓ same scan as A-31 | n/a | ✓ item 7's commit command, whose subject it now tests | ✓ slot unchanged | none |
| A-42 | s194f44a | .claude/commands/maintenance.md | ✓ `grep -n 'run --staged' .claude/commands/*.md` — the other uses plant on top of the index and never check out | n/a | ✓ the RED-blocks-the-commit line below | n/a | none |
| A-43 | s194f44a | .claude/commands/audit.md | ✓ `grep -n 'each inside' .claude/commands/*.md` → 1 | ✓ maintenance item 6 got the read-only-scan rule (A-48) | ✓ the D10.0 mandate it summarises | ✓ D10 report line reworded | none |
| A-44 | s194f44a | .claude/scripts/probe-sandbox.sh | ✓ `git remote -v` inside a sandbox — the fetch URL was the live path | n/a — one script | ✓ header step 2 and KNOWN LIMITS re-read | ✓ new selftest case `origin/main is the live remote ref, fetch fails closed` | none |
| A-45 | s194f44a | .claude/scripts/probe-sandbox.sh | ✓ `grep -n 'nothing was run' .claude/commands/*.md` → 0 consumers | n/a | ✓ header verdict list and the selftest classifier | n/a — no command greps the string | none |
| A-46 | s194f44a | .claude/commands/audit.md | ✓ `grep -n 'kept at' .claude/commands/audit.md` and `grep -n -- '--keep' .claude/commands/audit.md` | ✓ maintenance item 10 keeps `--keep` with its guarded delete | ✓ Authorized operations and the Phase 1 rule | n/a | none |
| A-47 | s194f44a | .claude/commands/audit.md | ✓ `grep -n "RED line's parts" .claude/commands/audit.md` and `grep -n 'parts that line names'` → 2, both fixed | n/a | ✓ Phase 1 closing line | ✓ Phase 3 `Report status:` | none |
| A-48 | s194f44a | .claude/commands/maintenance.md | ✓ `grep -n 'READ-ONLY scan' .claude/commands/*.md` | ✓ the rule now lives in both commands | ✓ item 6 sandbox lines | n/a | none |
| A-49 | s194f44a | .claude/commands/audit.md | ✓ the verifier's question (c) scan of the added references | n/a | ✓ Authorized operations (3 sites) | n/a | none |

**back-sweep:** 2 rules, 4 artifacts fixed of 4 found, re-run clean (2 + 2 = 4)
    $ for r in f6b9bc6 194f44a; do t=0; for f in $(git ls-tree --name-only $r assets/docs/ | grep 'audit-.*\.md$'); do w=$(git show $r:$f | awk '/^```/{if(g){if(w)n++; w=0} g=!g; next} g&&/git (commit|push|reset|stash|remote (add|remove|set-url))/{w=1} END{print n+0}'); e=$(git show $r:$f | grep -c 'probe-sandbox.sh run -- bash "\$T/negation_proof.sh"'); d=$((w-e)); [ $d -lt 0 ] && d=0; t=$((t+d)); done; echo "$r unneutralised writing fences: $t"; done
    f6b9bc6 unneutralised writing fences: 2
    194f44a unneutralised writing fences: 0
    $ for r in f6b9bc6 194f44a; do echo "$r fetch without --prune: $(git grep -c 'git fetch -q origin;' $r -- .claude/commands | awk -F: '{s+=$NF} END{print s+0}')"; done
    f6b9bc6 fetch without --prune: 2
    194f44a fetch without --prune: 0

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| A check that writes, a probe, a planted state or a persisted script runs ONLY inside `probe-sandbox.sh run` | a persisted fenced script in an audit report that commits, pushes, resets, stashes or rewires a remote, with no sandboxed-`$`-line erratum | the first `$` line above | 2 | 2 | 0 remaining |
| Endpoint detectors fetch with `--prune` | a `git fetch -q origin;` in a command | the second `$` line above | 2 | 2 | 0 remaining |

**control back-sweep:** 71 of 82 controls re-run, 0 defects found by this sweep. The 6 the verifier reproduced are under `verifier rounds:`, not counted twice. The denominator rose 71 → 82 on `audit.md`'s inline count (+11), and all 11 are command NAMES cited in prose (`git commit`, `git push`, `git fetch`, `git stash`, `git remote`, `git reset`, `git checkout`). None is a runnable check: that is the enumerator's known false-positive class, not a new control. One inline control (item 7's `git status --porcelain …`) became a fenced one the fenced counter cannot see (its lines start `p=`/`if`). Shapes this batch invented: the `probe-sandbox:` verdict line, the `report committed:` fence and the dedent wrapper. The controls run against them were the negation proof below, the selftest, and the moratorium and receipt-key loops
> **Errata 1 (added by the 2026-09-21 backlog triage batch, finding C-13).** The `$` line below
> WRITES INTO THE LIVE REPO and MUST NOT be pasted into a shell from the repo root. It
> redirects `git show` into `x` in the current directory; `git check-ignore -v x` returns rc=1,
> so `x` is NOT gitignored and lands as an untracked file in the working tree. Re-run it, if at
> all, only inside `bash .claude/scripts/probe-sandbox.sh run -- …`, or with the redirect
> pointed outside the repo. The figures below were not re-verified by this triage.
>
    $ for r in f6b9bc6 194f44a; do for f in .claude/commands/maintenance.md .claude/commands/audit.md; do git show $r:$f > x; inf=$(awk '/^```/{g=!g;next} g' x | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' x | wc -l); echo "$r $f: $inf fenced + $inl inline = $((inf+inl))"; done; done
    f6b9bc6 .claude/commands/maintenance.md: 6 fenced + 29 inline = 35
    f6b9bc6 .claude/commands/audit.md: 7 fenced + 29 inline = 36
    194f44a .claude/commands/maintenance.md: 6 fenced + 29 inline = 35
    194f44a .claude/commands/audit.md: 7 fenced + 40 inline = 47
    $ git diff f6b9bc6..194f44a -U0 -- .claude/commands/ | grep '^+' | grep -cE '(grep|python -c|diff -r|git (diff|show|status)|node|sed -n)'
    10
    $ sed -n '/^for k in "inventory sweep"/,/^done$/p' .claude/commands/maintenance.md | grep -oE '"[a-z][a-z ._-]*"' | wc -l; sed -n '/^## Post-change checklist/,/^## Version bumps/p' .claude/commands/maintenance.md | grep -cE '^[0-9]+\. \*\*'
    27
    14

**liveness:** skipped — no frontmatter touched
    $ git diff --name-only f6b9bc6..194f44a -- '*SKILL.md' .claude/agents docs/modules/agents | wc -l
    0

**negation proof:** every amended control goes RED on a planted bad state and GREEN on the healthy one:
- A-27: on the stale ref, PRE reads `unchanged` and FIX reads `RED`.
- A-31/A-41: a report never written, and a same-day re-run after a write-back, both read `NOT committed`; a report persisted last reads `report committed`.
- A-32: 2 → 7.
- A-30: PRE fails with `IndentationError`; FIX is OK both copied and de-indented, and reads `1 BROKEN` on a planted name.
- A-44: the remote ref is read, and the fetch is refused.

The script's own selftest reads 23 of 23. The persisted proof is below; its `$` line runs it through the sandbox by absolute path, and it writes only under its own `mktemp`
    $ bash .claude/scripts/probe-sandbox.sh run -- bash "$T/negation_proof.sh"   # $T = a directory OUTSIDE the repo holding the fence below
    A-27 remote drops main (stale remote-tracking ref)
      PRE: unchanged at H   (expect: unchanged — the stale read)
      FIX: RED: no origin/main ref after the fetch — origin/main unverified   (expect: RED)
    A-31/A-41 item 7 self-check
      never written — PRE form (status only): []   (expect: empty = false GREEN)
      never written — FIX: NOT committed — last report commit none, HEAD H   (expect: NOT committed)
      same-day re-run after a write-back — FIX: NOT committed — last report commit H, HEAD H   (expect: NOT committed)
      persisted last — FIX: report committed: sH   (expect: report committed)
    A-32 trigger (d) HM over two reports
      PRE (one report): 2   FIX (every report): 7   (expect: 2, 7)
    A-30 YAML check copied byte-for-byte (3-space list indent)
      PRE: IndentationError: unexpected indent
      FIX: raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK   de-indented FIX: raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK
      FIX on a planted wrong name: raw templates: 1 validated of 1 globbed, 0 skipped (fenced), 1 BROKEN   (expect: 1 BROKEN)
    A-44 sandbox origin/main with live main AHEAD of its remote
      fix: remote-ref fetch-refused probe-sandbox: GREEN — live repo unchanged; command exit 0 
      (expect: remote-ref fetch-refused. The pre-fix form was never committed; run pre-commit it printed LOCAL-main fetch-reached-live)
    probe-sandbox: GREEN — live repo unchanged; command exit 0
    $ bash .claude/scripts/probe-sandbox.sh selftest | tail -1
    selftest: 23 of 23 cases as expected

```bash
#!/usr/bin/env bash
# negation proof — batch s194f44a. RED on a planted bad state, GREEN on the healthy one, for every control
# the batch amended. Reads the pre-fix (PRE) and fixed (FIX) forms FROM GIT. Writes ONLY under its own mktemp.
# ALWAYS run it through the sandbox, by ABSOLUTE path:  bash .claude/scripts/probe-sandbox.sh run -- bash "$T/negation_proof.sh"
set -eu
PRE=${PRE:-f6b9bc6}; FIX=${FIX:-194f44a}
W=$(mktemp -d); [ -d "$W" ] || exit 2; trap 'rm -rf "$W"' EXIT
g() { git -c user.email=p@invalid -c user.name=p -c commit.gpgsign=false "$@"; }
fence() { git show "$1:$2" | awk -v k="$3" '/^ *```bash$/{b=1;s="";next} b&&/^ *```$/{b=0; if(index(s,k)) {printf "%s",s; exit}} b{s=s $0 "\n"}' | sed 's/^   //'; }
fence $PRE .claude/commands/maintenance.md 'push decay|Post-push' > "$W/det_pre.sh"
fence $FIX .claude/commands/maintenance.md 'push decay|Post-push' > "$W/det_fix.sh"
fence $FIX .claude/commands/audit.md 'report committed' | sed 's#audit-YYYY-MM-DD.md#audit-2099-01-01.md#' > "$W/i7_fix.sh"
git show $PRE:.claude/commands/maintenance.md | awk '/python -c "import io,yaml/{f=1} f{print} f&&/sys.exit\(1 if bad else 0\)/{exit}' > "$W/yaml_pre.sh"
git show $FIX:.claude/commands/maintenance.md | awk '/python -c "import textwrap/{f=1} f{print} f&&/^   .{3}\)\)"$/{exit}' > "$W/yaml_fix.sh"
git show $FIX:.claude/scripts/probe-sandbox.sh > "$W/sb_fix.sh"
REPO=$(pwd)
echo "A-27 remote drops main (stale remote-tracking ref)"
git init -q --bare "$W/r.git"; git init -q "$W/c"; cd "$W/c"; git config core.autocrlf false
mkdir -p assets/docs; echo x > f; g add -A; g commit -qm one; git branch -M main
git remote add origin "$W/r.git"; git push -q origin main; git fetch -q origin
printf '**push:** not requested — origin/main at %s\n' "$(git rev-parse --short HEAD)" > assets/docs/audit-2099-01-01.md
git -C "$W/r.git" branch -q other main; git -C "$W/r.git" update-ref -d refs/heads/main
echo "  PRE: $(bash "$W/det_pre.sh" 2>&1 | tail -1 | sed -E 's/[0-9a-f]{7,}/H/g')   (expect: unchanged — the stale read)"
echo "  FIX: $(bash "$W/det_fix.sh" 2>&1 | tail -1)   (expect: RED)"
echo "A-31/A-41 item 7 self-check"
cd "$W"; git init -q r31; cd r31; git config core.autocrlf false; echo a > a; g add -A; g commit -qm base
echo "  never written — PRE form (status only): [$(git status --porcelain assets/docs/audit-2099-01-01.md)]   (expect: empty = false GREEN)"
echo "  never written — FIX: $(bash "$W/i7_fix.sh" | sed -E 's/[0-9a-f]{7,}/H/g')   (expect: NOT committed)"
mkdir -p assets/docs; echo r1 > assets/docs/audit-2099-01-01.md; g add -A; g commit -qm "docs(audit): persist run 1"
echo wb >> assets/docs/audit-2099-01-01.md; g add -A; g commit -qm "docs(audit): write back X-1"
echo "  same-day re-run after a write-back — FIX: $(bash "$W/i7_fix.sh" | sed -E 's/[0-9a-f]{7,}/H/g')   (expect: NOT committed)"
echo r2 >> assets/docs/audit-2099-01-01.md; g add -A; g commit -qm "docs(audit): persist run 2"
echo "  persisted last — FIX: $(bash "$W/i7_fix.sh" | sed -E 's/[0-9a-f]{7,}/H/g')   (expect: report committed)"
echo "A-32 trigger (d) HM over two reports"
cd "$W"; for i in 1 2 3 4 5; do echo "| X-$i | HIGH | l | f | applied \`sabc1234\` |"; done > r1.md; for i in 1 2; do echo "| Y-$i | MEDIUM | l | f | applied \`sdef5678\` |"; done > r2.md
echo "  PRE (one report): $(grep -E '^\| [A-Z]-[0-9]+ \| (HIGH|MEDIUM) \|' r2.md | grep -cE 'applied `?s(abc1234|def5678)')   FIX (every report): $(grep -hE '^\| [A-Z]-[0-9]+ \| (HIGH|MEDIUM) \|' r1.md r2.md | grep -cE 'applied `?s(abc1234|def5678)')   (expect: 2, 7)"
echo "A-30 YAML check copied byte-for-byte (3-space list indent)"
cd "$REPO"
echo "  PRE: $(bash "$W/yaml_pre.sh" 2>&1 | tail -1)"
echo "  FIX: $(bash "$W/yaml_fix.sh" 2>&1 | tail -1)   de-indented FIX: $(sed 's/^   //' "$W/yaml_fix.sh" | bash 2>&1 | tail -1)"
mkdir -p "$W/y/docs/modules/agents" "$W/y/docs/modules/skills"; printf -- '---\nname: wrong\n---\n' > "$W/y/docs/modules/agents/foo.md"
echo "  FIX on a planted wrong name: $(cd "$W/y" && bash "$W/yaml_fix.sh" 2>&1 | tail -1)   (expect: 1 BROKEN)"
echo "A-44 sandbox origin/main with live main AHEAD of its remote"
cd "$W"; git init -q --bare o.git; git init -q l; cd l; g commit -q --allow-empty -m s; git branch -M main
git remote add origin ../o.git; git push -q -u origin main; g commit -q --allow-empty -m ahead; om=$(git rev-parse refs/remotes/origin/main)
for v in fix; do
  echo "  $v: $(bash "$W/sb_$v.sh" run -- bash -c "[ \"\$(git rev-parse refs/remotes/origin/main)\" = \"$om\" ] && echo remote-ref || echo LOCAL-main; git fetch -q origin 2>/dev/null && echo fetch-reached-live || echo fetch-refused" | tr '\n' ' ')"; done
echo "  (expect: remote-ref fetch-refused. The pre-fix form was never committed; run pre-commit it printed LOCAL-main fetch-reached-live)"
```

**version:** none — v2.26.0 stands on 3/3 canonical surfaces; the batch changes only this repo's own `/audit`/`/maintenance` apparatus (precedent `c2c226f`)
    $ grep -rln "v2.26.0" README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md | wc -l
    3

**classification:** 7 add, 4 substitute, 0 delete, 0 relocate
    $ n/a — judgement, not measurement
    ADD: `probe-sandbox.sh`; the isolation rules (Authorized operations, Phase 1 agent rule, maintenance items 5, 6, 10); Errata 1-4; the ID-exhaustion rule; the `--prune` rule; the read-only-scan rule in maintenance; the fail-closed-reason wording.
    SUBSTITUTE, each justified:
    - D7.6's isolation proof `git status --porcelain` → the sandbox verdict. The old proof cannot see a commit that was made, pushed and reset away.
    - item 7's inline self-check → a fenced one that tests HEAD and the commit subject. The old one read EMPTY for a report never written.
    - the YAML check's `python -c` → a `textwrap.dedent` wrapper. The old one failed when copied verbatim.
    - item 10's `mktemp` + `git clone -q .` recipe → the sandbox. The old recipe had no push lock and was never deleted.
    None of the four loses information.

**new component:** probe-sandbox.sh — placed `.claude/scripts/` (mother runtime only; no project runs `/audit` or `/maintenance`, so not in `docs/modules/`), invoker `/audit` Authorized operations + Phase 1 agent rule + D7.6 + D10.0 and `/maintenance` items 5, 6, 10, liveness selftest 23 of 23 (a script, no frontmatter)
    $ bash .claude/scripts/probe-sandbox.sh selftest | tail -1
    selftest: 23 of 23 cases as expected

**gates:** twin parity PASS | receipt-rows-vs-diff 18/18 | row-count 18 vs 18 | slots 1/1
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo PASS
    PASS
    $ git show --name-only --format="" 194f44a
    .claude/commands/audit.md
    .claude/commands/maintenance.md
    .claude/scripts/probe-sandbox.sh
    CLAUDE.md
    README.md
    assets/docs/audit-2026-09-15.md
    $ grep -cE 'applied `?s194f44a' assets/docs/audit-2026-09-16.md
    18
    $ git diff f6b9bc6..194f44a -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:'
    ALWAYS REPORT the path used and the final `probe-sandbox:
    Its slot, separate from the mandate: Agent 3's REPORT FORMAT row `- probe (D7.6): [synthetic-project path] | [the final probe-sandbox: line]`.

**applied-proof:** 18 of 18 IDs resolve to a commit touching their line (form A: 5, form B: 13), 0 forced to open. Form B is used where the fix only INSERTS beside unchanged text (errata, new rule lines) and for the IDs filed in this batch, whose lines never existed in a commit
    $ bash applied_proof.sh   # the per-ID script pasted below; run from the repo root with Git Bash
    A-1 form A .claude/commands/audit.md:15 -> 194f44a
    A-2 form B assets/docs/audit-2026-09-15.md:1018 -> 194f44a
    A-27 form A .claude/commands/audit.md:95 -> 194f44a
    A-28 form B assets/docs/audit-2026-09-15.md:1840 -> 194f44a
    A-29 form B assets/docs/audit-2026-09-15.md:1325 -> 194f44a
    A-30 form A .claude/commands/maintenance.md:960 -> 194f44a
    A-31 form A .claude/commands/audit.md:1390 -> 194f44a
    A-32 form A .claude/commands/maintenance.md:216 -> 194f44a
    A-38 form B .claude/commands/audit.md:1279 -> 194f44a
    A-41 form B .claude/commands/audit.md:1443 -> 194f44a
    A-42 form B .claude/commands/maintenance.md:874 -> 194f44a
    A-43 form B .claude/commands/audit.md:909 -> 194f44a
    A-44 form B .claude/scripts/probe-sandbox.sh:120 -> 194f44a
    A-45 form B .claude/scripts/probe-sandbox.sh:142 -> 194f44a
    A-46 form B .claude/commands/audit.md:47 -> 194f44a
    A-47 form B .claude/commands/audit.md:236 -> 194f44a
    A-48 form B .claude/commands/maintenance.md:1021 -> 194f44a
    A-49 form B .claude/commands/audit.md:23 -> 194f44a
    applied-proof: 18 of 18 IDs resolve to a commit touching their line (form A: 5, form B: 13), 0 forced to open

```bash
# applied-proof — per finding ID over f6b9bc6..194f44a. Form A: the pre-batch line's TEXT, pickaxed.
# Form B: a TIP line (for fixes that only INSERT beside unchanged text), via git log -L.
F=f6b9bc6; L=194f44a; a=0; b=0; n=0; k=0
A() { id=$1 f=$2 txt=$3; ln=$(git show $F:"$f" | grep -nF -- "$txt" | head -1 | cut -d: -f1)
  anchor=$(git show $F:"$f" | sed -n "${ln}p"); h=$(git log -S"$anchor" --format=%h $F..$L -- "$f" | head -1)
  n=$((n+1)); if [ -n "$h" ]; then a=$((a+1)); echo "$id form A $f:$ln -> $h"; else k=$((k+1)); echo "$id form A $f:${ln:-?} -> NONE"; fi; }
B() { id=$1 f=$2 txt=$3; ln=$(git show $L:"$f" | grep -nF -- "$txt" | head -1 | cut -d: -f1)
  h=$(git log -L "$ln,$ln:$f" --format=%h -s $F..$L 2>/dev/null | grep -E '^[0-9a-f]{7,}$' | head -1)
  n=$((n+1)); if [ -n "$h" ] && git show --name-only --format= $h | grep -qx "$f"; then b=$((b+1)); echo "$id form B $f:$ln -> $h"; else k=$((k+1)); echo "$id form B $f:${ln:-?} -> NONE"; fi; }
A A-1  .claude/commands/audit.md 'git check-ignore`, `git reflog`. Several checks below MANDATE these'
B A-2  assets/docs/audit-2026-09-15.md '> **Errata 1 (added by the application'
A A-27 .claude/commands/audit.md 'if git fetch -q origin; then'
B A-28 assets/docs/audit-2026-09-15.md '> **Errata 3 (added by the application'
B A-29 assets/docs/audit-2026-09-15.md '> **Errata 4 (added by the application'
A A-30 .claude/commands/maintenance.md 'python -c "import io,yaml,glob,os,sys'
A A-31 .claude/commands/audit.md '`git status --porcelain assets/docs/audit-YYYY-MM-DD.md` — **THIS run'
A A-32 .claude/commands/maintenance.md "HM=\$(grep -E '^"
B A-38 .claude/commands/audit.md 'WHEN EVERY OTHER LETTER IS USED'
B A-41 .claude/commands/audit.md "grep -q '^docs(audit): persist'"
B A-42 .claude/commands/maintenance.md 'NEVER combine `--staged` with a checkout'
B A-43 .claude/commands/audit.md 'each one that WRITES inside `probe-sandbox.sh run`, the read-only ones in place'
B A-44 .claude/scripts/probe-sandbox.sh 'remote add origin PROBE-SANDBOX-FETCH-DISABLED'
B A-45 .claude/scripts/probe-sandbox.sh 'or it exited $rc itself'
B A-46 .claude/commands/audit.md 'agents never pass `--keep`'
B A-47 .claude/commands/audit.md 'or, for a fail-closed RED, its reason'
B A-48 .claude/commands/maintenance.md 'NEVER run the D16 gate or any other READ-ONLY scan inside the sandbox'
B A-49 .claude/commands/audit.md 'D10.0 (forward references, flagged)'
echo "applied-proof: $((a+b)) of $n IDs resolve to a commit touching their line (form A: $a, form B: $b), $k forced to open"
```

**placeholder:** none — the fix commit existed before this write-back was written, so every hash is real
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-16.md; echo "(end)"
    (end)

**commit correction:** none
    $ git log -1 --format=%B 194f44a | awk '/^Applies /{f=1} f&&/^$/{exit} f' | grep -oE '\b[A-Z]-[0-9]+\b' | sort -u | wc -l
    18

**report deletions:** none — 0 structural decreases across 2 report files (`audit-2026-09-15.md` staged for 194f44a, `audit-2026-09-16.md` staged for this write-back; the fence-aware six-pattern gate plus the fenced-body count)
    $ for f in $(git diff --cached --name-only -- 'assets/docs/audit-*.md'); do for pat in '^#{1,3} ' '^\| [A-Z]-[0-9]+ \|' '^> \*\*Errata ' '^ *\$ ' '^\*\*[a-z][a-z ._-]*:' '^```'; do b=$(git show HEAD:"$f" 2>/dev/null | awk '/^```/{g=!g;print;next} !g' | grep -cE "$pat"); if [ -f "$f" ]; then a=$(awk '/^```/{g=!g;print;next} !g' "$f" | grep -cE "$pat"); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: $pat  ${b:-0} -> ${a:-0}"; done; b=$(git show HEAD:"$f" 2>/dev/null | awk '/^```/{g=!g;next} g' | grep -c .); if [ -f "$f" ]; then a=$(awk '/^```/{g=!g;next} g' "$f" | grep -c .); else a=0; fi; [ "${a:-0}" -lt "${b:-0}" ] && echo "RED $f: fenced body lines  ${b:-0} -> ${a:-0}"; done; echo "(end)"
    (end)

**report restore:** none
    $ git log --diff-filter=D --format=%h f6b9bc6..194f44a -- 'assets/docs/audit-*.md' | wc -l
    0

**defect series:** N/A — a maintenance session; `/audit` Phase 3 appends the row
    $ git diff f6b9bc6..194f44a -- .claude/commands/audit.md | grep -c '^+| [0-9]* | `audit-'
    0

**verifier rounds:** 1 round, 6 defects reproduced (+3 cross-surface gaps), 9 fixed before commit — A-41..A-49; trigger 3 files; bash -n ratchet GREEN
    $ git diff --name-only f6b9bc6 194f44a -- .claude/commands .claude/scripts docs/modules/skills | wc -l
    3
    $ A=f6b9bc6; B=194f44a   # then the item-10 ratchet fence, verbatim
    .claude/commands/audit.md base=0 tip=0 new=0
    .claude/commands/maintenance.md base=0 tip=0 new=0
    The verifier's reproduced-defect list, verbatim (counts only):
    D1 `.claude/commands/audit.md:1442-1445` — The Phase 3 item 7 self-check reads GREEN for a report that was never written. This happens on a second run on the same day when the previous commit was a write-back to today's report. How reproduced: sandbox clone checked out at the write-back commit that came just before a real same-day Run 3 persist, with `p` set to that day's report. It printed `report committed: sHASH` although Run 3 was not written yet.
    D2 `.claude/commands/maintenance.md:871-872` — The instruction to "reconstruct the pre-fix state INSIDE `run --staged` (check out `<commit>~1` there)" cannot be followed as written. With `--staged` the fixed files are dirty in the sandbox, so the checkout aborts. Before the commit, the pre-fix state is plain HEAD, reached without `--staged`, and `<commit>~1` goes one commit too far. How reproduced: in a `run --staged` sandbox, `git checkout -q HEAD~1` printed "Your local changes to the following files would be overwritten by checkout" (1 command file named) and exited 1.
    D3 `.claude/commands/audit.md:909` — The D10 report format says the self-checks are "executed verbatim (D10.0), each inside `probe-sandbox.sh run`". That contradicts D10.0, Authorized operations and the agent rule. Followed literally, it gives wrong results: the push-decay detector run in the sandbox reports the live LOCAL `main` as `origin/main`. How reproduced: throwaway live repo, bare remote, 1 unpushed commit. In place the detector printed `advanced <other>..<REAL-REMOTE-main>`; inside `probe-sandbox run` it printed `advanced <other>..<LOCAL-unpushed-main>` with a GREEN verdict.
    D4 `.claude/scripts/probe-sandbox.sh:111` — Only the push URL is disabled. The clone's fetch URL stays the live repo path, so any fetch, `ls-remote` or `refs/remotes/origin/*` check inside the sandbox reads the live local branches as if they were the remote. The known-limits list does not mention this. How reproduced: `git remote -v` in the sandbox shows fetch = the live path and push = `PROBE-SANDBOX-PUSH-DISABLED`. D3's probe shows the effect.
    D5 `.claude/scripts/probe-sandbox.sh:131` — Exit code 126 or 127 always prints "nothing was run", even when the command ran and had side effects. It is a false fail-closed verdict, never a false GREEN. How reproduced: `run -- bash -c 'echo ran; echo w > $T/f; exit 127'` printed `ran`, then the "nothing was run" RED. The side-effect file was present.
    D6 `.claude/commands/audit.md:46-47` — The text says the sandbox clone "is deleted when the command ends". That is false with `--keep`, which the Phase 1 agent rule offers, and nothing in `audit.md` tells an agent to delete a kept clone. How reproduced: `run --keep -- /nonexistent-qzxv` printed `kept at <path>` and the directory stayed until removed by hand.
    Cross-surface: (a) a fail-closed RED names no parts for INCOMPLETE to cite → A-47; (b) the read-only-scan NEVER had no counterpart in maintenance → A-48; (c) 3 added references pointed forward unflagged → A-49; (d) covered by D3, D6 and (a). All 5 verifier sandbox runs: `probe-sandbox: GREEN — live repo unchanged; command exit 0`.

**receipt generator:** none — receipts typed by hand from commands run this session
    $ ls receipt_generator.* 2>/dev/null | wc -l
    0

**measurer:** N/A — no lineage doc written
    $ git diff --name-only f6b9bc6..194f44a -- 'assets/docs/framework-evolution-upstream-*' | wc -l
    0

**audit:** N/A — no version bump and no upstream absorption this session
    $ git diff --name-only f6b9bc6..194f44a -- docs/modules | wc -l
    0

**verification audit:** proposed — trigger (d) fires; Step 0 found no doc pending absorption, so rule 4 does not reorder it
    $ S=$(git diff --name-only f6b9bc6 194f44a -- docs/modules examples .claude/commands/bootstrap.md .claude/commands/existing_project_adaptation.md | wc -l); HM=$(grep -hE '^\| [A-Z]-[0-9]+ \| (HIGH|MEDIUM) \|' assets/docs/audit-2026-09-16.md | grep -cE 'applied `?s(194f44a)'); [ "$S" -gt 0 ] || [ "$HM" -gt 0 ] && echo "trigger (d): fires — shipped files $S, HIGH/MEDIUM applied $HM" || echo "trigger (d): does not fire — apparatus-only, LOW-only"
    trigger (d): fires — shipped files 0, HIGH/MEDIUM applied 4

**push:** not requested — origin/main at f6b9bc6
    $ git rev-parse --short refs/remotes/origin/main; git log --oneline origin/main..main | wc -l   # run before this write-back's own commit, which makes it 2
    f6b9bc6
    1
