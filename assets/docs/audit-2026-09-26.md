# Framework maintenance receipts — 2026-09-26
*(superseded)* **Run mode:** receipts-only — no audit ran this day

Batch: `3030551` (v2.33.1). It applies AF-1, AF-3, AF-4, AF-5, AF-6, AF-12 and AF-14 from `assets/docs/audit-2026-09-23.md`. The owner approved AF-1/3/4/5/13; AF-6, AF-12 and AF-14 were applied under "Cycle governance" rule 2, because the batch already edited their files. It also fixes AF-16..AF-21, which this session filed in that report from its independent pre-commit verifier. AF-13 was fixed in a gitignored note (working tree only) and stays `open` in the ledger. Statuses are written back in that report's ledger.

## Post-change checklist receipts (`s3030551`)

**inventory sweep:** N/A — no artifact added, removed, renamed or split, and no user-visible capability added. Stale-count surfaces checked by hand: CLAUDE.md, README.md, docs/modules/README.md, docs/agentic_engineering_framework.md, examples/README.md, maintenance.md, both command twins — 0 stale claims. The only count changed ("9 sections" → 13) lives in EPA and now carries its own check; bootstrap received the twin check.
    $ git diff --name-status --diff-filter=ADR 3030551~1..3030551 -- docs/modules .claude/commands .claude/skills .claude/rules .claude/scripts examples | wc -l
    0

**instruction style:** 13 added, 1 rewritten, 0 buried — range `3030551~1..3030551`; unit: an added line carrying a CAPS imperative a session must execute. Twice before commit the index read 2 buried; each time the imperatives were split onto their own lines.
    $ D='git diff 3030551~1..3030551 -U0'; echo "instruction style: $($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    instruction style: 13 added, 1 rewritten, 0 buried

**references:** 2 of 2 cited targets resolve. `Phase 3 item 1` and `Phase 3 item 3` are numbered items of `/audit` → "Phase 3 — Persist the report", cited only from audit.md and maintenance.md, which run in this repo. The Agent 1 prompt no longer cites them (AF-17).
    $ git diff 3030551~1..3030551 | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u
    Phase 3 item 1
    Phase 3 item 3

**fences:** 13 of 13 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; n=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$n" -gt 0 ] && echo ok; done | wc -l; ls docs/modules/templates/*.md docs/modules/rules/*.md | wc -l
    13
    13

**isolation:** D16 log (patches + messages + paths): 1 commits scanned, 0 hits
    $ bash .claude/scripts/d16-gate.sh log 3030551~1..3030551
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**class sweep:** 13 fixes × 4 directions, 6 extra instances found and fixed. AF-16..AF-21 were reproduced by the verifier and filed as findings; AF-6 and AF-14 were applied as LATERAL instances of AF-1 and of the D16 copy-path list.
    $ git show --name-only --format='' 3030551 | wc -l
    7

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| AF-1 | s3030551 | .claude/commands/audit.md | ✓ `grep -rln "receipts-only" .claude/ docs/ README.md CLAUDE.md` → audit.md, maintenance.md | ✓ maintenance.md's two readers amended | ✓ the new item-1 line checked against "The defect series" → AF-16 | ✓ the report format's own `**Run mode:**` line is the one kept | AF-6, AF-16, AF-17, AF-18 |
| AF-3 | s3030551 | .claude/commands/existing_project_adaptation.md | ✓ `grep -nE "\(([0-9]+) sections\)\|full checklist \(" .claude/commands/*.md` → only EPA | ✓ bootstrap Step 8 existing-agent branch → AF-19 | ✓ the count sentence → AF-21 | ✓ report slot → AF-20 | AF-19, AF-20, AF-21 |
| AF-4 | s3030551 | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "5 lines of pointers"` → the EPA v2.1.0 line, fixed | ✓ `grep -n "bullet" .claude/commands/bootstrap.md` → 0 → AF-19 | ✓ path-scoped bullet only after Step 2.7's conversion | ✓ CLAUDE.md report slot → AF-20 | AF-19, AF-20 |
| AF-5 | s3030551 | docs/modules/agents/security_reviewer.md | ✓ `grep -rn "live harness\|live probe\|refused call" docs/ .claude/ examples/` → only the deleted box | n/a — the agent template has no twin | ✓ "What this review covered" still lists §13 (5 boxes remain) | ✓ v2.33.1 migration note + slot | none |
| AF-6 | s3030551 | .claude/commands/audit.md | same edit as AF-1 | same edit as AF-1 | ✓ Agent 1 input → AF-17 | ✓ prediction verdict `n/a` path | same edit as AF-1 |
| AF-12 | s3030551 | .claude/commands/existing_project_adaptation.md | ✓ `grep -rnE '→ Gate [0-9]\|→ "?Gate [0-9] \(' .claude/commands docs/modules` → 0 | n/a — bootstrap reads the template, no embed | ✓ surrounding paragraph unchanged | n/a — citation only | none |
| AF-14 | s3030551 | .claude/commands/audit.md | ✓ `grep -oE 'projects/\$ARGUMENTS/scripts/[A-Za-z0-9_./-]+' .claude/commands/bootstrap.md \| sort -u` → 2 files | n/a — /audit has no twin | ✓ the "As of" stamp in the same sentence bumped | n/a — no report slot names the list | none |
| AF-16 | s3030551 | .claude/commands/audit.md | same edit as AF-1 | n/a | same edit as AF-1 | ✓ the series grep counts one `verification` line per run | same edit as AF-1 |
| AF-17 | s3030551 | .claude/commands/audit.md | same edit as AF-1 | n/a | ✓ definition inlined in Agent 1's fenced prompt | n/a | same edit as AF-1 |
| AF-18 | s3030551 | .claude/commands/maintenance.md | same edit as AF-1 | ✓ audit.md "both findings readers" rewritten | ✓ the header paragraph | n/a | same edit as AF-1 |
| AF-19 | s3030551 | .claude/commands/bootstrap.md | same edit as AF-3 | ✓ the twin itself | ✓ the pre-existing "tiered security testing model" residual is named in the ledger row, not fixed | n/a | same edit as AF-3 |
| AF-20 | s3030551 | .claude/commands/existing_project_adaptation.md | same edit as AF-3 | n/a — bootstrap's report has no per-file upgrade lines | ✓ two existing slots amended, no new key | ✓ the slots themselves | same edit as AF-3 |
| AF-21 | s3030551 | .claude/commands/existing_project_adaptation.md | same edit as AF-3 | n/a | ✓ the sentence under the list | n/a | same edit as AF-3 |

Per-fix table: rows 13 = findings applied in `s3030551` 13.

**back-sweep:** N/A — no process rule promoted
    $ git diff 3030551~1..3030551 -- .claude/rules docs/modules/rules | wc -l
    0

**liveness:** 19 components OK [full parse] — raw templates, `name:` matches file/dir; this batch edited no frontmatter
    $ python -c "import io,yaml,glob,os; b=0; n=0; k=0; exec('for p in sorted(glob.glob(\'docs/modules/skills/*/SKILL.md\'))+sorted(glob.glob(\'docs/modules/agents/*.md\')):\n s=io.open(p,encoding=\'utf-8\').read().replace(chr(13)+chr(10),chr(10))\n if not s.startswith(\'---\'): k+=1; continue\n n+=1; d=yaml.safe_load(s[4:s.index(chr(10)+\'---\',4)+1]); e=os.path.basename(os.path.dirname(p)) if p.endswith(\'SKILL.md\') else os.path.basename(p)[:-3].replace(\'_\',\'-\'); b+=d.get(\'name\')!=e'); print('raw templates: %d validated of %d globbed, %d skipped (fenced),' % (n,n+k,k), 'OK' if not b else 'BROKEN')"
    raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK

**negation proof:** the new EPA Step 2.5 count check → red as expected on the pre-fix tree (9 numbered boxes vs 13 template sections) and green at the tip (13 vs 13). The receipts-only definition reads 0 on both ledger-bearing reports, and this file's own header is the positive case (1). These are read-only comparisons over `git show` with no planted state, so no sandbox was used.
    $ for r in 3030551~1 3030551; do git show $r:.claude/commands/existing_project_adaptation.md | awk '/Compare against the full checklist/{f=1;next} f&&/^```$/{c++; if(c==2)exit; next} f&&c==1' | grep -cE '^□ [0-9]+\.'; done; grep -cE '^## [0-9]+\.' docs/modules/agents/security_reviewer.md
    9
    13
    13
    $ grep -cE '^\*\*Run mode:\*\* receipts-only' assets/docs/audit-2026-09-22.md assets/docs/audit-2026-09-23.md assets/docs/audit-2026-09-26.md
    assets/docs/audit-2026-09-22.md:0
    assets/docs/audit-2026-09-23.md:0
    assets/docs/audit-2026-09-26.md:1

**version:** v2.33.1 applied to 4/4 canonical surfaces (README.md, claude_md.md, existing_project_adaptation.md, audit.md) — 0 v2.33.0 residues outside migration notes
    $ grep -rn 'v2\.33\.1' README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | cut -d: -f1 | sort | uniq -c
          1 .claude/commands/audit.md
          3 .claude/commands/existing_project_adaptation.md
          1 docs/modules/templates/claude_md.md
          2 README.md
    $ grep -rn 'v2\.33\.0' README.md docs/ .claude/ CLAUDE.md | grep -v 'created: framework-v' | grep -vE 'v[0-9.]+ migration|pre-v[0-9.]+' | wc -l
    0

**classification:** 0 add, 12 substitute, 1 delete, 0 relocate. SUBSTITUTE covers every reworded rule: each keeps its content and becomes strictly more precise (AF-1, AF-3, AF-4, AF-6, AF-12, AF-14, AF-16..AF-21). DELETE is security-reviewer §13's last box (AF-5), which could never be executed: its facts are absent from the agent's Input, and its BOUNDARIES forbid the logs that could carry them.
    $ n/a — judgement, not measurement

**new component:** none this session
    $ n/a — judgement, not measurement

**gates:** twin parity PASS | receipt-rows-vs-diff 13/13 | row-count 13 vs 13 | slots 0/0 (no new ALWAYS REPORT key)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo twin-dir PASS
    twin-dir PASS
    $ diff <(sed -n '/^```json$/,/^```$/p' docs/modules/templates/settings_json.md | sed '1d;$d' | tr -d '\r') <(sed -n "/<< 'SETTINGS'/,/^SETTINGS$/p" .claude/commands/existing_project_adaptation.md | sed '1d;$d' | tr -d '\r') && echo settings-embed PASS
    settings-embed PASS
    $ git show --name-only --format='' 3030551
    .claude/commands/audit.md
    .claude/commands/bootstrap.md
    .claude/commands/existing_project_adaptation.md
    .claude/commands/maintenance.md
    README.md
    docs/modules/agents/security_reviewer.md
    docs/modules/templates/claude_md.md
    $ grep -cE 'applied `?s3030551' assets/docs/audit-2026-09-23.md
    13
    $ git diff 3030551~1..3030551 -U0 | grep '^+' | grep -oE 'ALWAYS REPORT[^`]*`[A-Za-z][A-Za-z0-9 ._-]*:' | wc -l
    0

**push:** requested — D16 GREEN over 5 unpushed commits → pushed sc98db39..saad5b74 — origin/main at saad5b74
    $ bash .claude/scripts/d16-gate.sh log origin/main..main   # run immediately before the push, with origin/main at c98db39
    D16 log (patches + messages + paths): 5 commits scanned, 0 hits
    $ git -c push.followTags=false push origin main
       c98db39..aad5b74  main -> main

**audit:** skipped — PATCH bump, which does not require the pre-bump audit ("Version bumps": PATCH bumps do not require it)
    $ git log -1 --format=%s 3030551 | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+'
    v2.33.1

**verification audit:** not triggered — no evidence from outside the loop
    $ PEND=0; for d in $(grep -L 'STATUS.*upstreamed' projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); [ "$PEND" -gt 0 ] || [ "$OBS" -gt 0 ] && echo "trigger (d): fires — pending upstream docs $PEND, HIGH observed in use $OBS" || echo "trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog"
    trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog

**placeholder:** none — the write-back cites the real hash; no placeholder token was used
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-23.md | wc -l
    0

**control back-sweep:** 3 of 3 touched controls re-run, 1 defect found (AF-21: the new EPA count check compared against the wrong denominator; fixed before commit). The touched controls are the EPA Step 2.5 count, the bootstrap Step 8 count and the prediction-check comment. The column-0 enumerator's per-file totals are below; the docs/modules files carry only inline controls.
    $ for f in .claude/commands/maintenance.md .claude/commands/audit.md $(git diff --name-only 3030551~1 3030551 -- docs/modules); do inf=$(awk '/^```/{g=!g;next} g' "$f" | grep -cE '^\s*(\$ )?(grep|awk |python -c|diff -r|git |node|sed -n|for [A-Za-z_][A-Za-z0-9_]* in )'); inl=$(grep -ohE '`(grep|git|diff|awk|sed|node|python|ls) [^`]{4,}`' "$f" | wc -l); echo "$f: $inf fenced + $inl inline = $((inf+inl))"; done
    .claude/commands/maintenance.md: 7 fenced + 30 inline = 37
    .claude/commands/audit.md: 10 fenced + 43 inline = 53
    docs/modules/agents/security_reviewer.md: 0 fenced + 1 inline = 1
    docs/modules/templates/claude_md.md: 0 fenced + 1 inline = 1
    $ git diff -U0 3030551~1..3030551 -- .claude/commands/ docs/modules/ | grep '^+' | grep -cE '(grep|python -c|diff -r|git (diff|show|status)|node|sed -n)'
    3

**commit correction:** none
    $ git log --format=%s 3030551~1..3030551 | grep -ciE 'correct'
    0

**defect series:** not appended — no audit ran this day
    $ grep -c '^# Run [0-9]' assets/docs/audit-2026-09-26.md
    0

**applied-proof:** 13 of 13 IDs resolve to a commit touching their line (form A: 13, form B: 0), 0 forced to open. AF-13 stays `open`: its file is gitignored (see its ledger row).
    $ bash applied_proof.sh   # the fence below saved as a file, run from the repo root
    AF-1 form A: 1
    AF-3 form A: 1
    AF-4 form A: 1
    AF-5 form A: 1
    AF-6 form A: 1
    AF-12 form A: 1
    AF-14 form A: 1
    AF-16 form A: 1
    AF-17 form A: 1
    AF-18 form A: 1
    AF-19 form A: 1
    AF-20 form A: 1
    AF-21 form A: 1
```bash
R=3030551~1..3030551
while IFS='|' read -r id f a; do echo "$id form A: $(git log -S"$a" --oneline $R -- "$f" | wc -l)"; done <<'LIST'
AF-1|.claude/commands/audit.md|whose header does NOT say `**Run mode:** receipts-only`
AF-3|.claude/commands/existing_project_adaptation.md|Compare against the full checklist (9 sections):
AF-4|.claude/commands/existing_project_adaptation.md|Session Protocol section is 5 lines of pointers
AF-5|docs/modules/agents/security_reviewer.md|The gate was proven to BLOCK by a real refused call
AF-6|.claude/commands/audit.md|11. The PREVIOUS audit report in `assets/docs/` — required
AF-12|.claude/commands/existing_project_adaptation.md|Gate 1 (TWIN PARITY)**, which owns
AF-14|.claude/commands/audit.md|**and `projects/*/scripts/`** — Bootstrap
AF-16|.claude/commands/audit.md|NEVER add a second `**Run mode:**` line for it
AF-17|.claude/commands/audit.md|does not count) — required
AF-18|.claude/commands/maintenance.md|every reader that resolves "the previous report"
AF-19|.claude/commands/bootstrap.md|twin of `/existing_project_adaptation` Step 2.5
AF-20|.claude/commands/existing_project_adaptation.md|N of 13 numbered sections present
AF-21|.claude/commands/existing_project_adaptation.md|NUMBERED □ in the section list above
LIST
```

**report deletions:** none
    $ git diff --cached --name-only -- 'assets/docs/audit-*.md'   # the write-back; the structural gate below printed no RED line over both files
    assets/docs/audit-2026-09-23.md
    assets/docs/audit-2026-09-26.md

**push decay:** unchanged at session start (c98db39); advanced only by this session's own gated push — origin/main at saad5b74
    $ git fetch -q --prune origin && git rev-parse --short refs/remotes/origin/main   # after the push
    aad5b74

**report restore:** none
    $ git log --diff-filter=D --name-only --format='' c98db39..HEAD -- 'assets/docs/audit-*.md' | wc -l
    0

**verifier rounds:** 1 round, 6 defects reproduced, 6 fixed before commit — AF-16, AF-17, AF-18, AF-19, AF-20, AF-21; trigger 6 files; bash -n ratchet GREEN (audit.md, bootstrap.md, existing_project_adaptation.md, maintenance.md: new=0, twins tip=0). Reproduced defects, verbatim in substance:
- (1) The drafted supersede rule adds a second `**Run mode:**` line beside the appended run's own; a planted receipts-only + verification file made the defect-series gate read RED 20+2 vs 21 (probe-sandbox: GREEN — live repo unchanged).
- (2) Agent 1's FILES TO READ item 11 cited "Phase 3 item 3", which its prompt never carries; the agent would match a superseded header.
- (3) "two readers … Both" is stale: four sites skip receipts-only files.
- (4) Bootstrap's existing-CLAUDE.md and existing-security-reviewer branches did not mirror EPA's new bullet-by-bullet and 13-section checks.
- (5) EPA's report had no slot for the in-place edit offers, the 13-section count or the bullet comparison.
- (6) Step 2.5's "equal to the number of □ above" counted 20 boxes against 13.
    $ git diff --name-only 3030551~1 3030551 -- .claude/commands .claude/scripts docs/modules | wc -l
    6
    $ A=3030551~1; B=3030551; T=$(mktemp -d); for f in $(git diff --name-only "$A" "$B" -- .claude/commands); do for side in A B; do r=${!side}; git show "$r:$f" 2>/dev/null | awk '/^ *```bash *$/{b=1;s="";next} b&&/^ *```$/{b=0;printf "%s%c", s, 0;next} b{s=s $0 "\n"}' | while IFS= read -r -d '' body; do printf '%s' "$body" | bash -n 2>/dev/null || printf '%s' "$body" | grep -qE '<[a-z][^>]*>' || printf '%s' "$body" | cksum; done | sort > "$T/$side"; done; echo "$f base=$(wc -l < "$T/A") tip=$(wc -l < "$T/B") new=$(comm -13 "$T/A" "$T/B" | wc -l)"; done; rm -rf "$T"
    .claude/commands/audit.md base=0 tip=0 new=0
    .claude/commands/bootstrap.md base=0 tip=0 new=0
    .claude/commands/existing_project_adaptation.md base=0 tip=0 new=0
    .claude/commands/maintenance.md base=0 tip=0 new=0

**receipt generator:** none — receipts typed by hand from commands run in the session; every `$` line above was executed as written, with the applied-proof script pasted in full above
    $ ls applied_proof.sh 2>/dev/null | wc -l   # no generator file is committed; the one script is pasted under applied-proof
    0

**measurer:** N/A — no lineage doc written
    $ git diff --name-only 3030551~1..3030551 -- 'assets/docs/framework-evolution-upstream-*' | wc -l
    0

> **Errata 1 (added after the push, 2026-09-26).** The `push:` and `push decay:` keys above were written before the owner asked for the push; both are updated in place to the pushed endpoint `aad5b74`, as "Authorized operations" requires ("WHEN A PUSH LANDS, ALWAYS UPDATE EVERY HASH-ENDING KEY"). Before the update they read `not requested … c98db39` and `unchanged … c98db39`.

## Post-change checklist receipts (`sefca24e`)

Batch: `efca24e` (v2.34.0, MINOR). It applies AF-7, AF-8, AF-9, AF-10, AF-11, AF-15, AE-15 and W-26, at the owner's request to fix what was still open; the owner chose W-26's wording on 2026-09-26. It also fixes AF-22..AF-27, which this session filed in `assets/docs/audit-2026-09-23.md` from the batch's pre-commit verifier. AF-2 stays open: its remainder needs a new control, which the moratorium forbids.

**inventory sweep:** N/A — no artifact added, removed, renamed or split; the new routing trigger lives inside validation-orchestrator. Every surface that repeats the changed verdict (`agentic_engineering_framework.md`, the VO report block, `autonomous-loop`) was updated in the same commit, and so were the three trigger-(b) surfaces (CLAUDE.md, README.md, audit.md Phase 0).
    $ git diff --name-status --diff-filter=ADR efca24e~1..efca24e -- docs/modules .claude/commands .claude/skills .claude/rules .claude/scripts examples | wc -l
    0

**instruction style:** 18 added, 3 rewritten, 1 buried — range `efca24e~1..efca24e`; unit: an added line carrying a CAPS imperative a session must execute. The 1 buried is the `NEVER emit nothing` tail of the widened `security-reviewer:` mandate — the known false positive, GREEN.
    $ D='git diff efca24e~1..efca24e -U0'; echo "instruction style: $($D | grep '^+' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') added, $($D | grep '^-' | grep -cE '\b(ALWAYS|MUST|NEVER)\b') rewritten, $($D | grep '^+' | grep -cE '[^0-9][.:] +\**(ALWAYS|MUST|NEVER)\b') buried"
    instruction style: 18 added, 3 rewritten, 1 buried

**references:** 2 of 2 cited targets resolve in their tier. `existing_project_adaptation.md` is tracked and cited by the lineage record; `project.md` is project-tier, created by bootstrap and cited by validation-orchestrator.
    $ git diff efca24e~1..efca24e | grep '^+' | grep -ohE '`[A-Za-z0-9_./-]+\.md`|component-design §[0-9]+|D[0-9]+\.[0-9]+[a-z]?|Phase [0-9]+ item [0-9]+' | sort -u
    `existing_project_adaptation.md`
    `project.md`

**fences:** 13 of 13 templates extract non-empty
    $ for t in docs/modules/templates/*.md docs/modules/rules/*.md; do f=$(grep -m1 -oE '^`{3,5}[a-z]*' "$t"); c=${f%%[a-z]*}; n=$(sed -n "/^$f$/,/^$c$/p" "$t" | wc -l); [ "$n" -gt 0 ] && echo ok; done | wc -l
    13

**isolation:** D16 log (patches + messages + paths): 1 commits scanned, 0 hits
    $ bash .claude/scripts/d16-gate.sh log efca24e~1..efca24e
    D16 log (patches + messages + paths): 1 commits scanned, 0 hits

**class sweep:** 14 fixes × 4 directions, 6 extra instances found and fixed (AF-22..AF-27, from the verifier)
    $ git show --name-only --format='' efca24e | wc -l
    14

| Fix ID | Commit | File touched | LATERAL | PARALLEL | ADJACENT | DESCENDING | Extra instances found |
|--------|--------|--------------|---------|----------|----------|------------|----------------------|
| AF-7 | sefca24e | docs/modules/rules/session_rules.md | ✓ `grep -rn "The oracle\|An oracle" docs/modules` → only this blockquote | n/a — rule has no twin | ✓ the closing "An oracle" also qualified | n/a | AF-27 |
| AF-8 | sefca24e | docs/modules/rules/session_rules.md | ✓ same blockquote | n/a | ✓ | n/a | none |
| AF-9 | sefca24e | docs/modules/skills/validation-orchestrator/SKILL.md | ✓ `grep -rn "VERBATIM" docs/modules` → only this rule | n/a — skill only in docs/modules | ✓ evidence sentence reworded | n/a — no report key | none |
| AF-10 | sefca24e | assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md | ✓ `grep -n "coordinated refresh"` → this line only | ✓ EPA v2.33.0 note already correct | ✓ correction note added beside it | n/a | none |
| AF-11 | sefca24e | assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md | same edit as AF-10 | n/a | same edit as AF-10 | n/a | same edit as AF-10 |
| AF-15 | sefca24e | docs/modules/skills/validation-orchestrator/SKILL.md | ✓ `grep -rln "Log in session log and project.md\|will trigger model switch\|If this is task 3+\|Could this pattern appear elsewhere"` → docs/agentic_engineering_framework.md (descriptive, left) | ✓ sprint-proposer, code_reviewer | ✓ | n/a | none |
| AE-15 | sefca24e | docs/modules/skills/validation-orchestrator/SKILL.md | ✓ `grep -rn "no client-bound data" docs/ .claude/ examples/` → 3 surfaces, all widened | ✓ autonomous-loop, security_reviewer | ✓ recognisable paths → AF-25 | ✓ report row + EPA slots → AF-23 | AF-22, AF-23, AF-25 |
| W-26 | sefca24e | .claude/commands/maintenance.md | ✓ residue grep under back-sweep → 0 | ✓ CLAUDE.md, README.md, audit.md Phase 0 (D10.6 parity) | ✓ push procedure now points at the rule | n/a — no report key | none |
| AF-22 | sefca24e | docs/agentic_engineering_framework.md | same edit as AE-15 | n/a | ✓ | n/a | same edit as AE-15 |
| AF-23 | sefca24e | .claude/commands/existing_project_adaptation.md | same edit as AE-15 | n/a | ✓ | ✓ the slots | same edit as AE-15 |
| AF-24 | sefca24e | docs/modules/agents/code_reviewer.md | ✓ `grep -n "added: s"` → the section's one format kept | n/a | ✓ | n/a | none |
| AF-25 | sefca24e | docs/modules/skills/validation-orchestrator/SKILL.md | same edit as AE-15 | n/a | ✓ | n/a | same edit as AE-15 |
| AF-26 | sefca24e | .claude/commands/existing_project_adaptation.md | ✓ `grep -n "^\*\*v2\.3[34]\.[01] migration"` → ascending order | n/a | ✓ | n/a | none |
| AF-27 | sefca24e | docs/modules/rules/session_rules.md | ✓ one other unqualified ops-rules citation, pre-existing, left | n/a | ✓ | n/a | none |

Per-fix table: rows 14 = findings applied in `sefca24e` 14.

**back-sweep:** 1 rule, 4 artifacts fixed of 4 found, re-run clean

| Rule promoted | Shape it governs | Grep that ENUMERATES the shape | N found | N fixed | Re-run |
|---|---|---|---|---|---|
| Trigger (b) is "before the bump is PUSHED" (W-26) | every surface stating trigger (b) with the old wording | the loop below, pre-fix vs tip | 4 | 4 | 0 remaining |

    $ P='MINOR or MAJOR version bump,|MINOR/MAJOR bump, on|BEFORE a MINOR or MAJOR bump\*\*|MINOR or MAJOR version bump \|'; for r in efca24e~1 efca24e; do n=0; for f in CLAUDE.md README.md .claude/commands/audit.md .claude/commands/maintenance.md; do n=$((n+$(git show $r:$f | grep -cE "$P"))); done; echo "$r residue: $n"; done
    efca24e~1 residue: 4
    efca24e residue: 0

**liveness:** skipped — no frontmatter touched
    $ git diff efca24e~1..efca24e -- docs/modules/skills/*/SKILL.md docs/modules/agents/*.md | grep -E '^[+-](name|description|model|effort):' | wc -l
    0

**negation proof:** the tool-gate trigger is absent from the pre-fix skill (0 hits) and present at the tip (6); the W-26 residue grep under back-sweep reads 4 pre-fix and 0 at the tip. These are read-only comparisons over `git show`, so no sandbox was used.
    $ git show efca24e~1:docs/modules/skills/validation-orchestrator/SKILL.md | grep -ciE 'tool gate'; git show efca24e:docs/modules/skills/validation-orchestrator/SKILL.md | grep -ciE 'tool gate'
    0
    6

**version:** v2.34.0 applied to 4/4 canonical surfaces — 0 v2.33.1 residues outside migration notes
    $ grep -rn 'v2\.34\.0' README.md docs/modules/templates/claude_md.md .claude/commands/existing_project_adaptation.md .claude/commands/audit.md | cut -d: -f1 | sort | uniq -c
          1 .claude/commands/audit.md
          5 .claude/commands/existing_project_adaptation.md
          1 docs/modules/templates/claude_md.md
          2 README.md
    $ grep -rn 'v2\.33\.1' README.md docs/ .claude/ CLAUDE.md | grep -v 'created: framework-v' | grep -vE 'v[0-9.]+ migration|pre-v[0-9.]+' | wc -l
    0

**classification:** 1 add, 13 substitute, 0 delete, 0 relocate. ADD is the agent-tool-gate spawn trigger (AE-15). SUBSTITUTE covers every other fix, each of which rewords existing content and loses nothing; W-26's rewording is the owner's decision.
    $ n/a — judgement, not measurement

**new component:** none this session
    $ n/a — judgement, not measurement

**gates:** twin parity PASS | receipt-rows-vs-diff 14/14 | row-count 14 vs 14 | slots 1/1 (`security-reviewer:` — its slot in the VO report block was widened in the same commit)
    $ diff -r .claude/skills/cross-cutting-analysis docs/modules/skills/cross-cutting-analysis && echo twin-dir PASS
    twin-dir PASS
    $ grep -cE 'applied `?sefca24e' assets/docs/audit-2026-09-23.md
    14
    $ grep -c "Security reviewer: ran — \[verdict\] | skipped — no client-bound data or tool gate" docs/modules/skills/validation-orchestrator/SKILL.md
    1

**push:** not requested for this batch — v2.34.0 is MINOR, so trigger (b) holds the push until its `/audit` report is persisted — origin/main at saad5b74
    $ git fetch -q --prune origin && git rev-parse --short refs/remotes/origin/main
    aad5b74

**audit:** proposed — trigger (b): a baseline `/audit` is owed before this MINOR bump is pushed
    $ git log -1 --format=%s efca24e | grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+'
    v2.34.0

**verification audit:** not triggered — no evidence from outside the loop
    $ PEND=0; for d in $(grep -L 'STATUS.*upstreamed' projects/*/.claude/docs/framework-evolution-*.md 2>/dev/null); do grep -rql "$(basename "$d")" assets/docs/framework-evolution-upstream-*.md || PEND=$((PEND+1)); done; OBS=$(grep -hE '^\| [A-Z]{1,2}-[0-9]+ \| HIGH \|' assets/docs/audit-*.md | grep -c '\[observed in use\]'); [ "$PEND" -gt 0 ] || [ "$OBS" -gt 0 ] && echo "trigger (d): fires — pending upstream docs $PEND, HIGH observed in use $OBS" || echo "trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog"
    trigger (d): does not fire — no evidence from outside the loop; this batch's findings are backlog

**placeholder:** none
    $ grep -nE 'applied `?s(BATCH|GROUP[0-9]+|PENDING)`?|\| *`?s(BATCH|GROUP[0-9]+|PENDING)`? *\|' assets/docs/audit-2026-09-23.md | wc -l
    0

**control back-sweep:** 1 of 1 touched controls re-run, 0 defects found — the one command-bearing added line is the EPA v2.33.1 detection grep, moved together with the notes
    $ git diff -U0 efca24e~1..efca24e -- .claude/commands/ docs/modules/ | grep '^+' | grep -cE '(grep|python -c|diff -r|git (diff|show|status)|node|sed -n)'
    1

**commit correction:** none
    $ git log --format=%s efca24e~1..efca24e | grep -ciE 'correct'
    0

**defect series:** not appended — no audit ran this day
    $ grep -c '^# Run [0-9]' assets/docs/audit-2026-09-26.md
    0

**applied-proof:** 14 of 14 IDs resolve to a commit touching their line (form A: 14, form B: 0), 0 forced to open
    $ bash applied_proof_2.sh   # the fence below saved as a file, run from the repo root
    AF-7 form A: 1
    AF-8 form A: 1
    AF-9 form A: 1
    AF-10 form A: 1
    AF-11 form A: 1
    AF-15 form A: 1
    AE-15 form A: 1
    W-26 form A: 1
    AF-22 form A: 1
    AF-23 form A: 1
    AF-24 form A: 1
    AF-25 form A: 1
    AF-26 form A: 1
    AF-27 form A: 1
```bash
R=efca24e~1..efca24e
while IFS='|' read -r id f a; do echo "$id form A: $(git log -S"$a" --oneline $R -- "$f" | wc -l)"; done <<'LIST'
AF-7|docs/modules/rules/session_rules.md|The oracle caught NONE of the six.
AF-8|docs/modules/rules/session_rules.md|> **unmeasured — no measurer below `production`, AND none above it either:** `framework-audit` Q4
AF-9|docs/modules/skills/validation-orchestrator/SKILL.md|audit found the transcript faithful but carrying number formats from the other language.)
AF-10|assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md|Six independent text additions; no coordinated refresh set.
AF-11|assets/docs/framework-evolution-upstream-2026-09-23-business-team-pilot.md|→ "v2.33.0 migration".
AF-15|docs/modules/skills/validation-orchestrator/SKILL.md|6. Log in session log and project.md.
AE-15|docs/modules/skills/validation-orchestrator/SKILL.md|AGENT TOOL GATE — on ANY route.
W-26|.claude/commands/maintenance.md|**ALWAYS PROPOSE `/audit` BEFORE a MINOR or MAJOR bump** — this is trigger (b) in CLAUDE.md, and
AF-22|docs/agentic_engineering_framework.md|the client-bound-data and agent-tool-gate triggers
AF-23|.claude/commands/existing_project_adaptation.md|v2.34.0 Known Bug Patterns rule
AF-24|docs/modules/agents/code_reviewer.md|in the entry format above AND grep for existing instances
AF-25|docs/modules/skills/validation-orchestrator/SKILL.md|a script a hook in those files runs
AF-26|.claude/commands/existing_project_adaptation.md|All three changed skills
AF-27|docs/modules/rules/session_rules.md|ops-rules §6, installed at `production`+
LIST
```

**report deletions:** none
    $ git diff --cached --name-only -- 'assets/docs/audit-*.md'   # the structural gate printed no RED line
    assets/docs/audit-2026-09-23.md
    assets/docs/audit-2026-09-26.md

**push decay:** unchanged since this session's own push — origin/main at saad5b74
    $ git fetch -q --prune origin && git rev-parse --short refs/remotes/origin/main
    aad5b74

**report restore:** none
    $ git log --diff-filter=D --name-only --format='' aad5b74..HEAD -- 'assets/docs/audit-*.md' | wc -l
    0

**verifier rounds:** 1 round, 6 defects reproduced, 6 fixed before commit — AF-22..AF-27; trigger 10 files; bash -n ratchet GREEN (audit.md, existing_project_adaptation.md, maintenance.md: new=0; twin tip=0). The reproduced defects, summarised:
- (1) The framework doc widened the row, but the two lines explaining its trigger still said client-bound only.
- (2) The EPA report had no slot for the two v2.34.0 in-place edits.
- (3) The code-reviewer rewrite added a second, incompatible Known Bug Patterns record format.
- (4) The VO tool-gate trigger located permission rules only; hooks and confined scripts had no recognisable path.
- (5) The EPA v2.34.0 note sat out of order and named only one coupled set.
- (6) session-rules cited ops-rules §6 (production+) with no tier qualifier, in a rule shipped at every tier.
    $ git diff --name-only efca24e~1 efca24e -- .claude/commands .claude/scripts docs/modules | wc -l
    10
    $ A=efca24e~1; B=efca24e; T=$(mktemp -d); for f in $(git diff --name-only "$A" "$B" -- .claude/commands); do for side in A B; do r=${!side}; git show "$r:$f" 2>/dev/null | awk '/^ *```bash *$/{b=1;s="";next} b&&/^ *```$/{b=0;printf "%s%c", s, 0;next} b{s=s $0 "\n"}' | while IFS= read -r -d '' body; do printf '%s' "$body" | bash -n 2>/dev/null || printf '%s' "$body" | grep -qE '<[a-z][^>]*>' || printf '%s' "$body" | cksum; done | sort > "$T/$side"; done; echo "$f base=$(wc -l < "$T/A") tip=$(wc -l < "$T/B") new=$(comm -13 "$T/A" "$T/B" | wc -l)"; done; rm -rf "$T"
    .claude/commands/audit.md base=0 tip=0 new=0
    .claude/commands/existing_project_adaptation.md base=0 tip=0 new=0
    .claude/commands/maintenance.md base=0 tip=0 new=0

**receipt generator:** none — receipts typed by hand from commands run in the session; the applied-proof script is pasted in full above
    $ ls applied_proof_2.sh 2>/dev/null | wc -l   # no generator file is committed
    0

**measurer:** N/A — no lineage doc written (the lineage record was corrected, not written)
    $ git diff --name-status --diff-filter=A efca24e~1..efca24e -- 'assets/docs/framework-evolution-upstream-*' | wc -l
    0

---

# Run 1

# Framework Audit Report

**Date:** 2026-09-26
**Framework version:** v2.34.0 (committed in `efca24e`, NOT pushed — this run is its trigger-(b) audit)
**Run mode:** `baseline` — trigger (b), before the v2.34.0 MINOR bump is PUSHED (owner request, 2026-09-26)
**Post-push D16:** log aad5b74..aad5b74 → 0 commits scanned, 0 hits — origin/main at saad5b74
**Dimensions checked:** 17
**Agents dispatched:** 6. Each agent received the two prepended rules verbatim, plus a pointer to its contract block in `.claude/commands/audit.md` (by line range), which it read from disk as its prompt.

**carried: 104 open + 0 escalated from `audit-2026-09-23.md` — AF-2 AF-13 AD-1 AD-9 AD-10 AD-14 AD-15 AD-16 AA-3 AA-9 AA-10 AA-12 AA-13 AA-16 AB-2 AB-3 AC-3 AC-6 AC-7 AC-8 AC-10 AC-12 AC-13 AC-20 AC-21 AC-22 A-14 A-15 A-16 A-17 A-20 A-21 A-22 A-23 A-25 A-26 A-34 A-35 A-37 A-39 C-3 C-4 C-5 C-8 C-10 C-12 C-14 C-15 C-21 C-22 D-4 D-5 D-7 D-8 D-9 D-10 D-11 B-1 B-4 B-15 B-20 B-25 V-1 V-2 V-3 V-4 V-5 V-6 V-7 V-8 V-10 V-14 V-18 V-19 W-15 W-21 W-22 W-23 W-24 W-25 W-27 W-29 W-31 X-15 X-18 Y-12 Y-14 Y-16 Z-2 Z-4 Z-5 Z-6 Z-7 Z-8 Z-9 Z-10 Z-12 Z-13 Z-14 Z-15 Z-16 Z-17 Z-18 Z-19**
- **Re-verification of the carried rows, stated honestly.**
  - 15 rows sit in files the audited range `c190212..1905c6e` did not touch, so they are still true by construction (Phase 3 item 3).
  - 89 rows sit in files the range touched.
  - None of the 104 IDs appears in any `Applies` line of the range's commits (`git log --format=%B c190212..1905c6e | awk '/^Applies /'` → 27 IDs, intersection with the carried set empty), so none was fixed by intent. They are carried as `open`.
  - A line-by-line re-read of those 89 was NOT done this run. Agents 1, 3 and 4 independently re-confirmed W-23, W-24, Z-18 and A-15 as still true.
- OBSERVATION — E-1 (accepted-risk; see its record): `log --all` still reads 9 hits in 4 already-pushed commits, one source-project doc filename. This is the residue the record closes. It is not re-opened.
- OBSERVATION — M-44, N-53, T-11, T-36, U-48, B-17: accepted-risk, per their records. They are not re-opened.

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | `cross-cutting-analysis` byte-identical in both placements |
| C | Instruction style | Quality | 5 | FAIL | 4 LOW, all older than this batch (AG-3..AG-6) |
| E | Version consistency | Currency | 5 | PASS | canonical v2.34.0, 0 mismatches, no `created:` rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15 listed = 15 on disk |
| D5 | Bootstrap file refs | References | 2 | PASS | 52 of 52 source paths resolve |
| D6 | Gap vocabulary | References | 3 | PASS | declarers 3 of 3; 10 gaps, 10 specialists, install rows on both twins |
| D7 | Pushy Description | Quality | 3 | PASS | `activation chains: 10 verified, 0 broken, 17 info` on both twins; 14/14 regression cases |
| D8 | Orchestrator refs | Process | 4 | PASS | every name and §heading citation resolves |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | all counts and entries match |
| D10 | Command → skill paths | References | 4 | PASS | 45 self-checks run, all match expectation; D10.6 parity holds on the new "is PUSHED" wording; 2 LOW (AG-10, AG-11) |
| D11 | Bootstrap sequencing | Process | 2 | PASS | 15 primary + 10 sub-steps = 25, matches the claim |
| D12 | Examples conventions | Currency | 5 | PASS | 20 agents / 9 skills / 11 rules, all conventions hold |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | every section read exists |
| D14 | Bootstrap output | Process | 2 | PASS | 13 of 13 template references traced |
| D15 | Doc factual accuracy | Currency | 5 | FAIL | 1 LOW omission in the concepts doc's pipeline table (AG-7) |
| D16 | Project-info isolation | Privacy | 1 | PASS | 0 hits on every live surface and the unpushed range; history residue = E-1 accepted-risk |
| D17 | Process coverage (meta) | Meta | 6 | FINDINGS | 1 LOW (AG-8); W-26 closed and complied with on its first MINOR |

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AG-1 | MEDIUM | `docs/modules/agents/security_reviewer.md:139`, `:196` | The shipped template cites `examples/agents/sast-scanner.md` and `examples/rules/compliance-rules.md`; in a project those live under `assets/examples/`, so both paths are unresolvable in every project that receives this agent (all tiers). (Agent 2.) | open |
| AG-2 | LOW | `.claude/commands/existing_project_adaptation.md:440` | "Before Steps 2.4-2.8" reads `assets/examples/README.md` "if it exists", but only Step 4.1 creates it; the forward reference is undeclared, so on a never-bootstrapped project the read is skipped silently. The sibling Step 2.7.1 defers explicitly. (Agent 2.) | open |
| AG-3 | LOW | `docs/modules/agents/validator.md:154` | "Restore code after each mutation." sits inside the mutation-test paragraph with no CAPS verb; skipping it leaves mutated code in the tree. (Agent 5, [C].) | open |
| AG-4 | LOW | `docs/modules/agents/validator.md:148` | The mobile-viewport (≤430px) check is lowercase-descriptive; its orchestrator twin (`validation-orchestrator/SKILL.md:98`) now says ALWAYS. (Agent 5, [C].) | open |
| AG-5 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:362-363` | "flag it for the next codebase-audit / framework-audit" has no CAPS verb and no stated destination for the flag. (Agent 5, [C].) | open |
| AG-6 | LOW | `docs/modules/skills/sprint-proposer/SKILL.md:231` | "After 3, flag to human…" (the discovery cap) is buried at the end of a bullet; the Sprint Report template has no slot for "cap exceeded". (Agent 5, [C].) | open |
| AG-7 | LOW | `docs/agentic_engineering_framework.md:414` | The bootstrap pipeline table's Step 5.8 row lists only the tier-gated skills and `skill_reviewer.md`; `bootstrap.md:493-504` also extracts `metrics_md`, `framework_metrics_md`, `ops_rules` and `quality_budgets` there. (Agent 5, D15.) | open |
| AG-8 | LOW | `assets/docs/audit-2026-09-26.md` (`control back-sweep:` receipt for `efca24e`) | The receipt attributes its count of 1 to the EPA v2.33.1 detection grep; re-run, the counted line is prose in `code_reviewer.md:293`, and the EPA grep was added in `3030551`. The step-(1) denominator command is also missing. Honest verdict: `N/A — no control touched`. Remedy is an erratum, not a control. (Agent 6, D17.5.) | open |
| AG-9 | LOW | `.claude/commands/audit.md` Agent 1 FILES TO READ vs D16.2c | D16.2c mandates grepping `.claude/commands/bootstrap.md` for the copy paths, and the list omits that file — its own `:321` rule. Same class as Z-18. (Agent 1.) | open |
| AG-10 | LOW | `.claude/commands/maintenance.md:1093` | Item 6 cites "`bootstrap.md:350` copies with `cp -r`"; the `cp -r docs/modules/skills/*` line is at `bootstrap.md:400` (350 is inside the plugin-vetting checklist). (Agent 4.) | open |
| AG-11 | LOW | `assets/docs/audit-2026-09-26.md` (`fences:` and `report deletions:` receipts for `efca24e`) | Two `$` lines are not the mandated commands byte-for-byte (a rewritten counting variant; a `git diff --cached --name-only` in place of the deletions gate). Re-run as mandated, both outcomes hold (13 of 13; no RED). (Agent 4.) | open |
| AG-12 | LOW | commit `efca24e` subject | Audit intake item 6 requires the subject to carry the count of distinct finding IDs; the subject states none, so the `Applies` count check has nothing to compare against. (Agent 4, noted for others; verified by the orchestrator: `git log -1 --format=%s efca24e`.) | open |
| AG-13 | LOW | `.claude/commands/audit.md` Phase 3 item 1 ("This is the only file the audit writes") | Contradicts the Authorized-operations list and CLAUDE.md, which both say the audit writes TWO files (the report and, in verification mode, one defect-series row). (Agent 4, noted for others; verified by the orchestrator.) | open |

**Carried forward from `audit-2026-09-23.md`** (rows copied verbatim, ORIGINAL IDs):

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| AF-2 | HIGH | `assets/docs/audit-2026-09-20.md:14` → `audit-2026-09-21.md` → `audit-2026-09-22.md` | **The carry-forward chain dropped 48 still-open findings. AD-1's recovery missed them.** `audit-2026-09-20.md` carried "the 48 older rows" in prose. AD-1's walk-back stopped at `audit-2026-09-16.md`, so B-, V-, W-, X-, Y- and Z-series findings last recorded `open` in `audit-2026-09-14.md` / `-15.md` have had no row for two runs. Measured: 114 IDs end `open`; 56 are in 09-22's rows, 10 are superseded R-series, 48 dropped. **Recovered into this run's ledger** (rows below). AD-1's mechanism (nothing COUNTS rows) is unchanged, so the class stays open. (Agent 6.) | open |
| AF-13 | LOW | `.claude/docs/idea-team-modules.md:203` (gitignored) | A local framework note names one of the production project's pilot agents by its literal name where a role descriptor would do. Its direction (spec → project or project → spec) cannot be read from the note. Not published: the file is gitignored and appears in no commit. (Agent 1, D16.3.) | open — fixed 2026-09-26 in the WORKING TREE only: the file is gitignored, so no commit can carry it and applied-proof cannot resolve it; re-verify by grep |
| AD-1 | HIGH | `assets/docs/audit-2026-09-20.md:14` and `:411` | **The carry-forward chain broke between two runs of the same file.** Run 3 declared `carried: 81 open` in PROSE and wrote no ledger ROW for any of them; Run 4 then read the ledger rows and declared `carried: 8`. Mechanically: 41 open/escalated findings across `audit-2026-09-16.md` and `audit-2026-09-19.md` have **zero** `^\| ID \|` rows in `audit-2026-09-20.md` (the A/C/D rows present there are Part 1 *verdict* rows, not carry-forward status rows). Phase 3 item 3's re-verification never ran over them. This run's own first carry-forward measurement read 24 and would have propagated the loss. **HALF-FIXED in `280c2f2`:** item 3 now requires ROWS and says to read the previous report's rows, not its `carried:` sentence; the 41 rows are restored. NOTHING COUNTS THEM — the reconciliation was drafted and cut (it read GREEN on this finding's own data, and the moratorium forbids a new control in that file). Stays `open`. | open |
| AD-9 | MEDIUM | `.claude/commands/maintenance.md` Gate 3 / Audit intake item 4 | Nothing asserts that an audit-applying session wrote a receipts SECTION into the report file, so the 27-key loop — self-scoped to "this run's section" — is skipped silently when the section is absent. Measured receipt-key coverage per commit body: `28b157a 0/27 · 847d18f 0/27 · 1b0bb67 1/27 · c9355b0 3/27 · 34a6c97 5/27 · 455c299 5/27 · 0bb7949 6/27 · 34a7c1c 9/27 · 961244b 27/27`. `34a7c1c` is the **v2.30.0 MINOR bump** — 9 of 27 keys, `verifier rounds:` written as prose (invisible to the loop's `^\*\*key:` anchor), zero `$` lines in the whole body. No control noticed. **HALF-FIXED in `280c2f2`:** the gap is now STATED in Gate 3, with both cut forms and why each failed. No check exists. Stays `open`. | open |
| AD-10 | LOW | `.claude/commands/bootstrap.md` (Steps 3, 4 vs `:449`) | `.claude/phases/` is never `mkdir`'d before Steps 3 and 4 write into it; the only creation sits inside Step 5.8's `internal-tool`+ branch, which never runs at `prototype`. Every other directory is explicitly created (`:378`, `:409`, `:421`, `:816`). Benign — the Write tool creates parents — but it is the one directory the command relies on implicitly. | open |
| AD-14 | LOW | `framework-audit/SKILL.md`, cited from `autonomous-loop/SKILL.md` and `session_rules.md` | `framework-audit` → Q4 is not a markdown heading in the target's body: it is the numbered list item `4. **Aspirational-vs-real audit**`; the only `### Q4 …` line sits INSIDE the fenced report template. Unambiguous and present, so not a broken reference — but it fails D8.7's "real markdown heading" test, and D8.7 says a bold/list lead-in is not a heading. | open |
| AD-15 | LOW | `.claude/commands/audit.md` D6.7 | D6.7's literal instruction — "`diff` the two install BLOCKS — the table AND the prose around it … expected: empty" — is **unsatisfiable by design**. The two commands deliberately diverge on mode-specific prose (bootstrap's Step 7/8/10 parenthetical; the `created:` lineage stamp), and the repo bounded the byte-equivalence claim to the `TWINNED SPAN` markers precisely to make it falsifiable (`/audit` 2026-09-10 U-24). Both bounded diffs are empty. **The check text is stale, not the repo** — a check that fires on the healthy state, the K-8/L-7 inversion. | open |
| AD-16 | LOW | `.claude/commands/maintenance.md:427` | "**ALWAYS report the harvest with its command and stdout**" names no key and is not in the 27-key registry, so the "every key exactly 1" loop cannot miss it. Measured: reported in 1 of the last 9 batches (`1b0bb67`); before that only in `audit-2026-09-15.md:566-568`, folded under `control back-sweep:`. component-design §10's own class inside the framework's apparatus. | open |
| AA-3 | LOW | `assets/docs/audit-2026-09-19.md:742`; harvesters at `.claude/commands/maintenance.md:97`, `.claude/commands/audit.md:111` | The `push:` receipt does not end with `— origin/main at sHASH` (a parenthetical follows), against `maintenance.md:50`. The harvester is `$`-anchored and takes `tail -1`, so it silently discarded that key and read the earlier `push decay:` line instead; its only RED fires when NOTHING matches. Harmless here (both lines carry the same hash), blind by construction. | open |
| AA-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227` vs `docs/modules/rules/session_rules.md:278-283` | A-18's schema/migration extra-receipt rule exists only inside the executed component; the section that owns the deploy moment ("Deploy gates"), which `:169` names as the gate's caller, never mentions it — component-design §9. | open |
| AA-10 | LOW | `.claude/commands/maintenance.md:939-942` vs `:949-950` | C-18's third element: step 4's mandated `probe-sandbox:` line has no slot in the `control back-sweep:` report form, which asks for "the command and stdout from step 1". It is reported only if the executor volunteers it. | open |
| AA-12 | LOW | `.claude/commands/audit.md:125`, `.claude/commands/maintenance.md:111` | Both state expectations (`origin/main at sNEW`, `unchanged at sHASH`) carrying an `s` prefix their fences never print; live output is `origin/main at af974ec`. The harvester tolerates it (`s?`), but a reader comparing stated-to-actual sees a mismatch on the healthy state — D10.0's own class. | open |
| AA-13 | LOW | `docs/modules/skills/codebase-audit/SKILL.md:145` | `HOOKS=$(grep -hsE '"command" *:' .claude/settings.json)` is line-based: with a single-line or minified `settings.json` the whole file lands in `$HOOKS` and a `permissions.allow` entry counts as an invoker again, re-opening C-33. Confirmed in the sandbox with pretty-printed vs one-line JSON. | open |
| AA-16 | LOW | `.claude/commands/audit.md:548-572` (Agent 3 FILES TO READ) | D7.6 names `.claude/scripts/probe-sandbox.sh` and mandates running it, but the file appears in no agent contract's list — violating the list's own closing rule ("Every file a CHECK below names MUST appear in this list"). Repo-wide: 0 hits for `probe-sandbox` inside any FILES TO READ / INPUTS block. Same class as the open Z-18 (`d16-gate.sh`), so the Z-18 fix should carry this sweep. | open |
| AB-2 | MEDIUM | `.claude/commands/existing_project_adaptation.md` | The COUPLED REFRESH mandate has no slot in the final report; the guard slot offers refreshed/kept and cannot hold a coupled decision | open |
| AB-3 | MEDIUM | `docs/modules/rules/session_rules.md` | "NEVER DOWNGRADE a component's model…" is a banned process anti-pattern with no mechanical self-check (component-design section 6) — see AC-7, which refutes the stated deferral reason | open |
| AC-3 | MEDIUM | commit `adc7481` body vs `assets/docs/` | AB-1..AB-11 were filed only in a commit message. A grep for the ledger row shape over every report returns 0. Three readers are keyed to `assets/docs/audit-*.md` and none reads a commit body: the ID collision check, Phase 3's carry-forward, and trigger (d)'s `OBS` term. AB-2, AB-3 and AB-7 are `open` and in no ledger. Deposited into THIS report's ledger below as the remedy for the instance; the class needs a second form on `maintenance.md` item 10. | open |
| AC-6 | MEDIUM | `.claude/commands/maintenance.md` receipts form vs its self-check | The file authorizes the commit message body as a receipts home; the mandatory receipt self-check extracts a `## Post-change checklist receipts (…)` heading from a REPORT FILE and asserts a `$` line per key. Measured against `adc7481`'s body: 0 bold-form keys, 0 `$` lines, while all 27 keys are present in plain form. One authorized receipts home has a self-check that structurally cannot pass. The push-decay detector carries a written KNOWN LIMIT for this same form; this check carries none. | open |
| AC-7 | MEDIUM | `docs/modules/rules/session_rules.md` → the NEVER DOWNGRADE line | AB-3 was left open on the reason "a check would need value history, not presence". The reason does not survive: its two neighbouring NEVERs ARE mechanically guarded, and value history is what a git repo has — the framework already ships a base-vs-tip set-comparison ratchet (the bash-syntax ratchet). A `git diff` over `.claude/agents` scoped to removed/added `model:` pairs moving down the tier order is the direct analogue. | open |
| AC-8 | MEDIUM | `.claude/commands/maintenance.md` checklist item 6 | `model:` on the 30 shipped agent templates is verified by nobody in THIS repo. Item 6's guard command scans `.claude/skills/` and `.claude/agents/` (this repo has no `.claude/agents/`); its raw-template loop globs the module skills and agents and asserts only `name:`. `examples/` appears in no glob in that command file. All 30 carry it today; a future edit dropping it ships to every bootstrapped project and is caught only there, after the fact — the `criteria_enforcer.md` shape item 6 itself cites. | open |
| AC-10 | MEDIUM | `.claude/commands/audit.md` D6.7 | The mandated install-BLOCK diff states "expected: empty" over a span that legitimately diverges on both paths (bootstrap pre-selection vs EPA already-exists; the two lineage stamps). It can never return empty, so every run hand-adjudicates. The block carries no TWINNED SPAN markers, which is the mechanism the repo already uses for the Step 12.5b criteria; the span boundaries are described in prose, so two auditors extract two different spans. | open |
| AC-12 | MEDIUM | `.claude/commands/bootstrap.md` vs the EPA twin | EPA says "Then RUN it once now"; bootstrap names `check-agent-frontmatter.mjs` four times and never executes it, and Step 15 has no guard verdict slot. Bootstrap is the run that hand-writes `model:` into six agents from templates it adapts, then commits at Step 14.5 — and per component-design section 8 the break cannot manifest in the session that wrote it. At `prototype` the CI floor is skipped, so first detection is session 1.  **CUT from the 2026-09-21 backlog batch 4** — the "Then RUN it once now" mandate is sound and the step order checks out, but reporting the run needs named outcomes (pass / fail on exit 1 / skipped without Node) that no step defines yet. Destination first, then mandate (component-design §10). | open |
| AC-13 | MEDIUM | commit `adc7481` vs Cycle governance rule 2 | AA-13 lives at `docs/modules/skills/codebase-audit/SKILL.md:145`. `adc7481` edited that file. Rule 2: "ALWAYS apply a LOW finding when this session already edits its file for another reason, and say so in the commit." It was neither applied nor named; the commit records `class sweep: N/A`. | open |
| AC-20 | LOW | `.claude/commands/maintenance.md`, the push-decay KNOWN LIMIT | The documented failure mode ("the next session sees RED: no recorded origin/main hash") does not occur: the extraction returns a live hash three commit-body sessions stale, so the detector prints a plausible `advanced …`. It FAILS SAFE (over-scans an already-scanned range), but a reader relying on that sentence will not recognize the state they are in. | open |
| AC-21 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:246` | A nested bold marker inside a bolded mandate inverts the rendering: the outer bold closes at the inner marker, leaving a second distinct mandate UNbolded and a dangling trailing marker. Section 6 property 2 (dedicated line) plus a rendering defect. | open |
| AC-22 | LOW | `docs/modules/skills/sprint-proposer/SKILL.md:144-145` | Announces "(expected result stated)" and states neither a command nor a count — it cannot go red. The same file's Step 1 and `session-end` both give a real countable grep. | open |
| A-14 | LOW | `docs/modules/skills/session-log-creator/scripts/create-log.sh:26-27` vs `docs/modules/skills/session-log-creator/SKILL.md:46` | The generator writes the `## Review receipts` slot with no pre-deploy gate line in either the GREEN or the RED form. A log created by the script cannot carry the gate result the skill requires. B-23's and B-26's twin, not swept (847d18f did not touch the script). | open |
| A-15 | LOW | `.claude/scripts/d16-gate.sh:216-221` (`dir` scope); `.claude/commands/audit.md` D16.3b | `dir` matches paths as given. The memory directory can only be passed as an absolute path, so every ancestor directory name is scanned, and an identifier matching a word in the owner's home path hits every file although the contents are clean. Real run: 12 hits over 12 clean files. Probe: absolute path 2 hits, relative 0. D16.3b does not mention it. | open |
| A-16 | LOW | `autonomous-loop/SKILL.md:630-631` vs `:668-671`, `:313-315` | A mid-task marker removal by another session makes the closure check RED. The closure-RED rule then requires writing `State: stopped`, which needs the marker, and the revocation branch says NEVER re-create the marker. No precedence line. Introduced by X-13's fix. | open |
| A-17 | LOW | `autonomous-loop/SKILL.md:675-677`, `:706`, `:736-738` | The C5 digest offers "triage held/deferred" as an owner answer, but neither the resume line (`:675-677`) nor C6's reset (`:736-738`) says whether it resumes or resets. X-19's class, not swept; the receipt's claim about `:675-677` is false. | open |
| A-20 | LOW | `docs/modules/agents/diff_pattern_extractor.md:87-92`; `autonomous-loop/SKILL.md:505-509`; `codebase-audit/SKILL.md:110-112`, `:147-148`; `framework-audit/SKILL.md:81-82` | Five behavioural requirements added by the two batches are buried in note sentences, KNOWN LIMIT paragraphs or sentence tails ("— and ALWAYS file ONE task", "MUST carry its own", "say so, and record the step as ⏭️", "and say so in the report", "say so when one does"). Third consecutive batch pair needing an instruction-style follow-up. | open |
| A-21 | LOW | `autonomous-loop/SKILL.md:353-354`, `:494` vs `:366-373` | The every-subagent resource declaration still omits the MUTATOR/READER class and the scratchpad. `READERS` at C1 `:494` means "never adds a task", a different sense from READER at `:366-373`, so `criteria-enforcer` (writes `pendencias.md`) is classed both ways. B-12's class plus a new defect. | open |
| A-22 | LOW | `autonomous-loop/SKILL.md:421-424`, `:148` vs `:641` | "Emit the lessons block before `/session-end`" exists only under C5 (continuous). Segment-mode continuation and exception stops run `/session-end` with no such step, so their orchestration lessons never reach the log. B-19 D9, not swept. | open |
| A-23 | LOW | `autonomous-loop/SKILL.md:608-627` | The C4 closure check's first-parent traversal reads OK for a `--no-ff` merge whose side branch carries code committed after the counter moved (throwaway case P4); the KNOWN LIMIT (`:626-627`) does not declare it. Introduced by X-9's fix. | open |
| A-25 | LOW | `.claude/commands/maintenance.md` "Cycle governance" rule 3 (`:232-233`) | The moratorium self-check counts receipt keys (27) and numbered items (14) only. A new prose-embedded control reads 27/14 (planted), so 742a691's new "expected ≥1 hit in EACH" check and D15.2's new coverage check passed unseen. It does not cover `audit.md`, and batch 2's receipts neither run nor report it. | open |
| A-26 | LOW | `autonomous-loop/SKILL.md:5-19`, `:536`, `:664`; `sprint-proposer/SKILL.md:68-102` | "cancel continuous mode" is read only in the autonomous-loop body. A session without that skill loaded is never told to remove the marker. X-8 residue. | open |
| A-34 | LOW | `.claude/commands/existing_project_adaptation.md:598-600` vs `:856-864` | Step 2.9 takes the refresh/keep decision for `session-log-creator` before the profile exists; the Step 2.9b pairing rule ("at `production`+, ALWAYS PRESENT THEM TOGETHER" with framework-audit and autonomous-loop) then cannot apply to a decision already taken. Introduced by 5ecefa9 (B-11 fix). | open |
| A-35 | LOW | `.claude/commands/existing_project_adaptation.md:1358`, `:1377-1382` | The EPA report has no slot for `.claude/docs/` (created at `:682`) or `.claude/skill-gate/review_reports/` (created at `:819`); bootstrap reports both (`:944`, `:984`). Twin asymmetry, W-18's class.  **CUT from the 2026-09-21 backlog batch 4** — slots were drafted with verdicts `created` / `already present`, but both directories are made by a bare SILENT `mkdir -p` and no step emits either value (and the report retires `already present` two lines below). Closing it needs the fences to `test -d` and echo a verdict FIRST, then the slot copies it. | open |
| A-37 | LOW | `.claude/commands/existing_project_adaptation.md:517`, `:595`, `:862`, `:894`, `:1121`; `.claude/commands/bootstrap.md:113`, `:342`, `:892` | V-17's class, not swept: a rationale clause fused with its ALWAYS in one sentence survives at 8 sites. Two of them (`EPA:595`, `:862`) are invisible to the style regex because `**` precedes the space, and the batch 2 receipt marked the twin "n/a — prose only". | open |
| A-39 | LOW | `.claude/commands/audit.md`, `.claude/commands/maintenance.md:1416-1424`; lineage docs' efficacy-anchor tables | B-11's mother-side half: the maintenance writer must name a measurer, but no `/audit` or `/maintenance` step ever reads an anchor's measured result back (`grep -ciE 'efficacy\|hypothes\|measurer' audit.md` → 3 unrelated hits). An anchor whose measurer silently breaks (A-3) is never noticed from here. | open |
| C-3 | MEDIUM | `assets/docs/audit-2026-09-15.md:1336`, `:1349`, `:1915`, `:1928`; enumerator `assets/docs/audit-2026-09-16.md:380` | A-2's class is not swept. The two persisted receipt generators execute the fail-open np1/np2 (`bash "$NP"`) under `$` lines that still say "run from the repo root", with no erratum. The batch's back-sweep enumerator reads `0` over them because it matches only literal git write verbs inside a fence. Agent 6's stand-in probe: np1 still commits (213 → 218), pushes and rewires origin when its scratch clone fails.  **CUT from the 2026-09-21 backlog batch 1** — three errata were written and all three withdrawn: one was spliced INSIDE the bash fence it annotated (breaking its parse), and the replacement set over-applied to a third generator that uses neither `bash -c "$cmd" 2>/dev/null` nor `$NP`. Needs errata for exactly the TWO generators this finding names. | open |
| C-4 | MEDIUM | `.claude/commands/audit.md:1437-1441` (Phase 3 item 7) vs `:36-38` | Item 7's fence `git add`s only the report, and the rewritten self-check is scoped to the report path. When item 6 has appended a defect-series row to `audit.md`, that row stays uncommitted while the check prints `report committed`. Sandbox: `report committed: sH` then `uncommitted after the fence: M .claude/commands/audit.md`. U-47's residue, adjacent to A-31. | open |
| C-5 | MEDIUM | `.claude/commands/audit.md:18-31`, `:223-236` | Whether a check WRITES — and therefore goes to the sandbox — is decided by whoever runs it, and nothing compares the live repo before dispatch with the live repo after the agents return. A writing check misrouted in place (the incident's own shape) is still caught by nothing: `grep -c fingerprint .claude/commands/audit.md` → 0. Concrete instance: C-13. | open |
| C-8 | LOW | `.claude/commands/audit.md:1439-1441` | A same-day re-run whose HEAD is the previous run's `docs(audit): persist` commit to the same file reads `report committed` before this run's report exists (sandbox: `report committed: sH` with no Run 2 content). Phase 3 item 1 (`:1368-1371`) calls same-day runs the normal shape. A-31/A-41 class, one case over. | open |
| C-10 | LOW | `.claude/commands/audit.md:852` (D10.0) vs `.claude/commands/maintenance.md:874-875`, `:1106` and `audit.md:47` | A-42 and A-46 were not swept into D10.0. D10.0 mandates `run --staged` for every writing check, including item 5's pre-fix reconstruction, which maintenance says must NEVER combine `--staged` with a checkout. D10.0's run-every-check mandate also reaches item 10's `--keep` recipe, which `audit.md:47` now forbids agents. | open |
| C-12 | LOW | `.claude/commands/audit.md:674-686` (D7.6 steps 1-2) | The steps require a synthetic project in a scratch directory, run in the sandbox, whose working directory is the clone. They never say that the extracted loop resolves `projects/$ARGUMENTS/.claude/agents` relative to the current directory (`bootstrap.md:697`), so the auditor must work out the `cd` and `ARGUMENTS` alone. | open |
| C-14 | LOW | `assets/docs/audit-2026-09-16.md:374-376` (`fences:` receipt) | The `$` line is the mandated command with a `| awk` summariser appended, so the pasted stdout (`zero: 0 of 12`) is not the mandated command's literal output — against `maintenance.md`'s "THE `$` LINE MUST BE THE MANDATED COMMAND, BYTE-FOR-BYTE". | open |
| C-15 | LOW | `.claude/commands/maintenance.md:1022`; `.claude/commands/audit.md:137` | Cross-file citations `/audit` → Authorized operations (a bold lead-in; added by 194f44a for A-48) and `maintenance.md` → the `push:` rule (a bullet) point at non-headings, which D8.7 marks RED.  **CUT from the 2026-09-21 backlog batch 2 after three verifier rounds** — the D8.7 amendment (sanctioning a self-declared non-heading citation) is the right idea: two such citations were already live and resolving, and unamended D8.7 flags both. But every implementation left a gap — a citation with a FALSE "where" fell outside BOTH the sanctioned and the RED list, letting a citer silence the check on an unverified assertion. Reverted whole. | open |
| C-21 | LOW | `.claude/commands/audit.md:226-232`, `:20-30` | The isolation rule sends "every probe" to the sandbox and forbids ever putting a read-only scan (`d16-gate.sh … dir`) there, with no tie-break for a probe that is itself a read-only scan (e.g. a planted `D16_EXTRA` over `dir`, which Agent 1 had to run in place to see `.claude/docs/`). | open |
| C-22 | LOW | `.claude/commands/audit.md:229` vs D16.2 (`:318-320`) | D16.2 harvests "repo URLs", but the verbatim rule bans `git remote`, the only direct source, and neither names a sanctioned read-only route (e.g. `git config --get-regexp '^remote\..*\.url'`). Agent 1 ran `git -C projects/<each>/ remote -v` read-only to fill the class. | open |
| D-4 | LOW | `.claude/commands/existing_project_adaptation.md:773-777` vs `:897-953` | A-8's residue: the PHASE-3 RE-CHECK (risk profile, and now the Red/Blue Team decision) is mandated only inside Phase-2 Step 2.9b, and Phase 3 never points back to it (0 grep hits for `2.9b|2.6|profile|re-check`). The moment has no invoker (component-design §9.1). | open |
| D-5 | LOW | `.claude/commands/existing_project_adaptation.md:1215-1365` (Step 5.1) vs `:877-878` | A-8's residue: nothing mechanically checks that `.claude/agents/red-team.md` exists when `code-reviewer.md` carries "red-team is MANDATORY on every diff that touches a money path". Step 5.1's fence has 0 red-team lines although it checks validator and arbitrator existence (`:1246`), so the defect A-8 named can still ship, caught only by prose (§9.2). | open |
| D-7 | LOW | `.claude/commands/existing_project_adaptation.md:617-623` (added by 87210b1) | The v2.27.0 migration note has three defects: its headline says "two pairs travel together" and names an ALWAYS for one; it claims an older `session-log-creator` "only lacks the pathspec-RED slot" when the pre-batch slot also lacked the `… K without receipt — deploy blocked` form; and its "ALWAYS offer `session-log-creator` with it" is buried mid-paragraph with no executing moment — `validation-orchestrator` is asked in Step 2.9b's coupled-set question (`:599-603`) while `session-log-creator` is asked in Step 2.9, so the pairing crosses that split (A-34's class). | open |
| D-8 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:227-229`, `:231` | A-18's new ALWAYS line (a schema or migration commit also needs an adversarial or data-reviewer receipt) has no output format and no stated-skip form, and the ALWAYS REPORT line covers only the gate's four strings — so whether the deploying session performed the check is structurally invisible (component-design §6 property 3, §9.3). | open |
| D-9 | LOW | `docs/modules/skills/validation-orchestrator/SKILL.md:218-219` | "A verdict the list does not know BLOCKS — extend `PASS`, never the reverse" is a behavioural requirement for every project that customises the gate, buried in lowercase at the end of a rationale paragraph (§6 C.4). | open |
| D-10 | LOW | `.claude/commands/maintenance.md:1112-1115` | C-19's fix introduced a defect: `rm -rf "${M%/r}"` now sits in the same executable fence that creates `$M`, so a verbatim run deletes the scratch mother repo before the verifier receives it, and "run it when the verifier returns" lives only in a `#` comment. Shell state does not persist between calls, so a deferred run has no `$M` either.  **CUT from the 2026-09-21 backlog batch 1** — the fix hoisted the deletion’s timing out of a `#` comment, and a verifier proved the mandate unexecutable: `$M` does not survive between shell invocations, so the deferred `rm -rf` is a silent no-op and the temp dir leaks. Closing it needs the recipe to persist the path, not a better sentence. | open |
| D-11 | LOW | `docs/modules/templates/metrics_md.md:21` vs `:14-17` | A-7's class, unswept to the twin template: the CODE-series baseline row reads `COMPLETE` with no `steps:` line although its own prose says Status is followed by the per-step line, and `framework_metrics_md.md:30` was updated. The same self-check gives 1 on the sibling row and 0 on this one. | open |
| B-1 | LOW | `.claude/commands/existing_project_adaptation.md:829-833`, `:805-813`, `:582` | A leftover coupled-set member (`codebase-audit` or `skill-gate`) at a tier below internal-tool gets no verdict from any fence in Steps 2.9 or 2.9b (probe E2: zero lines for a locally-modified leftover). The coupled-set question and slot can therefore read "no member differed" while one differs, or the set refresh can overwrite it without the diff being shown. Introduced by the V-9 fix. | open |
| B-4 | LOW | `.claude/commands/existing_project_adaptation.md:819`, `:1341` vs `.claude/commands/bootstrap.md:464-465` | EPA copies `ops-rules.md` but never fills the ops-rules §6 reconciliation queries for production-financial, which bootstrap Step 5.8 does; EPA's money-path slot carries only the red-team half. V-13's class, not swept. | open |
| B-15 | LOW | `.claude/commands/existing_project_adaptation.md:805-813`, `:1339` | The Step 2.9b tier fence prints nothing at prototype tier and also nothing for a PROFILE left as the placeholder or mistyped (probe G: `production_financial`), so a typo reads as prototype; the slot value `not copied (tier)` is printed by no fence. Pre-existing, in no earlier ledger. | open |
| B-20 | LOW | `.claude/commands/existing_project_adaptation.md` Step 2.9b tier fence (`tier_skill framework-audit` only in the `production` branch) | [filed by the maintenance session applying Run 3, on 2026-09-16, from its independent pre-commit verifier, round 1] A leftover `framework-audit` copy in a project now below `production` gets no verdict line, so the new `framework-audit` + `session-log-creator` ↔ `autonomous-loop` pairing rule cannot see it. B-1's class (leftover tier-gated members), not in this session's scope. | open |
| B-25 | MEDIUM | `docs/modules/rules/evolution_policy.md` → Exemption; `docs/modules/skills/session-end/SKILL.md` gate activity; `docs/modules/skills/framework-audit/SKILL.md` Q5; `.claude/commands/existing_project_adaptation.md` | [filed by the maintenance session applying the 2026-09-16 upstream and the carried X, W, V and Z findings, on 2026-09-16, from its independent pre-commit verifier, round 3] The exemption's commit-message line has no writer at the moment of an upstream copy (the adaptation command copies templates and never mentions it) and no reader: session-end counts only `promotion.log`, and nothing detects "a new file with neither that line nor a promotion.log entry", so an exempt file and a silent bypass look the same. A mechanical detector also cannot tell a copied rules template from a new rule: skill templates carry `created: framework-v…`, rules templates carry no stamp. | open |
| V-1 | MEDIUM | `.claude/commands/audit.md:233`, `:315-325` (D16.1, D16.3c); `.claude/scripts/d16-gate.sh` | The one-pass D16.3c procedure's `expected: …, 0 hits` is unreachable on a healthy repo: D16.2c identifiers are harvested from framework surfaces and read RED by construction (113 → `staged` 362 hits), D16.2b shapes the dimension calls legitimate read RED (e-mail → `log --all` 808 from author lines and the mandated trailer; `user:pass@` → 1 scanner example), the gate has no allowlist and prints one total, and "NEVER by printing the matched line" leaves D16.5's file:line and D16.2b's legitimacy judgement no sanctioned route. Introduced by the Z-3 fix. | open |
| V-2 | MEDIUM | `.claude/commands/maintenance.md:18-28`, `:438` vs `CLAUDE.md:111` | The push gate and the `isolation:` receipt run the gate on folder names only and never feed `D16_EXTRA`/`D16_EXTRA_RE`, while CLAUDE.md mandates D16 — all three blocklist sources — over the unpushed commits before every push; person names, domains and code identifiers in an unpushed commit are scanned only if `/audit` happens to run first. Z-3's class, not swept. | open |
| V-3 | LOW | `.claude/commands/audit.md:285` (D16.3), `:293` (D16.3b), `:192`, `:367` (Agent 1 FILES TO READ, REPORT FORMAT) | D16.3 and D16.3b still prescribe hand greps for the blocklist and value shapes over tracked files and memory, and Agent 1's input list and report format still describe the banned "contents + messages" shape with no paths, tags or `D16_EXTRA` proof. Z-3's class, not swept. | open |
| V-4 | LOW | `.claude/scripts/d16-gate.sh:17-21`, `:112-123` | Nothing proves the extras were loaded: the output line is identical with and without them; `D16_EXTRA=` set to an empty string is treated as unset and reads GREEN (the header says a set variable fails closed); a PCRE escape (`\d`) in `D16_EXTRA_RE` silently matches nothing; there is no minimum identifier length (a 2-letter identifier gives 25 false hits); the identifier files are unnamed and unpersisted, so a second reader cannot re-run the scan. | open |
| V-5 | LOW | `.claude/scripts/d16-gate.sh:14`, `:185`, `:29-30`; `.claude/commands/maintenance.md:26-28` | Z-1's class not swept: the script's usage line and error text still offer `origin/main..HEAD` as the example range; and the snippet's refnames are unqualified — a local branch `refs/heads/origin/main` at `main`'s tip with `core.warnAmbiguousRefs=false` makes the gate read `0 commits scanned` while the push publishes the leak. | open |
| V-6 | LOW | `.claude/scripts/d16-gate.sh` selftest (`:226-379`) vs `.claude/commands/maintenance.md:482` | The Z-21 fail-closed branches — empty or whitespace-only `D16_EXTRA`, NUL bytes or an unreadable `D16_EXTRA_RE`, an invalid ERE — have no selftest case, although the commit says "the gate cases are selftest cases". Extends Z-6. | open |
| V-7 | LOW | `.claude/commands/bootstrap.md:27-34`; `.claude/commands/existing_project_adaptation.md:27-34`, `:577-578` | Both Setup guards state "Every fence in this command expands `projects/$ARGUMENTS`" (false for 6 of 18 bootstrap fences and at least 4 EPA fences); the EPA backstop runs after 7 fences that already expand the name without saying so; bootstrap has no mechanical backstop. Introduced by the Y-8 fix. | open |
| V-8 | LOW | `.claude/commands/existing_project_adaptation.md:709-714`, `:577-578` | The Step 2.9 STOP rule bans continuing past a FAILED component with no mechanical self-check (component-design §6), offers no reportable value when the owner declines a fix (§9.3), and does not cover the backstop's `FAILED: project name…` lines. Y-9 residue. | open |
| V-10 | LOW | `.claude/commands/maintenance.md:978` | "Version bumps" still says `/existing_project_adaptation` refreshes an existing skill "only on the owner's per-skill decision"; the batch made EPA ask one question for the whole coupled set. X-4's lateral, not swept (the receipt's LATERAL grep covered EPA only). | open |
| V-14 | LOW | `.claude/commands/bootstrap.md:902` vs `:249-250`, `:269-270` | The Step 15 cross-cutting slot copies the Step 1.1 and Step 13 verdicts but still paraphrases the Step 3 and Step 4 receivers ("A/A decisions · T/T tasks") against their mandated `cross-cutting received: …` lines. W-18's class, not swept. | open |
| V-18 | LOW | `.claude/commands/maintenance.md:966-973` ("Version bumps") | The PATCH/MINOR policy does not classify additions to the command pipeline, and afa8ef0 was labelled PATCH while adding new contract — a line written into adapted projects' `code-reviewer.md`, new report sections, new Setup and STOP mandates, a new gate interface — which exempted it from trigger (b). Second consecutive batch with the ambiguity (Run 1's observation). | open |
| V-19 | LOW | `.claude/commands/maintenance.md:786-791`, `:802-804` | The control-enumerator's inline shape counts any backticked `git …` span, so the "+1" recorded in the re-measured baseline is `` `git push origin main` `` — the gated action, not a control; the baseline comment calls it "one inline control". | open |
| W-15 | LOW | `.claude/commands/bootstrap.md:145-156` | Step 1.2 says "Steps 2 and 3 ALWAYS confirm receipt", but neither receiver mentions the risk profile. The mandate lives only in the sender (component-design §9). | open |
| W-21 | LOW | `.claude/commands/maintenance.md:236`, :177 | The elision grep catches only a LEADING `<` on a `$` line. It returned 0 on d812c07 while two mid-line angle-bracket `$` lines existed (a looser form returns 2). The "stdout must be literal, never a parenthetical" rule has no mechanical check. | open |
| W-22 | LOW | `.claude/commands/maintenance.md:420` | The references denominator regex cannot see `→ "Heading"` citations, the class d812c07 added (5 distinct), so `references:` under-counts its own stated unit. | open |
| W-23 | LOW | `.claude/commands/audit.md:243-246` | The copy-path derivation returns 29 lines against a prose list of exactly 5; an "at least 5" threshold cannot realistically go RED. | open |
| W-24 | LOW | `.claude/commands/audit.md` D6.7 | "diff the two install BLOCKS — table AND prose — expected: empty" no longer fits the commands, which scope parity to the marked TWINNED SPAN (U-24). The whole-block diff is structurally non-empty (rc=1), while the TWINNED SPAN diff and the loops are identical. | open |
| W-25 | LOW | `.claude/commands/maintenance.md` Audit intake item 8, :537 | A correction-only follow-up commit has no stated receipt form. Its three executions (d41022b, b9d2ee2, e82176f) used three forms. Item 7's exemption regex covers bumps and substitutions, not corrections. | open |
| W-27 | LOW | `docs/modules/skills/session-end/SKILL.md:112` vs :125 | "Items 1-3 are the critical minimum" vs "at minimum items 2, 3, and 6". | open |
| W-29 | LOW | session-end:125; sprint-proposer:199-203, :214, :240; validation-orchestrator:104, :145; validator.md:147-148, :164-166; code_reviewer.md:208-210, :264, :291 | Pre-existing instruction-style instances (lowercase or title-case imperatives, fields buried in prose). | open |
| W-31 | LOW | `docs/modules/skills/rules-agents-updater/SKILL.md:119` | The vocabulary-alignment check is still a whole-file `grep -l`, the T-9 false-match shape the activation loop was fixed to avoid. | open |
| X-15 | LOW | `assets/docs/audit-2026-09-14.md` Run 1 receipts (`:379`, `:411-425`, `:456`, `:498`, `:531`) | The batch's receipts repeat the evidence-form class W-11 corrected: 20 `✓` cells with no quoted command; `push:` outside the enumerated verdicts; `control back-sweep:` not in `K of N` form; `negation proof:` points at a command-less commit body; `:438`'s negation is claimed with no `$` line; "no figure below was typed" is false. | open |
| X-18 | LOW | `.claude/commands/maintenance.md:931-935` | The new "ALWAYS NAME THE MIGRATION PATH" rule has no receipt key and no mechanical check. | open |
| Y-12 | LOW | `.claude/commands/maintenance.md:638` | Gate 3 run verbatim over the report returns 7 against 6: `audit-2026-09-14.md:901`, a receipt quoting the pattern, self-matches. The section-scoping rule at :647-653 is prose; the command itself is unscoped. | open |
| Y-14 | LOW | `audit-2026-09-14.md` receipts (`s700a382`, `:810-964`) | X-15 recurs. 8 of 15 `✓` cells quote no runnable command (one records the X-23 defect under `✓`). `push:` is outside the enumerated form. `control back-sweep:` reads "2 of 63 re-run" against the mandate to run every control. `negation proof:` gives no `$` line for the gate seeds. The `liveness:` `$` line is a rewritten variant of the mandated command. | open |
| Y-16 | LOW | `.claude/commands/audit.md` D17.6 | The shipped/apparatus split is by path, so `existing_project_adaptation.md` and `bootstrap.md` — the delivery path of the shipped contract — count as apparatus. X-1 (HIGH, 0 process agents delivered to existing projects) sat in a "0% shipped" batch. | open |
| Z-2 | MEDIUM | `.claude/scripts/d16-gate.sh:224-260`, `:305`, `:309-310` | The selftest generates its seeds with the gate's own derivation, so a derivation gap is invisible to it: over a synthetic mother repo with an uppercase-run CamelCase and a dotted folder it reads `66 of 66` while the gate reads 0 on their variant forms; with the letter-boundary fix mutated out it still reads `37 of 37`. Its green controls are five fixed words that cannot fail under a different folder set. Y-6's class restated at the oracle. | open |
| Z-4 | LOW | `.claude/scripts/d16-gate.sh:76-83`, `:32-37` | Name derivation splits only on `_`, `-`, space and on lower→Upper: a folder name with `.` or `+`, or an uppercase-run CamelCase (acronym prefix), derives no working variants or parts (snake, spaced and part forms read 0). Neither fails closed nor appears in KNOWN LIMITS. Latent: 0 of 3 real folders. | open |
| Z-5 | LOW | `.claude/scripts/d16-gate.sh:32-37` | Content transformed before it reaches grep — gzip, base64, a deflated `.docx`, an HTML entity inside a part — reads 0 hits (plain text: 2) and is not a declared KNOWN LIMIT. Latent: no tracked binary/encoded files today. | open |
| Z-6 | LOW | `.claude/scripts/d16-gate.sh` selftest (`:227-310`) vs `.claude/commands/maintenance.md:475` | No selftest case for a regex-metacharacter folder name, a missing `projects/` or a bad revision range, although the batch's own rule requires a case for every form the gate is taught; those negations rest on unpersisted verifier seeds. | open |
| Z-7 | LOW | `.claude/scripts/d16-gate.sh:129-133`, `:310` | GREEN has no floor on units scanned: `0 commits scanned` / `0 files scanned` exits 0; `log HEAD..HEAD`, a mistyped receipt range, or `GIT_INDEX_FILE` pointing at an empty index turn a leak (2 hits) into `0 hits`; the selftest treats an empty index as a GREEN control. | open |
| Z-8 | LOW | `.claude/scripts/d16-gate.sh:36-37`; `.claude/commands/audit.md:317` | `log --all` reads no ref NAMES: a branch name, a lightweight tag name, and a lightweight tag pointing at a leaking blob each read 0. KNOWN LIMITS mentions tag objects only. Real ref names are clean (17 lines, 0 hits). | open |
| Z-9 | LOW | `.claude/commands/maintenance.md:204`, `:432`, `:454-456`; `audit-2026-09-14.md` receipts `isolation:` | A receipt that lives in a commit body cannot run the mandated `log <first>~1..<last>` before the commit exists: 6b4baa8 and 30c3d6e discharged with the `staged` form and a file count, the unit Y-11 removed. The persisted report receipt paraphrases the gate line instead of pasting it. | open |
| Z-10 | LOW | `.claude/commands/audit.md:1317` | Phase 3 item 6 states `GREEN 9+2=11 today` beside a command that returns `GREEN 11+2=13`: a copied run count in the defect-series section, the class Y-13 fixed two paragraphs away. | open |
| Z-12 | LOW | `.claude/commands/maintenance.md:980-985` | The version-bump residue check expects "ZERO hits" after a bump but excludes only `created: framework-v`; the lineage sentence at `:970` ("v2.23.0 shipped continuous mode") reads 1 on a healthy state, so the Run 3 receipt narrated the exception instead of the check stating it. | open |
| Z-13 | LOW | `.claude/commands/maintenance.md:503-519` | The LATERAL "redirect with no mkdir" control mandates "BOTH HALVES" but its fence carries a command for half 2 only; half 1 is described in a comment and can never go RED. | open |
| Z-14 | LOW | `CLAUDE.md:7`; `docs/agentic_engineering_framework.md:204-222` | The inventory class was not swept: CLAUDE.md:7 "Only documentation, templates, examples, and project folders" became false with `.claude/scripts/d16-gate.sh`, and the concepts doc's framework-repo tree lists `.gitignore` but not `.gitattributes` (and has no `.claude/` block) — while the batch's `inventory sweep:` receipt names that doc as checked. | open |
| Z-15 | LOW | `.claude/commands/maintenance.md:415-417`; `.claude/commands/audit.md:232` | Two batch-written requirements are not dedicated imperative lines: the D16 gate mandate is the third clause of the fence-failure sentence, and "Extend by hand only with the D16.2 / D16.2c identifiers" is a lowercase constraint after the ALWAYS line. The commit claimed `0 buried` (the B regex cannot see either shape). | open |
| Z-16 | LOW | `.claude/commands/bootstrap.md:369-373`, `:439-442`; `existing_project_adaptation.md:553`, `:673-675`, `:838` | The 6 shipped `.sh` skill scripts (one wired as a PostToolUse hook) get no LF protection in bootstrapped or adapted projects: neither twin writes a `.gitattributes` or mentions eol, while the mother repo's new `.gitattributes` names exactly the CRLF failure and protects only its own checkout. | open |
| Z-17 | LOW | `.claude/commands/maintenance.md:458-461`; `.claude/commands/audit.md` (0 `selftest` references) | The gate's negation proof runs only in a session that edits the script; `/audit` D16 runs the gate but never its selftest, so git/grep drift after the edit (the Y-19 class) goes unproven between edits. | open |
| Z-18 | LOW | `.claude/commands/audit.md:175-192` (Agent 1 FILES TO READ) | D16.1 (`:229`) and D16.3c (`:317`) name `.claude/scripts/d16-gate.sh`, but Agent 1's FILES TO READ does not list it — the K-15/L-3 class; this run supplied it from the dispatch prompt. | open |
| Z-19 | LOW | `.claude/commands/audit.md` D17.6 command | The shipped/apparatus pathspecs cover neither `README.md`, `docs/agentic_engineering_framework.md` nor `.gitattributes`: 17 lines of this batch, all of d156dc4, are counted nowhere. Extends Y-16. | open |

## Detailed Findings

## Agent 1: Structural Sync

### PART 1 — Verification ledger (verification mode ONLY; write `Part 1: N/A — baseline mode` otherwise)
Part 1: N/A — baseline mode

### [A] Dual Placement
- Status: PASS
- 15 folders in `docs/modules/skills/`; 1 in `.claude/skills/` (`cross-cutting-analysis/`), which also exists in `docs/modules/skills/` and is byte-identical there (`diff -rq` clean). It is the documented runtime skill for `/prd_planning` and `/prd_change` (A.4).
- Findings: none

### [D4] Skills README vs Disk
- Status: PASS
- README lists: 15 skills (table rows 1-15)
- Disk has: 15 folders
- Missing from README: none
- Missing from disk: none
- Count claim accuracy: correct.
  - `docs/modules/skills/README.md:8-13`: "15 skills: 12 lifecycle (1+5+3+1+1+1) + 3 tier-gated" matches.
  - `:38`: "7 declare `user` … 8 declare `inline`" matches `grep '^invocation:'` over every SKILL.md (user: autonomous-loop, codebase-audit, commit, context-recovery, framework-audit, session-end, sprint-proposer; the other 8 are inline).

### [D9] CLAUDE.md Diagram vs Disk
- Status: PASS
- In diagram but not on disk: none
- On disk but not in diagram: none significant.
  - `assets/docs/` holds 16 audit files and 8 lineage/base docs, all covered by the generic entry.
  - `.claude/scheduled_tasks.lock` is a harness runtime file, excluded via `.git/info/exclude` and not tracked.
- Count mismatches: none.
  - Commands: 6 (5 + 1).
  - `.claude/scripts/`: 2. `.claude/rules/`: 1. `.claude/skills/`: 1.
  - Module templates: 8. Module agents: 10. Module rules: 5, all names as listed.
  - Skills: 15 (12 + 3; the 6 named plus "9 more").
  - Examples: agents 20, skills 9, rules 11.
  - `.gitignore` contains `projects/`, `.claude/settings.local.json` and `.claude/docs/`, as the diagram says.

### [D16] Project-Information Isolation
- Status: PASS. Live and unpushed surfaces are clean. The only history hits are PUSHED residue already closed by the E-1 accepted-risk record.
- Blocklist derived — ALL THREE sources, each counted separately:
  - **3** project folder names. `d16-gate.sh` derived their variants and ≥4-char PARTs.
  - **61** identifiers harvested from project DOCS (CLAUDE.md, `.claude/phases/project.md` and `assets/docs/prd.md` of each project, plus the project's own doc filenames). Classes:
    - person names and name variants;
    - deployment domains, custom domains and a sub-domain;
    - a personal e-mail local part and a phone number;
    - 2 Supabase project refs;
    - a city and a street address;
    - client/company and product names;
    - single-project domain terms (business vertical, a module name, an academic program, an institution, a market index, a data-source API, an author name);
    - project-specific agent, hook and skill names;
    - 21 project doc filenames.
    - One harvested entry, a doc filename that is also a framework convention, was dropped as a false positive after it hit a framework script. It is framework vocabulary, not a leak.
  - **90** source-project CODE identifiers (D16.2c).
    - Harvest: 417 camelCase/snake_case identifiers from every framework-layer surface (all tracked non-`projects/` files plus `.claude/docs/`).
    - Cross-grep: against 11,289 identifiers from 1,031 project `*.ts/*.tsx/*.js/*.mjs/*.sql/*.py` sources.
    - Excluded: the `bootstrap.md`-derived copy paths (`assets/examples/`, `.claude/skills/`, `.claude/agents/`, `.claude/rules/`, and only `scripts/check-agent-frontmatter.mjs` and `scripts/check-rules-paths.mjs`), plus `node_modules/`, `.next/`, `dist/`, `build/` and venvs.
    - Intersection: 90, all standard platform/framework/industry vocabulary.
      - The hand-rolled-looking schema names are canonical e-commerce names in `examples/`, added in `3e581b1` and `7e6cdc7` (v1.0).
      - `done_tasks` and `parsePaths` are framework vocabulary that travelled framework → project.
    - 0 fed to `D16_EXTRA` as hand-rolled.
  - Session numbers: 0 source-project session numbers. The only matches are the D16.2c rule's own `S12`/`Sessão 90` example, PKCE `S256`, and past audit reports quoting them.
- Value shapes (D16.2b): 11 EREs (JWT, sk-/ghp_/AKIA/xox prefixes, `user:pass@` DSN, long Bearer, BR phone, CPF, CNPJ, personal-provider e-mail). No tracked `.env`, dump or key files.
- Proof the extras load: a probe identifier file with a known framework term gave `128 files scanned, 96 hits` (135 hits with a probe ERE added).
- Surfaces scanned — the gate was run read-only, in place (no sandbox needed):
  - **Tracked files** (`staged`): base `128 files scanned, 0 hits`; with extras `128 files scanned, 0 hits`.
  - **`.claude/docs/`** (`dir`): base `4 files scanned, 0 hits`; with extras `4 files scanned, 0 hits`. This also re-verifies AF-13's working-tree fix: the pilot agent names are in the extras and read 0.
  - **Agent memory** (`C:\Users\Gabriel\.claude\projects\C--Users-Gabriel-OneDrive-Documentos-Projetos-Agency-agentic-engineering\memory`, 20 files incl. MEMORY.md):
    - `dir .` run from inside the directory, avoiding the A-15 ancestor-path false positive: base `20 files scanned, 0 hits`; with extras `20 files scanned, 0 hits`.
    - Commit hashes: 48 distinct 7-40-hex tokens outside UUIDs and URLs; 48 of 48 resolve via `git cat-file -e <h>^{commit}`.
  - **Git history** (`log --all`, 269 commits: patches, messages, raw objects, tags, paths): base `269 commits scanned, 0 hits`; with extras `269 commits scanned, 9 hits`.
  - **Unpushed range** (`log origin/main..HEAD`, 3 commits: 0ae5f08, efca24e, 1905c6e), with extras: `3 commits scanned, 0 hits`.
- Hits:
  - Tracked files, `.claude/docs/`, memory and the unpushed range: none.
  - Git history: 9 hits. Narrowed by per-entry `git log -G` without printing lines, they are ONE source-project doc filename in the patches of 4 commits (`3e581b1`, `dd7bb78`, `23b8478`, `dd98dea`). All 4 are ancestors of `origin/main`, so the hits are PUSHED.
  - **OBSERVATION (accepted-risk):** this is the residue covered by E-1 (owner decision, `assets/docs/audit-2026-09-02.md:603`), the same 9 hits Run 2026-09-23 reported. Not re-reported as a hit.
  - The 2 "domain term" hits in `3e581b1`/`11ce6e9` that the prior run saw did not reproduce: this run's harvest did not carry that term. Also under E-1.
- Unpushed-vs-pushed: every history hit is PUSHED and closed under E-1. The unpushed range `aad5b74..HEAD` is GREEN on all three sources and on value shapes. `git fetch --prune` → `## main...origin/main [ahead 3]`, origin/main at `aad5b74`.

#### Candidate findings (structural, from reading this contract)
- **LOW** — `.claude/commands/audit.md:299-320` (Agent 1 FILES TO READ) vs `:375-379` (D16.2c).
  - D16.2c mandates deriving the copy-path exclusion by grepping `.claude/commands/bootstrap.md`, but FILES TO READ does not list `bootstrap.md`.
  - This violates the list's own rule at `:321` (K-15/L-3 class).
  - It is a sibling of the still-open Z-18, which covers `d16-gate.sh`, missing from the same list and cited at `:355` and `:447`.
- Carried, not re-filed:
  - W-23 (open): the copy-path grep now returns 31 lines against the prose list of 5; "at least 5" still cannot go RED.
  - Z-18 (open).
  - A-15 (open): avoided here by running `dir .` from inside the memory directory. D16.3b still does not say so.

## Agent 2: Bootstrap Integrity

Run: 2026-09-26, mode `baseline` (trigger b — before pushing v2.34.0). HEAD 1905c6e.
All checks were read-only (file reads, `ls`, `grep`, `sed -n`, and the twinned-span `diff` run in place). Nothing was written and no probe was needed, so no `probe-sandbox:` line applies.

### PART 1 — Verification ledger
Part 1: N/A — baseline mode

### [D5] File References
- Status: PASS
- Total paths found: 52 distinct framework-tier source paths (project-tier TARGET paths are excluded: they resolve in the project, per the tier-of-reference rule)
  - docs/modules/templates/: claude_md, project_md, pendencias_md, metrics_md, framework_metrics_md, settings_json, check_agent_frontmatter, check_rules_paths (8)
  - docs/modules/agents/: prd_sync_checker, criteria_enforcer, diff_pattern_extractor, skill_reviewer, code_reviewer, security_reviewer, red_team, blue_team, validator, arbitrator (10)
  - docs/modules/rules/: session_rules, evolution_policy, component_design, ops_rules, quality_budgets (5)
  - docs/modules/skills/: `*` glob, codebase-audit, framework-audit, skill-gate (4)
  - examples/ (Step 1.5) (1); .claude/commands/bootstrap.md + existing_project_adaptation.md (twin diff, :713-714) (2)
  - assets/examples/agents/ specialists from the 12.5a table (10, which resolve via examples/agents/ after Step 1.5); assets/examples/rules/ from the Step 13 table (11, via examples/rules/); assets/examples/README.md (1, via examples/README.md)
- Resolved: 52
- Unresolved: none
- Extraction fences: every `sed` extraction source has exactly one opening and one closing 4-backtick fence, and each extracts a non-empty body (session_rules 339 lines, evolution_policy 170, component_design 366, metrics_md 11, framework_metrics_md 26, ops_rules 57, quality_budgets 26, check_agent_frontmatter 216, check_rules_paths 162).
- Section/anchor references resolve: `session_rules.md -> Execution proof` (:125) and "Model by risk class" (:184); settings_json.md **Merge rule** (:75); security_reviewer.md Section 8 (:179) and 13 numbered sections (the Step 8 grep target); code_reviewer.md "Known Bug Patterns" (:263) and "Architecture Patterns" (:92); component_design.md sections 1-3; skill-gate/scripts/skill_gate_hook.sh (the hook target).
- Claims checked: the 12-lifecycle split of 7 `inline` + 5 `user` matches each SKILL.md `invocation:`; the guard FAILs on a missing agent `model:` and on a skill `model:` (check_agent_frontmatter.md:191-208); `ALWAYS_LOADED` ships the two named entries, and component-design ships `paths:`.
- D5.4 naming: PASS. Every agent copy goes from an underscore source to a hyphen target (:416-418, :495, and the Steps 7-11 targets); rules go `session_rules.md -> session-rules.md` etc.; guards go `check_*.md -> check-*.mjs`.
- Adjacent observation (a shipped template that bootstrap reads, not bootstrap.md itself): `docs/modules/agents/security_reviewer.md:139` and `:196` cite `examples/rules/compliance-rules.md` and `examples/agents/sast-scanner.md`. In a project these live at `assets/examples/...` (Step 1.5), so both references are unresolvable in the tier where the text runs.

### [D11] Step Sequencing
- Status: PASS
- Step list (D11.1/D11.2/D11.3):
  - Setup (unnumbered): refuses a bad name. Produces projects/<n>/, assets/docs/ and `git init`.
  - 0.5: framework-clone freshness check. Produces a verdict. Consumes the framework git.
  - 1: reads the PRD. Produces extracted data. Consumes prd.md.
  - 1.1: builds the cross-cutting routing list. Consumed by Steps 3/4/13 (declared hand-off).
  - 1.2: sets the risk profile. The receipt grep runs after Step 3 (declared).
  - 1.5: copies examples. Produces assets/examples/.
  - 2: writes CLAUDE.md from claude_md.md.
  - 3: writes project.md. Receives 1.1 (A) and 1.2.
  - 4: writes pendencias.md and done_tasks.md. Receives 1.1 (T).
  - 5: installs MCPs and fills the CLAUDE.md MCP section.
  - 5.5: installs the skill-creator plugin. Records a key for Step 14 (declared).
  - 5.7: produces .claude/{skills,agents,docs,rules}, the 3 process agents, 3 rules and 2 guard scripts.
  - 5.8: copies the tier-gated skeletons (codebase-audit, metrics.md, skill-gate, skill-reviewer, drafts/, framework-audit, framework-metrics.md, ops-rules, quality-budgets). Consumes the 1.2 profile and the 5.7 dirs.
  - 6: installs stack skills and registers them in CLAUDE.md (Step 2).
  - 7-11: creates code-reviewer, security-reviewer, red/blue-team (conditional), validator and arbitrator. They consume assets/examples/README.md (1.5) and session-rules (5.7).
  - 12: stack and domain-test-pattern skills.
  - 12.5a/b: specialist pre-install and the activation-chain loop. Consumes the Step 7/8/10 agents, assets/examples/agents (1.5) and pendencias (4).
  - 13: domain rules from assets/examples/rules. Receives 1.1 (R).
  - 14: logs/, settings.json and the CLAUDE.md Hooks section. Receives 5.5.
  - 14.2: CI floor. Consumes the CLAUDE.md Commands section (2) and the guards (5.7).
  - 14.5: initial commit. Consumes everything above.
  - 15: report.
- Primary steps: 15 (1-15), Sub-steps: 10 (0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5; 12.5a/12.5b count as sub-parts of 12.5), Total: 25 discrete steps plus the unnumbered Setup.
- Claimed: "15-step pipeline" (docs/agentic_engineering_framework.md:50 and :389). Each claim qualifies itself as "15 primary steps 1-15 + Setup + Step 0.5 + sub-steps such as 5.7/5.8/14.5", which matches the 15 primary steps. Actual: 25 discrete steps, 15 of them primary. The claim is accurate as qualified. The pipeline diagram (:391-429) lists every step row 0.5 through 15, and that sequence matches bootstrap.md.
- Forward references in bootstrap.md: none undeclared. All the forward hand-offs are explicitly declared as sender/receiver pairs: 1.1->3/4/13, 1.2c->2/3 with the grep "run after Step 3", Step 1->Step 2 PRD line, 2->5/6/14 placeholders, 5.5->14, 5.7 guard->14.2 ("forward reference, flagged"), 5.8->7 red-team note, and the Step 7 preamble citing the Step 14.2 `guards` stage.
- Forward references in existing_project_adaptation.md (the D11.4 twin scope):
  - Declared, and correct: 2.2/2.3->4.1b; 2.2 row/log->4.2; 2.6->2.9b re-check; 2.7.1->4.6; the Phase-2 PRD version "set after Phase 3".
  - Undeclared: `existing_project_adaptation.md:440`. "Before Steps 2.4-2.8: If `assets/examples/README.md` exists, read it". That file is copied only by Step 4.1. On a never-bootstrapped project the conventions read therefore no-ops silently, unlike the sibling Step 2.7.1 (:566), which checks and DEFERS explicitly.
  - INFO (already recorded at audit-2026-09-14.md:159 and audit-2026-09-16.md:156): the twinned-span START marker (bootstrap.md:718, EPA:1376) and the lead paragraphs (bootstrap.md:703, EPA:1361) say "EPA Step 5.1". The EPA criteria span actually sits in Step 4.6.5; only the loop is in Step 5.1 (EPA:1476). The twinned-span diff itself is empty (exit 0).

### [D13] PRD <-> Bootstrap Compatibility
- Status: PASS
- PRD sections defined (prd_planning.md:228-458): header block (Version/Date/Author/Status); Cross-cutting Concerns; 1 Product Vision (1.1 Problem, 1.2 Solution, 1.3 Target Audience, 1.4 Competitive Differentiator); 2 Scope (2.1 In Scope, 2.2 Out of Scope, 2.3 Constraints); 3 Functional Requirements (3.x per module, including Dependencies, Business rules, Data model and Acceptance criteria); 4 NFRs (4.1-4.4); 5 Architecture and Stack (5.1 Stack, 5.2 External Integrations, 5.3 Data Model, 5.4 Build Order); 6 Design and UX (6.1-6.3); 7 Business Model (7.1-7.3); 8 High-Level Roadmap; 9 Risks and Dependencies; Changelog.
- Bootstrap reads:
  - Step 1 (by content): product name, audience, MVP modules, out of scope, stack, constraints, business rules, integrations, business model.
  - Step 1.1: Cross-cutting Concerns.
  - Step 2: section 5 and section 6 (Design and UX), plus Build Order from module dependencies.
  - Step 3: sections 1.1, 1.2 and 1.3, plus the Changelog (PRD version).
  - Step 4: section 4 (NFRs) and section 9 (Risks and Dependencies).
  - Step 5.8: the data model (production-financial).
  - Steps 7/8/9/12/13: stack, domain signals and risk indicators.
- EPA (twin): Phase 3 writes every top-level section plus Cross-cutting and the Changelog. Every subsection it names (1.1-1.4, 2.1-2.3, 3.x, 4, 5.1-5.4, 6, 7, 8, 9) exists in the template. Steps 4.1b and 4.6 read Cross-cutting and domain signals.
- Bootstrap reads but PRD lacks: none (EPA: none)
- PRD defines but bootstrap ignores (INFO only): 1.4 Competitive Differentiator; 7.2 Plans and 7.3 Success Metrics (Step 1 reads "business model" generically); 8 High-Level Roadmap (bootstrap derives phases from Build Order/5.4 instead); header Author/Status.

### [D14] Output Completeness
- Status: PASS
- Template references: 13 distinct targets inside the claude_md.md body fence:
  - `.claude/rules/session-rules.md` (x2)
  - `assets/docs/prd.md` (conditional pointer; pre-exists)
  - `.claude/phases/pendencias.md`
  - `.claude/logs/`
  - skills: `/sprint-proposer`, `/autonomous-loop`, `/session-end`, `/context-recovery`, `validation-orchestrator`, `/commit`
  - `.claude/skills/`, `.claude/agents/`
  - `.claude/rules/` domain rules
  - The two step placeholders (Step 5 MCP, Step 14 Hooks) are replaced by those steps (:358-364, :887-890).
  - Out of scope: `docs/modules/README.md` in the template's pre-fence note is framework-tier and not shipped.
- Traced to bootstrap steps: 13. The session-rules file, the skills, the agents dir and the rules dir are created in Step 5.7. pendencias is Step 4. logs/ is Step 14. The prd.md pointer is Step 2's PRD-line selection, over Setup's assets/docs/. Domain rules are Step 13.
- Untraced: none
- D14.4 (final report lists every created file): PASS. Step 15 carries a line or slot for every artifact Steps Setup through 14.5 create:
  - Files created: CLAUDE, project, pendencias, done_tasks, the Steps 7-11 agents, the Step 12 skills, settings.json, logs/, .claude/docs/, assets/examples/.
  - Process skills, process agents, Rules, Guards.
  - Domain rules and specialists, including the pendencias registrations.
  - MACRO skeletons, including drafts/, review_reports/ and the CI floor.
  - The Initial commit slot, which covers `.git`.
  - Only the empty `.claude/phases/` directory and `assets/docs/` are implicit (as parents of listed files).

### Candidate findings
- MEDIUM — `docs/modules/agents/security_reviewer.md:196` (and `:139`). A shipped universal agent template cites `examples/agents/sast-scanner.md` and `examples/rules/compliance-rules.md`. Those paths do not exist in any project, because bootstrap Step 1.5 and EPA Step 4.1 copy them to `assets/examples/`. This is the tier-of-reference defect in a template every project receives.
- LOW — `existing_project_adaptation.md:440`. "Before Steps 2.4-2.8" reads `assets/examples/README.md` "if it exists", but only Step 4.1 creates it. This is an undeclared forward reference that silently no-ops on a never-bootstrapped project, where the sibling Step 2.7.1 (:566) checks and defers.
- INFO (carried, already recorded twice) — bootstrap.md:703/:718 and EPA:1361/:1376. The twinned-span label says "EPA Step 5.1", but the EPA criteria span is in Step 4.6.5.

## Agent 3: Activation Chain

Run: 2026-09-26 · mode `baseline` (trigger b: before pushing the v2.34.0 MINOR bump) · HEAD `1905c6e`, working tree clean.

### PART 1 — Verification ledger (verification mode ONLY; write `Part 1: N/A — baseline mode` otherwise)
Part 1: N/A — baseline mode

### [D6] Vocabulary Alignment
- Status: **PASS**
- Gap declarations found (one sub-bullet per declaring component the D6.8 grep returned):
  - code-reviewer (`docs/modules/agents/code_reviewer.md:214,222,230,237,249`, blockquote form): accessibility, performance, concurrency, visual regression, data integrity
  - security-reviewer (`docs/modules/agents/security_reviewer.md:250,259,267,277,285`, blockquote form; plus bold `**static analysis gap**` at `:201`): static analysis, secrets coverage, federation protocol, compliance, infrastructure security
  - validator (`docs/modules/agents/validator.md:150`, bold form): visual regression
- Specialist matches:

  | Gap | Declared by | Specialist file | Phrase match | bootstrap install row | EPA install row |
  |-----|-------------|-----------------|--------------|-----------------------|-----------------|
  | accessibility | code-reviewer | examples/agents/accessibility-checker.md | exact ("code-reviewer declares an accessibility gap") | `bootstrap.md:677` | `existing_project_adaptation.md:1337` |
  | performance | code-reviewer | examples/agents/performance-auditor.md | exact | `:678` | `:1338` |
  | concurrency | code-reviewer | examples/agents/concurrency-tester.md | exact | `:679` | `:1339` |
  | visual regression | code-reviewer + validator | examples/agents/visual-regression-tester.md | exact ("code-reviewer or validator declares a visual regression gap") | `:680` (code-reviewer + validator) | `:1340` (code-reviewer + validator) |
  | data integrity | code-reviewer | examples/agents/data-integrity-checker.md | exact | `:681` | `:1341` |
  | static analysis | security-reviewer | examples/agents/sast-scanner.md | exact | `:682` | `:1342` |
  | secrets coverage | security-reviewer | examples/agents/secrets-scanner.md | exact | `:683` | `:1343` |
  | federation protocol | security-reviewer | examples/agents/oauth-flow-tester.md | exact | `:684` | `:1344` |
  | compliance | security-reviewer | examples/agents/compliance-auditor.md | exact | `:685` | `:1345` |
  | infrastructure security | security-reviewer | examples/agents/iac-scanner.md | exact ("declares an infrastructure security gap") | `:686` | `:1346` |

- Install-block diff (D6.7): **table diff empty (rc 0)**; TWINNED SPAN diff empty (rc 0); the two extracted loops are byte-identical (78 = 78 lines, `cmp`). Whole-block diff (intro paragraph through "…can never act on.", 22 = 22 lines) is **rc=1 with two path-specific hunks only**:
  ```
  1c1  bootstrap: "(code-reviewer in Step 7, security-reviewer in Step 8, validator in Step 10 …) Identify which Coverage Gap Declaration sections were KEPT … check if a matching specialist example exists"
       EPA:       "Identify which Coverage Gap Declaration sections are present. For each gap declaration, check if a matching specialist agent ALREADY EXISTS … pre-install it:"
  17c17 created: lineage — `s0 (bootstrap — pre-installed from example template)` vs `adaptation (pre-installed from example template)`
  ```
  Both hunks are legitimate per-path differences. The D6.7 text's "expected: empty" for the whole block is the already-open **W-24** (`audit-2026-09-14.md:70`), still true — carried, not re-filed.
- **Fourth-surface check (D6.9):** `docs/agentic_engineering_framework.md:998-1003` names **3** declarers (code-reviewer, security-reviewer, validator; two passes); policy §1/§3 names **3**; match: **yes**. (Also consistent: `validation-orchestrator/SKILL.md:141-159`, `rules-agents-updater/SKILL.md:128,138`.)
- `declarers extracted: 3 of 3` (M derived by the D6.2b grep over both forms: `grep -rlE … docs/modules/agents/ | wc -l` → 3)
- Gap SOURCES found (D6.8): code-reviewer, security-reviewer, validator (other `gap` hits in `docs/modules/agents/` are `blue_team.md` "gap analysis" and `red_team.md` prose — not declarations). Policy §1/§3 lists: code-reviewer, security-reviewer, validator — in BOTH twins (`diff` of §1–§3 between `.claude/rules/component-design.md` and `docs/modules/rules/component_design.md` → empty, rc 0); match: **yes**.
- Broken links (gap → no specialist): none
- Orphaned specialists (specialist → no gap): none
- v2.34.0 note: the new "agent tool gate" is a SPAWN TRIGGER for security-reviewer (`validation-orchestrator/SKILL.md:121-130`, security_reviewer §13 `:221`), not a gap phrase; no declaring component emits a "tool gate gap" and no specialist names one, so D6 is unaffected. Surfaces checked agree (`security_reviewer.md:54-55`, `agentic_engineering_framework.md:984,1022`, `autonomous-loop/SKILL.md:252`).

### [D7] Pushy Description Compliance
- Status: **PASS**
- Agents checked: 30 (10 in `docs/modules/agents/`, 20 in `examples/agents/`, all `invocation: subagent`)

  | Agent | Core function | Triggers | Exclusions | Consequence | Output | Status |
  |-------|--------------|----------|------------|-------------|--------|--------|
  | accessibility-checker | Y | Y | Y | Y | Y | COMPLIANT |
  | api-security-scanner | Y | Y | Y | Y | Y | COMPLIANT |
  | compliance-auditor | Y | Y | Y | Y | Y | COMPLIANT |
  | concurrency-tester | Y | Y | Y | Y | Y | COMPLIANT |
  | config-schema-validator | Y | Y | Y | Y | Y | COMPLIANT |
  | data-integrity-checker | Y | Y | Y | Y | Y | COMPLIANT |
  | dependency-auditor | Y | Y | Y | Y | Y | COMPLIANT |
  | deploy-validator | Y | Y | Y | Y | Y | COMPLIANT |
  | iac-scanner | Y | Y | Y | Y | Y | COMPLIANT |
  | integration-contract-tester | Y | Y | Y | Y | Y | COMPLIANT |
  | load-tester | Y | Y | Y | Y | Y | COMPLIANT |
  | migration-runner | Y | Y | Y | Y | Y | COMPLIANT |
  | multi-tenancy-auditor | Y | Y | Y | Y | Y | COMPLIANT |
  | oauth-flow-tester | Y | Y | Y | Y | Y | COMPLIANT |
  | performance-auditor | Y | Y | Y | Y | Y | COMPLIANT |
  | sast-scanner | Y | Y | Y | Y | Y | COMPLIANT |
  | secrets-scanner | Y | Y | Y | Y | Y | COMPLIANT |
  | state-machine-verifier | Y | Y | Y | Y | Y | COMPLIANT |
  | test-quality-reviewer | Y | Y | Y | Y | Y | COMPLIANT |
  | visual-regression-tester | Y | Y | Y | Y | Y | COMPLIANT |
  | skill-reviewer (modules) | Y | Y (USE when) | Y (NOT for) | Y | Y | COMPLIANT |
  | arbitrator (modules) | Y | spawn condition | — | — | outputs named | PARTIAL (sanctioned, protocol-spawned) |
  | blue-team (modules) | Y | spawn condition | — | — | — | PARTIAL (sanctioned) |
  | code-reviewer (modules) | Y | spawn condition | — | — | — | PARTIAL (sanctioned) |
  | criteria-enforcer (modules) | Y | MUST run | — | Y (Skipping…) | — | PARTIAL (sanctioned) |
  | diff-pattern-extractor (modules) | Y | MUST run | — | Y | — | PARTIAL (sanctioned) |
  | prd-sync-checker (modules) | Y | Runs at step 3 | — | Y (Skipping…) | — | PARTIAL (sanctioned) |
  | red-team (modules) | Y | spawn condition | — | — | Y | PARTIAL (sanctioned) |
  | security-reviewer (modules) | Y | spawn condition | — | — | — | PARTIAL (sanctioned) |
  | validator (modules) | Y | spawned after impl. | — | — | Y | PARTIAL (sanctioned) |

  (Per D7.5, PARTIAL on a `docs/modules/agents/` agent is sanctioned and never a finding.)

- **`activation chains: N verified, K broken, I info` — one line per twin (D7.6, D7.7):**
  - bootstrap twin: `activation chains: 10 verified, 0 broken, 17 info` (exit 0)
  - EPA twin: `activation chains: 10 verified, 0 broken, 17 info` (exit 0)
  - `twins agree: yes` — counts AND the full INFO strings (`diff -q` of the complete outputs, empty). Resolved count 10 = the 10 gap phrases D6.1/D6.2/D6.2b extracted; K = 0. The 17 INFO lines = 10 trigger-activated shipped examples + 7 protocol-spawned non-declarers from `docs/modules/agents/`.
  - Regression set, run against the loop AS EXTRACTED from each command file (both twins, identical output in every row):

    | # | Input | Expected | Literal output (both twins) | Result |
    |---|-------|----------|------------------------------|--------|
    | 1 | `declares a coverage gap for X` | BREAK | `BROKEN CHAIN: r1-agent declares 'coverage gap' but no declaring component declares it` / `10 verified, 1 broken, 17 info` | OK |
    | 2 | `visual-regression gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 3 | `data/integrity gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 4 | TAB fold straddling `data⏎\tintegrity gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 5 | `declares performance gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 6 | second bogus `quantum flux gap` | BREAK on second | `BROKEN CHAIN: performance-auditor declares 'quantum flux gap' …` / `10 verified, 1 broken, 17 info` | OK |
    | 7 | sentence-initial `Declares an accessibility gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 8 | space fold inside `data⏎  integrity gap` | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 9 | gap phrase in BODY only | no BREAK | `10 verified, 0 broken, 18 info` (r9 → INFO "not a shipped example") | OK |
    | 10 | `NEVER declares an imaginary gap` in body | no BREAK | `10 verified, 0 broken, 18 info` | OK |
    | 11 | `OAuth2 federation gap` | never silent | `BROKEN CHAIN: r11-agent declares 'OAuth2 federation gap' …` / `10 verified, 1 broken, 17 info` | OK |
    | 12 | `Node.js runtime gap` | never silent | `BROKEN CHAIN: r12-agent declares 'Node.js runtime gap' …` / `10 verified, 1 broken, 17 info` | OK |
    | 13 | code-reviewer's visual-regression blockquote removed (validator bold form is sole declarer) | VERIFY | `10 verified, 0 broken, 17 info` | OK |
    | 14 | all three declarers removed | `none installed`, exit 0 | `activation chains: none installed - no declaring component in projects/synth/.claude/agents` (exit 0) | OK |

    Silently missed phrasings: none found. No regression.
  - probe (D7.6): synthetic projects built in `mktemp -d` → `/tmp/tmp.W3YzVqiysB` (outside every repo; sandbox clone `/tmp/tmp.sVqsEPj3Cw/r`; workspace removed at end: yes); harness `…/scratchpad/audit/d76_harness.sh`, full output `…/scratchpad/audit/d76_output.txt` | `probe-sandbox: GREEN — live repo unchanged; command exit 0`
- **Planted-specialist probe (D7.8):** (both twins, identical)
  ```
  INFO: planted-nogap-helper has no gap phrase and is not a shipped example - verify it is protocol-spawned
  BROKEN CHAIN: planted-telemetry-auditor declares 'telemetry gap' but no declaring component declares it
  activation chains: 10 verified, 1 broken, 18 info
  ```
  Non-zero output, both planted names reported — GREEN.
- **Invariant proof output (D7.5):** `files: 10 | output: 0` — `ls docs/modules/agents/*.md | wc -l` = 10, matches; GREEN.
- Anti-pattern instances: none (every description, in both directories, opens with a core-function statement).

### Notes (INFO, not findings — already recorded)
- The twinned-span marker reads "EPA Step 5.1" while EPA's span sits in Step 4.6.5, and both loops carry the leftover comment `# candidate replacement for bootstrap Step 12.5b / EPA Step 5.1` (`bootstrap.md:754`, `existing_project_adaptation.md:1475`) — recorded at `audit-2026-09-14.md:159`, `audit-2026-09-16.md:156`, `audit-2026-09-19.md:125`.
- The `grep -P` fail-closed probe is written `printf 'a<literal newline>'` rather than `printf 'a\n'` (`bootstrap.md:761-762`, `existing_project_adaptation.md:1482-1483`); functionally equivalent inside single quotes, identical in both twins, executes correctly — no finding.
- W-24 (D6.7 "expected: empty" vs. the structurally non-empty whole-block diff) remains open and true.

### Candidate findings
None new. D6 PASS, D7 PASS.

## Agent 4: Orchestration & Commands

Run: 2026-09-26 · mode: baseline (trigger (b), before pushing v2.34.0) · HEAD `1905c6e` (`main...origin/main [ahead 3]`, origin/main `aad5b74`).
Privacy: no project folder name or project identifier is printed below. Step 0 outputs are reduced to counts.

### PART 1 — Verification ledger
Part 1: N/A — baseline mode

### [D8] Orchestrator References
- Status: **PASS**
- References found (method: grep of `.claude/skills|agents/<name>` paths plus the component-suffix name class `*-updater|-creator|-reviewer|-checker|-enforcer|-extractor|-team|-orchestrator|-proposer|-loop|-audit|-gate|-analysis|-recovery` and bare `validator|arbitrator|red/blue team` over the 8 orchestrator skills):

  | Source file | Referenced name | Expected path | Exists? |
  |-------------|----------------|---------------|---------|
  | session-end | project-md-updater | docs/modules/skills/project-md-updater/ | yes |
  | session-end | pendencias-updater | docs/modules/skills/pendencias-updater/ | yes |
  | session-end | config-file-updater | docs/modules/skills/config-file-updater/ | yes |
  | session-end | rules-agents-updater | docs/modules/skills/rules-agents-updater/ | yes |
  | session-end | session-log-creator | docs/modules/skills/session-log-creator/ | yes |
  | session-end | skill-gate | docs/modules/skills/skill-gate/ | yes |
  | session-end | diff-pattern-extractor (agent) | docs/modules/agents/diff_pattern_extractor.md | yes |
  | validation-orchestrator | code-reviewer | docs/modules/agents/code_reviewer.md | yes |
  | validation-orchestrator | security-reviewer | docs/modules/agents/security_reviewer.md | yes |
  | validation-orchestrator | validator | docs/modules/agents/validator.md | yes |
  | validation-orchestrator | arbitrator | docs/modules/agents/arbitrator.md | yes |
  | validation-orchestrator | red team / blue team | docs/modules/agents/red_team.md, blue_team.md | yes |
  | validation-orchestrator | criteria-enforcer | docs/modules/agents/criteria_enforcer.md | yes |
  | validation-orchestrator | autonomous-loop, codebase-audit, framework-audit, project-md-updater, session-log-creator, sprint-proposer | docs/modules/skills/<name>/ | yes (all 6) |
  | sprint-proposer | autonomous-loop, codebase-audit, framework-audit, context-recovery, project-md-updater, session-end, skill-gate, validation-orchestrator | docs/modules/skills/<name>/ | yes (all 8) |
  | sprint-proposer | prd-sync-checker, diff-pattern-extractor, arbitrator (agents) | docs/modules/agents/<snake>.md | yes (all 3) |
  | autonomous-loop | sprint-proposer, session-end, validation-orchestrator, framework-audit, context-recovery, project-md-updater, config-file-updater, rules-agents-updater, session-log-creator | docs/modules/skills/<name>/ | yes (all 9) |
  | autonomous-loop | code-reviewer, security-reviewer, criteria-enforcer, validator, arbitrator, red-team, diff-pattern-extractor (agents) | docs/modules/agents/<snake>.md | yes (all 7) |
  | skill-gate | skill-reviewer, security-reviewer, red-team (agents) | docs/modules/agents/<snake>.md | yes |
  | skill-gate | autonomous-loop, rules-agents-updater, session-end, sprint-proposer | docs/modules/skills/<name>/ | yes |
  | codebase-audit | code-reviewer, red-team, diff-pattern-extractor (agents); autonomous-loop, framework-audit, sprint-proposer (skills) | docs/modules/… | yes |
  | codebase-audit | data-integrity-checker, performance-auditor ("whichever the project has") | examples/agents/*.md (optional specialists) | yes — optional by wording, not a core install |
  | framework-audit | codebase-audit, skill-gate, sprint-proposer, autonomous-loop (skills); prd-sync-checker (agent) | docs/modules/… | yes |
  | cross-cutting-analysis | (self only) | — | — |

  Non-name hits dismissed: `mid-loop`, `non-loop`, `meta-audit`, `meta-loop`, `end-to-end` (prose). Project-tier hyphenated agent names map to snake-case sources exactly as bootstrap copies them (`bootstrap.md:416-418`, `:495`).

- Cross-section citations (D8.7/D8.8):

  | Citer | Cited heading | Target file | Real heading? | Exact / sanctioned-prefix / RED |
  |-------|---------------|-------------|---------------|--------------------------------|
  | validation-orchestrator:42 | §"Reasoning depth mechanisms (complementary)" | rules/session_rules.md:175 | yes | exact |
  | validation-orchestrator:42 | §"MODEL SWITCH entries" | skills/project-md-updater/SKILL.md:42 `### 4. MODEL SWITCH entries` | yes | sanctioned (leading ordinal) |
  | validation-orchestrator:127 | §13 "AI-Agent Tool Gates" | agents/security_reviewer.md:221 `## 13. AI-Agent Tool Gates (check when …)` | yes | sanctioned (ordinal + parenthetical) |
  | validation-orchestrator:155 | "Items for human verification" section | agents/validator.md:113 `### Items for human verification:` | yes | exact |
  | validation-orchestrator:186, autonomous-loop:100,322,417 | session-rules → "Autonomous loop watchdog" | session_rules.md:83 | yes | sanctioned prefix (`— subtitle`) |
  | validation-orchestrator:186 (2nd hop) | … → RECEIPT | session_rules.md:107 bold bullet `- **RECEIPT — …**` inside that heading | no (bullet) | not RED: a sub-item named after a resolving heading, unquoted |
  | validation-orchestrator:187 | session-rules → "Deploy gates" | session_rules.md:327 `## Deploy gates (production+ profiles)` | yes | sanctioned (parenthetical) |
  | validation-orchestrator:331, autonomous-loop:228 | session-rules → "Execution proof" | session_rules.md:125 | yes | sanctioned prefix |
  | validation-orchestrator:336, sprint-proposer:80,231, skill-gate:95, codebase-audit:193 | `autonomous-loop` → "Continuous mode" | autonomous-loop:449 `## Continuous mode — …` | yes | sanctioned prefix (`## Continuous Mode Policy:` at :522 is inside a fence opened at :521, so not ambiguous) |
  | sprint-proposer:55, codebase-audit:105,108,111, framework-audit:97,143,149,154 | session-rules → "Cadence integrity" | session_rules.md:291 | yes | sanctioned prefix |
  | sprint-proposer:278 | `autonomous-loop` → "Final report" | autonomous-loop:761 `## Final report` | yes | exact |
  | sprint-proposer:115,258; autonomous-loop:171 | validation-orchestrator → "Before Implementing" | validation-orchestrator:27 `## Before Implementing` | yes | exact |
  | autonomous-loop:141,763 | `sprint-proposer` → "Between Tasks" | sprint-proposer:251 `## Between Tasks (after validation passes)` | yes | sanctioned (parenthetical) |
  | autonomous-loop:147,409 | `sprint-proposer` → "Exception stops" | sprint-proposer:233 `### Exception stops` | yes | exact |
  | autonomous-loop:396-397 | `framework-audit` → Q4 → the HYPOTHESIS check | framework-audit:14/69 numbered item 4 + bold lead-in | no (list item; unquoted) | not a quoted §heading citation — navigational; not RED under D8.7's scope |
  | skill-gate:31 | evolution-policy → "Component creation gate" | rules/evolution_policy.md:51 `## Component creation gate (when …)` | yes | sanctioned (parenthetical) |
  | framework-audit:170 | evolution-policy → "Framework-evolution docs — the upstream lifecycle" | evolution_policy.md:117 `… (project → mother framework)` | yes | sanctioned (parenthetical) |
  | codebase-audit:47, framework-audit:44 | `session-rules` → "Model by risk class" | session_rules.md:184 | yes | sanctioned prefix |
  | **codebase-audit:174 (D8.8)** | `code-reviewer.md` → `### Periodic review — the triage VERDICTS` | agents/code_reviewer.md:278 | yes | **exact** |
  | sprint-proposer:110,141,310 (internal) | §4c / §4d / §1b | sprint-proposer:179 `#### 4c.`, :209 `#### 4d.`, :104 `### 1b.` | yes | exact |
  | all orchestrators | component-design §5/§6/§7/§8/§9 | rules/component_design.md:99/140/208/235/287 | yes | sanctioned (ordinal) |

- Unresolved: **none**

### [D10] Command → Skill Paths
- Status: **PASS** (one LOW stale-line citation found under D10.5, below)
- References found:

  | Command file | Referenced skill/section | Expected location | Exists? |
  |-------------|--------------------------|-------------------|---------|
  | prd_planning.md:66,174 | `cross-cutting-analysis` (+ `.claude/skills/cross-cutting-analysis/SKILL.md`), "generation mode" | docs/modules/skills/ + .claude/skills/; `## Generation Mode` :31 | yes |
  | prd_change.md:141,143 | `cross-cutting-analysis`, "consultation mode" | same; `## Consultation Mode` :128 | yes |
  | maintenance.md (×10) | component-design §5 / §6 / §8 / §9 | .claude/rules/component-design.md:99/140/220/272 | yes (sanctioned ordinal) |
  | maintenance.md:15,1176,1196,1201 | "New component creation" | maintenance.md:1330 | yes (prefix) |
  | maintenance.md:20,1158 | "Version bumps" | maintenance.md:1271 | yes (prefix) |
  | maintenance.md:158 | "Audit intake" | maintenance.md:1367 | yes (prefix) |
  | maintenance.md:159,222 | "Upstream intake" | maintenance.md:1588 | yes (prefix) |
  | maintenance.md:223,1472,1579 | "Cycle governance" rule 1/2/4 | maintenance.md:229 (rules numbered 1-4) | yes (prefix) |
  | maintenance.md:231,999,1577 | `/audit` → "The defect series" | audit.md:209 | yes (prefix) |
  | maintenance.md:1245 | `/audit` → "ID allocation" | audit.md:1363 | yes (prefix) |
  | maintenance.md:1430,1456 | `/audit` → "Errata and superseded status" | audit.md:1344 | yes (prefix) |
  | maintenance.md:1574 | `/audit` Phase 0 | audit.md:65 | yes |
  | maintenance.md:97,263,357,358,1475,1524 | `/audit` Phase 3 (item 1 / item 3) | audit.md:1461; items 1 (receipts-only definition) and 3 (carry-forward) name what they are cited for | yes |
  | maintenance.md:486,1138 | `session_rules.md` → "Execution proof" | docs/modules/rules/session_rules.md:125 | yes (prefix; shipped rule, cited for its proposition) |
  | maintenance.md:1352 | `session-rules` → "Model by risk class" | session_rules.md:184 | yes (prefix) |
  | maintenance.md:46 | `CLAUDE.md` → Repository Lifecycle | CLAUDE.md:94 `## Repository Lifecycle` | yes (exact) |
  | maintenance.md:1281,1573,1643 | trigger (b) / (d) / (a) in CLAUDE.md | CLAUDE.md:152-155 `**When it runs**` bold lead-in, letters (a)-(d) present | yes (cited as a trigger letter, not a heading) |
  | maintenance.md:591, 1458, 291 | "Upstream intake step 6", "Audit intake item 8", "item 9 of Audit intake" | Upstream intake 6 = post-change verification; Audit intake 8 = follow-up not `--amend`; 9 = verification audit | yes (ordinals name their content) |
  | **maintenance.md:1093** | **`bootstrap.md:350` "copies [the skills] with `cp -r`"** | **the `cp -r docs/modules/skills/*` line is `bootstrap.md:400`; `:350` is inside the plugin checklist fence** | **STALE line citation (LOW)** |
  | audit.md:80 | `CLAUDE.md` → the `**When it runs**` paragraph under **Utilities** | CLAUDE.md:152 | yes — correctly cited AS a bold lead-in |
  | audit.md:114,965,1519 | `/maintenance` → "Cycle governance" | maintenance.md:229 | yes (prefix) |
  | audit.md:161 | `maintenance.md` → the `push:` rule | maintenance.md:47 bold `**ALWAYS REPORT `push:`**` | descriptive pointer, unquoted — not a §heading citation |
  | audit.md:865,937 | `session_rules.md` → "Execution proof" | session_rules.md:125 | yes (prefix) |
  | audit.md:901 | §"MODEL SWITCH entries" (D8.7's own example) | project-md-updater:42 | yes (ordinal) |
  | audit.md:1062-type citations of component-design §1/§2/§3/§5/§6/§8/§9 | .claude/rules/component-design.md | yes (ordinal) |
  | audit.md "New component creation", "Version bumps", "Audit intake", "Upstream intake" | maintenance.md :1330/:1271/:1367/:1588 | yes (prefix) |

- **Self-checks executed verbatim (D10.0).** Read-only ones ran IN PLACE; every check that writes, plants a state or is a persisted script ran inside `probe-sandbox.sh run` (no `--keep`). Batch-scoped checks were run over the v2.34.0 batch (`efca24e~1..efca24e`) and its receipts section (`assets/docs/audit-2026-09-26.md`, `## Post-change checklist receipts (\`sefca24e\`)`, extracted with the file's own fence-aware `sec()`).

  | Check | file:line | Stated expectation | Actual output | Match? | Can go RED? | Can go GREEN? |
  |---|---|---|---|---|---|---|
  | Push gate — D16 half only (push NOT executed) | maintenance.md:27-31 | `0 hits` → push; else BLOCKED | `D16 log …: 3 commits scanned, 0 hits` → `(would push)` | yes | yes (a `D16 RED:` line leaves `hits` empty → string compare fails closed) | yes (observed) |
  | push-decay detector | maintenance.md:111-122 | no `behind`; `unchanged at sHASH` | `## main...origin/main [ahead 3]` / `unchanged at aad5b74` | yes | yes (4 RED branches + `advanced`) | yes (observed) |
  | Step 0 channel + pending sweep | maintenance.md:183-190 | step 1 empty AND step 2 empty = nothing pending | step 1: 2 `NO CHANNEL` lines (of 3 projects); step 2: 1 path (names redacted) | expectation is a report condition, not met — the state is the known pre-fix one the text itself describes (`:179-181`); the check works | yes (observed non-empty) | yes |
  | Step 0 lineage cross-check | maintenance.md:201-203 | a hit → absorbed, awaiting discharge | 1 of 1 printed path has a lineage hit → `0 pending (1 awaiting project-side discharge)` | yes | yes | yes |
  | Trigger (d) decider | maintenance.md:238-246 | one of the two lines | `trigger (d): does not fire — no evidence from outside the loop; …` | yes | yes (fires on PEND>0 or OBS>0) | yes (observed) |
  | Moratorium counts | maintenance.md:272-275 | 27 and 14; any increase RED | `27` / `14` | yes | yes (ratchet on increase) | yes |
  | `sec()` fence-aware extractor | maintenance.md:388-391 | the section only | 188 lines, stops at EOF (no following H1/H2) | yes | n/a (extractor) | n/a |
  | 27-key loop + `$`-line assert | maintenance.md:393-411 | every key exactly 1; `$-line >= 1` | all 27 keys `keys 1`; `$-line` 1 (version 2, gates 3, verifier rounds 2) | yes | yes — in-memory negation: section minus `**measurer:**` → `0` | yes |
  | `$` elision/angle-bracket grep | maintenance.md:416 | 0 | `0` | yes | yes (by construction; anchored) | yes |
  | `n/a` escape per key | maintenance.md:422-424 | only `classification`, `new component` | `classification` / `new component` | yes | yes — in-memory negation planting `$ n/a` under `isolation:` printed `isolation` | yes |
  | Key-list harvest cross-check | maintenance.md:442 | every harvested key in the loop | 18 keys harvested, 0 absent from loop | yes | yes | yes |
  | Harvest-vs-registered | maintenance.md:460-463 | harvested LOWER than registered | `harvested 18 of 27 registered` | yes | yes (harvest ≥ registered would signal a stale loop) | yes |
  | Instruction style (range form) | maintenance.md:556-575 | 3 numbers, B = eyeballed tails | `18 added, 3 rewritten, 1 buried` = receipt | yes | n/a (measure) | n/a |
  | Fences (mandated form) | maintenance.md:601-606 | every template non-zero | 13 of 13 non-zero (13–368 lines) | yes | yes (a `0` line) | yes |
  | References denominator | maintenance.md:618-622 | M from the command | `2` (= receipt's 2 of 2) | yes | n/a | n/a |
  | D16 staged + log | maintenance.md:641-645 | `…, 0 hits` both | `staged: 128 files scanned, 0 hits`; `log efca24e~1..1905c6e: 2 commits scanned, 0 hits` | yes | yes (selftest below) | yes |
  | D16 selftest | maintenance.md:652-654 | `selftest: K of K cases as expected` | `selftest: 106 of 106 cases as expected` | yes | yes (106 seeded RED cases) | yes (2 GREEN controls) |
  | mkdir halves 1 and 2 | maintenance.md:697-720 | no output from either half | no output | yes | yes (stated historical hit on `.claude/docs/`) | yes |
  | Receipt-template pipe count | maintenance.md:741 | three identical numbers | `8 8 8` | yes | yes | yes |
  | Commits-in-batch count | maintenance.md:770-774 | rows = commits − exempt | 1 commit, 0 exempt; per-fix table form (rows 14 = findings 14) | yes | yes | yes |
  | Gate 1 twin parity | maintenance.md:798-800 | no output | no output, exit 0 | yes | yes — in-memory 1-line change → `diff exit=1` | yes |
  | Gate 1 EPA settings embed | maintenance.md:810-813 | no output | no output, exit 0 | yes | yes (diff) | yes |
  | Gate 2 per hash | maintenance.md:819-824 | every `File touched` cell in the commit | 7 of 7 distinct cells `IN` `efca24e` | yes | yes (`ABSENT`) | yes |
  | Gate 2 per finding (applied-proof, form A) — the receipt's pasted script | maintenance.md:842-851; audit-2026-09-26.md receipts | every applied ID returns a commit | efca24e: 14 of 14 `form A: 1`; 3030551: 13 of 13 `form A: 1` | yes | yes (empty `-S` → 0) | yes |
  | Gate 3 row count | maintenance.md:884-892 | equal to receipt row count | ledger `applied sefca24e` = 14; receipt rows = 14 | yes | yes | yes |
  | Gate 4 slots (range form) | maintenance.md:903-921 | every added key has a slot | 1 key (`security-reviewer:`); slot at validation-orchestrator:309 (`- Security reviewer:`) | yes (slot named by the mandate itself) | yes | yes |
  | Control enumerator, TIP vs BASE | maintenance.md:1008-1047 | no per-file decrease | TIP = BASE on all 9 files (maintenance 37, audit 53, 7 shipped files 1–8) | yes | yes (per-file decrease; column-0 fences only — stated limit) | yes |
  | Liveness guard (`check_agent_frontmatter`) | maintenance.md:1087-1089 | exit 0 | `1 componente(s) OK … [modo estrutural — js-yaml ausente]`, exit 0 | yes (DEGRADED mode, as the text anticipates at :1135) | yes (colon-space class) | yes |
  | Raw templates YAML + name | maintenance.md:1097-1112 | `N validated of M globbed, S skipped (fenced), OK` | `raw templates: 19 validated of 25 globbed, 6 skipped (fenced), OK` | yes | yes (name mismatch / parse fail) | yes |
  | Version stamp | maintenance.md:1165-1167 | every canonical surface hits `v2.34.0` | audit.md 1, EPA 5, claude_md.md 1, README.md 2 | yes | yes | yes |
  | Old-version residue (OLD=2.33.1) | maintenance.md:1309-1312 | AFTER: zero outside migration notes | no output | yes | yes | yes |
  | New-component key count | maintenance.md:1205-1210 | exactly 1 | `1` | yes | yes | yes |
  | Verifier-round trigger | maintenance.md:1223-1226 | index form 0 post-commit; range form = N | index `0`; range `10` (= receipt) | yes | n/a (count) | n/a |
  | Bash-syntax ratchet | maintenance.md:1247-1259 | `new=0`; twins `tip=0` | efca24e and 3030551 ranges: every file `new=0`; HEAD: bootstrap 0/19, EPA 0/26 failing non-placeholder fences | yes | **yes — planted a broken fence in bootstrap.md (staged, sandbox) → `base=0 tip=1 new=1`** | yes |
  | Report-deletions structural gate | maintenance.md:1395-1422 | no output | healthy planted append → no output | yes | **yes — planted deletion of the receipts section → 6 RED lines (`^#{1,3} ` 3→2 … fenced body 33→16)** | yes |
  | Accepted-risk 5 fields | maintenance.md:1485-1493 | exactly 5 per record | 7 records across 6 reports, all `5` | yes | yes | yes |
  | `Applies` count vs subject | maintenance.md:1511-1516 | equal to subject count | efca24e 14 (subject states no count); 3030551 13 = "13 findings"; write-backs 0 (no Applies line, not fix commits) | yes where a count exists; efca24e's subject carries no number to compare | yes | yes |
  | Placeholder residue | maintenance.md:1548-1550 | no output | no output | yes | yes (stated negation proof) | yes |
  | Measurer phrase | maintenance.md:1620 | 0 | 6 lineage docs, all `0` | yes | yes | yes |
  | Post-push D16 | audit.md:134-143 | `origin/main at sNEW` then `…, 0 hits` | `origin/main at aad5b74` / `D16 log …: 0 commits scanned, 0 hits` | yes | yes (RED branches; gate hit) | yes |
  | Prediction evaluation grep | audit.md:1408-1415 | ≥1 over the PREVIOUS non-receipts-only report | previous = audit-2026-09-23.md (09-26 skipped as receipts-only); `1` | yes | yes — in-memory, `Prediction` lines removed → `0` | yes |
  | Per-file verification count | audit.md:1553-1555 | informational | 10 files, 18 lines | n/a | n/a | n/a |
  | Defect-series reconciliation | audit.md:1563-1571 | GREEN | `GREEN 18+2=20` | yes | yes — in-memory, row 20 removed → `RED 18+2 vs 19` | yes |
  | Report-committed check | audit.md:1584-1587 | `report committed: sHASH` after this run's commit | `NOT committed — last report commit 1905c6e, HEAD 1905c6e` (correct pre-persist state: HEAD is a write-back, not a `persist` commit) | yes for the current state | yes (observed) | yes (c190212 at its time) |
  | D10.6 parity grep | audit.md:951-954 | ≥1 on each of three | README.md:1, CLAUDE.md:1, Phase 0 table:1 | yes | yes — README with the phrase stripped in-memory → `0` | yes |

  probe-sandbox lines (one per sandboxed run, in order: liveness guard, bash ratchet, control enumerator TIP/BASE, report-deletions negation, report-deletions healthy, ratchet negation, D16 selftest, applied-proof 3030551, applied-proof efca24e):
  ```
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  probe-sandbox: GREEN — live repo unchanged; command exit 0
  ```

  **NOT RUN, and why:**
  - maintenance.md:27-31 `git push` half — forbidden to this agent; only the D16 verdict half ran.
  - maintenance.md:1233-1238 scratch-mother-repo recipe — requires `--keep`, which this run is forbidden to pass.
  - maintenance.md:1182-1189 per-RELOCATE grep — the batch declares 0 relocations (`classification: … 0 relocate`); nothing to grep.
  - maintenance.md:845-850 FORM B (`git log -L`) — the batch used form A for all 14 IDs; form A was re-run.
  - audit.md:119-122 report reconstruction — documentation of a recovery path with `<hash>` placeholders; no damaged report to reconstruct (the log of audit-2026-09-23.md shows persist `c190212` followed by two write-backs).
  - audit.md:1579-1581 report commit — a write; it is the orchestrating session's Phase 3 act, not this agent's.
  - The mechanical checks inside the other five agents' prompt blocks in audit.md (Agents 1-3, 5, 6) — each is executed by its owning agent in this same run; only this block's own D10.6 grep ran here.
  - Inline one-liners that serve as rationale evidence, not controls with an expectation (e.g. `grep -rn "placeholder"` at maintenance.md:1537, `grep -n "prediction"` at audit.md:1404).

- Runtime dual copy (D10.4): `cross-cutting-analysis` present in both | byte-identical: **yes** (`diff -r` exit 0, no output; `cmp` exit 0; both 7985 bytes)
- Trigger-list parity (D10.6) — THREE surfaces:
  CLAUDE.md events [(a) after every upstream absorption; (b) before every MINOR/MAJOR bump is PUSHED; (c) owner request; (d) after a /maintenance session applies an audit batch AND evidence from OUTSIDE the loop is waiting] | audit.md Phase 0 rows [(a) after an upstream absorption; (b) before a MINOR or MAJOR version bump is PUSHED; (c) owner request; (d) … AND evidence from outside the loop is waiting] | README.md `/audit` row (:382, "What each command does") [after an upstream absorption; before a MINOR/MAJOR bump is pushed; on request; after a /maintenance session applies an audit batch AND evidence from OUTSIDE the loop is waiting] —
  match: **yes** (grep: README 1, CLAUDE.md 1, Phase 0 table 1; the W-26 "is PUSHED" wording is on all three)
- Unresolved: **one stale line citation** — maintenance.md:1093 `bootstrap.md:350` (the `cp -r` of the skills is at `bootstrap.md:400`).

### Candidate findings
| Sev | Location | Finding |
|---|---|---|
| LOW | `.claude/commands/maintenance.md:1093` | Item 6 says the 15 skill templates are the ones "`bootstrap.md:350` copies with `cp -r`", but `:350` is inside the plugin-vetting checklist fence and the `cp -r docs/modules/skills/*` line is `bootstrap.md:400` (the step name would be a sturdier citation than a line number). |
| LOW | `assets/docs/audit-2026-09-26.md` (`sefca24e` receipts: `fences:`, `report deletions:`) | Two receipt `$` lines are not the mandated commands, which maintenance.md:323 forbids: `fences:` runs a rewritten counter variant (`… && echo ok | wc -l`), and `report deletions:` quotes `git diff --cached --name-only` rather than the structural gate. Re-run verbatim, both verdicts hold (13 of 13; no RED), so the defect is in the evidence, not the result. |

Observations outside D8/D10, left for the owning agents:
- The v2.34.0 commit `efca24e` subject states no finding count, so the `Applies` equality check (maintenance.md:1511-1517) has nothing to compare against. This is not RED, but it is unmeasurable.
- `audit.md:1469` ("This is the only file the audit writes") and CLAUDE.md ("TWO files … and — in verification mode — ONE appended row") describe the audit's writes differently. This belongs to Agent 5 / D12.
- Today's report will land in `audit-2026-09-26.md`, which is currently a `receipts-only` file. Phase 3 item 1's supersede rule (audit.md:1474-1479) applies to this run's persist.

## Agent 5: Document Accuracy

Run mode: baseline (trigger b, before pushing the v2.34.0 MINOR bump). HEAD 1905c6e. No writes in the repo; every check was a read-only scan run in place (ls / grep / git log / git blame / git diff). No probe, planted state or persisted script was needed, so probe-sandbox.sh was not invoked.

### PART 1 — Verification ledger
Part 1: N/A — baseline mode

### [C] Instruction Style
- Status: FAIL (4 LOW instances. All are pre-existing and were never filed: `git blame` dates them 2026-04-01..2026-08-31, and none appears in `assets/docs/audit-*.md`. The v2.34.0 batch added none of them.)
- Files sampled: docs/modules/skills/session-end/SKILL.md, docs/modules/skills/sprint-proposer/SKILL.md, docs/modules/skills/validation-orchestrator/SKILL.md, docs/modules/agents/code_reviewer.md, docs/modules/agents/validator.md
- Anti-pattern instances: 4
- Details:
  | File | Line area | Issue | Current text (excerpt) |
  |------|-----------|-------|----------------------|
  | docs/modules/agents/validator.md | 154 | A requirement that must hold on every mutation run is buried as the 5th sentence of a step paragraph, with no CAPS verb. If the agent skips it, the mutated code is left in the tree. | "Criteria MUST fail with broken code. … Restore code after each mutation. Max 3 mutations." |
  | docs/modules/agents/validator.md | 148 | The mobile-viewport check is written descriptively in lowercase. Its twin in validation-orchestrator Route 1 (`SKILL.md:98`) says "ALWAYS test at a mobile viewport (≤430px)", so the subagent route carries the weaker form of the same mandate. | "**Responsive check:** After verifying at default viewport, test at mobile viewport (≤430px width)." |
  | docs/modules/skills/validation-orchestrator/SKILL.md | 362-363 | A behavioural action ("flag it") sits at the tail of the ledger step, inside explanatory prose. It has no CAPS verb and no output destination: neither a report line nor a field says where the flag goes (all three §6 properties are missing). | "A `YES` means a one-off fix is NOT enough: flag it for the next codebase-audit / framework-audit as a class with a missing owner." |
  | docs/modules/skills/sprint-proposer/SKILL.md | 231 | The discovery-cap action is buried at the end of a long bullet, with no CAPS verb. The Sprint Report template (`:260-273`) has no slot for "cap exceeded", only "Discoveries added to backlog: [N]". | "**Cap: max 3 discoveries per sprint.** After 3, flag to human at next exception stop or sprint report." |

  Clean: session-end/SKILL.md (all 6 steps carry ALWAYS REPORT lines plus the mechanical count of 6) and code_reviewer.md (every "flag as FIX REQUIRED" carries ALWAYS; Coverage Gap Declaration says "ALWAYS include").

### [E] Version Consistency
- Status: PASS
- Canonical version: v2.34.0 (README.md:1)
- All occurrences:
  | File | Line | Version string | Matches? |
  |------|------|---------------|----------|
  | README.md | 1 | v2.34.0 | yes |
  | README.md | 151 | v2.34.0 (modules tree) | yes |
  | docs/modules/templates/claude_md.md | 4 | v2.34.0 slim orchestrator | yes |
  | .claude/commands/existing_project_adaptation.md | 260 | v2.34.0 slim orchestrator | yes |
  | .claude/commands/existing_project_adaptation.md | 619 | v2.34.0 CLAUDE.md | yes |
  | .claude/commands/existing_project_adaptation.md | 736, 1576, 1577 | v2.34.0 migration / install-report lines | yes |
  | .claude/commands/existing_project_adaptation.md | 289, 293, 305, 663-727, 885-887, 1031 | v1.6.0 / v2.1.0 / v2.25.0-v2.33.1 | historical migration notes (lineage, not current-version claims) |
  | docs/agentic_engineering_framework.md | 643 | v1.6.0 ("Since v1.6.0") | historical, n/a |
  | docs/modules/skills/README.md | 35 | v2.1.0 ("In v2.1.0, protocol logic moved") | historical, n/a |
  | docs/modules/skills/autonomous-loop/SKILL.md | 21, 28, 271 | v2.8.0 / v2.5.0 | historical lineage, n/a |
  | docs/modules/skills/sprint-proposer/SKILL.md | 285 | v2.8.0 | historical, n/a |
  | docs/modules/skills/commit/SKILL.md | 10 | created: framework-v2.2.1 | lineage (E.5). Not rewritten by any bump: `git diff 3030551 HEAD` shows no `created: framework` line changed |
  | CLAUDE.md | — | no version string | n/a |
- Mismatches: 0

### [D12] Examples Convention Compliance
- Status: PASS
- Files sampled (conventions read in full): agents concurrency-tester.md, load-tester.md, secrets-scanner.md; skills nextjs-supabase/, e-commerce-patterns/; rules auth-rules.md, multi-tenancy-rules.md. The D12.6 truth-tests were measured mechanically across the FULL set (20 agents / 9 skills / 11 rules):
  - Agents 20/20 carry name, effort, model, description, invocation: subagent, receives, produces. `## Output Format` 20/20; `## Output` 10/20. `## Input` is immediately followed by an output heading in 20/20. `## When this agent is invoked` 20/20; `## BOUNDARIES` 20/20; `- [ ]` checklist 20/20. `## When spawned` 10/10, and it is on exactly the 10 agents whose description carries a "declares a … gap" phrase (both sets identical).
  - Skills 9/9 use folder/SKILL.md with name, effort, description, invocation: inline. 0 carry `model:` and 0 carry `last_eval:`. `## Testing` is missing only in the 5 domain/process-pattern skills README exempts (ci-cd-pipeline, database-migration-guide, e-commerce-patterns, multi-tenancy-patterns, scheduling-patterns). All 3 stack skills have it.
  - Rules 11/11 carry `domain:`, `paths:` and `# Scope:`. 0 carry name/effort/invocation/model/applies_to. "New Table Checklist": 1/11 (README says 1). `- [ ]` checklist: 5/11 (README says 5). Inviolable + testing/verification heading: 11/11.
- Violations:
  | File | Missing field/issue | Convention reference |
  |------|-------------------|---------------------|
  | none | | |

### [D15] Factual Accuracy
- Status: FAIL (1 LOW omission; every numeric claim is accurate)
- Disk truth: docs/modules/skills = 15 folders (user 7 / inline 8; lifecycle 12 + tier-gated 3). docs/modules/agents = 10. docs/modules/templates = 8. docs/modules/rules = 5. examples = 20 agents / 9 skills / 11 rules. .claude/commands = 6. bootstrap.md primary steps 1-15 all present, plus Setup, 0.5, 1.1, 1.2, 1.5, 5.5, 5.7, 5.8, 12.5, 14.2, 14.5. Concepts doc = 2151 lines.
- Claims checked:
  | File | Claim | Actual | Accurate? |
  |------|-------|--------|-----------|
  | docs/agentic_engineering_framework.md:46 | 8 template blueprints | 8 | yes |
  | docs/agentic_engineering_framework.md:47 | 15 skills (12 lifecycle + 3 tier-gated); 3 process agents | 15 / 12+3 / 3 | yes |
  | docs/agentic_engineering_framework.md:48 | examples 20 / 9 / 11 | 20 / 9 / 11 | yes |
  | docs/agentic_engineering_framework.md:49 | 6 slash commands | 6 | yes |
  | docs/agentic_engineering_framework.md:50, :389 | 15-step pipeline (Setup + 1-15 + 0.5 + sub-steps) | matches bootstrap.md headings | yes |
  | docs/agentic_engineering_framework.md:256, :339 | 15 skills (12 lifecycle + 3 tier-gated) | 15 | yes |
  | docs/agentic_engineering_framework.md:405 | Step 5.7 copies 12 lifecycle skills | 12 | yes |
  | docs/agentic_engineering_framework.md:414 (pipeline table row 5.8) | lists only the tier-gated skills + skill_reviewer.md as 5.8 sources | bootstrap.md:493-504 also extracts metrics_md, framework_metrics_md, ops_rules, quality_budgets at 5.8 | OMISSION (LOW) |
  | docs/agentic_engineering_framework.md | banned "inline" sense ("N inline process skills", "inline (always copied)") | 0 hits (the only "inline skills" hit, rules-agents-updater:103, is the real `invocation: inline` value) | yes |
  | README.md:21-24 | 8 doc templates, 10 agent files, 5 rules files, 15 skills | 8 / 10 / 5 / 15 | yes |
  | README.md:34, :170, :190, :227 | baseline 12 lifecycle skills + 3 core rules | 12 / 3 (session, evolution, component) | yes |
  | README.md:45, :47, :170 | 5 session modes + 1 utility | 5 + /audit | yes |
  | README.md:55, :125 | 17 dimensions, 6 parallel agents | A, C, E, D4-D17 = 17; Agents 1-6 | yes |
  | README.md:141 | 6 slash commands | 6 | yes |
  | README.md:153-155 | 10 agents, 5 rules files, 15 skills (12+3) | 10 / 5 / 15 | yes |
  | README.md:159-161 | examples 20 / 9 / 11 | 20 / 9 / 11 | yes |
  | README.md:436 | concepts doc 1900+ lines | 2151 | yes |
  | README.md coverage: Maturity Levels table | rows 1-5 | concepts doc `### Level 1..5` | yes (complete) |
  | README.md coverage: Available Commands + What each command does | both tables list all 6 commands | 6 command files | yes (complete) |
  | docs/modules/README.md:10 | 15 skills (12 at 5.7, 3 tier-gated at 5.8) | 15 | yes |
  | docs/modules/README.md:7 | templates list (8 named) | 8 | yes |
  | docs/modules/skills/README.md:5-8 | 3 process agents; 15 skills = 12 lifecycle (1+5+3+1+1+1) + 3 tier-gated | 3; the breakdown sums to 12 | yes |
  | docs/modules/skills/README.md:36 | 7 `user`, 8 `inline` (named) | 7 / 8, names match frontmatter | yes |
  | docs/modules/skills/README.md list | 15 rows | 15 folders | yes |
  | CLAUDE.md:20, :39 | 6 commands (5 sessions + 1 utility) | 6 | yes |
  | CLAUDE.md:40, :73 | 15 process skills (12 + 3 tier-gated) | 15 | yes |
  | CLAUDE.md templates block | 8 templates named | 8 | yes |
  | CLAUDE.md agents block | 10 agent templates named | 10 | yes |
  | CLAUDE.md skills block | 6 named + "(9 more)" | 15 | yes |
  | CLAUDE.md:83-85 | examples 20 / 9 / 11 | 20 / 9 / 11 | yes |
  | CLAUDE.md:146 | 17 dimensions, 6 parallel agents | 17 / 6 | yes |
  | examples/README.md:12, :34, :54 | 20 / 9 / 11 | 20 / 9 / 11 | yes |
- Inaccuracies: 1 (LOW omission. The concepts-doc pipeline table's 5.8 row is incomplete; no count is wrong.)

## Agent 6: Process Coverage (meta)

### PART 1 — Verification ledger (verification mode ONLY; write `Part 1: N/A — baseline mode` otherwise)
Part 1: N/A — baseline mode

### [D17] Process Coverage
- Status: FINDINGS (1 LOW candidate; every other question PASS)

- **Commits classified — 40 (`34a7c1c..1905c6e`), `git log --format="%h|%ad|%s" --date=short -40`:**
  - audit-fix application 8: `efca24e`, `3030551`, `7be4aea`, `1487052`, `4b25457`, `3c24435`, `908a53a`, `280c2f2`
  - write-back 7: `1905c6e`, `aad5b74`, `c16b5d4`, `aab3790`, `9a539ba`, `172eb8b`, `a10b4a0`
  - push record 5: `0ae5f08`, `c98db39`, `e895711`, `08e2123`, `f48b421`
  - maintenance feature/fix (no audit batch) 4: `34a7c1c`, `0bb7949`, `455c299`, `961244b`
  - commit correction 4: `df26d7e`, `5e9bac5`, `34a6c97`, `c9355b0`
  - audit persist 3: `c190212`, `ab90b2c`, `6b4ae42`
  - placeholder substitution 3: `d925474`, `8b57b0d`, `db6cb1d`
  - upstream absorption 2: `2cb4ede`, `ece8b08`
  - receipts persist 1 (`9b0fffa`), errata 1 (`9a959e3`), backlog triage 1 (`7899e99`), owner-decision record 1 (`cc3e14b`). Each of these is below the 2+ threshold.
  - Sum: 8+7+5+4+4+3+3+2+4×1 = 40.

- **Undocumented executed flows: none.** Every type seen 2+ times has a written home:
  - audit-fix application and write-back: `maintenance.md:1367` "Audit intake".
  - push record: `maintenance.md:18-30`, `:61` (WHEN A PUSH LANDS…) and `:138-141` (ALWAYS WRITE THE `push:` LINE INTO THE NEWEST-DATED REPORT FILE).
  - commit correction: `maintenance.md:1570`.
  - placeholder: `maintenance.md:1528-1551`.
  - audit persist: `audit.md` Phase 3 item 1.
  - upstream: `maintenance.md:1588`.
  - receipts persist / receipts-only: `maintenance.md:350-357`.
  - Last run's one flow gap was W-26 ("commit the bump, audit, then push"). It now has a home at `maintenance.md:1281-1287` and `:19-20`, with parity at `CLAUDE.md:153`, `README.md:382` and `audit.md:89` (back-sweep residue re-measured: `efca24e~1` 4 → `efca24e` 0 → `HEAD` 0).
  - It is also the first observed COMPLIANT execution: v2.34.0 `efca24e` is committed and unpushed (`git status -sb` → `## main...origin/main [ahead 3]`, `origin/main` = `aad5b74` after `git fetch --prune`), and this baseline run is the held audit.
  - Today's other new shape was a baseline `/audit` appended to a same-day `receipts-only` file. It is instructed at `audit.md:1474-1482` (supersede the header and add no second `**Run mode:**` line).

- **Referenced-but-unowned conventions: none new.**
  - `evolution_policy.md:144-146` promises that a mother-repo `/maintenance` session reads the doc and records the disposition in lineage. The owner is `maintenance.md:1588` "Upstream intake", plus Step 0 at `:166`.
  - `framework-audit/SKILL.md:174` ("upstream vehicle to the mother framework repo") has the same owner.
  - Carried observation, still below the bar: `evolution_policy.md:138-141` asks a pilot doc to record its CUT CONDITION, and nothing on the mother side reads it (`grep -ciE "cut condition|pilot" .claude/commands/maintenance.md` → `0`). The template promises no mother-side behaviour, so this is not a D17.2 finding.
  - Out of scope, noted for Agent 2: the one shipped line these batches added that cites the mother repo is `validation-orchestrator/SKILL.md:128`, "(`/audit` 2026-09-22 AE-15)". It is an evidence citation in the established convention (23 such citations across `docs/modules`), not a promised behaviour.

- **Aspirational mechanisms: none.** The framework-level cadence claims all have owners:
  - `CLAUDE.md:98` (fetch before every bootstrap/adaptation) is owned by `/bootstrap` Step 0.5 and EPA Step 0.5.
  - `CLAUDE.md:151-175` (the `/audit` trigger list) is owned by `audit.md:86-91` and `maintenance.md:1281` for trigger (b), and by `maintenance.md:236-246` for trigger (d).
  - `CLAUDE.md:227` (Step 0 runs in EVERY maintenance session) is owned by `maintenance.md:166`.
  - Every other "every session" / "periodic" hit in `README.md` and `docs/agentic_engineering_framework.md` is project-level (Known Bug Patterns, project.md, codebase-audit, framework-audit), which is out of scope.

- **Unprovable self-checks (D17.4)** — D10's verdicts are Agent 4's and are cited, not recomputed. Only the provability axis is reported here.

  | Check | Can go RED? | Can go GREEN? | Verdict |
  |---|---|---|---|
  | `/audit` Phase 0 post-push D16 (`audit.md:134-142`) | yes (fetch fail / no hash / hit) | yes: re-run now → `origin/main at aad5b74`, `0 commits scanned, 0 hits`, reading the receipts-only 09-26 file | provable (D10: see Agent 4) |
  | `/maintenance` push-decay detector | yes | yes (same file, same endpoint) | provable (D10: Agent 4) |
  | trigger (d) PEND/OBS (`maintenance.md:236-246`) | yes | yes (re-run → "does not fire") | provable |
  | apparatus moratorium ratchet 27 / 14 (`maintenance.md:271-272`) | yes (any increase) | yes (re-run → `27`, `14`) | provable (known false RED on a new sub-list, AC-16) |
  | receipts-only definition, line-anchored (`audit.md:1480`, `maintenance.md:357`), NEW in `3030551` | yes: the 09-26 file reads `1` → skipped | yes: 09-22/09-23 read `0`, and 09-26 reads `0` once this run supersedes it | **now provable.** Last run rated it unprovable (AF-1); the fix closed that |
  | EPA Step 2.5 box count vs template (`existing_project_adaptation.md:511`), NEW in `3030551` | yes: `3030551~1` → 9 vs 13 (reproduced) | yes: `3030551` → 13 vs 13 (reproduced) | provable. The template side is a command; the list side is counted by the reader (the receipt's awk is not in EPA) |
  | bootstrap Step 8 twin (`bootstrap.md:596`), NEW in `3030551` | n/a (a directive count with no stated expected result) | n/a | not a self-check; no new unprovable check |
  | version residue grep (`maintenance.md:1316-1318`) | yes (pre-bump) | yes (`v2.33.1` residue → `0` at tip) | provable |
  | control back-sweep numerator (`maintenance.md:1044-1045`) | — | **did not read 0 on a batch that touched no control:** at `efca24e` it counted `1`, and that one line is prose (`code_reviewer.md:293` "…AND grep for existing instances") | fires on a non-control state; same class as U-45 (09-10). See the finding below |
  | `efca24e` new checks | none added (0 frontmatter keys, 0 new ALWAYS REPORT keys beyond the widened `security-reviewer:` slot, whose slot grep reads `1`) | — | no new unprovable check |

- **Self-reported controls (D17.5)** — re-measured sample of the 09-26 receipts, run in place (all read-only):

  | Key re-run | Receipt says | Re-measured now |
  |---|---|---|
  | instruction style `s3030551` | 13 / 1 / 0 | 13 / 1 / 0 ✓ |
  | instruction style `sefca24e` | 18 / 3 / 1 | 18 / 3 / 1 ✓ (the 1 buried is the `NEVER emit nothing` tail, as stated) |
  | isolation `3030551` / `efca24e` | 1 commit, 0 hits each | same ✓; plus `origin/main..main` 3 commits, 0 hits |
  | applied grep `3030551` / `efca24e` | 13 / 14 | 13 / 14 ✓ |
  | applied-proof (7 of 27 IDs sampled: AF-1, AF-5, AF-19, AF-7, AE-15, W-26, AF-24) | form A 1 each | 1 each ✓ |
  | negation proof EPA / tool gate | 9, 13, 13 / 0, 6 | same ✓ |
  | W-26 back-sweep residue | 4 → 0 | 4 → 0, HEAD 0 ✓ |
  | version `v2.34.0` / residue `v2.33.1` | 1,5,1,2 / 0 | same ✓ |
  | class-sweep file counts | 7 / 14 | 7 / 14 ✓ |
  | verifier trigger file counts | 6 / 10 | 6 / 10 ✓ |
  | liveness `efca24e`, defect series, report restore | 0 / 0 / 0 | 0 / 0 / 0 ✓ |
  | twin parity | PASS | PASS ✓ |
  | push endpoint (`push:` / `push decay:`) | `aad5b74` | fetched `origin/main` = `aad5b74` ✓ |
  | control back-sweep `sefca24e` | "1 of 1 … the one command-bearing added line is the EPA v2.33.1 detection grep, moved together with the notes" | the number `1` reproduces, **but the counted line is `code_reviewer.md:293` prose.** `git diff -U0 efca24e~1..efca24e -- existing_project_adaptation.md \| grep "proven to BLOCK"` → no added line (the detection grep was added in `3030551`). The step-(1) denominator command and stdout that `maintenance.md:1071` requires are absent ✗ |

  Result: 19 of 20 sampled keys reproduce exactly. One key's number reproduces, but its narrative is wrong and its mandated evidence is missing.

  | Control | Verified by whom | Output persisted where | Gate? |
  |---|---|---|---|
  | `$`-line receipt keys (27 per batch) | re-runnable by any later session; 19/20 reproduced this run | `audit-2026-09-26.md` | **Yes** |
  | judgement keys (`classification:`, `new component:`) | author only (`$ n/a — judgement`) | same file | No (disclosed as judgement) |
  | class-sweep ✓ cells (27 rows) | author; most cells quote a command, some only a verdict ("✓", "same edit as") | same file | Partial |
  | pre-commit verifier rounds (6 + 6 defects, all fixed) | independent agent | same file, list | **Yes** |
  | "Twice before commit the index read 2 buried" (`s3030551` instruction style) | author only; not reproducible after the fact | same file | No (history claim) |
  | control back-sweep narrative (`sefca24e`) | author only; wrong on re-measure | same file | **No** — see finding |
  | W-26 push hold for an unpushed MINOR/MAJOR (`maintenance.md:1285-1287`) | nobody at push time: the push gate branches on D16 only, and the file says so | the `push:` / `audit:` keys | **No.** It is prose-held by owner decision, and the moratorium forbids a new control. Honoured once (today) |
  | `push:` rewritten after the push (Errata 1, `audit-2026-09-26.md:209`) | the next session's push-decay detector (re-run now, it matches) | same file | Yes |

- **Convergence assessment (D17.6)** — shipped = `docs/modules` + `examples`; apparatus = `.claude` + `assets/docs` + `CLAUDE.md` (the audit.md command, run verbatim per range):

  | Range | Kind | Shipped | Apparatus | Uncounted (README / framework doc — Z-19) |
  |---|---|---|---|---|
  | `2cb4ede~1..2cb4ede` | upstream v2.33.0 | **+43/-2** | +93/-3 | +2/-2 |
  | `9b0fffa~1..9b0fffa` | receipts | 0 | +278 | 0 |
  | `c190212~1..c190212` | baseline audit | 0 | +549/-1 | 0 |
  | `3030551~1..3030551` | audit-fix v2.33.1 | +1/-2 | +60/-18 (of which EPA + bootstrap +33/-8, the delivery path — Y-16) | +2/-2 |
  | `aad5b74~1..aad5b74` | write-back + receipts | 0 | +219/-9 | 0 |
  | `0ae5f08~1..0ae5f08` | push record | 0 | +10/-6 | 0 |
  | `efca24e~1..efca24e` | audit-fix + feature v2.34.0 | **+46/-20** | +33/-12 | +6/-6 |
  | `1905c6e~1..1905c6e` | write-back + receipts | 0 | +204/-9 | 0 |

  - **Mechanical judgement:**
    - (1) **Do the controls these batches added address the causes of what they fixed?** Yes.
      - `3030551` fixed AF-1 (an undefined "receipts-only header") by making the definition line-anchored and mechanical. That check had no reachable RED/GREEN last run and has both now (table above).
      - It fixed AF-3 (a stale count) by making the count derive from the template. Negation was reproduced: 9→13.
      - `efca24e` added one shipped spawn trigger (AE-15), a product change and not apparatus.
      - Neither batch added a receipt key or a checklist item: the moratorium ratchet reads 27 / 14, unchanged.
      - AF-2 was left `open` rather than given a forbidden control, so the moratorium is holding.
      - **The controls are adequate.** The one prose-only control, W-26's push hold, is an explicit owner choice, and it was honoured today.
    - (2) **Defect rate.**
      - No verification run has happened since row 20 of "The defect series" (09-20). This is the trigger-(d) exit working: `verification audit:` → "does not fire" on both of today's batches (re-run).
      - So there is no new post-commit rate to trend. This baseline run's own findings (other agents) are the next data point.
      - The pre-commit verifier caught 10 (`2cb4ede`), 6 (`3030551`) and 6 (`efca24e`) defects. Every one was fixed before commit, so the gate is converting them earlier. The generation rate per applied fix is not falling: 6 per 7 findings, then 6 per 8.
    - (3) **Is the loop consuming more effort than it returns?** Measured: **yes, in line terms, but it is not trending to zero shipped.**
      - Today's two batches with their write-backs: shipped +47/-22 against apparatus +526/-54, a **92/8 split**.
      - The upstream batch with its receipts and audit: shipped +43 against +920.
      - Shipped delta did not trend to zero. `efca24e` is the first audit-fix commit in the series whose own shipped delta (+46) exceeds its apparatus (+33), so re-pointing the loop is **not** warranted.
      - The dominant apparatus term is now receipts and write-back: 219 and 204 lines to certify +1 and +46 shipped lines. The moratorium caps receipt KEYS, not receipt VOLUME.
      - This is an observation for the owner, not a finding, because no rule is violated and "Cycle governance" already governs the loop.
    - Measurement caveat carried from the open ledger: Y-16 (EPA and bootstrap count as apparatus) and Z-19 (`README.md` and `docs/agentic_engineering_framework.md` are counted nowhere: +10/-10 across today's batches) still bias the split. Correcting both would move `3030551` from 1 to 34 delivered lines and would not change the conclusion.

#### Candidate findings
| # | Severity | Location | Finding |
|---|---|---|---|
| 1 | LOW | `assets/docs/audit-2026-09-26.md:317-319` (the `sefca24e` `control back-sweep:` receipt) vs `.claude/commands/maintenance.md:1071-1072` | The receipt attributes its numerator `1` to "the EPA v2.33.1 detection grep, moved together with the notes". Re-measured, the only counted line is prose in `docs/modules/agents/code_reviewer.md:293` ("AND grep for existing instances"). The step-(1) denominator command and stdout that the mandate requires are absent. The key's verifier was its author, and its narrative does not survive re-measurement. The numerator counting prose is the U-45 class (09-10); the correct report was arguably `N/A — no control touched`. Natural home: none new (moratorium). Correct the receipt by errata, and apply U-45's class when `maintenance.md:1044` is next edited. |

No other gaps. "Controls adequate" is the D17.6 verdict.


## Recommended Fixes

0. **LOW BACKLOG — never a session on its own.** These are applied only when a later session already edits the file (`/maintenance` → "Cycle governance"):
   - AG-2 and AG-12's rule context (EPA; the commit convention);
   - AG-3 and AG-4 (`validator.md`);
   - AG-5 (validation-orchestrator);
   - AG-6 (sprint-proposer);
   - AG-7 (the concepts doc);
   - AG-8 and AG-11 (this file's receipts; remedy is an erratum);
   - AG-9 and AG-13 (`audit.md`);
   - AG-10 (`maintenance.md`);
   - plus the carried LOWs.
1. **Quick fixes:**
   - AG-1 is two path corrections on a SHIPPED template (`examples/` → `assets/examples/`), which is why it is MEDIUM. It is one edit and blocks nothing.
2. **Structural fixes:** none new this run.
3. **Quality improvements:** AG-3..AG-6, instruction style on shipped skills and agents. They are backlog under rule 2.
4. **ESCALATED — owner decision required:** none this run.
5. **ACCEPTED-RISK — no action:** E-1, M-44, N-53, T-11, T-36, U-48, B-17 (see the header OBSERVATIONS).

**No finding blocks the v2.34.0 push.** The only MEDIUM (AG-1) predates the batch: the cited paths date from the §10/§12 additions. It sits on a shipped surface and can ride the next batch. Trigger (b) is discharged by this report.

## Meta-observation

**prediction: 3 clauses evaluated — (1) void, (2) void, (3) held.** Scoped to `audit-2026-09-23.md` Run 1, the previous report with a ledger.
- **(1) void.** The clause was conditional on AF-1 being fixed "by adding a third skip-or-append rule rather than removing the special file kind". `3030551` took the other branch the clause itself names: it supersedes the header and keys every reader on a line-anchored definition. So the antecedent never occurred. D17.4 now rates the definition provable in both directions.
- **(2) void.** No upstream absorption happened between the runs.
- **(3) held.** The v2.34.0 MINOR bump was committed (`efca24e`), the audit ran after it (this run), and the push is held by prose. The rule was reworded the same day (W-26, owner) to make this order the compliant one, so the recurrence is now sanctioned rather than a violation. The hold is still prose, not a mechanism the push gate reads (Agent 6, D17.5).

**The recurring class this run saw: RECEIPTS are now the least-verified text in the repo.**
- The shipped and command surfaces the batches touched came back clean in every dimension: D5, D6, D7, D8, D10, D11, D14 and D16 all PASS. The 45 self-checks all match their expectations.
- Every new finding about today's batches is in the RECEIPTS (AG-8, AG-11) or the commit metadata (AG-12).
- The rest are older text the batches never touched (AG-1..AG-7, AG-9, AG-10, AG-13).
- The receipts were typed by hand. The verifier rounds check the diff, not the receipts written after it, and nothing re-runs a receipt before it is committed. D17.6 confirms that receipts and write-backs are now most of the apparatus volume (219 + 204 lines today).

**Prediction, stated so it can fail.**
- **(1)** The next batch whose receipts are typed by hand will carry at least one `$` line that is not the mandated command, or a count attributed to the wrong line. **Falsifier:** the next receipts section re-runs clean line-for-line under Agent 4's D10.0, or is produced by a pasted generator.
- **(2)** AG-1 has a class: shipped templates citing `examples/...` where the project path is `assets/examples/...`. It will be found in at least one more shipped file when AG-1 is swept. **Falsifier:** a LATERAL sweep during AG-1's fix (`grep -rn '`examples/' docs/modules`) returns only AG-1's two sites.
- **(3)** The first Phase 2 batch (the company-root bootstrap) will produce findings concentrated in the bootstrap↔EPA twin parity. Two of this week's three verifier rounds found twin-parity defects (AF-19, AF-26). **Falsifier:** its verifier round and its trigger-(b) audit find no twin-parity defect.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan. AG-1 can ride the next batch; nothing here blocks pushing v2.34.0.

---

**Report status:** COMPLETE — all 6 agents returned, all 17 dimensions evaluated, no `probe-sandbox: RED` (Agents 3 and 4 ran sandboxed probes: `probe-sandbox: GREEN — live repo unchanged; command exit 0`).
**defect series:** N/A — baseline mode.
**Application status:** PENDING
