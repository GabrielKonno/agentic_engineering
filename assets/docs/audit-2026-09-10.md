# Framework Audit Report — 2026-09-10

**Framework version:** v2.20.0
**Run mode:** `verification (over the Run 10 batch — s8e18c1d, s51f395c, s1715699, scf52379, sfd09b39, sa6349da, s7bf4449)`
**Dimensions checked:** 17 + a Part 1 fix-verification pass
**Agents dispatched:** 6

**carried: 0 open + 0 escalated from `audit-2026-09-09.md`** — Run 10 closed at 51 of 51 disposed.
Part 1 input read directly from disk; the report is intact (531 lines, 51 ledger rows) and needed
no reconstruction.

**Accepted-risk OBSERVATIONS (re-measured, never re-opened):**
- **E-1** — 39 blobs in one lineage file's revision chain, working tree **0**. Consistent. Closed.
- **M-44** — no new occurrence. Closed.
- **N-53** — 0 working-tree occurrences. Closed.
- **T-11** — five-field record verified; the seven withheld identifiers return **0 files** across
  tracked files, `.claude/docs/` and agent memory. Closed.
- **T-36** — five-field record verified; exactly one commit-message body line carries it, as the
  record states. Closed.
- Two schema names under D16.2c's industry-names exemption — standing per Run 6's closing note.

**ID series:** `U`. Collision check before writing: `grep -c "^| U-" assets/docs/*.md` → **0**.

**Push state:** `git status -sb` → `## main...origin/main`, no ahead/behind. **All 164 commits are
PUSHED, including the entire Run 10 batch.** Nothing in history is locally fixable.

---

## Part 1 — Verification ledger for the Run 10 batch

| ID | Verdict | Evidence (structural) |
|----|---------|------|
| T-1 | **CONFIRMED-FIXED** | Restoration is byte-faithful. `diff` of `6565f5c` vs `8e18c1d` yields 36 `<` lines; normalising only the status column and the `Application status:` line returns **0 deletions**. 574 → 729 → 747 lines. `Errata 2` at `:447`. The ten never-applied rows are deliberately left `open`, matching the erratum's stated intent. |
| T-2 | **CONFIRMED-FIXED** | Superset measured: **184** distinct provenance IDs cited across `.claude/commands/*.md`, `CLAUDE.md`, `.claude/rules/*.md`; **0 unresolved** (was 37 dangling). |
| T-3 | **CONFIRMED-FIXED** | `grep -c '^> [*][*]Errata 1 '` → **1**; `grep -c '^\| R-1 \|'` → **1**. Both were 0. |
| T-4 | **CONFIRMED-FIXED** | Gate 3: **35** applied + **10** open = **45** = total `R-` rows. |
| T-5 | **PARTIALLY-FIXED — INTRODUCED-A-DEFECT** | The `$`-line assertion works BOTH directions — GREEN over Run 10's receipts (23 keys, all `$-line ≥1`), RED when a key or its `$` line is removed. Compliance moved **5/20 → 23/23**. **But the third component fires on the healthy state:** the elision grep is unanchored, so pasted `## main...origin/main` stdout matches `\.\.\.` — returns **5**, expected **0**. → `U-11` |
| T-6 | **CONFIRMED-FIXED** (first form INTRODUCED-A-DEFECT, self-caught inside the batch) | Rule + command + expected result at `maintenance.md:845-876`. The first form counted raw deletions and read 52 on a write-back that removed nothing; `a6349da` replaced it with a structural count. Proved both directions independently: GREEN on `fd09b39`, RED on `ae71374` (`13→1`, `167→0`, `1→0`). **First time in the series a batch caught and corrected its own control before the audit did.** Two blind spots → `U-3`, `U-8`. |
| T-7 | **CONFIRMED-FIXED** | `awk -F'\|' 'NF{print NF-2}'` over the three template lines → `8 / 8 / 8`. Negation: deleting a header cell → `7 / 8 / 8`. |
| T-8 | **CONFIRMED-FIXED** | Two-form harvest installed on both twins. **Negation proof reproduced independently on identical input** (code-reviewer's blockquote deleted): old loop → `9 verified, 1 broken`; new loop → `10 verified, 0 broken`. |
| T-9 | **CONFIRMED-FIXED — FIXED-BUT-CLASS-NOT-SWEPT** | Body-prose-only and negated-body cases both return `10 verified, 0 broken` ✓. **Class not swept:** the range terminator `/^[a-z_]*: /` cannot match hyphenated YAML keys and the closing `---` does not stop it, so on the CANONICAL agent shape (`name:`/`description:`/`allowed-tools:`) the range runs to EOF and the false-BREAK returns. → `U-4` |
| T-10 | **PARTIALLY-FIXED — INTRODUCED-A-DEFECT** | `applied-proof` installed at `maintenance.md:473-489` and provable both ways in isolation. **Two defects:** it does not say WHICH line numbering to use — `git log -L` resolves against the range TIP while ledger cells are PRE-fix numbers, so run with the ledger's own numbers it reads **RED on 23 of 38** parseable IDs of a healthy batch; and its `$` line runs a file-union command that tests no ID and no line. → `U-1`, `U-2` |
| T-12 | **CONFIRMED-FIXED — INTRODUCED-A-DEFECT** | All three slots exist (D6.9, D7.7, D7.8) and D6/D7 are in ascending order ✓. **The applying commit added a contradiction:** `audit.md:579` mandates a two-count `activation chain check: N of M specialists resolved, K broken` that the loop can never emit and that contradicts D7.7 and the slot added in the same commit. → `U-29` |
| T-13 | **PARTIALLY-FIXED** | The MANDATE was rewritten on both twins to the three-count form ✓. **The report slot the finding named was never touched:** `bootstrap.md:825-826` and `EPA:1119-1120` still read `N verified, M broken` · `none installed`. `git log -L 825,827` over the batch → **EMPTY**. Found independently by two agents. → `U-5` |
| T-14 | **CONFIRMED-FIXED** | A sanctioned `prototype` that strips every gap section now prints `none installed` and **exits 0**; the surviving `exit 1` fires only on the genuine RED. |
| T-15 | **CONFIRMED-FIXED — CLASS-NOT-SWEPT** | Digit and dot domains now surface as BROKEN ✓, every branch echoes ✓. **Third door:** `&` is still outside the class, and the fall-through branch then MISREPORTS — `INFO: … no gap phrase` on an agent that has one, with `0 broken`. → `U-9` |
| T-16 | **CONFIRMED-FIXED** | The loop derives the declarer set; the typed `for dc in …` is gone from both twins; the paragraph is now on both sides, closing R-32's inverted divergence. |
| T-17 | **PARTIALLY-FIXED — INTRODUCED-A-DEFECT** | The two-shape enumerator is installed and gained a RED CONDITION ✓. **Arithmetic wrong:** re-run verbatim it returns `22 + 33 = 55`, not 52; the receipt pasted `audit.md: 4 fenced + 26 = 30`, a hybrid true at no single commit. The in-file calibration is also wrong on the fenced half. → `U-6` |
| T-18 | **PARTIALLY-FIXED** | Disk is right (`10 of 20`) and the commit message was corrected by follow-up ✓. **The third surface was not:** `audit-2026-09-04.md:679` still reads `20 keys, 11 harvestable`, and Errata 3 covers only T-19…T-25. → `U-21` |
| T-19 | **FIXED-BUT-CLASS-NOT-SWEPT** | Errata 3 present ✓; 22/6/4 reproduces exactly with its range stated ✓. **The prose disposing of the 4 flagged lines is true of only 1 of 4** — the other three are mandates appended after a rationale clause. → `U-39` |
| T-20 | **FIXED (instance) — CLASS RECURS IN THE SAME BATCH** | Errata 3 records the Run 9 defect ✓. **The Run 10 back-sweep header reads `4 artifacts fixed of 4 found` while its own rows sum to 18** — the rule count silently reused as the artifact count. Third recurrence of this class. → `U-10` |
| T-21 | **FIXED (record) — INTRODUCED-A-DEFECT** | Errata 3 present ✓ and item 3 gained the command ✓. **Its first discharge violates it:** the receipt's `$` line DELETES the first regex alternative, and one of the nine printed targets does not appear in the diff at all. Mandated command returns 41 (receipt's range) / 47 (batch). Third consecutive occurrence. → `U-7` |
| T-22 | **CONFIRMED-FIXED** | `isolation: 11 files scanned, 0 hits` — the mandated form, and 11 is the true unique tracked-file count. |
| T-23 | **CONFIRMED-FIXED** | Gate 2 run per hash: all four `File(s) touched` cells reproduce exactly, set-identical. |
| T-24 | **FIXED-BUT-CLASS-NOT-SWEPT** | The rule landed ✓. **Three keys written under it violate it:** `references:` (narrowed command), `applied-proof:` (a command that does not test the claim), `fences:` (a parenthetical where stdout belongs). → `U-7`, `U-2`, `U-32` |
| T-25 | **PARTIALLY-FIXED** | Errata 3 records the Run 9 state ✓. The receipt's own discharge claims **1** where the command returns **2** — the second hit was added by `a6349da`, after the claim was written and never re-measured. → `U-11` |
| T-26 | **PARTIALLY-FIXED — INTRODUCED-A-DEFECT** | The per-form rule landed ✓. **Both halves false on first use:** `rows 4 = commits 4` over a **7-commit** batch, and `findings-union 51` while the gate's own command returned 48. Splice residue at `:434-435`. → `U-14`, `U-37` |
| T-27 | **CONFIRMED-FIXED** | The orphan fragment is gone; the sentence is whole with a single citation. |
| T-28 | **CONFIRMED-FIXED — INTRODUCED-A-DEFECT** | Content correct — `CLAUDE.md:110-113` now covers BETWEEN SESSIONS ✓. **The replacement bullet carries 5 `**` markers (odd ⇒ unbalanced)**; the opener before `ALWAYS run D16` is unmatched. The R-39/T-37/T-39 class, in the batch fixing T-37 and T-39. → `U-20` |
| T-29 | **CONFIRMED-FIXED** | Invoker installed at the moment it runs (`maintenance.md:58-67`), with command, stated expectation and a registered `push decay:` key. component-design §9's three obligations discharged. |
| T-30 | **CONFIRMED-FIXED** | Pure-version-bump commits now have a written home and an exemption that says so. |
| T-31 | **CONFIRMED-FIXED** | "EXTENDING A CLOSED SERIES" installed with the collision check and the `[filed by …]` marker. |
| T-32 | **CONFIRMED-FIXED** | Self-check RUN: `grep -c '^## ' claude_md.md` → **13**; `□` boxes → **13**. Equal. `## Commit Hygiene` present; Session Protocol now names five pointers. |
| T-33 | **FIXED-BUT-CLASS-NOT-SWEPT** | Exactly 7 session coordinates genericised ✓, matching the receipt. **The finding's second half (line-count figures) was swept at one site and not two others** in the same file. → `U-22` |
| T-34 | **FIXED-BUT-CLASS-NOT-SWEPT** | All three named sites fixed ✓ (`0` repo-wide). **The class survives 35 lines above the fix**, in the same file: a duplicated clause separated by a comma, invisible to the doubled-word grep. → `U-19` |
| T-35 | **CONFIRMED-FIXED (disk-state)** | Applied outside the repo; both memory files carry the expected mtime. Doubled-word scan with newlines folded over all 10 files → **0 hits**; no Portuguese fragment outside the sanctioned role descriptor. Receipt count disagrees → `U-23`. |
| T-36 | **CONFIRMED-FIXED** | Five-field record; measured claim TRUE today; publishes no literal. |
| T-37 | **CONFIRMED-FIXED** | Three balanced `**` pairs; emphasis renders correctly. Both figures re-measured on the full 11-file set and the two sets verified disjoint. |
| T-38 | **PARTIALLY-FIXED — INTRODUCED-A-DEFECT** | The `.,` splice is gone and only one rationale survives ✓. **The clause the finding named still has no predicate** (`EPA:363`), and deleting the old list orphaned `:365`, which now begins mid-sentence. The intact form survives at three sibling sites. → `U-17` |
| T-39 | **CONFIRMED-FIXED** | Per-line `**` counts balance; the block total is even. |
| T-40 | **PARTIALLY-FIXED — NOT-FIXED (second half)** | Heading swept ✓. **The second defect was never touched:** `git log -L 1236,1236` and `1238,1238` over the batch → empty; the defect-series command still carries no expected result. → `U-35` |
| T-41 | **CONFIRMED-FIXED — INTRODUCED-A-DEFECT (LOW)** | All five run-count copies deleted; the table has exactly 10 rows and row 10 matches the score ✓. **The replacement clause says trigger (d) is "below" when it is three lines above.** → `U-34` |
| T-42 | **CONFIRMED-FIXED** | `row N appended` / "ONE surface, never three"; the only `of 3 surfaces` hit describes the superseded form. |
| T-43 | **PARTIALLY-FIXED** | The 14-case regression table IS on disk ✓ and "against BOTH twins" landed ✓. **"outside the repo" never landed** — still prompt-only, which is T-43's own class. → `U-31` |
| T-44 | **CONFIRMED-FIXED** | The typed 10-file roster is replaced by a glob with its citation. |
| T-45 | **CONFIRMED-FIXED** | The reconstruction fallback, its two commands and the mandated header line are all present. |
| T-46 | **PARTIALLY-FIXED — NOT-FIXED (first half)** | D17.6's measurement mandate landed with its slot ✓. **D17.4's division of labour was never written** — `git log -L 975,990` over the batch → empty. → `U-36` |
| T-47 | **CONFIRMED-FIXED** | The `Setup` row is present and matches what `## Setup` does; the diagram now carries 26 rows. |
| T-48 | **CONFIRMED-FIXED — FIXED-BUT-CLASS-NOT-SWEPT** | The path resolves ✓. **The block carrying the fix still asserts word-for-word twin equivalence, and that claim is still false** in four places. → `U-24` |
| T-49 | **CONFIRMED-FIXED** | The file opens with an H1, date, version, run mode and agent count. Class swept: all 6 reports open with an H1 and carry a date and version within 4 lines. |
| T-50 | **CONFIRMED-FIXED** | The append is authorised and bounded; the contradiction with item 6 is resolved. |
| T-51 | **FIXED-BUT-CLASS-NOT-SWEPT** | The fifth verdict landed in the agent mandate ✓. **Not swept to the report template:** the Part 1 row still lists four values and both score-line forms offer three not-clean categories with no `not-fixed` slot. Gate 4's DESCENDING direction. → `U-12` |

**Score, counted over the FINDINGS (denominator = 51 findings; the ledger's row total coincides at
51, with no VERSION row and no carried-forward IDs): 27 of 51 clean, 24 not clean — 47%.**
Of the not clean: **10 partially-fixed · 8 class-not-swept · 0 not-fixed-as-a-whole-finding ·
14 introduced-a-defect** (verdicts combine; each finding counted once under its most severe).

**Clean (27):** T-1, T-2, T-3, T-4, T-6, T-7, T-8, T-11, T-14, T-16, T-22, T-23, T-27, T-29, T-30,
T-31, T-32, T-35, T-36, T-37, T-39, T-42, T-44, T-45, T-47, T-49, T-50.

**The `NOT-FIXED` category, measured:** **0 as a whole-finding verdict** — no finding in this batch
was written back `applied` with nothing written, against 10 of 45 last run. Two findings (T-40,
T-46) have a NOT-FIXED **half**. This is the sharpest single improvement in the series, and its
caveat is stated in the Meta-observation: the gate credited with it was not executed as written.

---

## Summary

| # | Dimension | Category | Agent | Status | Finding |
|---|-----------|----------|-------|--------|---------|
| A | Dual placement | Structural | 1 | PASS | 15 module skills, 1 runtime skill, md5-identical |
| C | Instruction style | Quality | 5 | PASS | 1 buried mandate; the rest is the sanctioned tail idiom |
| E | Version consistency | Currency | 5 | PASS | v2.20.0 canonical; 0 lineage fields rewritten |
| D4 | Skills README vs disk | Currency | 1 | PASS | 15/15; every count and the 7/8 split verified |
| D5 | Bootstrap file refs | References | 2 | PASS | 77/77 resolve across both commands |
| D6 | Gap vocabulary | References | 3 | PASS | 10/10 gaps; install BLOCKS diffed; 3 of 3 declarers |
| D7 | Pushy Description | Quality | 3 | **FAIL** | Content 30/30; the check fails 3 of 7 new break attempts |
| D8 | Orchestrator refs | Process | 4 | PASS | All names and 25 §citations resolve, 0 RED |
| D9 | CLAUDE.md diagram | Currency | 1 | PASS | Every entry and count resolves |
| D10 | Command → skill paths | References | 4 | **FAIL** | 6 self-checks do not match their stated expectation |
| D11 | Bootstrap sequencing | Process | 2 | **FAIL** | One unacknowledged forward reference in EPA |
| D12 | Examples conventions | Currency | 5 | PASS | 0 violations; all 8 full-set truth-tests hold |
| D13 | PRD ↔ bootstrap compat | Process | 2 | PASS | 0 sections read-but-undefined |
| D14 | Bootstrap output | Process | 2 | PASS | 13/13 template refs traced |
| D15 | Doc factual accuracy | Currency | 5 | PASS | 0 inaccuracies; banned "inline" sense has 0 survivors |
| D16 | Project-info isolation | Privacy | 1 | **PASS** | All surfaces clean; 2 standing accepted-risk observations |
| D17 | Process coverage (meta) | Meta | 6 | **FINDINGS** | 3 undocumented flows; convergence clause fired |

---

## Findings ledger

| ID | Severity | Location | Finding | Status |
|----|----------|----------|---------|--------|
| U-1 | HIGH | `maintenance.md:478-479` | **`applied-proof` does not say which line numbering to use, and reads RED on 23 of 38 findings of a healthy batch.** `git log -L` resolves against the range TIP; ledger `Location` cells are PRE-fix numbers. Four of the 23 were independently confirmed FIXED. The K-8/L-7 inversion, fourth instance, in the gate installed to close that class. | applied `s0738f28` |
| U-2 | HIGH | `audit-2026-09-09.md:336-354` | **`applied-proof` was discharged at the FILE level, not the mandated LINE level, by a `$` line that cannot produce its own number.** The pasted command is a file union returning 11 paths; the claim is "49 of 49 IDs". It is additionally false for T-2, T-3 and T-4, whose commit touches only the report. The headline gate of the batch, violating the rule the same batch wrote. | applied `s0738f28` + `sb9d2ee2` |
| U-3 | HIGH | `maintenance.md:855-863` | **The append-only gate cannot detect deletion of an ENTIRE persisted report**, in two independent layers: the shell expands `audit-*.md` before git sees it, so a deleted filename is never passed; and `grep -c` on a missing file yields empty, so `[ "" -lt "13" ]` errors instead of firing. Proved in a clone. This is strictly worse than T-1 — `ae71374` at least left the file. | applied `s0738f28` |
| U-4 | HIGH | `bootstrap.md:656`, `EPA:1028` | **T-9's description-only anchor is defeated by the CANONICAL Claude Code frontmatter.** The range terminator `^[a-z_]*: ` cannot match hyphenated keys (`allowed-tools:`) and the closing `---` does not stop it, so the range runs to EOF and the false-BREAK returns — for exactly the population component-design §1 mandates a "When spawned" section on. Shipped templates escape only by accident of key order. | applied `s995c388` |
| U-5 | HIGH | `bootstrap.md:825-826`, `EPA:1119-1120` | **T-13's report slot was never touched on either twin** — only the mandate was. Both still offer a two-count form the loop cannot emit, ~140 lines below a paragraph asserting the repair. Found independently by two agents. `git log -L` over the batch → empty. | applied `s995c388` |
| U-6 | HIGH | `maintenance.md:612-613`, `audit-2026-09-09.md:465-472` | **The control-back-sweep denominator is 55, not 52; the pasted stdout was never produced by the quoted command; and the in-file calibration is wrong.** `audit.md` returns 7 fenced + 26 inline = 33. The `4` was true only at `cf52379~1` — the denominator certifying the batch was measured against the tree BEFORE the batch. Because the RED condition anchors on the wrong figure, deleting up to 3 controls now reads GREEN. | applied `s0738f28` |
| U-7 | HIGH | `audit-2026-09-09.md:398-410` | **`references: 9 of 9` was discharged with the mandated command silently narrowed** — the `$` line deletes the first regex alternative. Run verbatim it returns 41 (receipt's range) / 47 (batch), and one of the nine printed targets appears nowhere in the diff. Editing the command to fit the number is what T-24, installed in the same batch, forbids. Third consecutive occurrence of this class. | applied `s5422f9a` |
| U-8 | MEDIUM | `maintenance.md:857` | **The append-only gate sees 105 of 531 report lines (20%).** The Meta-observation, all Recommended Fixes, both accepted-risk records, all 23 receipt keys with their stdout, and both closing status lines delete GREEN; `###` headings are invisible to `^#{1,2} `. The receipts are the surface D17.5's only external verifier reads. | applied `s0738f28` |
| U-9 | MEDIUM | `bootstrap.md:657`, `EPA:1029` | **`&` is still outside the domain char class, and the fall-through branch MISREPORTS.** A specialist declaring a gap nobody declares is announced as `INFO: … no gap phrase` with `0 broken`; the only signal is `verified` dropping by one. R-10's symptom through a third door, aggravated by an INFO string that asserts a fact the file contradicts. | applied `s995c388` |
| U-10 | MEDIUM | `audit-2026-09-09.md:451` | **`back-sweep: 4 artifacts fixed of 4 found` against rows summing to 18.** The rule count from the same sentence's first clause was silently reused as the artifact count. R-36 (26 vs 22) → T-20 (19 vs 39) → third instance, in the batch applying T-20. | applied `s5422f9a` |
| U-11 | MEDIUM | `maintenance.md:201`, `audit-2026-09-09.md:459-461` | **The `$`-line quality grep fires on the healthy state.** `\.\.\.` is unanchored, so pasted `## main...origin/main` stdout matches: returns 5, expected 0. The receipt discharged an unadvertised variant returning 1, and the mandated form finds 3 genuine survivors plus 2 false ones. | applied `s0738f28` |
| U-12 | MEDIUM | `audit.md:1062`, `:119`, `:1065-1066` | **T-51's fifth verdict was added to the agent mandate and not swept to the report template.** The Part 1 row still lists four values; both score-line forms offer three not-clean categories with no `not-fixed` slot. A verdict with no field will not be reported. | applied `s0738f28` |
| U-13 | MEDIUM | `bootstrap.md:892-894`, `EPA:1138-1140` | **Gate 4 RED: `activation chains:` was added as a report key by this batch with no slot in either twin's final-report block.** Both carry only `← activation chain verified`. | applied `s995c388` |
| U-14 | MEDIUM | `audit-2026-09-09.md:320-334` | **Two commits of a seven-commit batch appear in no receipts table**, and the group table self-certifies `Rows = commits (4 = 4)`. `a6349da` changed 17 lines of normative text in a command file — a FIX commit covered by neither item 7 exemption — and is named only in prose. Item 6: "A commit that appears in NO receipts table is the violation." | applied `s5422f9a` |
| U-15 | MEDIUM | `maintenance.md:428-433` | **The group-table row-count check has no red state:** `rows N = commits N` derives BOTH sides from the table, and no command derives the commit count from git. Discharged `4 = 4` over a 7-commit batch. | applied `s0738f28` |
| U-16 | MEDIUM | `audit.md:1156` | **Evaluating the prior run's stated prediction has no owner.** The Meta-observation section MANDATES stating a prediction "so it is checkable next run", and nothing instructs the next run to check it — `grep -n "prediction"` returns exactly 1 hit, the line creating the obligation. Executed three times from dispatch prompts. component-design §9. | applied `s0738f28` |
| U-17 | MEDIUM | `EPA:363-365` | **The T-38 fix introduced two grammar defects.** `:363` ends `**NEVER assume the file exists** —` with its predicate deleted — the clause the finding named. `:365` opens mid-sentence because the bridging subject was deleted with the old list. Three sibling sites carry the intact form. | applied `s995c388` |
| U-18 | MEDIUM | `EPA:609-611` | **Step 2.9b (Phase 2) consumes the retroactive PRD, which Phase 3 creates afterwards.** On the common path — an existing project with no `Risk profile:` line, exactly what EPA targets — the fallback source does not yet exist, and the risk profile gates every tier-gated copy in the same step. Every other Phase-2→later dependency in this file is explicitly flagged; this one is silent. | applied `s995c388` |
| U-19 | MEDIUM | a lineage doc, 1 site | **T-34's class survives 35 lines above T-34's own fix**, in the same file: a duplicated clause produced by a genericisation substitution, escaping the doubled-word grep because a comma separates the copies. Untouched by the batch that repaired three instances of this exact class in this exact file. | applied `s347f2fa` |
| U-20 | MEDIUM | `CLAUDE.md:110-113` | **The T-28 replacement bullet carries 5 `**` markers — odd, therefore necessarily unbalanced.** The opener before `ALWAYS run D16` is unmatched and renders literally. The R-39/T-37/T-39 class, committed by the batch that was fixing T-37 and T-39. | applied `s347f2fa` |
| U-21 | MEDIUM | `audit-2026-09-04.md:679` | **T-18's third surface was never corrected** — still `20 keys, 11 harvestable` against a measured 10. Errata 3, added to that same file by the same batch, covers only T-19…T-25. One measurement, still two values on disk. | applied `s5422f9a` |
| U-22 | LOW | a lineage doc, 2 sites | **T-33's line-count half was swept inconsistently** — one exact source-project file metric genericised, two others of the same class left in the same file, in the same commit. | applied `s347f2fa` |
| U-23 | LOW | `audit-2026-09-09.md:159` + `cf52379` message | **The T-35 receipt states a count its finding contradicts:** ledger says `5 sites`, finding says "four plus a fifth", commit message says `4`. Unverifiable by diff because T-35 was applied outside the repo — which makes the receipt the only record. | applied `s5422f9a` |
| U-24 | LOW | `EPA:923-926` | **The block mandating "word-for-word equivalent" twins is itself divergent from its twin in four places** — item 2's punctuation, the lead-in, the closer, and a bootstrap-only paragraph with no EPA counterpart. Q-34's class: the table and loop are byte-identical, the prose drifted again. | applied `s995c388` |
| U-25 | LOW | `bootstrap.md:579` | **Stale step attribution:** the step reads all three declaring components but closes `(created in Steps 7-8)`; `validator.md` is created in Step 10. The parenthetical was never updated when the validator became the third declarer. | applied `s995c388` |
| U-26 | LOW | `docs/agentic_engineering_framework.md:50`, `:336` | **The `Setup` row T-47 added to the diagram is absent from the two prose claims that enumerate the pipeline.** Both still read "15 primary steps … plus Step 0.5 … and sub-steps", which accounts for the diagram's rows only if the reader silently drops one. | applied `s995c388` |
| U-27 | LOW | `EPA:305` vs `:364` | **The derive-never-type rule is honoured at one site and violated two steps earlier**, under an identical mandate. The typed list is currently correct, so this is a consistency defect — but it is the same list-typing that has been wrong five times on the sibling. | applied `s995c388` |
| U-28 | LOW | `bootstrap.md:142-143` | **Step 1.2c's risk-profile handoff has no receiver mandate**, unlike Step 1.1's three. It survives only because both templates ship the line as a placeholder. | applied `s995c388` |
| U-29 | LOW | `audit.md:579` | **`51f395c` added a two-count mandate that contradicts the three-count mandate it added in the same commit**, and both mandated keys say `activation chain check:` while the loop prints `activation chains:`. | applied `s995c388` |
| U-30 | LOW | `EPA` loop header | **The 11-line rationale comment explaining why each regex is shaped as it is exists only on the bootstrap side.** An editor maintaining the EPA copy has no record of why the class admits digits and dots. R-32's one-sided-paragraph shape, one level down. | applied `s995c388` |
| U-31 | LOW | `audit.md:575-577` | **T-43's "outside the repo" half never landed** — still prompt-only, which is T-43's own class. | applied `s0738f28` |
| U-32 | LOW | `audit-2026-09-09.md:71`, `:124` | **Two `$`-line outputs are parentheticals standing in for real output** — `fences:` shows `(every value > 0…)` where the command prints 11 counts; the negation proof's third block narrates a number with no `$` line. The form T-24 bans verbatim. | applied `s5422f9a` |
| U-33 | LOW | `maintenance.md:225-226` | **The harvester-reach figure went stale in the batch that changed it:** the text says `10 of the 20 registered keys`; re-run it is `13` of `23`, because the batch registered three new keys. ADJACENT, three lines above the loop it describes. | applied `s0738f28` |
| U-34 | LOW | `CLAUDE.md:151` | **A directional citation pointing the wrong way, introduced by the T-41 fix:** "runs on trigger (d) **below**" — trigger (d) is three lines above. The sentence is also fully redundant with the lines above it. | applied `s347f2fa` |
| U-35 | LOW | `audit.md:1236`, `:1238` | **T-40's second half never landed** — the defect-series command still carries no stated expected result, so it can be evaluated neither red nor green. `git log -L` over the batch → empty. | applied `s0738f28` |
| U-36 | LOW | `audit.md` D17.4 | **T-46's first half never landed** — D17.4's division of labour with Agent 4 was never written; `git log -L 975,990` over the batch → empty. D17.6's half did land. | applied `s0738f28` |
| U-37 | LOW | `maintenance.md:434-435` | **Splice residue from the T-26 fix:** the GROUP bullet ends mid-sentence and the pre-fix flat paragraph resumes at column 0, carrying the old form-agnostic rule inside the bullet that exists to say one rule cannot govern both forms. | applied `s0738f28` |
| U-38 | LOW | `audit-2026-09-09.md:489` | **The `placeholder:` check self-matches:** discharged at 0, it returns 1 because the receipt line quoting the pattern is itself the match — R-16's literal defect. It would not see the `sPENDING` two lines above either. | applied `s5422f9a` |
| U-39 | LOW | `audit-2026-09-09.md:419-421` | **The `instruction style:` prose is wrong for 3 of 4 flagged lines** — only one is the sanctioned tail idiom; the others are mandates appended after a rationale or citation clause. R-17's sub-class. | applied `s5422f9a` |
| U-40 | LOW | `session-end/SKILL.md:96` | **A behavioral mandate with its own output fields buried at the tail of a four-sentence bullet whose subject is a different check.** component-design §6 property 2. Named as residue by Run 10's dimension C and never filed. | applied `s347f2fa` |
| U-41 | LOW | `audit-2026-09-09.md:481-486` | **The `push:` receipt states a claim now false on disk** ("the batch is unpushed"), and `[ahead 5]` was never re-measured after the last two commits — so two of seven sit outside the range the D16 GREEN covers. The receipts header also declares four hashes for a seven-commit batch. | applied `s5422f9a` |
| U-42 | LOW | `audit-2026-09-09.md:448` | **The `version:` receipt enumerates 3 of 4 version-stamped surfaces.** `audit.md:239` carries a `v2.20.0` stamp, correctly bumped and uncounted — the inventory-completeness failure `inventory sweep:` exists to catch. | applied `s5422f9a` |
| U-43 | LOW | `maintenance.md:875-876` | **`a6349da` changed the gate's unit from raw lines to structure and did not sweep the report key it feeds** — the mandated value form still reads `N lines deleted from M report files`, a line count the structural gate no longer produces. DESCENDING, missed inside the correction commit. | applied `s0738f28` |
| U-44 | LOW | `maintenance.md` Audit intake | **Restoring a destroyed persisted report has no maintenance-side instruction.** T-45's fix tells the AUDIT to reconstruct for reading; nothing tells the APPLYING session to restore, keep the intervening receipts, and record it as an erratum — all of which was done correctly and unprompted, which is why it should be written down. | applied `s0738f28` |
| U-45 | LOW | `audit.md:263`, `:998`, `:73-74` | **The re-derived control enumerator still counts prose** — T-17's own defect in a new shape. Of the 7 "fenced" hits in `audit.md`, three are a prose sentence, a template placeholder and T-45's recovery commands. T-17 removed the `grep`-prose shape and reintroduced it via `for `. | applied `s0738f28` |
| U-47 | MEDIUM | `audit.md:22` vs `:18-21` | **T-50 authorised the defect-series append but did not sweep the COMMIT clause that must persist it.** The authorized-operations list now permits appending one row to `.claude/commands/audit.md`, while the commit bullet three lines below still reads "`git add` + `git commit` of **THAT ONE FILE**" — the report. Taken literally the appended row would live only in the working tree, which is the exact failure Phase 3 item 7 exists to prevent. Found by executing Phase 3 in this run; both files were committed and the divergence is recorded here rather than resolved silently. ADJACENT direction, in the fix that created the operation. | applied `s0738f28` |
| U-46 | INFO | `bootstrap.md:637`, `EPA:1009` | A legitimately indented blockquote declaration (a `>` inside a list item) is not harvested. Recorded as INFO, not a defect: it fails LOUD, unlike `U-9`. | applied `s995c388` |

**Carried forward from `audit-2026-09-09.md`:** none — Run 10 closed at 51 of 51 disposed.

---

## Recommended Fixes

**1. Quick fixes** — `U-20`, `U-34`, `U-19`, `U-22`, `U-23`, `U-25`, `U-26`, `U-29`, `U-33`,
`U-37`, `U-39`, `U-40`, `U-42`, `U-43`.

**2. Structural fixes** — `U-3` and `U-4` FIRST: both are silent-failure modes in gates that
currently read GREEN. Then `U-5`, `U-9`, `U-13`, `U-17`, `U-18`, `U-12`, `U-24`, `U-30`.

**3. Quality improvements** — `U-1`, `U-2`, `U-6`, `U-7`, `U-8`, `U-10`, `U-11`, `U-14`, `U-15`,
`U-16`, `U-21`, `U-27`, `U-28`, `U-31`, `U-32`, `U-35`, `U-36`, `U-38`, `U-41`, `U-44`, `U-45`.

**4. ESCALATED — owner decision required** — **none this run.** Both prior escalations (T-11, T-36)
are closed by record and re-measured clean. No new privacy hit reached the working tree.

**5. ACCEPTED-RISK — no action** — E-1, M-44, N-53, T-11, T-36, and the two schema names under the
industry-names exemption. All re-measured; all standing.

---

## Meta-observation

**The recurring class this run is narrow and specific: a control's TEXT is now reliably correct
and its FIRST DISCHARGE is not.** Of the 24 not-clean findings, fourteen carry
INTRODUCED-A-DEFECT, and the overwhelming majority of those are receipts whose numbers do not
reproduce — `references: 9` against 41, `control back-sweep: 52` against 55, `back-sweep: 4`
against rows summing to 18, `placeholder: 0` against a self-match, `applied-proof: 49 of 49` from a
command returning 11 filenames. The rules those receipts discharge are, without exception, correctly
written. **The defect has migrated from the rule to the act of running it**, and it has a single
signature: a number produced by reading, then a command pasted beneath it that was never executed
against the state being certified. Two were measured against the tree *before* the batch.

**Run 10's prediction, evaluated clause by clause.** Both defect-rate clauses **held**: not-clean
fell 84% → **47%**, and `NOT-FIXED` as a whole-finding verdict went **10 of 45 → 0 of 51**. The
convergence clause also **fired, harder than in any prior run**: 1,336 apparatus insertions against
**2** shipped, where the stated threshold was "500+ apparatus and fewer than 10 shipped". All three
readings are true and they are not in tension.

**But the zero is not evidence for the gate that was credited with it.** `applied-proof` was
discharged at the file level by a command that cannot produce its own number (`U-2`), and run at the
unit the rule mandates it reads RED on 23 of 38 healthy findings (`U-1`). The category is 0 partly
because it was measured at the wrong unit. What *did* demonstrably work is smaller and realer: the
`$`-line assertion moved receipt compliance from **5/20 to 23/23**, and the append-only gate is the
first control in this series that a batch **caught failing and corrected before the audit did** —
`a6349da` exists because its author ran the check and saw it fire on a healthy commit.

**The convergence table, extended and re-measured:**

**The row is the BATCH; the rate is the audit that verified it.**

| Batch | Shipped (`docs/modules` + `examples`) | Apparatus (`.claude` + `assets/docs` + `CLAUDE.md`) | Not clean, per the run that verified it |
|---|---|---|---|
| Run 5 | 62 | 502 | 45% |
| Run 6 | 38 | 1,145 | — |
| Run 7 | 23 | 561 | 49% |
| Run 8 | 12 | 632 | 58% |
| Run 9 | 4 | 987 | 60% → then 84% (Run 10) |
| **Run 10** | **4** (2 ins + 2 del) | **1,529** (1,336 + 193) | **47%** (this run) |

Six batches: **143 lines onto the surface bootstrapped projects receive, 5,632 onto the machinery
that audits the machinery — 97.5% / 2.5%.** `maintenance.md` and `audit.md` together went
1,360 → 2,253 lines in nine days. Run 10's apparatus is the largest of the series — **931 even
after excluding the entire 598-line T-1 restoration**, which was unavoidable repair work.

**The judgement, stated plainly: the controls are now adequate and should be left alone.** The
defect rate halved, the worst category went to zero, and one control demonstrated self-correction —
that is what a working control set looks like. What has not changed, and got worse, is **aim**. By
the rule Run 10 wrote for itself, the conclusion is not another remediation batch: it is to **stop
tuning this loop and re-point it at the shipped surface**. The forty-six findings above are real,
and all but a handful describe the audit apparatus auditing itself.

**Prediction, stated so it can fail.** If a Run 12 remediation batch is run, the not-clean rate will
land between 40% and 55% and the dominant class will again be receipts whose numbers do not
reproduce — because the rules are right and the execution is what drifts. **The single change that
would falsify this is not a new control but a different actor: have the receipts computed by
something that is not the author, or generated mechanically from the diff rather than written.**
If instead the next session is spent on `docs/modules/` and `examples/`, this prediction is void and
that is the better outcome.

**Suggestion:** Run `/maintenance` to apply fixes, using this report as the correction plan — but
read the Meta-observation first, because this report's own recommendation is that the highest-value
next session is not this one.

**Report status:** `COMPLETE` — all 6 agents returned; all 17 dimensions evaluated; 51 of 51 findings verdicted.
**defect series:** row 11 appended to the one table; no other surface touched.
    $ grep -c '^| [0-9]* | `audit-' .claude/commands/audit.md
    11
**Application status:** `PENDING`
